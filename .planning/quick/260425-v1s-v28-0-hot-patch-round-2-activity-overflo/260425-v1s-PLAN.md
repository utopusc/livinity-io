---
mode: quick
type: hot-patch
phase: 260425-v1s
plan: 01
wave: 1
depends_on: []
autonomous: true
files_modified:
  - livos/packages/ui/public/figma-exports/docker-app-icon.png
  - livos/packages/ui/src/routes/docker/activity/activity-section.tsx
  - livos/packages/ui/src/modules/desktop/desktop-content.tsx
  - livos/packages/ui/src/routes/server-control/index.tsx
  - livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  - livos/packages/ui/src/routes/docker/status-bar.tsx
  - livos/packages/ui/src/routes/docker/status-footer.tsx
  - livos/packages/ui/src/routes/docker/docker-app.tsx
  - livos/packages/ui/src/routes/docker/use-trpc-connection.ts
  - livos/packages/ui/src/routes/docker/shell/shell-section.tsx
  - livos/packages/ui/src/routes/docker/resources/image-section.tsx
  - livos/packages/ui/src/routes/docker/resources/image-history-panel.tsx
  - livos/packages/ui/src/routes/docker/resources/scan-result-panel.tsx
  - livos/packages/ui/src/routes/docker/resources/volume-section.tsx
  - livos/packages/ui/src/routes/docker/resources/volume-usage-panel.tsx
must_haves:
  truths:
    - "Activity tab Recent events list scrolls inside the section, never overflows DockerApp window"
    - "Desktop Docker tile uses /figma-exports/docker-app-icon.png; dock + spotlight + mobile-tab still use legacy dock-server.svg"
    - "Server Management page shows ONLY Overview / PM2 / Monitoring / Domains tabs (no Containers/Images/Volumes/Networks/Stacks/Events)"
    - "Settings sidebar no longer lists Environments entry; Docker app's own Settings tab still shows EnvironmentsSection"
    - "Docker app top StatusBar has only [EnvironmentSelector] + [SearchButton + AlertsBell]; all stat pills move to a sticky bottom StatusFooter"
    - "Docker WS connection indicator either reflects real connection state OR is removed if Docker uses HTTP-only tRPC routes"
    - "Shell empty state ('Click a container in the sidebar to open a shell session.') text + icon are white"
    - "Images Actions column stays visible (sticky right-0); Repository:Tag truncates with tooltip; table never overflows window"
    - "Image detail panels (Layer history, Vulnerabilities) use light theme — no dark backgrounds, no inverted text"
    - "Volumes Actions column stays visible (sticky right-0); Mount Point + long IDs truncate with tooltip; table never overflows window"
    - "All 4 services (livos liv-core liv-worker liv-memory) active after Mini PC redeploy"
  artifacts:
    - path: "livos/packages/ui/public/figma-exports/docker-app-icon.png"
      provides: "Docker whale icon for desktop tile only"
    - path: "livos/packages/ui/src/routes/docker/status-footer.tsx"
      provides: "Sticky bottom stat pill bar for DockerApp"
      contains: "StatusFooter"
  key_links:
    - from: "livos/packages/ui/src/routes/docker/docker-app.tsx"
      to: "livos/packages/ui/src/routes/docker/status-footer.tsx"
      via: "<StatusFooter /> rendered as last child of <main>"
      pattern: "StatusFooter"
    - from: "livos/packages/ui/src/modules/desktop/desktop-content.tsx"
      to: "/figma-exports/docker-app-icon.png"
      via: "AppIcon src literal in dockerItem"
      pattern: "/figma-exports/docker-app-icon.png"
---

<objective>
Round 2 hot-patch bundle for v28.0 Docker Management UI — 10 sıralı UI/yapısal fix
covering Activity overflow, desktop icon swap, Server Management Docker-strip,
Settings menu cleanup, StatusBar split (top/bottom), WS indicator audit, Shell
empty-state contrast, and Images/Volumes table overflow + light-theme regressions.

Purpose: Round 1 (260425-sfg) closed sidebar + container-form regressions; Round 2
closes the next 10 user-reported issues from a hands-on session at
https://bruce.livinity.io. Each fix has a known target file and known change pattern
(user did the root-cause investigation upfront).

Output: 10 atomic git commits + 1 deploy/verify pass + a SUMMARY.md.
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260425-sfg-v28-0-hot-patch-bundle-tailwind-sync-bg-/260425-sfg-SUMMARY.md
@livos/packages/ui/src/routes/docker/docker-app.tsx
@livos/packages/ui/src/routes/docker/status-bar.tsx
@livos/packages/ui/src/routes/docker/use-trpc-connection.ts

<interfaces>
<!-- Pre-flight verified — these files exist and these are the relevant exports/anchors.
     Executor should NOT re-explore the tree; use these directly. -->

Section file routing (ALL `sections/*.tsx` are 1-line re-export shims):
- sections/activity.tsx → re-exports `Activity` from `../activity/activity-section`
- sections/images.tsx   → re-exports `Images`   from `../resources/image-section`
- sections/volumes.tsx  → re-exports `Volumes`  from `../resources/volume-section`
- sections/shell.tsx    → re-exports `Shell`    from `../shell/shell-section`
EDIT THE TARGET FILE, NOT THE SHIM.

Desktop Docker tile (Fix 2):
- File: livos/packages/ui/src/modules/desktop/desktop-content.tsx
- `dockerItem` block at lines 295–311
- Currently: `src={systemAppsKeyed['LIVINITY_docker'].icon}` on AppIcon (line 305)
- Change ONLY this AppIcon's src to `'/figma-exports/docker-app-icon.png'` literal
- Line 261 (initial system items array) and the `onClick={launchWindowApp(...)}` arg on line 306
  also reference the same icon — DO NOT touch those (the dock/spotlight/mobile keep dock-server.svg
  per task spec). Only the desktop tile's AppIcon visual src changes.

Settings menu (Fix 4):
- File: livos/packages/ui/src/routes/settings/_components/settings-content.tsx
- Type union member `| 'environments'` at line 142
- MENU_ITEMS entry at line 172
- switch case at lines 459-460
- `EnvironmentsSectionLazy` lazy import at lines 1409-1411
- Remove all 4 anchors. Docker app's `routes/docker/sections/settings.tsx` is unaffected.

Status bar (Fix 5):
- status-bar.tsx (143 lines) — currently renders EnvironmentSelector + 8 stat pills + SearchButton + AlertsBell
- docker-app.tsx (87 lines) — `<main>` is `flex min-w-0 flex-1 flex-col overflow-hidden`,
  contains `<StatusBar />` then `<div className='min-h-0 flex-1 overflow-auto'><SectionView/></div>`
- StatusBar header is `sticky top-0 z-10 flex h-12 ...`
- After split: header keeps EnvSelector + SearchButton + AlertsBell only.
  New status-footer.tsx mounts after the section div as `sticky bottom-0 z-10 flex h-12 ...`.

WS indicator (Fix 6):
- use-trpc-connection.ts polls `wsClient.getConnection()?.readyState === OPEN` every 1s
- LivePill in status-bar.tsx renders Live/Offline based on `connected` boolean
- Decision tree per task spec — investigate first, then patch

Shell empty state (Fix 7):
- File: livos/packages/ui/src/routes/docker/shell/shell-section.tsx
- Lines 108-113:
  ```
  <div className='flex h-full flex-col items-center justify-center gap-2 text-center'>
    <IconTerminal2 size={32} className='text-text-tertiary' />
    <p className='text-sm text-text-secondary'>
      Click a container in the sidebar to open a shell session.
    </p>
  </div>
  ```
- Change icon className `text-text-tertiary` → `text-white`
- Change `<p>` className `text-sm text-text-secondary` → `text-sm text-white`

Images table (Fix 8 + 9):
- File: livos/packages/ui/src/routes/docker/resources/image-section.tsx (360 lines)
- Detail panels live in resources/image-history-panel.tsx (84 lines) and resources/scan-result-panel.tsx (275 lines)
- Apply `table-fixed`, Repository:Tag truncate + Tooltip, sticky right Actions column

Volumes table (Fix 10):
- File: livos/packages/ui/src/routes/docker/resources/volume-section.tsx (269 lines)
- Detail panel at resources/volume-usage-panel.tsx
- Same pattern as Images
</interfaces>

<environment>
**Repo root:** C:/Users/hello/Desktop/Projects/contabo/livinity-io
**UI package:** livos/packages/ui
**Docker logo source:** C:/Users/hello/Downloads/docker.png (160KB, exists)
**Mini PC SSH:**
  ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc \
      -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      bruce@10.69.31.68 'CMD'  (sudo NOPASS)
**Deploy:** sudo bash /opt/livos/update.sh  (on Mini PC)
**Browser:** Chrome DevTools MCP open at https://bruce.livinity.io
**Commit trailer:**
  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
</environment>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Activity Recent events overflow fix</name>
  <files>livos/packages/ui/src/routes/docker/activity/activity-section.tsx</files>
  <action>
    Open activity-section.tsx (186 lines). Recent events list currently overflows the
    DockerApp window when many events accumulate.

    Steps:
    1. Read the full file to map structure: outer wrapper, filter/header bar, events list container.
    2. The outer wrapper MUST be `flex h-full min-h-0 flex-col` (or equivalent) so it can host
       a scrolling child without pushing past the DockerApp `<div className='min-h-0 flex-1 overflow-auto'>`
       in docker-app.tsx (line 80).
    3. Section header (filters + counter) stays at the top, NOT scrolling — wrap it (or apply to it)
       `shrink-0`. If it should be sticky relative to the scroll container, also add `sticky top-0 z-10
       bg-white` (light theme — Docker app is locked light).
    4. Events list container (the parent that maps over events) gets `min-h-0 flex-1 overflow-y-auto`.
       File already has one `overflow-y-auto` at line 130 — verify it is on the correct (events list)
       container, NOT a sibling. If wrong target, move it.

    Anti-regression: do NOT touch event-row markup, event-mappers, or use-activity-feed hook.
    Light-theme only — Docker app dark-class is no longer mounted (per docker-app.tsx hot-patch comment).
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -20</automated>
    Manual (post-deploy via Chrome DevTools MCP at bruce.livinity.io):
    1. Open Docker app → Activity tab
    2. evaluate_script: check `.activity-section` (or its `<section>`) computed `overflow-y === 'auto'`
       on the events list container
    3. Visual: list scrolls inside the docker window; filter bar stays put; nothing pushes below dock
  </verify>
  <done>
    - File compiles, full UI build green
    - Events list scrolls within section bounds
    - Filter/header row visible and stationary at top during scroll
    - No new console errors / no layout shift outside DockerApp
    - Atomic commit: `fix(ui): contain Activity Recent events list scroll within docker window`
  </done>
</task>

<task type="auto">
  <name>Task 2 — Desktop Docker tile uses whale icon (others unchanged)</name>
  <files>
    livos/packages/ui/public/figma-exports/docker-app-icon.png
    livos/packages/ui/src/modules/desktop/desktop-content.tsx
  </files>
  <action>
    Step 1 — Copy asset:
      cp "C:/Users/hello/Downloads/docker.png" \
         "C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/ui/public/figma-exports/docker-app-icon.png"
      Verify with: ls -la livos/packages/ui/public/figma-exports/docker-app-icon.png  (should be ~160KB)

    Step 2 — Patch desktop-content.tsx:
      In the `dockerItem` definition (lines 295–311), change ONLY the `<AppIcon src={...}>` value:
        - Before: `src={systemAppsKeyed['LIVINITY_docker'].icon}`
        - After:  `src='/figma-exports/docker-app-icon.png'`
      That is line 305 only.

    DO NOT touch:
    - Line 261 (initial system items array) — keeps `systemAppsKeyed['LIVINITY_docker'].icon`
      so the dock + spotlight + mobile-tab-bar continue using dock-server.svg
    - Line 306 (`launchWindowApp(..., systemAppsKeyed['LIVINITY_docker'].icon)`) — the launched
      window's titlebar icon argument; keep as-is so window UI stays consistent
    - dock.tsx, apple-spotlight.tsx, mobile-tab-bar.tsx, apps.tsx — out of scope per task spec

    Tradeoff: desktop tile shows the whale; dock + spotlight + mobile + launched-window icon stay on
    dock-server.svg. Visual mismatch is intentional per user direction.
  </action>
  <verify>
    <automated>ls -la livos/packages/ui/public/figma-exports/docker-app-icon.png &amp;&amp; grep -n "docker-app-icon" livos/packages/ui/src/modules/desktop/desktop-content.tsx &amp;&amp; cd livos &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -10</automated>
    Manual (post-deploy):
    1. Hard reload bruce.livinity.io desktop
    2. Desktop Docker tile shows whale; dock Docker icon (if present) still shows old icon
  </verify>
  <done>
    - PNG file exists at public/figma-exports/docker-app-icon.png (~160KB)
    - desktop-content.tsx line 305 uses literal path; no other lines changed
    - Build green
    - Atomic commit: `fix(ui): use docker whale icon for desktop tile only (dock/spotlight unchanged)`
  </done>
</task>

<task type="auto">
  <name>Task 3 — Strip Docker management tabs from Server Management</name>
  <files>livos/packages/ui/src/routes/server-control/index.tsx</files>
  <action>
    File is 4793 lines and currently hosts both Server Control AND a Docker management surface
    (containers/images/volumes/networks/stacks/events).

    Step 1 — Map current tabs:
      grep -n "TabsTrigger\|TabsContent" livos/packages/ui/src/routes/server-control/index.tsx
      Identify each tab's `value=` and the function/sub-tree it gates.

    Step 2 — Remove these tabs (TabsTrigger + TabsContent + supporting state/handlers/imports):
      - Containers
      - Images
      - Volumes
      - Networks
      - Stacks
      - Events  (Docker events only — NOT Monitoring's events if any)

    Step 3 — Keep these tabs (no functional change):
      - Overview  (CPU/RAM/Disk/Temp + Docker Engine info card stays — read-only summary)
      - PM2
      - Monitoring
      - Domains

    Step 4 — Cleanup:
      - Remove imports from `../docker/resources/...` and `../docker/_components/...` no longer used
        (e.g. ContainerCreateForm, ContainerDetailSheet — check `import.*from '..\/docker\/'` lines).
      - Remove hooks no longer used: useContainers, useImages, useVolumes, useNetworks, useStacks,
        useDockerEvents (verify via search — only remove if NO remaining reference).
      - Remove component fns that ONLY served deleted tabs: EventsTab, ImagesTab, VolumesTab,
        ContainersTab, NetworksTab, StacksTab, PruneImagesDialog, ContainerCreateForm dialogs, etc.
      - Remove dead state/handlers tied to deleted tabs: selectedContainers, removeTarget, bulkManage,
        selectedImage/scan modals, etc. (search by usage, delete only if 0 remaining refs).
      - Domains tab stays — DomainsTab import is fine if still used.
      - Engine info card on Overview stays — uses useEngineInfo only, not container/image hooks.

    Step 5 — Validation loop:
      - Run TypeScript: `cd livos && pnpm --filter ui exec tsc --noEmit -p tsconfig.json 2>&1 | grep "error TS"`
      - Iterate: TS errors will surface every dangling reference; resolve until zero errors.
      - Run full build: `cd livos && pnpm --filter ui build`

    SAFETY:
    - Make multiple smaller edits (one tab at a time), not a single mass deletion.
    - After each tab removal, re-run the tsc check.
    - DO NOT remove TabsList/Tabs primitives entirely — Overview/PM2/Monitoring/Domains still need them.
    - If a dialog component (e.g. PruneImagesDialog) is exported, prefer DELETING the function definition
      AND its imports rather than just unmounting it.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -10 &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -10</automated>
    Post-deploy (Chrome DevTools MCP at bruce.livinity.io):
    1. Open Server Management
    2. Verify tabs visible: Overview, PM2, Monitoring, Domains (4 tabs total)
    3. Click each remaining tab — no console errors
    4. Verify Docker management tabs (Containers/Images/Volumes/Networks/Stacks/Events) are GONE
    5. Engine Info card still visible on Overview
  </verify>
  <done>
    - tsc reports zero errors
    - Build green
    - 4 tabs only on Server Management
    - No dangling imports from `../docker/`
    - Atomic commit: `refactor(server-control): remove Docker management tabs (now only in Docker app)`
  </done>
</task>

<task type="auto">
  <name>Task 4 — Remove Environments entry from Settings sidebar</name>
  <files>livos/packages/ui/src/routes/settings/_components/settings-content.tsx</files>
  <action>
    Surgical removal of 4 anchors (line numbers from pre-flight grep — verify before edit):

    1. Line 142 — Type union member: remove `| 'environments'` from SettingsSection union.
    2. Line 172 — MENU_ITEMS array: remove the entry
       `{id: 'environments', icon: TbBrandDocker, label: 'Environments', description: 'Multi-host Docker connections', adminOnly: true},`
    3. Lines 459-460 — switch statement: remove
       `case 'environments': return <Suspense ...><EnvironmentsSectionLazy /></Suspense>`
    4. Lines 1409-1411 — lazy import:
       `const EnvironmentsSectionLazy = React.lazy(() => import('./environments-section').then((m) => ({default: m.EnvironmentsSection})))`

    After removal, also check for and remove if newly-orphaned:
    - `import { TbBrandDocker } from '...'` — KEEP if still used elsewhere; remove if 0 refs.
      (Run: grep -n "TbBrandDocker" livos/packages/ui/src/routes/settings/_components/settings-content.tsx
       — if 0 occurrences after the entry deletion, also remove the import line.)
    - Suspense / Loader2 — almost certainly used by other lazy sections; do NOT touch.

    DO NOT touch:
    - livos/packages/ui/src/routes/settings/_components/environments-section.tsx (file stays — Docker app's
      Settings tab consumes it via `routes/docker/sections/settings/...`).
    - settings-content-mobile.tsx — separate file, separate menu (verify with grep; if it references
      'environments' independently, leave it alone unless user asks).
  </action>
  <verify>
    <automated>grep -n "environments\|Environments\|EnvironmentsSectionLazy" livos/packages/ui/src/routes/settings/_components/settings-content.tsx | grep -v "//\|#" &amp;&amp; cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -5</automated>
    Expectation: grep returns 0 matches for `EnvironmentsSectionLazy`; tsc clean.
    Post-deploy: Settings menu has no Environments item; Docker app → Settings tab still shows env list.
  </verify>
  <done>
    - 4 anchors removed
    - tsc clean
    - environments-section.tsx file untouched
    - Atomic commit: `fix(settings): remove standalone Environments menu entry (still in Docker app settings)`
  </done>
</task>

<task type="auto">
  <name>Task 5 — Split DockerApp StatusBar into top-minimal + sticky bottom StatusFooter</name>
  <files>
    livos/packages/ui/src/routes/docker/status-bar.tsx
    livos/packages/ui/src/routes/docker/status-footer.tsx
    livos/packages/ui/src/routes/docker/docker-app.tsx
  </files>
  <action>
    Step 1 — Strip status-bar.tsx (143 lines) down to top-only minimal:
      Keep imports for: EnvironmentSelector, SearchButton, AlertsBell, cn.
      Remove: useEngineInfo, useEnvironments, useNow, useTrpcConnection, trpcReact (uptime/disk),
              format helpers (formatDiskFree etc), Pill + LivePill helper components, all icon imports
              (IconBrandDocker, IconCircleFilled, IconClock, IconCpu, IconDatabase, IconDeviceSdCard).
      `StatusBar` JSX becomes:
        ```
        <header className={cn(
          'sticky top-0 z-10 flex h-12 shrink-0 items-center gap-3 border-b border-zinc-200 bg-white/95 px-3 backdrop-blur',
          'dark:border-zinc-800 dark:bg-zinc-900/95',
        )}>
          <EnvironmentSelector />
          <div className='flex flex-1' />
          <div className='flex shrink-0 items-center gap-1'>
            <SearchButton />
            <AlertsBell />
          </div>
        </header>
        ```

    Step 2 — Create new file livos/packages/ui/src/routes/docker/status-footer.tsx:
      Move ALL stat-pill rendering + data hooks (useEngineInfo, useEnvironments + useSelectedEnvironmentId,
      useNow, useTrpcConnection, trpcReact.system.uptime + systemDiskUsage, format helpers, Pill + LivePill
      components) into this new file.
      Container:
        ```
        export function StatusFooter() {
          // ... all hooks ...
          return (
            <footer className={cn(
              'sticky bottom-0 z-10 flex h-12 shrink-0 items-center gap-3 border-t border-zinc-200 bg-white/95 px-3 backdrop-blur',
              'dark:border-zinc-800 dark:bg-zinc-900/95',
            )}>
              <div className='flex min-w-0 flex-1 items-center gap-2 overflow-x-auto'>
                <Pill icon={IconBrandDocker}>{versionLabel}</Pill>
                <Pill>{socketLabel}</Pill>
                <Pill icon={IconCpu}>{coresLabel}</Pill>
                <Pill icon={IconDatabase}>{ramLabel}</Pill>
                <Pill icon={IconDeviceSdCard}>{diskLabel}</Pill>
                <Pill icon={IconClock}>{uptimeLabel}</Pill>
                <Pill>{timeLabel}</Pill>
                <LivePill connected={connected} />
              </div>
            </footer>
          )
        }
        ```
      (Note `border-t` instead of `border-b`, `bottom-0` instead of `top-0`. Same h-12, same backdrop.)

    Step 3 — Update docker-app.tsx (87 lines):
      ```
      import {StatusFooter} from './status-footer'
      // ...
      <main className='flex min-w-0 flex-1 flex-col overflow-hidden'>
        <StatusBar />
        <div className='min-h-0 flex-1 overflow-auto'>
          <SectionView section={section} />
        </div>
        <StatusFooter />
      </main>
      ```
      The `<div className='min-h-0 flex-1 overflow-auto'>` already absorbs all available height between
      sticky header (top) and sticky footer (bottom) — no other layout change needed.

    Visual consistency requirement (per task spec): both top header and bottom footer use the same
    border-zinc-200 + bg-white/95 backdrop classes.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -10</automated>
    Post-deploy (Chrome DevTools MCP):
    1. Open Docker app
    2. Top bar shows ONLY EnvSelector + Search + AlertsBell (no Pills)
    3. Bottom of pane shows sticky footer with all 8 pills (Docker version, Socket, cores, RAM, free, uptime, time, Live/Offline)
    4. Both bars stay visible on Activity scroll, Containers scroll, Logs scroll
    5. No layout overlap with section content
  </verify>
  <done>
    - status-bar.tsx slimmed to top-minimal
    - status-footer.tsx created with stat pills
    - docker-app.tsx renders StatusFooter as last main child
    - Build + tsc green
    - Atomic commit: `refactor(docker): split StatusBar into minimal top header + sticky StatusFooter`
  </done>
</task>

<task type="auto">
  <name>Task 6 — Investigate + fix Docker WS "Offline" indicator</name>
  <files>
    livos/packages/ui/src/routes/docker/use-trpc-connection.ts
    livos/packages/ui/src/routes/docker/status-footer.tsx
  </files>
  <action>
    Diagnostic phase (REQUIRED before touching code):

    1. Re-read use-trpc-connection.ts (43 lines, already loaded). Hook polls
       `wsClient.getConnection()?.readyState === OPEN` every 1s. Check `livos/packages/ui/src/trpc/trpc.ts`
       (or `common.ts`) to understand:
       - Is `wsClient` ever instantiated/connected?
       - Does the Docker app's tRPC client even use WebSocket, or is it on `httpOnlyPaths`?
       - Per MEMORY.md: "tRPC transport: New multi-user routes added to `httpOnlyPaths` in `common.ts`"
         — verify whether docker.* routes are HTTP-only.

    2. Browser inspection (Chrome DevTools MCP at bruce.livinity.io):
       evaluate_script:
         () => ({
           wsKeys: Object.keys(window).filter(k => k.toLowerCase().includes('ws') || k.toLowerCase().includes('trpc')),
           trpcInternals: window.__TRPC_QUERY_CLIENT__ || null,
         })
       Plus open Network tab, filter by WS, see if a WebSocket actually opens for /trpc.

    3. Server-side check:
       ssh bruce@10.69.31.68 'sudo journalctl -u livos --since "10 min ago" | grep -i "ws\|websocket\|connection" | tail -30'

    Decision tree:

    **Case A — WS is actually used and broken (reconnect issue, wrong path):**
    Fix the underlying WS client setup OR the polling logic. Most likely: `getConnection()` returns
    undefined because `wsClient` was never created (HTTP-only setup) — see Case C.

    **Case B — WS works but indicator logic wrong:**
    Verify `WebSocket.OPEN` constant lookup happens client-side correctly. Check for SSR/initial-render
    issue where `wsClient` is undefined on first poll.

    **Case C — Docker uses HTTP-only tRPC (most likely per MEMORY.md):**
    The "Live/Offline" pill is meaningless. Two acceptable resolutions:
      (i) REMOVE LivePill entirely from status-footer.tsx (preferred — honest UI).
      (ii) Replace with "Connected" pill driven by useEngineInfo's last successful `dataUpdatedAt` <10s ago:
            ```
            const {dataUpdatedAt} = useEngineInfo()
            const fresh = Date.now() - (dataUpdatedAt ?? 0) < 10_000
            ```
          (use `trpcReact.docker.engineInfo.useQuery({}, {refetchInterval: 5_000})` if engineInfo
          hook doesn't expose dataUpdatedAt; this turns Live/Offline into "did our last poll succeed?")

    Pick (i) UNLESS the user already requested a presence indicator. Document choice in commit message.

    Implementation depends on diagnostic outcome. Whatever path is chosen:
    - If keeping useTrpcConnection: leave the file alone (no false positives that way).
    - If removing LivePill: also delete useTrpcConnection.ts and its import (it has no other consumer
      after Task 5 moves the pill to status-footer.tsx). Verify with:
        grep -rn "useTrpcConnection" livos/packages/ui/src
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -10</automated>
    Post-deploy:
    1. Open Docker app, watch StatusFooter for ~30s
    2. Indicator either shows green/connected (matching reality) OR is absent entirely
    3. NEVER shows "Offline" while Docker dashboard data is loading successfully
  </verify>
  <done>
    - Diagnosis documented in commit message body (which case applied)
    - LivePill behavior is now truthful (or removed)
    - Build green, tsc clean
    - Atomic commit: `fix(docker): make StatusFooter Live indicator truthful (or remove if WS unused)`
      — body explains the diagnosis and choice (Case A/B/C/i/ii)
  </done>
</task>

<task type="auto">
  <name>Task 7 — Shell empty-state white text + icon</name>
  <files>livos/packages/ui/src/routes/docker/shell/shell-section.tsx</files>
  <action>
    File: livos/packages/ui/src/routes/docker/shell/shell-section.tsx (138 lines).
    Lines 108-113 contain the empty state (verified via grep).

    Change ONLY two className strings:
    1. Line 109: `<IconTerminal2 size={32} className='text-text-tertiary' />`
       →           `<IconTerminal2 size={32} className='text-white' />`
    2. Line 110: `<p className='text-sm text-text-secondary'>`
       →           `<p className='text-sm text-white'>`

    Do NOT change:
    - Container div className (`flex h-full flex-col items-center justify-center gap-2 text-center`)
    - The text content
    - The IconTerminal2 size

    Note (visibility): `text-white` on light theme is INTENTIONAL per task spec. If background is white
    (Docker app is light-locked), check if there's a wrapping panel with a dark/gradient background that
    makes white text legible. If empty-state is on a white background, white-on-white is invisible —
    re-read lines 95-115 for the parent wrapper. If the parent is light, the user may have intended
    a different color; in that case implement as specified (white) AND flag in commit body
    "potential visibility concern — text-white on light parent — confirm with user".
  </action>
  <verify>
    <automated>grep -n "text-white\|text-text-tertiary\|text-text-secondary\|Click a container" livos/packages/ui/src/routes/docker/shell/shell-section.tsx | head -10 &amp;&amp; cd livos &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -5</automated>
    Expectation: lines 109 + 110 (or shifted equivalent) show `text-white`.
    Post-deploy: Open Docker app → Shell tab with no sessions; empty state text appears in white.
  </verify>
  <done>
    - Two className strings updated
    - No other shell-section.tsx changes
    - Build green
    - Atomic commit: `fix(ui): make Shell empty state text and icon white`
  </done>
</task>

<task type="auto">
  <name>Task 8 — Images table sticky Actions + truncated Repository:Tag</name>
  <files>livos/packages/ui/src/routes/docker/resources/image-section.tsx</files>
  <action>
    File: livos/packages/ui/src/routes/docker/resources/image-section.tsx (360 lines).

    1. Find the `<table>` element (likely from shadcn/ui Table primitive — `<Table>` / `<TableHeader>` /
       `<TableRow>` / `<TableCell>`). Add `table-fixed` className to the `<table>` (or `<Table>` className):
        before: `<Table>` or `<table className="...">`
        after:  add `table-fixed w-full` to className

    2. Repository:Tag column — typically the leftmost or second column:
        - On the `<th>` / TableHead: keep existing width OR set explicit `w-[40%]` to give it room.
        - On the `<td>` / TableCell: add `min-w-0` to the cell.
        - Inside the cell, wrap the rendered name string:
          ```
          <Tooltip>
            <TooltipTrigger asChild>
              <span className='block truncate'>{`${repo}:${tag}`}</span>
            </TooltipTrigger>
            <TooltipContent>{`${repo}:${tag}`}</TooltipContent>
          </Tooltip>
          ```
          (Tooltip primitive likely already imported elsewhere in the docker tree; if not, add:
          `import {Tooltip, TooltipTrigger, TooltipContent} from '@/shadcn-components/ui/tooltip'`
          and wrap the table in `<TooltipProvider>` if not already wrapped at app level.)

    3. Actions column — make it sticky right:
        - On the `<th>` / TableHead for Actions: add `sticky right-0 bg-white z-10`
          (z-10 keeps the column above other cells when other rows scroll horizontally).
        - On each `<td>` / TableCell for Actions: add `sticky right-0 bg-white`
          + handle hover state: row likely has a hover background; add `group-hover:bg-zinc-50`
            (or whatever the row's hover bg is) to keep the cell color consistent.
          Easiest: use `bg-white group-hover:bg-zinc-50` and ensure parent row has `group` class
          (add to TableRow: `className='group ...'` if not present).

    4. Light theme only — Docker app is locked light per docker-app.tsx hot-patch. Use `bg-white`
       not `bg-background` (background token may resolve dark on parent app shell).
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -5</automated>
    Post-deploy (Chrome DevTools MCP):
    1. Open Docker app → Images tab
    2. Resize window narrow (e.g. 800px wide)
    3. Verify Actions column stays visible at right edge (sticky)
    4. Long Repository:Tag names truncate with `…`; hover shows tooltip with full string
    5. No horizontal table overflow past the section bounds
  </verify>
  <done>
    - table-fixed applied
    - Repository:Tag truncates + Tooltip wraps it
    - Actions th + td sticky right-0 with bg-white
    - Build green
    - Atomic commit: `fix(docker/images): sticky Actions column + truncated Repository:Tag with tooltip`
  </done>
</task>

<task type="auto">
  <name>Task 9 — Image detail panels light theme</name>
  <files>
    livos/packages/ui/src/routes/docker/resources/image-section.tsx
    livos/packages/ui/src/routes/docker/resources/image-history-panel.tsx
    livos/packages/ui/src/routes/docker/resources/scan-result-panel.tsx
  </files>
  <action>
    Targets: image detail subviews — "Layer history" tab + "Vulnerabilities" tab.

    File map (verified):
    - image-section.tsx hosts the Tabs container (lines around 2191-2199 in legacy server-control showed
      `<TabsTrigger value='history'>Layer history</TabsTrigger><TabsTrigger value='scan'>Vulnerabilities</TabsTrigger>`
      pattern — same may live in image-section.tsx now).
    - image-history-panel.tsx (84 lines) renders Layer history list.
    - scan-result-panel.tsx (275 lines) renders Vulnerabilities scan results.

    Audit each file for hardcoded dark classes — replace with light equivalents:

    | Search pattern              | Replace with        |
    |-----------------------------|---------------------|
    | `bg-zinc-9XX` (900, 950)    | `bg-white` or `bg-zinc-50` |
    | `bg-neutral-9XX`            | `bg-white` or `bg-zinc-50` |
    | `bg-black`                  | `bg-white`          |
    | `bg-slate-9XX`              | `bg-white`          |
    | `text-zinc-1XX` (100, 200)  | `text-zinc-900`     |
    | `text-zinc-3XX` (on dark)   | `text-zinc-700`     |
    | `text-white` (on dark bg)   | `text-zinc-900`     |
    | `border-zinc-8XX/9XX`       | `border-zinc-200`   |

    Method:
    1. grep -n "bg-zinc-9\|bg-neutral-9\|bg-black\|bg-slate-9\|text-zinc-1\|text-zinc-2\|border-zinc-8\|border-zinc-9" \
         livos/packages/ui/src/routes/docker/resources/image-section.tsx \
         livos/packages/ui/src/routes/docker/resources/image-history-panel.tsx \
         livos/packages/ui/src/routes/docker/resources/scan-result-panel.tsx
    2. For each match, INSPECT the surrounding markup to understand the intended visual hierarchy,
       then replace with the light-theme equivalent that preserves the layer (e.g. background tier 1
       stays slightly darker than tier 0).
    3. Keep `dark:` modifiers if any exist — they're harmless under light-locked app.
    4. For severity badges in scan-result-panel.tsx (HIGH/CRITICAL etc), KEEP the colored badges
       (red-500, orange-500 etc) — those are semantic, not theme.

    Be surgical: don't touch markup, only className strings. If a panel uses CSS variable tokens
    (`bg-surface-base` etc), prefer leaving those alone — Round 1 already showed those resolve to
    transparent/dark on this app shell, but `bg-white` is the explicit fix per Round 1 SUMMARY.
  </action>
  <verify>
    <automated>grep -n "bg-zinc-9\|bg-neutral-9\|bg-black\|text-zinc-1\|text-zinc-2" livos/packages/ui/src/routes/docker/resources/image-section.tsx livos/packages/ui/src/routes/docker/resources/image-history-panel.tsx livos/packages/ui/src/routes/docker/resources/scan-result-panel.tsx | grep -v "dark:" &amp;&amp; cd livos &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -5</automated>
    Expectation: grep returns 0 non-`dark:` matches (or minimal, intentional ones).
    Post-deploy: Click an image → Layer history panel + Vulnerabilities panel both light theme,
    text legible, no inverted/dark sections.
  </verify>
  <done>
    - All hardcoded dark backgrounds in 3 files replaced with light
    - All inverted text colors corrected
    - Severity badges intact
    - Build green
    - Atomic commit: `fix(docker/images): light theme for Layer history + Vulnerabilities panels`
  </done>
</task>

<task type="auto">
  <name>Task 10 — Volumes table sticky Actions + truncated Mount Point</name>
  <files>
    livos/packages/ui/src/routes/docker/resources/volume-section.tsx
    livos/packages/ui/src/routes/docker/resources/volume-usage-panel.tsx
  </files>
  <action>
    Same pattern as Task 8, applied to the Volumes table.

    File: livos/packages/ui/src/routes/docker/resources/volume-section.tsx (269 lines).

    1. Add `table-fixed w-full` to the `<table>` / `<Table>` element.

    2. Volume name / ID column (long IDs like `02cf56023f73...`):
        - Cell gets `min-w-0`
        - Inner span: `<span className='block truncate font-mono text-xs'>{volumeId}</span>` wrapped in
          Tooltip that shows the full ID.

    3. Mount Point column (often very long paths):
        - Cell: `min-w-0`
        - Inner span: `<span className='block truncate'>{mountPoint}</span>` wrapped in Tooltip.

    4. Actions column:
        - `<th>`: `sticky right-0 bg-white z-10`
        - `<td>`: `sticky right-0 bg-white group-hover:bg-zinc-50`
        - TableRow: ensure `group` class is on it.

    5. Also audit volume-usage-panel.tsx for the same dark-class regressions audited in Task 9 —
       if found, fix in same atomic commit (it's part of the volumes detail surface).
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter ui exec tsc --noEmit 2&gt;&amp;1 | tail -5 &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -5</automated>
    Post-deploy (Chrome DevTools MCP):
    1. Open Docker app → Volumes tab
    2. Resize window narrow
    3. Actions column stays visible at right edge (sticky)
    4. Long volume IDs + mount points truncate with `…`; hover shows tooltip with full string
    5. No horizontal overflow
  </verify>
  <done>
    - table-fixed applied
    - Volume ID + Mount Point truncate + Tooltip
    - Actions sticky right-0
    - Build green
    - Atomic commit: `fix(docker/volumes): sticky Actions column + truncated Mount Point/ID with tooltip`
  </done>
</task>

<task type="auto">
  <name>Task 11 — Build, push, deploy, verify</name>
  <files>(no source edits — integration step)</files>
  <action>
    Step 1 — Local final build (sanity check after 10 commits):
      cd livos
      pnpm --filter @livos/config build
      pnpm --filter ui build
      Both must exit 0. If `tsc` errors surface, find which task introduced them and patch in a
      follow-up commit (do NOT amend a prior commit per GSD safety rules).

    Step 2 — Push:
      git log --oneline origin/master..HEAD   (should show ~10 commits)
      git push origin HEAD:master

    Step 3 — Mini PC deploy:
      ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc \
          -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
          bruce@10.69.31.68 'sudo bash /opt/livos/update.sh 2>&1 | tail -50'
      Expect: "✓ Nexus core built", "✓ Nexus worker built", "✓ Nexus mcp-server built",
              "✓ Nexus memory built", UI vite build line, services restart lines.

    Step 4 — Service health check:
      ssh ... 'systemctl is-active livos liv-core liv-worker liv-memory'
      All four must report `active`.
      If any inactive: `journalctl -u <service> -n 50 --no-pager` for diagnosis.

    Step 5 — Browser verification (Chrome DevTools MCP at https://bruce.livinity.io):
      a. Service worker reset:
         evaluate_script: `navigator.serviceWorker.getRegistrations().then(rs => Promise.all(rs.map(r => r.unregister())))`
         evaluate_script: `caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))`
         Hard reload (Ctrl+Shift+R).

      b. Per-fix smoke checks:
         1. Activity tab → events list scrolls inside section, doesn't overflow window. ✓ Fix 1
         2. Desktop → Docker tile shows whale icon. ✓ Fix 2
         3. Server Management → only 4 tabs visible (Overview/PM2/Monitoring/Domains). ✓ Fix 3
         4. Settings sidebar → no Environments item. ✓ Fix 4
         5. Docker app top bar → only EnvSelector + Search + Bell. ✓ Fix 5a
            Docker app bottom bar → all stat pills present, sticky. ✓ Fix 5b
         6. StatusFooter Live indicator → green/connected (or absent per Task 6 decision). ✓ Fix 6
         7. Docker → Shell with no sessions → empty state white text. ✓ Fix 7
         8. Docker → Images, narrow window → Actions sticky, Repo:Tag truncate+tooltip. ✓ Fix 8
         9. Docker → Images → click image → Layer history + Vulnerabilities both light. ✓ Fix 9
         10. Docker → Volumes, narrow window → Actions sticky, Mount Point truncate+tooltip. ✓ Fix 10

      c. Console: no new errors during the above flows.

    Step 6 — Document outcomes in SUMMARY.md (next plan-run step writes it; this task just leaves
    the work in a verified state).

    If any verification step fails: log the failure in scratchpad, do NOT roll back unless the failure
    is a build/runtime error. Per Round 1 convention: visual-only failures get logged as partial pass.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build 2&gt;&amp;1 | tail -10 &amp;&amp; git log --oneline origin/master..HEAD</automated>
    SSH: `systemctl is-active livos liv-core liv-worker liv-memory` → 4×active
  </verify>
  <done>
    - All ~10 commits on origin/master
    - update.sh deploy succeeded
    - All 4 services active
    - Per-fix smoke checks pass (or logged partial-pass)
    - No new console errors
  </done>
</task>

</tasks>

<verification>
After all 11 tasks:

1. `git log --oneline origin/master..HEAD` shows ~10 atomic commits, each scoped to one fix.
2. Local `cd livos && pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
3. `ssh bruce@10.69.31.68 'systemctl is-active livos liv-core liv-worker liv-memory'` → all `active`.
4. Browser verification at https://bruce.livinity.io confirms each fix's user-visible behavior
   (Activity scroll, Docker icon, Server Management 4-tab, Settings menu, StatusBar split,
   Live indicator, Shell empty state, Images sticky+truncate+light, Volumes sticky+truncate).
5. No new console errors during the verification flows.
</verification>

<success_criteria>
- 10 fix commits + 1 deploy commit chain on master
- All 4 Mini PC services active post-deploy
- All 10 user-reported issues either resolved (verified) or logged as partial-pass with reason
- No regressions: Round 1 fixes (sidebar bg, container-form opacity, container-row click) still work
</success_criteria>

<output>
After completion, write summary to:
.planning/quick/260425-v1s-v28-0-hot-patch-round-2-activity-overflo/260425-v1s-SUMMARY.md

Following the format from Round 1 SUMMARY (260425-sfg-SUMMARY.md):
- Frontmatter (mode, type, status, completed_at, commits[], files_modified[], deployed_to)
- Tasks Executed table
- Deploy Verification
- Browser Verification (per-fix pass/partial-pass list)
- Deviations from Plan
- Self-Check
</output>
