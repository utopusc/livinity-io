---
phase: 57-passthrough-mode-agent-mode
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts
  - livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts
autonomous: true
requirements:
  - FR-BROKER-A2-02
  - FR-BROKER-A1-04
tags:
  - broker
  - agent-mode
  - byte-identity
  - sacred-file-gate
  - phase-57

must_haves:
  truths:
    - "All v29.5 broker integration test cases pass byte-identical when X-Livinity-Mode: agent header is injected (FR-BROKER-A2-02)"
    - "Test diffs limited to the header-injection lines + setup/comments — no behavior changes inside test bodies"
    - "Sacred file nexus/packages/core/src/sdk-agent-runner.ts SHA = 4f868d318abff71f8c8bfbcf443b2393a553018b unchanged at end of phase (FR-BROKER-A1-04)"
    - "Sacred file integrity test (sdk-agent-runner-integrity.test.ts) PASSES with the same BASELINE_SHA constant"
    - "Full broker test suite (vitest run livinity-broker) is GREEN"
    - "Agent mode behavior end-to-end is byte-identical to v29.5 (response shape, error envelopes, SSE event sequence, tool warn-and-ignore log)"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts"
      provides: "Anthropic Messages integration tests, modified to inject X-Livinity-Mode: agent header on every test request"
      contains: "X-Livinity-Mode"
    - path: "livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts"
      provides: "OpenAI Chat Completions integration tests, modified to inject X-Livinity-Mode: agent header"
      contains: "X-Livinity-Mode"
  key_links:
    - from: "integration.test.ts test requests"
      to: "agent-mode dispatch branch in router.ts"
      via: "X-Livinity-Mode: agent header injection"
      pattern: "X-Livinity-Mode.*agent"
    - from: "openai-integration.test.ts test requests"
      to: "agent-mode dispatch branch in openai-router.ts"
      via: "X-Livinity-Mode: agent header injection"
      pattern: "X-Livinity-Mode.*agent"
---

<objective>
Wave 4 — Agent Mode Byte-Identity Proof + Sacred File Final Verification.

Purpose: Modify the existing v29.5 broker integration tests (`integration.test.ts` + `openai-integration.test.ts`) to inject `X-Livinity-Mode: agent` header on every request. With the header, all tests MUST pass byte-identical to v29.5 — proving FR-BROKER-A2-02 (agent mode preserves current Nexus-tooled behavior end-to-end). NO production code changes in this wave — TESTS ONLY.

This wave is the closing gate of Phase 57: it both proves the dispatch dichotomy works (passthrough = NEW path; agent = OLD path byte-identical) AND verifies the sacred file SHA is unchanged after all 5 waves of work.

CRITICAL ARCHITECTURAL FINDING (per RESEARCH.md Q3 verdict): LivOS in-app chat uses `/ws/agent` WebSocket — NOT the broker. Therefore FR-BROKER-A2-02 ("LivOS in-app chat remains unchanged") is satisfied AUTOMATICALLY by routing topology — no UI-side header injection is needed. The Wave 4 test work is the ONLY in-app-chat-related work in this phase.

Output:
- `integration.test.ts` modified to inject `X-Livinity-Mode: agent` on all requests
- `openai-integration.test.ts` modified to inject `X-Livinity-Mode: agent` on all requests
- All test bodies UNCHANGED (only the header injection added)
- Full broker test suite GREEN
- Sacred file SHA pre-flight + post-execute confirmed `4f868d318abff71f8c8bfbcf443b2393a553018b`
- Sacred file integrity test (sdk-agent-runner-integrity.test.ts) PASSES
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-passthrough-mode-agent-mode/57-CONTEXT.md
@.planning/phases/57-passthrough-mode-agent-mode/57-RESEARCH.md
@.planning/phases/57-passthrough-mode-agent-mode/57-01-SUMMARY.md
@.planning/phases/57-passthrough-mode-agent-mode/57-02-SUMMARY.md
@.planning/phases/57-passthrough-mode-agent-mode/57-03-SUMMARY.md
@.planning/phases/57-passthrough-mode-agent-mode/57-04-SUMMARY.md

@livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts
@livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts
@livos/packages/livinityd/source/modules/livinity-broker/router.ts
@livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts
@nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts

<interfaces>
<!-- Header injection patterns. Plan-author should match existing test fixture style. -->

Likely existing test request pattern in integration.test.ts (planner — verify by Read in Step 1):
```typescript
const response = await fetch(`${baseUrl}/u/${userId}/v1/messages`, {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({...}),
})
```

Required transform — add header to EVERY POST/GET that hits the broker:
```typescript
const response = await fetch(`${baseUrl}/u/${userId}/v1/messages`, {
  method: 'POST',
  headers: {'Content-Type': 'application/json', 'X-Livinity-Mode': 'agent'},
  body: JSON.stringify({...}),
})
```

Sacred file BASELINE_SHA constant (per nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts:42 — verified in research):
```typescript
const BASELINE_SHA = '4f868d318abff71f8c8bfbcf443b2393a553018b'
```
This constant MUST NOT change in Wave 4 (or any other wave).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inject X-Livinity-Mode: agent header into both integration test files</name>
  <files>
    livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts
    livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts
  </files>
  <read_first>
    .planning/phases/57-passthrough-mode-agent-mode/57-RESEARCH.md
    livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts
    livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts
    livos/packages/livinityd/source/modules/livinity-broker/router.ts
    livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts
  </read_first>
  <action>
    Pre-flight: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` MUST equal `4f868d318abff71f8c8bfbcf443b2393a553018b`.

    Step 1 — Inspect both integration test files to find the request invocation pattern:
      Run `grep -n "fetch\\|supertest\\|axios\\|request(" livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts | head -20`
      Run `grep -n "fetch\\|supertest\\|axios\\|request(" livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts | head -20`
      Determine the HTTP-call style used (fetch, supertest, etc.). The transform pattern depends on this — see Step 2 alternatives.

    Step 2 — Apply the minimal-diff header injection. Two approaches; pick whichever produces the smallest diff per file:

    **Approach A — Centralized helper (preferred when all requests share a common request-builder):**
      If the test file has a helper like `function postBroker(body) { return fetch(...) }` or similar, add `'X-Livinity-Mode': 'agent'` to that helper's headers object ONCE.
      Add a comment above the helper:
      ```typescript
      // Phase 57: agent mode is opt-in via X-Livinity-Mode: agent header.
      // These v29.5 integration tests preserve byte-identical behavior in agent mode.
      ```

    **Approach B — Per-request injection (fallback if there's no central helper):**
      For EACH `fetch(...)` (or equivalent) call that hits `/u/<id>/v1/messages` or `/u/<id>/v1/chat/completions`, add `'X-Livinity-Mode': 'agent'` to the headers object. Example:

      Before:
      ```typescript
      const response = await fetch(`${baseUrl}/u/${userId}/v1/messages`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({...}),
      })
      ```
      After:
      ```typescript
      const response = await fetch(`${baseUrl}/u/${userId}/v1/messages`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json', 'X-Livinity-Mode': 'agent'},
        body: JSON.stringify({...}),
      })
      ```

      Add a SINGLE top-of-file comment block (after existing imports):
      ```typescript
      // Phase 57: every request below sets `X-Livinity-Mode: agent` to opt into the
      // existing Strategy B HTTP-proxy → nexus → sacred sdk-agent-runner.ts path.
      // Default (header absent) = passthrough mode (covered by passthrough-handler.test.ts).
      // Agent mode preserves v29.5 behavior byte-identical (FR-BROKER-A2-02).
      ```

    Step 3 — Constraint: NO changes to test bodies, assertions, or fixture data. Only:
      - Headers objects in HTTP requests get `'X-Livinity-Mode': 'agent'` added
      - One top-of-file comment block explaining the injection rationale
      The diff for each file should be: 1 comment block + N header-additions where N = number of broker HTTP calls.

    Step 4 — Run integration.test.ts:
      `cd livos/packages/livinityd && npx vitest run source/modules/livinity-broker/integration.test.ts --reporter=verbose 2>&1 | tail -40`
      Expected: ALL cases GREEN (byte-identical to v29.5 passing state — RESEARCH.md confirms agent-mode code path is unchanged).
      If any case fails: investigate. Possible causes:
        a) A request was missed (still defaults to passthrough → unexpected behavior). Solution: grep for remaining requests without the header.
        b) The dispatch in router.ts has a typo or accidental fallthrough — re-inspect Wave 2 router.ts edit.
        c) An OpenAI test hits the OpenAI route — same fix in openai-integration.test.ts.

    Step 5 — Run openai-integration.test.ts:
      `cd livos/packages/livinityd && npx vitest run source/modules/livinity-broker/openai-integration.test.ts --reporter=verbose 2>&1 | tail -40`
      Expected: ALL cases GREEN.

    Step 6 — Run the FULL broker test suite (Phase gate):
      `cd livos/packages/livinityd && npx vitest run source/modules/livinity-broker/ --reporter=verbose 2>&1 | tail -30`
      Expected: ALL test files GREEN, including:
        - mode-dispatch.test.ts (Wave 0/1)
        - credential-extractor.test.ts (Wave 0/1) including Risk-A1 smoke gate
        - passthrough-handler.test.ts (Wave 0/2/3)
        - openai-translator.test.ts (existing + Wave 3 additions)
        - integration.test.ts (this wave — agent-mode byte-identity)
        - openai-integration.test.ts (this wave — agent-mode byte-identity)
        - existing test files (auth.test.ts, sse-adapter.test.ts, openai-sse-adapter.test.ts, translate-request.test.ts) — must remain GREEN

    Step 7 — Run sacred file integrity test (FR-BROKER-A1-04 final gate):
      The integrity test is at `nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts` per RESEARCH.md.
      Run: `cd nexus/packages/core && npm run test 2>&1 | grep -E "sdk-agent-runner|integrity" | head -20` OR (if nexus uses tsx):
        `cd nexus/packages/core && npx tsx src/providers/sdk-agent-runner-integrity.test.ts 2>&1` OR
        `cd nexus/packages/core && npx vitest run src/providers/sdk-agent-runner-integrity.test.ts 2>&1 | tail -20`
      Plan-author: pick the correct invocation by inspecting `nexus/packages/core/package.json` `scripts` block — the integrity test must run via the existing pattern (RESEARCH.md states it runs via `npm run test:phase39`).
      Expected: integrity test PASSES with `BASELINE_SHA = "4f868d318abff71f8c8bfbcf443b2393a553018b"` matching `git hash-object` output exactly.

    Step 8 — Final sacred file verification:
      `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` MUST output `4f868d318abff71f8c8bfbcf443b2393a553018b`.
      `git diff 4f868d318abff71f8c8bfbcf443b2393a553018b -- nexus/packages/core/src/sdk-agent-runner.ts 2>&1` MUST be empty (this is FR-BROKER-A1-04's literal acceptance criterion from ROADMAP.md success criterion 4).
      `git status nexus/packages/core/src/sdk-agent-runner.ts` MUST show no modifications.
      Verify BASELINE_SHA constant unchanged in integrity test: `grep -q "4f868d318abff71f8c8bfbcf443b2393a553018b" nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts` exits 0.

    Step 9 — Document Phase 57 completion in 57-05-SUMMARY.md (output file). Include:
      - Test counts: integration.test.ts cases passing / openai-integration.test.ts cases passing
      - Header-injection diff sizes per file
      - Sacred file SHA confirmed pre-flight + post-execute
      - Integrity test outcome (PASS/FAIL)
      - Total broker test suite pass count
      - Confirmation that NO production code (router.ts, openai-router.ts, passthrough-handler.ts, mode-dispatch.ts, credential-extractor.ts, openai-translator.ts) was modified in this wave — TESTS ONLY
      - Phase 57 final verdict: 6/6 requirements satisfied, sacred file untouched, ready for Phase 58 (true token streaming)

    No edits permitted in this task to any file under:
      - `nexus/packages/core/src/`
      - `livos/packages/livinityd/source/modules/livinity-broker/*.ts` (excluding the two test files explicitly listed in `files_modified`)
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && bash -c 'test "$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)" = "4f868d318abff71f8c8bfbcf443b2393a553018b" && grep -q "X-Livinity-Mode" livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts && grep -q "X-Livinity-Mode" livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts && grep -q "agent" livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts && grep -q "Phase 57" livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts && grep -q "Phase 57" livos/packages/livinityd/source/modules/livinity-broker/openai-integration.test.ts && test "$(git diff -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l)" = "0" && cd livos/packages/livinityd && npx vitest run source/modules/livinity-broker/ --reporter=verbose 2>&1 | grep -E "Tests +[0-9]+ passed.*Tests" || npx vitest run source/modules/livinity-broker/ --reporter=verbose 2>&1 | grep -E "passed" && echo PHASE_57_COMPLETE'</automated>
  </verify>
  <acceptance_criteria>
    - Sacred file SHA: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` = `4f868d318abff71f8c8bfbcf443b2393a553018b` (THE Phase 57 closing gate)
    - **`git diff -- nexus/packages/core/src/sdk-agent-runner.ts` is EMPTY** (FR-BROKER-A1-04 verbatim acceptance from ROADMAP.md)
    - `git status nexus/packages/core/src/sdk-agent-runner.ts` shows no modifications
    - integration.test.ts contains `X-Livinity-Mode` header on every broker request: `grep -c "X-Livinity-Mode" integration.test.ts` ≥ 1 (typically 1 if centralized helper, OR ≥ N where N = number of broker fetch calls)
    - integration.test.ts contains the value `'agent'` paired with the header: `grep -c "X-Livinity-Mode.*agent\\|agent.*X-Livinity-Mode" integration.test.ts` ≥ 1
    - openai-integration.test.ts contains `X-Livinity-Mode: agent` similarly
    - integration.test.ts contains the Phase 57 explanation comment: `grep -q "Phase 57" integration.test.ts`
    - openai-integration.test.ts contains the Phase 57 explanation comment: `grep -q "Phase 57" openai-integration.test.ts`
    - Diff size sanity check (header-injection-only — no body changes):
      - `git diff --numstat integration.test.ts | awk '{ if ($2 == 0 && $1 < 50) print "OK"; else print "FAIL_too_many_changes_or_deletions" }'` returns "OK" (≤50 additions, 0 deletions). If centralized helper, much smaller.
      - Same check for openai-integration.test.ts.
    - Full broker test suite GREEN (Phase gate): `cd livos/packages/livinityd && npx vitest run source/modules/livinity-broker/ --run 2>&1 | grep -E "FAIL|✗" | wc -l | grep -q "^0$"` succeeds (zero FAIL/✗ lines).
    - Sacred file integrity test PASSES — confirmed via existing test runner. Plan-author records actual command + output in SUMMARY.
    - BASELINE_SHA constant in integrity test unchanged: `grep -q "4f868d318abff71f8c8bfbcf443b2393a553018b" nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts` exits 0
    - **No edits to any non-test file**: `git status livos/packages/livinityd/source/modules/livinity-broker/ | grep -E "M.*\\.ts$" | grep -v "test\\.ts" | wc -l` returns "0" (only test files modified in this wave)
    - **No edits to nexus/**: `git status nexus/` shows no entries from this task
    - **No Edit/Write tool calls were made to `nexus/packages/core/src/sdk-agent-runner.ts`** — verified by zero diff and SHA stability
  </acceptance_criteria>
  <done>
    Both integration test files inject X-Livinity-Mode: agent header on every broker request. Full broker test suite GREEN. Sacred file SHA confirmed unchanged across all 5 waves. Sacred file integrity test PASSES. Phase 57 ALL 6 REQUIREMENTS SATISFIED (FR-BROKER-A1-01..04 + FR-BROKER-A2-01..02). Ready for Phase 58 (true token streaming).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → broker (agent mode) | Integration tests inject explicit `X-Livinity-Mode: agent` to route through unchanged Strategy B path — proves agent mode dispatch works with header. |
| Wave 4 changes → production code | NONE. This wave touches only `*test.ts` files. Production code (router.ts, openai-router.ts, passthrough-handler.ts, mode-dispatch.ts, credential-extractor.ts, openai-translator.ts) MUST NOT be modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-17 | T | Sacred file edited inadvertently in Wave 4 | mitigate | Pre-flight + post-execute SHA assertion; full `git diff` empty check; acceptance criterion enforces. ANY drift HALTS Phase 57 close until reverted. |
| T-57-18 | T | Test-only wave accidentally modifies production code | mitigate | Acceptance criterion: `git status` filtered for non-test `.ts` files in livinity-broker/ shows zero modifications. Reviewer can verify via diff. |
| T-57-19 | T | Header-injection misses a request → defaults to passthrough → spurious failure misattributed | mitigate | If integration test fails with "Nexus identity missing" or "MCP tools not invoked", grep for remaining `fetch.*\\/v1\\/(messages|chat\\/completions)` calls without `X-Livinity-Mode` header. Plan task body lists this as a debugging step. |
</threat_model>

<verification>
## Phase 57 Closing Verification (run in this order)

1. **Pre-flight sacred SHA:** `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` = `4f868d318abff71f8c8bfbcf443b2393a553018b` ✓
2. **integration.test.ts modified:** header injected, body unchanged
3. **openai-integration.test.ts modified:** header injected, body unchanged
4. **integration.test.ts GREEN:** all v29.5 cases pass byte-identical
5. **openai-integration.test.ts GREEN:** all v29.5 cases pass byte-identical
6. **Full broker test suite GREEN:** vitest run across all *.test.ts files in livinity-broker/
7. **Sacred file integrity test PASSES:** sdk-agent-runner-integrity.test.ts BASELINE_SHA matches
8. **Post-execute sacred SHA:** unchanged + git diff empty
9. **No production code modified in Wave 4:** git status confirms only test files touched
</verification>

<success_criteria>
- 2 modified test files (integration.test.ts + openai-integration.test.ts) — header injection only
- ALL broker test files GREEN (Wave 0+1+2+3+4 — entire test surface)
- FR-BROKER-A1-04 final gate: sacred file SHA + git diff verified clean
- FR-BROKER-A2-02 satisfied: agent mode preserves v29.5 behavior byte-identical
- Phase 57 6/6 requirements satisfied (A1-01..04 + A2-01..02)
- Ready for Phase 58 (true token streaming for passthrough route)
</success_criteria>

<output>
After completion, create `.planning/phases/57-passthrough-mode-agent-mode/57-05-SUMMARY.md` capturing:
- Header-injection diff sizes per file (LOC additions; should be small)
- Test counts per file post-injection (integration.test.ts X passed, openai-integration.test.ts Y passed)
- Full broker suite test count (total tests, total passed)
- Sacred file SHA confirmed pre-flight + post-execute (both must equal `4f868d318abff71f8c8bfbcf443b2393a553018b`)
- Sacred file integrity test command + outcome (e.g., "ran via `npm run test:phase39` → PASS")
- Confirmation NO production code modified in Wave 4
- Phase 57 verdict: 6/6 requirements satisfied, ready for Phase 58
- Forensic trail: timestamp + which wave each requirement was actually satisfied:
  - FR-BROKER-A1-01: Wave 2 (Anthropic) + Wave 3 (OpenAI)
  - FR-BROKER-A1-02: Wave 2 + Wave 3 (passthrough handler verbatim forwarding)
  - FR-BROKER-A1-03: Wave 2 + Wave 3 (no MCP tools injected)
  - FR-BROKER-A1-04: Wave 0 + Wave 1 + Wave 2 + Wave 3 + Wave 4 (continuous SHA verification)
  - FR-BROKER-A2-01: Wave 1 (mode-dispatch) + Wave 2 (router insertion) + Wave 3 (openai-router insertion)
  - FR-BROKER-A2-02: Wave 4 (this — byte-identity proof via existing integration tests with header)
</output>
