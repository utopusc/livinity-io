---
phase: 229-single-user-posture-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - docs/v42-single-user-posture.md
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04]
tags: [v42, docs, single-user, posture, repo-only]
must_haves:
  truths:
    - "A NEW file `docs/v42-single-user-posture.md` is created. It captures (a) the v42 single-user posture DECISION in one short paragraph, (b) the RATIONALE for why multi-user is deferred, (c) an INVENTORY of v7.0 multi-user surfaces that are PRESERVED but inactive (PostgreSQL schema path + 7 table names, admin auth + RBAC location, app sharing UI component, login route, app gateway middleware location, tRPC route namespaces), (d) DEFERRED-TO-V43 items (per-user Liv Assistant data isolation strategy with three concrete option sketches, per-user Claude subscription auth concerns, Caddy wildcard cert + CF_API_TOKEN integration, v43 announcement + migration runbook), and (e) a short HOW-TO-ENABLE pointer for the eventual v43 activation path. Tone is factual + decision-recording + future-aware. No emojis. No marketing language."
    - "`.planning/PROJECT.md` gets a NEW `## v42 Posture: Single-User (Multi-User Deferred to v43)` section inserted between the closing line of the `## Current State (post v22.0 — AGI Platform)` paragraph (line 129 in the 2026-05-27 repo read) and the `### Validated (v23.0)` subsection header (line 131). The new section is 6-10 lines: opening one-line statement of the v42 posture, a short paragraph (2-3 sentences) summarising the rationale + the v7.0 preservation guarantee, and a final line linking to `docs/v42-single-user-posture.md` for the full inventory + v43 deferred-item details."
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (`liv/packages/core/src/sdk-agent-runner.ts`) is UNCHANGED. Neither file in this plan lives under `liv/packages/core/`. Pre-commit hook PASSES. Confirm: `git diff --cached --name-only | grep -E '^liv/packages/core/' || echo 'sacred safe'` returns `sacred safe`, AND `git hash-object liv/packages/core/src/sdk-agent-runner.ts` returns the canonical SHA byte-for-byte."
    - "Single atomic commit with message starting `docs(229-01):` covering both files. NO behavioural code changes — pure docs. NO Mini PC contact (no SSH, no curl against bruce.livinity.io, no `update.sh`). The phase is repo-only."
    - "Idempotency: re-running this plan (or cherry-picking the commit fresh into a clean tree) produces zero further diff. The new docs file's `# v42 Single-User Posture` H1 + the PROJECT.md `## v42 Posture: Single-User (Multi-User Deferred to v43)` header are unique strings — a re-run sees them already present and no-ops."
    - "The new docs file references `livos/packages/livinityd/source/modules/database/schema.sql` as the canonical preserved schema location and explicitly enumerates the 7 v7.0 tables: `users`, `sessions`, `user_preferences`, `system_settings`, `user_app_access`, `user_app_instances`, `invites` (matches MEMORY.md inventory verbatim)."
    - "The new docs file flags the known Caddy wildcard-cert blocker (`CF_API_TOKEN`) as a v43-prerequisite per existing project memory — same blocker that gates the Settings → Users → Multi-user mode toggle today."
    - "The new docs file documents the per-user Liv Assistant data isolation as an OPEN design question for v43 (NOT a decision), with three concrete option sketches: (A) shared `/opt/liv-assistant/data/` (v42 status quo), (B) per-user `~/<username>/.liv-assistant/data/` volume-mount with multi-instance + port allocation, (C) AionUi upstream multi-tenant mode pending upstream feature audit. No option is selected — Phase 229 is a recording phase, not a deciding phase."
  artifacts:
    - path: "docs/v42-single-user-posture.md"
      provides: "NEW operator + future-self reference doc capturing v42's single-user posture decision, v7.0 multi-user preservation inventory, and v43 deferred-design-question list"
      contains: "v42 Single-User Posture"
    - path: "docs/v42-single-user-posture.md"
      provides: "Same file — enumerates the 7 v7.0 PostgreSQL tables preserved"
      contains: "user_app_access"
    - path: "docs/v42-single-user-posture.md"
      provides: "Same file — references the canonical schema location"
      contains: "livos/packages/livinityd/source/modules/database/schema.sql"
    - path: "docs/v42-single-user-posture.md"
      provides: "Same file — names the known multi-user blocker (Caddy wildcard cert + CF_API_TOKEN)"
      contains: "CF_API_TOKEN"
    - path: "docs/v42-single-user-posture.md"
      provides: "Same file — frames per-user Liv Assistant data isolation as a v43 open design question"
      contains: "per-user Liv Assistant"
    - path: ".planning/PROJECT.md"
      provides: "Master project record — new v42 Posture section near the top of Current State"
      contains: "## v42 Posture: Single-User (Multi-User Deferred to v43)"
    - path: ".planning/PROJECT.md"
      provides: "Same file — the new section links out to the deferred doc"
      contains: "docs/v42-single-user-posture.md"
  key_links:
    - from: ".planning/PROJECT.md v42 Posture section"
      to: "docs/v42-single-user-posture.md (full inventory + v43 deferred list)"
      via: "inline markdown link in the new PROJECT.md section's last line"
      pattern: "docs/v42-single-user-posture.md"
    - from: "docs/v42-single-user-posture.md preserved-surfaces inventory"
      to: "Actual v7.0 schema source-of-truth"
      via: "explicit path reference to `livos/packages/livinityd/source/modules/database/schema.sql`"
      pattern: "livos/packages/livinityd/source/modules/database/schema.sql"
    - from: "docs/v42-single-user-posture.md Caddy wildcard blocker note"
      to: "Existing project knowledge (MEMORY.md multi-user toggle blocker)"
      via: "named env var token `CF_API_TOKEN` matches the blocker referenced in MEMORY.md"
      pattern: "CF_API_TOKEN"
    - from: "Phase 229 SC-01 (PROJECT.md has v42 single-user section)"
      to: "PROJECT.md insertion at line 130"
      via: "exact-match header `## v42 Posture: Single-User (Multi-User Deferred to v43)`"
      pattern: "## v42 Posture: Single-User (Multi-User Deferred to v43)"
    - from: "Phase 229 SC-02 (docs file exists with rationale)"
      to: "docs/v42-single-user-posture.md"
      via: "file existence + required-token grep gate in the verify block"
      pattern: "docs/v42-single-user-posture.md"
---

<objective>
Create `docs/v42-single-user-posture.md` (NEW reference doc) and add a `## v42 Posture: Single-User (Multi-User Deferred to v43)` section to `.planning/PROJECT.md` documenting v42's deliberate single-user posture. The v7.0 multi-user infrastructure (PostgreSQL schema + login screen + invite system + per-user Docker compose + app gateway + RBAC + app sharing) is PRESERVED but INACTIVE — Phase 229 records that decision so the architectural rationale survives across resumes and future operator handoffs. Per-user Liv Assistant data isolation is documented as an OPEN design question for v43, with three option sketches (shared dir / per-user volume-mounted / AionUi multi-tenant) — Phase 229 records the question, NOT the answer.

Purpose: v42 closes the OpenClawOS-to-AionUi swap (Phases 222-228 + 232 shipped) without re-activating multi-user. The single-user posture is a deliberate scope choice — not a regression. Future operators (and future Claude sessions resuming work) need a single source-of-truth doc that names the decision, names what's preserved, and names what's deferred. PROJECT.md gets a short top-level pointer; the deep inventory + v43-thinking lives in the dedicated docs file.

Output: Two file edits in one atomic commit. Pure repo edits — no Mini PC, no deploy, no behavioural code change. Sacred SHA unchanged, pre-commit hook PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/liv-assistant-install.md
@.planning/phases/228-claude-auth-bridge/228-01-PLAN.md

<interfaces>
<!-- ============================================================
     .planning/PROJECT.md — INSERTION POINT (2026-05-27 repo read)
     ============================================================
     Section anchors (left col line numbers from local read):

         Line   1   # Livinity — Self-Hosted AI Server Platform
         Line   3   ## What This Is
         Line   7   ## Core Value
         Line  11   ## Requirements
         Line 127   ## Current State (post v22.0 — AGI Platform)
         Line 129   [closing paragraph of v22 Current State narrative]
         Line 130   [blank line]
         Line 131   ### Validated (v23.0)
         Line 133   - ✓ PWA installability: ...
         ...
         Line 139   ## Current State: v27.0 Shipped (Docker Management Upgrade)
         Line 145   ## Current State: v28.0 Shipped (Docker Management UI)
         ...
         Line 337   ## Current Milestone: v41 — Admin Panel + Store Hardening ...

     The new `## v42 Posture: Single-User (Multi-User Deferred to v43)` section
     goes IMMEDIATELY BEFORE the `### Validated (v23.0)` header at line 131.
     This keeps the v22 narrative paragraph intact and places the v42 posture
     callout at the natural seam between the historical Current State narrative
     and the chronological "Validated" timeline — operators reading top-down
     hit the v42 posture flag before scrolling into the v23+ timeline.

     Do NOT insert at line 337 (`## Current Milestone: v41`) — v41 is a
     separate (parallel) milestone. v42 is its own milestone and the posture
     callout belongs near the cross-cutting Current State header, not nested
     under v41's section.

     ============================================================
     docs/v42-single-user-posture.md — NEW FILE STRUCTURE
     ============================================================
     The new file follows the same operator-doc style as the existing
     docs/liv-assistant-install.md:
       - H1 title + 1-line description
       - "## Decision" — what + when, no rationale yet
       - "## Rationale" — why v42 ships single-user
       - "## Preserved multi-user surfaces (v7.0 inventory)" — table of what
         IS shipped + inactive (schema path, 7 tables, admin auth, app
         sharing UI, login route, app gateway, tRPC namespaces)
       - "## Deferred to v43" — list of open items with brief design
         sketches for the open design questions
       - "## How to (eventually) enable multi-user" — short pointer
         section: Settings → Users toggle + CF_API_TOKEN setup +
         pointer to v7.0 phase artifacts in `.planning/`

     Avoid: speculation about v43 ship dates, judgment about "best"
     option among the three Liv Assistant isolation sketches, or any
     marketing tone. Tone matches docs/liv-assistant-install.md
     (factual, runbook-style, table-driven where it helps).

     ============================================================
     v7.0 multi-user infrastructure — INVENTORY (verbatim from MEMORY.md)
     ============================================================
     PostgreSQL schema location: livos/packages/livinityd/source/modules/database/schema.sql
     7 tables: users, sessions, user_preferences, system_settings,
               user_app_access, user_app_instances, invites
     Admin auth + RBAC: `is-authenticated.ts` resolves `ctx.currentUser`;
                        `adminProcedure` enforces role hierarchy
                        (admin > member > guest)
     App gateway: Express middleware in `server/index.ts` intercepts
                  subdomain requests, checks session, routes to per-user
                  container
     Per-user Docker: `installForUser()` in `apps.ts` generates per-user
                      compose files with unique container names, ports
                      (10000+), volumes
     App sharing: `user_app_access` table; share/unshare via tRPC routes;
                  UI via `ShareAppDialog`
     Login screen: `/login` route with avatar grid, password entry,
                   multi-user JWT generation
     tRPC routes: `users.*`, `invites.*`, `userApps.*`
     Known blocker: CF_API_TOKEN required in Caddy systemd service env for
                    wildcard DNS challenge → enables Settings > Users >
                    Multi-user mode toggle

     ============================================================
     v43 deferred — Per-user Liv Assistant data isolation
     ============================================================
     Liv Assistant in v42 runs as a single bruce-owned systemd unit at
     `:3020` with `/opt/liv-assistant/data/` as the single SQLite + log
     dir (Phase 223-02 / 223-04 ship state). Activating multi-user means
     more than one operator persona would collide on that shared dir.

     Three concrete option sketches to record in the doc (NO selection):
       Option A — Status quo (shared dir, single-tenant assumption)
                  All users share `/opt/liv-assistant/data/`. Chat
                  history + agent state co-mingles. Acceptable for
                  "operator + family-trusted users" but breaks isolation.
       Option B — Per-user volume-mount + multi-instance
                  Spawn one liv-assistant.service per active user,
                  port-allocate (3020 + N), mount `~/<user>/.liv-assistant/data/`
                  as the data dir. Requires Caddy per-subdomain routing
                  + systemd template unit (`liv-assistant@.service`).
                  Storage + port-coordination concern.
       Option C — Upstream AionUi multi-tenant mode (TBD)
                  AionUi v2.1.4 may or may not support a native
                  multi-tenant mode. Requires upstream feature audit
                  before commit. If supported, would reduce LivOS-side
                  work to per-user auth-token passthrough.

     Plus: per-user Claude subscription auth concern. v42 ships a single
     shared `/home/bruce/.claude/.credentials.json` (Phase 228-01 audit).
     Multi-user would need per-user credentials.json paths under each
     home dir, with the multi-instance liv-assistant.service pattern
     reading `$HOME/.claude/.credentials.json` per its own `HOME` env.

     ============================================================
     Sacred SHA invariant
     ============================================================
     Neither `.planning/PROJECT.md` nor `docs/v42-single-user-posture.md`
     lives under `liv/packages/core/`. Sacred SHA
     `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (blob SHA of
     `liv/packages/core/src/sdk-agent-runner.ts`) is UNCHANGED.
     Pre-commit hook PASSES. Verify with:

         git ls-files -s liv/packages/core/src/sdk-agent-runner.ts
         # → 100644 f3538e1d811992b782a9bb057d1b7f0a0189f95f 0 liv/packages/core/src/sdk-agent-runner.ts

     ============================================================
     No Mini PC contact in this plan
     ============================================================
     Phase 229 is REPO ONLY. No SSH, no curl against bruce.livinity.io,
     no `update.sh`. The phase has no SC that requires Mini PC state.
     This is a posture-recording phase, not a deploy phase.

     ============================================================
     Server4 / Server5 — DO NOT mention
     ============================================================
     v42 lives on Mini PC only (HARD RULE 2026-04-27). The new docs
     file may reference "Mini PC" but MUST NOT reference Server4 or
     Server5 in the multi-user discussion (multi-user activation is
     a Mini PC concern only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/v42-single-user-posture.md + insert v42 Posture section into .planning/PROJECT.md, single atomic commit</name>
  <files>docs/v42-single-user-posture.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 125-140 minimum — confirm the exact insertion point: `## Current State (post v22.0 — AGI Platform)` at line 127, closing paragraph at line 129, blank line at line 130, `### Validated (v23.0)` at line 131; if line numbers have drifted, locate the same anchors by string match before inserting)
    - .planning/ROADMAP.md lines 3556-3573 (Phase 229 scope + 4 SCs)
    - docs/liv-assistant-install.md (FULL — style reference for tone + table-driven runbook structure)
    - .planning/phases/228-claude-auth-bridge/228-01-PLAN.md (style reference for plan-shape: this plan mirrors 228-01's repo-only docs-append cadence)
    - MEMORY.md project memory section "Multi-User Architecture (v7.0)" — already loaded in conversation context; verify the 7 table names + schema path + RBAC location verbatim against the docs file's inventory before commit
  </read_first>
  <action>
**CRITICAL constraints:**
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` MUST remain UNCHANGED. Neither `.planning/PROJECT.md` nor `docs/v42-single-user-posture.md` lives under `liv/packages/core/`. Pre-commit hook PASSES. Confirm before commit: `git diff --cached --name-only | grep -E '^liv/packages/core/' || echo 'sacred safe'`.
- Mini PC ONLY for v42 — and this plan does NOT contact Mini PC at all. Pure repo edits.
- NO emojis anywhere in either file.
- NO marketing language. Tone = factual, decision-recording, future-aware. Match the runbook tone of `docs/liv-assistant-install.md`.
- Do NOT reference Server4 or Server5 (HARD RULE 2026-04-27). Multi-user activation is Mini-PC-only.
- Do NOT speculate about v43 ship dates or pick a "best" option among the three Liv Assistant isolation sketches. Phase 229 records the question, NOT the answer.

**Step A — Create `docs/v42-single-user-posture.md` (NEW FILE).**

Write the following content verbatim:

```markdown
# v42 Single-User Posture

> **What this is:** A reference doc recording v42's deliberate single-user posture decision, the v7.0 multi-user infrastructure preserved but inactive on Mini PC, and the open design questions deferred to v43.

## Decision

v42 ships single-user (operator = `bruce`) by default. All v7.0 multi-user code paths and database tables remain in the source tree and on the Mini PC database, but are not activated. The Settings → Users → Multi-user mode toggle exists in the UI but is gated behind a known prerequisite (see below).

## Rationale

The v42 milestone goal is replacing the legacy OpenClawOS chat surface with Liv Assistant (vendored upstream AionUi 2.1.4) — Phases 222 through 232. That scope is focused: vendor the upstream binary, wrap it in a systemd unit, wire Caddy `/liv` reverse-proxy + iframe-friendly CSP headers, mount it in the LivOS shell as a dock window, and verify the Claude subscription credential path. Re-activating multi-user mid-milestone would have added: (a) per-user Liv Assistant data isolation design work (open question — see Deferred section), (b) Caddy wildcard certificate setup blocked on `CF_API_TOKEN` configuration, (c) end-to-end multi-user UAT walk on Mini PC. Each of those is a self-contained workstream — handling them inside v42 would have diluted both. Phase 229 records the decision to keep them deferred so the rationale survives across resumes.

The v7.0 multi-user surfaces are preserved verbatim in source and on the Mini PC PostgreSQL database. Nothing was deleted. Activation is reversible by flipping the Settings UI toggle once the prerequisites are met.

## Preserved multi-user surfaces (v7.0 inventory)

| Surface | Location | State |
|---|---|---|
| PostgreSQL schema | `livos/packages/livinityd/source/modules/database/schema.sql` | shipped, applied, inactive |
| Tables | `users`, `sessions`, `user_preferences`, `system_settings`, `user_app_access`, `user_app_instances`, `invites` | present in `livos` database, admin row only |
| Admin auth resolver | `is-authenticated.ts` (`ctx.currentUser`) | resolves admin via legacy JWT mapping |
| RBAC enforcement | `adminProcedure` (role hierarchy: admin > member > guest) | shipped, single admin only |
| App sharing UI | `ShareAppDialog` component | shipped, no other users to share with |
| Login screen | `/login` route (avatar grid + password) | route exists, not currently the entry point |
| App gateway middleware | `server/index.ts` (Express subdomain interceptor) | shipped, single-user path active |
| Per-user Docker compose | `installForUser()` in `apps.ts` (unique containers / 10000+ ports / volumes) | shipped, single-user invocation only |
| tRPC route namespaces | `users.*`, `invites.*`, `userApps.*` | mounted, callable by admin |

The Liv Assistant systemd unit (`liv-assistant.service`, Phase 223-02) runs as `bruce` with `HOME=/home/bruce` and a single shared `/opt/liv-assistant/data/` dir (Phase 228-01 audit). Per-user isolation for the Liv Assistant surface is an open design question — see Deferred below.

## Deferred to v43

### Per-user Liv Assistant data isolation

Liv Assistant in v42 runs as one bruce-owned process at port 3020 with one shared data dir. Multi-user activation requires choosing how multiple operator personas isolate chat history, agent state, and per-user Claude credentials. Three concrete option sketches:

- **Option A — Shared dir (v42 status quo).** All users read/write `/opt/liv-assistant/data/`. Chat history co-mingles. Acceptable for "operator + family-trusted users" trust model, but breaks per-user isolation guarantees. No new infrastructure.
- **Option B — Per-user volume-mount + multi-instance.** One `liv-assistant@<user>.service` per active user via a systemd template unit, port-allocated (`3020 + N`), mounting `~/<user>/.liv-assistant/data/` as the data dir. Requires Caddy per-subdomain routing for the `/liv` handle (e.g. `/liv` on `<user>.livinity.io` proxies to that user's allocated port). Storage + port-coordination concern.
- **Option C — Upstream AionUi multi-tenant mode.** AionUi 2.1.4 may or may not support a native multi-tenant mode. Requires upstream feature audit (read upstream README + open issues + release notes) before commit. If supported, would reduce LivOS-side work to per-user auth-token passthrough on the existing single instance.

No option is selected. Phase 229 records the question.

### Per-user Claude subscription auth

v42 ships a single shared `/home/bruce/.claude/.credentials.json` (Phase 228-01 audit). Multi-user activation needs per-user credential paths under each `/home/<user>/.claude/`, with the per-user `liv-assistant@<user>.service` (Option B above) reading `$HOME/.claude/.credentials.json` per its own `HOME` env. Edge cases: a single Claude Max subscription cannot legally back multiple end-users — multi-user activation also implies each operator persona owns their own Claude subscription (or the operator decides their family-trusted users share one subscription, accepting the ToS implications). Phase 229 names the constraint; v43 picks a path.

### Caddy wildcard certificate + CF_API_TOKEN

Multi-user activation surfaces subdomain routes like `<user>.livinity.io`. Provisioning TLS for those requires either (a) a wildcard cert via DNS-01 challenge (needs `CF_API_TOKEN` exported into the Caddy systemd service env for the Cloudflare DNS provider module), or (b) per-subdomain on-demand TLS via HTTP-01 (already supported by Phase 19.0 custom-domain machinery, but each new user triggers a fresh Let's Encrypt request). The wildcard path is simpler and is the v7.0-era recommended setup, but the `CF_API_TOKEN` is not provisioned on Mini PC today.

### v43 announcement + migration runbook

When v43 ships, it needs: a posture announcement doc (mirroring this one but recording the activation decision), a migration runbook (operator steps to flip the toggle + provision `CF_API_TOKEN` + smoke-test per-user subdomain routing), and a rollback plan (how to revert to single-user if the activation goes sideways). None of those exist today.

## How to (eventually) enable multi-user

When the v43 prerequisites are met:

1. Provision `CF_API_TOKEN` in the Caddy systemd unit env (or via systemd `EnvironmentFile=` drop-in).
2. Pick one of Options A / B / C above for Liv Assistant data isolation; implement.
3. Pick a Claude subscription policy (per-user OAuth vs shared subscription with ToS acceptance); implement.
4. In LivOS UI, visit Settings → Users → Multi-user mode → toggle on.
5. Verify wildcard cert issuance: `curl -fsSI https://<new-user>.livinity.io/` after creating a new user via the invite flow.
6. Walk the v7.0 phase artifacts in `.planning/` for the original multi-user UAT steps (search for `multi-user` in the phase summaries — the v7.0 milestone shipped a full UAT walk).

## Related phases

- **Phase 228** (Claude auth bridge): shipped the audit that confirms the shared `/home/bruce/.claude/.credentials.json` path Liv Assistant reads today.
- **Phase 223** (vendor AionUi install): shipped the `liv-assistant.service` systemd unit, install runbook (`docs/liv-assistant-install.md`), and the single shared `/opt/liv-assistant/data/` dir.
- **Phase 226** (Caddy `/liv` proxy): shipped the iframe-friendly reverse proxy that today routes `bruce.livinity.io/liv` to the single shared Liv Assistant instance. Multi-user would extend this to per-subdomain `/liv` handles.
- **v7.0 milestone** (multi-user): the original ship that introduced the 7 PostgreSQL tables, the login screen, the invite system, the per-user Docker compose templating, and the app gateway middleware. All preserved.
```

The file MUST end with a single trailing newline (standard markdown convention). No emojis. No Server4 / Server5 references. No code edits outside the two files in `files_modified`.

**Step B — Insert `## v42 Posture: Single-User (Multi-User Deferred to v43)` section into `.planning/PROJECT.md`.**

Locate the `### Validated (v23.0)` header (currently line 131 per 2026-05-27 read; if drifted, locate by string match — the header is unique). Insert the following new section IMMEDIATELY BEFORE that header (preserving the existing blank line between the v22 narrative paragraph and the new section, and between the new section and `### Validated (v23.0)`):

```markdown
## v42 Posture: Single-User (Multi-User Deferred to v43)

v42 ships single-user (operator = `bruce`). All v7.0 multi-user code paths and database tables (PostgreSQL `users` / `sessions` / `invites` / `user_app_access` / `user_app_instances` / `user_preferences` / `system_settings` — 7 tables at `livos/packages/livinityd/source/modules/database/schema.sql`), the admin auth + RBAC resolver (`is-authenticated.ts` + `adminProcedure`), the login screen (`/login`), the invite system, the app sharing UI (`ShareAppDialog`), the app gateway middleware (`server/index.ts`), the per-user Docker compose templating (`installForUser()`), and the `users.*` / `invites.*` / `userApps.*` tRPC namespaces — all PRESERVED but INACTIVE. Activation is reversible via Settings → Users → Multi-user mode toggle once `CF_API_TOKEN` is provisioned for the Caddy wildcard cert (the known v7.0-era blocker).

v42's scope was the OpenClawOS → AionUi Liv Assistant swap (Phases 222-228 + 232). Multi-user re-activation requires additional design work that does not belong inside that scope — most notably the open question of per-user Liv Assistant data isolation (shared `/opt/liv-assistant/data/` vs per-user volume-mount + multi-instance vs upstream AionUi multi-tenant mode) and per-user Claude subscription auth. See `docs/v42-single-user-posture.md` for the full inventory and the v43 deferred design questions.
```

Verify after edit:
- The new section header `## v42 Posture: Single-User (Multi-User Deferred to v43)` appears exactly once in `.planning/PROJECT.md`.
- The existing `### Validated (v23.0)` header STILL appears (just moved further down).
- The existing `## Current State (post v22.0 — AGI Platform)` header STILL appears unchanged.
- The new section's last line contains `docs/v42-single-user-posture.md`.

**Step C — Local verification (before commit):**

Run these checks (PowerShell-friendly; use Bash if available, otherwise Bash tool):

```bash
# New docs file exists with required tokens:
test -f docs/v42-single-user-posture.md
grep -c '^# v42 Single-User Posture$' docs/v42-single-user-posture.md
# expect: 1
grep -c 'livos/packages/livinityd/source/modules/database/schema.sql' docs/v42-single-user-posture.md
# expect: >= 1
grep -c 'user_app_access' docs/v42-single-user-posture.md
# expect: >= 1
grep -c 'CF_API_TOKEN' docs/v42-single-user-posture.md
# expect: >= 1
grep -c 'per-user Liv Assistant' docs/v42-single-user-posture.md
# expect: >= 1
# All 7 v7.0 tables named in the new doc:
for tbl in users sessions user_preferences system_settings user_app_access user_app_instances invites; do
  grep -q "\`$tbl\`" docs/v42-single-user-posture.md || echo "MISSING: $tbl"
done
# expect: no MISSING output

# PROJECT.md insertion landed:
grep -c '^## v42 Posture: Single-User (Multi-User Deferred to v43)$' .planning/PROJECT.md
# expect: 1
grep -c '^## Current State (post v22.0 — AGI Platform)$' .planning/PROJECT.md
# expect: 1  (still present, not clobbered)
grep -c '^### Validated (v23.0)$' .planning/PROJECT.md
# expect: 1  (still present, just relocated)
grep -c 'docs/v42-single-user-posture.md' .planning/PROJECT.md
# expect: >= 1  (cross-link in new section)

# Sacred file guard:
git diff --name-only | grep -E '^liv/packages/core/' || echo 'sacred safe'
# expect: sacred safe
[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] && echo 'sacred SHA pinned'
# expect: sacred SHA pinned

# Stage and inspect:
git add docs/v42-single-user-posture.md .planning/PROJECT.md
git diff --cached --stat
# expect: 2 files changed
```

**Step D — Commit (single atomic commit):**

```
git commit -m "docs(229-01): record v42 single-user posture + v7.0 preservation + v43 deferred items

Phase 229 Plan 01 (Wave 1) — pure repo docs-only phase. No Mini PC contact.

Creates docs/v42-single-user-posture.md (NEW) capturing:
  - the v42 single-user posture DECISION
  - RATIONALE for keeping multi-user deferred (v42 milestone scope was the
    OpenClawOS to AionUi Liv Assistant swap, Phases 222 through 232)
  - INVENTORY of v7.0 multi-user surfaces preserved but inactive
    (7 PostgreSQL tables, admin auth + RBAC, app sharing UI, login route,
    app gateway middleware, per-user Docker compose, tRPC namespaces)
  - DEFERRED-TO-V43 open design questions:
      * per-user Liv Assistant data isolation (3 option sketches: shared
        dir / per-user volume-mount + multi-instance / AionUi multi-tenant)
      * per-user Claude subscription auth
      * Caddy wildcard cert + CF_API_TOKEN provisioning
      * v43 announcement + migration runbook
  - HOW-TO-ENABLE pointer for the eventual v43 activation path
  - RELATED PHASES cross-links (228 Claude auth bridge, 223 vendor install,
    226 Caddy /liv proxy, v7.0 milestone)

Adds a new section ## v42 Posture: Single-User (Multi-User Deferred to v43)
to .planning/PROJECT.md immediately before ### Validated (v23.0). The section
is 2 paragraphs: opening posture statement + v7.0 preservation inventory,
followed by scope rationale + link to the deferred docs file for full detail.

Phase 229 is a recording phase, not a deciding phase. The three Liv Assistant
isolation options are documented; none is selected. The Claude subscription
policy question is named; not answered.

Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f UNCHANGED. Neither file
touched lives under liv/packages/core/. Pre-commit hook PASSES.

Closes Phase 229 SC-01 (PROJECT.md has v42 single-user section), SC-02
(docs/v42-single-user-posture.md exists with rationale + deferred items),
SC-03 (sacred SHA unchanged), SC-04 (docs commit landed)."
```

Pre-commit hook must PASS (no `liv/packages/core/` paths touched).
  </action>
  <verify>
    <automated>test -f docs/v42-single-user-posture.md && [ "$(grep -c '^# v42 Single-User Posture$' docs/v42-single-user-posture.md)" -eq 1 ] && [ "$(grep -c 'livos/packages/livinityd/source/modules/database/schema.sql' docs/v42-single-user-posture.md)" -ge 1 ] && [ "$(grep -c 'user_app_access' docs/v42-single-user-posture.md)" -ge 1 ] && [ "$(grep -c 'CF_API_TOKEN' docs/v42-single-user-posture.md)" -ge 1 ] && [ "$(grep -c 'per-user Liv Assistant' docs/v42-single-user-posture.md)" -ge 1 ] && grep -q '`users`' docs/v42-single-user-posture.md && grep -q '`sessions`' docs/v42-single-user-posture.md && grep -q '`user_preferences`' docs/v42-single-user-posture.md && grep -q '`system_settings`' docs/v42-single-user-posture.md && grep -q '`user_app_access`' docs/v42-single-user-posture.md && grep -q '`user_app_instances`' docs/v42-single-user-posture.md && grep -q '`invites`' docs/v42-single-user-posture.md && [ "$(grep -c '^## v42 Posture: Single-User (Multi-User Deferred to v43)$' .planning/PROJECT.md)" -eq 1 ] && [ "$(grep -c '^## Current State (post v22.0 — AGI Platform)$' .planning/PROJECT.md)" -eq 1 ] && [ "$(grep -c '^### Validated (v23.0)$' .planning/PROJECT.md)" -eq 1 ] && [ "$(grep -c 'docs/v42-single-user-posture.md' .planning/PROJECT.md)" -ge 1 ] && git log -1 --name-only | grep -qE '^docs/v42-single-user-posture\.md$' && git log -1 --name-only | grep -qE '^\.planning/PROJECT\.md$' && ! git log -1 --name-only | grep -qE '^liv/packages/core/' && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/v42-single-user-posture.md` exists with H1 `# v42 Single-User Posture` (exactly once)
    - All 7 v7.0 table names appear in the new doc as backtick-quoted identifiers: `` `users` ``, `` `sessions` ``, `` `user_preferences` ``, `` `system_settings` ``, `` `user_app_access` ``, `` `user_app_instances` ``, `` `invites` ``
    - The schema path `livos/packages/livinityd/source/modules/database/schema.sql` appears at least once
    - `CF_API_TOKEN` is named at least once (blocker call-out)
    - The phrase `per-user Liv Assistant` appears at least once (deferred design question framing)
    - `.planning/PROJECT.md` contains the new section header `## v42 Posture: Single-User (Multi-User Deferred to v43)` exactly once
    - The existing `## Current State (post v22.0 — AGI Platform)` header is still present (not clobbered)
    - The existing `### Validated (v23.0)` header is still present (just relocated by the insertion)
    - The new PROJECT.md section contains a link to `docs/v42-single-user-posture.md`
    - Single atomic commit landed; `git log -1 --name-only` shows BOTH `docs/v42-single-user-posture.md` AND `.planning/PROJECT.md`
    - `git log -1 --name-only` does NOT include any path under `liv/packages/core/`
    - Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED (`git hash-object liv/packages/core/src/sdk-agent-runner.ts` returns the canonical SHA)
    - Pre-commit hook PASSED for the commit
    - No emojis anywhere in either file
    - No Server4 / Server5 references in either file (HARD RULE 2026-04-27)
  </acceptance_criteria>
  <done>v42 single-user posture is recorded in a dedicated docs file + a prominent PROJECT.md section. Future operators (and future Claude sessions resuming work) have a single source-of-truth doc naming the decision, the preserved v7.0 surfaces, and the v43 deferred design questions. Sacred SHA unchanged, one atomic commit landed. Phase 229 closes.</done>
</task>

</tasks>

<verification>
- The single task's `<verify>` block must PASS (echoes `PASS` at end of chain).
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged at end of plan (`git hash-object liv/packages/core/src/sdk-agent-runner.ts`).
- Git working tree clean post-commit (only the 2 staged files committed; no stray edits).
- No Mini PC contact in this plan (no SSH, no curl against bruce.livinity.io, no `update.sh` invocation).
- Pre-commit hook PASSED.
</verification>

<success_criteria>
- SC-01 (`.planning/PROJECT.md` has new section "v42 Posture: Single-User (Multi-User Deferred to v43)") — CLOSED by Step B.
- SC-02 (`docs/v42-single-user-posture.md` exists with rationale, deferred multi-user pieces inventory, per-user-data-isolation v43 thinking) — CLOSED by Step A.
- SC-03 (Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged) — held by the file scope (both files outside `liv/packages/core/`) + verified by the `git hash-object` check in the verify block.
- SC-04 (docs commit lands sacred-SHA hook PASS) — CLOSED by Step D + pre-commit hook PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/229-single-user-posture-docs/229-01-SUMMARY.md` capturing: line counts for the new docs file + PROJECT.md insertion, commit SHA, sacred SHA verify snapshot (pre + post), grep gate results (7 table names + schema path + CF_API_TOKEN + per-user Liv Assistant + 3 Liv Assistant option sketches present), and the per-SC verdict (4/4 PASS expected).
</output>
