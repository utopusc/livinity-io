---
phase: 231-openclawos-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/server/trpc/index.ts
  - livos/packages/livinityd/source/modules/server/trpc/common.ts
  - livos/packages/livinityd/source/modules/server/trpc/openclaw-router.ts
  - livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts
  - livos/packages/livinityd/source/modules/server/trpc/openclawos-router.test.ts
  - livos/packages/livinityd/source/modules/server/trpc/openclawos-gateway-router.ts
  - livos/packages/livinityd/source/modules/server/trpc/openclawos-gateway-router.test.ts
  - livos/packages/livinityd/source/modules/domain/caddy.ts
  - livos/packages/livinityd/source/modules/domain/caddy.test.ts
  - livos/packages/ui/src/providers/apps.tsx
  - livos/packages/ui/src/modules/desktop/dock.tsx
  - livos/packages/ui/src/modules/desktop/dock.test.tsx
  - livos/packages/ui/src/modules/window/window-content.tsx
  - livos/packages/liv-claw-os (git mv -> attic/liv-claw-os if exists; else N/A)
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04, SC-06]
tags: [v42, retirement, openclaw, tRPC, caddy, ui, attic, point-of-no-return]

must_haves:
  truths:
    - "Discovery artifact enumerates EVERY openclaw/openclawos/LIV_AI_CHAT source path BEFORE any edit (single grep-derived source of truth)"
    - "openclaw.* and openclawos.* tRPC namespaces are gone from the livinityd appRouter (imports + opts fields + router mounts all removed)"
    - "All openclaw.* and openclawos.* entries are removed from httpOnlyPaths in common.ts"
    - "OPENCLAWOS_HANDSHAKE_HANDLE constant + its 3 emit sites are removed from caddy.ts; the Phase 203-09/10/12 @livAiOpenclawos block is also removed"
    - "caddy.test.ts has the 3 Phase 203-05/09/10/12 describe blocks deleted, the Phase 226-04 port allow-list dropped :18789, plus 2 new Phase 231 negative-grep assertions added"
    - "LIV_AI_CHAT and LIV_AI_CHAT_SHORTCUT dock tiles + their window-content branch + their systemApps registry entries are all removed"
    - "dock.test.tsx Test 4 (Phase 227 coexistence) is deleted, plus 1 new negative-grep assertion confirms tile removal"
    - "If livos/packages/liv-claw-os/ exists as a workspace package, it is git mv'd to attic/liv-claw-os/ (preserves history)"
    - "Orphaned router/module source files (openclaw-router.ts, openclawos-router{,.test}.ts, openclawos-gateway-router{,.test}.ts, modules/openclawos/*, modules/openclaw-cli/*, agent-runtime/openclaw-client.{ts,test.ts}) are git rm'd ONLY when discovery confirms zero external consumers"
    - "pnpm --filter @livos/livinityd typecheck exits 0 after excision"
    - "pnpm --filter @livos/livinityd test -- modules/domain/caddy exits 0 (incl new Phase 231 negative-grep tests)"
    - "pnpm --filter @livos/ui typecheck + test -- dock.test.tsx exit 0"
    - "pnpm --filter @livos/config build && pnpm --filter ui build exit 0 (catches any dangling LIV_AI_CHAT import that grep missed)"
    - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f UNCHANGED on every commit (pre-commit hook gates it)"
  artifacts:
    - path: ".planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md"
      provides: "Single grep-derived enumeration of every openclaw* / openClaw* / OpenClaw* / OPENCLAWOS_* / LIV_AI_CHAT* path in the repo, with disposition column (REMOVE / MV_TO_ATTIC / DELETE_FILE / KEEP / N/A) per row -- produced BEFORE any edits"
      contains: "git ls-files | grep -iE 'claw|openclaw' enumeration + per-source-file grep output for the 7 known impacted files + zero-external-consumer audit for every DELETE_FILE row + a Commit plan section"
    - path: "livos/packages/livinityd/source/modules/server/trpc/index.ts"
      provides: "Excised tRPC appRouter -- openclaw/openclawos namespaces gone"
      excluded_patterns: ["openclawosApps", "openclawosGateway", "openclawCli", "openclawos:", "openclaw:", "createOpenclawosAppsRouter", "createOpenclawosGatewayRouter", "createOpenclawCliRouter"]
    - path: "livos/packages/livinityd/source/modules/server/trpc/common.ts"
      provides: "Excised httpOnlyPaths -- no openclaw.* or openclawos.* entries"
      excluded_patterns: ["openclawos.apps.", "openclawos.gateway.", "openclaw.providers.", "openclaw.models.", "openclaw.auth.", "openclaw.config.", "openclaw.profiles."]
    - path: "livos/packages/livinityd/source/modules/domain/caddy.ts"
      provides: "Excised Caddyfile generator -- no OPENCLAWOS_HANDSHAKE_HANDLE + no /liv-ai-app/openclawos handle"
      excluded_patterns: ["OPENCLAWOS_HANDSHAKE_HANDLE", "handle /openclawos/handshake", "@livAiOpenclawos", "/liv-ai-app/openclawos", "/plugins/openclawos"]
    - path: "livos/packages/livinityd/source/modules/domain/caddy.test.ts"
      provides: "Updated test suite -- Phase 203 describes deleted, port allow-list dropped :18789, 2 new Phase 231 negative-grep tests added"
      excluded_patterns: ["/openclawos/handshake", "@livAiOpenclawos", "/liv-ai-app/openclawos", "OPENCLAWOS_HANDSHAKE"]
    - path: "livos/packages/ui/src/modules/desktop/dock.tsx"
      provides: "Dock with LIV_AI_CHAT and LIV_AI_CHAT_SHORTCUT entries removed"
      excluded_patterns: ["LIV_AI_CHAT", "LIV_AI_CHAT_SHORTCUT"]
    - path: "livos/packages/ui/src/modules/window/window-content.tsx"
      provides: "Window-content with LIV_AI_CHAT_APP_ID branch + lazy import removed (LIV_ASSISTANT_APP_ID from Phase 227 stays)"
      excluded_patterns: ["LIV_AI_CHAT_APP_ID", "'LIV_AI_CHAT'"]
    - path: "livos/packages/ui/src/providers/apps.tsx"
      provides: "systemApps registry -- LIV_AI_CHAT entries removed (Phase 227 LIVINITY_liv-assistant entry stays)"
      excluded_patterns: ["'LIV_AI_CHAT'", "'LIV_AI_CHAT_SHORTCUT'"]
    - path: "livos/packages/ui/src/modules/desktop/dock.test.tsx"
      provides: "Updated dock smoke test -- Test 4 (Phase 227 coexistence) deleted, 1 new Phase 231 negative-grep test added"
      excluded_patterns: ["legacy LIV_AI_CHAT entry remains rendered"]
    - path: "attic/liv-claw-os/"
      provides: "Moved liv-claw-os/ tree via git mv (preserves history). If livos/packages/liv-claw-os/ doesn't exist, Task 5 is N/A and documents that explicitly."
  key_links:
    - from: "Task 1 DISCOVERY.md"
      to: "Tasks 2-5 source edits"
      via: "every disposition row drives an edit; no edit happens without a row"
      pattern: "Each Task N's commit message references DISCOVERY.md row IDs (e.g. 'per DISCOVERY rows R03-R12')"
    - from: "livos/packages/livinityd/source/modules/server/trpc/index.ts"
      to: "livos/packages/livinityd/source/index.ts (production boot wire-up)"
      via: "createAppRouter() consumer must still type-check after the 3 openclaw* opts are removed"
      pattern: "pnpm --filter @livos/livinityd typecheck exits 0"
    - from: "livos/packages/livinityd/source/modules/domain/caddy.ts"
      to: "livos/packages/livinityd/source/modules/domain/caddy.test.ts"
      via: "removing OPENCLAWOS_HANDSHAKE_HANDLE coordinates with deletion of the Phase 203-05/09/10/12 describes"
      pattern: "vitest run caddy.test.ts passes; grep '/openclawos/handshake' caddy.ts caddy.test.ts -> 0 matches"
    - from: "livos/packages/ui/src/modules/desktop/dock.tsx"
      to: "livos/packages/ui/src/modules/desktop/dock.test.tsx"
      via: "removing the two DockItem entries changes rendered tile count -- Test 4 is deleted in lock-step"
      pattern: "pnpm --filter @livos/ui test -- dock.test.tsx exits 0"
---

<objective>
Phase 231 Plan 01 -- **DISCOVERY + EXCISION**.

Land all source-code excision for the OpenClawOS retirement in ONE plan, with a discovery-first step that enumerates every affected path before any edit, then atomic commits per logical group: (a) tRPC routes + httpOnlyPaths, (b) Caddy emit + tests, (c) UI dock + window-content + apps registry + dock.test, (d) `liv-claw-os/` git-mv to `attic/` (if directory exists). All work is in-tree; deploy + verification is Plan 02.

Purpose: Phase 233 GREEN-gated Phase 231 on 2026-05-27 (UAT proved Liv Assistant fully replaces OpenClawOS end-to-end through the external relay path). Retire the now-superseded chat surface from the source tree. Phase 230 backup (`/opt/livos/backups/pre-v42-cutover-2026-05-27.tgz`, sha256 `ad532b80c5a1f8c43a307056b412a6b83c5d343edbf350000c707e023cd2f1d8`, 3.8 GB) provides full Mini PC rollback path if Plan 02 deploy regresses anything.

Output:
- `.planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md` -- grep-derived enumeration + disposition table
- Source edits across livinityd (tRPC + caddy.ts + tests) + UI (dock + window-content + apps + dock.test) + workspace mv
- 4-5 atomic commits (discovery+disposition, tRPC excision, caddy excision + tests, UI excision + tests, mv liv-claw-os -> attic)
- Typecheck + targeted vitest + UI build all GREEN locally before push
- Sacred SHA UNCHANGED on every commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/233-v42-e2e-uat/233-DEPLOY-LOG.md
@.planning/phases/230-pre-cutover-backup/230-02-DEPLOY-LOG.md
@.planning/phases/226-caddy-liv-proxy-iframe-headers/226-04-SUMMARY.md
@.planning/phases/227-livos-shell-livassistant-window/227-02-SUMMARY.md

@livos/packages/livinityd/source/modules/server/trpc/index.ts
@livos/packages/livinityd/source/modules/server/trpc/common.ts
@livos/packages/livinityd/source/modules/domain/caddy.ts
@livos/packages/livinityd/source/modules/domain/caddy.test.ts
@livos/packages/ui/src/providers/apps.tsx
@livos/packages/ui/src/modules/desktop/dock.tsx
@livos/packages/ui/src/modules/desktop/dock.test.tsx
@livos/packages/ui/src/modules/window/window-content.tsx

<interfaces>
<!-- Key constants + identifiers the executor will excise. Pre-extracted from the source so no scavenger-hunt is needed. -->

From `livos/packages/livinityd/source/modules/server/trpc/index.ts`:
- Imports to REMOVE (lines ~150-193):
  ```typescript
  import { createOpenclawosAppsRouter, openclawosAppsRouter } from './openclawos-router.js'
  import { createOpenclawosGatewayRouter, openclawosGatewayRouter } from './openclawos-gateway-router.js'
  import { createOpenclawCliRouter, openclawCliRouter } from './openclaw-router.js'
  ```
- createAppRouter opts fields to REMOVE (lines ~247-270):
  ```typescript
  openclawosApps?: ReturnType<typeof createOpenclawosAppsRouter>
  openclawosGateway?: ReturnType<typeof createOpenclawosGatewayRouter>
  openclawCli?: ReturnType<typeof createOpenclawCliRouter>
  ```
- router mount sites to REMOVE (lines ~378-404):
  ```typescript
  openclawos: router({ apps: opts.openclawosApps ?? openclawosAppsRouter,
                       gateway: opts.openclawosGateway ?? openclawosGatewayRouter }),
  openclaw: opts.openclawCli ?? openclawCliRouter,
  ```

From `livos/packages/livinityd/source/modules/server/trpc/common.ts`:
- httpOnlyPaths entries to REMOVE (lines ~379-743, 24 string entries):
  ```
  'openclawos.gateway.config.read', 'openclawos.gateway.config.write',
  'openclawos.apps.{list,get,create,update,delete,version}',
  'openclawos.gateway.devices.{list,revoke}',
  'openclawos.gateway.origins.{list,add,remove}',
  'openclawos.gateway.auth.{get,setMode,rotateToken}',
  'openclaw.providers.list', 'openclaw.models.list',
  'openclaw.auth.{status,setApiKey,logout}',
  'openclaw.config.{setDefaultModel,getDefaultModel}',
  'openclaw.profiles.list',
  ```
  Task 1 discovery enumerates ALL via `grep -nE "'openclaw" common.ts`.

From `livos/packages/livinityd/source/modules/domain/caddy.ts`:
- Constant to REMOVE (lines 303-307):
  ```typescript
  const OPENCLAWOS_HANDSHAKE_HANDLE = `\thandle /openclawos/handshake {
  \t\treverse_proxy 127.0.0.1:8080 {
  ${WS_TRANSPORT_BODY}
  \t}
  \t}`
  ```
- 3 emit sites to REMOVE (lines ~421, ~453, ~483):
  ```
  ${OPENCLAWOS_HANDSHAKE_HANDLE}
  ${apexCacheHeader}${OPENCLAWOS_HANDSHAKE_HANDLE}
  ${OPENCLAWOS_HANDSHAKE_HANDLE}
  ```
- ALSO REMOVE the `@livAiOpenclawos` matcher + handle block + the `/plugins/openclawos` rewrite block (Phase 203-09/10 directives, lines ~185-230 + emit sites). Discovery (Task 1) gives exact line ranges via `grep -n -E 'livAiOpenclawos|plugins/openclawos|liv-ai-app/openclawos' caddy.ts`.

From `livos/packages/livinityd/source/modules/domain/caddy.test.ts`:
- 3 describe blocks to DELETE WHOLESALE:
  - `describe('Phase 203-05 -- /openclawos/handshake handle (D-203-12 / INV-203-10)' ...)` (lines 21-122)
  - `describe('Phase 203-09 -- /liv-ai-app split: openclaw gateway vs Next.js subapp' ...)` (lines ~126-181)
  - `describe('Phase 203-10/12/Hot-fix-C -- gateway URL rewrite to /plugins/openclawos' ...)` (lines ~182-onwards)
- Phase 226-04 port allow-list (currently `[8080, 18789, 3010, 5678, 3020]`) -- if it survives outside the deleted describes, drop `18789` from the allow-list and from any positive `:18789` assertions (no emit produces this port anymore).

From `livos/packages/ui/src/modules/desktop/dock.tsx` (lines ~222-292):
- DockItem entries to REMOVE:
  - `<DockItem appId='LIV_AI_CHAT' ... onClick={() => launch('LIV_AI_CHAT', ...)} />`
  - `<DockItem appId='LIV_AI_CHAT_SHORTCUT' ... onClick={() => launch('LIV_AI_CHAT', ...)} />`
- Surrounding comment block at lines ~222-236 about Hot-fix F switching tiles to LIV_AI_CHAT window.

From `livos/packages/ui/src/modules/window/window-content.tsx` (lines ~31-152):
- `const LIV_AI_CHAT_APP_ID = 'LIV_AI_CHAT'` (line ~45)
- `LIV_AI_CHAT_APP_ID` from the `fullHeightApps` Set (line ~79) -- keep `LIV_ASSISTANT_APP_ID`
- `if (appId === LIV_AI_CHAT_APP_ID) { ... }` branch (line ~152 region)
- Comment block at lines ~31-32 + ~149 about the LIV_AI_CHAT appId + `/plugins/openclawos` rewrite
- The lazy import for the LIV_AI_CHAT iframe component (verify with grep -- if any other file imports the same component, keep the import there only)

From `livos/packages/ui/src/providers/apps.tsx`:
- systemApps entries with id `'LIV_AI_CHAT'` and `'LIV_AI_CHAT_SHORTCUT'` -- DELETE
- Keep `'LIVINITY_liv-ai'` and `'LIVINITY_liv-assistant'` (Phase 227 entry)

From `livos/packages/ui/src/modules/desktop/dock.test.tsx`:
- Test 4 (line ~186): `it('legacy LIV_AI_CHAT entry remains rendered alongside Liv Assistant (Phase 231 is the remover)' ...)` -- DELETE
- Header comment block bullet at line ~15: `4. Legacy LIV_AI_CHAT tile remains rendered ...` -- DELETE
- Other 3 tests (Liv Assistant present + click contract + flag-off hide) must keep passing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discovery -- enumerate every openclaw/openclawos/LIV_AI_CHAT surface in the repo</name>
  <files>.planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md</files>
  <action>
    Produce the discovery artifact BEFORE editing any source. Run (and capture verbatim output for each):

    ```bash
    # 1. Top-level filesystem enumeration
    git ls-files | grep -iE 'claw|openclaw'

    # 2. Source-code reference enumeration (per-file)
    grep -nE 'openclaw|openClaw|OpenClaw|OPENCLAWOS_HANDSHAKE' livos/packages/livinityd/source/modules/server/trpc/index.ts
    grep -nE "'openclaw|'openclawos" livos/packages/livinityd/source/modules/server/trpc/common.ts
    grep -nE 'OPENCLAWOS_HANDSHAKE|openclawos|@livAiOpenclawos|/plugins/openclawos|/liv-ai-app/openclawos' livos/packages/livinityd/source/modules/domain/caddy.ts
    grep -nE 'openclawos|/openclawos/|@livAiOpenclawos|LIV_AI_CHAT' livos/packages/livinityd/source/modules/domain/caddy.test.ts
    grep -nE 'LIV_AI_CHAT|openclaw' livos/packages/ui/src/providers/apps.tsx
    grep -nE 'LIV_AI_CHAT|openclaw' livos/packages/ui/src/modules/desktop/dock.tsx
    grep -nE 'LIV_AI_CHAT|openclaw' livos/packages/ui/src/modules/desktop/dock.test.tsx
    grep -nE 'LIV_AI_CHAT|openclaw' livos/packages/ui/src/modules/window/window-content.tsx

    # 3. livinityd production boot wire-up
    grep -nE 'openclaw|openclawos|createOpenclaw' livos/packages/livinityd/source/index.ts

    # 4. systemd + install-script enumeration
    grep -nE 'openclaw|claw-gateway' scripts/install/*.sh 2>/dev/null
    ls -la scripts/install/sudoers.d/ 2>/dev/null

    # 5. Zero-external-consumer audit for candidate DELETE_FILE rows
    grep -rn "from '.*openclawos-router'" livos/ --include='*.ts' --include='*.tsx'
    grep -rn "from '.*openclawos-gateway-router'" livos/ --include='*.ts' --include='*.tsx'
    grep -rn "from '.*trpc/openclaw-router'" livos/ --include='*.ts' --include='*.tsx'
    grep -rn "from '.*modules/openclawos/" livos/ --include='*.ts' --include='*.tsx'
    grep -rn "from '.*modules/openclaw-cli/" livos/ --include='*.ts' --include='*.tsx'
    grep -rn "from '.*agent-runtime/openclaw-client" livos/ --include='*.ts' --include='*.tsx'
    ```

    For each unique path/symbol discovered, add a row to a markdown table in DISCOVERY.md with columns:
    `| Row ID | Path | Lines | Disposition (REMOVE / MV_TO_ATTIC / DELETE_FILE / KEEP / N/A) | Rationale |`

    Disposition rules:
    - **MV_TO_ATTIC** -- `livos/packages/liv-claw-os/` (workspace package -- moves to `attic/liv-claw-os/`); same for `livos/packages/liv-claw-gateway/` IF it exists and IF discovery confirms zero on-Mini-PC dependency outside of the openclaw runtime.
    - **DELETE_FILE** -- `livos/packages/livinityd/source/modules/server/trpc/openclaw-router.ts`, `openclawos-router.ts`, `openclawos-router.test.ts`, `openclawos-gateway-router.ts`, `openclawos-gateway-router.test.ts` (standalone routers; only consumer is index.ts after Task 2's edit).
    - **DELETE_FILE** -- `livos/packages/livinityd/source/modules/openclawos/*` (entire dir, ~15 files) IF the zero-external-consumer audit in step 5 returns nothing outside this dir.
    - **DELETE_FILE** -- `livos/packages/livinityd/source/modules/openclaw-cli/*`, `livos/packages/livinityd/source/modules/agent-runtime/openclaw-client.{ts,test.ts}` IF zero external consumers.
    - **DELETE_FILE** -- `scripts/install/install-openclaw-cli.sh`, `scripts/install/sudoers.d/livos-claw-gateway` (orphaned after Plan 02 masks the service).
    - **REMOVE** -- in-file line/block excisions in KEPT files (`index.ts`, `common.ts`, `caddy.ts`, `caddy.test.ts`, the 4 UI files, livinityd `index.ts` boot wire-up).
    - **KEEP** -- historical `.planning/` artifacts (immutable history). The Phase 231 PLAN itself is an N/A row.
    - **N/A** -- Plan 231 own files (this PLAN, DEPLOY-LOG, DISCOVERY).

    For every DELETE_FILE row, the zero-external-consumer audit (grep set #5 above) MUST show no out-of-scope consumer. If any out-of-scope consumer surfaces, add a "SCOPE EXPANSION" row to DISCOVERY.md: either widen the plan to include the consumer, OR keep the file with a TODO note explaining why it survives.

    DISCOVERY.md ends with a `## Commit plan` section enumerating the 4-5 atomic commits Plan 01 will produce, with the file-set per commit derived from the disposition table. Suggested commit shape:

    1. `docs(231-01): discovery + disposition table for OpenClawOS retirement` (this artifact + Task 1 only)
    2. `feat(231-01): excise openclaw/openclawos tRPC routes + httpOnlyPaths` (Task 2)
    3. `feat(231-01): remove OPENCLAWOS_HANDSHAKE + /liv-ai-app/openclawos handle from caddy.ts + tests` (Task 3)
    4. `feat(231-01): remove LIV_AI_CHAT dock + window-content + systemApp entries` (Task 4)
    5. `chore(231-01): mv livos/packages/liv-claw-os -> attic/liv-claw-os (preserve history)` (Task 5, if applicable)
  </action>
  <verify>
    <automated>test -f .planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md &amp;&amp; grep -q '^| Row ID' .planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md &amp;&amp; grep -q '^## Commit plan' .planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md &amp;&amp; grep -qE 'DELETE_FILE|MV_TO_ATTIC|REMOVE' .planning/phases/231-openclawos-retirement/231-01-DISCOVERY.md</automated>
  </verify>
  <done>
    DISCOVERY.md contains: (a) verbatim grep output sections, (b) row-ID disposition table covering every grep match, (c) zero-external-consumer audit results for every DELETE_FILE row, (d) Commit plan section with 4-5 atomic commits. No source edits yet -- this task is purely investigative.
    Atomic commit: `docs(231-01): discovery + disposition table for OpenClawOS retirement`. Sacred SHA UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Excise tRPC routes -- drop openclaw/openclawos namespaces from livinityd appRouter + httpOnlyPaths</name>
  <files>
    livos/packages/livinityd/source/modules/server/trpc/index.ts,
    livos/packages/livinityd/source/modules/server/trpc/common.ts,
    livos/packages/livinityd/source/index.ts (boot wire-up -- remove openclaw* opt passthroughs),
    plus all DELETE_FILE rows from DISCOVERY.md (likely: trpc/openclaw-router.ts, trpc/openclawos-router{,.test}.ts, trpc/openclawos-gateway-router{,.test}.ts, modules/openclawos/*, modules/openclaw-cli/*, agent-runtime/openclaw-client.{ts,test.ts})
  </files>
  <action>
    Apply the tRPC excision drawn from DISCOVERY.md row IDs:

    1. **index.ts (trpc/)** -- delete the 3 `import { create<Openclaw...>Router, <openclaw...>Router } from './<router>.js'` blocks at lines ~150-193 (see `<interfaces>` above). Delete the 3 opts fields (`openclawosApps?`, `openclawosGateway?`, `openclawCli?`) at lines ~247-270. Delete the `openclawos: router({ apps: ..., gateway: ... })` and `openclaw: opts.openclawCli ?? openclawCliRouter` mount sites at lines ~378-404 along with their explanatory comment blocks.

    2. **common.ts httpOnlyPaths** -- delete every string entry beginning with `openclaw.` or `openclawos.` (24 entries enumerated in `<interfaces>`). Preserve surrounding comment-block structure for adjacent non-openclaw entries (Phase 219/220/221 entries bracket the openclaw block).

    3. **Production boot wire-up** `livos/packages/livinityd/source/index.ts` -- grep for `openclawosApps:`, `openclawosGateway:`, `openclawCli:` in the `createAppRouter({...})` call site (DISCOVERY row IDs identify exact lines). Remove those 3 opts plus any preparatory `const ...openclaw... = createOpenclaw...Router(...)` lines that become orphaned. Remove the corresponding `createOpenclaw*Router` imports from livinityd index.ts as well.

    4. **`git rm` every DELETE_FILE row from DISCOVERY.md.** Standard set (verify each row was confirmed by zero-external-consumer audit):
       - `livos/packages/livinityd/source/modules/server/trpc/openclaw-router.ts`
       - `livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts`
       - `livos/packages/livinityd/source/modules/server/trpc/openclawos-router.test.ts`
       - `livos/packages/livinityd/source/modules/server/trpc/openclawos-gateway-router.ts`
       - `livos/packages/livinityd/source/modules/server/trpc/openclawos-gateway-router.test.ts`
       - `livos/packages/livinityd/source/modules/openclawos/` (entire dir if discovery confirmed)
       - `livos/packages/livinityd/source/modules/openclaw-cli/` (entire dir if discovery confirmed)
       - `livos/packages/livinityd/source/modules/agent-runtime/openclaw-client.{ts,test.ts}` (if discovery confirmed)
       - `scripts/install/install-openclaw-cli.sh`
       - `scripts/install/sudoers.d/livos-claw-gateway`

    5. **Run typecheck** -- `pnpm --filter @livos/livinityd typecheck`. If errors surface from a not-yet-deleted file importing a deleted router:
       - (a) That file is itself orphan -> add to DELETE_FILE in DISCOVERY.md + delete now (document the late-binding in commit message);
       - (b) That file has non-openclaw consumers -> keep the file, surgically remove openclaw-only imports/exports (DISCOVERY.md scope expansion row).

    6. **Run surviving tests** -- `pnpm --filter @livos/livinityd test -- modules/server/trpc --reporter=basic`. Expect 5 deleted test files + reduced test count, NOT a regression in surviving tests.

    7. **Sacred SHA check** -- `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST print `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If it changed, ABORT and investigate (the file is in `liv/`, not `livos/`, so this excision should not touch it).

    Atomic commit: `feat(231-01): excise openclaw/openclawos tRPC routes + httpOnlyPaths`. Pre-commit hook gates sacred SHA.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/livinityd typecheck 2>&amp;1 | tail -20 &amp;&amp; cd .. &amp;&amp; ! grep -E "openclaw|openclawos" livos/packages/livinityd/source/modules/server/trpc/index.ts &amp;&amp; ! grep -E "'openclaw|'openclawos" livos/packages/livinityd/source/modules/server/trpc/common.ts &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    livinityd typecheck exits 0 with no openclaw-related errors. `grep -E "openclaw|openclawos" trpc/index.ts trpc/common.ts` returns zero matches. Deleted router source files are absent from `git ls-files`. Sacred SHA UNCHANGED. Atomic commit on master.
  </done>
</task>

<task type="auto">
  <name>Task 3: Excise Caddy /openclawos/handshake emit + /liv-ai-app/openclawos handle + update caddy.test.ts</name>
  <files>
    livos/packages/livinityd/source/modules/domain/caddy.ts,
    livos/packages/livinityd/source/modules/domain/caddy.test.ts
  </files>
  <action>
    1. **caddy.ts -- remove OPENCLAWOS_HANDSHAKE_HANDLE constant** (lines 303-307, exact shape in `<interfaces>`) AND its 3 emit sites at lines ~421, ~453, ~483. The emit sites currently embed `${OPENCLAWOS_HANDSHAKE_HANDLE}` between other handles -- delete the interpolation expression and surrounding whitespace/newlines as needed so the resulting template string is still well-formed Caddy v2.

    2. **caddy.ts -- remove the Phase 203-09/10/12 LIV-AI-app openclawos block.** Discovery (Task 1) listed exact line ranges; the block contains:
       - `@livAiOpenclawos path /liv-ai-app/openclawos /liv-ai-app/openclawos/*` matcher
       - `handle @livAiOpenclawos { reverse_proxy 127.0.0.1:18789 { ... } }` block
       - `handle_path /liv-ai-app/openclawos/* { rewrite * /plugins/openclawos{path} ... reverse_proxy 127.0.0.1:18789 ... }` block (whichever shape currently exists post-Hot-fix-C)
       - Any helper constant analogous to OPENCLAWOS_HANDSHAKE_HANDLE that holds these directives
       - The surrounding doc-comment block (lines ~185-230) explaining openclawos routing -- replace with a single retirement note: `// Phase 231 retirement -- OpenClawOS routing removed; Liv Assistant (Phase 226-04 LIV_ASSISTANT_HANDLE) is the v42 chat surface.`

    3. **caddy.test.ts -- delete 3 describe blocks WHOLESALE:**
       - `describe('Phase 203-05 -- /openclawos/handshake handle (D-203-12 / INV-203-10)', () => { ... })` (lines 21-122)
       - `describe('Phase 203-09 -- /liv-ai-app split: openclaw gateway vs Next.js subapp', () => { ... })` (~lines 126-181)
       - `describe('Phase 203-10/12/Hot-fix-C -- gateway URL rewrite to /plugins/openclawos', () => { ... })` (~lines 182-onwards)

    4. **caddy.test.ts -- update Phase 226-04 port allow-list.** The current assertion (now part of the deleted Phase 203-09 describe):
       ```typescript
       for (const port of portMatches) {
         expect([8080, 18789, 3010, 5678, 3020]).toContain(port)
       }
       ```
       The entire enclosing test goes with the deleted describe in step 3. If a separate allow-list assertion lives inside the Phase 226-04 describe (`describe('Phase 226-04 -- /liv reverse-proxy handle (regen-survivable)', ...)`), drop `:18789` from that allow-list and from any positive `:18789` assertions (no emit produces this port anymore). vitest fail output is the discovery mechanism.

    5. **caddy.test.ts -- confirm Phase 226-04 LIV_ASSISTANT_HANDLE tests still pass** (the 9 assertions). The anchor pattern documented in Phase 226-04 SUMMARY (`'\thandle {\n\t\treverse_proxy 127.0.0.1:8080'` for the catch-all) MAY need updating if OPENCLAWOS_HANDSHAKE removal changed surrounding context -- vitest fail output surfaces any anchor mismatch.

    6. **Add Phase 231 negative-grep guard describe** at the end of caddy.test.ts:
       ```typescript
       describe('Phase 231 -- OpenClawOS handles excised', () => {
         it('generated Caddyfile contains zero /openclawos/handshake references', () => {
           const out = generateFullCaddyfile({mainDomain: 'bruce.livinity.io', subdomains: []}, false, false, [])
           expect(out).not.toContain('/openclawos/handshake')
           expect(out).not.toContain('@livAiOpenclawos')
           expect(out).not.toContain('/plugins/openclawos')
           expect(out).not.toContain('/liv-ai-app/openclawos')
         })
         it('generated Caddyfile contains zero :18789 reverse_proxy targets', () => {
           const out = generateFullCaddyfile({mainDomain: 'bruce.livinity.io', subdomains: []}, false, false, [])
           expect(out).not.toContain('127.0.0.1:18789')
         })
       })
       ```
       These lock in the excision against accidental reintroduction.

    7. **Run** `pnpm --filter @livos/livinityd test -- modules/domain/caddy --reporter=basic`. All surviving tests + the 2 new Phase 231 negative-grep tests must PASS. Sacred SHA UNCHANGED.

    Atomic commit: `feat(231-01): remove OPENCLAWOS_HANDSHAKE + /liv-ai-app/openclawos handle from caddy.ts + tests`.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/livinityd test -- modules/domain/caddy --reporter=basic 2>&amp;1 | tail -20 &amp;&amp; cd .. &amp;&amp; ! grep -E "OPENCLAWOS_HANDSHAKE|openclawos/handshake|@livAiOpenclawos|/plugins/openclawos" livos/packages/livinityd/source/modules/domain/caddy.ts &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    caddy.ts grep for OPENCLAWOS / openclawos / @livAiOpenclawos / /plugins/openclawos returns ZERO matches. caddy.test.ts vitest PASS -- every surviving test green + 2 new Phase 231 negative-grep assertions GREEN. Sacred SHA UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove LIV_AI_CHAT dock + window-content + apps + dock.test entries (UI surface)</name>
  <files>
    livos/packages/ui/src/providers/apps.tsx,
    livos/packages/ui/src/modules/desktop/dock.tsx,
    livos/packages/ui/src/modules/window/window-content.tsx,
    livos/packages/ui/src/modules/desktop/dock.test.tsx
  </files>
  <action>
    1. **apps.tsx** -- delete the 2 systemApps entries with id `'LIV_AI_CHAT'` and `'LIV_AI_CHAT_SHORTCUT'` (DISCOVERY.md row IDs give exact line ranges). Keep `'LIVINITY_liv-ai'` (legacy non-chat AI page) and `'LIVINITY_liv-assistant'` (Phase 227-02). Verify with `grep "LIV_AI_CHAT" livos/packages/ui/src/providers/apps.tsx` -> 0 matches.

    2. **dock.tsx** -- delete the 2 DockItem JSX blocks at lines ~265-292 (`appId='LIV_AI_CHAT'` + `appId='LIV_AI_CHAT_SHORTCUT'`). Also delete the surrounding comment block at lines ~222-236 ("Hot-fix F switches both dock tiles to open the LIV_AI_CHAT window") -- no longer relevant.

    3. **window-content.tsx** -- delete:
       - `const LIV_AI_CHAT_APP_ID = 'LIV_AI_CHAT'` (line ~45)
       - Comment block at lines ~31-32 about the LIV_AI_CHAT appId
       - `LIV_AI_CHAT_APP_ID` from the `fullHeightApps` Set (line ~79) -- KEEP `LIV_ASSISTANT_APP_ID`
       - The `if (appId === LIV_AI_CHAT_APP_ID) { ... }` branch (line ~152 region)
       - Comment block at line ~149 about `/plugins/openclawos` rewrite
       - The lazy import for the LIV_AI_CHAT iframe component (verify with grep first -- if any other file imports the same component, keep the import declaration there only)

    4. **dock.test.tsx** -- delete Test 4 at line ~186 (`it('legacy LIV_AI_CHAT entry remains rendered alongside Liv Assistant (Phase 231 is the remover)' ...)`). Update the file-header comment block (line ~15) -- drop the bullet `4. Legacy LIV_AI_CHAT tile remains rendered in both states (Phase 231 ...)`. The remaining 3 tests (Liv Assistant present + click contract + flag-off hide) must still pass.

    5. **Add Phase 231 negative-grep assertion** at the end of the dock.test.tsx describe:
       ```typescript
       it('legacy LIV_AI_CHAT dock tile has been removed (Phase 231 retirement)', () => {
         renderDock()
         expect(document.querySelector('[data-test-dock-item="LIV_AI_CHAT"]')).toBeNull()
         expect(document.querySelector('[data-test-dock-item="LIV_AI_CHAT_SHORTCUT"]')).toBeNull()
       })
       ```
       Use the rendering helper already defined in dock.test.tsx (DISCOVERY.md confirms shape).

    6. **Run** `pnpm --filter @livos/ui typecheck` AND `pnpm --filter @livos/ui test -- dock.test.tsx --reporter=basic`. Both must pass with sacred SHA UNCHANGED.

    7. **Final UI build smoke** -- `cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build 2>&amp;1 | tail -10`. Build must succeed (catches any dangling LIV_AI_CHAT import elsewhere in the UI bundle that grep missed).

    Atomic commit: `feat(231-01): remove LIV_AI_CHAT dock/window/systemApp entries (legacy openclaw chat surface gone)`.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/ui typecheck 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter @livos/ui test -- dock.test.tsx --reporter=basic 2>&amp;1 | tail -10 &amp;&amp; cd .. &amp;&amp; ! grep -rE "LIV_AI_CHAT" livos/packages/ui/src --include='*.ts' --include='*.tsx' &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    UI typecheck + dock.test.tsx vitest PASS. `grep -rE "LIV_AI_CHAT" livos/packages/ui/src` returns ZERO matches. UI build (vite) succeeds. Sacred SHA UNCHANGED. Atomic commit on master.
  </done>
</task>

<task type="auto">
  <name>Task 5: `git mv livos/packages/liv-claw-os/ attic/liv-claw-os/` (preserve history) -- or N/A if directory absent</name>
  <files>
    attic/liv-claw-os/ (created from livos/packages/liv-claw-os via git mv),
    livos/packages/liv-claw-os/ (deleted),
    pnpm-workspace.yaml (drop `livos/packages/liv-claw-os` entry if listed),
    livos/packages/livinityd/package.json (drop liv-claw-os dependency if listed)
  </files>
  <action>
    DISCOVERY.md row R-LIVCLAW determines whether this task runs.

    1. **Confirm directory exists** -- `test -d livos/packages/liv-claw-os && echo PRESENT || echo ABSENT`. If ABSENT, write a single-line note to DEPLOY-LOG.md (Plan 02 will append): `Task 5 N/A -- livos/packages/liv-claw-os/ does not exist (already retired in a prior phase).` and skip to step 5.

    2. **Create attic/ dir if it doesn't exist** -- `mkdir -p attic/` (no `git add attic` needed -- git mv creates the destination).

    3. **`git mv livos/packages/liv-claw-os/ attic/liv-claw-os/`** -- this preserves full git history of the package's ~150+ files. Verify post-mv with `git log --follow attic/liv-claw-os/package.json | head -5` (should show original commits, not just the mv).

    4. **Update workspace declarations:**
       - `pnpm-workspace.yaml` -- if it lists `livos/packages/liv-claw-os` (likely a glob `livos/packages/*` covers it, in which case the mv to `attic/` automatically excludes it because `attic/` is not in the workspace glob). Verify: `cat pnpm-workspace.yaml`. If an explicit entry exists, remove it.
       - `livos/packages/livinityd/package.json` -- if it depends on `@livos/liv-claw-os` or similar, remove the dependency (Task 2 already excised the routers consuming it; the package.json dep is the last link).
       - `livos/packages/liv-claw-gateway/package.json` -- if it depends on the moved `@livos/liv-claw-os`, decide: (a) also `git mv livos/packages/liv-claw-gateway -> attic/liv-claw-gateway` per DISCOVERY row, OR (b) keep liv-claw-gateway in place but stub its broken imports (NOT recommended -- the gateway is retired by definition; prefer mv to attic).
       - Likely outcome: ALSO `git mv livos/packages/liv-claw-gateway/ attic/liv-claw-gateway/` in the same task, per DISCOVERY row.
       - `pnpm install` to refresh lockfile (`pnpm-lock.yaml` will update).

    5. **Re-run final typecheck across all impacted packages:**
       ```
       pnpm --filter @livos/livinityd typecheck
       pnpm --filter @livos/ui typecheck
       pnpm --filter @livos/config typecheck
       ```
       All must exit 0. If any fail, the mv broke a still-living import -- investigate before committing.

    6. **Sacred SHA check** -- `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST equal `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. The mv is in `livos/`, not `liv/`, so this should hold.

    Atomic commit: `chore(231-01): mv livos/packages/liv-claw-os -> attic/liv-claw-os (preserve history; gateway also attic'd)`.
  </action>
  <verify>
    <automated>test -d attic/liv-claw-os || test ! -d livos/packages/liv-claw-os &amp;&amp; ! test -d livos/packages/liv-claw-os &amp;&amp; cd livos &amp;&amp; pnpm --filter @livos/livinityd typecheck 2>&amp;1 | tail -10 &amp;&amp; pnpm --filter @livos/ui typecheck 2>&amp;1 | tail -10 &amp;&amp; cd .. &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    Either: (a) `attic/liv-claw-os/` exists, `livos/packages/liv-claw-os/` is gone, typecheck on all 3 packages PASSES, sacred SHA UNCHANGED, atomic commit on master. OR (b) Task 5 documented N/A in DEPLOY-LOG.md because the source dir was already absent and no work was needed.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks complete locally (before push):

```bash
# 1. Sacred SHA hard-gate
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# Expected: f3538e1d811992b782a9bb057d1b7f0a0189f95f

# 2. Zero openclaw/openclawos in active source (not .planning/, not attic/)
git ls-files | grep -v '^\.planning/' | grep -v '^attic/' | xargs grep -lE 'openclaw|openclawos|OPENCLAWOS_HANDSHAKE|LIV_AI_CHAT' 2>/dev/null | grep -v PLAN.md | grep -v DEPLOY-LOG | grep -v DISCOVERY | grep -v SUMMARY
# Expected: zero output (all live source references are gone; .planning/attic kept)

# 3. Workspace-wide typecheck
cd livos && pnpm --filter @livos/livinityd typecheck && pnpm --filter @livos/ui typecheck && pnpm --filter @livos/config typecheck

# 4. Targeted vitest
pnpm --filter @livos/livinityd test -- modules/domain/caddy --reporter=basic
pnpm --filter @livos/ui test -- dock.test.tsx --reporter=basic

# 5. Full UI build
pnpm --filter @livos/config build && pnpm --filter ui build

# 6. Git status -- expect 4-5 staged atomic commits ready for push
git log --oneline -10
```

Push to origin/master once all checks GREEN. Plan 02 picks up from `bash /opt/livos/update.sh` on Mini PC.
</verification>

<success_criteria>
- [ ] DISCOVERY.md exists with row-ID disposition table + zero-external-consumer audit + Commit plan
- [ ] livinityd typecheck PASSES with zero openclaw/openclawos references in active source
- [ ] caddy.test.ts PASSES including 2 new Phase 231 negative-grep tests
- [ ] dock.test.tsx PASSES including 1 new Phase 231 negative-grep test
- [ ] UI vite build PASSES (no dangling LIV_AI_CHAT imports)
- [ ] If livos/packages/liv-claw-os/ existed, it now lives at attic/liv-claw-os/ (history preserved)
- [ ] 4-5 atomic commits on master, each with sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED
- [ ] All atomic commits pushed to origin/master ready for Plan 02 deploy
</success_criteria>

<output>
After completion, create `.planning/phases/231-openclawos-retirement/231-01-SUMMARY.md` covering:
- Task-by-task delivery (commit hashes per atomic commit)
- Lines added / removed per file group (tRPC, Caddy, UI, attic mv)
- DISCOVERY.md row count + disposition breakdown (REMOVE / DELETE_FILE / MV_TO_ATTIC / KEEP / N/A counts)
- Typecheck + vitest + UI build evidence (last-10 lines of each)
- Sacred SHA evidence per commit (`git log --format='%h %s' -10` + `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs)
- Any scope expansions surfaced during discovery (DISCOVERY.md row IDs)
- Handoff note to Plan 02: "Push range XYZ..ABC ready; Mini PC `bash /opt/livos/update.sh` invocation will regenerate Caddyfile without OPENCLAWOS handles."
</output>
