---
phase: v2.0-08-app-store-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/app-store/discover.tsx
  - livos/packages/ui/src/modules/app-store/discover/*.tsx
autonomous: true

must_haves:
  truths:
    - "App store has a hero featured section at the top"
    - "Categories shown as horizontal pill/chip bar"
    - "App sections display as horizontal scroll rows (Netflix-style)"
    - "App cards show icon, name, tagline, status, action button"
---

<objective>
Redesign App Store from generic grid to an engaging marketplace with hero section, category pills, and horizontal scroll rows.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add hero featured section</name>
  <files>livos/packages/ui/src/routes/app-store/discover.tsx or equivalent discover page</files>
  <action>
Read the discover page file. Add a hero section at the very top of the page, BEFORE any existing sections.

The hero should be:
```tsx
<div className='mb-8 overflow-hidden rounded-radius-lg bg-gradient-to-br from-brand/20 to-brand/5 border border-border-subtle p-8'>
    <div className='flex items-center gap-6'>
        {/* Featured app icon */}
        <div className='h-20 w-20 shrink-0 rounded-radius-md bg-surface-2 flex items-center justify-center'>
            <span className='text-3xl'>🌐</span>
        </div>
        <div className='flex flex-col gap-2'>
            <span className='text-caption text-brand font-medium'>Featured</span>
            <h2 className='text-heading-lg text-text-primary'>Nextcloud</h2>
            <p className='text-body text-text-secondary'>Your personal cloud storage, calendar, and collaboration platform</p>
            <button className='mt-2 w-fit rounded-radius-sm bg-brand px-4 py-2 text-body font-medium text-white transition-colors hover:bg-brand-lighter'>
                Install
            </button>
        </div>
    </div>
</div>
```

The hero can show the first item from the gallery/featured section, or a hardcoded featured app. Read the existing data sources to determine what's available.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add category pill bar</name>
  <files>livos/packages/ui/src/routes/app-store/discover.tsx</files>
  <action>
Add a horizontal scrollable category pill bar below the hero:

```tsx
<div className='mb-6 flex gap-2 overflow-x-auto pb-2'>
    {categories.map((cat) => (
        <button
            key={cat}
            className={cn(
                'shrink-0 rounded-full px-4 py-1.5 text-body-sm font-medium transition-colors',
                activeCat === cat
                    ? 'bg-text-primary text-[#111111]'
                    : 'bg-surface-1 text-text-secondary hover:bg-surface-2 hover:text-text-primary'
            )}
            onClick={() => setActiveCat(cat)}
        >
            {cat}
        </button>
    ))}
</div>
```

Categories: ['All', 'Media', 'Productivity', 'Development', 'Network', 'Security', 'AI', 'Other']

Use useState for active category. When a category is selected, filter the displayed sections.
  </action>
</task>

<task type="auto">
  <name>Task 3: Update section layouts to horizontal scroll</name>
  <files>livos/packages/ui/src/modules/app-store/discover/*.tsx</files>
  <action>
Read the existing section components (apps-grid-section.tsx, apps-row-section.tsx, apps-three-column-section.tsx).

For each section type, ensure the app cards display in a horizontal scrollable row:

```tsx
<div className='mb-8'>
    <div className='flex items-center justify-between mb-3'>
        <h3 className='text-heading-sm text-text-primary'>{title}</h3>
        <button className='text-caption text-brand hover:text-brand-lighter'>See All</button>
    </div>
    <div className='flex gap-3 overflow-x-auto pb-2'>
        {apps.map(app => <AppCard key={app.id} app={app} />)}
    </div>
</div>
```

This creates the Netflix-style horizontal scroll pattern.
  </action>
</task>

<task type="auto">
  <name>Task 4: Improve app card design</name>
  <files>livos/packages/ui/src/modules/app-store/discover/*.tsx</files>
  <action>
Update app cards to show more information:

```tsx
<div className='flex w-48 shrink-0 flex-col gap-2 rounded-radius-md border border-border-subtle bg-surface-base p-3 transition-colors hover:bg-surface-1'>
    <div className='flex items-start gap-3'>
        <img src={app.icon} alt={app.name} className='h-12 w-12 rounded-radius-sm' />
        <div className='min-w-0 flex-1'>
            <h4 className='truncate text-body font-medium text-text-primary'>{app.name}</h4>
            <p className='truncate text-caption text-text-tertiary'>{app.tagline || app.developer}</p>
        </div>
    </div>
    <button className='w-full rounded-radius-sm bg-surface-2 px-3 py-1.5 text-body-sm font-medium text-text-primary transition-colors hover:bg-surface-3'>
        {app.installed ? 'Open' : 'Install'}
    </button>
</div>
```

Adjust based on actual app data properties available.
  </action>
</task>

</tasks>

<verification>
1. Hero section visible at top of app store
2. Category pills scrollable horizontally
3. App sections use horizontal scroll layout
4. App cards show icon, name, tagline, action button
5. Run `cd livos && pnpm build`
</verification>
