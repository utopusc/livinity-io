---
phase: 202-agents-platform
plan: 06
type: execute
wave: 2
depends_on: [202-05]
files_modified:
  - livos/packages/liv-ai-app/app/agents/new/page.tsx (NEW)
  - livos/packages/liv-ai-app/components/agents/CronPicker.tsx (NEW)
  - livos/packages/liv-ai-app/components/agents/ToolPicker.tsx (NEW)
  - livos/packages/liv-ai-app/components/agents/SubAgentPicker.tsx (NEW)
  - livos/packages/liv-ai-app/components/agents/ModelPicker.tsx (NEW — reuses Plan 201-04 LivAiModelPicker shape)
autonomous: true
requirements: [REQ-202-06]
tags: [frontend, agents-new, form, wave-2]
must_haves:
  truths:
    - "Route `/agents/new` shows the create form"
    - "CronPicker offers presets (every 15m / hourly / daily 09:00 / weekly Mon 09:00) + free-form input"
    - "ToolPicker lists Luse 17 + Built-in 10 with category headers"
    - "SubAgentPicker shows all other agents as candidate children"
    - "Sub-agent depth guard — D-202-13: cannot select an agent that is itself a child"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/liv-ai-app/app/agents/new/page.tsx"
      provides: "Create form page"
      min_lines: 80
---

<objective>
A polished create form that bundles the four pickers (cron, tools, sub-agents, model) + name + instructions, with inline validation and a clear cron preview.
</objective>

<context>
@.planning/phases/202-agents-platform/202-CONTEXT.md (D-202-13 depth limit, D-202-15 cron format)
Skill:senior-frontend — form ergonomics + accessibility
Skill:openui — for inspiration on dynamic form structure (forward-compat for Plan 202-08)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CronPicker</name>
  <action>
    `components/agents/CronPicker.tsx`. Inputs: `{ value, onChange }`. Layout:

    - Top: 4 preset buttons (every 15m, hourly, daily 09:00, weekly Mon 09:00). Click sets value.
    - Below: free-form Input. On change, debounce 300ms then call `agents.cronPreview` tRPC. Show preview text below (`"At 09:00 AM, only on Monday"` etc.) or error.
    - "None" option clears value.
  </action>
  <acceptance_criteria>
    - Preset clicks update parent value
    - Preview text appears for valid cron
    - Error text appears for malformed cron
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ToolPicker</name>
  <action>
    `components/agents/ToolPicker.tsx`. Inputs: `{ value: string[], onChange }`. Categories:

    - **Computer-use (Luse)** — 17 tool checkboxes
    - **Built-in** — 10 tool checkboxes from `mastra.agent.listBuiltInTools`

    Each checkbox: label + 1-line description (text-xs muted) + destructive badge if applicable.

    "Select all" + "Clear" buttons per category.

    Empty selection = "all tools available" (matches `toolIds: []` semantics in repo).
  </action>
  <acceptance_criteria>
    - Categories render
    - Selections sync to parent state
    - Destructive badges visible
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SubAgentPicker</name>
  <action>
    `components/agents/SubAgentPicker.tsx`. Inputs: `{ value: string | null, onChange, excludeAgentId? }`.

    Renders a Select with:
    - "None — top-level agent" (default)
    - One option per existing agent whose `parent_agent_id IS NULL` (can have children) AND `id !== excludeAgentId` (no self-parent)

    Disabled state for agents that already have a parent (would be grandchild — D-202-13).
  </action>
  <acceptance_criteria>
    - Select renders allowed candidates only
    - Grandchild candidates greyed out with tooltip "Already a sub-agent"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: ModelPicker</name>
  <action>
    `components/agents/ModelPicker.tsx`. Port Plan 201-04 `LivAiModelPicker` shape; difference: shown inline in form (not just in chat composer footer). Inputs: `{ value, onChange }`.

    Fetches `mastra.agent.listAvailableModels` once, renders shadcn `<Select>` with 3 Grok options + labels.
  </action>
  <acceptance_criteria>
    - 3 model options
    - Default value: `grok-4.3`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Create page</name>
  <action>
    `app/agents/new/page.tsx`. Form state: name, instructions, modelName, toolIds, scheduleCron, parentAgentId.

    Layout: vertical stack, max-w-3xl mx-auto.

    On submit:
    1. Validate name non-empty + unique (catch `AGENT_NAME_TAKEN` error)
    2. POST `/trpc/agents.create`
    3. On success: toast + redirect to `/agents/[newId]`

    Cancel button → router.back().
  </action>
  <acceptance_criteria>
    - Form mounts with default values
    - Submit creates agent and redirects
    - Duplicate name shown inline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Commit</name>
  <action>
    Commit: `feat(202-06): /agents/new create form + cron/tool/sub-agent/model pickers`
    Sacred SHA hook MUST PASS.
  </action>
  <acceptance_criteria>
    - `[sacred-sha] PASS: 20 files verified`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Create form renders, all pickers wired
- Create succeeds and lands on detail page
- Duplicate name error shown inline
</verification>

<output>
Path: `.planning/phases/202-agents-platform/202-06-SUMMARY.md`
</output>
