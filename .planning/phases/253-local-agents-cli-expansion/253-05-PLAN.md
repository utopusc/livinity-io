---
phase: 253-local-agents-cli-expansion
plan: 05
type: execute
wave: 3
depends_on: [253-01, 253-02, 253-03, 253-04]
files_modified: []
autonomous: true
requirements: [LAX-253]

must_haves:
  truths:
    - "deploy-livinityd.sh copies all 15 new scripts/install/cli/<id>.sh to /opt/livos by glob (no deploy-script edit needed, G12)"
    - "install-liv-assistant.sh re-deploys the updated panel patch JS with the G18 sha256 cache-bust"
    - "the deployed panel JS the running liv-assistant serves contains all 15 new CLI ids (post-deploy grep)"
  artifacts: []
  key_links:
    - from: "repo scripts/install/cli/*.sh (15 new)"
      to: "/opt/livos/scripts/install/cli/*.sh"
      via: "deploy-livinityd.sh _dld_clone_source rsync glob (G12)"
      pattern: "rsync -a .*scripts/install/cli/"
    - from: "scripts/aionui-patches/local-agents-install-section.js"
      to: "deployed liv-240-install-section.js asset"
      via: "install-liv-assistant.sh sha256 cache-bust (G18)"
      pattern: "install-section"
---

<objective>
Deploy the 20-CLI roster to the test box and PROVE the deploy machinery carried both the 15 new install scripts (by glob, G12) and the updated panel patch JS (cache-busted, G18). No source files change here — this plan verifies the existing deploy paths actually picked up Phase 253's additions.

Purpose: Plans 01-04 are inert on the server until deployed. The install scripts ride deploy-livinityd.sh's directory-glob rsync (G12) and the panel rides install-liv-assistant.sh's sha256 cache-bust (G18). This plan confirms BOTH paths worked, so the Plan 06 operator walk sees the new rows and `cliInstaller.install` finds the new scripts.
Output: a verified deployment to the test box (hello.livinity.io / 154.53.56.75 — NOT Mini PC, NOT Server4, NOT Server5) plus grep evidence that the 15 ids landed in both the script dir and the deployed panel JS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md
@scripts/install/deploy-livinityd.sh
@scripts/install-liv-assistant.sh

<interfaces>
<!-- Deploy facts (RESEARCH "Deploy verification (answered)"): -->
<!-- deploy-livinityd.sh ~line 452: rsync -a "$_DLD_STAGE_DIR/scripts/install/cli/" "$_DLD_LIVOS_DIR/scripts/install/cli/" -->
<!--   = a DIRECTORY GLOB, NOT a hardcoded list. New cli/<id>.sh files are picked up automatically. -->
<!--   The ONLY hardcoded special-case is the openclaw sibling delegate (lines ~458-459) — Phase 253 -->
<!--   scripts are self-contained in cli/, so NO deploy edit is needed. -->
<!-- install-liv-assistant.sh redeploys the panel patch JS + sha256 cache-busts (G18). -->
<!-- TARGET BOX (project memory HARD RULE): test box hello.livinity.io / 154.53.56.75 ONLY. -->
<!--   NEVER Mini PC (10.69.31.68), NEVER Server4 (45.137.194.103), NEVER Server5 (45.137.194.102). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to the test box and verify glob pickup of the 15 scripts</name>
  <read_first>
    - scripts/install/deploy-livinityd.sh (the _dld_clone_source rsync glob ~line 440-463 — confirm it copies cli/ by directory glob, not a hardcoded list)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md -> "Deploy verification (answered)"
  </read_first>
  <action>
    Deploy Phase 253 to the test box (hello.livinity.io / 154.53.56.75 per project memory — NEVER Mini PC, NEVER Server4/Server5). Use the standard flow: SSH to the test box and run `bash /opt/livos/update.sh` (which clones master, runs deploy-livinityd.sh, builds, restarts services).

    Then VERIFY on the test box that all 15 new install scripts landed by glob:
      ls /opt/livos/scripts/install/cli/ | grep -E '^(codex|qwen-code|augment|github-copilot|codebuddy|qoder-cli|goose|factory-droid|cursor-agent|kimi-cli|mistral-vibe|hermes-agent|nanobot|snow-cli|kiro)\.sh$' | wc -l
    -> MUST equal 15. No deploy-script edit should be needed (glob, G12). If < 15, the glob regressed (or a script is missing from master) — fix the glob / re-deploy before continuing.
    Also confirm the scripts are executable (deploy chmod +x's cli/*.sh).
  </action>
  <verify>
    <automated>echo "RUN ON TEST BOX (154.53.56.75): ls /opt/livos/scripts/install/cli/ | grep -E '^(codex|qwen-code|augment|github-copilot|codebuddy|qoder-cli|goose|factory-droid|cursor-agent|kimi-cli|mistral-vibe|hermes-agent|nanobot|snow-cli|kiro)\.sh$' | wc -l  == 15"</automated>
  </verify>
  <acceptance_criteria>
    - 15 new cli/<id>.sh files present under /opt/livos/scripts/install/cli/ on the test box (glob count == 15)
    - the deploy required NO hardcoded-list edit (G12 glob carried them)
    - each new script is executable on the server
  </acceptance_criteria>
  <done>All 15 new install scripts deployed to /opt/livos by the deploy glob, executable, no deploy-script edit needed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the panel JS re-deployed with all 15 ids (G18 cache-bust)</name>
  <read_first>
    - scripts/install-liv-assistant.sh (the panel-JS redeploy + sha256 cache-bust path — G18)
  </read_first>
  <action>
    Prove the G18 sha256 cache-bust actually re-deployed the UPDATED panel JS (not a stale precache). On the test box, grep the deployed/served asset for all 15 new ids. First locate the asset the running liv-assistant serves (the install-liv-assistant.sh output / the static assets dir), then:
      for id in codex qwen-code augment github-copilot codebuddy qoder-cli goose factory-droid cursor-agent kimi-cli mistral-vibe hermes-agent nanobot snow-cli kiro; do
        grep -q "'$id'" <deployed-liv-240-install-section.js> || echo "MISSING $id"
      done
    -> MUST print nothing (all 15 present). If ANY id is missing, the deployed panel is stale — re-run install-liv-assistant.sh so the sha256 cache-bust re-emits the updated bundle, then re-grep until clean.
  </action>
  <verify>
    <automated>echo "RUN ON TEST BOX: for id in codex qwen-code augment github-copilot codebuddy qoder-cli goose factory-droid cursor-agent kimi-cli mistral-vibe hermes-agent nanobot snow-cli kiro; do grep -q \"'\$id'\" <served liv-240-install-section.js> || echo MISSING \$id; done -> prints nothing"</automated>
  </verify>
  <acceptance_criteria>
    - the served liv-240-install-section.js contains all 15 new CLI ids (grep, 0 MISSING)
    - the G18 sha256 cache-bust re-deployed the updated panel (not a stale precache)
  </acceptance_criteria>
  <done>The served panel JS contains all 15 new ids — proving the cache-bust re-deployed the updated bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo to test-box filesystem | deploy rsyncs version-controlled repo scripts to /opt/livos as the deploy user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-11 | Tampering | deploy glob picks up unexpected scripts | accept | The glob copies only version-controlled `scripts/install/cli/*.sh`; the Plan 04 whitelist gate still rejects any name not in the 20-tuple at runtime. |
| T-253-12 | Information disclosure | stale cached panel JS served | mitigate | G18 sha256 cache-bust forces the updated bundle; post-deploy grep verifies the new ids are actually served. |
</threat_model>

<verification>
- 15 new cli/<id>.sh under /opt/livos on the test box (glob count == 15).
- served liv-240-install-section.js contains all 15 ids (0 MISSING).
- no deploy-script source edit required.
</verification>

<success_criteria>
The 20-CLI roster is deployed to the test box: 15 new install scripts present by glob (no deploy edit) and the panel JS re-deployed with all 15 ids via the G18 cache-bust. Ready for the Plan 06 operator walk.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-05-SUMMARY.md`
</output>
