---
phase: 253-local-agents-cli-expansion
plan: 06
type: execute
wave: 4
depends_on: [253-01, 253-02, 253-03, 253-04, 253-05]
files_modified: []
autonomous: false
requirements: [LAX-253]

must_haves:
  truths:
    - "The Liv AI Local Agents 'Available to Install' panel lists all 20 CLIs (5 existing + 15 new)"
    - "A Wave A CLI (codex) installs from the panel and flips to Installed, then its Auth button opens the LivOS Terminal"
    - "The 6 authHidden CLIs (kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro) render with NO Auth button"
  artifacts: []
  key_links:
    - from: "panel Install click"
      to: "cliInstaller.install on test box"
      via: "/liv/trpc proxy"
      pattern: "cliInstaller.install"
    - from: "panel Auth click (auth-capable CLI)"
      to: "LivOS Terminal"
      via: "use-cli-auth-bridge postMessage"
      pattern: "cli-auth"
---

<objective>
Operator browser walk: confirm the expanded Local Agents panel on the test box actually shows all 20 CLIs, that a representative new CLI installs + opens its terminal-auth flow, and that the authHidden CLIs render without an Auth button. This is the human-verify gate that the automated drift-lock + deploy plans cannot cover (real DOM + real install subprocess + real terminal).

Purpose: Plans 01-05 prove the code and deploy are correct mechanically; only a human can confirm the panel renders correctly, the install actually lands a binary, and the terminal-auth bridge opens. Per the operator's full-autonomous preference this is the single unavoidable visual checkpoint.
Output: operator confirmation (or a defect list) for the 20-CLI panel on the test box.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator verifies the 20-CLI Local Agents panel on the test box</name>
  <what-built>
    Phase 253 added 15 new CLIs (Codex, Qwen Code, Augment, GitHub Copilot, CodeBuddy, Qoder, Goose, Factory Droid, Cursor Agent, Kimi CLI, Mistral Vibe, Hermes Agent, Nanobot, Snow CLI, Kiro) to Liv AI's Local Agents -> Available to Install panel, each with a self-contained install script, drift-locked whitelist/auth/detect entries, panel rows, and (for the 9 auth-capable ones) a terminal-auth bridge entry. The 6 install-only CLIs are authHidden. All deployed to the test box in Plan 05.
  </what-built>
  <how-to-verify>
    1. Open the test box desktop in a browser: https://hello.livinity.io (154.53.56.75 — NOT Mini PC).
    2. Open Liv AI -> Local Agents tab -> scroll to "Available to Install".
       EXPECT: 20 rows total (the original claude-code, opencode, gemini, openclaw, aion-cli + the 15 new ones). The hint text reads "20 supported CLI agents".
    3. Pick a Wave A CLI — Codex. Click Install.
       EXPECT: row shows "Installing…", then flips to "Installed ✓" with an Auth button appearing (codex auth login is wired). If it shows "Failed", capture the truncated error text.
    4. Click Codex's Auth button.
       EXPECT: the LivOS Terminal window opens (or focuses) and `codex auth login` is queued/running in it. (You do NOT have to complete the OAuth — just confirm the terminal opened with the right command.)
    5. Find Kimi CLI (an authHidden CLI). Confirm its row has Install but NO Auth button (and same for Nanobot / Snow CLI / Mistral Vibe / Hermes Agent / Kiro).
    6. (Optional) Install one Wave B CLI (Goose) and one Wave C CLI (Kimi CLI) to confirm the curl/Python installers run without erroring on the test box.
  </how-to-verify>
  <resume-signal>Type "approved" if all 20 rows show, Codex installs + opens terminal-auth, and the 6 authHidden CLIs have no Auth button. Otherwise list the specific rows/behaviors that failed (with truncated error text) and which CLIs are affected.</resume-signal>
</task>

</tasks>

<verification>
- Operator confirms 20 rows in the panel.
- Operator confirms a new Wave A CLI installs + opens terminal-auth.
- Operator confirms the 6 authHidden CLIs render without an Auth button.
</verification>

<success_criteria>
The operator confirms the deployed Local Agents panel lists all 20 CLIs, a new CLI installs and triggers the terminal-auth flow, and the install-only CLIs correctly hide the Auth button — or returns a precise defect list for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-06-SUMMARY.md`
</output>
