---
phase: 253-local-agents-cli-expansion
plan: 06
type: execute
wave: 4
depends_on: [253-01, 253-02, 253-03, 253-04, 253-05]
files_modified: []
autonomous: false
requirements: [LAX-253]

must_haves:
  truths:
    - "The Liv AI Local Agents 'Available to Install' panel lists all 20 CLIs (5 existing + 15 new)"
    - "Wave A+B (the 9 verified CLIs) install+detect+auth are PHASE-GATING on the test box"
    - "Wave C (the 6 install-only/UNVERIFIED CLIs) install failures are NON-BLOCKING by design (partial-success accepted) — WARNING 2"
    - "cursor-agent install→detect round-trips green (binary cursor-agent on PATH — the BLOCKER 1 detect-after-install path)"
    - "The 6 authHidden CLIs (kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro) render with NO Auth button"
    - "The deployed/served panel JS contains all 15 new CLI ids (post-deploy grep cross-check — WARNING 3)"
  artifacts: []
  key_links:
    - from: "panel Install click"
      to: "cliInstaller.install on test box"
      via: "/liv/trpc proxy"
      pattern: "cliInstaller.install"
    - from: "panel Auth click (auth-capable CLI)"
      to: "LivOS Terminal"
      via: "use-cli-auth-bridge postMessage"
      pattern: "cli-auth"
    - from: "deployed liv-240-install-section.js"
      to: "the 15 new CLI ids"
      via: "post-deploy grep (G18 cache-bust proof)"
      pattern: "install-section"
---

<objective>
Operator browser walk: confirm the expanded Local Agents panel on the test box shows all 20 CLIs,
with EXPLICIT partial-success gating — the 9 verified Wave A/B CLIs are phase-gating
(install+detect+show-auth); the 6 Wave-C install-only CLIs are non-blocking (install failures
accepted by design, WARNING 2). This is the human-verify gate that the automated drift-lock +
deploy plans cannot cover (real DOM + real install subprocess + real terminal).

Purpose: Plans 01-05 prove the code and deploy are correct mechanically; only a human can confirm
the panel renders correctly, the install actually lands a binary, and the terminal-auth bridge
opens. With 15 new CLIs of mixed confidence (9 VERIFIED, 6 MEDIUM/UNVERIFIED), the phase gate must
not hinge on the unverifiable Python/build-from-source tools — hence the partial-success acceptance
documented below.
Output: operator confirmation (or a defect list) for the 20-CLI panel on the test box, with
per-Wave-C install results recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-walk panel-JS cross-check (WARNING 3)</name>
  <action>
    Before the operator walk, re-confirm (cross-check of Plan 05 Task 2) that the deployed panel JS
    the running liv-assistant SERVES contains all 15 new ids. Locate the served asset
    (install-liv-assistant.sh static-assets path, e.g.
    /opt/liv-assistant/current/static/assets/liv-240-install-section.js), then on the test box:
      for id in codex qwen-code augment github-copilot codebuddy qoder-cli goose factory-droid cursor-agent kimi-cli mistral-vibe hermes-agent nanobot snow-cli kiro; do
        grep -q "'$id'" <served-liv-240-install-section.js> || echo "MISSING $id"
      done
    Must print nothing. If ANY id is missing → STOP, re-run install-liv-assistant.sh (Plan 05 G18
    sha256 cache-bust), do NOT proceed to the walk against a stale panel.
  </action>
  <verify>
    <automated>echo "RUN ON TEST BOX (154.53.56.75): 0 MISSING ids in served liv-240-install-section.js (WARNING 3 cross-check)"</automated>
  </verify>
  <acceptance_criteria>
    - the served panel JS contains all 15 new CLI ids (0 MISSING) — re-verified at walk time (WARNING 3),
      proving the G18 sha256 cache-bust re-deployed the updated panel JS
  </acceptance_criteria>
  <done>Served panel JS confirmed to contain all 15 ids immediately before the operator walk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies the 20-CLI Local Agents panel (partial-success gated)</name>
  <what-built>
    Phase 253 added 15 new CLIs to Liv AI's Local Agents → Available to Install panel, each with a
    self-contained install script, drift-locked whitelist/auth/detect entries, panel rows, and (for
    the 9 auth-capable ones) a terminal-auth bridge entry. The 6 install-only CLIs are authHidden.
    Wave A/B = 9 VERIFIED CLIs (full install+detect+auth, phase-gating). Wave C = 6
    install-only/authHidden CLIs (MEDIUM/UNVERIFIED, non-blocking). All deployed to the test box in Plan 05.
  </what-built>
  <how-to-verify>
    Open the test box desktop: https://hello.livinity.io (154.53.56.75 — NOT Mini PC, NOT Server4/5).
    Liv AI → Local Agents tab → "Available to Install".
    First: EXPECT 20 rows total (original claude-code, opencode, gemini, openclaw, aion-cli + the 15
    new). Hint text reads "20 supported CLI agents".

    == PHASE-GATING (Wave A + B — the 9 VERIFIED CLIs) ==
    CLIs: codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli (Wave A npm),
          goose, factory-droid, cursor-agent (Wave B curl).
      1. Each of the 9 appears as an "Available to Install" row.
      2. Click Install on each → completes; the row flips to "Installed ✓".
         - cursor-agent SPECIFICALLY (BLOCKER 1 detect-after-install path): after Install the row
           must show "Installed" and SURVIVE a refresh. If cursor-agent installs but reverts to
           "Available to Install" on refresh, the binary-name pin is wrong (detector probing the
           wrong name) → FAIL.
      3. Each of the 9 shows an Auth button (authHidden:false). Clicking Auth opens the LivOS
         Terminal running the verified login command (codex auth login / qwen auth / auggie login /
         copilot / codebuddy / qodercli / goose configure / droid login / cursor-agent login).
         Completing the real OAuth is optional — verify the terminal opens with the right command.
      → ALL 9 must PASS install + detect + auth-button. These GATE the phase.

    == NON-BLOCKING (Wave C — the 6 install-only CLIs, WARNING 2) ==
    CLIs: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro.
      4. Each appears as an "Available to Install" row with NO Auth button (authHidden:true).
      5. Clicking Install MAY succeed OR fail — Wave C is MEDIUM/UNVERIFIED (Python/uv,
         build-from-source, MCP-host, unpinned installer). A Wave-C install FAILURE is ACCEPTABLE
         and does NOT block the phase. Record which Wave-C CLIs installed cleanly vs failed (for the
         SUMMARY / follow-up), but do not gate on them.

    == PARTIAL-SUCCESS ACCEPTANCE (WARNING 2) ==
    The phase PASSES when:
      (a) all 9 Wave A/B CLIs install + detect (cursor-agent detect-after-install green) + render
          the Auth button, AND
      (b) all 6 Wave-C CLIs render as install-only (authHidden) rows.
    Wave-C install SUCCESS is recorded but NOT required. Wave-C install FAILURE is non-blocking by design.
  </how-to-verify>
  <resume-signal>Type "approved" (note any Wave-C install results), OR list which Wave A/B CLI failed (gating) with truncated error text.</resume-signal>
</task>

</tasks>

<acceptance_criteria>
- [ ] Served panel JS contains all 15 new ids (re-checked at walk time — WARNING 3).
- [ ] Panel shows 20 rows; hint text reads "20 supported CLI agents".
- [ ] PHASE-GATING: all 9 Wave A/B CLIs install + detect + render Auth button; cursor-agent
      detect-after-install survives refresh (BLOCKER 1).
- [ ] NON-BLOCKING: all 6 Wave-C CLIs render as install-only (authHidden) rows; their install
      failures are accepted (partial success by design — WARNING 2). Per-CLI Wave-C results recorded.
</acceptance_criteria>

<verification>
- Operator confirms 20 rows in the panel.
- 9 Wave A/B CLIs: install+detect+auth-button all PASS (gating); cursor-agent detect-after-install green.
- 6 Wave-C CLIs: install-only rows render (authHidden); install pass/fail recorded, non-blocking (WARNING 2).
- served panel JS contains all 15 ids (WARNING 3 cross-check).
</verification>

<success_criteria>
Operator-verified 20-CLI roster: 9 verified CLIs fully working (phase gate met), 6 Wave-C CLIs
present install-only with non-blocking install (partial-success accepted — WARNING 2), panel JS
serving all 15 ids (WARNING 3) — or a precise defect list for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-06-SUMMARY.md`.
Record per-Wave-C install result (clean / failed) for follow-up.
</output>
