---
phase: 253-local-agents-cli-expansion
plan: 04
type: execute
wave: 2
depends_on: [253-01, 253-02, 253-03]
files_modified:
  - livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts
  - livos/packages/livinityd/source/modules/cli-installer/types.ts
  - livos/packages/livinityd/source/modules/cli-installer/auth.ts
  - livos/packages/livinityd/source/modules/cli-installer/detector.ts
  - scripts/aionui-patches/local-agents-install-section.js
  - livos/packages/ui/src/hooks/use-cli-auth-bridge.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/installer.test.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/auth.test.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/detector.test.ts
autonomous: true
requirements: [LAX-253]

must_haves:
  truths:
    - "SUPPORTED_CLIS contains all 20 names (5 existing + 15 new) and the RCE whitelist rejects any non-listed name"
    - "CliName union includes all 15 new literals; the module type-checks"
    - "CLI_AUTH_COMMANDS keys exactly match SUPPORTED_CLIS; auth subcommands match RESEARCH (verified login / bare-TUI / null)"
    - "detector CLI_BIN_NAMES + CLI_VERSION_ARGS have an entry for every new CLI with the verified binary name"
    - "panel patch JS lists all 20 CLIs with CLI_META; Wave C + nanobot are authHidden:true"
    - "shell-bridge CLI_AUTH_COMMANDS includes the auth-capable CLIs and omits authHidden ones"
    - "the 3 vitest count assertions are bumped from 5 to 20 and the full cli-installer suite is green"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts"
      provides: "20-name SUPPORTED_CLIS tuple + CLI_BIN_NAMES + CLI_VERSION_ARGS"
      contains: "qodercli"
    - path: "livos/packages/livinityd/source/modules/cli-installer/types.ts"
      provides: "CliName union of 20 literals"
      contains: "cursor-agent"
    - path: "livos/packages/livinityd/source/modules/cli-installer/auth.ts"
      provides: "CLI_AUTH_COMMANDS with 20 keys"
      contains: "kimi-cli"
    - path: "scripts/aionui-patches/local-agents-install-section.js"
      provides: "panel SUPPORTED_CLIS + CLI_META for 20 CLIs"
      contains: "authHidden"
  key_links:
    - from: "install-scripts.ts SUPPORTED_CLIS"
      to: "scripts/install/cli/<name>.sh"
      via: "resolveInstallScript name to script path"
      pattern: "resolveInstallScript"
    - from: "auth.ts CLI_AUTH_COMMANDS keys"
      to: "install-scripts.ts SUPPORTED_CLIS"
      via: "auth.test.ts asserts key set equals tuple"
      pattern: "Object.keys"
    - from: "installer.test.ts / auth.test.ts count assertions"
      to: "SUPPORTED_CLIS size"
      via: ".size / keys.length === 20"
      pattern: "toBe.20."
---

<objective>
Register all 15 new CLIs across the 6 drift-locked files and bump the 3 vitest count assertions from 5 to 20, keeping the cli-installer suite green.

Purpose: The install scripts (Plans 01-03) are inert until each CLI name is added consistently to the RCE whitelist, the CliName type, the auth map, the detector bin/version maps, the panel patch JS, and the shell-bridge whitelist. This is the ONE serialized plan because all six files + the three tests are shared across every CLI — splitting it would create parallel write conflicts (LAX-253).
Output: 6 edited source/patch files + 3 edited test files; new total count = 20 everywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md

<interfaces>
<!-- Source of truth: RESEARCH.md "Drift-Lock Checklist" (exact files + line numbers) + "CLI_AUTH_COMMANDS additions (auth.ts)" code block + fact-table binary names. -->

The 15 new names to add (use these EXACT key literals everywhere — they match the .sh filenames from Plans 01-03):
  codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli,   (Wave A)
  goose, factory-droid, cursor-agent,                                (Wave B)
  kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro      (Wave C)
Existing 5 (already present — DO NOT re-add): claude-code, opencode, gemini, openclaw, aion-cli.
New total everywhere = 20.

Verified binary names (CLI_BIN_NAMES — RESEARCH Pitfall 2; KEY then BINARY):
  codex / codex   qwen-code / qwen   augment / auggie   github-copilot / copilot   codebuddy / codebuddy   qoder-cli / qodercli
  goose / goose   factory-droid / droid   cursor-agent / cursor-agent
  kimi-cli / kimi   mistral-vibe / vibe   hermes-agent / hermes   nanobot / nanobot   snow-cli / snow   kiro / kiro
CLI_VERSION_ARGS: use the existing convention (['--version'] for each) unless RESEARCH notes otherwise — match the shape already used for gemini/opencode.

CLI_AUTH_COMMANDS (auth.ts — verbatim from RESEARCH "CLI_AUTH_COMMANDS additions"):
  codex:          ['codex', ['auth','login']]
  augment:        ['auggie', ['login']]
  'cursor-agent': ['cursor-agent', ['login']]
  'factory-droid':['droid', ['login']]
  goose:          ['goose', ['configure']]
  'qwen-code':    ['qwen', ['auth']]
  'github-copilot':['copilot', []]      // bare TUI -> operator types /login
  'qoder-cli':    ['qodercli', []]      // bare TUI
  codebuddy:      ['codebuddy', []]     // bare TUI
  'kimi-cli':     null                  // authHidden
  'mistral-vibe': null                  // authHidden
  'hermes-agent': null                  // authHidden
  nanobot:        null                  // authHidden
  'snow-cli':     null                  // authHidden
  kiro:           null                  // authHidden

Panel CLI_META authHidden:true for: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro (the 6 null-auth CLIs).
Shell-bridge use-cli-auth-bridge.ts CLI_AUTH_COMMANDS: include ONLY the 9 auth-capable new CLIs (codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli, goose, factory-droid, cursor-agent) as name->command strings; OMIT the 6 authHidden ones (no Auth button).

Exact drift-lock locations (RESEARCH checklist — verify at runtime, line numbers may have shifted):
  1. install-scripts.ts: SUPPORTED_CLIS tuple (~lines 17-23), CLI_BIN_NAMES (~43-49), CLI_VERSION_ARGS (~52-58), doc-comment "exactly 5 names" (~line 13)
  2. types.ts: CliName union (~line 7), "The 5 supported CLIs" comment (~line 6)
  3. auth.ts: CLI_AUTH_COMMANDS, header "exactly 5 keys" (~line 11), PATH array (~198-206) — add snow-cli's npm-link bin dir ONLY if not already covered
  4. local-agents-install-section.js: SUPPORTED_CLIS array (~line 26), CLI_META (~30-36), hint text "5 supported CLI agents" (~line 271)
  5. use-cli-auth-bridge.ts: CLI_AUTH_COMMANDS map (~lines 35-40)
  6. detector.ts PATH array (~121-129) — add snow-cli's link dir ONLY if not already in {~/.local/bin, ~/.opencode/bin, /opt/livos/bin, ~/.bun/bin, ~/.npm-global/bin, /usr/local/bin}

Vitest assertions to bump (RESEARCH A7 — EXACT locations):
  installer.test.ts:~148 -> expect([...SUPPORTED_CLIS]).toEqual([...]) — ADD all 15 new names in the SAME order as the tuple
  installer.test.ts:~155 -> expect(SUPPORTED_CLIS_SET.size).toBe(5) -> toBe(20)
  auth.test.ts:~320   -> expect(keys.length).toBe(5) -> toBe(20)  (lines ~317-319 compare sorted keys to sorted SUPPORTED_CLIS — auto-passes if both edited)
  detector.test.ts: NO count assertion; add a CLI_BIN_NAMES bin-name assert per new CLI if following the test style (optional but recommended).
  DO NOT touch INSTALL_TIMEOUT_MS / AUTH_TIMEOUT_MS === 300_000 assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 15 names in the 3 livinityd source maps (install-scripts.ts, types.ts, detector.ts)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts (SUPPORTED_CLIS tuple + CLI_BIN_NAMES + CLI_VERSION_ARGS + resolveInstallScript + doc-comment)
    - livos/packages/livinityd/source/modules/cli-installer/types.ts (CliName union, line 7)
    - livos/packages/livinityd/source/modules/cli-installer/detector.ts (PATH array ~121-129)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md -> "Drift-Lock Checklist" rows 1, 2, 6 + Pitfall 2 binary names
  </read_first>
  <action>
    1. types.ts: extend the CliName union (line 7) with all 15 new string literals: 'codex' | 'qwen-code' | 'augment' | 'github-copilot' | 'codebuddy' | 'qoder-cli' | 'goose' | 'factory-droid' | 'cursor-agent' | 'kimi-cli' | 'mistral-vibe' | 'hermes-agent' | 'nanobot' | 'snow-cli' | 'kiro'. Update the "The 5 supported CLIs" comment to "20".
    2. install-scripts.ts:
       - add all 15 names to the SUPPORTED_CLIS tuple (preserve existing 5 first, then append the 15 in the order above — this order is mirrored by installer.test.ts:148).
       - add CLI_BIN_NAMES entries using the VERIFIED binaries (KEY -> BINARY): codex -> 'codex', qwen-code -> 'qwen', augment -> 'auggie', github-copilot -> 'copilot', codebuddy -> 'codebuddy', qoder-cli -> 'qodercli', goose -> 'goose', factory-droid -> 'droid', cursor-agent -> 'cursor-agent', kimi-cli -> 'kimi', mistral-vibe -> 'vibe', hermes-agent -> 'hermes', nanobot -> 'nanobot', snow-cli -> 'snow', kiro -> 'kiro'.
       - add CLI_VERSION_ARGS entries (['--version'] for each, matching the existing convention).
       - confirm resolveInstallScript maps each name -> scripts/install/cli/<name>.sh (the .sh basenames from Plans 01-03 match these keys exactly; resolveInstallScript derives the path from the name, so no per-CLI edit is expected — verify).
       - update the "exactly 5 names" doc-comment to 20.
    3. detector.ts: the install dirs for all new CLIs are ~/.npm-global/bin (Wave A) or ~/.local/bin (Wave B/C), already in the PATH array. Add snow-cli's npm-link bin dir ONLY if Plan 03's snow-cli.sh linked somewhere outside that set (check the 253-03 SUMMARY / the snow-cli.sh script). If snow links into ~/.npm-global/bin or ~/.local/bin, no edit.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd exec tsc --noEmit -p packages/livinityd/tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - install-scripts.ts contains codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli, goose, factory-droid, cursor-agent, kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro (all 15)
    - install-scripts.ts CLI_BIN_NAMES contains the binary literals qwen and auggie and qodercli and droid (the binary names that differ from their key)
    - types.ts CliName union contains cursor-agent and mistral-vibe and kiro
    - install-scripts.ts no longer contains a doc-comment asserting "exactly 5" (updated to 20)
    - the livinityd tsc --noEmit pass reports no NEW errors in the cli-installer module
  </acceptance_criteria>
  <done>SUPPORTED_CLIS has 20 names, CliName union has 20 literals, CLI_BIN_NAMES/CLI_VERSION_ARGS cover all new CLIs with verified binaries, and the module type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth + panel + shell-bridge (auth.ts, local-agents-install-section.js, use-cli-auth-bridge.ts)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/auth.ts (CLI_AUTH_COMMANDS map + PATH array + doc-comment)
    - scripts/aionui-patches/local-agents-install-section.js (SUPPORTED_CLIS array + CLI_META + hint text)
    - livos/packages/ui/src/hooks/use-cli-auth-bridge.ts (CLI_AUTH_COMMANDS map)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md -> "CLI_AUTH_COMMANDS additions (auth.ts)" code block (verbatim values) + Drift-Lock rows 3, 4, 5 + Pitfall 4 (TUI auth)
  </read_first>
  <action>
    1. auth.ts CLI_AUTH_COMMANDS — add all 15 entries EXACTLY as the RESEARCH "CLI_AUTH_COMMANDS additions" block:
       codex -> ['codex',['auth','login']], augment -> ['auggie',['login']], cursor-agent -> ['cursor-agent',['login']],
       factory-droid -> ['droid',['login']], goose -> ['goose',['configure']], qwen-code -> ['qwen',['auth']],
       github-copilot -> ['copilot',[]], qoder-cli -> ['qodercli',[]], codebuddy -> ['codebuddy',[]],
       kimi-cli -> null, mistral-vibe -> null, hermes-agent -> null, nanobot -> null, snow-cli -> null, kiro -> null.
       Update the "exactly 5 keys" header doc-comment to 20. (auth.test.ts asserts keys === SUPPORTED_CLIS, so all 20 keys must be present; null still counts as a key.)
       If Plan 03's snow-cli linked outside the covered PATH dirs, add that dir to auth.ts's PATH array too (mirror detector.ts).
    2. local-agents-install-section.js — add all 15 names to SUPPORTED_CLIS and a CLI_META[name] entry (label + icon) for each. Set authHidden:true on the 6 null-auth CLIs: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro. Use sensible human labels (e.g. 'Codex', 'Qwen Code', 'Augment', 'GitHub Copilot', 'CodeBuddy', 'Qoder', 'Goose', 'Factory Droid', 'Cursor Agent', 'Kimi CLI', 'Mistral Vibe', 'Hermes Agent', 'Nanobot', 'Snow CLI', 'Kiro'). Update the "5 supported CLI agents" hint text to 20.
    3. use-cli-auth-bridge.ts CLI_AUTH_COMMANDS — add ONLY the 9 auth-capable new CLIs as name->command strings (matching auth.ts: codex -> 'codex auth login', augment -> 'auggie login', cursor-agent -> 'cursor-agent login', factory-droid -> 'droid login', goose -> 'goose configure', qwen-code -> 'qwen auth', github-copilot -> 'copilot', qoder-cli -> 'qodercli', codebuddy -> 'codebuddy'). Do NOT add the 6 authHidden CLIs (their absence = no Auth button — the intended behavior).
  </action>
  <verify>
    <automated>node --check scripts/aionui-patches/local-agents-install-section.js && echo PATCH_JS_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts contains a codex auth-login entry (e.g. ['codex', ['auth', 'login']])
    - auth.ts contains ['goose', ['configure']]
    - auth.ts contains a kimi-cli key with a null value (authHidden)
    - auth.ts contains all 15 new keys (codex through kiro)
    - local-agents-install-section.js contains all 15 new names in SUPPORTED_CLIS
    - local-agents-install-section.js sets authHidden true on kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro
    - use-cli-auth-bridge.ts contains codex and cursor-agent and goose
    - use-cli-auth-bridge.ts does NOT contain kimi-cli / nanobot / snow-cli (authHidden -> no bridge entry)
    - `node --check scripts/aionui-patches/local-agents-install-section.js` exits 0 (PATCH_JS_PARSE_OK)
  </acceptance_criteria>
  <done>auth.ts has 20 keys with verified auth values, panel JS lists 20 CLIs with the 6 authHidden flagged, and the shell-bridge whitelists exactly the 9 auth-capable new CLIs.</done>
</task>

<task type="auto">
  <name>Task 3: Bump the 3 vitest count assertions 5 to 20 and prove the cli-installer suite green</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/installer.test.ts (lines ~147-158: the name-array assert + .size assert)
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/auth.test.ts (lines ~316-321: sorted-keys compare + keys.length assert)
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/detector.test.ts (line ~87: CLI_BIN_NAMES bin-name assert style)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md -> "Drift-Lock Checklist" row 8 + A7 (exact assertion locations) + "Validation Architecture"
  </read_first>
  <action>
    1. installer.test.ts:
       - the expect([...SUPPORTED_CLIS]).toEqual([...]) literal name array (~line 148): append all 15 new names in the SAME order they appear in the SUPPORTED_CLIS tuple (existing 5 first, then the 15 in the canonical order).
       - the expect(SUPPORTED_CLIS_SET.size).toBe(5) (~line 155): change 5 to 20.
       - DO NOT touch the INSTALL_TIMEOUT_MS === 300_000 assertion.
    2. auth.test.ts:
       - expect(keys.length).toBe(5) (~line 320): change 5 to 20.
       - the sorted-keys-equals-SUPPORTED_CLIS compare (~317-319) auto-passes once auth.ts + tuple both have 20 — no edit needed unless it hard-codes a 5-name list.
       - DO NOT touch AUTH_TIMEOUT_MS === 300_000.
    3. detector.test.ts: optionally add a CLI_BIN_NAMES[key] === binary assert for each new CLI following the existing line-87 style (recommended for drift safety; at minimum add the ones where binary != key: qwen-code -> qwen, augment -> auggie, qoder-cli -> qodercli, factory-droid -> droid).
    4. Run the cli-installer vitest; if any OTHER count assertion fails, bump it to 20 too (RESEARCH says only these 3 exist, but verify). The suite MUST be green.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test source/modules/cli-installer 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - installer.test.ts contains toBe(20) (the SUPPORTED_CLIS_SET.size assert)
    - installer.test.ts no longer contains SUPPORTED_CLIS_SET.size).toBe(5)
    - installer.test.ts name-array assert contains cursor-agent and kiro
    - auth.test.ts contains toBe(20) for keys.length
    - auth.test.ts no longer contains keys.length).toBe(5)
    - `pnpm --filter livinityd test source/modules/cli-installer` exits 0 (all cli-installer tests pass)
    - the INSTALL_TIMEOUT_MS / AUTH_TIMEOUT_MS 300_000 assertions are unchanged
  </acceptance_criteria>
  <done>All 3 count assertions are 20, the name-array assertion lists all 20 names in tuple order, and the full cli-installer vitest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe to livinityd cliInstaller | iframe sends only a CLI name; SUPPORTED_CLIS_SET rejects any name not in the 20-entry whitelist (RCE boundary). |
| iframe to shell bridge | postMessage carries a name; use-cli-auth-bridge maps name to a FIXED command string; raw commands never accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-08 | Elevation/Tampering | cliInstaller name whitelist | mitigate | Every new name added to SUPPORTED_CLIS maps to a FIXED bash cli/<name>.sh; the whitelist guard rejects unknown names (installer.test.ts Test 1 enforces). |
| T-253-09 | Tampering | shell-bridge auth command | mitigate | use-cli-auth-bridge maps name to a fixed command string (e.g. 'codex auth login'); never interpolates iframe input; authHidden CLIs have NO bridge entry. |
| T-253-10 | Tampering | drift between the 6 mirrors | mitigate | auth.test.ts asserts CLI_AUTH_COMMANDS keys === SUPPORTED_CLIS; installer.test.ts asserts the exact 20-name set — drift fails CI. |
</threat_model>

<verification>
- `pnpm --filter livinityd test source/modules/cli-installer` is green (installer + auth + detector).
- `pnpm --filter livinityd test` full suite is green (phase gate).
- grep confirms all 20 names present in install-scripts.ts, types.ts, auth.ts; 20 in panel JS; 9 auth-capable in shell bridge; 6 authHidden flagged.
- No 300_000 timeout assertion changed.
</verification>

<success_criteria>
All 15 new CLIs are registered identically across the 6 drift-locked files, the CliName type compiles, the panel flags the 6 authHidden CLIs, the shell bridge whitelists only the 9 auth-capable new CLIs, and the 3 vitest count assertions are 20 with the full cli-installer suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-04-SUMMARY.md`
</output>
