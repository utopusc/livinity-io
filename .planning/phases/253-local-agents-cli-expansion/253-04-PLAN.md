---
phase: 253-local-agents-cli-expansion
plan: 04
type: execute
wave: 2
depends_on: [253-01, 253-02, 253-03]
files_modified:
  - livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts
  - livos/packages/livinityd/source/modules/cli-installer/types.ts
  - livos/packages/livinityd/source/modules/cli-installer/auth.ts
  - livos/packages/livinityd/source/modules/cli-installer/detector.ts
  - scripts/aionui-patches/local-agents-install-section.js
  - livos/packages/ui/src/hooks/use-cli-auth-bridge.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/installer.test.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/auth.test.ts
  - livos/packages/livinityd/source/modules/cli-installer/__tests__/detector.test.ts
autonomous: true
requirements: [LAX-253]

must_haves:
  truths:
    - "SUPPORTED_CLIS contains all 20 names (5 existing + 15 new) and the RCE whitelist rejects any non-listed name"
    - "CliName union includes all 15 new literals; the module type-checks"
    - "CLI_AUTH_COMMANDS keys exactly match SUPPORTED_CLIS; auth subcommands match RESEARCH (verified login / bare-TUI / null)"
    - "detector CLI_BIN_NAMES + CLI_VERSION_ARGS have an entry for every new CLI with the verified binary name"
    - "cursor-agent's CLI_BIN_NAMES entry + detector.test.ts assertion are both 'cursor-agent' — identical to Plan 02's install binary (BLOCKER 1)"
    - "installer.test.ts name ARRAY contains all 20 ids in tuple order (array-equality, not a count-only bump) (BLOCKER 2)"
    - "panel patch JS lists all 20 CLIs with CLI_META; Wave C + nanobot are authHidden:true"
    - "shell-bridge CLI_AUTH_COMMANDS includes the auth-capable CLIs and omits authHidden ones"
    - "the 3 vitest count assertions are bumped from 5 to 20 and the full cli-installer suite is green"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts"
      provides: "20-name SUPPORTED_CLIS tuple + CLI_BIN_NAMES + CLI_VERSION_ARGS"
      contains: "'qodercli'"
    - path: "livos/packages/livinityd/source/modules/cli-installer/types.ts"
      provides: "CliName union of 20 literals"
      contains: "'cursor-agent'"
    - path: "livos/packages/livinityd/source/modules/cli-installer/auth.ts"
      provides: "CLI_AUTH_COMMANDS with 20 keys"
      contains: "'kimi-cli'"
    - path: "scripts/aionui-patches/local-agents-install-section.js"
      provides: "panel SUPPORTED_CLIS + CLI_META for 20 CLIs"
      contains: "authHidden"
    - path: "livos/packages/livinityd/source/modules/cli-installer/__tests__/installer.test.ts"
      provides: "drift-lock: 20-id name array (order-locked) + .size===20"
      contains: "'cursor-agent'"
  key_links:
    - from: "install-scripts.ts SUPPORTED_CLIS"
      to: "scripts/install/cli/<name>.sh"
      via: "resolveInstallScript name → script path"
      pattern: "resolveInstallScript"
    - from: "auth.ts CLI_AUTH_COMMANDS keys"
      to: "install-scripts.ts SUPPORTED_CLIS"
      via: "auth.test.ts asserts key set equals tuple"
      pattern: "Object.keys\\(CLI_AUTH_COMMANDS\\)"
    - from: "installer.test.ts name array"
      to: "install-scripts.ts SUPPORTED_CLIS tuple"
      via: "toEqual([...20 ids in tuple order])"
      pattern: "expect\\(\\[\\.\\.\\.SUPPORTED_CLIS\\]\\)\\.toEqual"
    - from: "detector.test.ts CLI_BIN_NAMES['cursor-agent']"
      to: "Plan 02 cursor-agent.sh install binary"
      via: "identical binary name cursor-agent (BLOCKER 1)"
      pattern: "cursor-agent"
---

<objective>
Register all 15 new CLIs across the 6 drift-locked files and update the drift-lock vitest so the cli-installer suite is green with 20 CLIs.

Purpose: The install scripts (Plans 01-03) are inert until each CLI name is added consistently to the RCE whitelist, the CliName type, the auth map, the detector bin/version maps, the panel patch JS, and the shell-bridge whitelist. This is the ONE serialized plan because all six files + the three tests are shared across every CLI — splitting it would create parallel write conflicts (LAX-253).
Output: 6 edited source/patch files + 3 edited test files; new total count = 20 everywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md

<interfaces>
<!-- Source of truth: RESEARCH.md "Drift-Lock Checklist" (exact files + line numbers) + "CLI_AUTH_COMMANDS additions (auth.ts)" code block + fact-table binary names. -->

The 15 new names to add (use these EXACT key literals everywhere — they match the .sh filenames from Plans 01-03). THIS IS THE CANONICAL ORDER (tuple + panel array + installer.test.ts array all mirror it):
  codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli,   (Wave A)
  goose, factory-droid, cursor-agent,                                (Wave B)
  kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro      (Wave C)
Existing 5 (already present, FIRST, DO NOT re-add): claude-code, opencode, gemini, openclaw, aion-cli.
New total everywhere = 20.

Verified binary names (CLI_BIN_NAMES — RESEARCH Pitfall 2):
  codex→codex  qwen-code→qwen  augment→auggie  github-copilot→copilot  codebuddy→codebuddy  qoder-cli→qodercli
  goose→goose  factory-droid→droid  cursor-agent→cursor-agent  ← MUST be 'cursor-agent' NOT 'agent' (BLOCKER 1)
  kimi-cli→kimi  mistral-vibe→vibe  hermes-agent→hermes  nanobot→nanobot  snow-cli→snow  kiro→kiro
CLI_VERSION_ARGS: use the existing convention (['--version'] for each) — match gemini/opencode.

CLI_AUTH_COMMANDS (auth.ts — verbatim from RESEARCH "CLI_AUTH_COMMANDS additions"):
  codex:          ['codex', ['auth','login']]
  augment:        ['auggie', ['login']]
  'cursor-agent': ['cursor-agent', ['login']]   ← bin 'cursor-agent' == install binary (Plan 02) == CLI_BIN_NAMES (BLOCKER 1)
  'factory-droid':['droid', ['login']]
  goose:          ['goose', ['configure']]
  'qwen-code':    ['qwen', ['auth']]
  'github-copilot':['copilot', []]      // bare TUI → operator types /login
  'qoder-cli':    ['qodercli', []]      // bare TUI
  codebuddy:      ['codebuddy', []]     // bare TUI
  'kimi-cli':     null                  // authHidden
  'mistral-vibe': null                  // authHidden
  'hermes-agent': null                  // authHidden
  nanobot:        null                  // authHidden
  'snow-cli':     null                  // authHidden
  kiro:           null                  // authHidden

Panel CLI_META authHidden:true for: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro (the 6 null-auth CLIs).
Shell-bridge use-cli-auth-bridge.ts CLI_AUTH_COMMANDS: include ONLY the 9 auth-capable new CLIs (codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli, goose, factory-droid, cursor-agent) as name→command strings; OMIT the 6 authHidden ones (no Auth button).

Exact drift-lock locations (RESEARCH checklist — verify at runtime, line numbers may have shifted):
  1. install-scripts.ts: SUPPORTED_CLIS tuple (~lines 17-23), CLI_BIN_NAMES (~43-49), CLI_VERSION_ARGS (~52-58), doc-comment "exactly 5 names" (~line 13)
  2. types.ts: CliName union (~line 7), "The 5 supported CLIs" comment (~line 6)
  3. auth.ts: CLI_AUTH_COMMANDS, header "exactly 5 keys" (~line 11), PATH array (~198-206) — add snow-cli's npm-link bin dir ONLY if not already covered
  4. local-agents-install-section.js: SUPPORTED_CLIS array (~line 26), CLI_META (~30-36), hint text "5 supported CLI agents" (~line 271)
  5. use-cli-auth-bridge.ts: CLI_AUTH_COMMANDS map (~lines 35-40)
  6. detector.ts PATH array (~121-129) — add snow-cli's link dir ONLY if not already in {~/.local/bin, ~/.opencode/bin, /opt/livos/bin, ~/.bun/bin, ~/.npm-global/bin, /usr/local/bin}

Vitest assertions — CURRENT shape (verified from source):
  installer.test.ts ~143-159:
    expect([...SUPPORTED_CLIS]).toEqual(['claude-code','opencode','gemini','openclaw','aion-cli'])  ← ARRAY-EQUALITY (BLOCKER 2)
    expect(SUPPORTED_CLIS_SET.size).toBe(5)
  auth.test.ts ~316-321:
    const keys = Object.keys(CLI_AUTH_COMMANDS).sort()
    expect(keys).toEqual([...SUPPORTED_CLIS].sort())   ← auto-passes once both 20
    expect(keys.length).toBe(5)
  detector.test.ts ~87: expect(CLI_BIN_NAMES['claude-code']).toBe('claude')  ← per-CLI bin assert style, NO count
  DO NOT touch INSTALL_TIMEOUT_MS / AUTH_TIMEOUT_MS === 300_000 assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 15 names in the 3 livinityd source maps (install-scripts.ts, types.ts, detector.ts)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts (SUPPORTED_CLIS tuple + CLI_BIN_NAMES + CLI_VERSION_ARGS + resolveInstallScript + doc-comment)
    - livos/packages/livinityd/source/modules/cli-installer/types.ts (CliName union, line 7)
    - livos/packages/livinityd/source/modules/cli-installer/detector.ts (PATH array ~121-129)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → "Drift-Lock Checklist" rows 1, 2, 6 + Pitfall 2 binary names
  </read_first>
  <action>
    1. types.ts: extend the CliName union (line 7) with all 15 new string literals in the canonical
       order: 'codex' | 'qwen-code' | 'augment' | 'github-copilot' | 'codebuddy' | 'qoder-cli' |
       'goose' | 'factory-droid' | 'cursor-agent' | 'kimi-cli' | 'mistral-vibe' | 'hermes-agent' |
       'nanobot' | 'snow-cli' | 'kiro'. Update the "The 5 supported CLIs" comment to "20".
    2. install-scripts.ts:
       - add all 15 names to the SUPPORTED_CLIS tuple (existing 5 first, then the 15 in the
         canonical order above — this EXACT order is mirrored by installer.test.ts in Task 3).
       - add CLI_BIN_NAMES entries using the VERIFIED binaries: codex→'codex', qwen-code→'qwen',
         augment→'auggie', github-copilot→'copilot', codebuddy→'codebuddy', qoder-cli→'qodercli',
         goose→'goose', factory-droid→'droid', cursor-agent→'cursor-agent', kimi-cli→'kimi',
         mistral-vibe→'vibe', hermes-agent→'hermes', nanobot→'nanobot', snow-cli→'snow', kiro→'kiro'.
         CRITICAL (BLOCKER 1): cursor-agent→'cursor-agent' (NOT 'agent'). The detector probes the
         binary named in CLI_BIN_NAMES; this MUST equal the binary Plan 02's cursor-agent.sh pins
         on PATH (`cursor-agent`). Same name across install (Plan 02) + detect (here) + auth
         (Task 2) prevents the G13d detect-after-install false-negative.
       - add CLI_VERSION_ARGS entries (['--version'] for each, matching the existing convention).
       - confirm resolveInstallScript maps each name → scripts/install/cli/<name>.sh (the .sh
         basenames from Plans 01-03 match these keys exactly; if resolveInstallScript derives the
         path from the name, no per-CLI edit needed — verify).
       - update the "exactly 5 names" doc-comment to 20.
    3. detector.ts: the install dirs for all new CLIs are ~/.npm-global/bin (Wave A) or ~/.local/bin
       (Wave B/C), already in the PATH array. Add snow-cli's npm-link bin dir ONLY if Plan 03's
       snow-cli.sh linked somewhere outside that set (check the SUMMARY / script). If snow links
       into ~/.npm-global/bin or ~/.local/bin (or symlinked into ~/.local/bin per 253-03), no edit.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd exec tsc --noEmit -p packages/livinityd/tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - install-scripts.ts contains all 15 new ids
    - install-scripts.ts CLI_BIN_NAMES contains 'qwen' and 'auggie' and 'qodercli' and 'droid' and 'cursor-agent' (the binary names that differ from their key)
    - install-scripts.ts CLI_BIN_NAMES['cursor-agent'] === 'cursor-agent' (NOT 'agent') — BLOCKER 1
    - types.ts CliName union contains 'cursor-agent' and 'mistral-vibe' and 'kiro'
    - install-scripts.ts no longer contains a doc-comment asserting "exactly 5" (updated to 20)
    - the livinityd tsc --noEmit pass reports no errors in the cli-installer module
  </acceptance_criteria>
  <done>SUPPORTED_CLIS has 20 names, CliName union has 20 literals, CLI_BIN_NAMES/CLI_VERSION_ARGS cover all new CLIs with verified binaries (cursor-agent→'cursor-agent'), and the module type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth + panel + shell-bridge (auth.ts, local-agents-install-section.js, use-cli-auth-bridge.ts)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/auth.ts (CLI_AUTH_COMMANDS map + PATH array + doc-comment)
    - scripts/aionui-patches/local-agents-install-section.js (SUPPORTED_CLIS array + CLI_META + hint text)
    - livos/packages/ui/src/hooks/use-cli-auth-bridge.ts (CLI_AUTH_COMMANDS map)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → "CLI_AUTH_COMMANDS additions (auth.ts)" code block (verbatim values) + Drift-Lock rows 3, 4, 5 + Pitfall 4 (TUI auth)
  </read_first>
  <action>
    1. auth.ts CLI_AUTH_COMMANDS — add all 15 entries EXACTLY as the RESEARCH "CLI_AUTH_COMMANDS additions" block:
       codex→['codex',['auth','login']], augment→['auggie',['login']], cursor-agent→['cursor-agent',['login']],
       factory-droid→['droid',['login']], goose→['goose',['configure']], qwen-code→['qwen',['auth']],
       github-copilot→['copilot',[]], qoder-cli→['qodercli',[]], codebuddy→['codebuddy',[]],
       kimi-cli→null, mistral-vibe→null, hermes-agent→null, nanobot→null, snow-cli→null, kiro→null.
       cursor-agent's bin MUST be 'cursor-agent' (== install binary Plan 02 == CLI_BIN_NAMES Task 1 — BLOCKER 1).
       Update the "exactly 5 keys" header doc-comment to 20. (auth.test.ts asserts keys === SUPPORTED_CLIS, so all 20 keys must be present; null counts as a key.)
       If Plan 03's snow-cli linked outside the covered PATH dirs, add that dir to auth.ts's PATH array too (mirror detector.ts).
    2. local-agents-install-section.js — add all 15 names to SUPPORTED_CLIS (canonical order) and a CLI_META[name] entry (label + icon) for each. Set authHidden:true on the 6 null-auth CLIs: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro. Use sensible human labels ('Codex', 'Qwen Code', 'Augment', 'GitHub Copilot', 'CodeBuddy', 'Qoder', 'Goose', 'Factory Droid', 'Cursor Agent', 'Kimi CLI', 'Mistral Vibe', 'Hermes Agent', 'Nanobot', 'Snow CLI', 'Kiro'). Update the "5 supported CLI agents" hint text to 20.
    3. use-cli-auth-bridge.ts CLI_AUTH_COMMANDS — add ONLY the 9 auth-capable new CLIs as name→command strings (matching auth.ts: codex→'codex auth login', augment→'auggie login', cursor-agent→'cursor-agent login', factory-droid→'droid login', goose→'goose configure', qwen-code→'qwen auth', github-copilot→'copilot', qoder-cli→'qodercli', codebuddy→'codebuddy'). Do NOT add the 6 authHidden CLIs (their absence = no Auth button — the intended behavior).
  </action>
  <verify>
    <automated>cd livos && node -e "new Function(require('fs').readFileSync('../scripts/aionui-patches/local-agents-install-section.js','utf8'))" && echo PATCH_JS_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts CLI_AUTH_COMMANDS has 20 keys; cursor-agent → ['cursor-agent', ['login']] (BLOCKER 1); the 6 Wave C CLIs → null
    - auth.ts no longer asserts "exactly 5 keys" (updated to 20)
    - local-agents-install-section.js SUPPORTED_CLIS has all 20 names; CLI_META has authHidden:true for kimi-cli/mistral-vibe/hermes-agent/nanobot/snow-cli/kiro
    - use-cli-auth-bridge.ts CLI_AUTH_COMMANDS contains codex/cursor-agent/goose/qwen-code (auth-capable) and does NOT contain kimi-cli/nanobot/kiro (authHidden)
    - the patch JS parses (node new Function smoke)
  </acceptance_criteria>
  <done>auth.ts has 20 keys with correct subcommands (cursor-agent pinned), the panel lists 20 CLIs with the 6 authHidden, and the shell-bridge whitelists only the auth-capable CLIs.</done>
</task>

<task type="auto">
  <name>Task 3: Drift-lock the vitest — name ARRAY contents (BLOCKER 2) + counts + cursor bin assert (BLOCKER 1)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/installer.test.ts (SUPPORTED_CLIS name-array + size assertions ~lines 143-159)
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/auth.test.ts (keys.length assertion ~lines 316-321)
    - livos/packages/livinityd/source/modules/cli-installer/__tests__/detector.test.ts (CLI_BIN_NAMES assertion style ~line 87)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → Drift-Lock row 8 + "Count constant to bump"
  </read_first>
  <action>
    BLOCKER 2 — this is NOT a count-only bump. installer.test.ts's assertion is ARRAY-EQUALITY
    (`expect([...SUPPORTED_CLIS]).toEqual([...literal 5 ids...])`). A stale 5-entry literal array
    FAILS `toEqual` even after `.size` is bumped to 20. The literal array must be rewritten to
    contain ALL 20 ids in the SAME order as the SUPPORTED_CLIS tuple.

    1. installer.test.ts (the name ARRAY, ~line 148): REPLACE the 5-id literal inside
       `expect([...SUPPORTED_CLIS]).toEqual([ ... ])` with ALL 20 ids — the existing 5
       (claude-code, opencode, gemini, openclaw, aion-cli) FOLLOWED BY the 15 new ids in the EXACT
       SAME canonical order as the tuple (codex, qwen-code, augment, github-copilot, codebuddy,
       qoder-cli, goose, factory-droid, cursor-agent, kimi-cli, mistral-vibe, hermes-agent,
       nanobot, snow-cli, kiro). ORDER MUST MATCH THE TUPLE or `toEqual` fails. Then bump
       `expect(SUPPORTED_CLIS_SET.size).toBe(5)` → `toBe(20)`. Update the `test(...)` description
       text "exactly 5 names" → "20".
    2. auth.test.ts: lines ~317-319 compare `Object.keys(CLI_AUTH_COMMANDS).sort()` to
       `[...SUPPORTED_CLIS].sort()` (auto-passes once both have 20). ALSO bump the explicit
       `expect(keys.length).toBe(5)` → `toBe(20)` (~line 320). IF auth.test.ts asserts any
       SPECIFIC individual keys (not just length), add the 15 new keys in the asserted form too
       (BLOCKER 2 tail). Update the "exactly 5 keys" description → "20". Do NOT touch the
       AUTH_TIMEOUT_MS === 300_000 assertion.
    3. detector.test.ts: add a per-CLI `expect(CLI_BIN_NAMES['<id>']).toBe('<bin>')` assertion for
       each of the 15 new ids (verified binaries), matching the existing claude-code style. MUST
       include `expect(CLI_BIN_NAMES['cursor-agent']).toBe('cursor-agent')` (BLOCKER 1 cross-ref —
       asserts the detector probe binary == the install binary from Plan 02 == CLI_BIN_NAMES, the
       same name across all four maps). The other 14: codex→codex, qwen-code→qwen, augment→auggie,
       github-copilot→copilot, codebuddy→codebuddy, qoder-cli→qodercli, goose→goose,
       factory-droid→droid, kimi-cli→kimi, mistral-vibe→vibe, hermes-agent→hermes, nanobot→nanobot,
       snow-cli→snow, kiro→kiro.
    4. Run the full cli-installer vitest; fix any assertion the run flags until green.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test source/modules/cli-installer 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - installer.test.ts `toEqual([...])` literal name array CONTAINS all 15 new ids (not just .size bumped) — `grep -E "'(codex|qwen-code|augment|github-copilot|codebuddy|qoder-cli|goose|factory-droid|cursor-agent|kimi-cli|mistral-vibe|hermes-agent|nanobot|snow-cli|kiro)'" installer.test.ts` returns all 15 (BLOCKER 2)
    - installer.test.ts name array ORDER matches the SUPPORTED_CLIS tuple order exactly (else toEqual fails)
    - installer.test.ts SUPPORTED_CLIS_SET.size assertion reads 20
    - auth.test.ts keys.length assertion reads 20; if it asserted specific keys, the 15 new keys were added too (BLOCKER 2 tail)
    - detector.test.ts has a bin-name assertion for each of the 15 new CLIs, INCLUDING `expect(CLI_BIN_NAMES['cursor-agent']).toBe('cursor-agent')` (BLOCKER 1 cross-ref)
    - `pnpm --filter livinityd test source/modules/cli-installer` passes (all green)
  </acceptance_criteria>
  <done>The installer.test.ts name ARRAY contains all 20 ids in tuple order (array-equality, not just .size), all count assertions read 20, detector has per-CLI bin asserts incl. the cursor-agent pin, and the full cli-installer vitest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe → livinityd cliInstaller | iframe posts a CLI NAME only; server maps name → fixed script/argv (RCE boundary D-239-07). |
| shell-bridge → terminal | bridge maps name → fixed login command string; never raw command from iframe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-08 | Elevation | cliInstaller whitelist | mitigate | SUPPORTED_CLIS_SET gate rejects any name not in the 20-tuple before spawn; every new entry maps to a FIXED script path / argv. |
| T-253-09 | Tampering | shell-bridge command map | mitigate | use-cli-auth-bridge maps name → fixed command string; authHidden CLIs absent (no button); no iframe-supplied command. |
| T-253-10 | Spoofing | binary collisions (cursor `agent`, `snow`) | mitigate | Use verified binary names in CLI_BIN_NAMES (`cursor-agent`, not `agent`); document `snow` collision. |
</threat_model>

<verification>
- livinityd tsc --noEmit clean (no cli-installer errors).
- cli-installer vitest suite green: installer.test.ts name array = 20 ids in tuple order (BLOCKER 2), .size===20, auth keys.length===20, detector bin asserts incl. cursor-agent (BLOCKER 1).
- patch JS parses; shell-bridge omits authHidden CLIs.
- all 20 names appear in install-scripts.ts, auth.ts, types.ts, patch JS.
</verification>

<success_criteria>
All 15 new CLIs registered across the 6 drift-locked files with the verified binary + auth facts; the drift-lock vitest is green with 20 CLIs — name array order-locked to the tuple (BLOCKER 2), counts at 20, per-CLI bin asserts incl. the cursor-agent pin (BLOCKER 1); livinityd type-checks. Existing 5 CLIs unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-04-SUMMARY.md`
</output>
