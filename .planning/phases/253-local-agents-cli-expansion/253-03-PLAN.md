---
phase: 253-local-agents-cli-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install/cli/kimi-cli.sh
  - scripts/install/cli/mistral-vibe.sh
  - scripts/install/cli/hermes-agent.sh
  - scripts/install/cli/nanobot.sh
  - scripts/install/cli/snow-cli.sh
  - scripts/install/cli/kiro.sh
autonomous: true
requirements: [LAX-253]

must_haves:
  truths:
    - "Each of the 6 Wave C CLIs has a self-contained install-only script under scripts/install/cli/"
    - "Python/uv CLIs (kimi, mistral-vibe, hermes) use their official installer that bootstraps uv (NOT the npm template)"
    - "nanobot uses pip/release binary; snow-cli uses git-clone build; kiro uses install-only with API-key note"
    - "All 6 land their binary on a PATH dir covered by detector/auth (snow-cli's npm-link dir gets added in Plan 04 if needed) and persist via .profile"
    - "Each script is idempotent and bash -n clean; all 6 are authHidden (no auth wiring here)"
  artifacts:
    - path: "scripts/install/cli/kimi-cli.sh"
      provides: "official uv-bootstrapping installer for kimi (binary kimi)"
      contains: "code.kimi.com/install.sh"
    - path: "scripts/install/cli/mistral-vibe.sh"
      provides: "official uv-bootstrapping installer for vibe (binary vibe)"
      contains: "mistral.ai/vibe/install.sh"
    - path: "scripts/install/cli/hermes-agent.sh"
      provides: "official installer for hermes (binary hermes)"
      contains: "NousResearch/hermes-agent"
    - path: "scripts/install/cli/nanobot.sh"
      provides: "pip/release-binary install for nanobot (binary nanobot)"
      contains: "nanobot"
    - path: "scripts/install/cli/snow-cli.sh"
      provides: "git-clone build-from-source for snow (binary snow)"
      contains: "MayDay-wpf/snow-cli"
    - path: "scripts/install/cli/kiro.sh"
      provides: "install-only kiro (binary kiro), API-key headless note"
      contains: "kiro"
  key_links:
    - from: "scripts/install/cli/<name>.sh"
      to: "~/.local/bin"
      via: "official installer / uv shim / pip shim"
      pattern: "HOME./.local/bin"
    - from: "scripts/install/cli/<name>.sh"
      to: "~/.profile"
      via: "LivOS CLI PATH marker append"
      pattern: "LivOS CLI PATH"
---

<objective>
Create the six Wave C (install-only / authHidden) Local Agents install scripts: kimi-cli, mistral-vibe, hermes-agent, nanobot, snow-cli, kiro.

Purpose: These CLIs are Python/uv (kimi, mistral-vibe, hermes), an MCP host (nanobot), build-from-source (snow-cli), or have an unpinned installer (kiro). Per the operator scope decision (ship ALL 15) and the deferred-ideas rule, they ship install-only with authHidden rather than being dropped (LAX-253). Their official installers bootstrap uv themselves where Python is needed.
Output: 6 new shell scripts under scripts/install/cli/, file-disjoint from all other plans (parallel-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md

<interfaces>
<!-- Source of truth: RESEARCH.md fact table (kimi-cli / mistral-vibe / hermes-agent / nanobot / snow-cli / kiro rows) + "Pattern 3: Python/uv install script" + Pitfall 5 (Non-Node CLIs) + "Code Examples → Wave C Python/uv script (Kimi)". -->

Wave C facts (verbatim from RESEARCH.md):
  kimi-cli      → curl -LsSf https://code.kimi.com/install.sh | bash   → binary `kimi` → ~/.local/bin (uv shim, Python; installer bootstraps uv)
  mistral-vibe  → curl -LsSf https://mistral.ai/vibe/install.sh | bash → binary `vibe` → ~/.local/bin (uv shim, Python; installer bootstraps uv)
  hermes-agent  → curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash → binary `hermes` → ~/.local/bin (heavy: uv+Python+Node+ripgrep+ffmpeg)
  nanobot       → pip install nanobot-ai (fallback: release Go binary github.com/nanobot-ai/nanobot) → binary `nanobot` → ~/.local/bin (MCP host; NO auth — config/env driven)
  snow-cli      → git clone https://github.com/MayDay-wpf/snow-cli && cd snow-cli && npm install && npm run link → binary `snow` (Ink TUI; build-from-source; collides w/ Snowflake `snow`)
  kiro          → install-only; Kiro CLI 2.0 (rebranded Amazon Q), installer one-liner UNVERIFIED → ship install-only with API-key headless note → binary `kiro`

  ALL 6 are authHidden (null auth) — auth wiring is intentionally absent; Plan 04 sets CLI_AUTH_COMMANDS=null + authHidden:true for these.

Locked conventions (RESEARCH Pattern 3):
  export PATH="${HOME}/.local/bin:${HOME}/.cargo/bin:/usr/local/bin:${PATH}" before idempotency
  idempotency: command -v <bin> >/dev/null 2>&1 && { ok "..."; exit 0; }
  run official installer; on failure `fail "<name>: installer failed" 75`
  hash -r + re-verify command -v <bin>
  .profile marker append (same 'LivOS CLI PATH' grep-guard)
  DO NOT shoehorn Python CLIs into the npm template.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Python/uv installers — kimi-cli.sh, mistral-vibe.sh, hermes-agent.sh</name>
  <read_first>
    - scripts/install/cli/opencode.sh (curl-installer skeleton to mirror — Python installers use the same thin-wrapper shape)
    - scripts/install/cli/_logging.sh (sourced helpers)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → "Pattern 3: Python/uv install script", "Code Examples → Wave C Python/uv script (Kimi)", and fact-table rows kimi-cli / mistral-vibe / hermes-agent
  </read_first>
  <action>
    Create three thin-wrapper scripts pointing at each CLI's OFFICIAL installer (which bootstraps uv/Python itself — do NOT add a custom uv install step):

    scripts/install/cli/kimi-cli.sh — binary `kimi`, installer `curl -LsSf https://code.kimi.com/install.sh | bash`.
    scripts/install/cli/mistral-vibe.sh — binary `vibe`, installer `curl -LsSf https://mistral.ai/vibe/install.sh | bash`.
    scripts/install/cli/hermes-agent.sh — binary `hermes`, installer `curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash`.

    Each MUST:
    - `set -euo pipefail`, source ../_logging.sh
    - `export PATH="${HOME}/.local/bin:${HOME}/.cargo/bin:/usr/local/bin:${PATH}"` BEFORE the idempotency probe (uv shims land in ~/.local/bin; uv itself may go to ~/.cargo/bin)
    - idempotency `command -v <bin> >/dev/null 2>&1 && { ok "✓ <bin> already installed"; exit 0; }`
    - run the official installer; on failure `fail "<name>: official installer failed" 75`
    - re-export ~/.local/bin, `hash -r 2>/dev/null || true`, re-verify `command -v <bin>` (fail 75 if missing)
    - append the `.profile` `LivOS CLI PATH` marker (idempotent grep-guard)
    - end with `ok` printing `<bin> --version` (tolerate non-zero version exit with `|| echo unknown`)
    Self-contained scripts; no sibling delegate; no npm template.
  </action>
  <verify>
    <automated>bash -n scripts/install/cli/kimi-cli.sh && bash -n scripts/install/cli/mistral-vibe.sh && bash -n scripts/install/cli/hermes-agent.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install/cli/kimi-cli.sh` exits 0
    - `bash -n scripts/install/cli/mistral-vibe.sh` exits 0
    - `bash -n scripts/install/cli/hermes-agent.sh` exits 0
    - scripts/install/cli/kimi-cli.sh contains 'code.kimi.com/install.sh'
    - scripts/install/cli/kimi-cli.sh contains 'command -v kimi'
    - scripts/install/cli/mistral-vibe.sh contains 'mistral.ai/vibe/install.sh'
    - scripts/install/cli/mistral-vibe.sh contains 'command -v vibe'
    - scripts/install/cli/hermes-agent.sh contains 'NousResearch/hermes-agent'
    - scripts/install/cli/hermes-agent.sh contains 'command -v hermes'
    - none of the 3 scripts contains 'npm install -g' (must use official Python installers)
    - each of the 3 contains 'set -euo pipefail' and 'LivOS CLI PATH'
  </acceptance_criteria>
  <done>kimi-cli.sh, mistral-vibe.sh, hermes-agent.sh exist, bash -n clean, use official Python installers (no npm), prepend ~/.local/bin + ~/.cargo/bin, and append the .profile marker.</done>
</task>

<task type="auto">
  <name>Task 2: Write nanobot.sh, snow-cli.sh, kiro.sh</name>
  <read_first>
    - scripts/install/cli/opencode.sh (curl/install skeleton)
    - scripts/install/cli/aion-cli.sh (the install-only / best-effort precedent — the closest analog for nanobot/snow/kiro)
    - scripts/install/cli/kimi-cli.sh (the sibling Wave C script from Task 1)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → fact-table rows nanobot / snow-cli / kiro + Pitfall 5 + Anti-Pattern notes (snow `snow` collision, kiro unverified installer)
  </read_first>
  <action>
    Create three install-only scripts (all authHidden; aion-cli.sh is the precedent for best-effort install-only):

    scripts/install/cli/nanobot.sh — binary `nanobot` (MCP host, no auth).
      Install: `pip install --user nanobot-ai` (preferred; brew not guaranteed on server). Optional fallback comment noting the release Go binary at github.com/nanobot-ai/nanobot if pip unavailable.
      Ensure pip's user-bin (~/.local/bin) is on PATH.

    scripts/install/cli/snow-cli.sh — binary `snow` (build-from-source; collides with Snowflake `snow`).
      Install: git-clone https://github.com/MayDay-wpf/snow-cli into a user-writable dir (e.g. ~/.livos/snow-cli), `npm install`, `npm run link`.
      Idempotency probe on `command -v snow`. Note the Snowflake-CLI name collision in a comment.
      (Plan 04 will add snow's npm-link bin dir to detector/auth PATH arrays if it is not ~/.npm-global/bin — the script MUST print where it linked so Plan 04 can confirm the dir.)

    scripts/install/cli/kiro.sh — binary `kiro` (install-only, API-key headless note).
      Since the installer one-liner is UNVERIFIED (RESEARCH A9), the script MUST be best-effort like aion-cli.sh: attempt the kiro.dev install path if known, otherwise `warn` that kiro requires manual install + an API-key env var for headless, and exit 0 (install-only, non-fatal) OR `fail ... 75` only on a confirmed-attempt failure. Do NOT guess a wrong package name (the aion-cli two-name-guess anti-pattern). Prefer: warn + exit 0 so the panel still lists it install-only.

    All three: `set -euo pipefail`, source ../_logging.sh, ~/.local/bin on PATH, idempotency probe, .profile `LivOS CLI PATH` marker, self-contained. authHidden (no auth wiring here).
  </action>
  <verify>
    <automated>bash -n scripts/install/cli/nanobot.sh && bash -n scripts/install/cli/snow-cli.sh && bash -n scripts/install/cli/kiro.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install/cli/nanobot.sh` exits 0
    - `bash -n scripts/install/cli/snow-cli.sh` exits 0
    - `bash -n scripts/install/cli/kiro.sh` exits 0
    - scripts/install/cli/nanobot.sh contains 'nanobot-ai'
    - scripts/install/cli/nanobot.sh contains 'command -v nanobot'
    - scripts/install/cli/snow-cli.sh contains 'MayDay-wpf/snow-cli'
    - scripts/install/cli/snow-cli.sh contains 'command -v snow'
    - scripts/install/cli/kiro.sh contains 'command -v kiro'
    - scripts/install/cli/kiro.sh contains 'kiro' (and does NOT contain an invented npm package literal)
    - each of the 3 contains 'set -euo pipefail' and 'LivOS CLI PATH'
  </acceptance_criteria>
  <done>nanobot.sh, snow-cli.sh, kiro.sh exist, bash -n clean. nanobot uses pip nanobot-ai, snow-cli git-clone-builds MayDay-wpf/snow-cli, kiro is best-effort install-only with an API-key note (no guessed package). All append the .profile marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install script → installer host | curl/pip/git fetch from official HTTPS hosts (code.kimi.com, mistral.ai, raw.githubusercontent.com/NousResearch, PyPI, github.com). |
| iframe → livinityd | name-only; whitelist-gated in Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-05 | Tampering | Python/curl installers | mitigate | Only official installer hosts over HTTPS; uv bootstrapped by the CLI's own installer (no custom uv fetch). |
| T-253-06 | Spoofing | snow binary collision | mitigate | Document the `snow` ↔ Snowflake-CLI collision; snow-cli ships install-only (authHidden) and prints its link dir. |
| T-253-07 | Tampering | kiro unverified installer | accept | Installer one-liner unverified (RESEARCH A9); ship best-effort install-only with API-key note, no guessed package name — degrades to a warn, not a wrong install. |
</threat_model>

<verification>
- All 6 Wave C scripts pass `bash -n`.
- Python scripts (kimi/mistral/hermes) contain NO `npm install -g`.
- kiro.sh contains no invented npm/pip package literal (best-effort warn path).
</verification>

<success_criteria>
6 self-contained Wave C install-only scripts exist, each bash -n clean, each using the correct mechanism (official Python installer / pip / git-build / best-effort) with the .profile marker. All are authHidden. No file overlap with Plans 01/02/04.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-03-SUMMARY.md`
</output>
