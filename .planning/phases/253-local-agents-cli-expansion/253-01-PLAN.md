---
phase: 253-local-agents-cli-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install/cli/codex.sh
  - scripts/install/cli/qwen-code.sh
  - scripts/install/cli/augment.sh
  - scripts/install/cli/github-copilot.sh
  - scripts/install/cli/codebuddy.sh
  - scripts/install/cli/qoder-cli.sh
autonomous: true
requirements: [LAX-253]

must_haves:
  truths:
    - "Each of the 6 Wave A CLIs has a self-contained install script under scripts/install/cli/"
    - "Each script uses the exact scoped npm package name from RESEARCH (no unscoped names)"
    - "Each script installs to ~/.npm-global prefix (G15 EACCES fix) and persists PATH via .profile (G20.1)"
    - "Each script is idempotent (command -v short-circuit) and bash -n clean"
  artifacts:
    - path: "scripts/install/cli/codex.sh"
      provides: "npm-global install for @openai/codex (binary codex)"
      contains: "@openai/codex"
    - path: "scripts/install/cli/qwen-code.sh"
      provides: "npm-global install for @qwen-code/qwen-code (binary qwen)"
      contains: "@qwen-code/qwen-code"
    - path: "scripts/install/cli/augment.sh"
      provides: "npm-global install for @augmentcode/auggie (binary auggie)"
      contains: "@augmentcode/auggie"
    - path: "scripts/install/cli/github-copilot.sh"
      provides: "npm-global install for @github/copilot (binary copilot)"
      contains: "@github/copilot"
    - path: "scripts/install/cli/codebuddy.sh"
      provides: "npm-global install for @tencent-ai/codebuddy-code (binary codebuddy)"
      contains: "@tencent-ai/codebuddy-code"
    - path: "scripts/install/cli/qoder-cli.sh"
      provides: "npm-global install for @qoder-ai/qodercli (binary qodercli)"
      contains: "@qoder-ai/qodercli"
  key_links:
    - from: "scripts/install/cli/<name>.sh"
      to: "~/.npm-global/bin"
      via: "npm install -g --prefix ~/.npm-global"
      pattern: "npm install -g --prefix"
    - from: "scripts/install/cli/<name>.sh"
      to: "~/.profile"
      via: "LivOS CLI PATH marker append"
      pattern: "LivOS CLI PATH"
---

<objective>
Create the six Wave A (npm-global) Local Agents install scripts: codex, qwen-code, augment, github-copilot, codebuddy, qoder-cli.

Purpose: These are the clean npm-package CLIs from AionUi's Multi-Agent roster (LAX-253). Each gets a self-contained script mirroring the verified gemini.sh pattern so the deploy glob picks it up automatically and livinityd's cliInstaller can spawn it.
Output: 6 new executable shell scripts under scripts/install/cli/, each file-disjoint from every other plan (parallel-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/253-local-agents-cli-expansion/253-CONTEXT.md
@.planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md

<interfaces>
<!-- Source of truth: RESEARCH.md "Per-CLI Fact Table" + "Code Examples → Wave A npm-global script". -->
<!-- The executor MUST mirror the EXACT structure of scripts/install/cli/gemini.sh (npm-global prefix + G20.1 .profile PATH). -->

Wave A facts (verbatim from RESEARCH.md fact table):
  codex          → npm @openai/codex            → binary `codex`     → Node 22+
  qwen-code      → npm @qwen-code/qwen-code@latest → binary `qwen`   → Node 20+  (binary is `qwen`, NOT `qwen-code`)
  augment        → npm @augmentcode/auggie      → binary `auggie`    → Node 20+  (binary `auggie`, NOT `augment`)
  github-copilot → npm @github/copilot          → binary `copilot`   → Node 22+  (NEW @github/copilot, NOT gh extension)
  codebuddy      → npm @tencent-ai/codebuddy-code → binary `codebuddy` → Node 18.20+
  qoder-cli      → npm @qoder-ai/qodercli       → binary `qodercli`  → Node      (binary `qodercli`, NOT `qoder`)

Locked PATH/persistence conventions (from RESEARCH "Pattern 1" + Code Examples):
  NPM_PREFIX="${HOME}/.npm-global"  → mkdir -p, npm install -g --prefix "$NPM_PREFIX" <pkg>
  export PATH="${NPM_PREFIX}/bin:${HOME}/.local/bin:/usr/local/bin:${PATH}" before idempotency check
  idempotency: command -v <bin> >/dev/null 2>&1 && { ok "..."; exit 0; }
  .profile marker (idempotent grep-guard on string 'LivOS CLI PATH'):
    printf '\n# LivOS CLI PATH (login shells read .profile, not .bashrc)\nexport PATH="/opt/livos/bin:$HOME/.npm-global/bin:$HOME/.opencode/bin:$HOME/.local/bin:$PATH"\n' >> "${HOME}/.profile"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write codex.sh, qwen-code.sh, augment.sh</name>
  <read_first>
    - scripts/install/cli/gemini.sh (the EXACT npm-global structure to mirror: header, _logging.sh source, NPM_PREFIX, idempotency, npm install, hash -r, verify, .profile marker)
    - scripts/install/cli/_logging.sh (the info/ok/warn/fail/step helpers being sourced)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → "Per-CLI Fact Table" rows codex / qwen-code / augment, and "Code Examples → Wave A npm-global script" (verbatim template)
  </read_first>
  <action>
    Create three scripts, each a near-verbatim copy of gemini.sh with only the package name + binary name swapped (per RESEARCH fact table):

    scripts/install/cli/codex.sh — package `@openai/codex`, binary `codex`.
      CRITICAL: the scoped name `@openai/codex` (unscoped `codex` is an unrelated 2012 package — the silent-fail trap).
    scripts/install/cli/qwen-code.sh — package `@qwen-code/qwen-code@latest`, binary `qwen` (NOT qwen-code).
    scripts/install/cli/augment.sh — package `@augmentcode/auggie`, binary `auggie` (NOT augment).

    Every script MUST:
    - start with `set -euo pipefail`
    - source ../_logging.sh exactly as gemini.sh does (same SCRIPT_DIR + fallback-stubs pattern)
    - set NPM_PREFIX="${HOME}/.npm-global", mkdir -p it, and `export PATH="${NPM_PREFIX}/bin:${HOME}/.local/bin:/usr/local/bin:${PATH}"` BEFORE the idempotency probe
    - idempotency: `command -v <bin> >/dev/null 2>&1 && { ok "✓ <bin> already installed"; exit 0; }`
    - guard `command -v npm` (fail with code 75 if missing)
    - `npm install -g --prefix "${NPM_PREFIX}" <exact-package>` (fail 75 on error)
    - `hash -r 2>/dev/null || true` then re-verify `command -v <bin>` on PATH (fail 75 if not found)
    - append the `.profile` LivOS CLI PATH marker with the idempotent `grep -qF 'LivOS CLI PATH'` guard, EXACTLY as in gemini.sh / RESEARCH Code Examples
    - end with an `ok` line printing `<bin> --version`

    Do NOT use the openclaw sibling-delegate pattern (each script must be fully self-contained in cli/).
  </action>
  <verify>
    <automated>bash -n scripts/install/cli/codex.sh && bash -n scripts/install/cli/qwen-code.sh && bash -n scripts/install/cli/augment.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install/cli/codex.sh` exits 0
    - `bash -n scripts/install/cli/qwen-code.sh` exits 0
    - `bash -n scripts/install/cli/augment.sh` exits 0
    - scripts/install/cli/codex.sh contains '@openai/codex'
    - scripts/install/cli/qwen-code.sh contains '@qwen-code/qwen-code'
    - scripts/install/cli/qwen-code.sh contains 'command -v qwen'
    - scripts/install/cli/augment.sh contains '@augmentcode/auggie'
    - scripts/install/cli/augment.sh contains 'command -v auggie'
    - each of the 3 scripts contains 'set -euo pipefail'
    - each of the 3 scripts contains 'LivOS CLI PATH'
    - each of the 3 scripts contains 'npm install -g --prefix'
  </acceptance_criteria>
  <done>All three scripts exist, are bash -n clean, contain the exact scoped package names and correct binary probes, and include the .profile PATH marker.</done>
</task>

<task type="auto">
  <name>Task 2: Write github-copilot.sh, codebuddy.sh, qoder-cli.sh</name>
  <read_first>
    - scripts/install/cli/gemini.sh (same npm-global structure)
    - scripts/install/cli/codex.sh (the sibling Wave A script just created in Task 1 — use as the in-phase template)
    - .planning/phases/253-local-agents-cli-expansion/253-RESEARCH.md → fact-table rows github-copilot / codebuddy / qoder-cli (note: these three are TUI-auth CLIs, but the INSTALL script is identical npm-global; auth wiring happens in Plan 04)
  </read_first>
  <action>
    Create three more npm-global scripts, identical structure to Task 1, swapping package + binary:

    scripts/install/cli/github-copilot.sh — package `@github/copilot`, binary `copilot`.
      CRITICAL: scoped `@github/copilot` (the NEW standalone CLI, NOT the old `gh copilot` extension; unscoped `copilot` is wrong).
    scripts/install/cli/codebuddy.sh — package `@tencent-ai/codebuddy-code`, binary `codebuddy`.
    scripts/install/cli/qoder-cli.sh — package `@qoder-ai/qodercli`, binary `qodercli` (NOT qoder).

    Same MUST-list as Task 1 (set -euo pipefail, _logging.sh source, NPM_PREFIX + PATH export, idempotency probe, npm guard, npm install -g --prefix, hash -r + reverify, .profile marker, version echo).
    Self-contained scripts only — no sibling delegate.
  </action>
  <verify>
    <automated>bash -n scripts/install/cli/github-copilot.sh && bash -n scripts/install/cli/codebuddy.sh && bash -n scripts/install/cli/qoder-cli.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install/cli/github-copilot.sh` exits 0
    - `bash -n scripts/install/cli/codebuddy.sh` exits 0
    - `bash -n scripts/install/cli/qoder-cli.sh` exits 0
    - scripts/install/cli/github-copilot.sh contains '@github/copilot'
    - scripts/install/cli/github-copilot.sh contains 'command -v copilot'
    - scripts/install/cli/codebuddy.sh contains '@tencent-ai/codebuddy-code'
    - scripts/install/cli/codebuddy.sh contains 'command -v codebuddy'
    - scripts/install/cli/qoder-cli.sh contains '@qoder-ai/qodercli'
    - scripts/install/cli/qoder-cli.sh contains 'command -v qodercli'
    - each of the 3 scripts contains 'set -euo pipefail' and 'LivOS CLI PATH'
  </acceptance_criteria>
  <done>All three scripts exist, bash -n clean, contain the exact scoped package names and correct binary probes, and include the .profile PATH marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe → livinityd | iframe sends only a CLI name; livinityd maps name → fixed `bash cli/<name>.sh` (whitelist-gated in Plan 04). |
| install script → npm registry | `curl`/`npm` fetch from public registry over HTTPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-01 | Tampering | npm package resolution | mitigate | Use only the exact scoped package names verified in RESEARCH (`@openai/codex`, `@github/copilot`, `@augmentcode/auggie`, `@qwen-code/qwen-code`, `@tencent-ai/codebuddy-code`, `@qoder-ai/qodercli`); unscoped names are unrelated/hostile packages. |
| T-253-02 | Elevation | install script execution | mitigate | Scripts install to user-writable `~/.npm-global` (no root); whitelist gate in Plan 04 ensures only these named scripts run. |
</threat_model>

<verification>
- All 6 Wave A scripts pass `bash -n`.
- `grep -l 'npm install -g --prefix' scripts/install/cli/{codex,qwen-code,augment,github-copilot,codebuddy,qoder-cli}.sh` lists all 6.
- No script references a sibling install-*.sh outside cli/ (no openclaw-style delegate).
</verification>

<success_criteria>
6 self-contained Wave A npm-global install scripts exist, each bash -n clean, each with the exact scoped package name + correct binary probe + .profile PATH marker. No file overlap with Plans 02/03/04.
</success_criteria>

<output>
After completion, create `.planning/phases/253-local-agents-cli-expansion/253-01-SUMMARY.md`
</output>
