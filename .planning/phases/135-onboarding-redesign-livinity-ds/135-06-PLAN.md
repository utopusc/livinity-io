# Plan 135-06 — Restore Flow Redesign

**Wave:** 4 (after 135-01..05)
**Autonomous:** true
**Sacred SHA invariant:** preserved

## What

Redesign `routes/onboarding/restore.tsx` (356 LOC) — the "restore from backup" entry path. Standalone flow accessible via `/onboarding/restore` (separate from main setup wizard).

## Files touched

- `livos/packages/ui/src/routes/onboarding/restore.tsx`

## How

Apply same DS recipe:
1. Wrap in `OnboardingShell` (already in place from 135-01 since route is under `/onboarding/*`).
2. Replace internal `Layout` from `@/layouts/bare/shared` with `.liv-card`.
3. Replace gradient buttons with `liv-btn` / `liv-btn-secondary`.
4. Re-skin file picker / location list with DS borders + spacing.
5. Progress indicators (if any) use DS color (`--liv-fg` foreground, `--liv-fg-faint` for incomplete).

## Acceptance criteria

- [ ] AC-135-06-1: `/onboarding/restore` shows DS-styled restore flow.
- [ ] AC-135-06-2: File picker / backup-location selection works.
- [ ] AC-135-06-3: Restore action triggers existing tRPC mutation.
- [ ] AC-135-06-4: No console errors.
- [ ] AC-135-06-5: Sacred SHA preserved.

## Atomic commit

```
feat(135-06/restore): redesign /onboarding/restore per Livinity DS

DS card surface, monochrome buttons, DS form inputs. Restore flow logic
(file picker, restore mutation, progress) unchanged.

Files:
  ~ src/routes/onboarding/restore.tsx

Sacred SHA: f3538e1d811992b782a9bb057d1b7f0a0189f95f (preserved)
```
