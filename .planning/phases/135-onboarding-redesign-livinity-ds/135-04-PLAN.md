# Plan 135-04 — Create-Account + Account-Created Redesign

**Wave:** 3 (parallel with 135-05; depends on 135-01)
**Autonomous:** true
**Sacred SHA invariant:** preserved

## What

Two smaller redesigns:
1. `routes/onboarding/create-account.tsx` (104 LOC) — display name + password setup form.
2. `routes/onboarding/account-created.tsx` (61 LOC) — success state + advance.

## Files touched

- `livos/packages/ui/src/routes/onboarding/create-account.tsx`
- `livos/packages/ui/src/routes/onboarding/account-created.tsx`

## How

### `create-account.tsx`

Existing form layout: 1 password field + (optional?) display name input + submit.

DS version:
```tsx
<div className="liv-card" style={{display: 'flex', flexDirection: 'column', gap: 20}}>
    <DsStepIndicator current={1} total={3} />
    <header>
        <h1 style={{fontSize: 28, fontWeight: 600}}>{t('onboarding.create-account.title')}</h1>
        <p style={{color: 'var(--liv-fg-mute)'}}>{t('onboarding.create-account.subtitle')}</p>
    </header>
    <form onSubmit={handleSubmit} style={{display: 'flex', flexDirection: 'column', gap: 16}}>
        <DsLabeledInput
            label={t('onboarding.create-account.password-label')}
            type="password"
            value={password}
            onChange={...}
            autoFocus
        />
        {/* if confirmPassword exists, second input */}
        <button className="liv-btn" type="submit" disabled={!valid}>
            {t('onboarding.create-account.submit')}
        </button>
    </form>
</div>
```

`DsLabeledInput` wraps the existing shadcn `<Input>` with a DS-styled `<label>` block.

### `account-created.tsx`

Existing: success message + auto-navigate or click-to-continue.

DS version:
```tsx
<div className="liv-card" style={{textAlign: 'center', display: 'flex', flexDirection: 'column', gap: 24, padding: 'var(--liv-pad)'}}>
    <div style={{fontSize: 48}}>✓</div>
    <header>
        <h1>{t('onboarding.account-created.title')}</h1>
        <p style={{color: 'var(--liv-fg-mute)'}}>{t('onboarding.account-created.subtitle')}</p>
    </header>
    <Link to='/onboarding' className="liv-btn">
        {t('onboarding.account-created.continue')}
    </Link>
</div>
```

(`✓` could be a Lucide check icon for production — placeholder shown here.)

## Acceptance criteria

- [ ] AC-135-04-1: Create-account form renders + accepts input.
- [ ] AC-135-04-2: Submit triggers existing tRPC mutation (no logic change).
- [ ] AC-135-04-3: Success → `account-created` shows DS-styled confirmation.
- [ ] AC-135-04-4: Click "Continue" → advances to next step.
- [ ] AC-135-04-5: No console errors.
- [ ] AC-135-04-6: Sacred SHA preserved.

## Atomic commit

```
feat(135-04/onboarding-account): redesign create-account + account-created

DS card surface + monochrome typography + DS form inputs. tRPC mutation
flow unchanged.

Files:
  ~ src/routes/onboarding/create-account.tsx
  ~ src/routes/onboarding/account-created.tsx

Sacred SHA: f3538e1d811992b782a9bb057d1b7f0a0189f95f (preserved)
```
