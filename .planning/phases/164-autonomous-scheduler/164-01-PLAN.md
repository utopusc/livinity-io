---
phase: 164-autonomous-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.ts
  - livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.test.ts
  - livos/packages/livinityd/source/modules/autonomous-scheduler/index.ts
autonomous: true
requirements: []
tags:
  - autonomous-scheduler
  - agent-definitions
  - cron-parser
  - phase-164
  - v34
must_haves:
  truths:
    - "parseAgentDefinition(markdown) splits YAML frontmatter from markdown body and returns a normalised AgentDefinition object"
    - "Required fields (name, schedule, model) raise a structured ParseError when missing; optional fields receive defaults (max_turns=20, max_budget_usd=5, allowed_tools=['Read','Bash','Glob','Grep'], mcp_servers=[], enabled=true)"
    - "Schedule field is validated as a standard 5-field cron expression; node-cron's validate() is used since node-cron is already a direct dep of livinityd (D-NO-NEW-DEPS satisfied)"
    - "parseAgentDefinitionsDir(dir) walks the vault/livos-agents/ tree, parses each *.md, and returns {ok: AgentDefinition[], errors: ParseError[]} (partial-failure resilient — one broken file does NOT block others)"
    - "Vitest suite covers happy path, missing required fields, invalid cron, default fill-in, and directory-walk partial-failure modes"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.ts"
      provides: "AgentDefinition type + parseAgentDefinition() + parseAgentDefinitionsDir()"
      contains: "export function parseAgentDefinition"
    - path: "livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.test.ts"
      provides: "vitest coverage for the parser"
      contains: "describe('parseAgentDefinition'"
    - path: "livos/packages/livinityd/source/modules/autonomous-scheduler/index.ts"
      provides: "barrel re-exporting AgentDefinition type + parseAgentDefinition + parseAgentDefinitionsDir"
      contains: "export {parseAgentDefinition"
  key_links:
    - from: "agent-definition-parser.ts"
      to: "node-cron"
      via: "import * as cron from 'node-cron' + cron.validate(schedule)"
      pattern: "node-cron"
    - from: "agent-definition-parser.ts"
      to: "js-yaml"
      via: "import yaml from 'js-yaml' for frontmatter parse (js-yaml is already a transitive dep — see @types/js-yaml in package.json devDeps)"
      pattern: "js-yaml|jsYaml"
---

<objective>
Build the agent-definition parser layer for the v34 autonomous scheduler (D-V34-G). Parse `vault/livos-agents/<name>.md` files — YAML frontmatter (name, schedule cron, model, max_turns, max_budget_usd, allowed_tools, mcp_servers, enabled) + markdown body = agent prompt — into a strongly-typed `AgentDefinition` object the scheduler (164-02) and inbox-writer (164-03) consume.

Purpose: lock the agent definition contract before the scheduler is written so 164-02 has a stable shape to register cron jobs against and 164-04 sample agents can be authored against a known schema.

Output: New module `livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.ts` + vitest suite + barrel `index.ts`. Zero `package.json` diff — `node-cron` + `js-yaml` are already deps.

D-NO-NEW-DEPS check: `node-cron@^3.0.3` is a direct dep of livinityd (confirmed via package.json `dependencies`); `js-yaml` is a transitive dep (confirmed via `@types/js-yaml` in devDeps). No new package adds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-autonomous-scheduler/164-CONTEXT.md
@livos/packages/livinityd/package.json
@livos/packages/livinityd/source/modules/scheduler/index.ts
@livos/packages/livinityd/source/modules/claude-runner/auth-verifier.ts

<interfaces>
<!-- Patterns extracted from sister modules for the executor to mirror. -->

From livos/packages/livinityd/source/modules/scheduler/index.ts (node-cron import shape):
```typescript
import * as cron from 'node-cron'

// node-cron exposes `cron.validate(expr: string): boolean` — the canonical
// validator. Use this. Do NOT hand-roll a 5-field regex.
```

From livos/packages/livinityd/source/modules/claude-runner/vault-scaffolder.ts (resilient walk pattern):
```typescript
import {readdir} from 'node:fs/promises'
// Walk a directory, swallow per-file errors into a structured result.
```

From livos/packages/livinityd/source/modules/claude-runner/auth-verifier.ts (discriminator pattern):
```typescript
export type AuthVerifierResult =
  | {ok: true; model: string}
  | {ok: false; err: string}
// Use the same {ok: true; ...} | {ok: false; err: string} shape for the
// parse result so 164-02 can branch cleanly.
```
</interfaces>

<sacred-guardrails>
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` for `liv/packages/core/src/sdk-agent-runner.ts` — this plan does NOT touch that file.
- D-09 verbatim `livos/packages/livinityd/source/modules/computer-use/luse-system-prompt.ts` — UNCHANGED.
- D-NO-NEW-DEPS — verify `node-cron` + `js-yaml` are already deps; ship zero `package.json` diff.
- Phase 161-02 helper `livos/packages/livinityd/source/modules/ai/agent-prompt-builder.ts` — UNCHANGED.
- `liv/packages/core/src/agent-session.ts` — UNCHANGED.
- Phase 162 vault scaffolder (`livos/packages/livinityd/source/modules/claude-runner/vault-scaffolder.ts`) — UNCHANGED.
- Phase 163 surface routing — UNCHANGED.
</sacred-guardrails>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify node-cron + js-yaml availability (D-NO-NEW-DEPS guard)</name>
  <read_first>
    - livos/packages/livinityd/package.json (confirm node-cron in dependencies, js-yaml present transitively)
    - livos/packages/livinityd/source/modules/scheduler/index.ts (confirm `import * as cron from 'node-cron'` resolves)
  </read_first>
  <files>
    No files modified. This is a verification-only step. Output is a single line in the executor summary.
  </files>
  <behavior>
    - Behavior 1: `grep -nE '"node-cron"' livos/packages/livinityd/package.json` returns at least one match (the production dep) and exit code 0.
    - Behavior 2: `grep -nE 'js-yaml' livos/packages/livinityd/package.json` shows js-yaml is at minimum a transitive dep via `@types/js-yaml`. If `js-yaml` itself is NOT in dependencies, run `node -e "console.log(require.resolve('js-yaml'))"` from `livos/packages/livinityd/` to confirm it resolves through pnpm hoisting; if it does NOT resolve, switch the parser to a minimal hand-rolled YAML-frontmatter regex (key: value lines only — sample agents in 164-04 use no nested YAML, so simple parsing is sufficient).
  </behavior>
  <action>
    1. From repo root, run `grep -nE '"node-cron"|"js-yaml"' livos/packages/livinityd/package.json` and record the exact dep lines.
    2. If `js-yaml` is NOT in `dependencies` of `livos/packages/livinityd/package.json`, attempt resolution from inside the package: `cd livos/packages/livinityd && node -e "console.log(require.resolve('js-yaml'))"`. If it resolves, document the resolved path in the commit message and import `js-yaml` directly. If it does NOT resolve, you MUST implement an inline minimal YAML-frontmatter parser in Task 2 instead of importing js-yaml (5-field schema only — `key: value` lines, no nested objects, no arrays beyond the simple `["Read","Bash"]` JSON-style array shape).
    3. Record findings: in the commit message footer for the Task 2 commit, include a single line `dep-check: node-cron=<version>, js-yaml=<source>`.
    4. Hard guardrail: this step MUST NOT modify `package.json` or `pnpm-lock.yaml`. If a dep would need adding, ABORT the plan and surface a discuss-phase recommendation instead.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && grep -nE '"node-cron"' package.json &amp;&amp; node -e "console.log(require.resolve('node-cron'))"</automated>
  </verify>
  <done>node-cron confirmed as direct dep; js-yaml resolution path determined (direct dep OR transitive OR fallback-to-inline-parser strategy chosen) and recorded for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent-definition-parser.ts (parser + AgentDefinition type) with full vitest coverage</name>
  <read_first>
    - .planning/phases/164-autonomous-scheduler/164-CONTEXT.md (lines 21-55 — exhaustive spec for the YAML schema, defaults, validation rules)
    - livos/packages/livinityd/source/modules/claude-runner/vault-scaffolder.ts (resilient directory walk pattern with non-throw discriminator)
    - livos/packages/livinityd/source/modules/claude-runner/auth-verifier.ts (discriminator result type shape)
    - livos/packages/livinityd/source/modules/scheduler/index.ts (node-cron import + cron.validate() usage)
    - Task 1 dep-check output (decides js-yaml vs inline-parser path)
  </read_first>
  <files>
    livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.ts
    livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.test.ts
  </files>
  <behavior>
    - Test 1 (happy path): parseAgentDefinition() given full frontmatter + body returns `{ok: true; definition: AgentDefinition}` with every field populated and body as a multi-line trimmed string.
    - Test 2 (defaults): parseAgentDefinition() given ONLY required fields (name, schedule, model) returns ok=true with max_turns=20, max_budget_usd=5, allowed_tools=['Read','Bash','Glob','Grep'], mcp_servers=[], enabled=true.
    - Test 3 (missing required name): returns `{ok: false; err: "missing required field: name"}`.
    - Test 4 (missing required schedule): returns `{ok: false; err: "missing required field: schedule"}`.
    - Test 5 (invalid cron): schedule `"not a cron"` returns `{ok: false; err: /invalid cron/i}`.
    - Test 6 (valid cron variants): `"0 3 * * *"`, `"*/30 * * * *"`, `"0 0 * * 0"` all accepted.
    - Test 7 (empty body): markdown with frontmatter but no body returns ok=true with `body: ''` (empty string is legal — the scheduler will fall back to a generic prompt).
    - Test 8 (parseAgentDefinitionsDir partial-failure): given a temp dir with one valid `.md` and one broken `.md` (e.g., malformed frontmatter), returns `{ok: AgentDefinition[1]; errors: ParseError[1]}` — one error recorded, the other still parsed.
    - Test 9 (parseAgentDefinitionsDir ignores non-.md files): a `README.txt` or `.gitkeep` in the dir is silently skipped.
    - Test 10 (parseAgentDefinitionsDir missing dir): returns `{ok: []; errors: [{path: dir, err: /not found|ENOENT/i}]}` instead of throwing.
  </behavior>
  <action>
    1. Create `livos/packages/livinityd/source/modules/autonomous-scheduler/agent-definition-parser.ts` exporting:

       ```typescript
       export interface AgentDefinition {
         name: string                    // matches frontmatter `name` + must match filename minus .md
         schedule: string                // validated cron expression
         model: string                   // e.g. 'claude-sonnet-4-6' | 'claude-haiku-4-5'
         maxTurns: number                // default 20
         maxBudgetUsd: number            // default 5
         allowedTools: string[]          // default ['Read','Bash','Glob','Grep']
         mcpServers: string[]            // default []
         enabled: boolean                // default true
         body: string                    // markdown body = the prompt
         sourcePath: string              // absolute path of the source .md (for inbox backlinks)
       }

       export type ParseResult =
         | {ok: true; definition: AgentDefinition}
         | {ok: false; err: string}

       export interface ParseError {
         path: string
         err: string
       }

       export interface DirParseResult {
         ok: AgentDefinition[]
         errors: ParseError[]
       }

       export function parseAgentDefinition(
         markdown: string,
         sourcePath: string,
       ): ParseResult { /* ... */ }

       export async function parseAgentDefinitionsDir(
         dir: string,
       ): Promise<DirParseResult> { /* ... */ }
       ```

    2. Frontmatter extraction: Match the leading `---\n...\n---\n` block via a non-greedy regex (`/^---\r?\n([\s\S]*?)\r?\n---\r?\n?/`); the body is everything after the closing `---` (trimmed).
    3. YAML parse: If js-yaml resolves (Task 1), use `import yaml from 'js-yaml'` + `yaml.load(fmText, {schema: yaml.FAILSAFE_SCHEMA})` then narrow. If js-yaml does NOT resolve, implement a minimal line-based parser: split by `\n`, each line `key: value`, value parsed as either a JSON literal (`["a","b"]`, `true`, `5`, `"quoted"`) or a bare string. Document the chosen path in a comment at the top of the file.
    4. Cron validation: `import * as cron from 'node-cron'; if (!cron.validate(schedule)) return {ok: false, err: \`invalid cron expression: ${schedule}\`}`.
    5. Default fill-in: AFTER required-field validation, fill defaults for max_turns/max_budget_usd/allowed_tools/mcp_servers/enabled. Required field names match the YAML keys (snake_case in markdown), but the TypeScript interface uses camelCase — do the snake→camel conversion at parse time.
    6. `parseAgentDefinitionsDir(dir)`:
       - Use `fs.readdir(dir, {withFileTypes: true})`.
       - On ENOENT: return `{ok: [], errors: [{path: dir, err: 'directory not found'}]}` (do NOT throw).
       - Filter to entries where `entry.isFile() && entry.name.endsWith('.md')`.
       - For each: `fs.readFile()` then `parseAgentDefinition()`; aggregate into `ok` or `errors`.
       - Stable sort `ok` by `definition.name` so scheduler registration order is deterministic.
    7. Create `agent-definition-parser.test.ts` with the 10 test cases above. Use `tmp` fixture via `fs.mkdtemp(path.join(os.tmpdir(), 'livos-agents-parse-'))` for the dir-walk tests. Cleanup in `afterEach`.
    8. Per livinityd project convention, use tabs for indentation, single quotes for strings, no semicolons at line ends (mirror the style of vault-scaffolder.ts).
    9. Commit: `feat(164-01): add agent-definition-parser with frontmatter + cron validation`. Append the dep-check footer line from Task 1.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd &amp;&amp; npm run test:run -- modules/autonomous-scheduler/agent-definition-parser.test.ts</automated>
  </verify>
  <done>All 10 vitest cases PASS. `agent-definition-parser.ts` exports the AgentDefinition + ParseResult + parseAgentDefinition + parseAgentDefinitionsDir public API. `package.json` + `pnpm-lock.yaml` unchanged (verify with `git diff -- livos/packages/livinityd/package.json` and `git diff -- pnpm-lock.yaml` — both must be empty).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Barrel index.ts + tsc clean</name>
  <read_first>
    - livos/packages/livinityd/source/modules/claude-runner/index.ts (canonical barrel pattern)
  </read_first>
  <files>
    livos/packages/livinityd/source/modules/autonomous-scheduler/index.ts
  </files>
  <action>
    1. Create `livos/packages/livinityd/source/modules/autonomous-scheduler/index.ts`:

       ```typescript
       // Phase 164 — Autonomous scheduler module barrel.
       //
       // Re-exports the canonical parser API (Phase 164-01). Later plans add
       // the scheduler + inbox-writer exports.

       export {
         parseAgentDefinition,
         parseAgentDefinitionsDir,
       } from './agent-definition-parser.js'

       export type {
         AgentDefinition,
         ParseResult,
         ParseError,
         DirParseResult,
       } from './agent-definition-parser.js'
       ```

    2. Run `cd livos/packages/livinityd && npm run typecheck` and confirm zero new errors (pre-existing errors in unrelated files are acceptable; compare against the pre-task baseline).
    3. Commit: `feat(164-01): export autonomous-scheduler barrel`.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd &amp;&amp; npm run typecheck 2>&amp;1 | grep -E "modules/autonomous-scheduler" || echo "no autonomous-scheduler typecheck errors"</automated>
  </verify>
  <done>Barrel exports the parser + types. Typecheck reports zero new errors in `modules/autonomous-scheduler/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault/livos-agents/*.md → parser | User-edited markdown files crossing into structured JS objects that drive scheduled SDK invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01-01 | Tampering | YAML frontmatter parse | mitigate | Use FAILSAFE_SCHEMA (or strict line parser) — never executes embedded YAML directives; never evals strings |
| T-164-01-02 | Denial of Service | Cron field abuse (e.g. `* * * * *`) | accept | Sub-minute cron is allowed by node-cron. Daily budget cap (164-02) bounds spend; concurrent cap (164-02) bounds active runs. Sample agents 164-04 ship `enabled: false` and use 30-min + nightly schedules — runaway cron is operator-induced and bounded |
| T-164-01-03 | Elevation of Privilege | `allowed_tools` allowlist | mitigate | Parser passes the array through verbatim; the SDK enforces the tool allowlist at runtime via `allowedTools` option (164-02 passes this directly to `query()`). Parser does NOT execute tools; validation that values are known tool names is deferred to 164-02 (defense in depth at the spawn site) |
| T-164-01-04 | Information Disclosure | sourcePath leak | accept | sourcePath is an absolute path within `/home/bruce/livinity-vault/livos-agents/` — single-user system, no multi-tenant surface here |
</threat_model>

<verification>
- `npm run test:run -- modules/autonomous-scheduler/agent-definition-parser.test.ts` — 10 PASS
- `git diff -- livos/packages/livinityd/package.json` — empty (D-NO-NEW-DEPS)
- `git diff -- pnpm-lock.yaml` — empty (D-NO-NEW-DEPS)
- `npm run typecheck` — no new errors under `modules/autonomous-scheduler/`
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved for `liv/packages/core/src/sdk-agent-runner.ts` (this plan does not touch it)
- D-09 file `livos/packages/livinityd/source/modules/computer-use/luse-system-prompt.ts` byte-identical
- Phase 161-02 helper `livos/packages/livinityd/source/modules/ai/agent-prompt-builder.ts` byte-identical
- `liv/packages/core/src/agent-session.ts` byte-identical
- Phase 162 `vault-scaffolder.ts` byte-identical
</verification>

<success_criteria>
- New module `livos/packages/livinityd/source/modules/autonomous-scheduler/` exists with parser + test + barrel
- AgentDefinition + ParseResult + parseAgentDefinitionsDir API stable for 164-02 consumption
- D-NO-NEW-DEPS: zero `package.json` and `pnpm-lock.yaml` diff
- All sacred guards green (sdk-agent-runner.ts, luse-system-prompt.ts, agent-prompt-builder.ts, agent-session.ts, vault-scaffolder.ts UNCHANGED)
</success_criteria>

<output>
After completion, create `.planning/phases/164-autonomous-scheduler/164-01-SUMMARY.md`.
</output>
