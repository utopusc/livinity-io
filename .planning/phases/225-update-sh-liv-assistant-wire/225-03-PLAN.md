---
phase: 225-update-sh-liv-assistant-wire
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - update.sh
  - .planning/ROADMAP.md
  - .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md
autonomous: false
requirements: [SC-01, SC-02, SC-03, SC-04]
tags: [v42, update-sh, liv-assistant, healthcheck, pivot, deploy]
must_haves:
  truths:
    - "update.sh probe URL pivoted from `/api/health` to `/api/auth/status` (single bash variable LIV_ASSISTANT_HEALTH_URL or 2 inline literal replacements — both probe + log lines)"
    - "Top-level ROADMAP.md Phase 225 SC-02 wording amended `/api/health` → `/api/auth/status` (1 line)"
    - "Mini PC `bash /opt/livos/update.sh` succeeds end-to-end (exits 0, `LIVOS_UPDATE_COMPLETED=1` sentinel reached)"
    - "Mini PC `curl -fsS http://127.0.0.1:3020/api/auth/status` returns HTTP 200"
    - "Mini PC `systemctl is-active liv-assistant` → `active`"
    - "All 5 services (`livos`, `liv-core`, `liv-worker`, `liv-memory`, `liv-assistant`) active post-deploy"
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED — repo + Mini PC byte-identical"
    - "Idempotent re-run: 2nd `bash /opt/livos/update.sh` invocation also exits 0 with probe GREEN"
  artifacts:
    - path: "update.sh"
      provides: "Patched probe URL"
      contains: "/api/auth/status"
    - path: ".planning/ROADMAP.md"
      provides: "Amended SC-02 wording in Phase 225 section"
      contains: "/api/auth/status"
    - path: ".planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md"
      provides: "Audit transcript of the pivot deploy: pre-state, push, 2 Mini PC update.sh runs, post-state, sacred-SHA verify"
      contains: "[OK] liv-assistant health probe (HTTP 200)"
  key_links:
    - from: "update.sh probe block"
      to: "http://127.0.0.1:3020/api/auth/status"
      via: "`curl -fsS --max-time 5`"
      pattern: "/api/auth/status"
    - from: ".planning/ROADMAP.md Phase 225 section SC-02"
      to: "the curl health probe"
      via: "spec text"
      pattern: "/api/auth/status"
---

<objective>
One-line probe URL pivot — `/api/health` → `/api/auth/status` — then redeploy to Mini PC to prove the probe GREEN and complete Phase 225 SC-02 literally. Plan 02 surfaced that AionUi v2.1.4 binary serves `/api/auth/status` 200 and `/health` 200 but returns 404 on `/api/health`. Operator picked `/api/auth/status` (application-layer evidence, not just router-alive).

Pure surgical correction. NO new wiring. NO sacred-file touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/225-update-sh-liv-assistant-wire/225-02-SUMMARY.md
@.planning/phases/225-update-sh-liv-assistant-wire/225-02-DEPLOY-LOG.md
@.planning/phases/223-vendor-aionui-install/223-05-DEPLOY-LOG.md
@update.sh
</context>

<tasks>

### Task 1 — Patch update.sh + ROADMAP

<type>auto</type>

<files>
- update.sh
- .planning/ROADMAP.md
</files>

<action>
1. **Locate the probe URL in update.sh** — exactly 2 occurrences in the Phase 225 block: (a) the `curl -fsS --max-time 5 http://127.0.0.1:3020/api/health` line, (b) any echoed log line referencing `/api/health`. Replace `/api/health` with `/api/auth/status` at both sites. Keep the rest of the block (timeout, fail handler, success log) unchanged.

2. **Locate ROADMAP.md SC-02** — Phase 225 section near top of file (just after the Phases overview). The SC-02 bullet currently reads `SC-02: systemctl is-active liv-assistant returns active post-update` AND the SC-03 reads `SC-03: curl -fsS http://127.0.0.1:3020/api/health returns 200 inside update.sh smoke`. Update SC-03 wording: `/api/health` → `/api/auth/status`.

3. **Verify `bash -n update.sh`** exits 0.

4. **Commit** atomically with sacred-SHA hook PASS.
</action>

<verify>
```
bash -n update.sh && \
  grep -c "/api/auth/status" update.sh | awk '$1 >= 2 {exit 0} {exit 1}' && \
  ! grep -q "/api/health" update.sh && \
  grep -c "/api/auth/status" .planning/ROADMAP.md | awk '$1 >= 1 {exit 0} {exit 1}' && \
  git log -1 --name-only | grep -q "^update.sh$" && \
  ! git log -1 --name-only | grep -qE "^liv/packages/core/"
```
</verify>

<done>
- update.sh probe URL is `/api/auth/status` (2× minimum)
- `/api/health` no longer appears in update.sh
- ROADMAP.md SC-03 reflects new URL
- bash syntax valid
- Sacred SHA hook PASS on commit
</done>

---

### Task 2 — Push + Mini PC redeploy + idempotency proof

<type>checkpoint:human-verify</type>

<gate>blocking</gate>

<what-built>
Pushed Task 1 commit to `origin/master`. SSH'd to `bruce@10.69.31.68` and ran `bash /opt/livos/update.sh` twice — first run delivers the patched update.sh via self-rsync AND exercises the new probe URL (should be GREEN this time), second run proves idempotency. Captured everything to DEPLOY-LOG.md.

DEPLOY-LOG.md must contain:
1. Pre-state: Mini PC current `/opt/livos/update.sh` sha256 + `grep -n "/api/health\|/api/auth/status" /opt/livos/update.sh` output
2. `git push origin master` output (capturing the new SHA)
3. Run 1: full `bash /opt/livos/update.sh` output (or last 80 lines), including the `[OK] liv-assistant health probe (HTTP 200)` line + `LIVOS_UPDATE_COMPLETED=1` sentinel
4. Run 2: same — proving idempotency, install script no-op, probe GREEN
5. Post-state checks (one batched SSH):
   - `systemctl is-active livos liv-core liv-worker liv-memory liv-assistant` → all 5 `active`
   - `curl -fsS -w "HTTP %{http_code}\n" http://127.0.0.1:3020/api/auth/status` → HTTP 200
   - `git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts` → `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
   - `sha256sum /opt/livos/update.sh` matches local `sha256sum update.sh`
6. AUTO-APPROVE NOTE: the checkpoint is auto-approved per `workflow._auto_chain_active=true` chain flag (matches Phase 223-05 / 224-04 / 225-02 precedent). Operator UAT walk items (visual browser check that Liv Assistant still loads via `https://bruce.livinity.io/liv` IF that route exists yet — Phase 226 will add it, so currently N/A) are documented in SUMMARY as informational only.

DEPLOY-LOG.md ≥ 60 lines, with 5 grep tokens present: `/api/auth/status`, `HTTP 200`, `LIVOS_UPDATE_COMPLETED=1`, `sacred SHA`, `idempotent`.
</what-built>

<how-to-verify>
```
test -f .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md && \
  test $(wc -l < .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md) -ge 60 && \
  grep -c "/api/auth/status" .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md | awk '$1 >= 3 {exit 0} {exit 1}' && \
  grep -q "HTTP 200" .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md && \
  grep -q "LIVOS_UPDATE_COMPLETED=1" .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md && \
  grep -qi "sacred SHA" .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md && \
  grep -qi "idempotent" .planning/phases/225-update-sh-liv-assistant-wire/225-03-DEPLOY-LOG.md
```
</how-to-verify>

<resume-signal>
Auto-approve per `workflow._auto_chain_active=true` chain flag. Log: `⚡ Auto-approved checkpoint:human-verify per --auto chain`. Continue to SUMMARY + STATE/ROADMAP commit.
</resume-signal>

---

</tasks>

<acceptance>
- SC-01 (idempotent): Run 2 of update.sh exits 0 with no new install actions, probe stays GREEN
- SC-02 → amended in ROADMAP: `systemctl is-active liv-assistant` = active (proven by post-state batched SSH)
- SC-03 → pivoted in ROADMAP: `curl /api/auth/status` returns 200 (proven inline by update.sh probe + post-state curl)
- SC-04 (sacred SHA): byte-identical on Mini PC and repo (proven by `git hash-object` comparison)
</acceptance>

<notes>
- Phase 225 will close 3/3 plans after this. Move to ✅ SHIPPED in ROADMAP.
- v42 milestone advances to 4/12 (222 ✅, 223 ✅, 224 ✅, 225 ✅). Next active: Phase 226 (Caddy `/liv` reverse proxy + iframe headers).
- The same `feedback_update_sh_drift.md` mitigation pattern applies — capture pre-deploy state before first run.
</notes>
