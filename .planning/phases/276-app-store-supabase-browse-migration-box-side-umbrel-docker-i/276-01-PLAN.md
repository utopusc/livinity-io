---
phase: 276-app-store-supabase-browse-migration-box-side-umbrel-docker-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml
  - scripts/install/deploy-livinityd.sh
  - livos/install.sh
  - scripts/install/__tests__/test-deploy-livinityd.sh
autonomous: true
requirements: [WS2-A, WS2-B, WS2-C]

must_haves:
  truths:
    - "The legacy-compat docker-compose.yml no longer defines an `auth` service"
    - "Fresh-install scripts no longer pull or retag getumbrel/auth-server → livos/auth-server"
    - "The tor_proxy service AND the tor image pull are LEFT INTACT (tor is removed later in plan 276-05 after a box check)"
    - "docker-compose.common.yml + the networks: block + app-script are UNTOUCHED (load-bearing for ALL app launches)"
    - "The install-script test suite passes (TEST 40 now asserts only the tor retag entry; TEST 39/41 unchanged)"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml"
      provides: "Legacy-compat compose WITHOUT the auth service (tor_proxy + networks block still present until plan 05)"
      contains: "livinity_main_network"
    - path: "scripts/install/deploy-livinityd.sh"
      provides: "Fresh-install pipeline that still pulls getumbrel/tor but NOT getumbrel/auth-server"
    - path: "livos/install.sh"
      provides: "Mini PC install pipeline that still pulls getumbrel/tor but NOT getumbrel/auth-server"
  key_links:
    - from: "legacy-compat/docker-compose.yml"
      to: "no livos/auth-server:1.0.5 image reference"
      via: "auth service deleted"
      pattern: "livos/auth-server"
    - from: "scripts/install/deploy-livinityd.sh _dld_setup_docker_images() table"
      to: "no getumbrel/auth-server entry (tor entry kept)"
      via: "auth-server pull row removed"
      pattern: "getumbrel/auth-server"
---

<objective>
Remove the dead Umbrel `auth-server` image — and ONLY the auth-server, NOT tor. Delete the `auth` service
from the legacy-compat compose and remove the `getumbrel/auth-server:1.0.5 → livos/auth-server:1.0.5`
pull/retag row from BOTH install scripts (lockstep), then update the install-script test that asserted that
row. The `tor_proxy` service, the `getumbrel/tor` pull, the `_dld_setup_docker_images()` helper, its call
site, and the `networks:` block are all LEFT INTACT — tor is removed separately in plan 276-05 after an
operator box check (Remote Tor Access must be off).

This is the safe, independent quick win (adversarial verdict: auth-server is dead — LivOS auth is Caddy
`forward_auth` → livinityd `/auth/verify` in-process verifySessionFull; the auth container on 10.21.21.6:2000
is never reverse-proxied; LIVINITY_AUTH_SECRET='DEADBEEF' "not used"; its only would-be consumer is the
orphan packages/ui/app-auth dev app).

CRITICAL — DO NOT TOUCH (load-bearing for ALL app launches, proven by 2 independent agents):
- `legacy-compat/docker-compose.common.yml` (merged into EVERY app `docker compose up` via app-script:356)
- the `networks:` block in docker-compose.yml (creates `livinity_main_network` that every app joins)
- `app-script` / `app-script.ts` / `app-environment.ts` (the live launcher for ALL apps, not legacy-only)

Purpose: stop pulling/retagging + running the dead auth-server image, without disturbing tor or the live
app launcher/network. Output: auth service gone; auth-server pull gone from both scripts; tor untouched;
tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-CONTEXT.md
@.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-RESEARCH.md

<facts>
- LivOS auth = Caddy `forward_auth 127.0.0.1:8080` → livinityd `/auth/verify` (server/index.ts:1550,
  verifySessionFull). The Umbrel `auth` service (image livos/auth-server:1.0.5 on 10.21.21.6:2000, env
  UMBREL_AUTH_SECRET fed by LIVINITY_AUTH_SECRET='DEADBEEF') is never reverse-proxied → dead. CONTEXT
  Finding 2 + adversarial WS2 verdict.
- `_dld_setup_docker_images()` (deploy-livinityd.sh) and `setup_docker_images()` (install.sh) BOTH pull+
  retag a TWO-entry table: auth-server + tor. This plan removes ONLY the auth-server entry from both
  (lockstep); the tor entry, the helper function, and its call site STAY (tor removed in plan 276-05).
- livinityd runs via tsx (no build); compose/env edits are live on the next release deploy.
- Install-script changes affect FRESH installs only — they do NOT run during an in-place update.sh on an
  existing box. Existing boxes keep the stale auth-server image harmlessly.
- Do NOT rename UMBREL_AUTH_SECRET — delete the whole `auth` service.
- The `tor_proxy` service stays for now, so the compose still references `image: livos/tor:0.4.7.8` and the
  install still pulls it — boot `docker compose up` stays consistent (no missing-image error window).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the `auth` service from the legacy-compat compose (keep tor_proxy + networks)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml (current state — lines 1-49)
    - 276-RESEARCH.md §"WS2 — The actual fix (runtime)" (the auth/tor/networks layout)
  </read_first>
  <files>livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml</files>
  <action>
Delete the entire `auth:` service block — current lines 17-41 (from `  auth:` through the
`        ipv4_address: $AUTH_IP` line, inclusive). Leave the `tor_proxy:` service (lines 4-16) and the
`networks:` block (lines 42-49) UNTOUCHED — tor is removed in plan 276-05.

After the edit the file MUST read exactly:

```yaml
version: "3.7"

services:
  tor_proxy:
    container_name: tor_proxy
    image: livos/tor:0.4.7.8
    user: "1000:1000"
    restart: on-failure
    volumes:
      - ${LIVINITY_TORRC}:/etc/tor/torrc:ro
      - ${LIVINITY_DATA_DIR}/tor/data:/data
    environment:
      HOME: "/tmp"
    networks:
      default:
        ipv4_address: $TOR_PROXY_IP
networks:
  default:
    name: livinity_main_network
    ipam:
      driver: default
      config:
        - subnet: "$NETWORK_IP/16"
```

Do NOT touch app-environment.ts in this plan (its AUTH_IP/AUTH_PORT/LIVINITY_AUTH_SECRET env keys are
harmless once the service is gone; the auth + tor env are cleaned up together in plan 276-05).
  </action>
  <verify>
    <automated>grep -c "livos/auth-server" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "livos/auth-server" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml` returns `0`
    - `grep -c "UMBREL_AUTH_SECRET" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml` returns `0`
    - `grep -cE "^  auth:" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml` returns `0`
    - `grep -cE "^  tor_proxy:" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml` returns `1` (tor still present)
    - `grep -c "livinity_main_network" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml` returns `1` (network kept)
  </acceptance_criteria>
  <done>The `auth` service is gone; `tor_proxy` and the `networks:` block remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ONLY the auth-server pull/retag row from BOTH install scripts (lockstep, keep tor + helper)</name>
  <read_first>
    - scripts/install/deploy-livinityd.sh lines 786-844 (_dld_setup_docker_images — the getumbrel→livos table at ~799-802) + line 2922 (call site)
    - livos/install.sh lines 408-443 (setup_docker_images — the table at ~413-414) + line 1813 (call site)
    - 276-RESEARCH.md §"Install-script pull/retag (LOCKSTEP — edit BOTH)" + Pitfall 3
  </read_first>
  <files>scripts/install/deploy-livinityd.sh, livos/install.sh</files>
  <action>
Both scripts pull+retag a TWO-entry image table (auth-server + tor). Remove ONLY the auth-server entry from
the table in BOTH scripts. KEEP the helper function, the tor entry, and the call site (tor is removed in
plan 276-05). DO NOT delete the function or its call site.

(A) `scripts/install/deploy-livinityd.sh` `_dld_setup_docker_images()` (~787-844):
- In the image table (~799-802), DELETE only the row mapping `getumbrel/auth-server:1.0.5` → `livos/auth-server:1.0.5`.
  KEEP the row `getumbrel/tor:0.4.7.8` → `livos/tor:0.4.7.8`. (The table is a list the loop iterates over;
  remove the single auth-server line, leave the tor line.)
- KEEP the function `_dld_setup_docker_images()`, its `docker tag` loop, and the call site at line 2922.
- Update any inline comment that names auth-server (e.g. "pull+retag getumbrel/* → livos/*") so it no longer
  claims to pull auth-server; it now pulls only tor. Harmless if left, but prefer accurate.

(B) `livos/install.sh` `setup_docker_images()` (~408-443):
- In the table (~413-414), DELETE only the `getumbrel/auth-server:1.0.5` → `livos/auth-server:1.0.5` row.
  KEEP the `getumbrel/tor:0.4.7.8` → `livos/tor:0.4.7.8` row.
- KEEP the function and its call site at line 1813.
- Update the inventory comment at line 1785 if it names auth-server (e.g. "-> livos/auth-server + livos/tor")
  to just "-> livos/tor".

After the edits, neither script pulls/retags auth-server, but BOTH still pull/retag tor.
  </action>
  <verify>
    <automated>bash -n scripts/install/deploy-livinityd.sh && bash -n livos/install.sh && echo SYNTAX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install/deploy-livinityd.sh` exits 0 (no syntax error)
    - `bash -n livos/install.sh` exits 0 (no syntax error)
    - `grep -cE "getumbrel/auth-server|livos/auth-server" scripts/install/deploy-livinityd.sh livos/install.sh` total returns `0`
    - `grep -cE "getumbrel/tor" scripts/install/deploy-livinityd.sh` returns ≥`1` (tor pull KEPT in deploy script)
    - `grep -cE "getumbrel/tor" livos/install.sh` returns ≥`1` (tor pull KEPT in mini-pc script)
    - `grep -c "_dld_setup_docker_images" scripts/install/deploy-livinityd.sh` returns ≥`2` (function + call site KEPT)
    - `grep -c "setup_docker_images" livos/install.sh` returns ≥`2` (function + call site KEPT)
  </acceptance_criteria>
  <done>Neither install script pulls/retags auth-server; both still pull/retag tor; the helper + call site survive in both; both pass `bash -n`.</done>
</task>

<task type="auto">
  <name>Task 3: Update the install-script test so TEST 40 asserts only the tor retag entry (keep TEST 39/41)</name>
  <read_first>
    - scripts/install/__tests__/test-deploy-livinityd.sh lines 943-975 (TEST 39 helper-present, TEST 40 both-retag-entries, TEST 41 pipeline-calls-helper)
    - 276-RESEARCH.md §"Tests (WILL FAIL — same change, INVERT or REMOVE)"
  </read_first>
  <files>scripts/install/__tests__/test-deploy-livinityd.sh</files>
  <action>
TEST 39 asserts `_dld_setup_docker_images` is DEFINED — still true (function kept) → LEAVE UNCHANGED.
TEST 41 asserts the pipeline CALLS the helper — still true (call site kept) → LEAVE UNCHANGED.
TEST 40 (~956-964) asserts BOTH retag entries exist: `getumbrel/auth-server:1.0.5|livos/auth-server:1.0.5`
AND `getumbrel/tor:0.4.7.8|livos/tor:0.4.7.8`. The auth-server entry is now removed (Task 2), so TEST 40
will fail. UPDATE TEST 40 to assert ONLY the tor entry: remove the grep/assertion for the
`getumbrel/auth-server:1.0.5|livos/auth-server:1.0.5` entry; keep (and require) the
`getumbrel/tor:0.4.7.8|livos/tor:0.4.7.8` entry assertion. Update the TEST 40 description/comment to say it
checks the tor retag entry only.

Do NOT delete TEST 39/40/41 — only edit TEST 40's auth-server assertion out. (The full TEST 39/40/41 removal
happens in plan 276-05 when the helper itself is deleted.)
  </action>
  <verify>
    <automated>bash scripts/install/__tests__/test-deploy-livinityd.sh; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getumbrel/auth-server" scripts/install/__tests__/test-deploy-livinityd.sh` returns `0` (auth assertion removed)
    - `grep -c "getumbrel/tor" scripts/install/__tests__/test-deploy-livinityd.sh` returns ≥`1` (tor assertion kept)
    - `grep -c "_dld_setup_docker_images" scripts/install/__tests__/test-deploy-livinityd.sh` returns ≥`1` (TEST 39/41 kept)
    - `bash -n scripts/install/__tests__/test-deploy-livinityd.sh` exits 0
    - `bash scripts/install/__tests__/test-deploy-livinityd.sh` exits 0 (full suite green)
  </acceptance_criteria>
  <done>TEST 40 asserts only the tor retag entry; TEST 39/41 are unchanged; the test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Install script → Docker Hub | Fresh install pulls images from Docker Hub (untrusted registry) |
| Caddy forward_auth → livinityd /auth/verify | The REAL LivOS auth boundary (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-01 | Elevation | Removing the Umbrel `auth` service | accept | The `auth` service was never in any auth path — LivOS auth is Caddy `forward_auth` → livinityd `/auth/verify` (verifySessionFull, server/index.ts:1550), unchanged. The auth container (10.21.21.6:2000) is never reverse-proxied; its only would-be consumer is the orphan packages/ui/app-auth dev app. Removing it removes no protection (adversarial WS2 verdict). |
| T-276-02 | Tampering | Removing the auth-server pull/retag row | accept | Stops fetching an image whose only consumer (the `auth` service) is deleted in the same plan. The tor pull is intentionally kept (tor service still present until 276-05) so no missing-image boot error window is introduced. No new image-of-arbitrary-origin vector. |
| T-276-03 | Denial of Service | Disturbing the live app launcher/network | accept (avoided) | This plan deliberately does NOT touch docker-compose.common.yml, the networks: block, or app-script — all proven load-bearing for every app launch. Removing only the auth service + auth pull leaves the launcher and `livinity_main_network` intact. |

No new V5 input-validation or V6 crypto surface (deletion of a dead service). No HIGH-severity threat introduced.
</threat_model>

<verification>
- `grep -rE "livos/auth-server|getumbrel/auth-server" livos/packages/livinityd/source/modules/apps/legacy-compat/docker-compose.yml scripts/install/deploy-livinityd.sh livos/install.sh scripts/install/__tests__/test-deploy-livinityd.sh` returns 0 matches.
- `grep -rE "getumbrel/tor" scripts/install/deploy-livinityd.sh livos/install.sh` still returns matches (tor pull kept).
- `bash scripts/install/__tests__/test-deploy-livinityd.sh` exits 0.
- `bash -n` passes for all three shell scripts.
- docker-compose.common.yml + the networks: block + app-script are byte-unchanged (load-bearing — not touched).
- BOX-VERIFY (operator, post-release): on a FRESH box, `docker images | grep livos/auth-server` is absent; apps still start (network intact). Existing boxes keep the stale auth-server image harmlessly.
</verification>

<success_criteria>
- The `auth` service is removed from legacy-compat/docker-compose.yml (tor_proxy + networks kept).
- Neither install script pulls/retags auth-server; both STILL pull/retag tor; the helper + call site survive (lockstep).
- TEST 40 asserts only the tor retag entry; TEST 39/41 unchanged; the install-script test suite is green.
- docker-compose.common.yml, the networks block, and app-script are untouched (load-bearing).
- All shell scripts pass `bash -n`.
</success_criteria>

<output>
After completion, create `.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-01-SUMMARY.md`
</output>
</output>
