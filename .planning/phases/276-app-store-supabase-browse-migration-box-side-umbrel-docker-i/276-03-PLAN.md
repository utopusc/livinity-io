---
phase: 276-app-store-supabase-browse-migration-box-side-umbrel-docker-i
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/app-store/
  - livos/packages/ui/src/routes/community-app-store/
  - livos/packages/ui/src/modules/window/app-contents/app-store-routes/
  - livos/packages/ui/src/modules/app-store/discover/
  - livos/packages/ui/src/modules/app-store/community-app-store-dialog.tsx
  - livos/packages/ui/src/components/install-button-connected.tsx
  - livos/packages/ui/src/router.tsx
  - platform/web/src/app/api/admin/sync-catalog/route.ts
  - platform/web/src/app/admin/store/page.tsx
  - platform/web/src/app/admin/lib/admin-api.ts
autonomous: true
requirements: [WS1-C]

must_haves:
  truths:
    - "The dead native app-store React grid (unrouted since Phase 108) is deleted from the box UI"
    - "The dead one-way GitHub→Supabase sync-catalog importer is deleted from platform/web"
    - "The box UI still builds (vite) with no dangling import to a deleted file"
    - "The live marketplace iframe (AppStoreWindowContent) and the iframe install bridge are untouched"
  artifacts:
    - path: "livos/packages/ui/src/router.tsx"
      provides: "Router with the commented-out dead app-store lazy imports removed (no live import of deleted clusters)"
    - path: "platform/web/src/app/admin/store/page.tsx"
      provides: "Admin store page without the dead syncCatalog button"
  key_links:
    - from: "livos/packages/ui/src/modules/window/window-content.tsx"
      to: "AppStoreWindowContent (the iframe) — UNTOUCHED"
      via: "the live marketplace stays the iframe"
      pattern: "AppStoreWindowContent"
    - from: "platform/web/src/app/admin/store/page.tsx"
      to: "no import of syncCatalog"
      via: "sync-catalog importer deleted"
      pattern: "syncCatalog"
---

<objective>
Delete the DEAD native React app-store grid cluster on the box (unrouted since Phase 108 — all
`router.tsx` lazy imports are commented out at lines 35-38) and the dead one-way GitHub→Supabase
`sync-catalog` importer on platform/web (its `REPO='utopusc/livinity-apps'` source repo is deleted).

This is the RESEARCH "Tier A — SAFE TO DELETE OUTRIGHT" cluster. Per RESEARCH **Option 1 (safer first
ship)**, KEEP `available-apps.tsx` / `AvailableAppsProvider` (it resolves to `[]` via the stubbed
registry route from plan 276-02 and does not throw) and KEEP `use-debug-install-random-apps.ts` /
`cmdk` (they call `useAvailableApps()` which returns empty — harmless). Removing the provider + its 5
live mount points is an explicit follow-up, NOT this phase.

DO NOT touch the live marketplace iframe (`AppStoreWindowContent` / `app-store-content.tsx`) or the
iframe install bridge (`use-app-store-bridge.ts`).

Purpose: delete confirmed-dead UI; remove the dead admin sync importer; keep the box UI building.
Output: dead grid cluster gone; sync-catalog gone; vite build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-CONTEXT.md
@.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-RESEARCH.md

<interfaces>
<!-- DO NOT DELETE — live marketplace. -->
- livos/packages/ui/src/modules/window/window-content.tsx:214-215 → renders ONLY AppStoreWindowContent (iframe)
- livos/packages/ui/src/modules/window/app-contents/app-store-content.tsx:56-66 → the iframe of livinity.io/store
- livos/packages/ui/src/hooks/use-app-store-bridge.ts → postMessage install bridge (LIVE)

<!-- KEEP (Option 1) — resolve to empty via the stubbed registry route (plan 276-02). -->
- livos/packages/ui/src/providers/available-apps.tsx + AvailableAppsProvider (mounted router.tsx:83)
- livos/packages/ui/src/hooks/use-debug-install-random-apps.ts (called cmdk.tsx:104)
</interfaces>

<facts>
- router.tsx:35-38 = the dead app-store lazy imports, ALREADY commented out (no live route renders the grid).
- router.tsx:159-171 = the Phase-108 removal explanation comment.
- The dead grid cluster imports `install-button-connected.tsx` and `modules/app-store/discover/*` and
  `community-app-store-dialog.tsx` — these are Tier A (only the dead clusters import them).
- `layouts/app-store.tsx` + `modules/app-store/gallery-section.tsx` are part of the dead grid (use
  useAvailableApps for the dead grid) — delete ONLY if grep proves no LIVE (non-dead-cluster) importer.
- platform/web sync-catalog: route.ts (`REPO='utopusc/livinity-apps'`, deleted), called by admin/store/page.tsx
  (line 9 import, line 57 call) via admin-api.ts:494-504 (`syncCatalog`).
- The box UI (livos/packages/ui) is vite-built; a dangling import to a deleted file FAILS the build.
- This plan has ZERO file overlap with plan 276-02 (livinityd) — they run in the same wave in parallel.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead native app-store grid cluster (box UI)</name>
  <read_first>
    - livos/packages/ui/src/router.tsx lines 30-42 (commented dead imports) + 155-175 (Phase-108 removal note)
    - 276-RESEARCH.md §"Tier A — SAFE TO DELETE OUTRIGHT" (the file list) + §"WS1 file edit list" item 8
    - The directory listings of the cluster dirs (to delete recursively)
  </read_first>
  <files>
    livos/packages/ui/src/routes/app-store/,
    livos/packages/ui/src/routes/community-app-store/,
    livos/packages/ui/src/modules/window/app-contents/app-store-routes/,
    livos/packages/ui/src/modules/app-store/discover/,
    livos/packages/ui/src/modules/app-store/community-app-store-dialog.tsx,
    livos/packages/ui/src/components/install-button-connected.tsx,
    livos/packages/ui/src/router.tsx
  </files>
  <action>
Delete the Tier A dead cluster. BEFORE deleting each target, grep to confirm its only importers are
within the dead cluster (or commented-out in router.tsx). Targets:

(A) Delete recursively:
- `livos/packages/ui/src/routes/app-store/` (discover.tsx, category-page.tsx, app-page/, use-discover-query.tsx)
- `livos/packages/ui/src/routes/community-app-store/` (index.tsx, app-page/)
- `livos/packages/ui/src/modules/window/app-contents/app-store-routes/` (discover-window, category-page-window,
  app-page-window, app-store-layout-window, marketplace-app-window, shared-components)
- `livos/packages/ui/src/modules/app-store/discover/` (apps-three-column-section, apps-row-section, apps-grid-section)

(B) Delete files:
- `livos/packages/ui/src/modules/app-store/community-app-store-dialog.tsx`
- `livos/packages/ui/src/components/install-button-connected.tsx`

(C) `layouts/app-store.tsx` and `modules/app-store/gallery-section.tsx`: grep for LIVE importers first
(`rg "layouts/app-store" livos/packages/ui/src --glob '!**/routes/app-store/**' --glob '!**/community-app-store/**' --glob '!**/app-store-routes/**'`).
If the ONLY importers are the dead clusters being deleted in (A), delete these two as well. If ANY live
file (e.g. a sheet layout still routed) imports them, LEAVE them and note it in the SUMMARY (do not break
a live route).

(D) router.tsx: remove the commented-out dead lazy-import lines 35-38
(`// const AppStoreDiscover = ...` through `// const CommunityAppStore = ...`). Leave the Phase-108
removal explanation comment (155-175) — it documents intent. Confirm router.tsx has NO live (uncommented)
import or route referencing the deleted cluster.

DO NOT touch: window-content.tsx, app-store-content.tsx, use-app-store-bridge.ts, available-apps.tsx,
use-debug-install-random-apps.ts, cmdk.tsx (Option 1 — these stay; registry route returns [] from 276-02).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/ui build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Directories `livos/packages/ui/src/routes/app-store`, `.../routes/community-app-store`, `.../modules/window/app-contents/app-store-routes`, `.../modules/app-store/discover` no longer exist
    - `livos/packages/ui/src/components/install-button-connected.tsx` no longer exists
    - `livos/packages/ui/src/modules/app-store/community-app-store-dialog.tsx` no longer exists
    - `grep -c "routes/app-store/discover\|routes/community-app-store\|app-store-routes\|install-button-connected" livos/packages/ui/src/router.tsx` returns `0`
    - `cd livos && pnpm --filter @livos/ui build` exits 0 (no dangling-import error)
    - `grep -rc "AppStoreWindowContent" livos/packages/ui/src/modules/window/window-content.tsx` returns ≥1 (iframe untouched)
    - `livos/packages/ui/src/hooks/use-app-store-bridge.ts` and `.../providers/available-apps.tsx` still exist (Option 1 kept)
  </acceptance_criteria>
  <done>The dead grid cluster is deleted; router.tsx has no dangling/commented dead imports; the UI vite build is green; the iframe + install bridge + provider are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead sync-catalog importer (platform/web)</name>
  <read_first>
    - platform/web/src/app/api/admin/sync-catalog/route.ts (the dead REPO='utopusc/livinity-apps' importer)
    - platform/web/src/app/admin/store/page.tsx lines 9 (import) + 50-60 (syncCatalog call/button)
    - platform/web/src/app/admin/lib/admin-api.ts lines 494-504 (syncCatalog function + SyncCatalogResult type)
    - 276-RESEARCH.md §"WS1 file edit list" item 10
  </read_first>
  <files>
    platform/web/src/app/api/admin/sync-catalog/route.ts,
    platform/web/src/app/admin/store/page.tsx,
    platform/web/src/app/admin/lib/admin-api.ts
  </files>
  <action>
The `sync-catalog` route pulls `utopusc/livinity-apps` manifests into Supabase. That GitHub repo is
deleted (gh 404) → the route is dead. Remove it and its admin caller.

(A) DELETE the file `platform/web/src/app/api/admin/sync-catalog/route.ts` (the whole route directory if
it contains only this route).

(B) `platform/web/src/app/admin/store/page.tsx`:
- Remove the `syncCatalog` import (line 9) and the `SyncCatalogResult` type import if present.
- Remove the syncCatalog call + its button/handler (around lines 50-60, the
  `const result: SyncCatalogResult = await syncCatalog({ limit: 20 })` block and the UI element that
  triggers it). Leave the rest of the admin store page (featured/verified toggles, listing) intact.

(C) `platform/web/src/app/admin/lib/admin-api.ts`:
- Remove the `syncCatalog` function (lines 494-504) and the `SyncCatalogResult` type it returns (find
  and remove that type declaration too if it is used only by syncCatalog).

Confirm no other file imports `syncCatalog` or `SyncCatalogResult` after the edits.
  </action>
  <verify>
    <automated>cd platform/web && npx tsc --noEmit 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `platform/web/src/app/api/admin/sync-catalog/route.ts` no longer exists
    - `grep -rc "syncCatalog\|sync-catalog\|SyncCatalogResult" platform/web/src` returns `0`
    - `cd platform/web && npx tsc --noEmit` shows no new error related to syncCatalog (capture a pre-edit baseline of unrelated pre-existing errors)
    - The admin store page still renders the catalog listing (no removed import is still referenced)
  </acceptance_criteria>
  <done>The sync-catalog route + its admin caller + the api helper are deleted; no reference remains; platform/web typechecks with no syncCatalog error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → sync-catalog route → GitHub | Dead one-way importer (admin-gated) pulling from a deleted repo |
| Box UI render → live iframe (livinity.io/store) | The LIVE marketplace boundary — UNTOUCHED by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-08 | Tampering | Deleting the dead native grid UI | accept | The cluster is unrouted since Phase 108 (router.tsx imports commented out). No live code path renders it; deletion removes unreachable code only. The live marketplace (iframe) and install bridge are explicitly untouched. |
| T-276-09 | Tampering / SSRF | Deleting the sync-catalog route | accept (net WIN) | The route fetched manifests from a (now-deleted) GitHub repo into Supabase. Removing it deletes an admin-only outbound-fetch surface whose source no longer exists. No remaining feature depends on it; no new ingestion path created. |
| T-276-10 | Denial of Service | UI build breakage (dangling import) | mitigate | The vite build (acceptance criteria) is the gate — a dangling import to a deleted file FAILS the build and blocks the plan. Option 1 keeps available-apps.tsx/provider so the desktop provider does not throw at runtime (registry route returns [] from plan 276-02). |

No new input-validation/crypto surface. Pure deletion of unreachable/dead code. No HIGH introduced.
</threat_model>

<verification>
- `cd livos && pnpm --filter @livos/ui build` exits 0.
- `cd platform/web && npx tsc --noEmit` shows no syncCatalog-related error.
- `grep -rc "syncCatalog\|sync-catalog" platform/web/src` returns 0.
- The deleted directories/files are gone; the iframe + install bridge + available-apps provider remain.
- BOX-VERIFY (operator, post-release): the App Store window still opens the iframe and browse + install work (the dead grid was never visible; nothing user-facing changes).
</verification>

<success_criteria>
- The dead native app-store grid cluster (routes/app-store, routes/community-app-store, app-store-routes, modules/app-store/discover, community-app-store-dialog, install-button-connected) is deleted.
- router.tsx has no dangling or commented dead app-store imports; the box UI vite build is green.
- The sync-catalog route + admin caller + api helper are deleted; platform/web typechecks with no syncCatalog error.
- The live iframe marketplace + install bridge + AvailableAppsProvider (Option 1) are intact.
</success_criteria>

<output>
After completion, create `.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-03-SUMMARY.md`
</output>
