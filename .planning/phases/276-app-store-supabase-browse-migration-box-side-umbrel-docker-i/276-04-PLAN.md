---
phase: 276-app-store-supabase-browse-migration-box-side-umbrel-docker-i
plan: 04
type: execute
wave: 2
depends_on: ["276-02"]
files_modified:
  - livos/packages/livinityd/source/modules/test-utilities/create-test-livinityd.ts
  - livos/packages/livinityd/source/modules/apps/apps.integration.test.ts
  - livos/packages/livinityd/source/modules/apps/app-store.integration.test.ts
  - livos/packages/livinityd/source/modules/apps/app-repository.test.ts
  - livos/packages/livinityd/source/modules/apps/app-repository.integration.test.ts
autonomous: true
requirements: [WS1-B]

must_haves:
  truths:
    - "apps.integration.test.ts resolves sparkles-hello-world WITHOUT git-clone Step 3"
    - "The full apps lifecycle suite (install→state→restart→update→stop→start→uninstall→restore) passes"
    - "The tests for the deleted git-clone machinery (AppRepository, AppStore registry) are removed"
    - "run-git-server.js is NOT deleted (still used by widget.integration.test.ts)"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/test-utilities/create-test-livinityd.ts"
      provides: "Harness that no longer wires a git server as the default app store repo"
    - path: "livos/packages/livinityd/source/modules/apps/apps.integration.test.ts"
      provides: "Lifecycle suite resolving sparkles via the platform (Step 2) mock or a builtin"
  key_links:
    - from: "create-test-livinityd.ts"
      to: "no defaultAppStoreRepo = gitServer.url"
      via: "sparkles resolves via Step 2 mock (or builtin)"
      pattern: "defaultAppStoreRepo"
    - from: "apps.integration.test.ts install(sparkles-hello-world)"
      to: "fetchPlatformTemplate mock returning the local fixture path"
      via: "Step 2 resolution"
      pattern: "sparkles-hello-world"
---

<objective>
Rework the install test harness so the `apps.integration.test.ts` lifecycle suite (~40 stateful
assertions: install → state → backupIgnore → restore-auto-reinstall → restart → update → stop → start →
trackOpen → setTorEnabled → uninstall) resolves `sparkles-hello-world` WITHOUT the git-clone Step 3 that
plan 276-02 removed, and delete the test files that only exercise the now-deleted git-clone machinery.

This is RESEARCH **Option B (clean removal)** — the constraint CONTEXT missed. Plan 276-02 removed
install Step 3 + AppRepository + the AppStore git-clone, which breaks this harness
(`create-test-livinityd.ts:20-27` wires a local git server as `defaultAppStoreRepo`; the suite installs
`sparkles-hello-world`, which is NOT a builtin → resolved ONLY via Step 3).

**DO NOT delete `run-git-server.js`** — it is still used by `widget.integration.test.ts` (and was used
by the harness). Only stop using it for the default app store repo.

Depends on plan 276-02 (Step 3 removal must already be in place so the rework matches the new
resolution chain).

Purpose: keep the apps lifecycle test coverage green after git-clone removal; delete dead-machinery tests.
Output: harness reworked off git-clone; lifecycle suite green; 3 dead test files removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-RESEARCH.md

<interfaces>
<!-- The new resolution chain after plan 276-02 (no Step 3): -->
- apps.ts install(): Step 1 generateAppTemplate(appId) [builtin] → Step 2 fetchPlatformTemplate(appId)
  [GET /api/apps/<id>, returns a template DIR path] → else throw 'not found'.
- The harness must make sparkles resolve via ONE of these. Recommended: mock Step 2.

<!-- The sparkles fixture (local, already on disk — do NOT need git to serve it): -->
- livos/packages/livinityd/source/modules/test-utilities/fixtures/community-repo/sparkles-hello-world/
  { livinity-app.yml (id: sparkles-hello-world, name: Hello World, icon: https://svgur.com/i/mvA.svg,
    port: 4000, dependencies: [], path: '', backupIgnore: [data, logs, cache]), docker-compose.yml }

<!-- run-git-server consumers (KEEP the utility): -->
- app-repository.integration.test.ts (DELETE this test), app-store.integration.test.ts (DELETE),
  apps.integration.test.ts (rework off git), widget.integration.test.ts (KEEP — leave untouched),
  create-test-livinityd.ts (stop using it as default repo).
</interfaces>

<facts>
- `fetchPlatformTemplate(appId)` (apps.ts:1050-1112) returns a template DIRECTORY path (where
  readManifestInDirectory(appTemplatePath) then reads livinity-app.yml + docker-compose.yml), or null.
- The sparkles fixture dir already contains a valid livinity-app.yml + docker-compose.yml — point Step 2
  at that dir to preserve every existing assertion (same id, icon, port, manifest, backupIgnore).
- The 40 assertions reference `sparkles-hello-world` by id and the exact manifest fields above. Keep the
  appId and fixture so the assertions stay valid with minimal churn.
- livinityd runs via tsx; tests run via vitest.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the harness + apps.integration.test.ts to resolve sparkles without git-clone</name>
  <read_first>
    - livos/packages/livinityd/source/modules/test-utilities/create-test-livinityd.ts (full file, lines 1-93)
    - livos/packages/livinityd/source/modules/apps/apps.integration.test.ts (full file — the ~40 sequential assertions, lines 1-304)
    - livos/packages/livinityd/source/modules/apps/apps.ts lines 1050-1112 (fetchPlatformTemplate — what Step 2 returns)
    - livos/packages/livinityd/source/modules/test-utilities/fixtures/community-repo/sparkles-hello-world/{livinity-app.yml,docker-compose.yml}
    - 276-RESEARCH.md §"Install Step 3 & the test harness" (Option B)
  </read_first>
  <files>
    livos/packages/livinityd/source/modules/test-utilities/create-test-livinityd.ts,
    livos/packages/livinityd/source/modules/apps/apps.integration.test.ts
  </files>
  <action>
Make `sparkles-hello-world` resolve via Step 2 (platform mock) instead of git-clone, keeping every
existing assertion valid.

(A) `create-test-livinityd.ts`:
- Remove the `const gitServer = await runGitServer()` line (20) and the `import runGitServer` (line 9)
  IF the harness no longer needs it. Remove `defaultAppStoreRepo: gitServer.url` (line 27) — pass
  `defaultAppStoreRepo: ''` (the AppStore shell from 276-02 ignores it) or omit it (index.ts defaults to '').
- Remove `await gitServer.close()` from `cleanup()` (line 77).
- (The harness no longer wires git for the default repo; widget.integration.test.ts keeps its OWN
  runGitServer call — untouched.)

(B) `apps.integration.test.ts` — make Step 2 resolve sparkles from the local fixture:
- The suite already does `[livinityd, communityAppStoreGitServer] = await Promise.all([createTestLivinityd(), runGitServer()])`.
  Since git-clone resolution is gone, the `communityAppStoreGitServer` is no longer the resolution source.
  Replace the resolution mechanism with a spy/mock on `fetchPlatformTemplate` so installing
  `sparkles-hello-world` returns the local fixture directory path. Concretely, in `beforeAll` (after the
  instance is created), install a vitest spy:
  ```ts
  const SPARKLES_FIXTURE_DIR = path.join(__dirname, '../test-utilities/fixtures/community-repo/sparkles-hello-world')
  vi.spyOn(livinityd.instance.apps as any, 'fetchPlatformTemplate').mockImplementation(async (appId: string) =>
    appId === 'sparkles-hello-world' ? SPARKLES_FIXTURE_DIR : null,
  )
  ```
  (Adjust the spy target/signature to match the actual `fetchPlatformTemplate` location on the Apps
  instance — read apps.ts:1050 to confirm it is a method on `apps`. If it is private, spy via `as any`.)
  NOTE: `readManifestInDirectory` reads livinity-app.yml + docker-compose.yml from that dir, so the
  fixture must remain on disk (it does). If the test mutates the manifest (backupIgnore glob tests at
  lines 143-191 write to `dataDirectory/app-data/sparkles-hello-world/livinity-app.yml`, NOT the
  fixture), those still work because install copies the template into app-data first.
- Remove the now-unused `communityAppStoreGitServer` wiring (the `runGitServer()` in Promise.all line 16
  + the `communityAppStoreGitServer.close()` in afterAll line 20) IF nothing else in the file uses it
  after the spy is in place. If a test still needs a git server, keep it; otherwise remove the import.
- Keep ALL 40 sequential assertions and their `sparkles-hello-world` id / manifest expectations
  (icon `https://svgur.com/i/mvA.svg`, port 4000, etc.) unchanged.
- The `install unknown-app-id → /not found/` (line 68-69) and `restart/update unknown → /not found/`
  assertions stay green (plan 276-02 preserved the 'not found' throw).

(C) If mocking Step 2 proves harder than expected (e.g. fetchPlatformTemplate is not cleanly spy-able),
the FALLBACK per RESEARCH is to convert `sparkles-hello-world` into a builtin (add an entry to
builtin-apps.ts + compose-generator) so Step 1 resolves it — but the Step-2 mock is the smaller,
preferred diff. Document whichever path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/livinityd exec vitest run src/modules/apps/apps.integration.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "defaultAppStoreRepo: gitServer.url" livos/packages/livinityd/source/modules/test-utilities/create-test-livinityd.ts` returns `0`
    - `grep -c "sparkles-hello-world" livos/packages/livinityd/source/modules/apps/apps.integration.test.ts` returns ≥1 (assertions kept)
    - `cd livos && pnpm --filter @livos/livinityd exec vitest run src/modules/apps/apps.integration.test.ts` exits 0 (the full lifecycle suite passes)
    - `livos/packages/livinityd/source/modules/test-utilities/run-git-server.js` still exists (NOT deleted)
  </acceptance_criteria>
  <done>The harness no longer wires git-clone as the default repo; the apps lifecycle suite resolves sparkles via the Step 2 mock (or a builtin) and passes; run-git-server.js is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the test files that only exercise the deleted git-clone machinery</name>
  <read_first>
    - livos/packages/livinityd/source/modules/apps/app-store.integration.test.ts (tests the deleted AppStore git-clone)
    - livos/packages/livinityd/source/modules/apps/app-repository.test.ts (LIVOS-024 SSRF gate on the deleted AppRepository)
    - livos/packages/livinityd/source/modules/apps/app-repository.integration.test.ts (deleted AppRepository)
    - 276-RESEARCH.md §"WS1 file edit list" item 11 + §"Security Domain" (the SSRF gate test is deleted WITH the feature — expected)
  </read_first>
  <files>
    livos/packages/livinityd/source/modules/apps/app-store.integration.test.ts,
    livos/packages/livinityd/source/modules/apps/app-repository.test.ts,
    livos/packages/livinityd/source/modules/apps/app-repository.integration.test.ts
  </files>
  <action>
These three files test ONLY the git-clone machinery deleted in plan 276-02 (AppRepository class + its
SSRF URL gate, AppStore registry git-clone). With the feature gone they reference deleted symbols and
will not compile. DELETE all three:
- `livos/packages/livinityd/source/modules/apps/app-store.integration.test.ts`
- `livos/packages/livinityd/source/modules/apps/app-repository.test.ts`
- `livos/packages/livinityd/source/modules/apps/app-repository.integration.test.ts`

After deletion, grep the remaining test tree for any import of `app-repository.js` or
`getAppTemplateFilePath`/`atomicClone`/`readRegistry` and fix any straggler (there should be none —
`run-git-server.js` itself is kept and is unaffected).

NOTE (RESEARCH Security Domain): deleting `app-repository.test.ts` removes the LIVOS-024 SSRF gate test —
this is EXPECTED because the SSRF-prone git-clone feature it guarded is itself removed. No live SSRF
surface remains (the install path is now Step 1 builtin + Step 2 X-Api-Key-gated Supabase).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/livinityd exec vitest run src/modules/apps 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The three test files no longer exist
    - `grep -rn "from './app-repository" livos/packages/livinityd/source --include=*.ts` returns 0 lines
    - `grep -rn "getAppTemplateFilePath\|atomicClone\|readRegistry" livos/packages/livinityd/source --include=*.test.ts` returns 0 lines
    - `cd livos && pnpm --filter @livos/livinityd exec vitest run src/modules/apps` exits 0 (no orphan-import / missing-symbol failures)
  </acceptance_criteria>
  <done>The three git-clone-machinery test files are deleted; no remaining test imports the deleted AppRepository or git-clone symbols; the apps test directory passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → resolution path | Tests drive the install resolution chain (now Step 1 + Step 2 only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-276-11 | Tampering | Deleting app-repository.test.ts (LIVOS-024 SSRF gate test) | accept | The SSRF gate guarded the git-clone AppRepository, which plan 276-02 removed. Deleting the test for a deleted feature introduces no regression — there is no remaining code path the gate protected. The live install path (Step 2) is X-Api-Key-gated and does not clone arbitrary URLs. |
| T-276-12 | Repudiation / coverage loss | Reworking the lifecycle suite | mitigate | The rework PRESERVES all 40 lifecycle assertions (install/state/restart/update/stop/start/uninstall/restore) by keeping the same `sparkles-hello-world` fixture and resolving it via a Step-2 mock — coverage of the install lifecycle is retained, only the resolution mechanism changes. The vitest run (acceptance criteria) gates it. |

This is test-only change. No production attack surface introduced. No HIGH.
</threat_model>

<verification>
- `cd livos && pnpm --filter @livos/livinityd exec vitest run src/modules/apps` exits 0.
- The 3 git-clone test files are deleted; `run-git-server.js` + `widget.integration.test.ts` are intact.
- `grep -rn "from './app-repository" livos/packages/livinityd/source --include=*.ts` returns 0.
- `grep -c "defaultAppStoreRepo: gitServer.url" .../create-test-livinityd.ts` returns 0.
</verification>

<success_criteria>
- The harness no longer wires a git server as the default app store repo.
- The apps lifecycle suite resolves sparkles via the Step-2 mock (or a builtin) and passes — all 40 assertions retained.
- The 3 git-clone-machinery test files are deleted; no remaining test imports the deleted symbols.
- run-git-server.js (used by widget.integration.test.ts) is NOT deleted.
- `vitest run src/modules/apps` is green.
</success_criteria>

<output>
After completion, create `.planning/phases/276-app-store-supabase-browse-migration-box-side-umbrel-docker-i/276-04-SUMMARY.md`
</output>
