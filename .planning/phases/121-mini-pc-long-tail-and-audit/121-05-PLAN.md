---
phase: 121-mini-pc-long-tail-and-audit
plan: 05
type: execute
wave: 4
depends_on:
  - 121-04
files_modified:
  - livos/packages/ui/src/components/**/*.tsx
  - livos/packages/ui/src/features/**/*.tsx
  - livos/packages/ui/src/shadcn-components/ui/*.tsx
autonomous: true
requirements:
  - V35-AC1
  - V35-AC2
  - V35-AC4
  - V35-AC5
user_setup: []

must_haves:
  truths:
    - "All remaining generic .tsx files under components/ (~95) migrate to canonical tokens"
    - "All remaining features/ leaves not touched by 121-01 / 121-02 (i.e. survivors after backups+factory-reset+local-setup+files migrations) audit + migrate to canonical tokens"
    - "Every shadcn primitive under shadcn-components/ui/ (29 files) is AUDITED: if @livinity/ui-kit already exports an equivalent (Button, Card, Pill, Input, Modal, Stepper, CommandBox, Toast, NavBar, ThemeToggle, PasswordInput) → DELETE the shadcn file and redirect ALL imports across the codebase to ui-kit. If no ui-kit equivalent exists → KEEP and tag for ui-kit v0.2.0 candidate list (Phase 122+) in SUMMARY"
    - "After audit, `grep -rE \"from ['\\\"]@/shadcn-components\" livos/packages/ui/src/` returns near-zero (only survivors are ui-kit-uncovered primitives)"
    - "`pnpm --filter ui build` PASS after each commit"
    - "Sacred SHA preserved every commit"
    - "Behavioral-guard regex PASS every diff"
  artifacts:
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/121-05-SUMMARY.md"
      provides: "Component migration tally + shadcn audit verdict per file (delete/keep/v0.2.0-candidate) + ui-kit v0.2.0 candidate list + redirect-import grep evidence + sacred SHA verification"
      min_lines: 70
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/SHADCN-AUDIT.md"
      provides: "Per-file audit verdict for all 29 shadcn primitives: replaced-by-ui-kit-vN, deleted-and-redirected, kept-as-ui-kit-v0.2.0-candidate"
      min_lines: 40
  key_links:
    - from: "livos/packages/ui/src/**"
      to: "@livinity/ui-kit"
      via: "import redirects from @/shadcn-components → @livinity/ui-kit"
      pattern: "from ['\"]@livinity/ui-kit['\"]"
    - from: "livos/packages/ui/src/shadcn-components/ui/"
      to: "<deleted or kept>"
      via: "audit verdict per file"
      pattern: ".*"
---

<objective>
Migrate Mini PC livinityd UI long-tail batch #5 — the generic `components/` tree (~95 tsx survivors after Phase 120 chrome migrations), any `features/` leaves not touched by Plans 121-01/02, AND a comprehensive audit of `shadcn-components/ui/` (29 primitives). Audit each shadcn primitive: if ui-kit covers it, DELETE + redirect imports; otherwise KEEP and add to ui-kit v0.2.0 candidate list.

Purpose: This plan eliminates duplication between shadcn-components and ui-kit, closes the final long-tail migration gap, and produces the ui-kit v0.2.0 roadmap. After this plan, the only primitive library used in Mini PC UI is `@livinity/ui-kit` for things ui-kit covers, plus a SHRUNKEN shadcn-components/ui/ folder containing only primitives ui-kit doesn't yet have. Per v35.0 AC#2.

Output:
- All ~95 generic components migrated to canonical tokens.
- features/ survivors audited + migrated.
- 29 shadcn primitives audited: each marked delete-and-redirect / keep-as-v0.2.0-candidate.
- `SHADCN-AUDIT.md` produced.
- `pnpm --filter ui build` PASS.
- 121-05-SUMMARY.md authored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-CONTEXT.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-01-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md
@livos/packages/ui-kit/src/index.ts
@livos/packages/design-tokens/tailwind.preset.cjs

<interfaces>
<!-- ui-kit v0.1.0 exports (the audit reference): -->
<!-- Atoms: Button, Card, Pill, Input, PasswordInput -->
<!-- Composites: Stepper, CommandBox, Modal, Toast (ToastProvider, useToast), NavBar, ThemeToggle -->
<!-- Utilities: cn, LIV_THEME_STORAGE_KEY, LIV_THEMES, applyLivTheme, readLivTheme -->

<!-- shadcn primitives that ARE covered by ui-kit (audit target — DELETE + redirect): -->
<!-- - shadcn-components/ui/button.tsx → @livinity/ui-kit Button -->
<!-- - shadcn-components/ui/input.tsx → @livinity/ui-kit Input -->
<!-- - shadcn-components/ui/dialog.tsx → @livinity/ui-kit Modal (verify subcomponent parity first) -->
<!-- - shadcn-components/ui/badge.tsx → @livinity/ui-kit Pill -->
<!-- - shadcn-components/ui/label.tsx → @livinity/ui-kit Input has built-in label prop -->

<!-- shadcn primitives ui-kit DOES NOT cover (audit target — KEEP + v0.2.0 candidate): -->
<!-- - alert-dialog.tsx (destructive-action confirm) -->
<!-- - alert.tsx (inline alert message) -->
<!-- - carousel.tsx (image/content carousel) -->
<!-- - checkbox.tsx (form checkbox) -->
<!-- - command.tsx (cmdk command-palette primitive) -->
<!-- - context-menu.tsx (right-click menu) -->
<!-- - drawer.tsx (mobile drawer) -->
<!-- - dropdown-menu.tsx (menu primitive) -->
<!-- - form.tsx (react-hook-form integration) -->
<!-- - pagination.tsx (pagination controls) -->
<!-- - popover.tsx (floating popover) -->
<!-- - progress.tsx (progress bar) -->
<!-- - radio-group.tsx (form radio) -->
<!-- - resizable.tsx (panel split) -->
<!-- - scroll-area.tsx (custom scrollbar) -->
<!-- - select.tsx (dropdown select) -->
<!-- - separator.tsx (visual divider) -->
<!-- - sheet.tsx + sheet-scroll-area.tsx (side panel) -->
<!-- - slider.tsx (range input) -->
<!-- - switch.tsx (toggle switch) -->
<!-- - table.tsx (data table) -->
<!-- - tabs.tsx (tab bar) -->
<!-- - tooltip.tsx (hover tooltip) -->
<!-- - shared/ subfolder -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate generic components/ (~95 tsx files)</name>
  <files>livos/packages/ui/src/components/**/*.tsx</files>

  <read_first>
    `find livos/packages/ui/src/components -maxdepth 1 -name "*.tsx" | sort` plus `find livos/packages/ui/src/components -name "*.tsx" -not -path "*/ui/*" | sort` — exclude the components/ui/ subfolder (it's the shadcn family, handled in Task 3). Phase 120 touched: cmdk-providers, theme-toggle, apple-spotlight (some), and motion-primitives wrappers. Audit which are already canonical.
  </read_first>

  <action>
    1. Enumerate every .tsx in components/ (excluding components/ui/ if it's the shadcn alias path).

    2. **Audit each file:**
       - Already canonical (uses `bg-card-bg` etc.) → NOOP, document in SUMMARY.
       - Has zinc/blue/green/amber/red literal classes → migrate per the standard map.
       - Test files (*.test.tsx, *.unit.test.tsx) → leave alone unless their test fixtures contain visible classes that need updating.

    3. **ui-kit swap targets:**
       - `install-button.tsx`, `install-button-connected.tsx`, `progress-button.tsx`, `reload-page-button.tsx` → `<Button>` swap candidates (if onClick signatures match)
       - `inline-tool-pill.tsx` → `<Pill>` swap candidate
       - `update-confirm-modal.tsx`, `update-log-viewer-dialog.tsx` → `<Modal>` swap candidates
       - `theme-toggle.tsx` → already touched in Phase 120-01; verify uses ui-kit `<ThemeToggle>` or re-export pattern
       - `app-icon.tsx`, `cmdk.tsx` → Wave 1 carry-overs from Plan 120-02; finally migrate
       - `update-notification.tsx` → `<Pill>` for status + `<Button>` for action
       - `liv-tour/`, `motion/`, `motion-primitives/`, `fade-scroller/` subfolders → likely class-only migration

    4. Commit per logical chunk — 4-6 commits:
       - `feat(ui-121-05-components-install)`: install-button + progress-button + reload-page-button + update-* family
       - `feat(ui-121-05-components-app-icon)`: app-icon + cmdk + cmdk-providers (Wave 1 carry-overs)
       - `feat(ui-121-05-components-pills)`: inline-tool-pill + animated-wallpapers + theme-toggle + window-aware-link
       - `feat(ui-121-05-components-motion)`: motion/, motion-primitives/, fade-scroller/, liv-tour/
       - `feat(ui-121-05-components-misc)`: ai-quick, darken-layer, highlighted-text, iframe-checker, install-prompt-banner, markdown, apple-spotlight survivors

    5. Behavioral-guard regex + sacred SHA pre-flight per commit.
  </action>

  <acceptance_criteria>
    - All ~95 generic components migrated or audited NOOP.
    - ui-kit imports ≥ 10 across components/.
    - Build PASS each commit.
    - Sacred SHA preserved every commit.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      find livos/packages/ui/src/components -maxdepth 1 -name "*.tsx" | wc -l && \
      grep -rlE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/components/ --include="*.tsx" | grep -v "/ui/" | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Generic components migrated across 4-6 commits. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit + migrate features/ survivors not covered by 121-01/02</name>
  <files>livos/packages/ui/src/features/**/*.tsx</files>

  <read_first>
    Plan 121-01 covered backups + factory-reset + local-setup. Plan 121-02 covered files. Run `ls livos/packages/ui/src/features/` to discover any other feature folders that emerged or survivors.
  </read_first>

  <action>
    1. Discover any features/ subdirectory NOT covered by 121-01/02. If none, document as NOOP audit in SUMMARY and skip to Task 3.

    2. If any features/ leaves exist that were skipped (e.g., due to scope creep during 121-01/02 with TODO tags), migrate them now using the standard token + ui-kit pattern.

    3. Behavioral-guard regex + sacred SHA pre-flight if commits are produced.

    4. Commit: `feat(ui-121-05-features-survivors): migrate features/ leaves not covered by 121-01/02` (or skip if NOOP).
  </action>

  <acceptance_criteria>
    - Either: features/ survivors migrated + committed, OR audit-only NOOP documented in SUMMARY.
    - No `bg-zinc-(50|100|800|900)` literals remain in `features/` (already migrated by 121-01/02; this task closes any survivor).
  </acceptance_criteria>

  <verify>
    <automated>
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/features/ | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    features/ tree is fully canonical. Audit documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: shadcn audit — per-file verdict + delete-and-redirect cycle</name>
  <files>livos/packages/ui/src/shadcn-components/ui/*.tsx, livos/packages/ui/src/**/*.tsx (import redirects), .planning/phases/121-mini-pc-long-tail-and-audit/SHADCN-AUDIT.md</files>

  <read_first>
    Read each of the 29 shadcn primitives in `livos/packages/ui/src/shadcn-components/ui/`. For each, run `grep -rE "from ['\"]@/shadcn-components/ui/<name>['\"]" livos/packages/ui/src/` to see how many files import it.
  </read_first>

  <action>
    1. **Author SHADCN-AUDIT.md** with a per-file verdict table:

       | File | Imports count | ui-kit equivalent | Verdict | Notes |
       |---|---|---|---|---|
       | alert-dialog.tsx | N | NONE | KEEP (v0.2.0 candidate) | Destructive-confirm flow; ui-kit Modal lacks AlertDialog semantics. |
       | alert.tsx | N | NONE | KEEP (v0.2.0 candidate) | Inline alert. |
       | badge.tsx | N | Pill | DELETE + REDIRECT | Pill covers all tones. |
       | button-styles.css | N/A | N/A | KEEP | CSS file. |
       | button.tsx | N | Button | DELETE + REDIRECT | Carefully audit prop API parity first. |
       | carousel.tsx | N | NONE | KEEP (v0.2.0 candidate) | Carousel widget. |
       | checkbox.tsx | N | NONE | KEEP (v0.2.0 candidate) | Form checkbox. |
       | command.tsx | N | NONE | KEEP | cmdk wrapper, ui-kit no equivalent. |
       | context-menu.tsx | N | NONE | KEEP (v0.2.0 candidate) | Right-click menu. |
       | dialog.tsx | N | Modal | AUDIT carefully | Verify subcomponent parity. If ui-kit Modal can absorb DialogHeader/DialogTitle/DialogFooter via children, DELETE + REDIRECT. Otherwise KEEP. |
       | drawer.tsx | N | NONE | KEEP (v0.2.0 candidate) | Mobile drawer. |
       | dropdown-menu.tsx | N | NONE | KEEP (v0.2.0 candidate) | Menu primitive. |
       | form.tsx | N | NONE | KEEP | react-hook-form integration. |
       | input.tsx | N | Input | AUDIT carefully | Verify event-handler API parity. If shadcn `onValueChange` is widely used, redirect strategy must adapt callers (large patch). If only `onChange` is used, DELETE + REDIRECT. |
       | label.tsx | N | (Input has label prop) | AUDIT | If `<Label>` always pairs with a sibling input, callers can switch to ui-kit `<Input label="...">`. Likely DELETE + REDIRECT for those cases; KEEP for standalone usage. |
       | pagination.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | popover.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | progress.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | radio-group.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | resizable.tsx | N | NONE | KEEP | Window-pane splitting; specialized. |
       | scroll-area.tsx | N | NONE | KEEP | Custom scrollbar; specialized. |
       | select.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | separator.tsx | N | NONE | KEEP | Could be a 1px divider; lightweight enough to keep. |
       | sheet-scroll-area.tsx | N | NONE | KEEP |
       | sheet.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | shared/ | N | N/A | KEEP | Shared subfolder, audit contents. |
       | slider.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | switch.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | table.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | tabs.tsx | N | NONE | KEEP (v0.2.0 candidate) |
       | tooltip.tsx | N | NONE | KEEP (v0.2.0 candidate) |

       Fill in real import-counts via grep. Decide final DELETE+REDIRECT vs KEEP per file.

    2. **For each DELETE+REDIRECT verdict:**
       - Run `grep -rlE "from ['\"]@/shadcn-components/ui/<name>['\"]" livos/packages/ui/src/` to list every importing file.
       - In each importing file, replace the import line:
         ```
         - import { Button } from "@/shadcn-components/ui/button"
         + import { Button } from "@livinity/ui-kit"
         ```
       - Verify prop-API compatibility per call site (especially `onValueChange` vs `onChange`, `asChild`, `forwardRef`).
       - If a single call site fails parity → that import-redirect is NOT safe → DOWNGRADE the verdict to KEEP for now (with note in SUMMARY).
       - After all import-redirects pass, `git rm` the shadcn file.

    3. **For each KEEP verdict:**
       - Token-migrate the shadcn file itself (`bg-zinc-*` → `bg-card-bg`, etc.) so visually it matches canonical tokens.

    4. **v0.2.0 candidate list** (output of audit):
       - List all KEEP-v0.2.0-candidate primitives in SHADCN-AUDIT.md
       - These are the ui-kit v0.2.0 backlog for Phase 122+ (out of v35 scope per D-119-NO-CONSUMER-CHANGES analog — but the LIST is part of v35 deliverable).

    5. **Per-DELETE commit:** `feat(ui-121-05-shadcn-delete-<name>): replace shadcn/<name> with @livinity/ui-kit equivalent`. Each DELETE is its own commit so reverts are surgical.

    6. **Per-KEEP token-migrate commit:** Group all KEEP token migrations into one commit `feat(ui-121-05-shadcn-canonical): token-migrate kept shadcn primitives`.

    7. **Final grep evidence:**
       ```bash
       grep -rlE "from ['\"]@/shadcn-components/ui/[a-z-]+['\"]" livos/packages/ui/src/ | wc -l
       ```
       After all DELETE+REDIRECT commits, this number should be SIGNIFICANTLY lower than pre-plan (target: only KEEP-listed primitive paths remain).

    8. Behavioral-guard regex per import-redirect commit. Sacred SHA pre-flight every commit.

    9. Commit SHADCN-AUDIT.md: `docs(121-05): SHADCN-AUDIT.md — 29 primitives audited, N deleted + N kept as ui-kit v0.2.0 candidates`.
  </action>

  <acceptance_criteria>
    - SHADCN-AUDIT.md ≥ 40 lines, 29 per-file verdicts present, import-count grep numbers filled in.
    - Each DELETE-verdict shadcn file is git-removed, all importing files redirected to `@livinity/ui-kit`.
    - Each KEEP-verdict shadcn file is token-migrated (canonical colors).
    - Build PASS after every commit.
    - Sacred SHA preserved every commit.
    - Final grep number (residual shadcn imports) documented + drops significantly.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/SHADCN-AUDIT.md && \
      ls livos/packages/ui/src/shadcn-components/ui/ | wc -l && \
      grep -rlE "from ['\"]@/shadcn-components/ui/[a-z-]+['\"]" livos/packages/ui/src/ | wc -l && \
      grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/ | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts && \
      git log --oneline -15
    </automated>
  </verify>

  <done>
    SHADCN-AUDIT.md committed with all 29 verdicts. DELETE-verdicts shipped as atomic commits with import-redirects. KEEP-verdicts token-migrated. ui-kit v0.2.0 candidate list captured. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 121-05-SUMMARY.md</name>
  <files>.planning/phases/121-mini-pc-long-tail-and-audit/121-05-SUMMARY.md</files>

  <action>
    Author 121-05-SUMMARY.md (≥70 lines):

    - Header (phase / plan / wave / status / date)
    - Plans-shipped table: all 121-05 commits (generic-components 4-6 + features-survivors 0-1 + shadcn-delete N + shadcn-canonical 1 + audit-doc 1)
    - Generic components tally: count migrated + count NOOP + ui-kit import additions
    - features/ survivor audit result (likely NOOP — already covered by 121-01/02)
    - shadcn audit summary: 29 total → N deleted → M kept → K v0.2.0 candidates
    - Per-file shadcn outcome list (cross-reference SHADCN-AUDIT.md)
    - ui-kit v0.2.0 candidate set (count + names) — this is the input for a future Phase 122+ ui-kit expansion
    - Behavioral-guard PASS evidence per import-redirect commit
    - Sacred SHA: preserved across all 121-05 commits
    - Operator UAT block: `bash /opt/livos/update.sh → browse Mini PC → verify every surface still works (smoke test, not exhaustive — exhaustive UAT was 121-04 checkpoints + final 121-06)`
    - Carry-overs to 121-06 (cross-surface report + Playwright + STYLE-GUIDE)

    Commit: `docs(121-05): SUMMARY — generic components + features survivors + shadcn audit`.
  </action>

  <acceptance_criteria>
    - SUMMARY ≥ 70 lines
    - All 13 sections present
    - Cross-references SHADCN-AUDIT.md
  </acceptance_criteria>

  <verify>
    <automated>
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/121-05-SUMMARY.md && \
      git log --oneline -3
    </automated>
  </verify>

  <done>
    121-05-SUMMARY.md shipped. Plan 121-05 closed.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter ui build` PASS after every commit.
- `grep -rlE "from ['\"]@/shadcn-components/ui/[a-z-]+['\"]" livos/packages/ui/src/ | wc -l` significantly lower than pre-plan; only KEEP-verdict primitives' import paths survive.
- `grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/ | wc -l` significantly higher than pre-plan.
- Sacred SHA = `f3538e1d811992b782a9bb057d1b7f0a0189f95f` every commit.
- Behavioral-guard regex PASS every diff.
- SHADCN-AUDIT.md + 121-05-SUMMARY.md both committed.
- `livos/packages/livinityd/` + `liv/` untouched.
</verification>

<success_criteria>
- ~95 generic components migrated.
- features/ survivors audited.
- All 29 shadcn primitives audited; ui-kit-covered ones DELETED + REDIRECTED; ui-kit-uncovered ones KEPT + token-migrated + tagged for v0.2.0.
- SHADCN-AUDIT.md committed with verdicts table + v0.2.0 candidate list.
- 121-05-SUMMARY.md committed.
- Sacred SHA preserved every commit.
- D-V35-SACRED-SHA, D-V35-INCREMENTAL-COMMITS, D-121-NO-FUNCTIONAL-CHANGES — all honored.
</success_criteria>

<output>
After completion, create:
- `.planning/phases/121-mini-pc-long-tail-and-audit/SHADCN-AUDIT.md` per Task 3 step 1 spec
- `.planning/phases/121-mini-pc-long-tail-and-audit/121-05-SUMMARY.md` per Task 4 spec
</output>
