---
phase: 250-terminal-professional-ui
plan: 04
type: execute
wave: 4
depends_on: [250-01, 250-02, 250-03]
files_modified:
  - livos/packages/ui/src/features/v43-terminal/TerminalToolbar.tsx
  - livos/packages/ui/src/features/v43-terminal/TerminalToolbar.test.tsx
  - livos/packages/ui/src/features/v43-terminal/TerminalCheatSheet.tsx
  - livos/packages/ui/src/features/v43-terminal/TerminalCheatSheet.test.tsx
  - livos/packages/ui/src/features/v43-terminal/TerminalEmptyState.tsx
  - livos/packages/ui/src/features/v43-terminal/terminal-font-size.ts
  - livos/packages/ui/src/features/v43-terminal/terminal-font-size.test.ts
  - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.tsx
  - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.test.tsx
  - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx
  - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.test.tsx
autonomous: true
requirements: [TERM-250-TOOLBAR, TERM-250-ZOOM, TERM-250-CHEATSHEET, TERM-250-EMPTYSTATE, TERM-250-TABDOTS, TERM-250-ZEN]
user_setup: []
must_haves:
  truths:
    - "A compact monochrome icon toolbar (New/Search/Clear/Zoom±/Fullscreen/Help) sits merged with the tab bar row"
    - "Each toolbar button has a tooltip showing its keyboard shortcut (teaches the shortcuts)"
    - "Ctrl/Cmd +/-/0 changes the font size, re-fits (cols×rows re-propagate to the PTY), and persists to localStorage"
    - "Ctrl+wheel over the terminal zooms font size in/out and persists"
    - "? or F1 opens a keyboard cheat-sheet modal grouping clipboard/find/zoom/tabs/fullscreen/reconnect bindings"
    - "A fresh session shows a dismissible empty-state hint that auto-clears on the first keystroke (onData) and never returns (localStorage 'seen' flag)"
    - "Each tab shows a colored status DOT (connecting amber / live green / exited gray / expired red) plus an activity glyph on inactive tabs with output; 120-150ms transitions; honors prefers-reduced-motion"
    - "A zen/maximize toggle expands the panel; a cols×rows overlay appears briefly on toggle"
  artifacts:
    - path: livos/packages/ui/src/features/v43-terminal/TerminalToolbar.tsx
      provides: "Compact icon toolbar (New/Search/Clear/Zoom±/Fullscreen/Help) with shortcut tooltips"
      exports: ["TerminalToolbar"]
      min_lines: 80
    - path: livos/packages/ui/src/features/v43-terminal/terminal-font-size.ts
      provides: "Font-size clamp + localStorage persistence helpers"
      exports: ["clampFontSize", "loadFontSize", "saveFontSize", "FONT_SIZE_MIN", "FONT_SIZE_MAX"]
      min_lines: 30
    - path: livos/packages/ui/src/features/v43-terminal/TerminalCheatSheet.tsx
      provides: "Keyboard cheat-sheet modal grouping all LivOS terminal bindings"
      exports: ["TerminalCheatSheet"]
      min_lines: 60
  key_links:
    - from: livos/packages/ui/src/features/v43-terminal/TerminalToolbar.tsx
      to: livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx
      via: "buttons dispatch existing+new handlers (onCreate/onSearch/onClear/onZoomIn/onZoomOut/onZoomReset/onFullscreen/onHelp)"
      pattern: "onZoomIn|onFullscreen"
    - from: livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx
      to: "term.options.fontSize + fitAddon.fit()"
      via: "zoom handler sets fontSize then re-fits so cols×rows re-propagate"
      pattern: "fitAddonRef.current.*fit\\(\\)"
    - from: livos/packages/ui/src/features/v43-terminal/TerminalTabBar.tsx
      to: "ParentTabState.status colored dot"
      via: "replace text suffix with a colored dot + activity glyph"
      pattern: "data-test-tab-dot"
---

<objective>
Make every capability visible (operator goal: "easier to use"). Surface affordances + shortcut hints; mostly React/CSS, no new deps.

This plan:
1. `TerminalToolbar.tsx` — compact monochrome icon toolbar (New / Search / Clear / Zoom± / Fullscreen / Help) merged into the tab-bar row, each with a shortcut tooltip; dispatches existing + new handlers (the "+ New" button folds in here).
2. Font-size zoom — Ctrl/Cmd +/-/0 (via the existing `attachCustomKeyEventHandler`) + Ctrl+wheel; sets `term.options.fontSize` then **`fitAddon.fit()`** so cols×rows re-propagate to the PTY; persists to localStorage.
3. `TerminalCheatSheet.tsx` — ?/F1 modal grouping clipboard/find/zoom/tabs/fullscreen/reconnect bindings.
4. `TerminalEmptyState.tsx` — first-run hint; auto-clears on first `onData`; localStorage 'seen' flag.
5. Per-tab status DOTS + activity glyph + 120-150ms transitions in `TerminalTabBar` (reuse `ParentTabState.status`); honor `prefers-reduced-motion`.
6. Zen/maximize toggle + cols×rows overlay if room.

**CRITICAL correctness rule:** after ANY fontSize change (zoom), call `fitAddonRef.current.fit()` so the existing ResizeObserver/dims path re-propagates `{type:'resize', cols, rows}` to the PTY. Otherwise the grid desyncs from the PTY (silent corruption).

Output: 1 toolbar + 1 font-size helper + 1 cheat-sheet + 1 empty-state + tab-dot upgrade + tests.

**Locked invariants (verify every commit):**
- D-V44-SACRED — blob SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`; all edits under `livos/`, `liv/` untouched.
- D-V44-MINI-PC-ONLY.
- Build ON Phase 246 — the tab bar + "+ New" + status field already exist; this is additive (do not remove rename/close/create).
- v36 NO-BOLD-REDESIGNS — keep the existing dark identity; the toolbar is monochrome icons, not a redesign of the dock or window chrome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/250-terminal-professional-ui/250-CONTEXT.md
@.planning/phases/250-terminal-professional-ui/250-RESEARCH.md

<interfaces>
<!-- Tab bar today (TerminalTabBar.tsx): renders status as a text suffix (·… / ·exited / ·expired). ParentTabState.status = 'connecting'|'live'|'exited'|'expired'. The "+ New" button is `data-test='terminal-tab-create'`. -->
<!-- Panel: per-pane terminalRef + fitAddonRef exist; attachCustomKeyEventHandler already swallows clipboard + (250-02) find combos — EXTEND the same handler for zoom. lastDimsRef/ResizeObserver already send resize. -->
```ts
// Existing helpers to mirror for localStorage (terminal-session-storage.ts pattern):
//   keys are prefixed 'livos.v44.terminal.*'
// New keys this plan adds:
//   'livos.v44.terminal.fontSize'      → number (shared across tabs)
//   'livos.v44.terminal.emptyStateSeen' → '1' once dismissed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: terminal-font-size.ts helpers + zoom wiring into the panel (re-fit on every change)</name>
  <read_first>
    - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx (FULL — attachCustomKeyEventHandler ~338-361, ResizeObserver ~376-399, terminalRef/fitAddonRef)
    - livos/packages/ui/src/features/v43-terminal/terminal-session-storage.ts (localStorage helper pattern)
    - .planning/phases/250-terminal-professional-ui/250-RESEARCH.md (feature #4 — font zoom + CRITICAL re-fit rule)
  </read_first>
  <behavior>
    - terminal-font-size.test.ts: clampFontSize(5) === FONT_SIZE_MIN (8); clampFontSize(40) === FONT_SIZE_MAX (28); clampFontSize(13) === 13.
    - saveFontSize(16) then loadFontSize() === 16 (localStorage round-trip under key 'livos.v44.terminal.fontSize').
    - loadFontSize() with nothing stored → falls back to TERMINAL_DEFAULT_FONT_SIZE (13).
    - loadFontSize() with a stored out-of-range value clamps.
  </behavior>
  <action>
Create `livos/packages/ui/src/features/v43-terminal/terminal-font-size.ts`:
- `export const FONT_SIZE_MIN = 8`, `export const FONT_SIZE_MAX = 28`, `export const FONT_SIZE_STORAGE_KEY = 'livos.v44.terminal.fontSize'`.
- `export function clampFontSize(n: number): number` → clamp to [MIN, MAX].
- `export function loadFontSize(): number` → read localStorage, parse, clamp; default `TERMINAL_DEFAULT_FONT_SIZE` from `./terminal-theme`.
- `export function saveFontSize(n: number): void` → clamp + write localStorage (guard `typeof window`).

Wire zoom into `PersistentTerminalPanel.tsx`:
1. Initialize `term.options.fontSize` from `loadFontSize()` at construction (or set right after construct). Replace the hard `fontSize: TERMINAL_DEFAULT_FONT_SIZE` with `fontSize: loadFontSize()`.
2. Add a `applyZoom(delta: number | 'reset')` helper: compute next size (`delta==='reset'` → TERMINAL_DEFAULT_FONT_SIZE; else `clampFontSize(current + delta)`), set `term.options.fontSize = next`, **then `fitAddonRef.current?.fit()`** (CRITICAL — re-propagates cols×rows), then `saveFontSize(next)`. Expose via a ref so toolbar buttons + key handler can call it.
3. **Key bindings** — extend the EXISTING `attachCustomKeyEventHandler`: `event.code === 'Equal'` or `'NumpadAdd'` with ctrl/meta → applyZoom(+1) return false; `'Minus'`/`'NumpadSubtract'` → applyZoom(-1) return false; `'Digit0'`/`'Numpad0'` → applyZoom('reset') return false. (Match clipboard/find branch style.)
4. **Ctrl+wheel** — add a `wheel` listener on the container (passive:false) that, when `event.ctrlKey`, `event.preventDefault()` + applyZoom(event.deltaY < 0 ? +1 : -1). Remove the listener in cleanup.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui exec vitest run src/features/v43-terminal/terminal-font-size.test.ts && pnpm --filter ui exec tsc --noEmit 2>&1 | head -15</automated>
  </verify>
  <acceptance_criteria>
    - terminal-font-size.ts exports `clampFontSize`, `loadFontSize`, `saveFontSize`, `FONT_SIZE_MIN`, `FONT_SIZE_MAX` (grep each).
    - Storage key `'livos.v44.terminal.fontSize'` present (grep exact).
    - Panel `applyZoom` calls `term.options.fontSize =` THEN `fitAddonRef.current?.fit()` THEN `saveFontSize` (grep all three; fit() MUST follow the fontSize set).
    - Key handler has `'Equal'`, `'Minus'`, `'Digit0'` branches (grep each).
    - Ctrl+wheel listener with `event.ctrlKey` + `preventDefault` (grep).
    - terminal-font-size.test green; tsc clean.
  </acceptance_criteria>
  <done>Zoom via keys + Ctrl+wheel sets fontSize, re-fits (cols×rows re-propagate), persists; helper test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TerminalToolbar.tsx + TerminalCheatSheet.tsx + TerminalEmptyState.tsx + tests</name>
  <read_first>
    - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.tsx (FULL — row layout to merge into; theme tokens)
    - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx (ctx-menu modal styling for the cheat-sheet)
    - .planning/phases/250-terminal-professional-ui/250-RESEARCH.md (features #8 toolbar, #11 cheat-sheet, #12 empty-state)
  </read_first>
  <behavior>
    - TerminalToolbar.test.tsx: renders 7 buttons with data-test hooks (new/search/clear/zoom-in/zoom-out/fullscreen/help); clicking each fires the matching callback; each button has a title attribute containing its shortcut text (e.g. New title includes "Ctrl", Search title includes "Ctrl+Shift+F", Help title includes "?").
    - TerminalCheatSheet.test.tsx: when open=true renders rows for Copy (Ctrl+Shift+C), Paste (Ctrl+Shift+V), Find (Ctrl+Shift+F), Zoom (Ctrl +/-/0), Fullscreen, Reconnect; Esc or close button → onClose; when open=false renders nothing.
  </behavior>
  <action>
Create three components (controlled — own no terminal state):

**TerminalToolbar.tsx** — props `{onCreate, onSearch, onClear, onZoomIn, onZoomOut, onZoomReset, onFullscreen, onHelp, isFullscreen}`. A slim monochrome icon row (text-glyph icons are fine — `＋ 🔍 ⌫ ＋A −A ⤢ ?` or lucide if already a dep; prefer inline SVG/text to avoid a new dep). Each button: `title` attribute with the action + shortcut, `data-test-tool-new` / `-search` / `-clear` / `-zoom-in` / `-zoom-out` / `-fullscreen` / `-help`. Monochrome `text-[#9ca3af] hover:text-[#e7e7e8]`.

**TerminalCheatSheet.tsx** — props `{open, onClose}`. A centered modal (dark chrome like the ctx-menu) listing binding groups: Clipboard (Ctrl+Shift+C / Ctrl+Shift+V), Find (Ctrl+Shift+F), Font zoom (Ctrl +/− / Ctrl+0 / Ctrl+wheel), Tabs (+ New, right-click rename/close), Fullscreen, Reconnect (auto + manual Retry). Header note: "These are LivOS terminal bindings, not shell shortcuts." Esc → onClose; backdrop click → onClose; `data-test-cheatsheet`.

**TerminalEmptyState.tsx** — props `{host, onDismiss}`. A dismissible overlay centered in a fresh pane: "Running as bruce on {host}" + 2-3 hints (try `ls`, Ctrl+Shift+F to search, ? for shortcuts). `data-test-empty-state` + a dismiss button. (Auto-clear-on-first-keystroke wiring lives in the panel, Task 4.)

Write `TerminalToolbar.test.tsx` + `TerminalCheatSheet.test.tsx` (jsdom; mirror TerminalTabBar.test.tsx harness).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui exec vitest run src/features/v43-terminal/TerminalToolbar.test.tsx src/features/v43-terminal/TerminalCheatSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TerminalToolbar exports `TerminalToolbar`; renders `data-test-tool-new`, `-search`, `-clear`, `-zoom-in`, `-zoom-out`, `-fullscreen`, `-help` (grep each).
    - Each toolbar button has a `title=` attribute containing a shortcut (grep `title=` + `Ctrl+Shift+F`).
    - TerminalCheatSheet exports `TerminalCheatSheet`; renders `data-test-cheatsheet`; lists `Ctrl+Shift+F` and `Ctrl+0` (grep).
    - TerminalEmptyState exports `TerminalEmptyState`; renders `data-test-empty-state` (grep).
    - Toolbar + cheat-sheet tests green.
  </acceptance_criteria>
  <done>Toolbar, cheat-sheet, empty-state components render + behave per spec; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Per-tab status DOTS + activity glyph + transitions in TerminalTabBar</name>
  <read_first>
    - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.tsx (FULL — status suffix rendering ~107-118)
    - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.test.tsx (FULL — existing cases)
    - .planning/phases/250-terminal-professional-ui/250-RESEARCH.md (feature #5 — status dot + activity glyph)
  </read_first>
  <action>
Edit `TerminalTabBar.tsx`:
1. Add `hasActivity?: boolean` to `TerminalTab` (set by the panel when an inactive tab receives output).
2. Replace the text suffix (`·…` / `·exited` / `·expired`) with a colored DOT before the label: a small `span` with `data-test-tab-dot` whose color maps status → amber #facc15 (connecting, with a subtle pulse via `motion-safe:animate-pulse`), green #4ade80 (live), gray #9ca3af (exited), red #f87171 (expired). Keep an accessible `title`/`aria-label` with the status word.
3. Activity glyph: when `tab.hasActivity` AND the tab is NOT active, render a small `•`/dot accent (`data-test-tab-activity`) next to the label (activity only — NOT exit code).
4. Add a `120ms` transition on the tab background + a subtle active-tab elevation (`transition-colors duration-150 motion-reduce:transition-none`). Honor `prefers-reduced-motion` (use `motion-safe:`/`motion-reduce:`).
5. Merge the toolbar: accept new optional toolbar props OR render the toolbar as a sibling — simplest: the PANEL renders `<TerminalToolbar>` in the same flex row as `<TerminalTabBar>` (Task 4 wires it). Here, just ensure the tab bar row layout leaves room (it already uses flex). Do NOT remove the "+ New" button from the tab bar yet unless Task 4 moves it to the toolbar — coordinate: move "+ New" dispatch into the toolbar's New button and remove the tab-bar "+ New" (keep `onCreate` prop; the toolbar calls it). If removing, update TerminalTabBar.test.tsx case 5 accordingly.

Edit `TerminalTabBar.test.tsx`:
6. Update/add: a tab with status 'live' renders `data-test-tab-dot` with the live color class; status 'expired' → red. A tab with `hasActivity:true` + not active → `data-test-tab-activity` present; active tab → absent. Keep rename/close/activate cases green (adjust the "+ New" assertion if you moved it to the toolbar).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui exec vitest run src/features/v43-terminal/TerminalTabBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TerminalTab interface has `hasActivity` (grep).
    - `data-test-tab-dot` rendered with status→color map (grep `data-test-tab-dot` + `#4ade80` + `#f87171`).
    - `data-test-tab-activity` rendered only for inactive tabs with activity (grep).
    - Transition uses `motion-safe:`/`motion-reduce:` (grep `motion-`).
    - Tab bar tests green (dot color, activity glyph, rename/close/activate preserved).
  </acceptance_criteria>
  <done>Tab dots + activity glyph + reduced-motion-aware transitions; tab bar tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Wire toolbar + cheat-sheet + empty-state + zen + tab activity into the panel; extend panel test</name>
  <read_first>
    - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.tsx (FULL — header render ~195-221, pane fragment ~524-585, onData ~371-374, applyZoom from Task 1)
    - livos/packages/ui/src/features/v43-terminal/TerminalToolbar.tsx + TerminalCheatSheet.tsx + TerminalEmptyState.tsx (Task 2)
    - livos/packages/ui/src/features/v43-terminal/TerminalTabBar.tsx (Task 3)
    - livos/packages/ui/src/features/v43-terminal/PersistentTerminalPanel.test.tsx
  </read_first>
  <action>
Edit `PersistentTerminalPanel.tsx`:

1. **Toolbar** — render `<TerminalToolbar>` in the SAME flex row as `<TerminalTabBar>` (header). Wire: onCreate→existing `onCreate`; onSearch→open the active pane's find box (250-02) — add a per-pane ref/state bridge so the parent toolbar can open find on the active pane (e.g. a `Map<tabKey, ()=>void> openFindRef` the pane registers, mirroring `registerCloseSender`); onClear→active pane's `doClear`; onZoomIn/Out/Reset→active pane's `applyZoom`; onFullscreen→toggle zen; onHelp→open cheat-sheet. Use the same registration pattern already used for `registerCloseSender` to reach the active pane's handlers.

2. **Cheat-sheet** — `const [cheatOpen, setCheatOpen] = useState(false)`; render `<TerminalCheatSheet open={cheatOpen} onClose={()=>setCheatOpen(false)}/>`. Open on `?` (Shift+/) or F1 — add to the panel-level keydown OR the active pane's `attachCustomKeyEventHandler` (`event.key === '?'` or `event.code === 'F1'` → setCheatOpen(true), return false). (Guard so `?` typed into the shell still works when find/other input is focused — only intercept when the terminal itself has focus and no overlay input is active; simplest acceptable: F1 always opens, `?` opens only when buffer is at a prompt is too complex → use F1 as the primary binding and also a toolbar Help button; bind `?` as a convenience that the cheat-sheet header documents.)

3. **Empty-state** — per pane: show `<TerminalEmptyState host={window.location.hostname} onDismiss={...}/>` when the pane is fresh (create mode, no data received yet) AND `localStorage 'livos.v44.terminal.emptyStateSeen' !== '1'`. In `term.onData` (first keystroke) AND on dismiss → setLocalStorage seen='1' + hide. Track `const [emptyVisible, setEmptyVisible] = useState(...)`.

4. **Tab activity** — when an INACTIVE pane receives `{type:'data'}`, mark its tab `hasActivity:true` in parent state; clear `hasActivity` when the tab becomes active (in `onActivate`). Add a `markActivity(tabKey)` parent callback the pane calls in its data branch when `!isActive`.

5. **Zen/maximize** — `const [zen, setZen] = useState(false)`; when zen, apply a fullscreen-ish class to the panel root (e.g. `fixed inset-0 z-50` over the desktop) and a cols×rows overlay that shows briefly on toggle (reuse the dims state from 250-03 or a short timeout). Toolbar Fullscreen button + (optional) a key binding toggle it. Honor LivOS window-logic: do NOT add a route — this is a CSS state toggle within the existing window.

Edit `PersistentTerminalPanel.test.tsx`:

6. Add mocks for the new components if they pull heavy imports (they don't — plain React). Add case `it('10) toolbar renders + Help opens cheat-sheet (250-04)')`: render panel, click `[data-test-tool-help]`, assert `[data-test-cheatsheet]` appears, click close → disappears. Add case `it('11) zoom-in toolbar button calls applyZoom path (fontSize set + fit)')`: click `[data-test-tool-zoom-in]`, assert the terminal mock's `options.fontSize` changed AND the fit mock was called (extend the FitAddon mock to a shared spy + the Terminal mock to expose an `options` object).
  Add case `it('12) inactive tab receiving data gets activity glyph')` if feasible with the existing mock harness (assert tab `data-test-tab-activity` appears after delivering data to a non-active pane).

Keep cases 1-9 green.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui exec vitest run src/features/v43-terminal/PersistentTerminalPanel.test.tsx && pnpm --filter ui exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `<TerminalToolbar` rendered in the header row with onZoomIn/onFullscreen/onHelp wired (grep `<TerminalToolbar` + `onFullscreen`).
    - `<TerminalCheatSheet` rendered with open state (grep).
    - `<TerminalEmptyState` rendered with `host=` (grep).
    - Empty-state seen flag `'livos.v44.terminal.emptyStateSeen'` written on first data/dismiss (grep exact key).
    - Zen toggle applies `fixed inset-0` (grep) and is NOT a route (no `<Navigate`/`useNavigate` added — grep returns none).
    - `markActivity` / `hasActivity` plumbed to tab state (grep `hasActivity`).
    - Panel cases 10-12 added; all green; tsc clean.
  </acceptance_criteria>
  <done>Toolbar dispatches all actions; ?/F1 + Help open cheat-sheet; empty-state shows once + auto-clears; inactive-tab activity glyph; zen toggle; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolbar/key handlers → active pane handlers | parent reaches into the active pane via registered callbacks |
| localStorage (fontSize/emptyStateSeen) → UI behaviour | persisted client prefs read on mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-04-01 | Tampering | localStorage fontSize value | mitigate | `clampFontSize` bounds any stored value to [8,28] on load — out-of-range/garbage is clamped, never applied raw |
| T-250-04-02 | Denial of service | Ctrl+wheel zoom spam | mitigate | zoom clamped to MIN/MAX; fit() is cheap and already debounced via ResizeObserver path |
| T-250-04-03 | Information disclosure | empty-state shows host | accept | host is the operator's own minipc hostname, already visible in the browser URL bar |
</threat_model>

<verification>
- `git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts` === `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- `cd livos && pnpm --filter ui exec vitest run src/features/v43-terminal/` — all terminal tests green.
- `cd livos && pnpm --filter ui exec tsc --noEmit` — clean.
- CRITICAL: every zoom path calls `fitAddonRef.current?.fit()` after setting fontSize (grep + code review) — no PTY grid desync.
- LivOS window-logic honored: zen is a CSS state, NO new route / Navigate (grep returns none).
- No `liv/` file modified.
</verification>

<success_criteria>
- Compact monochrome toolbar (New/Search/Clear/Zoom±/Fullscreen/Help) merged with the tab row; tooltips teach shortcuts.
- Font zoom (Ctrl/Cmd +/−/0 + Ctrl+wheel) re-fits + persists across reload.
- ?/F1 cheat-sheet modal; first-run empty-state auto-clears on first keystroke (once, ever).
- Per-tab status dots + activity glyph + reduced-motion-aware 120-150ms transitions.
- Zen/maximize toggle + cols×rows overlay; zero regression to 246 + 250-01/02/03.
</success_criteria>

<output>
After completion, create `.planning/phases/250-terminal-professional-ui/250-04-SUMMARY.md`.
</output>
