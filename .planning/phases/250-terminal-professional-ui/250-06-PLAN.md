---
phase: 250-terminal-professional-ui
plan: 06
type: execute
wave: 6
depends_on: [250-01, 250-02, 250-03, 250-04, 250-05]
autonomous: true
requirements: [TERM-250-DEPLOY]
files_modified:
  - .planning/phases/250-terminal-professional-ui/250-06-DEPLOY-LOG.md
  - .planning/phases/250-terminal-professional-ui/250-06-UAT-CHECKLIST.md
user_setup:
  - service: minipc-ssh
    why: "Run git push + update.sh (pnpm install resolves @xterm/addon-search) + verify services on Mini PC"
    requires: "C:/Users/hello/Desktop/Projects/contabo/pem/minipc SSH key + bruce@10.69.31.68 reachable; sudo for /opt/livos"
must_haves:
  truths:
    - "Phase 250 commits are pushed to origin/master (utopusc/livinity-io)"
    - "update.sh on the Mini PC clones the latest commit and pnpm install resolves @xterm/addon-search 0.15.x"
    - "All systemd services (caddy, livos, liv-core, liv-worker, liv-memory) restart cleanly after deploy"
    - "Caddyfile on the Mini PC is UNCHANGED for /livos/terminal/* (D-V44-CADDY-REUSE-226-04 honored)"
    - "Redis flag livos:v43:terminal_panel is still 'true' after deploy (terminal stays visible)"
    - "Sacred SHA on the Mini PC matches f3538e1d811992b782a9bb057d1b7f0a0189f95f (git blob)"
    - "Smoke probe: clear-scrollback WS message DELs livos:pty:session:<id>:scrollback (ring empty after clear)"
    - "UAT checklist lists every operator-walk step from the CONTEXT/ROADMAP UAT spec"
  artifacts:
    - path: .planning/phases/250-terminal-professional-ui/250-06-DEPLOY-LOG.md
      provides: "Deploy timeline + Caddyfile delta (expected NONE) + service status table + smoke probe outputs"
      min_lines: 70
    - path: .planning/phases/250-terminal-professional-ui/250-06-UAT-CHECKLIST.md
      provides: "Operator-walk UAT items derived from the ROADMAP Phase 250 UAT line"
      min_lines: 30
  key_links:
    - from: bruce@10.69.31.68
      to: /opt/livos
      via: "bash /opt/livos/update.sh (git pull + pnpm install + build + systemctl restart)"
      pattern: "update.sh"
---

<objective>
Ship Phase 250 to the Mini PC and hand the operator a UAT checklist. Mirrors the 246-06 pattern.

Per D-V44-MINI-PC-ONLY: ONLY `bruce@10.69.31.68`. NEVER Server4. NEVER Server5 for LivOS work.
Per D-V44-CADDY-REUSE-226-04: do NOT modify Caddy emission; the existing `/livos/terminal/*` matcher already covers the WS (and the new `clear-scrollback` message rides the same socket). Verify the Caddyfile delta is NONE post-deploy.
Per D-V44-SACRED: verify the sacred blob SHA on the Mini PC.

**Deploy path (important):** the Mini PC has unpushed hot-fixes applied manually; a CLEAN Phase 250 deploy goes via `git push` (executor host) → `bash /opt/livos/update.sh` on the Mini PC. `update.sh` runs `pnpm install`, which resolves the new `@xterm/addon-search` dependency, then builds the UI (vite) and restarts services. Do NOT hand-scp individual files.

Output:
- Mini PC running deployed bytes.
- `250-06-DEPLOY-LOG.md` (deploy timeline, Caddyfile delta = NONE, service status, smoke probes).
- `250-06-UAT-CHECKLIST.md` (operator-walk items).
- Final commit closing the phase.

**NOTE:** If SSH to the Mini PC is unreachable from the executor host (as in 246-06 — TCP completes but ECDH stalls), this plan is ARTIFACT-COMPLETE / OPERATOR-PENDING: write the deploy script + UAT checklist + push the code, and hand off the live `update.sh` + smoke probes + browser walk to the operator (Steps A/B/C pattern from 246-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/250-terminal-professional-ui/250-CONTEXT.md
@.planning/phases/246-terminal-v2-multi-session/246-06-PLAN.md
@.planning/phases/246-terminal-v2-multi-session/246-06-DEPLOY-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push Phase 250 + write the Mini PC deploy script (Step A) + run it if SSH reachable</name>
  <read_first>
    - .planning/phases/246-terminal-v2-multi-session/246-06-DEPLOY-LOG.md (the proven update.sh deploy script + SSH reachability gate)
    - C:/Users/hello/.claude/projects/.../memory/reference_minipc_ssh.md (Mini PC SSH invocation)
  </read_first>
  <action>
1. Verify all Phase 250 commits (250-01..05) are on master and pushed: `git log --oneline | grep -E "250-0[1-5]"` then `git push origin master`.

2. Verify D-V44-SACRED on the executor host: `git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts` === `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. STOP and escalate if mismatched.

3. Create `.planning/phases/250-terminal-professional-ui/250-06-DEPLOY-LOG.md` with:
   - **Operator deploy script → Step A**: the batched SSH command to run `bash /opt/livos/update.sh` on `bruce@10.69.31.68` (mirror the 246-06 script exactly), followed by a service-status batch (`systemctl is-active caddy livos liv-core liv-worker liv-memory`) and the verification batch:
     - `redis-cli -a <pw-from-.env> GET livos:v43:terminal_panel` → expect `true`
     - Caddyfile delta check: grep that the live `/etc/caddy/Caddyfile` still has the `/livos/terminal` matcher (expect present, unchanged)
     - sacred SHA on Mini PC build tree: `git -C /tmp/livinity-update-* rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts` === `f3538e1d...`
     - `pnpm --filter ui list @xterm/addon-search` on the build tree resolves 0.15.x
   - A "## Deploy timeline" section (operator pastes transcript), a Caddyfile delta line (expected: NONE), a service status table, a probe outcomes table.

4. **Attempt the live deploy** from the executor host (per memory's Mini PC SSH invocation + batch into ONE session to avoid the fail2ban rate limit). If SSH connects → run Step A + paste the transcript into the deploy log. If SSH is unreachable (ECDH stall as in 246-06) → mark the deploy log ARTIFACT-COMPLETE / OPERATOR-PENDING and leave Step A for the operator.
  </action>
  <verify>
    <automated>git -C C:/Users/hello/Desktop/Projects/contabo/livinity-io log --oneline -20 | grep -E "250-0[1-5]" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Phase 250 commits present on master + pushed (git log shows 250-01..05 commits; `git status` shows origin up to date).
    - Sacred SHA verified on executor host (= f3538e1d...).
    - 250-06-DEPLOY-LOG.md exists with Step A script, Caddyfile-delta line, service-status table, probe table (grep `update.sh`, `livos:v43:terminal_panel`, `addon-search`).
    - Deploy log status is either ✅ SHIPPED (if SSH reachable + run) or ARTIFACT-COMPLETE / OPERATOR-PENDING.
  </acceptance_criteria>
  <done>Code pushed; deploy log + Step A script written; deploy run if reachable, else handed to operator.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke probes (Step B) + UAT checklist (Step C) + close the phase</name>
  <read_first>
    - .planning/phases/250-terminal-professional-ui/250-CONTEXT.md (UX direction + locked decisions)
    - .planning/ROADMAP.md Phase 250 entry (the "UAT:" line — the operator-walk script)
    - .planning/phases/246-terminal-v2-multi-session/246-06-UAT-CHECKLIST.md (format to mirror)
  </read_first>
  <action>
1. In `250-06-DEPLOY-LOG.md` add **Operator deploy script → Step B** smoke probes (wire-level, operator runs with a fresh proxy token):
   - WS create → init → `{type:'ready'}` (terminal still works post-deploy).
   - Type a command, then `redis-cli LRANGE livos:pty:session:<id>:scrollback 0 -1` returns non-empty.
   - Send `{type:'clear-scrollback'}` on the WS → `redis-cli LLEN livos:pty:session:<id>:scrollback` returns 0 (NEW 250-05 behaviour — ring wiped).
   - Reattach `?attach=<id>` → `{type:'reattached'}` with EMPTY scrollback after the clear (proves no reload-resurrect).
   - `@xterm/addon-search` resolved on the build tree (250-02 dep landed).

2. Create `.planning/phases/250-terminal-professional-ui/250-06-UAT-CHECKLIST.md` — operator browser-walk items derived from the ROADMAP "UAT:" line + the 5 success criteria. At minimum:
   - [ ] Open Terminal → polished 16-color theme + toolbar + bottom status bar render; brand identity (`#0b0b0c`/`#e7e7e8`/`#7dd3fc`) intact.
   - [ ] Ctrl+Shift+F → find box; type a query → all matches highlight + "N of M" count + overview-ruler markers paint (under WebGL).
   - [ ] Pull network (devtools offline / kill relay briefly) → pane dims + "Reconnecting… (n)" → restore network → reattaches with full scrollback + "Reconnected" flash.
   - [ ] Let a session sit > 24h OR kill it via admin panel, then reattach → "session expired" (4404, no infinite retry).
   - [ ] Ctrl+= / Ctrl+- / Ctrl+0 + Ctrl+wheel zoom; reload → font size persists; grid not clipped (re-fit worked).
   - [ ] Gear → settings drawer → switch to Nord/Dracula live; change font/cursor/scrollback live; reload → persists.
   - [ ] Right-click Clear (or toolbar Clear) → output clears; reload → cleared output does NOT resurrect (Redis ring wiped).
   - [ ] ? / F1 → cheat-sheet modal; (optional) Ctrl+Shift+P → command palette.
   - [ ] Per-tab status dots reflect connecting/live/exited/expired; inactive tab with output shows activity glyph.
   - [ ] Right-click copy/paste + Ctrl+Shift+C/V still work (zero regression to 246).
   - [ ] Zen/maximize toggle expands the panel + cols×rows overlay.

3. **Final commit** closing the phase:
   `docs(250-06): Phase 250 deploy log + UAT checklist (status: <SHIPPED | ARTIFACT-COMPLETE/OPERATOR-PENDING>)`
   plus STATE.md + ROADMAP Phase 250 flip (mark plans complete; status per deploy outcome).
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && node -e "const fs=require('fs');const f='.planning/phases/250-terminal-professional-ui/250-06-UAT-CHECKLIST.md';console.log(fs.existsSync(f)?'UAT-EXISTS lines='+fs.readFileSync(f,'utf8').split(/\r?\n/).length:'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - 250-06-DEPLOY-LOG.md has a Step B smoke-probe section including the NEW clear-scrollback probe (`clear-scrollback` → `LLEN ... 0`) and the reattach-after-clear probe (grep `clear-scrollback` + `LLEN` + `?attach`).
    - 250-06-UAT-CHECKLIST.md exists with >= 10 operator-walk checkbox items covering find/reconnect/expiry/zoom/drawer/clear-no-resurrect/cheat-sheet/tab-dots/copy-paste/zen (grep `[ ]` count >= 10).
    - Final docs commit closing the phase; ROADMAP Phase 250 plan list flipped + STATE.md updated.
  </acceptance_criteria>
  <done>Smoke probes (incl. clear-scrollback ring-wipe) documented; UAT checklist written; phase closed (SHIPPED or ARTIFACT-COMPLETE/OPERATOR-PENDING per SSH reachability).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor host → Mini PC over SSH | deploy commands run as bruce with sudo on /opt/livos |
| update.sh → npm registry (pnpm install) | new dependency resolved at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-06-01 | Tampering | new dep resolved on deploy | mitigate | committed pnpm lockfile pins `@xterm/addon-search` 0.15.x; `update.sh` `pnpm install` honors the lockfile (no floating upgrade) |
| T-250-06-02 | Elevation | deploy uses sudo on Mini PC | accept | operator-run, single-host (D-V44-MINI-PC-ONLY); no Server4/Server5; standard 246-06 deploy path |
| T-250-06-03 | Repudiation | deploy without audit trail | mitigate | deploy log captures timeline + service status + sacred SHA + Caddyfile delta; final commit records the verified state |
</threat_model>

<verification>
- `git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts` === `f3538e1d811992b782a9bb057d1b7f0a0189f95f` on executor AND Mini PC build tree.
- Phase 250 commits pushed to origin/master.
- Post-deploy: all 5 systemd services active; Redis flag `livos:v43:terminal_panel` === `true`.
- Caddyfile delta = NONE (D-V44-CADDY-REUSE-226-04); `caddy.ts` byte-identical.
- `@xterm/addon-search` resolves 0.15.x on the build tree.
- clear-scrollback smoke probe wipes the ring (LLEN 0) and reattach replays EMPTY.
- ONLY Mini PC touched; NO Server4/Server5.
</verification>

<success_criteria>
- Phase 250 pushed + deployed to the Mini PC via `git push` + `update.sh` (or ARTIFACT-COMPLETE / OPERATOR-PENDING with a complete Step A/B/C hand-off if SSH unreachable).
- Deploy log: timeline + Caddyfile delta (NONE) + service status + smoke probes (incl. clear-scrollback ring-wipe).
- UAT checklist covering all 5 success criteria + the ROADMAP UAT walk.
- Sacred SHA preserved on the Mini PC; phase closed.
</success_criteria>

<output>
After completion, create `.planning/phases/250-terminal-professional-ui/250-06-SUMMARY.md`.
</output>
