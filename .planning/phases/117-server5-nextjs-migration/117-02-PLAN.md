---
phase: 117-server5-nextjs-migration
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - .planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md
autonomous: true
requirements: []
tags: [v35, design-system, server5, auth, nextjs, restyle]
must_haves:
  truths:
    - "All 6 (auth)/* pages render with canonical Geist font + token-grounded surfaces"
    - "Each modified file has a `.pre-117-02.bak` backup on Server5"
    - "Login/register/verify/forgot-password/reset-password/device routes return 2xx/3xx via curl"
    - "Form submit handlers, fetch URLs, and session-cookie behavior are byte-identical to backups (D-117-NO-AUTH-FLOW-CHANGES)"
    - "Zinc palette references are replaced by canonical tokens (`text-zinc-*` → `text-[color:var(--accent-*)]` / `text-card-bg-2`-type token classes)"
    - "pm2 `web` online after the restart at the end of this plan"
  artifacts:
    - path: "/opt/platform/web/src/app/(auth)/layout.tsx"
      provides: "Canonical auth-shell wrapper (token surfaces)"
    - path: "/opt/platform/web/src/app/(auth)/login/page.tsx"
      provides: "Restyled login form, identical submit handler"
    - path: "/opt/platform/web/src/app/(auth)/register/page.tsx"
      provides: "Restyled register form"
    - path: "/opt/platform/web/src/app/(auth)/verify/page.tsx"
      provides: "Restyled verify status page"
    - path: "/opt/platform/web/src/app/(auth)/forgot-password/page.tsx"
      provides: "Restyled forgot-password form"
    - path: "/opt/platform/web/src/app/(auth)/reset-password/page.tsx"
      provides: "Restyled reset-password form (preserves Suspense)"
    - path: "/opt/platform/web/src/app/(auth)/device/page.tsx"
      provides: "Restyled device-pairing code-entry page"
    - path: ".planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md"
      provides: "Plan completion record"
  key_links:
    - from: "/opt/platform/web/src/app/(auth)/login/page.tsx"
      to: "/api/auth/login"
      via: "fetch POST in onSubmit"
      pattern: "fetch\\(.*api/auth/login"
    - from: "/opt/platform/web/src/app/(auth)/layout.tsx"
      to: "@livinity/design-tokens"
      via: "Tailwind classes resolved through preset (loaded in root layout.tsx)"
      pattern: "bg-card-bg|font-sans|text-accent-blue"
---

<objective>
Restyle the 6 Server5 `(auth)/*` routes (login, register, verify, forgot-password, reset-password, device) plus the shared `(auth)/layout.tsx` shell to consume the canonical design tokens installed in Phase 117-01.

**Scope discipline (D-117-NO-AUTH-FLOW-CHANGES):** Form handlers, `fetch()` calls, useState shapes, useSearchParams reads, redirect calls, and Suspense boundaries MUST remain byte-identical to the `.pre-117-02.bak` baseline. ONLY className strings, JSX wrapper structure (card chrome), and minor copy/heading typography may change.

**Scope discipline (D-117-NO-API-CHANGES):** Zero edits to `/opt/platform/web/src/app/api/auth/**`.

Purpose: Make `/login`, `/register`, `/verify`, `/forgot-password`, `/reset-password`, `/device` visually match dashboard.html (Geist, accent-blue primary, card-bg surfaces, dash radius/padding/shadow).
Output: 7 files restyled on Server5 (6 pages + 1 layout), backups in place, pm2 restarted, 117-02-SUMMARY.md committed in-repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/117-server5-nextjs-migration/117-CONTEXT.md
@.planning/phases/117-server5-nextjs-migration/117-01-PLAN.md
@.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/design-tokens/tokens.css
@livos/packages/design-tokens/tailwind.preset.cjs
@./CLAUDE.md

<interfaces>
**Canonical class map for (auth)/* restyle (D-117-NO-AUTH-FLOW-CHANGES — do not touch logic):**

```
# Auth shell wrapper (layout.tsx)
bg-zinc-50 dark:bg-zinc-950   → bg-card-bg-2 dark:bg-card-bg
min-h-screen flex items-center justify-center p-6   → unchanged (purely structural)

# Card chrome
rounded-2xl                   → rounded-dash
shadow-sm / shadow            → shadow-card
border border-zinc-200        → border border-dash-line
bg-white dark:bg-zinc-900     → bg-card-bg

# Form inputs
border-zinc-300               → border-dash-line-strong
focus:ring-blue-500           → focus:ring-accent-blue / focus-visible:ring-[color:var(--accent-blue)]
rounded-md                    → rounded-md (keep — inputs are not "dash" radius targets)

# Buttons
bg-blue-600 hover:bg-blue-700 → bg-accent-blue hover:bg-[color:rgb(37_99_235/0.9)]
text-white                    → text-white  (unchanged — accent-blue is dark enough)

# Error / success states
text-red-600                  → text-accent-red
text-green-600                → text-accent-green
bg-red-50                     → bg-[color:rgb(220_38_38/0.08)]
bg-green-50                   → bg-[color:rgb(22_163_74/0.08)]

# Heading
text-2xl font-bold            → text-2xl font-serif font-semibold tracking-tight  (Instrument Serif for h1 in auth cards — matches dashboard.html hero typography)
```

**Component shape after restyle (canonical auth card):**

```tsx
<div className="min-h-screen flex items-center justify-center bg-card-bg-2 dark:bg-card-bg p-6">
  <div className="w-full max-w-md rounded-dash border border-dash-line bg-card-bg shadow-card p-dash">
    <h1 className="text-2xl font-serif font-semibold tracking-tight mb-1">Livinity</h1>
    <p className="text-sm text-[color:var(--muted-fg)] mb-6">Sign in to your account</p>
    {/* form, fields, submit — handler logic UNCHANGED from .bak */}
  </div>
</div>
```
</interfaces>

<server5_ssh>
Same SSH base command as 117-01. Batch reads + writes into single sessions.
</server5_ssh>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline + restyle (auth)/layout.tsx + login + register</name>
  <files>
    <!-- Server5 cross-repo:
         /opt/platform/web/src/app/(auth)/layout.tsx
         /opt/platform/web/src/app/(auth)/login/page.tsx
         /opt/platform/web/src/app/(auth)/register/page.tsx
         Each with .pre-117-02.bak sibling. -->
  </files>
  <read_first>
    1. `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md` — `app/(auth)/` section (lines ~36-48). Confirms each file's current visual idiom (`Tailwind zinc, raw form`) and reaffirms `D-117-NO-AUTH-FLOW-CHANGES`.
    2. `.planning/phases/117-server5-nextjs-migration/117-01-PLAN.md` <interfaces> block — the canonical class table is the contract for this restyle.
    3. Live source from Server5 (SINGLE SSH call):
       ```
       ssh root@45.137.194.102 "set -e; \
         echo '===LAYOUT==='; cat /opt/platform/web/src/app/'(auth)'/layout.tsx; \
         echo '===LOGIN==='; cat /opt/platform/web/src/app/'(auth)'/login/page.tsx; \
         echo '===REGISTER==='; cat /opt/platform/web/src/app/'(auth)'/register/page.tsx"
       ```
       (Note: parentheses in `(auth)` route group MUST be quoted in shell.)
  </read_first>
  <action>
    **Pre-flight: validate 117-01 is in place.** If preset/tokens not present, ABORT — Wave dependency violated.
    ```
    ssh root@45.137.194.102 "test -f /opt/platform/web/node_modules/@livinity/design-tokens/tokens.css && echo TOKENS_PRESENT || (echo MISSING_117_01 && exit 1)"
    ```

    For EACH of the 3 files (layout, login, register):

    1. **Backup:**
       ```
       ssh root@45.137.194.102 "cp '/opt/platform/web/src/app/(auth)/<file>' '/opt/platform/web/src/app/(auth)/<file>.pre-117-02.bak'"
       ```

    2. **Restyle locally then ship via scp.** Method:
       - Pull the .bak content to local /tmp via `ssh ... cat`.
       - Apply the class-map transformations from <interfaces> block (Tailwind classNames only). DO NOT touch:
         - Import statements (except adding nothing — keep existing).
         - `useState` / `useEffect` / `useRouter` / `useSearchParams` / `useTransition` calls.
         - `onSubmit` handler bodies (including any `fetch(...)` URLs / methods / headers / body shapes).
         - JSX structural changes beyond wrapping in canonical card shape from <interfaces>.
         - Any non-className-string text content unless it's a heading typography polish (e.g. wrap `Livinity` in font-serif).
       - Write the new file to `/tmp/117-02-<short>.tsx`, scp it to its Server5 path.

    3. **Class-map transformations (apply via mental diff or a scripted regex pass — be conservative):**
       ```
       sed -E -i '
         s/bg-zinc-50/bg-card-bg-2/g; \
         s/dark:bg-zinc-950/dark:bg-card-bg/g; \
         s/bg-zinc-900/bg-card-bg/g; \
         s/bg-white(?![-:])/bg-card-bg/g; \
         s/border-zinc-200/border-dash-line/g; \
         s/border-zinc-300/border-dash-line-strong/g; \
         s/text-zinc-500/text-[color:var(--muted-fg)]/g; \
         s/text-zinc-600/text-[color:var(--muted-fg)]/g; \
         s/text-zinc-700/text-[color:var(--fg)]/g; \
         s/text-zinc-900/text-[color:var(--fg)]/g; \
         s/bg-blue-600/bg-accent-blue/g; \
         s/hover:bg-blue-700/hover:bg-[color:rgb(37_99_235\/0.9)]/g; \
         s/focus:ring-blue-500/focus-visible:ring-[color:var(--accent-blue)]/g; \
         s/text-red-600/text-accent-red/g; \
         s/text-green-600/text-accent-green/g; \
         s/rounded-2xl/rounded-dash/g; \
         s/shadow-sm shadow/shadow-card/g; \
       ' /tmp/117-02-target.tsx
       ```
       Apply ONLY to className string contents. Then visually diff against backup to confirm no logic changed.

       **Token aliases needed:** `--muted-fg` and `--fg` are NOT in the default tokens.css output Phase 116 published. If they don't exist in tokens.css, fall back to literal `text-[color:rgba(0,0,0,0.6)]` (light) and add a TODO note for Phase 119. Inspect tokens.css FIRST (one of the <read_first> reads in Task 1 implicitly includes this — if missing, the planner-bridge: use direct rgba in the class string).

    4. **For `(auth)/layout.tsx` specifically:** This shell wraps every auth child. Apply the canonical-card-frame structure from <interfaces>. The shell's `<h1>Livinity</h1>` should be wrapped in `font-serif font-semibold tracking-tight`. Add an optional logo `<img>` if the existing layout had one; do NOT introduce a new asset.

    5. **Build + restart (defer to end of last task in this plan to avoid 3× rebuilds).** Mark this task DONE without restart; Task 3 does the final build+restart.

    6. **Write per-file diff summaries** to a temp note for the final SUMMARY.md (Task 3 collates).
  </action>
  <verify>
    <automated>
      ```
      ssh root@45.137.194.102 "set -e; \
        for f in 'layout.tsx' 'login/page.tsx' 'register/page.tsx'; do \
          test -f \"/opt/platform/web/src/app/(auth)/\${f}.pre-117-02.bak\" && echo \"BAK_OK:\${f}\"; \
          grep -E 'bg-card-bg|rounded-dash|text-accent-|bg-accent-blue|font-serif' \"/opt/platform/web/src/app/(auth)/\${f}\" >/dev/null && echo \"TOKENS_APPLIED:\${f}\" || echo \"NO_TOKENS:\${f}\"; \
          ! grep -E 'bg-zinc-50|bg-zinc-900|bg-blue-600|rounded-2xl' \"/opt/platform/web/src/app/(auth)/\${f}\" >/dev/null && echo \"ZINC_GONE:\${f}\" || echo \"ZINC_REMAINS:\${f}\"; \
          diff <(grep -E 'fetch\\(|useState|useRouter|useSearchParams|onSubmit|action=' \"/opt/platform/web/src/app/(auth)/\${f}.pre-117-02.bak\") \
               <(grep -E 'fetch\\(|useState|useRouter|useSearchParams|onSubmit|action=' \"/opt/platform/web/src/app/(auth)/\${f}\") \
            >/dev/null && echo \"LOGIC_UNCHANGED:\${f}\" || echo \"LOGIC_DRIFT:\${f}\"; \
        done"
      ```
      All `BAK_OK:*`, `TOKENS_APPLIED:*`, `ZINC_GONE:*`, `LOGIC_UNCHANGED:*` lines MUST print for layout/login/register. Any `LOGIC_DRIFT:*` MUST be investigated before proceeding.
    </automated>
  </verify>
  <done>
    3 backups + 3 restyled files on Server5. Token classes present, zinc classes gone, form-handler logic byte-identical to backups (logic-grep diff is empty).
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle verify + forgot-password + reset-password + device</name>
  <files>
    <!-- Server5 cross-repo:
         /opt/platform/web/src/app/(auth)/verify/page.tsx
         /opt/platform/web/src/app/(auth)/forgot-password/page.tsx
         /opt/platform/web/src/app/(auth)/reset-password/page.tsx
         /opt/platform/web/src/app/(auth)/device/page.tsx
         Each with .pre-117-02.bak sibling. -->
  </files>
  <read_first>
    1. Live source from Server5 (SINGLE SSH):
       ```
       ssh root@45.137.194.102 "set -e; \
         for f in verify forgot-password reset-password device; do \
           echo \"===\$f===\"; cat \"/opt/platform/web/src/app/(auth)/\$f/page.tsx\"; \
         done"
       ```
    2. `INVENTORY-SERVER5.md` rows for verify, forgot-password (has inline `rounded-xl border bg-white shadow-sm` confirm card), reset-password (note the `Suspense` boundary — must be preserved), device (note the `useRef` for code input — must be preserved).
  </read_first>
  <action>
    Same restyle methodology as Task 1, applied to the 4 remaining `(auth)/*` page.tsx files.

    **Special cases (do NOT break, D-117-NO-AUTH-FLOW-CHANGES):**

    - **`verify/page.tsx`:** Reads `?token=` via `useSearchParams`. Two visual states (success / error) — restyle BOTH using canonical card chrome. Submit / redirect logic stays.
    - **`forgot-password/page.tsx`:** Has an inline "check your email" confirmation card (per INVENTORY: `rounded-xl border bg-white shadow-sm`). Restyle this confirmation card to canonical `rounded-dash border border-dash-line bg-card-bg shadow-card`. Two visual states (form / confirmation).
    - **`reset-password/page.tsx`:** Wrapped in `<Suspense fallback={...}>` because `useSearchParams` is used. Preserve the Suspense boundary and the fallback element (its styling can be restyled to a canonical skeleton: `<div className="rounded-dash bg-card-bg-2 animate-pulse h-40 max-w-md mx-auto" />`).
    - **`device/page.tsx`:** Has `useRef` on the code input for autofocus. Preserve ref usage. The 6-character code-entry inputs (if present) should keep their `maxLength={1}` + `onPaste` handlers verbatim — restyle only border / focus colors.

    For each file:
    1. `cp <file> <file>.pre-117-02.bak`
    2. Apply the same sed class-map from Task 1.
    3. Apply per-file structural changes ONLY where the canonical card chrome from <interfaces> needs to wrap a content block that wasn't already wrapped (e.g. forgot-password confirmation state).
    4. Run the same logic-diff guard (Task 1's verify command) against this file's backup.

    **Build + pm2 restart still deferred to Task 3.**
  </action>
  <verify>
    <automated>
      ```
      ssh root@45.137.194.102 "set -e; \
        for f in 'verify/page.tsx' 'forgot-password/page.tsx' 'reset-password/page.tsx' 'device/page.tsx'; do \
          test -f \"/opt/platform/web/src/app/(auth)/\${f}.pre-117-02.bak\" && echo \"BAK_OK:\${f}\"; \
          grep -E 'bg-card-bg|rounded-dash|text-accent-|font-serif' \"/opt/platform/web/src/app/(auth)/\${f}\" >/dev/null && echo \"TOKENS_APPLIED:\${f}\" || echo \"NO_TOKENS:\${f}\"; \
          ! grep -E 'bg-zinc-50|bg-zinc-900|bg-blue-600|rounded-2xl|rounded-xl' \"/opt/platform/web/src/app/(auth)/\${f}\" >/dev/null && echo \"ZINC_GONE:\${f}\" || echo \"ZINC_REMAINS:\${f}\"; \
          diff <(grep -E 'fetch\\(|useState|useRouter|useSearchParams|onSubmit|useRef|Suspense' \"/opt/platform/web/src/app/(auth)/\${f}.pre-117-02.bak\") \
               <(grep -E 'fetch\\(|useState|useRouter|useSearchParams|onSubmit|useRef|Suspense' \"/opt/platform/web/src/app/(auth)/\${f}\") \
            >/dev/null && echo \"LOGIC_UNCHANGED:\${f}\" || echo \"LOGIC_DRIFT:\${f}\"; \
        done"
      ```
      All `BAK_OK:*`, `TOKENS_APPLIED:*`, `ZINC_GONE:*`, `LOGIC_UNCHANGED:*` lines MUST print for the 4 files.
    </automated>
  </verify>
  <done>
    4 backups + 4 restyled files on Server5. Token classes present, zinc/blue-600/rounded-2xl gone, all form-handler / Suspense / useRef references identical to backups.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + restart + curl smoke-test all 6 auth routes + write SUMMARY.md</name>
  <files>
    - .planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md
  </files>
  <read_first>
    1. Task 1 + 2 verify logs (already captured).
    2. `livos/packages/design-tokens/DESIGN-SYSTEM.md` § "Smoke testing" if it exists (for canonical curl assertions).
  </read_first>
  <action>
    Single SSH session for build + restart + smoke:

    ```
    ssh root@45.137.194.102 "set -e; \
      cd /opt/platform/web; \
      npm run build 2>&1 | tee /tmp/117-02-build.log | tail -50; \
      pm2 restart web && sleep 3 && pm2 list | grep web; \
      echo '===SMOKE==='; \
      for path in /login /register /verify /forgot-password /reset-password /device; do \
        code=\$(curl -s -o /tmp/117-02-\${path//\\//}.html -w '%{http_code}' \"https://livinity.io\${path}\"); \
        echo \"\${path} -> HTTP \${code}\"; \
      done; \
      echo '===GREP TOKEN MARKERS IN BUILT CSS==='; \
      grep -roE -- '--accent-blue|--dash-pad|--dash-radius' /opt/platform/web/.next/static/css/ 2>&1 | head -5; \
      echo '===GEIST FONT URL IN BUILT CSS==='; \
      grep -roE -- 'Geist|GeistVariableVF' /opt/platform/web/.next/static/css/ 2>&1 | head -3"
    ```

    Acceptance:
    - `npm run build` exits 0 (no TS/Tailwind errors).
    - pm2 `web` `online`.
    - Each of 6 paths returns `200`, `302`, `307`, or `308` (any 2xx or 3xx is fine — `/login` may redirect logged-in users; `/verify` requires `?token=` and may render with empty state).
    - Token + Geist markers present in built CSS.

    Then write the in-repo SUMMARY:

    ```
    .planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md
    ```

    Contents (follow $HOME/.claude/get-shit-done/templates/summary.md structure):
    - **Status:** SHIPPED / CODE-COMPLETE / UAT-PENDING
    - **Files modified (Server5):** the 7 paths + backup paths
    - **In-repo:** this SUMMARY.md
    - **Build log tail** (last 30 lines)
    - **Curl smoke matrix** (path -> http code)
    - **Logic-diff guard results** (LOGIC_UNCHANGED for all 7 files)
    - **Rollback recipe per file:**
      ```
      ssh root@45.137.194.102 "set -e; \
        cd /opt/platform/web/src/app/'(auth)'; \
        for f in layout.tsx login/page.tsx register/page.tsx verify/page.tsx forgot-password/page.tsx reset-password/page.tsx device/page.tsx; do \
          cp \"\${f}.pre-117-02.bak\" \"\${f}\"; \
        done; \
        cd /opt/platform/web && npm run build && pm2 restart web"
      ```
    - **Followups for Phase 119 (ui-kit):** any `var(--muted-fg)` fallbacks used that should become canonical tokens; any duplicated card chrome that could become a `<AuthCard>` primitive.
  </action>
  <verify>
    <automated>
      ```
      test -f .planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md && echo SUMMARY_OK
      ssh root@45.137.194.102 "pm2 list | grep -E 'web.*online' && echo PM2_ONLINE; \
        for p in /login /register /verify /forgot-password /reset-password /device; do \
          c=\$(curl -s -o /dev/null -w '%{http_code}' \"https://livinity.io\$p\"); \
          [[ \$c =~ ^[23] ]] && echo \"OK_\$p:\$c\" || echo \"FAIL_\$p:\$c\"; \
        done"
      ```
      Expected: `SUMMARY_OK`, `PM2_ONLINE`, and 6× `OK_/...:2xx-or-3xx`.
    </automated>
  </verify>
  <done>
    pm2 `web` online, all 6 auth routes return 2xx/3xx, token + Geist present in built CSS, 117-02-SUMMARY.md in-repo with rollback recipe.
  </done>
</task>

</tasks>

<verification>
- 7 files modified on Server5 (1 layout + 6 pages), each with `.pre-117-02.bak`.
- Logic-grep diff against `.bak` is empty for every file (D-117-NO-AUTH-FLOW-CHANGES proven).
- `/opt/platform/web/src/app/api/auth/**` is byte-unchanged (D-117-NO-API-CHANGES). Verify:
  ```
  ssh root@45.137.194.102 "find /opt/platform/web/src/app/api -newer /opt/platform/web/_pre-117-src-src.tar.gz -type f 2>&1 | head -5"
  ```
  (should output nothing — no API files newer than the 117-01 marker).
- All 6 auth routes return 2xx/3xx.
- 117-02-SUMMARY.md committed in-repo.
</verification>

<success_criteria>
- 6 must_haves truths satisfied.
- 8 artifacts produced (7 Server5 files + 1 SUMMARY).
- Smoke-test matrix shows 6/6 routes responding.
- Rollback recipe documented.
</success_criteria>

<output>
After completion, create `.planning/phases/117-server5-nextjs-migration/117-02-SUMMARY.md`. Commit in this repo. Server5 files are cross-repo (NOT in this git tree) — capture their paths + backup paths in the SUMMARY for operator traceability.
</output>
