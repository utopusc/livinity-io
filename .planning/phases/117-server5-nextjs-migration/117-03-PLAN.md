---
phase: 117-server5-nextjs-migration
plan: 03
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - .planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md
autonomous: true
requirements: []
tags: [v35, design-system, server5, dashboard-install, audit, restyle]
must_haves:
  truths:
    - "All `/dashboard/install` source files audited against the canonical token set"
    - "Any file with drift after Phase 116 token rename has `.pre-117-03.bak` and a token-aligned patched version"
    - "Files already canonical are documented as NOOP in SUMMARY (no backup, no patch)"
    - "Visual flow of `/dashboard/install` is unchanged — step transitions, form submissions, install-command generation behave identically"
    - "`/dashboard/install` route returns 200 via curl after pm2 restart"
    - "D-117-PRESERVE-DASHBOARD-INSTALL honored: no refactor, only surface alignment"
  artifacts:
    - path: "/opt/platform/web/src/app/dashboard/install/page.tsx"
      provides: "Token-audited install wizard root (NOOP or minor patch)"
    - path: "/opt/platform/web/src/app/onboarding/install/components/mode-cards.tsx"
      provides: "Token-audited mode picker"
    - path: "/opt/platform/web/src/app/onboarding/install/components/mode-docs.tsx"
      provides: "Token-audited mode docs accordion"
    - path: "/opt/platform/web/src/app/onboarding/install/components/hybrid-form.tsx"
      provides: "Token-audited hybrid-mode form"
    - path: "/opt/platform/web/src/app/onboarding/install/components/local-form.tsx"
      provides: "Token-audited local-mode form"
    - path: "/opt/platform/web/src/app/onboarding/install/components/wizard-stepper.tsx"
      provides: "Token-audited stepper (replace-with-library candidate, but audited for v35 surface)"
    - path: "/opt/platform/web/src/app/onboarding/install/components/install-command-display.tsx"
      provides: "Token-audited final install-command card with copy button"
    - path: ".planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md"
      provides: "Plan completion record + per-file NOOP-vs-patched matrix"
  key_links:
    - from: "/opt/platform/web/src/app/dashboard/install/page.tsx"
      to: "/opt/platform/web/src/app/onboarding/install/components/*"
      via: "import"
      pattern: "from\\s+['\"]\\.\\./\\.\\.\\/onboarding/install/components"
    - from: "/opt/platform/web/src/app/onboarding/install/components/hybrid-form.tsx"
      to: "/api/cf/resolve-zone"
      via: "fetch"
      pattern: "fetch\\(.*api/cf/resolve-zone"
---

<objective>
Per D-117-PRESERVE-DASHBOARD-INSTALL: `/dashboard/install` was shipped in Phase 111 follow-up (2026-05-14) and already largely matches the canonical dashboard.html idiom. This plan **audits** every file in the install-wizard subtree against the Phase 116 token set, **patches only files that have drift** (NOOP everything else with a written note), and re-smoke-tests the route.

**Explicit non-goal:** No refactor. No primitive extraction. No structural JSX changes beyond replacing stale class strings with canonical equivalents.

**Scope locks:**
- D-117-NO-API-CHANGES — `/api/cf/resolve-zone` / install-event API routes untouched.
- D-117-NO-AUTH-FLOW-CHANGES — `getSession` / auth middleware around `/dashboard/install` untouched.
- D-117-PRESERVE-DASHBOARD-INSTALL — minimal-touch principle; document NOOPs.

Purpose: Close any drift `/dashboard/install` developed against the canonical token set without disturbing Phase 111's working pipeline.
Output: Per-file audit matrix in 117-03-SUMMARY.md; backups + patches only on drifted files; pm2 restart; route smoke-test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/117-server5-nextjs-migration/117-CONTEXT.md
@.planning/phases/117-server5-nextjs-migration/117-01-PLAN.md
@.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/design-tokens/tokens.css
@./CLAUDE.md

<interfaces>
**Audit rubric — each file is classified as:**

| Tag | Definition | Action |
|---|---|---|
| `CANONICAL` | All class strings already use tokens or canonical CSS vars; no zinc/blue-600/hard-coded gray | NOOP — record in SUMMARY |
| `MINOR-DRIFT` | 1-5 stale class names (e.g. `bg-yellow-50` warning) | Patch via class-map sed, single `.pre-117-03.bak` |
| `MAJOR-DRIFT` | Substantial zinc-* / blue-* / rounded-xl usage | Patch in full (still no structural refactor), `.pre-117-03.bak` |

**Canonical class map (subset relevant to install wizard):**
```
bg-zinc-50            → bg-card-bg-2
bg-zinc-900           → bg-card-bg
bg-white              → bg-card-bg
border-zinc-200/300   → border-dash-line / border-dash-line-strong
bg-yellow-50          → bg-[color:rgb(217_119_6/0.08)]   (canonical amber alert surface)
text-yellow-700/800   → text-accent-amber
bg-blue-50            → bg-[color:rgb(37_99_235/0.08)]   (canonical info surface)
bg-blue-600           → bg-accent-blue
text-blue-600         → text-accent-blue
rounded-xl            → rounded-dash    (the inventory's "inline rounded-xl" → canonical 18px)
rounded-2xl           → rounded-dash
shadow-sm shadow-md   → shadow-card     (when used as a card chrome shadow, not for small UI affordances)
```

**Files in scope (all on Server5):**
```
/opt/platform/web/src/app/dashboard/install/page.tsx              # wizard root
/opt/platform/web/src/app/onboarding/install/components/mode-cards.tsx
/opt/platform/web/src/app/onboarding/install/components/mode-docs.tsx
/opt/platform/web/src/app/onboarding/install/components/hybrid-form.tsx
/opt/platform/web/src/app/onboarding/install/components/local-form.tsx           # has bg-yellow-50 callout
/opt/platform/web/src/app/onboarding/install/components/wizard-stepper.tsx        # candidate for ui-kit
/opt/platform/web/src/app/onboarding/install/components/install-command-display.tsx
```

Additionally inspect (per Phase 111 commits) if present:
```
/opt/platform/web/src/app/onboarding/install/components/mode-form.tsx
/opt/platform/web/src/app/onboarding/install/components/mode-config-form.tsx
/opt/platform/web/src/app/onboarding/install/components/submit-button.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit pass — read every file, classify CANONICAL / MINOR / MAJOR drift</name>
  <files>
    <!-- Server5 read-only at this step. Result is an in-memory matrix used by Task 2. -->
  </files>
  <read_first>
    1. `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md` — `app/dashboard/` (lines ~52-58) + `app/onboarding/install/` (lines ~60-72) sections. Note: dashboard/install/page.tsx is tagged `needs-migration` with the docstring claim "matches /dashboard's design" being false against canonical tokens.
    2. `.planning/phases/117-server5-nextjs-migration/117-01-PLAN.md` <interfaces> — for the class-map source-of-truth.
    3. Live source from Server5 (SINGLE SSH). List the install/components dir first, then cat each:
       ```
       ssh root@45.137.194.102 "set -e; \
         echo '===dashboard/install dir==='; ls -la /opt/platform/web/src/app/dashboard/install/; \
         echo '===onboarding/install/components dir==='; ls -la /opt/platform/web/src/app/onboarding/install/components/; \
         for f in /opt/platform/web/src/app/dashboard/install/page.tsx \
                  /opt/platform/web/src/app/onboarding/install/components/*.tsx; do \
           echo \"===\$f===\"; cat \"\$f\"; done"
       ```
  </read_first>
  <action>
    For each file in <interfaces>, classify drift:

    1. Grep stale tokens (per file):
       ```
       ssh root@45.137.194.102 "for f in /opt/platform/web/src/app/dashboard/install/page.tsx \
                                          /opt/platform/web/src/app/onboarding/install/components/*.tsx; do \
         drift=\$(grep -cE 'bg-zinc-[0-9]+|text-zinc-[0-9]+|border-zinc-[0-9]+|bg-yellow-[0-9]+|text-yellow-[0-9]+|bg-blue-[0-9]+|text-blue-[0-9]+|rounded-xl|rounded-2xl' \"\$f\" || true); \
         echo \"\${f}: drift_lines=\${drift}\"; \
       done"
       ```

    2. Apply classification rule:
       - `drift_lines == 0` → `CANONICAL` (NOOP)
       - `1 <= drift_lines <= 5` → `MINOR-DRIFT`
       - `drift_lines > 5` → `MAJOR-DRIFT`

    3. Stash the matrix in `/tmp/117-03-audit-matrix.txt` on this runner machine. Format (one line per file):
       ```
       <abs-path>  drift_lines=<N>  classification=<CANONICAL|MINOR-DRIFT|MAJOR-DRIFT>
       ```

    4. **No edits in this task** — patches happen in Task 2.
  </action>
  <verify>
    <automated>
      ```
      test -f /tmp/117-03-audit-matrix.txt && wc -l /tmp/117-03-audit-matrix.txt
      grep -cE 'CANONICAL|MINOR-DRIFT|MAJOR-DRIFT' /tmp/117-03-audit-matrix.txt
      ```
      Expected: 7-10 lines (the install subtree size).
    </automated>
  </verify>
  <done>
    Audit matrix at `/tmp/117-03-audit-matrix.txt` lists every install-subtree file with a drift classification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch only MINOR/MAJOR-DRIFT files; NOOP CANONICAL files</name>
  <files>
    <!-- Server5 cross-repo:
         Per-file .pre-117-03.bak + patched version only for non-CANONICAL files. -->
  </files>
  <read_first>
    1. `/tmp/117-03-audit-matrix.txt` from Task 1.
    2. For each MINOR/MAJOR file, re-read its current contents from Server5 (the audit may have classified, but we need the bytes to patch).
  </read_first>
  <action>
    For each file with classification != `CANONICAL`:

    1. **Backup:**
       ```
       ssh root@45.137.194.102 "cp '<file>' '<file>.pre-117-03.bak'"
       ```

    2. **Apply class-map sed** (in-place on Server5, then re-read to confirm):
       ```
       ssh root@45.137.194.102 "sed -E -i \
         -e 's/bg-zinc-50/bg-card-bg-2/g' \
         -e 's/bg-zinc-900/bg-card-bg/g' \
         -e 's/\\bbg-white\\b/bg-card-bg/g' \
         -e 's/border-zinc-200/border-dash-line/g' \
         -e 's/border-zinc-300/border-dash-line-strong/g' \
         -e 's/text-zinc-500/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-600/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-700/text-[color:var(--fg)]/g' \
         -e 's/text-zinc-900/text-[color:var(--fg)]/g' \
         -e 's/bg-yellow-50/bg-[color:rgb(217_119_6\\/0.08)]/g' \
         -e 's/text-yellow-700/text-accent-amber/g' \
         -e 's/text-yellow-800/text-accent-amber/g' \
         -e 's/bg-blue-50/bg-[color:rgb(37_99_235\\/0.08)]/g' \
         -e 's/bg-blue-600/bg-accent-blue/g' \
         -e 's/hover:bg-blue-700/hover:bg-[color:rgb(37_99_235\\/0.9)]/g' \
         -e 's/text-blue-600/text-accent-blue/g' \
         -e 's/rounded-xl/rounded-dash/g' \
         -e 's/rounded-2xl/rounded-dash/g' \
         '<file>'"
       ```

    3. **Logic-diff guard** (D-117-NO-AUTH-FLOW-CHANGES, D-117-NO-API-CHANGES):
       ```
       ssh root@45.137.194.102 "diff \
         <(grep -E 'fetch\\(|useState|useRouter|useEffect|onSubmit|onChange|onClick|action=' '<file>.pre-117-03.bak') \
         <(grep -E 'fetch\\(|useState|useRouter|useEffect|onSubmit|onChange|onClick|action=' '<file>') \
         && echo 'LOGIC_UNCHANGED' || echo 'LOGIC_DRIFT'"
       ```

    4. **For CANONICAL files:** No backup, no edit. Record in SUMMARY as NOOP.

    5. **Special case — `local-form.tsx`:** Per INVENTORY note, it has `bg-yellow-50` warning callout. The class-map handles this, BUT verify the callout still reads clearly after restyle (amber on white is readable; check via curl in Task 3).

    6. **Special case — `wizard-stepper.tsx`:** Tagged `replace-with-library` in INVENTORY (Phase 119 candidate). For Phase 117, only sed-replace the tokens; do NOT extract or rewrite. Add a `// TODO(v35-phase-119): extract to @livinity/ui-kit` comment at the top of the file ONLY IF the file does not already have such a marker (idempotent grep-then-prepend).

    7. **Build + restart still deferred to Task 3.**
  </action>
  <verify>
    <automated>
      ```
      ssh root@45.137.194.102 "set -e; \
        for f in /opt/platform/web/src/app/dashboard/install/page.tsx \
                 /opt/platform/web/src/app/onboarding/install/components/*.tsx; do \
          if [ -f \"\${f}.pre-117-03.bak\" ]; then \
            echo \"PATCHED:\${f}\"; \
            drift=\$(grep -cE 'bg-zinc-[0-9]|text-zinc-[0-9]|border-zinc-[0-9]|bg-yellow-50|bg-blue-50|bg-blue-600|rounded-xl|rounded-2xl' \"\${f}\" || true); \
            [ \"\${drift}\" = '0' ] && echo \"CLEAN:\${f}\" || echo \"RESIDUE:\${f}:\${drift}\"; \
            diff <(grep -E 'fetch\\(|useState|useRouter|useEffect|onSubmit|onChange|onClick|action=' \"\${f}.pre-117-03.bak\") \
                 <(grep -E 'fetch\\(|useState|useRouter|useEffect|onSubmit|onChange|onClick|action=' \"\${f}\") >/dev/null \
              && echo \"LOGIC_UNCHANGED:\${f}\" || echo \"LOGIC_DRIFT:\${f}\"; \
          else \
            echo \"NOOP:\${f}\"; \
          fi; \
        done"
      ```
      Expected: each file has either `PATCHED:` + `CLEAN:` + `LOGIC_UNCHANGED:` triplet, or `NOOP:` line.
    </automated>
  </verify>
  <done>
    Every file in the install subtree is either patched cleanly (with logic identical to backup) or NOOP-classified. Audit matrix is up-to-date.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + pm2 restart + curl /dashboard/install + write SUMMARY.md with NOOP/PATCHED matrix</name>
  <files>
    - .planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md
  </files>
  <read_first>
    1. `/tmp/117-03-audit-matrix.txt` + Task 2 verify output.
    2. Capture build state pre-restart:
       ```
       ssh root@45.137.194.102 "pm2 list | grep web; cat /tmp/117-02-build.log 2>/dev/null | tail -5 || true"
       ```
  </read_first>
  <action>
    Single SSH:
    ```
    ssh root@45.137.194.102 "set -e; \
      cd /opt/platform/web; \
      npm run build 2>&1 | tee /tmp/117-03-build.log | tail -50; \
      pm2 restart web && sleep 3 && pm2 list | grep web; \
      echo '===SMOKE==='; \
      code=\$(curl -s -o /tmp/117-03-install.html -w '%{http_code}' 'https://livinity.io/dashboard/install'); \
      echo \"/dashboard/install -> HTTP \${code}\"; \
      echo '===GREP TOKEN MARKERS IN INSTALL HTML/CSS==='; \
      grep -roE -- '--accent-amber|--accent-blue|--dash-radius' /opt/platform/web/.next/static/css/ 2>&1 | head -5"
    ```

    Acceptance:
    - Build exits 0.
    - pm2 `web` `online`.
    - `/dashboard/install` returns 200 (auth-gated — if 307 redirect to /login, that's the existing behavior; cross-check against the 117-02 smoke baseline).
    - `--accent-amber` token present in built CSS (proof local-form's amber callout maps through the token system).

    Write `.planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md`:
    - **Per-file matrix:** `path | classification | drift_lines_before | patched? | logic_unchanged?`
    - **NOOP files** explicitly listed with rationale ("Phase 111 already canonical for these tokens")
    - **Build log tail** (last 30 lines)
    - **Smoke result** for `/dashboard/install`
    - **D-117-PRESERVE-DASHBOARD-INSTALL compliance note:** zero structural / handler changes; only className/value substitutions
    - **Rollback recipe:**
      ```
      ssh root@45.137.194.102 "set -e; \
        for f in /opt/platform/web/src/app/dashboard/install/page.tsx \
                 /opt/platform/web/src/app/onboarding/install/components/*.tsx; do \
          [ -f \"\${f}.pre-117-03.bak\" ] && cp \"\${f}.pre-117-03.bak\" \"\${f}\"; \
        done; \
        cd /opt/platform/web && npm run build && pm2 restart web"
      ```
    - **Followups for Phase 119:** wizard-stepper extraction; alert primitive for the bg-yellow callout in local-form.
  </action>
  <verify>
    <automated>
      ```
      test -f .planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md && echo SUMMARY_OK
      ssh root@45.137.194.102 "pm2 list | grep -E 'web.*online' && echo PM2_ONLINE; \
        c=\$(curl -s -o /dev/null -w '%{http_code}' 'https://livinity.io/dashboard/install'); \
        [[ \$c =~ ^[23] ]] && echo OK_install:\$c || echo FAIL_install:\$c"
      ```
      Expected: `SUMMARY_OK`, `PM2_ONLINE`, `OK_install:2xx-or-3xx`.
    </automated>
  </verify>
  <done>
    /dashboard/install responsive 2xx/3xx, NOOP/PATCHED matrix recorded in SUMMARY, rollback recipe committed.
  </done>
</task>

</tasks>

<verification>
- Audit matrix produced and recorded in SUMMARY.md.
- Every non-CANONICAL file has `.pre-117-03.bak` + logic-identical patched version on Server5.
- Every CANONICAL file is documented as NOOP (no backup is acceptable evidence).
- `/dashboard/install` responsive after pm2 restart.
- D-117-PRESERVE-DASHBOARD-INSTALL honored: zero structural refactor (greppable by absence of new JSX blocks vs .bak).
- D-117-NO-API-CHANGES: `/api/cf/**` byte-unchanged (no files under `/api/` are newer than the 117-01 marker tarball).
</verification>

<success_criteria>
- 6 must_haves truths satisfied.
- Per-file audit matrix complete.
- /dashboard/install green via curl.
- 117-03-SUMMARY.md committed in-repo with rollback recipe + Phase 119 followups.
</success_criteria>

<output>
After completion, create `.planning/phases/117-server5-nextjs-migration/117-03-SUMMARY.md`. Include the per-file matrix (file | classification | patched? | logic_unchanged?), the smoke result, the rollback recipe, and the Phase 119 followup list.
</output>
