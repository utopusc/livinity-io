---
phase: 04-onboarding-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/onboarding/setup-wizard.tsx
autonomous: true

must_haves:
  truths:
    - "Setup wizard Step 4 shows 'Connect Kimi AI' with API key input field and validate button"
    - "Entering a valid Kimi API key and clicking validate calls tRPC ai.kimiLogin and shows success"
    - "After successful validation, user can proceed to the next step"
    - "Skipping the AI step still advances to the final step"
    - "Zero references to 'Claude', 'Anthropic', or 'Gemini' exist in setup-wizard.tsx"
    - "The wizard title, subtitle, and instructions all reference 'Kimi' not 'Claude'"
  artifacts:
    - path: "livos/packages/ui/src/routes/onboarding/setup-wizard.tsx"
      provides: "Kimi AI onboarding step replacing Claude OAuth flow"
      contains: "Kimi"
  key_links:
    - from: "livos/packages/ui/src/routes/onboarding/setup-wizard.tsx"
      to: "livos/packages/livinityd/source/modules/ai/routes.ts"
      via: "tRPC ai.kimiLogin and ai.getKimiStatus calls"
      pattern: "trpcReact.ai.getKimiStatus|trpcReact.ai.kimiLogin"
---

<objective>
Replace the Claude OAuth onboarding step (Step 4 of the setup wizard) with a Kimi API key step. The current step uses Claude CLI OAuth with PKCE flow, device auth codes, and polling. The Kimi replacement is simpler: an API key input field that validates against the Kimi API and stores the key.

Purpose: ONBOARD-01 and ONBOARD-02 require the setup wizard to accept Kimi credentials and validate the connection. AUTH-04 (device auth) is optional and not needed for initial setup since API key auth is the primary flow established in Phase 2.
Output: Updated setup-wizard.tsx with StepKimiAuth replacing StepClaudeAuth.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The file to modify -- read FULLY before changing:
@livos/packages/ui/src/routes/onboarding/setup-wizard.tsx

# Phase 2 plan for understanding what tRPC routes exist:
@.planning/phases/02-configuration-layer/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace StepClaudeAuth with StepKimiAuth</name>
  <files>livos/packages/ui/src/routes/onboarding/setup-wizard.tsx</files>
  <action>
Replace the `StepClaudeAuth` component (lines 696-901) with a new `StepKimiAuth` component. The current component uses complex Claude OAuth PKCE flow with device auth codes. The Kimi replacement is much simpler: just an API key input and validation.

**Delete completely:**
- The entire `StepClaudeAuth` function (lines 696-901)

**Create `StepKimiAuth` in the same location with this behavior:**

1. **State:** `apiKey` (string), validation status via tRPC mutation
2. **tRPC calls:** Use `trpcReact.ai.getKimiStatus.useQuery()` to check if already configured, and `trpcReact.ai.kimiLogin.useMutation()` to save + validate the API key
3. **UI layout** (keep the same visual style as other steps):
   - Header: icon `IconSparkles` (already imported) + title "Connect Kimi AI"
   - Subtitle: "Enter your Kimi API key to enable AI features"
   - Body card (same `rounded-xl border border-border-default bg-surface-base p-5 space-y-4` pattern):
     - If already authenticated (kimiStatusQ.data?.authenticated): show green checkmark with "Connected" message, same animated spring pattern as current Claude success state
     - If not authenticated: show API key input field using `<PasswordInput>` (already imported) with placeholder "Enter your Kimi API key", a "Validate & Save" primary button that calls `kimiLoginMutation.mutate({ apiKey: apiKey.trim() })`
     - On mutation success: invalidate kimiStatus query so it shows connected state
     - On mutation error: show error message below the input
     - A help link: `<a href="https://kimi.com" target="_blank" ...>Get a Kimi API key</a>` with `IconExternalLink`
   - Continue button (shown when authenticated): same pattern as current Claude step
   - Skip button: always visible, same `skipButtonClass`

**IMPORTANT style notes:**
- Reuse all existing CSS class constants: `primaryButtonClass`, `skipButtonClass`, `secondaryButtonClass`
- Reuse existing animation patterns: `motion.div` with `initial/animate`, spring transitions
- Keep the green checkmark success animation identical to the current StepClaudeAuth success state (motion.div with scale spring)
- Use `t()` for translatable strings where the current step uses them. Update translation keys from `onboarding.claude.*` to `onboarding.kimi.*`

**Update the main wizard (SetupWizard component, line ~1089):**
- Change the Step 4 comment from `{/* Step 4: Claude AI Auth */}` to `{/* Step 4: Kimi AI Auth */}`
- Change `<StepClaudeAuth onNext={goNext} onSkip={goNext} />` to `<StepKimiAuth onNext={goNext} onSkip={goNext} />`

**Remove unused imports/references:**
- The existing tRPC calls in StepClaudeAuth reference: `trpcReact.ai.getClaudeCliStatus`, `trpcReact.ai.setClaudeAuthMethod`, `trpcReact.ai.startClaudeLogin`, `trpcReact.ai.submitClaudeLoginCode`, `trpcReact.ai.claudeLogout`. None of these should remain in the file.
- All these are used ONLY in StepClaudeAuth, so deleting that component removes all references.

**Do NOT change:**
- Steps 0-3 (Welcome, Create Account, Personalize, Domain Setup) -- leave untouched
- Step 5 (All Done) -- leave untouched
- Any imports used by other steps (motion, icons, etc.)
- The TOTAL_STEPS constant (stays 6)
  </action>
  <verify>
1. `grep -c "claude\|Claude\|anthropic\|Anthropic\|gemini\|Gemini" livos/packages/ui/src/routes/onboarding/setup-wizard.tsx` returns 0 (the dnd-kit comment on files-dnd-wrapper is a different file)
2. `grep -c "StepKimiAuth\|getKimiStatus\|kimiLogin" livos/packages/ui/src/routes/onboarding/setup-wizard.tsx` returns at least 3
3. `grep -c "StepClaudeAuth\|getClaudeCliStatus\|startClaudeLogin" livos/packages/ui/src/routes/onboarding/setup-wizard.tsx` returns 0
4. `cd livos && pnpm --filter ui typecheck` passes (or `tsc --noEmit` if available)
  </verify>
  <done>
Step 4 of the setup wizard shows "Connect Kimi AI" with API key input, validates via tRPC ai.kimiLogin, shows green checkmark on success, and allows skip. Zero Claude/Anthropic/Gemini references remain in setup-wizard.tsx.
  </done>
</task>

</tasks>

<verification>
1. Open setup-wizard.tsx and confirm StepKimiAuth exists with API key input field
2. Confirm no Claude/Anthropic/Gemini strings in the file
3. Confirm tRPC calls reference ai.getKimiStatus and ai.kimiLogin (not Claude equivalents)
4. TypeScript compiles without errors
</verification>

<success_criteria>
- Setup wizard Step 4 renders Kimi API key input (not Claude OAuth)
- Valid API key entry triggers tRPC kimiLogin mutation
- Successful validation shows green authenticated state
- Skip button advances to Step 5
- Zero claude/anthropic/gemini references in setup-wizard.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-cleanup/04-01-SUMMARY.md`
</output>
