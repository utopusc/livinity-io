---
phase: 04-onboarding-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nexus/packages/core/package.json
  - nexus/packages/core/src/providers/claude.ts
  - nexus/packages/core/src/sdk-agent-runner.ts
  - nexus/packages/core/src/providers/gemini.ts
  - nexus/packages/core/src/providers/index.ts
  - nexus/packages/core/src/providers/manager.ts
  - nexus/packages/core/src/providers/normalize.ts
  - nexus/packages/core/src/providers/types.ts
  - nexus/packages/core/src/api.ts
  - nexus/packages/core/src/agent.ts
  - nexus/packages/core/src/daemon.ts
  - nexus/packages/core/src/brain.ts
  - nexus/packages/core/src/ws-gateway.ts
  - nexus/packages/core/src/skill-loader.ts
  - nexus/packages/core/src/task-manager.ts
  - nexus/packages/core/src/tool-registry.ts
  - nexus/packages/core/src/multi-agent.ts
  - nexus/packages/core/src/config/schema.ts
  - nexus/packages/core/src/index.ts
  - nexus/packages/core/src/infra/errors.ts
  - nexus/packages/core/src/infra/backoff.ts
  - nexus/packages/memory/src/index.ts
  - nexus/setup.ts
  - livos/packages/ui/src/routes/settings/ai-config.tsx
  - livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  - livos/packages/ui/src/routes/settings/_components/ai-config-dialog.tsx
  - livos/packages/ui/src/routes/settings/usage-dashboard.tsx
  - livos/packages/livinityd/source/modules/ai/routes.ts
  - livos/.env.example
autonomous: true

must_haves:
  truths:
    - "grep -ri 'claude\\|anthropic' returns zero matches in *.ts and *.tsx source files (excluding comments about dnd-kit author 'clauderic', planning docs, and node_modules)"
    - "@anthropic-ai/sdk and @anthropic-ai/claude-agent-sdk are absent from package.json"
    - "@google/generative-ai is absent from package.json"
    - "ClaudeProvider class and file are deleted"
    - "SdkAgentRunner class and file are deleted"
    - "GeminiProvider class and file are deleted"
    - "ProviderManager references only KimiProvider, no fallback chain"
    - "All tRPC procedures reference Kimi, not Claude"
    - ".env.example has no ANTHROPIC_API_KEY or GEMINI_API_KEY entries"
    - "Settings UI AI config page references Kimi, not Claude or Gemini"
    - "TypeScript compilation succeeds with no errors"
  artifacts:
    - path: "nexus/packages/core/src/providers/claude.ts"
      provides: "DELETED"
    - path: "nexus/packages/core/src/sdk-agent-runner.ts"
      provides: "DELETED"
    - path: "nexus/packages/core/src/providers/gemini.ts"
      provides: "DELETED"
    - path: "nexus/packages/core/src/providers/manager.ts"
      provides: "ProviderManager with KimiProvider only"
      contains: "KimiProvider"
    - path: "nexus/packages/core/src/providers/index.ts"
      provides: "Exports KimiProvider, no Claude/Gemini exports"
      contains: "KimiProvider"
  key_links:
    - from: "nexus/packages/core/src/providers/manager.ts"
      to: "nexus/packages/core/src/providers/kimi.ts"
      via: "import and instantiation"
      pattern: "KimiProvider"
    - from: "nexus/packages/core/src/agent.ts"
      to: "nexus/packages/core/src/providers/types.ts"
      via: "ToolDefinition type (renamed from ClaudeToolDefinition)"
      pattern: "ToolDefinition"
---

<objective>
Delete all Claude, Anthropic, and Gemini code from the codebase. This covers CLEAN-01 through CLEAN-07: remove SDK packages, delete provider files, clean references from every importing file, update types, update config defaults, clean .env, and run final grep verification.

Purpose: After Phases 1-3 build the Kimi replacement, this plan removes all dead code from the previous provider. The system must compile cleanly with only Kimi as the AI provider.
Output: Clean codebase with zero Claude/Anthropic/Gemini references in active source, packages removed from package.json, deleted provider files.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files to read before modifying:
@nexus/packages/core/package.json
@nexus/packages/core/src/providers/index.ts
@nexus/packages/core/src/providers/manager.ts
@nexus/packages/core/src/providers/types.ts
@nexus/packages/core/src/providers/normalize.ts
@nexus/packages/core/src/api.ts
@nexus/packages/core/src/agent.ts
@nexus/packages/core/src/daemon.ts
@nexus/packages/core/src/tool-registry.ts
@nexus/packages/core/src/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete provider files and remove packages</name>
  <files>
    nexus/packages/core/src/providers/claude.ts
    nexus/packages/core/src/sdk-agent-runner.ts
    nexus/packages/core/src/providers/gemini.ts
    nexus/packages/core/package.json
  </files>
  <action>
**Step 1: Delete dead files**

Delete these 3 files completely:
- `nexus/packages/core/src/providers/claude.ts` (CLEAN-02: ClaudeProvider class)
- `nexus/packages/core/src/sdk-agent-runner.ts` (CLEAN-03: SdkAgentRunner class)
- `nexus/packages/core/src/providers/gemini.ts` (CLEAN-04: GeminiProvider)

Use `rm` or equivalent -- these files should not exist after this task.

**Step 2: Remove packages from package.json (CLEAN-01)**

In `nexus/packages/core/package.json`, remove these 3 dependencies:
- `"@anthropic-ai/claude-agent-sdk": "^0.2.0"` (line 23)
- `"@anthropic-ai/sdk": "^0.74.0"` (line 24)
- `"@google/generative-ai": "^0.21.0"` (line 25)

Do NOT run `npm install` yet -- that will happen at the end after all file changes are complete.

**Step 3: Update providers/index.ts**

In `nexus/packages/core/src/providers/index.ts`, the current content is:
```
export * from './types.js';
export * from './normalize.js';
export { ClaudeProvider } from './claude.js';
export { GeminiProvider } from './gemini.js';
export { ProviderManager } from './manager.js';
```

Replace with:
```
export * from './types.js';
export * from './normalize.js';
export { KimiProvider } from './kimi.js';
export { ProviderManager } from './manager.js';
```

Note: `kimi.ts` was created in Phase 1. If `kimi.js` doesn't exist in the exports, use whatever filename Phase 1 created (check `ls nexus/packages/core/src/providers/`).

**Step 4: Update providers/manager.ts**

The current ProviderManager imports ClaudeProvider and GeminiProvider, instantiates both, and maintains a fallback chain. Replace with single-provider Kimi architecture:

- Remove imports: `import { ClaudeProvider } from './claude.js'` and `import { GeminiProvider } from './gemini.js'`
- Add import: `import { KimiProvider } from './kimi.js'`
- In constructor: Replace the Claude+Gemini instantiation with just KimiProvider:
  ```typescript
  constructor(redis?: Redis) {
    const kimi = new KimiProvider(redis);
    this.providers.set('kimi', kimi);
    this.fallbackOrder = ['kimi'];
  }
  ```
- In `getActiveProviderId()`: Change the fallback default from `'gemini'` to `'kimi'`
- The rest of ProviderManager's methods (chat, chatStream, think, listProviders) are provider-agnostic and work with any AIProvider. Keep them as-is.

**Step 5: Update providers/types.ts**

Rename Claude-specific type names to generic names. These types are used throughout the codebase:

- `ClaudeToolDefinition` -> `ToolDefinition` (it's just a standard tool definition format, not Claude-specific)
- `ToolUseBlock` -> keep as-is (generic enough name)
- `ToolResultBlock` -> keep as-is (generic enough name)
- In the `ProviderChatOptions` interface, update the `tools` field type: `tools?: ToolDefinition[]`
- In the JSDoc comments, remove "Claude" references:
  - Line 23: `/** Tool definitions for native tool calling (Claude tool_use) */` -> `/** Tool definitions for native tool calling */`
  - Line 36: `/** Native tool calls from the provider (Claude tool_use blocks) */` -> `/** Native tool calls from the provider */`
  - Line 83: `/** Claude tool definition format (input_schema, not parameters) */` -> `/** Tool definition format (input_schema style) */`
  - Line 94: `/** A tool_use content block from Claude's response */` -> `/** A tool_use content block from provider response */`
  - Line 102: `/** A tool_result content block to send back to Claude */` -> `/** A tool_result content block to send back to provider */`
- In `PROVIDER_COST_DEFAULTS` (line 111-124): Remove both `claude` and `gemini` entries. Replace with a `kimi` entry:
  ```typescript
  export const PROVIDER_COST_DEFAULTS = {
    kimi: {
      flash: { input: 0.10, output: 0.40 },
      haiku: { input: 0.10, output: 0.40 },
      sonnet: { input: 0.60, output: 2.40 },
      opus: { input: 2.00, output: 8.00 },
    },
  } as const;
  ```

**Step 6: Update providers/normalize.ts**

- In `prepareForProvider` function signature (line 95): Change `provider: 'claude' | 'gemini'` to `provider: 'kimi'`
- Remove the entire `if (provider === 'claude')` block (lines 98-123) that does alternation validation and Claude-specific message formatting
- Remove the Gemini-specific format conversion (lines 126-134) with role mapping 'assistant' -> 'model'
- Replace both blocks with a Kimi/OpenAI-compatible format (since Kimi uses OpenAI-compatible API):
  ```typescript
  // Kimi uses OpenAI-compatible message format
  return merged.map((msg) => {
    if (msg.images && msg.images.length > 0) {
      return {
        role: msg.role,
        content: [
          { type: 'text' as const, text: msg.content },
          ...msg.images.map((img) => ({
            type: 'image_url' as const,
            image_url: { url: `data:${img.mimeType};base64,${img.base64}` },
          })),
        ],
      };
    }
    return { role: msg.role, content: msg.content };
  });
  ```
- In `normalizeAndPrepare` (line 143): Change `provider: 'claude' | 'gemini'` to `provider: 'kimi'`
- Remove or update JSDoc comments that mention "Claude" or "Gemini":
  - Line 38: `Critical for Claude compliance` -> `Prevents consecutive same-role messages`
  - Line 67-68: `Validate strict user/assistant alternation (required by Claude)` -> `Validate strict user/assistant alternation`
  - Line 93-94: Update JSDoc comment to remove "For Claude" and "Claude compliance"
  </action>
  <verify>
1. `ls nexus/packages/core/src/providers/claude.ts 2>/dev/null && echo EXISTS || echo DELETED` returns DELETED
2. `ls nexus/packages/core/src/sdk-agent-runner.ts 2>/dev/null && echo EXISTS || echo DELETED` returns DELETED
3. `ls nexus/packages/core/src/providers/gemini.ts 2>/dev/null && echo EXISTS || echo DELETED` returns DELETED
4. `grep -c "anthropic-ai\|@google/generative-ai" nexus/packages/core/package.json` returns 0
5. `grep -c "ClaudeProvider\|GeminiProvider" nexus/packages/core/src/providers/index.ts` returns 0
6. `grep -c "KimiProvider" nexus/packages/core/src/providers/index.ts` returns at least 1
  </verify>
  <done>
claude.ts, gemini.ts, and sdk-agent-runner.ts are deleted. @anthropic-ai/sdk, @anthropic-ai/claude-agent-sdk, and @google/generative-ai removed from package.json. providers/index.ts exports KimiProvider. manager.ts uses only KimiProvider. types.ts has generic names. normalize.ts uses Kimi/OpenAI format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean all importing files and verify zero matches</name>
  <files>
    nexus/packages/core/src/api.ts
    nexus/packages/core/src/agent.ts
    nexus/packages/core/src/daemon.ts
    nexus/packages/core/src/brain.ts
    nexus/packages/core/src/ws-gateway.ts
    nexus/packages/core/src/skill-loader.ts
    nexus/packages/core/src/task-manager.ts
    nexus/packages/core/src/tool-registry.ts
    nexus/packages/core/src/multi-agent.ts
    nexus/packages/core/src/config/schema.ts
    nexus/packages/core/src/index.ts
    nexus/packages/core/src/infra/errors.ts
    nexus/packages/core/src/infra/backoff.ts
    nexus/packages/memory/src/index.ts
    nexus/setup.ts
    livos/packages/ui/src/routes/settings/ai-config.tsx
    livos/packages/ui/src/routes/settings/_components/settings-content.tsx
    livos/packages/ui/src/routes/settings/_components/ai-config-dialog.tsx
    livos/packages/ui/src/routes/settings/usage-dashboard.tsx
    livos/packages/livinityd/source/modules/ai/routes.ts
    livos/.env.example
  </files>
  <action>
This task cleans every remaining file that references Claude, Anthropic, or Gemini. Read each file first, then make targeted edits. The changes are organized by file.

**NEXUS BACKEND FILES:**

**nexus/packages/core/src/api.ts:**
- Remove `import { SdkAgentRunner } from './sdk-agent-runner.js'` (line 13)
- Remove `import { ClaudeProvider } from './providers/claude.js'` (line 14)
- Delete the entire Claude CLI routes block (~lines 184-246): `GET /api/claude-cli/status`, `POST /api/claude-cli/login`, `POST /api/claude-cli/login-code`, `POST /api/claude-cli/logout`. Phase 2 already added Kimi routes in the same file, so these Claude routes are dead code.
- At ~line 1613-1614 there are references to `claudeProvider` and `getAuthMethod()` for determining agent runner mode. Replace this logic: instead of checking ClaudeProvider auth method, check if Kimi is configured (the KimiAgentRunner from Phase 3 handles this). Replace with:
  ```typescript
  const authMethod = 'api-key'; // Kimi uses API key auth only
  ```
  Or remove the auth method check entirely if it was only gating SdkAgentRunner vs AgentLoop -- since SdkAgentRunner is deleted, only the AgentLoop (or KimiAgentRunner from Phase 3) path should remain.

**nexus/packages/core/src/agent.ts:**
- Change `import type { ClaudeToolDefinition, ... }` to `import type { ToolDefinition, ... }` (rename the import)
- Rename all uses of `ClaudeToolDefinition` to `ToolDefinition` throughout the file (~lines 20-23)
- Replace variable names `claudeTools` with `nativeTools` and `claudeMessages` with `providerMessages` for clarity
- Replace `rawClaudeMessages` with `rawProviderMessages` in the interface and all references
- Update the system prompt variable name `CLAUDE_NATIVE_SYSTEM_PROMPT` to `NATIVE_SYSTEM_PROMPT`
- Update the comment on line 456 `// === CLAUDE NATIVE TOOL CALLING MODE ===` to `// === NATIVE TOOL CALLING MODE ===`
- Update the string comparison `activeProvider === 'claude'` to `activeProvider === 'kimi'` (line 365) so native tools activate for Kimi
- Update the conditional at line 389-394 that builds system prompts: the "useNativeTools" branch should still work (Kimi supports native tools via Phase 1)

**nexus/packages/core/src/daemon.ts:**
- Remove `import { ClaudeProvider } from './providers/claude.js'` (line 11)
- At ~lines 1081-1082, replace the ClaudeProvider auth method check. This was used to decide between SdkAgentRunner and AgentLoop. Since SdkAgentRunner is deleted, simplify to always use the Kimi path (KimiAgentRunner from Phase 3, or AgentLoop). Replace:
  ```typescript
  const claudeProvider = this.config.brain.getProviderManager().getProvider('claude') as ClaudeProvider | undefined;
  const authMethod = claudeProvider ? await claudeProvider.getAuthMethod() : 'api-key';
  ```
  With:
  ```typescript
  const authMethod = 'api-key'; // Kimi uses API key auth
  ```
- At ~lines 3061-3062, same pattern -- replace with `const authMethod = 'api-key';` (or whatever Phase 3 established as the agent runner selection logic)

**nexus/packages/core/src/brain.ts:**
- Check for any direct Claude/Gemini references. The brain.ts mostly uses ProviderManager which is already updated. Fix any remaining string references.

**nexus/packages/core/src/ws-gateway.ts:**
- Remove `import { ClaudeProvider } from './providers/claude.js'` (line 21)
- At ~lines 475-476, replace the ClaudeProvider auth method check with `const authMethod = 'api-key';`

**nexus/packages/core/src/skill-loader.ts:**
- Remove `import { ClaudeProvider } from './providers/claude.js'` (line 8)
- At ~lines 259-260, replace the ClaudeProvider auth method check with `const authMethod = 'api-key';`

**nexus/packages/core/src/task-manager.ts:**
- Remove `import { ClaudeProvider } from './providers/claude.js'` (line 17)
- At ~lines 310-311, replace the ClaudeProvider auth method check with `const authMethod = 'api-key';`

**nexus/packages/core/src/tool-registry.ts:**
- Change `import type { ClaudeToolDefinition }` to `import type { ToolDefinition }` (line 3)
- Rename method `toClaudeTools()` to `toToolDefinitions()` (line 273)
- Rename method `toClaudeToolsFiltered()` to `toToolDefinitionsFiltered()` (line 278)
- Rename private method `toolToClaudeDefinition()` to `toolToDefinition()` (line 282)
- Update JSDoc comments on these methods to remove "Claude" references
- Note: agent.ts calls `toClaudeToolsFiltered()` -- update that call to `toToolDefinitionsFiltered()` as well

**nexus/packages/core/src/multi-agent.ts:**
- Remove `import { SdkAgentRunner } from './sdk-agent-runner.js'` (line 3)
- At ~lines 317-362 where it creates `new SdkAgentRunner(...)`: Replace with the Kimi agent runner (KimiAgentRunner from Phase 3). Check what Phase 3 created and use that. If KimiAgentRunner doesn't exist yet, use the AgentLoop instead as a fallback and add a TODO comment.

**nexus/packages/core/src/config/schema.ts:**
- Replace all Claude model ID defaults with Kimi model IDs. The current defaults are:
  - `default: 'claude-haiku-4-5'` -> use Kimi equivalent (check what Phase 1 KimiProvider uses, likely `kimi-k2.5-flash` or similar)
  - `flash: 'claude-haiku-4-5'` -> Kimi flash model
  - `haiku: 'claude-haiku-4-5'` -> Kimi flash model
  - `sonnet: 'claude-sonnet-4-5'` -> Kimi balanced model
  - `opus: 'claude-opus-4-6'` -> Kimi powerful model
- Check Phase 1 plan (01-01-PLAN.md) or KimiProvider source for the actual Kimi model IDs. Use those.

**nexus/packages/core/src/index.ts:**
- Line 42 has a comment about Gemini stream parse errors. Update the comment to be generic: "Prevent unhandled errors from crashing the process (e.g. stream parse errors)"

**nexus/packages/core/src/infra/errors.ts:**
- Line 247 mentions "Direct retry delay property (Gemini SDK style)". Update comment to be generic.

**nexus/packages/core/src/infra/backoff.ts:**
- Line 23 mentions "Optimized for rate-limited APIs (like Gemini)". Update to "Optimized for rate-limited APIs"

**nexus/packages/memory/src/index.ts:**
- Line 91 references `process.env.GEMINI_API_KEY`. This is the memory service checking for API key availability. Replace with `process.env.KIMI_API_KEY` (or whatever env var Phase 1/2 established for Kimi).

**nexus/setup.ts:**
- Lines 169-170: Replace `GEMINI_API_KEY` prompt with `KIMI_API_KEY`
- Line 319: Replace `GEMINI_API_KEY=${config.GEMINI_API_KEY || ''}` with `KIMI_API_KEY=${config.KIMI_API_KEY || ''}`

**LIVOS UI FILES:**

**livos/packages/ui/src/routes/settings/ai-config.tsx:**
- This entire file is the AI Configuration settings page. It currently has Claude subscription UI, Anthropic API key input, and Gemini fallback section.
- Replace all Claude/Anthropic/Gemini references with Kimi equivalents:
  - Remove `anthropicKey`/`setAnthropicKey` state, `geminiKey`/`setGeminiKey` state
  - Add `kimiKey`/`setKimiKey` state
  - Replace all tRPC calls: `getClaudeCliStatus` -> `getKimiStatus`, `setClaudeAuthMethod` -> remove, `startClaudeLogin` -> remove, `submitClaudeLoginCode` -> remove, `claudeLogout` -> `kimiLogout`
  - Replace `configQ.data?.hasAnthropicKey`/`anthropicApiKey` with `configQ.data?.hasKimiKey`/`kimiApiKey`
  - Replace `configQ.data?.hasGeminiKey`/`geminiApiKey` -> remove entirely
  - Page title: "AI Configuration" -> "AI Configuration" (keep), description: "Configure how LivOS connects to Claude and Gemini" -> "Configure how LivOS connects to Kimi AI"
  - Remove entire "Claude Subscription" section (device auth UI)
  - Remove entire "Gemini (Fallback)" section
  - Replace Anthropic API key section with Kimi API key section (single key input)
  - Update the link from `https://console.anthropic.com/settings/keys` to `https://kimi.com`

**livos/packages/ui/src/routes/settings/_components/settings-content.tsx:**
- Line 148: Change `description: 'Claude subscription'` to `description: 'Kimi AI configuration'`
- Lines 744-877: This is the inline Claude auth UI in settings. It references `getClaudeCliStatus`, `setClaudeAuthMethod`, `startClaudeLogin`, `submitClaudeLoginCode`, `claudeLogout`. Replace all with Kimi equivalents:
  - Replace Claude subscription UI block with a simpler Kimi connection status indicator
  - Remove the device auth login flow (sign in with Claude button, auth code input)
  - Show Kimi connection status from `ai.getKimiStatus` instead

**livos/packages/ui/src/routes/settings/_components/ai-config-dialog.tsx:**
- Lines 34, 43, 52: References `geminiApiKey`. Replace with `kimiApiKey` equivalent
- Update the dialog text from "Gemini API key" to "Kimi API key"
- Update `configQ.data?.hasGeminiKey` and `configQ.data?.geminiApiKey` references

**livos/packages/ui/src/routes/settings/usage-dashboard.tsx:**
- Line 202: Comment mentions "Claude Code". Update to "Kimi" or make generic: "Note: Cost estimates assume per-API pricing. Subscription plans may have different billing."

**TRPC ROUTES:**

**livos/packages/livinityd/source/modules/ai/routes.ts:**
- Delete the 5 Claude-specific tRPC procedures (~lines 177-310):
  - `getClaudeCliStatus` (line 180)
  - `startClaudeLogin` (line 202)
  - `submitClaudeLoginCode` (line 235)
  - `claudeLogout` (line 266)
  - `setClaudeAuthMethod` (line 295)
- Phase 2 should have already added Kimi equivalents (`getKimiStatus`, `kimiLogin`, `kimiLogout`). If they exist, just delete the Claude ones. If they don't exist yet, also add Kimi replacements.
- In the `setConfig` and `getConfig` procedures, replace `anthropicApiKey`/`geminiApiKey` field handling with `kimiApiKey` handling
- Replace Redis key `nexus:config:anthropic_api_key` with `nexus:config:kimi_api_key`
- Replace Redis key `nexus:config:claude_auth_method` with removal (Kimi doesn't need auth method selection)

**ENV FILES:**

**livos/.env.example:**
- Remove `GEMINI_API_KEY=` line (line 13)
- Remove `ANTHROPIC_API_KEY=` line (line 15)
- Remove the comment `# Get from: https://console.anthropic.com/settings/keys` (line 14)
- Update `# Get from: https://aistudio.google.com/app/apikey` (line 12) to reference Kimi
- Add `KIMI_API_KEY=` with appropriate comment
- Replace `DEFAULT_MODEL=gemini-2.0-flash` (line 45) with appropriate Kimi model default

Note: Do NOT modify `livos/.env` (the actual env file with real secrets). That requires manual production updates (CLEAN-06 on production server).

**FINAL VERIFICATION (CLEAN-07):**

After all changes, run the comprehensive grep:
```bash
grep -ri "claude\|anthropic\|gemini" --include="*.ts" --include="*.tsx" nexus/packages/core/src/ livos/packages/ui/src/ livos/packages/livinityd/source/ | grep -v node_modules | grep -v '.planning/' | grep -v 'clauderic'
```

This MUST return zero matches. If any remain, fix them.

Then verify TypeScript compilation:
```bash
cd nexus/packages/core && npx tsc --noEmit
```
  </action>
  <verify>
1. `grep -ri "claude\|anthropic" --include="*.ts" --include="*.tsx" nexus/packages/core/src/ livos/packages/ui/src/ livos/packages/livinityd/source/ | grep -v node_modules | grep -v '.planning/' | grep -v 'clauderic' | wc -l` returns 0
2. `grep -ri "gemini\|@google/generative-ai" --include="*.ts" --include="*.tsx" nexus/packages/core/src/ livos/packages/ui/src/ livos/packages/livinityd/source/ | grep -v node_modules | grep -v '.planning/' | wc -l` returns 0
3. `grep -c "@anthropic-ai" nexus/packages/core/package.json` returns 0
4. `grep -c "@google/generative-ai" nexus/packages/core/package.json` returns 0
5. `ls nexus/packages/core/src/providers/claude.ts nexus/packages/core/src/sdk-agent-runner.ts nexus/packages/core/src/providers/gemini.ts 2>/dev/null | wc -l` returns 0
6. TypeScript compiles: `cd nexus/packages/core && npx tsc --noEmit` exits 0
  </verify>
  <done>
Zero Claude/Anthropic/Gemini references remain in active TypeScript/TSX source files. @anthropic-ai/sdk, @anthropic-ai/claude-agent-sdk, and @google/generative-ai absent from package.json. ClaudeProvider, SdkAgentRunner, and GeminiProvider files deleted. All imports, types, config defaults, tRPC routes, settings UI, and .env.example updated to reference Kimi only. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. Comprehensive grep for claude/anthropic/gemini in all *.ts and *.tsx files returns zero matches (excluding dnd-kit 'clauderic' author reference in comments)
2. `package.json` contains no @anthropic-ai or @google/generative-ai packages
3. Deleted files (claude.ts, gemini.ts, sdk-agent-runner.ts) do not exist on disk
4. TypeScript compilation succeeds
5. providers/manager.ts references only KimiProvider
6. providers/index.ts exports only KimiProvider (not Claude/Gemini)
7. All tRPC procedures reference Kimi endpoints (not Claude)
8. .env.example contains KIMI_API_KEY, not ANTHROPIC_API_KEY or GEMINI_API_KEY
</verification>

<success_criteria>
- `grep -ri "claude\|anthropic" --include="*.ts" --include="*.tsx"` across all source dirs returns 0 matches (excluding 'clauderic' in dnd-kit comment)
- `grep -ri "gemini\|@google/generative-ai" --include="*.ts" --include="*.tsx"` across all source dirs returns 0 matches
- @anthropic-ai/sdk and @anthropic-ai/claude-agent-sdk absent from all package.json files
- @google/generative-ai absent from all package.json files
- claude.ts, gemini.ts, sdk-agent-runner.ts deleted
- TypeScript compiles with zero errors
- Settings UI shows Kimi configuration only
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-cleanup/04-02-SUMMARY.md`
</output>
