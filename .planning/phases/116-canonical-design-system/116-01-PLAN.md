---
phase: 116-canonical-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/design-tokens/package.json
  - livos/packages/design-tokens/tokens.css
  - livos/packages/design-tokens/tailwind.preset.cjs
  - livos/packages/design-tokens/theme.json
  - livos/packages/design-tokens/DESIGN-SYSTEM.md
  - livos/packages/design-tokens/STYLE-GUIDE.md
  - livos/packages/design-tokens/README.md
  - livos/packages/design-tokens/.gitignore
autonomous: true
requirements: []
tags:
  - design-system
  - tokens
  - tailwind
  - css-variables
  - v35
must_haves:
  truths:
    - "The `@livinity/design-tokens` package exists at `livos/packages/design-tokens/` and is recognized by pnpm workspace"
    - "tokens.css contains every CSS variable from dashboard.html lines 27-80 verbatim, in :root, body.dark, body.iridescent blocks"
    - "tailwind.preset.cjs is a valid CommonJS module exporting a Tailwind 3.4-compatible preset that maps every token"
    - "theme.json is valid JSON with the same tokens, addressable by tooling"
    - "DESIGN-SYSTEM.md documents typography, color, spacing, radius, shadow, motion, accessibility, interaction patterns"
    - "No files outside livos/packages/design-tokens/ are modified (D-116-NO-CONSUMER-CHANGES)"
    - "Token values map EXACTLY to canonical dashboard.html (D-116-LOCK-CANONICAL) — verified via SSH cat + value comparison"
  artifacts:
    - path: "livos/packages/design-tokens/package.json"
      provides: "npm package metadata, exports map"
      contains: '"name": "@livinity/design-tokens"'
    - path: "livos/packages/design-tokens/tokens.css"
      provides: "Canonical CSS variables in :root + body.dark + body.iridescent"
      contains: "--dash-pad: 28px"
    - path: "livos/packages/design-tokens/tailwind.preset.cjs"
      provides: "Tailwind preset mapping tokens to theme.colors/spacing/borderRadius/fontFamily"
      contains: "module.exports"
    - path: "livos/packages/design-tokens/theme.json"
      provides: "JSON token manifest for tooling"
      contains: '"dashPad"'
    - path: "livos/packages/design-tokens/DESIGN-SYSTEM.md"
      provides: "Long-form spec for typography, colors, spacing, radius, shadow, motion, a11y, interaction"
      contains: "# LivOS Design System"
    - path: "livos/packages/design-tokens/STYLE-GUIDE.md"
      provides: "Skeleton for Phase 121 — How to add a new component"
      contains: "# Style Guide"
  key_links:
    - from: "livos/packages/design-tokens/tailwind.preset.cjs"
      to: "livos/packages/design-tokens/tokens.css"
      via: "Same source-of-truth values — Tailwind preset references the same hex/rgb/px values as the CSS file"
      pattern: "#2563eb"
    - from: "livos/packages/design-tokens/theme.json"
      to: "livos/packages/design-tokens/tokens.css"
      via: "Mirrors token values in JSON for tooling"
      pattern: '"#2563eb"'
    - from: "livos/packages/design-tokens/package.json"
      to: "pnpm-workspace.yaml"
      via: "Package discovered by livos/ pnpm workspace via `livos/packages/*` glob"
      pattern: "design-tokens"
---

<objective>
Create the standalone `@livinity/design-tokens` package at `livos/packages/design-tokens/` containing the canonical design system tokens, Tailwind preset, JSON manifest, and long-form specification documentation. This package will be the single source of truth for the dashboard.html aesthetic across every LivOS surface (UI, livinityd, landing, Server5 platform). Per D-116-NO-CONSUMER-CHANGES, NO consumers are migrated in this phase — the package ships standalone.

Purpose: Codify dashboard.html's shipped values (snapshot 2026-05-14) as a portable, version-controlled spec. After this plan ships, "what color is the primary button?" has exactly one answer in exactly one file. This unblocks Phase 117 (UI migration), Phase 118 (landing migration), Phase 119 (component library), Phase 120 (Server5 platform migration), and Phase 121 (Style guide expansion).

Output: 8 files in `livos/packages/design-tokens/` — package.json, tokens.css, tailwind.preset.cjs, theme.json, DESIGN-SYSTEM.md, STYLE-GUIDE.md (skeleton), README.md, .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/116-canonical-design-system/116-CONTEXT.md
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md
@.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md

<interfaces>
<!-- The canonical CSS variable block from /opt/landing/livinity.io/dashboard.html lines 27-80 -->
<!-- Light mode (`:root`): -->
```css
:root {
  --dash-pad: 28px;
  --dash-radius: 18px;
  --dash-line: rgba(0,0,0,0.07);
  --dash-line-strong: rgba(0,0,0,0.12);
  --card-bg: #ffffff;
  --card-bg-2: #fafafa;
  --card-shadow: 0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18);
  --hero-grad: linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%);
  --accent-blue:  #2563eb;
  --accent-green: #16a34a;
  --accent-amber: #d97706;
  --accent-red:   #dc2626;
  --font-mono:  "Geist Mono", ui-monospace, monospace;
  --font-serif: "Instrument Serif", serif;
}
```
<!-- Dark + iridescent variants MUST be fetched live from Server5 via SSH (see Task 1 read_first). -->

<!-- pnpm-workspace.yaml glob (livos/pnpm-workspace.yaml expected pattern): -->
```yaml
packages:
  - 'packages/*'
```
<!-- This means a new package at livos/packages/design-tokens/ is automatically picked up. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch canonical dark+iridescent token blocks from Server5 and create package skeleton</name>
  <files>
    - livos/packages/design-tokens/package.json
    - livos/packages/design-tokens/.gitignore
    - livos/packages/design-tokens/README.md
    - .planning/phases/116-canonical-design-system/116-01-dashboard-snapshot.txt  (working artifact — record the SSH-fetched canonical block for grep verification later)
  </files>
  <read_first>
    - .planning/phases/116-canonical-design-system/116-CONTEXT.md  (locked decisions D-116-LOCK-CANONICAL, D-116-NEW-PACKAGE-IN-LIVOS)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md  (lines 30-100 — canonical reference excerpt)
    - livos/pnpm-workspace.yaml  (confirm `packages/*` glob — package will be auto-discovered)
    - livos/packages/ui/package.json  (read for monorepo conventions — same `engines`, `private` fields, license style, author block)
  </read_first>
  <action>
    Step A — Fetch canonical dashboard.html token block from Server5 (read-only, never edited):

    Run via Bash tool:
    ```
    /c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@45.137.194.102 "sed -n '20,150p' /opt/landing/livinity.io/dashboard.html"
    ```

    Write the full SSH output to `.planning/phases/116-canonical-design-system/116-01-dashboard-snapshot.txt` exactly as returned. This is the source-of-truth artifact for Task 2's grep verification. It captures `:root`, `body.dark`, and `body.iridescent` blocks verbatim. DO NOT edit the snapshot — it is the canonical reference per D-116-LOCK-CANONICAL.

    Step B — Create `livos/packages/design-tokens/package.json` with EXACTLY this structure (per D-116-NEW-PACKAGE-IN-LIVOS):

    ```json
    {
      "name": "@livinity/design-tokens",
      "version": "1.0.0",
      "description": "Canonical LivOS design system — CSS variables, Tailwind preset, JSON token manifest. Single source of truth for the dashboard.html aesthetic.",
      "license": "MIT",
      "private": false,
      "type": "module",
      "main": "./theme.json",
      "exports": {
        ".": {
          "import": "./theme.json",
          "require": "./theme.json",
          "default": "./theme.json"
        },
        "./tokens.css": "./tokens.css",
        "./fonts.css": "./fonts.css",
        "./tailwind.preset": "./tailwind.preset.cjs",
        "./tailwind.preset.cjs": "./tailwind.preset.cjs",
        "./theme.json": "./theme.json",
        "./package.json": "./package.json"
      },
      "files": [
        "tokens.css",
        "fonts.css",
        "tailwind.preset.cjs",
        "theme.json",
        "fonts/",
        "DESIGN-SYSTEM.md",
        "STYLE-GUIDE.md",
        "LICENSE-FONTS.md",
        "README.md"
      ],
      "keywords": [
        "livos",
        "design-system",
        "design-tokens",
        "tailwind",
        "css-variables"
      ],
      "author": "Livinity",
      "publishConfig": {
        "access": "public"
      },
      "engines": {
        "node": ">=18"
      }
    }
    ```

    Note: `fonts.css`, `fonts/`, `LICENSE-FONTS.md` are intentionally declared in `files` and `exports` ahead of Plan 116-02 — the manifest is forward-compatible with the next wave. Per D-116-SELF-HOSTED-FONT-FALLBACK these will be filled in by 116-02.

    Step C — Create `.gitignore`:
    ```
    node_modules/
    dist/
    *.log
    .DS_Store
    ```

    Step D — Create `README.md` (short — full docs go in DESIGN-SYSTEM.md):
    ```markdown
    # @livinity/design-tokens

    Canonical LivOS design system tokens. Single source of truth for the dashboard.html aesthetic.

    **Snapshot:** 2026-05-14 (Phase 116, v35.0 milestone)

    ## Install

    Inside the `livos/` monorepo (already linked via pnpm workspace):
    ```ts
    // tokens.css — import in any HTML/CSS bundle
    @import "@livinity/design-tokens/tokens.css";
    @import "@livinity/design-tokens/fonts.css";

    // tailwind.preset.cjs — extend any Tailwind config
    module.exports = {
      presets: [require("@livinity/design-tokens/tailwind.preset.cjs")],
      // ...
    };

    // theme.json — read from any tooling
    import tokens from "@livinity/design-tokens";
    ```

    ## Files

    - `tokens.css` — CSS custom properties for `:root`, `body.dark`, `body.iridescent`
    - `tailwind.preset.cjs` — Tailwind 3.4 preset (4.x variant noted in DESIGN-SYSTEM.md)
    - `theme.json` — JSON manifest for tooling
    - `fonts.css` — `@font-face` declarations (Geist, Geist Mono, Instrument Serif)
    - `fonts/` — Self-hosted `.woff2` files (offline fallback)
    - `DESIGN-SYSTEM.md` — Long-form spec
    - `STYLE-GUIDE.md` — Component contribution guide (skeleton, expanded in Phase 121)
    - `LICENSE-FONTS.md` — Font license attribution

    ## Canonical reference

    `/opt/landing/livinity.io/dashboard.html` on Server5 (read-only). Drift from this file is a bug per D-116-LOCK-CANONICAL.
    ```
  </action>
  <verify>
    <automated>
      cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && test -f livos/packages/design-tokens/package.json && test -f livos/packages/design-tokens/README.md && test -f livos/packages/design-tokens/.gitignore && test -f .planning/phases/116-canonical-design-system/116-01-dashboard-snapshot.txt && node -e "const p=require('./livos/packages/design-tokens/package.json'); if(p.name!=='@livinity/design-tokens')process.exit(1); if(p.version!=='1.0.0')process.exit(1); if(!p.exports['./tokens.css'])process.exit(1); if(!p.exports['./tailwind.preset.cjs'])process.exit(1); console.log('package.json OK');" && grep -E ':root|body\.dark|body\.iridescent' .planning/phases/116-canonical-design-system/116-01-dashboard-snapshot.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `livos/packages/design-tokens/package.json` exists; `grep -c '"name": "@livinity/design-tokens"'` returns 1
    - `package.json` parses as valid JSON (node -e require check passes)
    - `package.json` `exports` map contains `./tokens.css`, `./tailwind.preset.cjs`, `./theme.json`, `./fonts.css`
    - `116-01-dashboard-snapshot.txt` exists and contains lines matching `:root`, `body.dark`, `body.iridescent`
    - `README.md` and `.gitignore` exist
    - `cd livos && pnpm -r list 2>&1 | grep -q design-tokens` returns success (pnpm sees the package)
  </acceptance_criteria>
  <done>
    Package skeleton committed. Canonical snapshot captured. pnpm workspace recognizes the new package. Ready for Task 2 to populate tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author tokens.css, tailwind.preset.cjs, theme.json — exact canonical values</name>
  <files>
    - livos/packages/design-tokens/tokens.css
    - livos/packages/design-tokens/tailwind.preset.cjs
    - livos/packages/design-tokens/theme.json
  </files>
  <read_first>
    - .planning/phases/116-canonical-design-system/116-01-dashboard-snapshot.txt  (canonical reference captured in Task 1 — body.dark + body.iridescent blocks are HERE, not in the master plan excerpt)
    - .planning/phases/116-canonical-design-system/116-CONTEXT.md  (D-116-LOCK-CANONICAL — drift = bug)
    - livos/packages/ui/tailwind.config.cjs  (if exists — read existing Tailwind 3.4 shape for compatibility) OR livos/packages/ui/tailwind.config.js
  </read_first>
  <action>
    Step A — Author `livos/packages/design-tokens/tokens.css`.

    Open the snapshot file from Task 1 and extract the EXACT contents of `:root { ... }`, `body.dark { ... }`, and `body.iridescent { ... }` blocks. Use them verbatim — DO NOT improvise values, DO NOT rename variables. Per D-116-LOCK-CANONICAL drift is a bug.

    The `:root` block MUST be exactly (from master plan excerpt, confirmed canonical):
    ```css
    :root {
      --dash-pad: 28px;
      --dash-radius: 18px;
      --dash-line: rgba(0,0,0,0.07);
      --dash-line-strong: rgba(0,0,0,0.12);
      --card-bg: #ffffff;
      --card-bg-2: #fafafa;
      --card-shadow: 0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18);
      --hero-grad: linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%);
      --accent-blue:  #2563eb;
      --accent-green: #16a34a;
      --accent-amber: #d97706;
      --accent-red:   #dc2626;
      --font-mono:  "Geist Mono", ui-monospace, monospace;
      --font-serif: "Instrument Serif", serif;
    }
    ```

    Then append `body.dark { ... }` and `body.iridescent { ... }` blocks AS THEY APPEAR in `116-01-dashboard-snapshot.txt`. If the snapshot has only stubs (`{ /* TODO */ }`) — that is the canonical state and you ship them stub-shaped too. If the snapshot has full overrides for any of the `--dash-*`, `--card-*`, `--hero-*`, `--accent-*` variables, include them verbatim.

    Prepend the file with a header comment:
    ```css
    /*!
     * @livinity/design-tokens — tokens.css
     * Canonical LivOS design tokens, snapshot 2026-05-14.
     * Source of truth: /opt/landing/livinity.io/dashboard.html (Server5).
     * D-116-LOCK-CANONICAL: Drift from canonical = bug.
     * Generated by Phase 116-01.
     */
    ```

    Step B — Author `livos/packages/design-tokens/tailwind.preset.cjs`.

    Map canonical tokens to Tailwind 3.4 theme shape. Use CommonJS (`.cjs`) so it's consumable without ESM in Tailwind configs. Exact contents:

    ```js
    /**
     * @livinity/design-tokens — Tailwind preset (3.4-compatible)
     *
     * Usage:
     *   // tailwind.config.cjs
     *   module.exports = {
     *     presets: [require("@livinity/design-tokens/tailwind.preset.cjs")],
     *     content: [...],
     *   };
     *
     * Tailwind 4 note: When migrating consumers to Tailwind 4 (CSS-first config),
     * import tokens.css directly and use @theme inline instead of a JS preset.
     * The token values in this preset MUST mirror tokens.css verbatim.
     */
    module.exports = {
      theme: {
        extend: {
          colors: {
            // Accent tokens — match --accent-* in tokens.css
            "accent-blue":  "#2563eb",
            "accent-green": "#16a34a",
            "accent-amber": "#d97706",
            "accent-red":   "#dc2626",
            // Surface tokens — match --card-bg / --card-bg-2 in tokens.css
            "card-bg":   "#ffffff",
            "card-bg-2": "#fafafa",
            // Line tokens — match --dash-line / --dash-line-strong in tokens.css
            "dash-line":        "rgba(0,0,0,0.07)",
            "dash-line-strong": "rgba(0,0,0,0.12)",
          },
          spacing: {
            // Match --dash-pad in tokens.css
            "dash": "28px",
          },
          borderRadius: {
            // Match --dash-radius in tokens.css
            "dash": "18px",
          },
          boxShadow: {
            // Match --card-shadow in tokens.css
            "card": "0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18)",
          },
          backgroundImage: {
            // Match --hero-grad in tokens.css
            "hero-grad": "linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%)",
          },
          fontFamily: {
            // Match --font-mono / --font-serif in tokens.css
            mono:  ["Geist Mono", "ui-monospace", "monospace"],
            serif: ["Instrument Serif", "serif"],
          },
          transitionDuration: {
            // dashboard.html ships "0.18s ease" for all transitions
            "dash": "180ms",
          },
        },
      },
    };
    ```

    Step C — Author `livos/packages/design-tokens/theme.json`.

    JSON manifest mirroring the same tokens — addressable by tooling (Storybook, Figma plugins, codegen). Use camelCase keys per JS convention:

    ```json
    {
      "$schema": "./theme.schema.json",
      "version": "1.0.0",
      "snapshot": "2026-05-14",
      "canonicalSource": "/opt/landing/livinity.io/dashboard.html",
      "themes": {
        "light": {
          "dashPad": "28px",
          "dashRadius": "18px",
          "dashLine": "rgba(0,0,0,0.07)",
          "dashLineStrong": "rgba(0,0,0,0.12)",
          "cardBg": "#ffffff",
          "cardBg2": "#fafafa",
          "cardShadow": "0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18)",
          "heroGrad": "linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%)",
          "accentBlue":  "#2563eb",
          "accentGreen": "#16a34a",
          "accentAmber": "#d97706",
          "accentRed":   "#dc2626",
          "fontMono":  "\"Geist Mono\", ui-monospace, monospace",
          "fontSerif": "\"Instrument Serif\", serif"
        },
        "dark": {
          "note": "Mirror body.dark overrides from tokens.css. Populate from 116-01-dashboard-snapshot.txt body.dark block."
        },
        "iridescent": {
          "note": "Mirror body.iridescent overrides from tokens.css. Populate from 116-01-dashboard-snapshot.txt body.iridescent block."
        }
      },
      "motion": {
        "transition": "0.18s ease",
        "transitionDurationMs": 180,
        "easing": "ease"
      }
    }
    ```

    For the `dark` and `iridescent` objects: open `116-01-dashboard-snapshot.txt`, extract the SAME variable names as `:root` if they're overridden in `body.dark` / `body.iridescent`, convert kebab-case `--dash-pad` to camelCase `dashPad`, and populate the JSON values verbatim. If the canonical block has no overrides for a variant, replace the `note` field with `{}` (empty object) and add a comment in DESIGN-SYSTEM.md (Task 3) noting the variant inherits from light.

    Validation: After writing, `node -e "console.log(JSON.parse(require('fs').readFileSync('livos/packages/design-tokens/theme.json','utf8')).themes.light.accentBlue)"` MUST print `#2563eb`.
  </action>
  <verify>
    <automated>
      cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && test -f livos/packages/design-tokens/tokens.css && test -f livos/packages/design-tokens/tailwind.preset.cjs && test -f livos/packages/design-tokens/theme.json && grep -q "\-\-dash-pad: 28px" livos/packages/design-tokens/tokens.css && grep -q "\-\-accent-blue:  #2563eb" livos/packages/design-tokens/tokens.css && grep -q "body.dark" livos/packages/design-tokens/tokens.css && grep -q "body.iridescent" livos/packages/design-tokens/tokens.css && node -e "const p=require('./livos/packages/design-tokens/tailwind.preset.cjs'); if(p.theme.extend.colors['accent-blue']!=='#2563eb')process.exit(1); if(p.theme.extend.borderRadius.dash!=='18px')process.exit(1); console.log('preset OK');" && node -e "const t=JSON.parse(require('fs').readFileSync('livos/packages/design-tokens/theme.json','utf8')); if(t.themes.light.accentBlue!=='#2563eb')process.exit(1); if(t.themes.light.dashPad!=='28px')process.exit(1); if(t.motion.transition!=='0.18s ease')process.exit(1); console.log('theme.json OK');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `tokens.css` contains literal `--dash-pad: 28px`
    - `tokens.css` contains literal `--dash-radius: 18px`
    - `tokens.css` contains literal `--accent-blue:  #2563eb` (note the double-space matching dashboard.html canonical alignment)
    - `tokens.css` contains literal `--accent-green: #16a34a`, `--accent-amber: #d97706`, `--accent-red:   #dc2626`
    - `tokens.css` contains literal `--card-shadow: 0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18)`
    - `tokens.css` contains literal `--hero-grad: linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%)`
    - `tokens.css` contains literal `--font-mono:  "Geist Mono", ui-monospace, monospace`
    - `tokens.css` contains literal `--font-serif: "Instrument Serif", serif`
    - `tokens.css` contains `:root`, `body.dark`, `body.iridescent` selectors
    - `tailwind.preset.cjs` is a valid CommonJS module (require returns object with `.theme.extend`)
    - `tailwind.preset.cjs` `theme.extend.colors['accent-blue']` === `'#2563eb'`
    - `theme.json` is valid JSON; `themes.light.accentBlue` === `'#2563eb'`
    - `theme.json` `motion.transition` === `'0.18s ease'`
    - Diff between `tokens.css` `:root` block and canonical `116-01-dashboard-snapshot.txt` `:root` block has zero non-whitespace differences (manual diff or grep -c)
  </acceptance_criteria>
  <done>
    Canonical tokens authored in 3 formats (CSS, Tailwind preset, JSON manifest). Each format mirrors the same values. Drift would fail Task 4 verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author DESIGN-SYSTEM.md + STYLE-GUIDE.md skeleton, run package verification</name>
  <files>
    - livos/packages/design-tokens/DESIGN-SYSTEM.md
    - livos/packages/design-tokens/STYLE-GUIDE.md
  </files>
  <read_first>
    - livos/packages/design-tokens/tokens.css  (read what Task 2 produced — DESIGN-SYSTEM.md documents these exact tokens)
    - livos/packages/design-tokens/theme.json  (cross-reference token names)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md  (lines 60-100 — motion, theme switching, body class toggles)
    - .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md  (existing primitives — DESIGN-SYSTEM.md references the 13 primary primitives for context)
  </read_first>
  <action>
    Step A — Author `livos/packages/design-tokens/DESIGN-SYSTEM.md`.

    Long-form spec. The structure MUST contain these top-level sections (each `## H2`):
    1. Overview — what this package is, snapshot date, canonical source
    2. Typography — fonts (Geist, Geist Mono, Instrument Serif), font-family tokens, when to use serif vs mono vs sans
    3. Color Tokens — three sub-sections: Light, Dark, Iridescent — each lists every variable with hex/rgba value and a one-line use case
    4. Spacing — `--dash-pad: 28px` is the canonical card padding; document the inline pixel literals seen in dashboard.html (8/16/24/28) and note that we ship `dash` as the named scale token, with raw px allowed for now (per CONTEXT.md open question — codify as documented intent)
    5. Radius — `--dash-radius: 18px` is the canonical card corner radius
    6. Shadow — `--card-shadow` is the canonical card elevation
    7. Motion — all transitions `0.18s ease`; document `transitionDuration.dash` (180ms) in Tailwind preset
    8. Accessibility — focus rings, contrast targets (note: dashboard.html does NOT ship explicit a11y tokens; document the contrast ratios of `--accent-*` against `--card-bg` and note focus-ring conventions to follow)
    9. Interaction Patterns — hover lift, theme switching (`body.dark` / `body.iridescent` toggles, `localStorage.getItem("liv_theme")`), transition timing
    10. Tailwind Integration — show example consumer config using the preset
    11. Tailwind 4 Migration Note — when consumers migrate to Tailwind 4, switch to `@theme inline` in `tokens.css`-style CSS-first config; the preset will be deprecated
    12. Canonical Reference — link to `/opt/landing/livinity.io/dashboard.html` (read-only), note D-116-LOCK-CANONICAL
    13. Changelog — `## 1.0.0 — 2026-05-14 — Initial release (Phase 116-01)`

    Each color token MUST be listed in a table:
    ```markdown
    | Token | Value | Use |
    |---|---|---|
    | `--accent-blue` | `#2563eb` | Primary action, links, focus |
    | `--accent-green` | `#16a34a` | Success states |
    | `--accent-amber` | `#d97706` | Warning, attention |
    | `--accent-red` | `#dc2626` | Errors, destructive actions |
    | `--card-bg` | `#ffffff` | Card surface (light) |
    | `--card-bg-2` | `#fafafa` | Card surface alt (light) |
    | `--dash-line` | `rgba(0,0,0,0.07)` | Subtle dividers |
    | `--dash-line-strong` | `rgba(0,0,0,0.12)` | Stronger dividers |
    ```

    For the Dark and Iridescent sub-sections: if `116-01-dashboard-snapshot.txt` has overrides, document them. If they're empty/stub in canonical, write: "Variant inherits from light — canonical dashboard.html ships stub `body.dark { }` / `body.iridescent { }` blocks at snapshot date. Future v1.1 may populate."

    For Motion section, include the exact body class toggle JS pattern as seen in dashboard.html (`localStorage.getItem("liv_theme")`).

    Step B — Author `livos/packages/design-tokens/STYLE-GUIDE.md` (skeleton — full content arrives in Phase 121):

    ```markdown
    # LivOS Style Guide — How to Add a New Component

    > **Status:** Skeleton — full content arrives in Phase 121.

    > Until then, this file documents the contract every new component must follow.

    ## The contract

    Every LivOS UI component MUST:

    1. Consume tokens from `@livinity/design-tokens` — never hard-code color/spacing/radius/shadow values.
    2. Use `var(--token-name)` in CSS, or Tailwind classes that map to the preset (e.g. `bg-card-bg`, `rounded-dash`, `shadow-card`).
    3. Respect theme switching — never assume `:root` (light); test under `body.dark` and `body.iridescent`.
    4. Use the canonical font stack — `font-mono` for code/data, `font-serif` for editorial display, default sans for body.
    5. Honor the motion token — transitions are `0.18s ease` (`transition-dash-180` in Tailwind, or `transition: 0.18s ease`).

    ## Anti-patterns

    - Hard-coded hex values in component source — **drift = bug** per D-116-LOCK-CANONICAL.
    - Importing fonts directly from Google Fonts in component code — fonts come from `@livinity/design-tokens/fonts.css` only (single import point, self-host fallback works offline).
    - One-off shadow/radius values — if you need a new value, add a token to the package first.

    ## Workflow

    1. Identify the design need.
    2. Check `DESIGN-SYSTEM.md` — does a token already cover it?
    3. If yes: use the token.
    4. If no: propose a new token in `@livinity/design-tokens` first; ship it as a new minor version; then consume in your component.

    ## To be expanded in Phase 121

    - Component naming conventions
    - File layout (`src/components/{name}/{name}.tsx + {name}.test.tsx + {name}.stories.tsx`)
    - Story authoring guide
    - Accessibility checklist
    - Cross-theme screenshot policy
    ```

    Step C — Run final package verification:

    ```bash
    cd C:/Users/hello/Desktop/Projects/contabo/livinity-io
    # Verify package.json + workspace recognition
    node -e "require('./livos/packages/design-tokens/package.json')"
    node -e "require('./livos/packages/design-tokens/tailwind.preset.cjs')"
    node -e "JSON.parse(require('fs').readFileSync('./livos/packages/design-tokens/theme.json','utf8'))"
    # Verify tokens.css is syntactically valid CSS (presence of expected selectors and closing braces)
    grep -c "}" livos/packages/design-tokens/tokens.css
    # Verify pnpm workspace discovers it
    cd livos && pnpm -r list 2>&1 | grep design-tokens
    ```

    All commands must exit 0 and pnpm must list `@livinity/design-tokens`.

    Step D — Verify D-116-NO-CONSUMER-CHANGES. Run `git status --short` and confirm every new/modified file path starts with `livos/packages/design-tokens/` OR `.planning/phases/116-canonical-design-system/`. No paths under `livos/packages/ui/`, `livos/packages/livinityd/`, `livos/packages/config/`, `livos/packages/marketplace/`, `liv/`, `scripts/`, or `/opt/`. If any unexpected file appears, abort and revert.
  </action>
  <verify>
    <automated>
      cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && test -f livos/packages/design-tokens/DESIGN-SYSTEM.md && test -f livos/packages/design-tokens/STYLE-GUIDE.md && grep -q "^# LivOS Design System" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Typography" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Color Tokens" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Spacing" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Radius" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Shadow" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Motion" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Accessibility" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "## Interaction Patterns" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "#2563eb" livos/packages/design-tokens/DESIGN-SYSTEM.md && grep -q "^# LivOS Style Guide" livos/packages/design-tokens/STYLE-GUIDE.md && cd livos && pnpm -r list 2>&1 | grep -q design-tokens && echo "VERIFY OK" && cd .. && git status --short -- livos/packages/ liv/ scripts/ | grep -v "design-tokens" | grep -v "^$" | wc -l | grep -qE "^\s*0\s*$" && echo "D-116-NO-CONSUMER-CHANGES OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `DESIGN-SYSTEM.md` exists and contains H2 sections: Typography, Color Tokens, Spacing, Radius, Shadow, Motion, Accessibility, Interaction Patterns
    - `DESIGN-SYSTEM.md` contains the color token table with `#2563eb`, `#16a34a`, `#d97706`, `#dc2626`
    - `DESIGN-SYSTEM.md` documents the `0.18s ease` motion token
    - `DESIGN-SYSTEM.md` documents the `body.dark` / `body.iridescent` / `localStorage` theme switching pattern
    - `STYLE-GUIDE.md` exists with skeleton sections and notes Phase 121 expansion
    - `pnpm -r list` from `livos/` directory lists `@livinity/design-tokens`
    - `git status --short` shows ZERO modified/added files outside `livos/packages/design-tokens/` and `.planning/phases/116-canonical-design-system/` (D-116-NO-CONSUMER-CHANGES)
  </acceptance_criteria>
  <done>
    Long-form spec authored. Style guide skeleton in place for Phase 121 to expand. Package fully discoverable by pnpm. Consumer-change firewall verified clean. Plan 116-01 complete — ready for Plan 116-02 (fonts.css + .woff2 self-hosted bundle).
  </done>
</task>

</tasks>

<verification>
End-of-plan verification (run all):

```bash
cd C:/Users/hello/Desktop/Projects/contabo/livinity-io

# 1. All 8 files exist
for f in package.json tokens.css tailwind.preset.cjs theme.json DESIGN-SYSTEM.md STYLE-GUIDE.md README.md .gitignore; do
  test -f "livos/packages/design-tokens/$f" || { echo "MISSING: $f"; exit 1; }
done
echo "All 8 files present"

# 2. Token canonical match (spot check 4 critical values)
grep -q '\-\-dash-pad: 28px' livos/packages/design-tokens/tokens.css
grep -q '\-\-accent-blue:  #2563eb' livos/packages/design-tokens/tokens.css
grep -q '\-\-card-shadow: 0 1px 2px' livos/packages/design-tokens/tokens.css
grep -q '\-\-font-mono:  "Geist Mono"' livos/packages/design-tokens/tokens.css
echo "Canonical tokens match"

# 3. Modules load
node -e "require('./livos/packages/design-tokens/package.json'); require('./livos/packages/design-tokens/tailwind.preset.cjs'); JSON.parse(require('fs').readFileSync('./livos/packages/design-tokens/theme.json','utf8')); console.log('Modules OK');"

# 4. pnpm workspace recognition
cd livos && pnpm -r list 2>&1 | grep -q design-tokens && echo "pnpm OK"

# 5. D-116-NO-CONSUMER-CHANGES — no files outside design-tokens dir
cd C:/Users/hello/Desktop/Projects/contabo/livinity-io
CHANGED=$(git status --short | grep -E "^(\?\?|.M|M.|A.)" | awk '{print $2}' | grep -v "^livos/packages/design-tokens/" | grep -v "^\.planning/phases/116-canonical-design-system/" || true)
if [ -n "$CHANGED" ]; then echo "CONSUMER CHANGES DETECTED:"; echo "$CHANGED"; exit 1; fi
echo "D-116-NO-CONSUMER-CHANGES verified clean"
```
</verification>

<success_criteria>
- 8 files exist under `livos/packages/design-tokens/`
- All token values match `/opt/landing/livinity.io/dashboard.html` canonical snapshot verbatim
- `package.json` valid; `name = @livinity/design-tokens`; `version = 1.0.0`
- `tailwind.preset.cjs` is a valid CommonJS module
- `theme.json` is valid JSON
- `DESIGN-SYSTEM.md` has all 9 required H2 sections plus typography/changelog/canonical reference
- pnpm workspace discovers the package
- ZERO modifications outside the package directory (D-116-NO-CONSUMER-CHANGES)
</success_criteria>

<output>
After completion, create `.planning/phases/116-canonical-design-system/116-01-SUMMARY.md` with:
- Files created (paths + line counts)
- Canonical SSH-fetched snapshot hash/checksum (for Plan 116-02 to reference)
- Confirmation D-116-LOCK-CANONICAL upheld (diff against snapshot = zero non-whitespace)
- Confirmation D-116-NO-CONSUMER-CHANGES upheld (`git status` excerpt)
- Open follow-ups for 116-02 (fonts.css + .woff2 + smoke test + LICENSE-FONTS.md)
- Note: `package.json` `files` array and `exports` map already include `fonts.css`, `fonts/`, `LICENSE-FONTS.md` — Plan 116-02 just needs to author those files
</output>
