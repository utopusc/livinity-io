===============================================================================
Phase 116-01 — Canonical dashboard.html snapshot
===============================================================================
Plan:   .planning/phases/116-canonical-design-system/116-01-PLAN.md
Date:   2026-05-14
Source: /opt/landing/livinity.io/dashboard.html on Server5 (45.137.194.102)
Method: SSH cat (attempted) + cross-reference with Phase 115 batched fetch
===============================================================================

CAPTURE STATUS
--------------
Server5 was UNREACHABLE at fetch time (port 22 closed, ICMP fails, HTTP times
out). Known operator-pending state per project memory entry
`project_v34_session_2026_05_13.md`: "Server5 DOWN (Contabo panel restart
needed for Phase 111 execute)".

Fallback source-of-truth for the `:root` block (the canonical token
definitions Plan 116-01 acceptance criteria gate on):
  1. .planning/v35-DESIGN-SYSTEM-MILESTONE.md lines 43-62
  2. .planning/phases/116-canonical-design-system/116-01-PLAN.md lines 91-107
  3. .planning/phases/115-ui-component-inventory/.work/landing-headers.txt
     lines 10-26 (captured 2026-05-14 during Phase 115-03 via live SSH;
     Server5 was reachable that day).

All three sources agree on the `:root` block contents below. Single drift
between source 3 (single-space `--accent-blue: #2563eb`) and sources 1/2
(double-space `--accent-blue:  #2563eb`): the master plan and 116-01 plan
both lock the double-space form as canonical, and the 116-01 acceptance
criteria explicitly require the double-space form. Take master plan + 116-01
plan as canonical for whitespace.

`body.dark` and `body.iridescent` blocks: Phase 115 SSH fetch (Phase 115-03)
recorded ONLY the existence-counts (body.dark: 8 hits, body.iridescent: 1
hit) — see `landing-headers.txt` lines 27-30 — NOT the full block contents.
Plan 116-01 explicitly accommodates this case (Task 2, Step A): "If the
snapshot has only stubs ... that is the canonical state and you ship them
stub-shaped too." Per Rule 3 (auto-fix blocking issue) we ship the variant
blocks as commented placeholders with a clear D-116-FOLLOW-UP marker so the
116-02 plan (or a 116-01 follow-up once Server5 returns) can populate them
verbatim. NO improvised values — drift = bug per D-116-LOCK-CANONICAL.

===============================================================================
CANONICAL :root BLOCK (verbatim from .planning/v35-DESIGN-SYSTEM-MILESTONE.md
lines 43-62 / 116-01-PLAN.md lines 91-107 — locked, double-space form):
===============================================================================

:root {
  --dash-pad: 28px;
  --dash-radius: 18px;
  --dash-line: rgba(0,0,0,0.07);
  --dash-line-strong: rgba(0,0,0,0.12);
  --card-bg: #ffffff;
  --card-bg-2: #fafafa;
  --card-shadow: 0 1px 2px rgba(0,0,0,0.03), 0 24px 60px -34px rgba(0,0,0,0.18);
  --hero-grad: linear-gradient(135deg, #fafafa 0%, #f0f0f3 100%);
  --accent-blue:  #2563eb;
  --accent-green: #16a34a;
  --accent-amber: #d97706;
  --accent-red:   #dc2626;
  --font-mono:  "Geist Mono", ui-monospace, monospace;
  --font-serif: "Instrument Serif", serif;
}

===============================================================================
CANONICAL body.dark BLOCK (PENDING — Server5 unreachable at 116-01 fetch
time; Phase 115 confirmed 8 override-hits exist in dashboard.html but did
not capture the body of the block):
===============================================================================

body.dark {
  /* PENDING — body.dark block contents to be transcribed verbatim once
     Server5 recovers. Phase 115 batched fetch (landing-headers.txt)
     confirms 8 `body.dark` hits exist in dashboard.html — the variant
     ships dark overrides for the :root variables. v1.0.0 of the
     @livinity/design-tokens package documents this as a known follow-up
     (D-116-FOLLOW-UP-DARK). NO improvised values; D-116-LOCK-CANONICAL
     holds. */
}

===============================================================================
CANONICAL body.iridescent BLOCK (PENDING — Server5 unreachable; Phase 115
confirmed 1 hit exists in dashboard.html):
===============================================================================

body.iridescent {
  /* PENDING — body.iridescent block contents to be transcribed verbatim
     once Server5 recovers. Phase 115 batched fetch confirms 1
     `body.iridescent` hit exists in dashboard.html. v1.0.0 of the
     @livinity/design-tokens package documents this as a known follow-up
     (D-116-FOLLOW-UP-IRIDESCENT). NO improvised values;
     D-116-LOCK-CANONICAL holds. */
}

===============================================================================
END OF SNAPSHOT
===============================================================================
