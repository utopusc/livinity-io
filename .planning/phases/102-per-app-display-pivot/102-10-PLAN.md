---
phase: 102
plan: 10
title: Mini PC deploy + 25-row UAT walk
wave: 4
depends_on: [102-04, 102-05, 102-06, 102-07, 102-08, 102-09]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/102-per-app-display-pivot/UAT-CHECKLIST.md
  - .planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md
  - .planning/phases/102-per-app-display-pivot/102-VALIDATION.md
autonomous: false
type: execute
requirements:
  - D-102-PER-APP-XVFB
  - D-102-PER-APP-CHROME
  - D-102-MASTER-PROFILE-SEED
  - D-102-MASTER-LOGIN-UI
  - D-102-X11VNC-WHOLE-DISPLAY
  - D-102-LUSE-DISPLAY-SCOPING
  - D-102-CLOSE-LIFECYCLE
  - D-102-NATIVE-APP-PARITY
  - D-102-SACRED
  - D-102-NO-SERVER4
estimated_tasks: 6
must_haves:
  truths:
    - "25/25 UAT rows pass on Mini PC (bruce@10.69.31.68)"
    - "Master Chrome Login flow seeds /opt/livos/data/chrome-master/Default/Cookies with Google auth"
    - "Per-app Xvfb at 1280x720 — Luse screenshots return 1280x720 (NOT 1920x1080) — fixes user-reported bug"
    - "Window overlap impossible: 5 concurrent WebApps + 2 native apps render distinctly, no stream contamination"
    - "Close lifecycle leaves zero zombies: pgrep -af Xvfb/chrome.*livos-chrome-app shows no residue after close"
    - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f verified on /opt/liv/packages/core/src/sdk-agent-runner.ts on Mini PC"
    - "ROADMAP.md Phase 102 entry flipped from [ ] to [x]"
    - "STATE.md current position advanced past Phase 102"
  artifacts:
    - path: ".planning/phases/102-per-app-display-pivot/UAT-CHECKLIST.md"
      provides: "25-row UAT log with pass/fail per row + remediation notes"
    - path: ".planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md"
      provides: "phase close-out summary: commits, files touched, lessons learned"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 102 marked [x]"
    - path: ".planning/STATE.md"
      provides: "Current Position advanced; v33 milestone status updated if applicable"
  key_links:
    - from: "GitHub master branch (push from local)"
      to: "Mini PC /opt/livos/ + /opt/liv/ via bash /opt/livos/update.sh"
      via: "deploy script clones from utopusc/livinity-io master, rsync + pnpm install + build + systemctl restart"
    - from: "Settings → Chrome Master Login"
      to: "/opt/livos/data/chrome-master/Default/Cookies"
      via: "Google OAuth in master Chrome on bruce :0"
---

<objective>
User-walked plan. Deploy Phase 102 to Mini PC via `bash /opt/livos/update.sh` (the canonical deploy command). Walk the 25-row UAT from CONTEXT.md against the running system. Document each row's pass/fail in UAT-CHECKLIST.md. On all-pass, flip ROADMAP.md + STATE.md + write PHASE-SUMMARY.md and commit close-out.

**autonomous: false** — this plan requires the user at the Mini PC physical screen (for the Master Login OAuth flow), at the user's LivOS browser (for clicking dock icons), and at the SSH terminal (for pgrep/lsof verification commands).

Output: completed UAT-CHECKLIST.md, PHASE-SUMMARY.md, ROADMAP + STATE updates, final commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-per-app-display-pivot/102-CONTEXT.md §"Success Criteria (UAT walk, 25 rows)"
@.planning/phases/102-per-app-display-pivot/102-01-PLAN.md through 102-09-PLAN.md (all Wave 1-3 plans must have shipped before this plan runs)
@.planning/phases/102-per-app-display-pivot/102-VALIDATION.md row 102-10-01 (Sacred SHA Mini PC check)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-deploy local verification (build + tests green; sacred SHA local)</name>
  <what-built>Wave 1-3 plans (102-01..102-09) shipped — all source changes are local in master branch.</what-built>
  <how-to-verify>
    Run on the dev workstation (cwd = repo root):
    ```bash
    pnpm -r build
    pnpm -r test:run -- --reporter=dot
    git hash-object liv/packages/core/src/sdk-agent-runner.ts
    git log --oneline master ^origin/master   # commits ready to push
    git status
    ```
    Expected:
    - `pnpm -r build` exits 0 (livinityd + ui + liv all build clean)
    - `pnpm -r test:run` exits 0 (no regressions across packages)
    - `git hash-object` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - `git log` shows the Wave 1-3 commits (~35-40 atomic commits across 102-01..102-09)
    - `git status` is clean (no uncommitted changes)
  </how-to-verify>
  <resume-signal>Type "all green" to proceed to deploy, or describe specific failures for triage.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push to GitHub master</name>
  <action>
    1. Push to GitHub: `git push origin master`
    2. Confirm push succeeded: `git log origin/master --oneline -1` should match local HEAD.
    3. (No commit in this task — push is the action.)
  </action>
  <acceptance_criteria>
    - `git rev-parse HEAD` == `git rev-parse origin/master`
  </acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse origin/master)"</automated>
  </verify>
  <done>
    Local master pushed to origin master.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Mini PC deploy via update.sh (USER walks SSH)</name>
  <what-built>Local commits on GitHub master. Mini PC needs to pull + build + restart.</what-built>
  <how-to-verify>
    On a Mini PC SSH terminal, the user runs:
    ```bash
    ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
      -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      bruce@10.69.31.68

    # On Mini PC:
    sudo bash /opt/livos/update.sh 2>&1 | tee /tmp/livos-update-$(date +%s).log
    ```
    Expected console output ends with:
    - `pnpm install` clean
    - `@livos/config build`, `ui build`, `@liv/core build`, `@liv/worker build`, `@liv/mcp-server build` all succeed
    - `systemctl restart livos liv-core liv-worker liv-memory` runs without error
    - All 4 services `active (running)` per `systemctl status`

    Post-deploy verification commands the user runs on Mini PC:
    ```bash
    systemctl status livos liv-core liv-worker liv-memory --no-pager | head -40
    git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts
    ls /opt/livos/.deployed-sha && cat /opt/livos/.deployed-sha
    ```
    Expected:
    - All 4 services `active (running)`
    - hash-object outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - .deployed-sha matches `git rev-parse origin/master` (full SHA)

    **Note:** If `liv-memory` is in restart loop (known pre-existing breakage per MEMORY.md), document it but do not block Phase 102 — that's a separate fix.
  </how-to-verify>
  <resume-signal>Type "deployed; services active; mini-PC sacred SHA verified" or describe failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 25-row UAT walk on Mini PC</name>
  <what-built>Phase 102 deployed. User walks the 25 UAT rows from CONTEXT.md against the running LivOS at bruce.livinity.io.</what-built>
  <how-to-verify>
    Open `.planning/phases/102-per-app-display-pivot/UAT-CHECKLIST.md` (creator: this task — copy the 25-row table from `.planning/phases/102-per-app-display-pivot/102-CONTEXT.md` §"Success Criteria"). For each row, record PASS / FAIL / NOTES.

    The 25 rows (verbatim from CONTEXT.md §"Success Criteria"):
    1. LivOS Settings → "Chrome Master Login" exists (UI affordance visible)
    2. Click Master Login → Chrome opens on bruce's :0 desktop with /opt/livos/data/chrome-master/ user-data-dir
    3. Log into Google in master Chrome, close → /opt/livos/data/chrome-master/Default/Cookies contains Google auth tokens
    4. Click WebApp icon livinity.io → Xvfb :10 spawns, Chrome on :10 with /tmp/livos-chrome-app-<uuid>, x11vnc on port 15900, LivOS stream window mounts
    5. Chrome on :10 is FULLSCREEN with NO chrome chrome (no tabs, no address bar)
    6. Stream window resolution = 1280x720 (pixel-measure noVNC canvas)
    7. Google account visible in livinity.io (logged in — inherited from master)
    8. Click 2nd WebApp google.com → Xvfb :11 spawns, different Chrome process, port 15901, different stream window
    9. Both Chromes show same Google account (master seed)
    10. `pgrep -af 'Xvfb :1'` on Mini PC shows :10 + :11 (two Xvfb processes)
    11. `pgrep -af 'chrome.*livos-chrome-app'` shows 2 distinct Chrome processes (distinct user-data-dirs)
    12. `lsof -i :15900 -i :15901` shows 2 x11vnc listeners
    13. Resize LivOS stream window 1 → content stays sharp (Xvfb :10 resolution unchanged; LivOS UI letterboxes)
    14. Add Ubuntu native app config: VSCode → tRPC apps.native.create succeeds, icon appears
    15. Click VSCode icon → Xvfb :12 spawns, VSCode binary on :12 with DISPLAY=:12, x11vnc :12 → port 15902
    16. VSCode UI renders correctly in stream (native app on dedicated display)
    17. Chat session in WebApp window has activeDisplay context — test prompt "Take a screenshot of this window" → Luse computer_screenshot returns 1280x720 image (NOT 1920x1080)
    18. Luse coord click works — click button at known coord → executes correctly (no offset/scaling)
    19. Teach mode v3: click stream → popover appears at click point
    20. Save Teach skill, replay → action coords replay correctly on :N display
    21. Close WebApp livinity.io → Chrome on :10 killed, x11vnc killed, Xvfb :10 killed, /tmp/livos-chrome-app-<uuid> removed, port + display released
    22. After close, only :11 Xvfb remains (`pgrep -af 'Xvfb :1'`)
    23. Close VSCode native app → binary killed, Xvfb :12 killed (same cleanup as WebApp)
    24. Sacred SHA preserved: `ssh bruce@10.69.31.68 'git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts'` = `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    25. Concurrent 5 WebApps + 2 native apps → all 7 Xvfb displays + Chrome/binary processes + x11vnc + stream windows, no overlap, no crash, ~3GB RAM total

    **Action during this task:**
    1. Create `UAT-CHECKLIST.md` with the 25 rows in a markdown table including Pass/Fail/Notes columns.
    2. Walk each row, marking PASS / FAIL.
    3. For each FAIL, capture: stderr from livinityd journal, exact reproduction steps, suspected file.
    4. Commit the UAT log: `docs(102-10-04): UAT walk complete — N/25 PASS, M/25 FAIL`.
  </how-to-verify>
  <resume-signal>Type "25/25 PASS" to proceed to close-out, or "N PASS, M FAIL — list of failures" to trigger remediation. Failures may require a /gsd-plan-phase 102-11 hot-fix round before close-out.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write PHASE-SUMMARY.md (close-out) + flip ROADMAP + STATE</name>
  <read_first>
    - .planning/ROADMAP.md (find Phase 102 entry)
    - .planning/STATE.md (find current position section)
    - .planning/phases/102-per-app-display-pivot/UAT-CHECKLIST.md (Task 4 output)
    - All SUMMARY.md files from 102-01..09 to aggregate (102-01-SUMMARY.md through 102-09-SUMMARY.md)
  </read_first>
  <action>
    1. Create `.planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md`:
       ```markdown
       # Phase 102 — Per-App Display Pivot — PHASE SUMMARY

       **Shipped:** 2026-05-DD
       **Status:** PASSED / PARTIAL-PASS / (depends on UAT)
       **Commits:** {commit range from `git log --oneline 102-01..102-10`}
       **Plans:** 10 (Wave 1: 3 parallel, Wave 2: 3 parallel, Wave 3: 3 parallel, Wave 4: 1 user-walked)
       **Sacred SHA:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNTOUCHED across all commits

       ## What Shipped
       - Per-app Xvfb display ({:10..:99}, 1280x720x24) via 102-01 DisplayAllocator + XvfbSpawner
       - Per-app Chrome subprocess via 102-02 ChromeProcessSpawner (T-102-02 input validation)
       - Master profile seed-copy via 102-03 (A1 reflink fallback + A7 SingletonLock cleanup)
       - Window-manager spawn body rewrite via 102-04 (CDP path removed)
       - Native-app-binder display swap via 102-05 (WM_CLASS poll dropped)
       - Luse MCP env switch (LUSE_TARGET_DISPLAY) + Active Display Context snippet via 102-06
       - Master Chrome Login UI + tRPC routes via 102-07 (T-102-07 admin gate)
       - Close lifecycle (8-step idempotent teardown) via 102-08
       - x11vnc -display :N canonical via 102-09

       ## UAT Result
       (paste from UAT-CHECKLIST.md summary)

       ## Issues / Carryovers
       - (any FAIL rows from UAT → list with proposed Plan 102-11 if remediation needed)

       ## Lessons Learned
       - {pulled from each 102-NN-SUMMARY.md "Lessons" section}

       ## Files Touched
       {aggregate from per-plan SUMMARYs}
       ```
    2. Edit `.planning/ROADMAP.md`: flip Phase 102 entry from `[ ]` to `[x]` (find line: `### Phase 102:`).
    3. Edit `.planning/STATE.md`: update `## Current Position` section. Phase 102 closes; advance to next phase (per ROADMAP). Note the v33 milestone status (Phase 102 closes the per-app-display pivot bug; if all v33 phases now closed, flip v33 ✅ Shipped status in ROADMAP too).
    4. Flip `.planning/phases/102-per-app-display-pivot/102-VALIDATION.md` row 102-10-01 to `✅ green`. Also flip `nyquist_compliant: true` in VALIDATION.md frontmatter (per Wave 1-3 plans completing + UAT passing).
    5. Commit: `docs(102-10-05): phase 102 close-out — PHASE-SUMMARY + ROADMAP [x] + STATE advance`.
    6. Push: `git push origin master`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md`
    - `grep -q "Phase 102.*\\[x\\]" .planning/ROADMAP.md` exit 0
    - `grep -q "nyquist_compliant: true" .planning/phases/102-per-app-display-pivot/102-VALIDATION.md` exit 0
    - Commit on local master matches origin/master after push
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md && grep -q "102.*\\[x\\]" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    PHASE-SUMMARY written, ROADMAP + STATE updated, push complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final confirmation</name>
  <what-built>Phase 102 closed. All artifacts committed and pushed.</what-built>
  <how-to-verify>
    Final visual check:
    1. Open `.planning/ROADMAP.md` — Phase 102 shows `[x]`
    2. Open `.planning/STATE.md` — Current Position past Phase 102
    3. Open `.planning/phases/102-per-app-display-pivot/PHASE-SUMMARY.md` — summary readable, UAT result documented
    4. On Mini PC, all WebApps + native apps spawn + close correctly (informal re-check optional)
    5. v33 milestone status updated in ROADMAP if applicable

    If anything is missing or wrong, point it out and I'll fix it in a follow-up commit.
  </how-to-verify>
  <resume-signal>Type "phase 102 closed" to mark this plan complete, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- 25/25 UAT rows pass on Mini PC (or documented failures with remediation plan)
- Mini PC services all active post-deploy
- Sacred SHA verified on Mini PC's /opt/liv/
- PHASE-SUMMARY.md written
- ROADMAP.md Phase 102 [x]
- STATE.md advanced
- All commits pushed to GitHub master
</verification>

<success_criteria>
The user-reported bugs from the original UAT trigger ("ayri displayde sorunumu nasil cozersin", "Luse ssleri 1920x1080 aliyor", "ayni screen de iki farkli yayin yapiyorsun ustuste bindiginde sorun cikiyor") are all resolved on the live Mini PC. Per-app Xvfb + per-app Chrome + master seed + Luse 1280x720 native + x11vnc whole-display capture all work in concert.
</success_criteria>

<output>
PHASE-SUMMARY.md is the close-out artifact for this plan. After this plan, the recommended next step is `/gsd-cleanup` (archive Phase 102 if user requests) followed by `/gsd-discuss-phase 103` for whatever comes next (likely two-way profile sync per D-102 deferred).
</output>
