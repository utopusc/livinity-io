---
phase: 115-ui-component-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md
autonomous: true
requirements: []
tags: [ui-inventory, design-system, v35, server5, nextjs, documentation]

must_haves:
  truths:
    - "INVENTORY-SERVER5.md exists and lists every TSX + TS UI file in /opt/platform/web/src/"
    - "Every inventoried row has a migration tag from the locked taxonomy"
    - "Zero source-tree edits — file walk is read-only SSH, no edits on Server5, no commits to repo source"
  artifacts:
    - path: ".planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md"
      provides: "Per-file inventory of Server5 Next.js platform UI"
      min_lines: 150
      contains: "## app/dashboard/"
  key_links:
    - from: "INVENTORY-SERVER5.md"
      to: "v35-DESIGN-SYSTEM-MILESTONE.md § Surface 2 Server5 platform Next.js"
      via: "Inventory matches the 68 TSX + 69 TS scope declared in milestone spec"
      pattern: "canonical|needs-migration|replace-with-library|wontfix|unknown"
---

<objective>
Walk the entire `/opt/platform/web/src/` directory on Server5 (~68 TSX + ~69 TS files) via SSH and produce an inventory document. Every UI-bearing file (TSX) gets one row capturing: file path, primary purpose, route it powers, current visual idiom, migration tag. TS files that are pure lib/util (no JSX) are inventoried in a separate "lib (non-UI)" section with minimal columns (path + purpose) — they exist for completeness but are not migration targets.

Purpose: Server5 is the second surface in the v35.0 unification. Unlike Mini PC, Server5 is NOT a git repo here — files are edited via SSH. This inventory map is the only way to scope Phase 117 (Server5 Next.js platform migration) precisely.

Output: `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md` (~150 lines, table-organized by Next.js app router structure: app/(auth)/, app/dashboard/, app/onboarding/, app/store/, app/download/, components/, lib/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-ui-component-inventory/115-CONTEXT.md
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md

# Server5 connection (memorized in MEMORY.md):
# SSH binary: /c/Windows/System32/OpenSSH/ssh.exe
# Key: C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master
# Host: root@45.137.194.102
# Server5 hosts /opt/platform/web (Next.js 16.1.7) + /opt/landing/livinity.io (HTML)

# Migration tag taxonomy (locked — same as 115-01):
# - canonical / needs-migration / replace-with-library / wontfix / unknown
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSH-walk Server5 to enumerate every TSX and TS file under /opt/platform/web/src/</name>
  <read_first>
    - .planning/phases/115-ui-component-inventory/115-CONTEXT.md (LOCKED scope)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "Surface 2: Server5 platform Next.js" (68 TSX + 69 TS breakdown across app/dashboard, app/(auth)/, app/onboarding/install, app/store, app/download, components/, lib/)
  </read_first>
  <files>.planning/phases/115-ui-component-inventory/.work/server5-file-list.txt</files>
  <action>
Run ONE batched SSH command (per `feedback_ssh_rate_limit` — Mini PC has fail2ban, Server5 doesn't, but batching is still good practice) to produce both file inventories:

```
/c/Windows/System32/OpenSSH/ssh.exe \
  -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
  -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
  root@45.137.194.102 \
  "echo '=== TSX FILES ==='; find /opt/platform/web/src -type f -name '*.tsx' | sort; echo '=== TS FILES ==='; find /opt/platform/web/src -type f -name '*.ts' ! -name '*.d.ts' | sort; echo '=== END ==='"
```

Save the entire output to `.planning/phases/115-ui-component-inventory/.work/server5-file-list.txt`.

Then, for each TSX file (and key TS files like `app/layout.tsx`, anything imported into a TSX), batch-fetch the first 40 lines via a SECOND single SSH session. Use a heredoc or `xargs`:

```
ssh ... root@45.137.194.102 "find /opt/platform/web/src -type f \\( -name '*.tsx' -o -name '*.ts' \\) ! -name '*.d.ts' -print0 | xargs -0 -I {} sh -c 'echo \"===FILE: {} ===\"; head -40 {}; echo'" > .planning/phases/115-ui-component-inventory/.work/server5-headers.txt
```

This produces a single concatenated headers file with `===FILE: <path>===` separators — the agent then parses it locally without further SSH round-trips.

Confirm:
- TSX count ≈ 68 (±5%)
- TS count ≈ 69 (±5%) — note that `lib/*.ts` files have NO JSX and only get minimal rows in the inventory (path + purpose)

If counts diverge by >10%, log the actuals and proceed using actual counts.

NOTE: `.work/` is scratch and not committed.
  </action>
  <verify>
    <automated>test -f .planning/phases/115-ui-component-inventory/.work/server5-file-list.txt && grep -c "\\.tsx$" .planning/phases/115-ui-component-inventory/.work/server5-file-list.txt | awk '{exit ($1 >= 55) ? 0 : 1}' && test -f .planning/phases/115-ui-component-inventory/.work/server5-headers.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.work/server5-file-list.txt` exists, contains "=== TSX FILES ===" and "=== TS FILES ===" sections
    - TSX file count is ≥55 (allowing for some drift below the spec'd 68)
    - `.work/server5-headers.txt` exists with `===FILE: ` markers separating per-file first-40-line blocks
    - Total SSH round-trips used: ≤2 (one for file list, one for batched headers) — verifiable from the agent's tool call log
  </acceptance_criteria>
  <done>Both scratch files exist locally; the agent has all data needed to classify offline without further SSH calls.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every Server5 file and write INVENTORY-SERVER5.md</name>
  <read_first>
    - .planning/phases/115-ui-component-inventory/.work/server5-headers.txt (from Task 1 — first 40 lines per file)
    - .planning/phases/115-ui-component-inventory/.work/server5-file-list.txt (from Task 1 — file enumeration)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "Surface 2 Server5 platform Next.js" (current state: zinc-only Tailwind palette, no Geist font globally, mostly bespoke per-page styling — pre-bias toward `needs-migration` tag)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "Phase 117 — Server5 Next.js Platform Migration" (the consumer of this inventory — see what plans need)
  </read_first>
  <files>.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md</files>
  <action>
Build the Server5 inventory document. Single output file: `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md`.

**Document structure (exact):**

```markdown
# Server5 Next.js Platform UI Inventory — `/opt/platform/web/src/`

**Phase:** 115 (v35.0 Design System Unification milestone foundation)
**Snapshot date:** 2026-05-14
**Host:** Server5 (45.137.194.102, root@) — NOT a git repo; files SSH-edited
**Total files inventoried:** {N_TSX} TSX (UI) + {N_TS} TS (non-UI lib)
**Source root:** `/opt/platform/web/src/`

## Migration tag taxonomy

| Tag | Meaning |
|---|---|
| `canonical` | Already matches dashboard.html design language; no migration needed |
| `needs-migration` | Functional component, needs visual restyle to canonical tokens |
| `replace-with-library` | Duplicates a primitive that will be replaced by `@livinity/ui-kit` (Phase 119) |
| `wontfix` | Out-of-scope for v35 (note column explains why) |
| `unknown` | Agent could not classify; needs operator review |

## Summary by directory

| Directory | TSX file count | Predominant idiom | Predominant tag |
|---|---|---|---|
| `app/(auth)/` | {N} | Tailwind zinc | needs-migration |
| `app/dashboard/` | {N} | Tailwind zinc | needs-migration |
| `app/onboarding/install/` | {N} | mixed (redirect to dashboard/install) | needs-migration |
| `app/store/` | {N} | Tailwind zinc | needs-migration |
| `app/download/` | {N} | Tailwind zinc | needs-migration |
| `components/` | {N} | mixed | needs-migration |
| `lib/` (non-UI TS) | {N} | n/a | wontfix |

## app/(auth)/

(Login, register, verify, forgot-password, reset-password, device pages.)

| File | Primary purpose | Route | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `app/(auth)/login/page.tsx` | Login form | `/login` | Tailwind utility | needs-migration | zinc palette, no Geist; Phase 117-02 target |
| ... | ... | ... | ... | ... | ... |

## app/dashboard/

(Includes the Next.js dashboard page + the recently-shipped install wizard `/dashboard/install`.)

| File | Primary purpose | Route | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `app/dashboard/page.tsx` | Main dashboard page (566 lines) | `/dashboard` | Tailwind utility | needs-migration | Currently NOT live (Caddy routes /dashboard to /opt/landing/livinity.io/dashboard.html); Phase 117-05 target |
| `app/dashboard/install/page.tsx` | First-run install wizard (244 lines) | `/dashboard/install` | Tailwind utility + dashboard-shell wrap | canonical OR needs-migration | Phase 111 follow-up; audit drift in Phase 117-03 |
| ... | ... | ... | ... | ... | ... |

## app/onboarding/install/

| File | Primary purpose | Route | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `app/onboarding/install/page.tsx` | Currently redirects to /dashboard/install | `/onboarding/install` | n/a (redirect) | wontfix | Pure redirect, no visual; survives v35 unchanged |
| ... | ... | ... | ... | ... | ... |

## app/store/

| File | Primary purpose | Route | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `app/store/[id]/page.tsx` | Public app detail page | `/store/[id]` | Tailwind utility | needs-migration | Phase 117-04 target |
| ... | ... | ... | ... | ... | ... |

## app/download/

| File | Primary purpose | Route | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `app/download/page.tsx` | LivOS download page | `/download` | Tailwind utility | needs-migration | Phase 117-05 target |

## components/

| File | Primary purpose | Used by | Visual idiom | Migration tag | Notes |
|---|---|---|---|---|---|
| `components/motion-primitives/X.tsx` | Animation primitive | various | framer-motion | needs-migration | Keep motion semantics; restyle visual |
| ... | ... | ... | ... | ... | ... |

## lib/ (non-UI TS — minimal columns)

(Pure backend/utility TS modules with no JSX. Inventoried for completeness; not migration targets — all tagged `wontfix`.)

| File | Primary purpose | Migration tag |
|---|---|---|
| `lib/auth.ts` | Session + JWT helpers | wontfix (backend) |
| `lib/db.ts` | Drizzle ORM client | wontfix (backend) |
| `lib/session-revocation.ts` | Session blacklist | wontfix (backend) |
| ... | ... | wontfix (backend) |

## API routes (app/api/**/route.ts) — explicit no-op for v35

These are server-side route handlers (Next.js Route Handlers). They emit JSON, not UI. Per D-117-NO-API-CHANGES they are out-of-scope for the entire v35 milestone. Listed here for inventory completeness only.

| File | Endpoint | Migration tag |
|---|---|---|
| `app/api/auth/login/route.ts` | POST /api/auth/login | wontfix (API; D-117-NO-API-CHANGES) |
| ... | ... | wontfix |

## Aggregate counts

| Migration tag | Count | % |
|---|---|---|
| canonical | {N} | {N}% |
| needs-migration | {N} | {N}% |
| replace-with-library | {N} | {N}% |
| wontfix | {N} | {N}% |
| unknown | {N} | {N}% |
| **TOTAL** | {N} | 100% |

## Server5 deployment quirks (notes for Phase 117 executor)

- Server5 is NOT a git repo. Edits via SSH at `/opt/platform/web/src/`. Backup pattern: copy file to `<file>.pre-v35-NN.bak` before editing (per D-V35-INCREMENTAL-COMMITS spirit, mirrored to SSH context).
- Build: `cd /opt/platform/web && npm run build`. Restart: `pm2 restart web`.
- `app/layout.tsx` is the global injection point for design-tokens (Phase 117-01).
- `tailwind.config.ts` lives at `/opt/platform/web/tailwind.config.ts` (extend with @livinity/design-tokens preset in Phase 117-01).
- `globals.css` at `/opt/platform/web/src/app/globals.css`.
```

**Classification rules (Server5-specific tweaks on top of 115-01 ruleset):**

1. Files under `app/api/**/route.ts` → tag `wontfix`, note "API; D-117-NO-API-CHANGES".
2. Files under `lib/` (non-UI TS) → minimal row, tag `wontfix`, note "backend lib".
3. Files in `app/(auth)/` and `app/dashboard/` and `app/store/` and `app/download/` → default tag `needs-migration` (zinc palette + no Geist is documented as universal in Surface 2 spec).
4. `app/dashboard/install/page.tsx` is special — Phase 111 follow-up — may be `canonical` if dashboard-shell wrap is intact. Inspect first-40-lines to decide. If headers show `import { DashboardShell }` and CSS variable references like `var(--dash-pad)` → tag `canonical`. Otherwise `needs-migration`.
5. `components/motion-primitives/*` → `needs-migration` with note "framer-motion primitive; keep semantics, restyle".
6. Pure redirect pages (`page.tsx` that only returns `redirect(...)`) → `wontfix` with note "redirect; no visual".
7. Everything else → standard 115-01 rules.

**Volume guidance:**
- ~68 TSX rows + ~69 TS rows + summary tables → ~150-200 total lines target.
- TS lib rows use the minimal 3-column format to keep the file readable.

**ZERO SOURCE EDITS** (D-115-READ-ONLY):
- No edits to `/opt/platform/web/` on Server5 (the SSH walk is read-only `find` + `head`)
- No edits to `livos/`, `liv/`, `scripts/`, `packages/` in the local repo
- Verify before finishing: `git diff HEAD -- livos/ liv/ scripts/ packages/` returns empty
  </action>
  <verify>
    <automated>test -f .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && [ "$(wc -l < .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md)" -ge 130 ] && grep -q "^## app/(auth)/" .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && grep -q "^## app/dashboard/" .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && grep -q "^## app/store/" .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && grep -q "^## components/" .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && grep -q "^## lib/" .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md && [ -z "$(git diff HEAD -- livos/ liv/ scripts/ packages/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md` exists with ≥130 lines
    - All section headers present: `## app/(auth)/`, `## app/dashboard/`, `## app/onboarding/install/`, `## app/store/`, `## app/download/`, `## components/`, `## lib/`, `## API routes`, `## Aggregate counts`, `## Server5 deployment quirks`
    - ≥55 TSX file rows (allowing ~5% drift below spec'd 68)
    - Every TSX migration tag is one of the 5 canonical values
    - "Server5 deployment quirks" section mentions: NOT a git repo, SSH edit pattern, `pm2 restart web`, `app/layout.tsx`, `tailwind.config.ts`, `globals.css`
    - `git diff HEAD -- livos/ liv/ scripts/ packages/` returns empty
  </acceptance_criteria>
  <done>INVENTORY-SERVER5.md ready; all Server5 UI files classified; deployment quirks documented for Phase 117 consumer; zero source drift.</done>
</task>

</tasks>

<verification>
- Run `wc -l .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md` → ≥130 lines.
- Run `grep -oE "canonical|needs-migration|replace-with-library|wontfix|unknown" INVENTORY-SERVER5.md | sort -u` → only those 5 tags present.
- Spot-check 5 random rows against the SSH file list: each path resolves to a real file on Server5 (or was a recognized API/lib stub).
- Run `git diff HEAD -- livos/ liv/ scripts/ packages/` → empty (D-115-READ-ONLY enforced).
</verification>

<success_criteria>
- INVENTORY-SERVER5.md ships at `.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md`
- Every TSX + TS file under `/opt/platform/web/src/` has an inventory row (TSX = full row, TS = minimal row)
- Server5 deployment quirks section enables Phase 117 executor to operate without rediscovery
- D-115-READ-ONLY honored: zero source-tree edits, zero edits on Server5
</success_criteria>

<output>
After completion, create `.planning/phases/115-ui-component-inventory/115-02-SUMMARY.md` covering:
- Final TSX + TS counts
- Migration tag distribution
- Any `dashboard/install/page.tsx` finding (canonical vs needs-migration verdict + evidence)
- Any `unknown`-tagged files needing operator review
- SSH session count used (target: 2; bonus: 1)
</output>
