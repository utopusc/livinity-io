---
phase: 184-v38-deploy-uat
plan: 02
type: execute
wave: 2
depends_on:
  - 184-01
files_modified:
  - .planning/phases/184-v38-deploy-uat/184-01-deploy-log.md
autonomous: true
requirements:
  - V38-milestone-ship-gate

must_haves:
  truths:
    - "update.sh completes with exit 0 on Mini PC"
    - "Deployed commit SHA on Mini PC matches local HEAD (e1f44ce75f0e4fe89aecdddc48381faa04ce7d44)"
    - "All 4 services restart clean (NRestarts=0 or minimal) post-deploy"
    - "Migration ran: /root/liv/ directory exists, items/ subdirectory populated"
    - "Backup file /root/.backups/v35-cc-sessions.json exists (idempotency gate from Phase 173-02)"
    - "All 25 sacred SHAs byte-identical post-deploy"
  artifacts:
    - path: ".planning/phases/184-v38-deploy-uat/184-01-deploy-log.md"
      provides: "Extended with § 2 deploy log + § 3 post-deploy verification"
      min_lines: 120
  key_links:
    - from: "Mini PC /opt/livos"
      to: "origin/master HEAD"
      via: "update.sh git clone + rsync"
      pattern: "e1f44ce75f0e4fe89aecdddc48381faa04ce7d44"
    - from: "migration-v35-to-v38.ts"
      to: "/root/liv/items/"
      via: "livinityd boot auto-migration call"
      pattern: "\\[vault-items\\].*items loaded"
---

<objective>
Run `bash /opt/livos/update.sh` on Mini PC using the detached + nohup + log-poll
pattern (ZeroTier unstable). After update.sh completes, verify the deployed SHA,
service health, sacred SHA integrity post-deploy, and Phase 173 migration outcome.

Purpose: The actual ship gate. update.sh pulls e1f44ce7 from origin/master, rsyncs
all v38.0 source, rebuilds both pnpm (livos/) and npm (liv/) workspaces, restarts
all 4 systemd services. The Phase 173-02 migration runs automatically on first boot
via livinityd startup hook (migrateV35SessionsToV38).

Output: § 2 + § 3 appended to `184-01-deploy-log.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/184-v38-deploy-uat/184-CONTEXT.md
@.planning/phases/184-v38-deploy-uat/184-01-SUMMARY.md

<interfaces>
<!-- ZeroTier-safe deploy pattern (from reference_zerotier_unstable.md) -->
<!-- Step 1: launch update.sh detached -->
LAUNCH_CMD="nohup sudo bash /opt/livos/update.sh > /tmp/v38-deploy.log 2>&1 &"
<!-- Step 2: poll log every 30s until "Done" or "Error" marker or 10min timeout -->
POLL_CMD="tail -20 /tmp/v38-deploy.log"
<!-- Step 3: retrieve full log when done -->
FETCH_LOG="cat /tmp/v38-deploy.log"

<!-- Post-deploy verification batch (single SSH invocation — fail2ban safety) -->
POST_VERIFY="
echo '=== POST-DEPLOY VERIFY ===' &&
date '+%Y-%m-%dT%H:%M:%SZ' &&
echo '--- Deployed SHA ---' &&
cat /opt/livos/.deployed-sha 2>/dev/null || echo 'NO .deployed-sha file — check update.sh' &&
echo '--- Service health ---' &&
for svc in livos liv-core liv-worker liv-memory; do
  sudo systemctl is-active \$svc && sudo systemctl show \$svc --property=NRestarts | head -1
done &&
echo '--- Sacred SHA post-deploy ---' &&
cd /opt/livos && sudo sh scripts/check-sacred.sh 2>&1 | tail -5 &&
echo '--- Migration outcome ---' &&
ls /root/liv/ 2>/dev/null || echo 'MISSING /root/liv/ — migration did not run' &&
ls /root/liv/items/ 2>/dev/null | wc -l | xargs echo 'items/ count:' &&
ls /root/.backups/v35-cc-sessions.json 2>/dev/null && echo 'backup-file: PRESENT' || echo 'backup-file: ABSENT (no v35 sessions to migrate, or migration not reached)' &&
echo '--- Boot journal tail ---' &&
sudo journalctl -u livos --since '5 min ago' --no-pager | tail -30
"

<!-- Expected deployed SHA -->
EXPECTED_SHA=e1f44ce75f0e4fe89aecdddc48381faa04ce7d44

<!-- update.sh location on Mini PC -->
UPDATE_SH_PATH=/opt/livos/update.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch update.sh detached + poll until complete</name>
  <files>.planning/phases/184-v38-deploy-uat/184-01-deploy-log.md</files>
  <action>
**ZeroTier safety rule**: Never hold foreground SSH for >30s. Use detached + poll pattern.

**Step 1 — Launch:**
```
/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 "nohup sudo bash /opt/livos/update.sh > /tmp/v38-deploy.log 2>&1 & echo LAUNCHED PID \$!"
```

Record the PID printed. Close SSH immediately after LAUNCHED.

**Step 2 — Poll (repeat every 30s, up to 10 minutes = 20 polls):**
For each poll, open a fresh SSH connection:
```
/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 "tail -20 /tmp/v38-deploy.log"
```

**Stop polling when log contains any of:**
- `"Done"` or `"Finished"` (update.sh success markers)
- `"Error"` or `"FAILED"` (update.sh failure)
- `"systemctl restart"` followed by service names (near-complete indicator)

Typically completes in 3-5 minutes.

**Step 3 — Retrieve full log:**
```
/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 "cat /tmp/v38-deploy.log"
```

Append full log under `## § 2 update.sh Execution Log` in `184-01-deploy-log.md`.

**If update.sh fails (non-zero exit):**
- Record error in log under `## § 2 DEPLOY FAILED`
- DO NOT proceed to Task 2 or Plan 184-03
- Pause and report failure to user with the last 30 lines of /tmp/v38-deploy.log

**pnpm-store quirk (per MEMORY.md):** After deploy, if livinityd logs import errors for
`@liv/core`, run on Mini PC:
```bash
sudo ls /opt/livos/node_modules/.pnpm/@liv+core*
```
If multiple dirs, identify which one `node_modules/@liv/core` symlink resolves to and
manually `cp -r /opt/liv/packages/core/dist` into that dir. Document in log if needed.
  </action>
  <verify>
    <automated>
      184-01-deploy-log.md contains "§ 2 update.sh" section. If deploy failed,
      the failure is documented and this task exits with a note that Plan 184-03
      is blocked.
    </automated>
  </verify>
  <done>
    update.sh completed (exit 0). Full deploy log captured in 184-01-deploy-log.md
    § 2. Services restarted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Post-deploy verification batch + migration check</name>
  <files>.planning/phases/184-v38-deploy-uat/184-01-deploy-log.md</files>
  <action>
Wait 60 seconds after update.sh completes (services need time to initialize).

Then run a single batched SSH command for all post-deploy checks:

```
/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 "
echo '=== POST-DEPLOY VERIFICATION ===' &&
date '+%Y-%m-%dT%H:%M:%SZ' &&

echo '--- Deployed SHA ---' &&
cat /opt/livos/.deployed-sha 2>/dev/null | head -1 &&

echo '--- Service status ---' &&
for svc in livos liv-core liv-worker liv-memory; do
  status=\$(sudo systemctl is-active \$svc 2>/dev/null)
  restarts=\$(sudo systemctl show \$svc --property=NRestarts 2>/dev/null | cut -d= -f2)
  echo \"\$svc: \$status (NRestarts=\$restarts)\"
done &&

echo '--- Sacred SHA post-deploy ---' &&
cd /opt/livos && sudo sh scripts/check-sacred.sh 2>&1 | tail -5 &&

echo '--- Migration: /root/liv/ layout ---' &&
ls -la /root/liv/ 2>/dev/null || echo 'WARN: /root/liv/ not found' &&
ls /root/liv/items/ 2>/dev/null | head -20 &&
echo 'items count:' \$(ls /root/liv/items/ 2>/dev/null | wc -l) &&

echo '--- Migration backup file ---' &&
ls -lh /root/.backups/v35-cc-sessions.json 2>/dev/null && echo 'backup: PRESENT' || echo 'backup: ABSENT (ok if no v35 sessions existed)' &&

echo '--- Boot journal (vault-items + liv-scaffolder + agent-schedule) ---' &&
sudo journalctl -u livos --since '3 min ago' --no-pager | grep -E 'vault-items|liv-scaffolder|agent-schedule|ERROR|WARN' | tail -20 &&

echo '--- pnpm store: @liv+core count ---' &&
ls /opt/livos/node_modules/.pnpm/ | grep '@liv+core' | wc -l | xargs echo '@liv+core dirs:'
"
```

Append full output under `## § 3 Post-Deploy Verification` in `184-01-deploy-log.md`.

**Acceptance gates (record PASS/FAIL for each):**
1. Deployed SHA matches `e1f44ce75f0e4fe89aecdddc48381faa04ce7d44`
2. All 4 services: `active` (NRestarts ≤ 2 acceptable for cold restart)
3. Sacred SHA: `[sacred-sha] PASS: 25 files verified`
4. `/root/liv/` exists
5. Journal contains `[vault-items]` or `[liv-scaffolder]` boot entries
6. No `ERROR` in journal relating to vault-items or agent-schedule

**If .deployed-sha is absent:** Run `cd /opt/livos && git rev-parse HEAD` as
fallback SHA check. (Mini PC is rsync-deployed, not a git checkout — update.sh may
write a `.deployed-sha` file or not, depending on version.)

Commit the extended deploy log:
```bash
git add .planning/phases/184-v38-deploy-uat/184-01-deploy-log.md
git commit -m "docs(184-02): post-deploy verification — SHA + services + migration + sacred SHAs"
git push origin master
```
  </action>
  <verify>
    <automated>
      184-01-deploy-log.md contains "§ 3 Post-Deploy Verification" section with
      PASS/FAIL entries for the 6 gates. Commit docs(184-02) in git log.
    </automated>
  </verify>
  <done>
    All 6 post-deploy gates recorded as PASS or FAIL with reasons. deploy SHA
    confirmed. Sacred SHA check: 25 files verified. Services active. Migration
    outcome documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| update.sh (root on Mini PC) → GitHub origin | Pulls from utopusc/livinity-io origin/master; must match expected HEAD SHA |
| deploy-log → git repo | Contains server metrics; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-184-02-01 | Tampering | update.sh pulls wrong SHA | mitigate | Task 2 gates on deployed SHA matching e1f44ce75f0e4fe89aecdddc48381faa04ce7d44; abort and report if mismatch |
| T-184-02-02 | Information Disclosure | /tmp/v38-deploy.log | accept | Temp file on Mini PC; not committed; cleared by next deploy |
| T-184-02-03 | Denial of Service | ZeroTier drop during poll | mitigate | Detached nohup pattern; poll over fresh SSH; update.sh runs independently of SSH session |
| T-184-02-04 | Elevation of Privilege | sudo bash update.sh | accept | bruce user has passwordless sudo for /opt/livos ops per server setup |
</threat_model>

<verification>
After Task 2:
- 184-01-deploy-log.md has §2 + §3 sections (≥120 total lines)
- git log shows docs(184-02) commit
- 6 post-deploy gates documented (PASS or FAIL with reason)
- If any critical gate FAIL: user notified before Plan 184-03 proceeds
</verification>

<success_criteria>
update.sh completed exit 0. Deployed SHA = e1f44ce75f0e4fe89aecdddc48381faa04ce7d44.
All 4 services active. Sacred SHA: 25/25. Migration artifact at /root/liv/.
All documented in pushed commit.
</success_criteria>

<output>
After completion, create `.planning/phases/184-v38-deploy-uat/184-02-SUMMARY.md`
</output>
