---
phase: 256-security-hardening-contained-autonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - liv/packages/core/src/shell.ts
  - liv/packages/core/src/sandbox.ts
  - liv/packages/core/src/sandbox.test.ts
  - liv/packages/core/src/daemon.ts
  - liv/packages/core/src/files-sandbox.ts
  - liv/packages/core/src/files-sandbox.test.ts
  - liv/packages/core/src/agent-git-snapshot.ts
  - liv/packages/core/src/agent-git-snapshot.test.ts
  - scripts/install/deploy-livinityd.sh
  - update.sh
requirements: [LIVOS-002]
autonomous: true

must_haves:
  truths:
    - "A `shell` tool command from the agent runs inside bubblewrap: `cat /opt/livos/.env` returns permission-denied / empty, not the secret (SC1)."
    - "A `files` tool read of `/opt/livos/data/secrets/jwt` is rejected by an allowlist before any fs.readFile runs (SC1)."
    - "An agent-tasked `curl https://attacker.example` fails (not on the egress allowlist); `curl https://api.anthropic.com` succeeds (SC2)."
    - "The agent child shell sees no LIV_API_KEY / DATABASE_URL / JWT secret / Redis-PG creds in its environment (SC3)."
    - "Every agent file write is captured to a per-session git snapshot ref so any run is one revert away (reversibility)."
    - "bubblewrap (bwrap) + the egress proxy deps are installed by both installer scripts; OpenDesign/OpenHands + agent autonomy still function (SC7)."
  artifacts:
    - path: "liv/packages/core/src/sandbox.ts"
      provides: "wrapWithBwrap(): builds the bubblewrap argv (workspace-write, deny-read secrets/home-creds/other-homes, no docker.sock, egress via proxy) + the scrubbed child env"
      contains: "bwrap"
    - path: "liv/packages/core/src/files-sandbox.ts"
      provides: "isFilePathAllowed(): realpath allowlist for the `files` tool (per-user workspace only; deny .env/secrets/home-creds)"
      contains: "isFilePathAllowed"
    - path: "liv/packages/core/src/agent-git-snapshot.ts"
      provides: "snapshotWorkspace(): per-session git commit ref of agent file changes"
      contains: "git"
  key_links:
    - from: "liv/packages/core/src/shell.ts ShellExecutor.execute"
      to: "liv/packages/core/src/sandbox.ts wrapWithBwrap"
      via: "exec(bwrap … sh -c <command>) with scrubbed env"
      pattern: "wrapWithBwrap"
    - from: "liv/packages/core/src/daemon.ts files tool execute"
      to: "liv/packages/core/src/files-sandbox.ts isFilePathAllowed"
      via: "guard before fs.readFile/writeFile/rm"
      pattern: "isFilePathAllowed"
    - from: "scripts/install/deploy-livinityd.sh + update.sh apt block"
      to: "bubblewrap + proxy package install"
      via: "DEBIAN_FRONTEND apt-get install bubblewrap"
      pattern: "bubblewrap"
---

<objective>
WS-A — Contained Autonomy. Close LIVOS-002 (the agent's shell/files/docker tools auto-execute
on the host as `bruce` with `permissionMode:'dontAsk'` and a dead approval gate). Per the
operator-locked decision and SECURITY-REMEDIATION-DESIGN.md, KEEP autonomy and move the safety
boundary onto the OS/network: (1) bubblewrap-sandbox the `shell` tool exec, (2) path-allowlist the
`files` tool, (3) route agent egress through a deny-by-default allowlist proxy (breaks the
lethal-trifecta exfil leg), (4) scrub host secrets from the agent child environment, (5) git-snapshot
every agent run for one-revert undo. NO manual per-action approval gate is reintroduced.

Purpose: a fully prompt-injected agent can neither read `/opt/livos/.env` / the JWT secret, nor
phone home, nor leave a non-revertible change — while ordinary autonomous ops keep working.
Output: a `sandbox.ts` bwrap wrapper + scrubbed env, a `files-sandbox.ts` allowlist, an
`agent-git-snapshot.ts` reversibility layer, wired into `shell.ts` + `daemon.ts`, plus the
bubblewrap/proxy apt deps in both installer scripts.

SCOPE NOTE for the operator (confirm before execution): the stripped-context injection-proof
**classifier gate** for irreversible off-box ops (force-push, prod deploy/migration, mass-delete,
IAM grants) is INTENTIONALLY DEFERRED out of this plan. Rationale: the four OS/network layers in
this plan already close the trifecta (no exfil path → injection cannot achieve off-box harm), the
classifier is a net-new subsystem that would push this plan past the context budget, and it is an
"only retained gate" nicety, not a trifecta-leg. If the operator wants it in-phase, it becomes
256-05-PLAN (a dedicated TDD plan). Flagged in the source audit as "may be deferred to a sub-plan."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/256-security-hardening-contained-autonomy/256-CONTEXT.md
@SECURITY-REMEDIATION-DESIGN.md
@SECURITY-AUDIT.md

<interfaces>
<!-- The CURRENT host-exec primitive WS-A wraps. Executor edits these exact shapes. -->

From liv/packages/core/src/shell.ts (the in-process host exec — NOT the SDK subprocess):
```typescript
export class ShellExecutor {
  constructor(cwd = NEXUS_BASE_DIR) { this.cwd = cwd }
  async execute(command: string, timeoutMs = 30_000):
    Promise<{ stdout: string; stderr: string; code: number }>
  // body: exec(command, { cwd: this.cwd, timeout, maxBuffer }, cb)  ← runs as bruce on the host
}
```

From liv/packages/core/src/daemon.ts (the `files` tool, ~:1668-1724 — NO path guard today):
```typescript
toolRegistry.register({
  name: 'files',
  // operation: read|write|list|stat|delete|mkdir, path: <absolute>, content?
  execute: async (params) => {
    const { operation, path, content } = params
    // fs.readFile(path) / fs.writeFile(path) / fs.rm(path,{recursive}) — UNGUARDED
  }
})
```

The `shell` tool (~daemon.ts:1356-1375) calls `shell.execute(cmd, timeout)` — so wrapping
`ShellExecutor.execute` covers the agent's shell primitive without touching the tool registration.

Existing GOOD pattern already present (do NOT regress): sdk-agent-runner.ts:330-339 builds a
`safeEnv` (HOME/PATH/NODE_ENV/LANG/ANTHROPIC_API_KEY only) for the SDK SUBPROCESS. The gap is that
`shell`/`files` run IN-PROCESS in liv-core (full `process.env`), so the scrub must be re-applied at
the bwrap boundary in `sandbox.ts`.
</interfaces>

<egress_proxy_note>
The egress allowlist proxy is a host service reachable by the bwrap'd child via `HTTP_PROXY` /
`HTTPS_PROXY`. Allowlist hosts: `api.anthropic.com`, `generativelanguage.googleapis.com`,
`github.com`, `*.githubusercontent.com`, `registry.npmjs.org`, `registry.npmjs.com`. Use tinyproxy
(`Allow`/`ConnectPort` + `upstream`/filter) installed via apt; the bwrap child gets
`HTTPS_PROXY=http://127.0.0.1:<port>` and `NO_PROXY=` empty so all egress transits the filter. The
proxy config file + systemd unit are emitted by the installer scripts in Task 4.
</egress_proxy_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bubblewrap sandbox + cred-scrub for the `shell` tool (LIVOS-002 / SC1, SC2, SC3)</name>
  <files>liv/packages/core/src/sandbox.ts, liv/packages/core/src/sandbox.test.ts, liv/packages/core/src/shell.ts</files>
  <read_first>
    - liv/packages/core/src/shell.ts:1-57 (the whole ShellExecutor — exec at :39, cwd at :23-24, BLOCKED_PATTERNS at :6-15)
    - liv/packages/core/src/sdk-agent-runner.ts:329-339 (the `safeEnv` allow-list shape to mirror for the scrub)
    - SECURITY-REMEDIATION-DESIGN.md:41-46 (the bwrap deny-read paths + egress + env-scrub spec)
  </read_first>
  <behavior>
    - Test 1 (argv shape): wrapWithBwrap('echo hi', {workspace:'/opt/livos/data/agent-workspace'}) returns
      argv starting with 'bwrap' that includes `--ro-bind /usr /usr`, `--bind <workspace> <workspace>`,
      `--tmpfs /tmp`, and `--die-with-parent`, and ends with `sh -c echo hi`.
    - Test 2 (deny-read secrets): the argv contains NO `--bind`/`--ro-bind` whose source is `/opt/livos/.env`,
      `/opt/livos/data/secrets`, any `~/.ssh`, `~/.claude`, or `~/.gemini`; assert none of these strings
      appear as a bind SOURCE in the argv.
    - Test 3 (no docker.sock): assert `/var/run/docker.sock` does not appear in the argv.
    - Test 4 (env scrub): buildScrubbedEnv(process.env-like {LIV_API_KEY,DATABASE_URL,REDIS_URL,JWT_SECRET,
      PATH,HOME,LANG}) returns an object WITHOUT LIV_API_KEY/DATABASE_URL/REDIS_URL/JWT_SECRET and WITH
      PATH/HOME/LANG, plus `HTTPS_PROXY`/`HTTP_PROXY` set to the configured proxy URL.
    - Test 5 (proxy wired): the returned env has HTTPS_PROXY pointing at the egress proxy (default
      `http://127.0.0.1:13128`, overridable via LIV_EGRESS_PROXY).
  </behavior>
  <action>
1. Create `liv/packages/core/src/sandbox.ts` exporting:
   - `const SANDBOX_DENY_READ = ['/opt/livos/.env','/opt/livos/data/secrets', path.join(os.homedir(),'.ssh'),
     path.join(os.homedir(),'.claude'), path.join(os.homedir(),'.gemini')]` (deny-read list).
   - `buildScrubbedEnv(srcEnv = process.env): NodeJS.ProcessEnv` — start from an empty object, copy ONLY
     `HOME, PATH, LANG, NODE_ENV, TERM, USER` from srcEnv; NEVER copy `LIV_API_KEY, DATABASE_URL, REDIS_URL,
     JWT_SECRET, JWT_SECRET_FILE, ANTHROPIC_API_KEY, GEMINI_API_KEY, PGPASSWORD`. Set
     `HTTPS_PROXY = HTTP_PROXY = process.env.LIV_EGRESS_PROXY || 'http://127.0.0.1:13128'` and `NO_PROXY=''`.
     (Mirrors the sdk-agent-runner safeEnv allow-list philosophy — allow-list, not deny-list, for the copy.)
   - `wrapWithBwrap(command: string, opts: {workspace: string}): { argv: string[]; usable: boolean }` —
     build the bwrap argv: `--unshare-all --share-net` (net stays so the proxy is reachable),
     `--die-with-parent`, `--ro-bind /usr /usr`, `--ro-bind /bin /bin`, `--ro-bind /lib /lib`,
     `--ro-bind /lib64 /lib64`, `--ro-bind /etc/resolv.conf /etc/resolv.conf`, `--proc /proc`, `--dev /dev`,
     `--tmpfs /tmp`, `--bind <opts.workspace> <opts.workspace>`, `--chdir <opts.workspace>`, then
     `sh -c <command>`. Do NOT bind any deny-read path. Do NOT bind `/var/run/docker.sock`. Return
     `usable:false` (with argv:[]) if `bwrap` is not on PATH (probe via `which bwrap` cached at module load)
     so non-Linux/dev boxes degrade gracefully — see Task 1 fallback note.
   - Add a `BWRAP_AVAILABLE` boolean (resolved once via a sync `which bwrap`) export for shell.ts to branch on.
2. In `shell.ts` `execute()`: after the BLOCKED_PATTERNS check, branch — when `BWRAP_AVAILABLE`, build
   `const { argv } = wrapWithBwrap(command, { workspace: this.cwd })` and run
   `execFile('bwrap', argv.slice(1), { timeout, maxBuffer, env: buildScrubbedEnv() })`; otherwise fall back
   to the existing `exec(command, { cwd, timeout, maxBuffer, env: buildScrubbedEnv() })` (still scrub the env
   even when bwrap is unavailable — defense in depth on dev boxes). Keep the existing truncate/timeout/code
   handling intact. Add `import { wrapWithBwrap, buildScrubbedEnv, BWRAP_AVAILABLE } from './sandbox.js'`.

Do NOT remove the BLOCKED_PATTERNS list. Do NOT change `shell.execute`'s return shape. The workspace
`this.cwd` defaults to `LIV_BASE_DIR || '/opt/liv'`; keep that as the write-confined root.
  </action>
  <verify>
    <automated>cd liv && npx vitest run packages/core/src/sandbox.test.ts</automated>
  </verify>
  <done>sandbox.ts exports wrapWithBwrap/buildScrubbedEnv/BWRAP_AVAILABLE; argv denies secret reads + docker.sock + scrubs creds + sets the proxy env; shell.ts routes exec through bwrap when available and scrubs env unconditionally; sandbox.test.ts passes (5 cases).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: path-allowlist the `files` tool (LIVOS-002 / SC1)</name>
  <files>liv/packages/core/src/files-sandbox.ts, liv/packages/core/src/files-sandbox.test.ts, liv/packages/core/src/daemon.ts</files>
  <read_first>
    - liv/packages/core/src/daemon.ts:1668-1724 (the `files` tool execute — read :1682, write :1688, delete/rm :1704-1711, mkdir :1713)
    - SECURITY-AUDIT.md:236-249 (LIVOS-009 — the `files`-tool no-sandbox finding; the luse isPathAllowed contrast)
  </read_first>
  <behavior>
    - Test 1: isFilePathAllowed('/opt/livos/data/agent-workspace/notes.md') → true (inside the write root).
    - Test 2: isFilePathAllowed('/opt/livos/.env') → false; isFilePathAllowed('/opt/livos/data/secrets/jwt') → false.
    - Test 3: isFilePathAllowed(path.join(os.homedir(),'.claude/.credentials.json')) → false (home-cred deny).
    - Test 4 (traversal): isFilePathAllowed('/opt/livos/data/agent-workspace/../../.env') → false (realpath-normalized).
    - Test 5: a denied path returns false WITHOUT throwing (so the tool can return a clean error, not a stack).
  </behavior>
  <action>
1. Create `liv/packages/core/src/files-sandbox.ts` exporting `isFilePathAllowed(p: string): boolean`:
   - Compute `const resolved = path.resolve(p)` then, if the path exists, `fs.realpathSync` it (catch ENOENT
     for not-yet-created write/mkdir targets and fall back to the resolved parent's realpath + basename).
   - ALLOW only when `resolved` starts with the agent workspace root
     `process.env.LIV_AGENT_WORKSPACE || '/opt/livos/data/agent-workspace'` (with a trailing-sep guard so
     `/opt/livos/data/agent-workspace-evil` does not match) OR `/opt/livos/data/uploads`.
   - Explicitly DENY (return false) if resolved is under any of: `/opt/livos/.env`, `/opt/livos/data/secrets`,
     `os.homedir()+'/.ssh'`, `+'/.claude'`, `+'/.gemini'`, `+'/.kimi'` — even if a future allow-root would
     otherwise cover it. Deny wins.
2. In `daemon.ts` `files` tool execute (after the `if (!path) return …` guard at :1677): add
   `if (!isFilePathAllowed(path)) return { success: false, output: '', error: 'Access denied: path outside agent sandbox' }`.
   Add `import { isFilePathAllowed } from './files-sandbox.js'` at the top of daemon.ts.
   Apply the SAME guard to the second `files`-shaped handler region near daemon.ts:917-968 (the router variant
   cited by LIVOS-009) — grep for `fs.readFile(path` to locate it; guard each unguarded fs op there too.

Do NOT change the tool's parameter schema or success-path output. The guard is purely additive.
  </action>
  <verify>
    <automated>cd liv && npx vitest run packages/core/src/files-sandbox.test.ts</automated>
  </verify>
  <done>files-sandbox.ts denies .env/secrets/home-creds/traversal and allows the workspace; daemon.ts `files` tool (both handler sites) rejects denied paths before any fs op; test passes (5 cases).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: per-session git snapshot for reversibility (LIVOS-002 reversibility leg / SC7-safe)</name>
  <files>liv/packages/core/src/agent-git-snapshot.ts, liv/packages/core/src/agent-git-snapshot.test.ts</files>
  <read_first>
    - liv/packages/core/src/sdk-agent-runner.ts:186-189 (sessionId derivation — `this.config.sessionId || randomUUID()`)
    - liv/packages/core/src/shell.ts:23-24 (the workspace cwd that the snapshot tracks)
  </read_first>
  <behavior>
    - Test 1: snapshotWorkspace({workspace: <tmpdir>, sessionId:'s1', when:'pre'}) on a fresh dir creates a git
      repo (if absent) and a commit/ref tagged `livos-agent/s1/pre`; returns the commit sha.
    - Test 2: after writing a file, snapshotWorkspace({…, when:'post'}) creates `livos-agent/s1/post`; the two
      refs differ and `git revert` of post→pre restores the file (assert the file is gone after revert).
    - Test 3: snapshot is best-effort — given a workspace on a filesystem where git is unavailable (inject a
      failing exec), snapshotWorkspace resolves `{ ok: false }` and NEVER throws (must not break agent runs).
  </behavior>
  <action>
Create `liv/packages/core/src/agent-git-snapshot.ts` exporting
`snapshotWorkspace(opts: { workspace: string; sessionId: string; when: 'pre'|'post'; exec?: ExecFn }):
Promise<{ ok: boolean; sha?: string }>`:
- Use an injectable `exec` (default = a thin promisified child_process.exec scoped to `cwd: opts.workspace`)
  so the test can stub failures.
- If `.git` absent: `git init -q && git config user.email livos@local && git config user.name livos-agent`.
- `git add -A` then `git commit -q --allow-empty -m "agent <sessionId> <when>"` and
  `git update-ref refs/livos-agent/<sessionId>/<when> HEAD`. Return `{ ok:true, sha }`.
- Wrap the whole body in try/catch → `{ ok:false }` on any failure (best-effort; reversibility is additive,
  never load-bearing for the run to proceed).
NOTE: wiring snapshotWorkspace into the runner's pre/post-run hooks is left to the SDK runner integration but
is intentionally a thin call (`await snapshotWorkspace({workspace, sessionId, when:'pre'})` before the
`query()` loop and `…when:'post'` after). Add that pair of calls in sdk-agent-runner.ts run() guarded by a
`process.env.LIV_AGENT_SNAPSHOT !== '0'` flag so it can be disabled. (This file's TEST proves the snapshot
mechanics; the two-line runner wiring is verified by the integration probe in Task 5/UAT, not a unit test.)
  </action>
  <verify>
    <automated>cd liv && npx vitest run packages/core/src/agent-git-snapshot.test.ts</automated>
  </verify>
  <done>snapshotWorkspace creates per-session pre/post refs, supports revert-to-pre, and fails soft when git is unavailable; test passes (3 cases). Runner wired with the LIV_AGENT_SNAPSHOT guard.</done>
</task>

<task type="auto">
  <name>Task 4: install bubblewrap + egress proxy in BOTH installer scripts (SC1, SC2, SC7 deploy invariant)</name>
  <files>scripts/install/deploy-livinityd.sh, update.sh</files>
  <read_first>
    - scripts/install/deploy-livinityd.sh:118-126 (the core apt block — `apt-get install -y -qq postgresql … samba`)
    - update.sh (the mirror apt block — grep for `apt-get install -y -qq` near the postgresql/redis line)
    - scripts/install/env-seed.sh:66-74 (the secret-gen region — pattern for where to add nothing here; proxy config goes in deploy)
  </read_first>
  <action>
1. In `scripts/install/deploy-livinityd.sh`, extend the core apt block at :120-125 to ALSO install
   `bubblewrap tinyproxy` (append them to the existing package list on the same `apt-get install -y -qq`
   invocation). Then, after that block's `ok "System packages installed"`, add a heredoc that writes
   `/etc/tinyproxy/livos-egress.conf` with: `Port 13128`, `Listen 127.0.0.1`, `Allow 127.0.0.1`,
   `FilterDefaultDeny Yes`, `Filter /etc/tinyproxy/livos-egress.filter`, and `ConnectPort 443`; and a
   companion `/etc/tinyproxy/livos-egress.filter` allowlist file containing the hostnames
   (`^api\.anthropic\.com$`, `^generativelanguage\.googleapis\.com$`, `^github\.com$`,
   `\.githubusercontent\.com$`, `^registry\.npmjs\.org$`, `^registry\.npmjs\.com$`). Then write a systemd
   drop-in / `systemctl enable --now tinyproxy` invocation pointed at the livos-egress.conf (use
   `tinyproxy -c /etc/tinyproxy/livos-egress.conf` via a `/etc/systemd/system/livos-egress.service` unit so it
   does not collide with any default tinyproxy unit). Guard each step with `|| warn "…(non-fatal)"` so a box
   lacking tinyproxy in its mirror still completes the install (bwrap is the hard requirement; the proxy is
   defense-in-depth and shell.ts already scrubs env without it).
2. In `update.sh`, apply the IDENTICAL apt-list addition (`bubblewrap tinyproxy`) and the IDENTICAL config +
   unit write so the two scripts stay mirror-identical (the repo treats them as paired sites — see Phase 252).
3. Add `bubblewrap` to whatever post-apt verify loop exists (grep `for bin in` in each script) as `bwrap`.

Do NOT remove existing packages. Do NOT change the secret-gen logic in env-seed.sh (LIV_API_KEY seeding is
WS-D Task in 256-04). Keep both scripts byte-identical in the added region.
  </action>
  <verify>
    <automated>bash -n scripts/install/deploy-livinityd.sh && bash -n update.sh && grep -c 'bubblewrap tinyproxy' scripts/install/deploy-livinityd.sh update.sh</automated>
  </verify>
  <done>Both installer scripts install bubblewrap+tinyproxy, write the livos-egress allowlist proxy config+unit, add `bwrap` to the verify loop, and pass `bash -n`; added regions are identical across the two files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → host shell/files exec | the LLM (prompt-injectable) emits tool calls that run as `bruce` on the host |
| agent child process → network | the agent's shell can open outbound connections (exfil leg of the trifecta) |
| agent child process → host secrets | in-process tools see full `process.env` + the whole filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256A-01 | Information Disclosure | `shell` tool reading `/opt/livos/.env` / `secrets/jwt` | mitigate | bwrap deny-read binds (sandbox.ts) — Task 1 |
| T-256A-02 | Information Disclosure | `files` tool reading secrets/home-creds | mitigate | isFilePathAllowed realpath allowlist (Task 2) |
| T-256A-03 | Exfiltration (Info Disc.) | agent egress to attacker host | mitigate | tinyproxy default-deny allowlist + HTTPS_PROXY in scrubbed env (Task 1+4) |
| T-256A-04 | Elevation of Privilege | docker.sock reachable inside agent exec | mitigate | bwrap never binds /var/run/docker.sock (Task 1) |
| T-256A-05 | Information Disclosure | host creds leaked via child env | mitigate | buildScrubbedEnv allow-list strips LIV_API_KEY/DATABASE_URL/JWT/Redis-PG (Task 1) |
| T-256A-06 | Tampering (irreversible) | agent destroys workspace files | mitigate | per-session git snapshot/revert (Task 3) |
| T-256A-07 | Tampering (off-box irreversible) | force-push / prod migration / IAM grant | accept (this plan) | DEFERRED classifier gate — trifecta already broken by T-256A-03; see objective scope note |
</threat_model>

<verification>
- SC1 probe: task the live agent `cat /opt/livos/.env` → returns permission-denied/empty, not the secret.
- SC1 probe: task `files read /opt/livos/data/secrets/jwt` → "Access denied" error, no key bytes.
- SC2 probe: task `curl -sS https://attacker.example` → fails (403/Forbidden from proxy); `curl https://api.anthropic.com/v1` → reaches Anthropic (TLS handshake / 401, NOT a proxy-deny).
- SC3 probe: task `env | grep -E 'LIV_API_KEY|DATABASE_URL|JWT'` → empty.
- SC7 regression: OpenDesign + OpenHands still open and run; the agent completes an ordinary multi-step task with no approval prompt.
- `cd liv && npx vitest run packages/core/src/sandbox.test.ts packages/core/src/files-sandbox.test.ts packages/core/src/agent-git-snapshot.test.ts` all green.
</verification>

<success_criteria>
SC1 (secret-read blocked), SC2 (egress allowlist), SC3 (env scrub) all demonstrated by synthetic probe;
reversibility ref exists per session; bubblewrap+proxy installed by both scripts; SC7 regression clean.
</success_criteria>

<output>
After completion, create `.planning/phases/256-security-hardening-contained-autonomy/256-01-SUMMARY.md`
</output>
