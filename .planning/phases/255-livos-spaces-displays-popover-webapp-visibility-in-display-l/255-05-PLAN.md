---
phase: 255-livos-spaces-displays-popover-webapp-visibility-in-display-l
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - livos/packages/livinityd/source/modules/shell/branded-shell.ts
  - livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png
  - livos/packages/livinityd/source/index.ts
  - update.sh
  - livos/install.sh
autonomous: false
requirements: [GOAL-255-LIVOS-SHELL]
must_haves:
  truths:
    - "On boot, the :1 host display renders a LivOS-branded shell — feh wallpaper + a design-token-themed fluxbox style + a slim tint2 dock — instead of a bare gray fluxbox root"
    - "feh and tint2 are spawned with subprocess-scoped DISPLAY=:1, never mutating server-side global process.env"
    - "The LivOS fluxbox STYLE (colors/fonts) is themed while the existing EMPTY_RC window-management behavior (defaultDeco NONE, fullMaximization) is preserved so WebApp keys/clicks are not swallowed"
    - "feh and tint2 are in the update.sh + install.sh apt lists so they exist on the Mini PC"
    - "A missing binary or spawn failure degrades gracefully (xsetroot solid color / fluxbox toolbar) and never breaks livinityd boot"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/shell/branded-shell.ts"
      provides: "bootBrandedShell — feh + tint2 + fluxbox style boot helper"
      contains: "feh"
    - path: "livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png"
      provides: "LivOS wallpaper filesystem asset (ships via source rsync)"
    - path: "update.sh"
      provides: "feh tint2 in apt list + verify loop"
      contains: "feh"
  key_links:
    - from: "index.ts boot (:1 block)"
      to: "bootBrandedShell"
      via: "call after startFluxbox + :1 registerExisting, inside same try/catch"
      pattern: "bootBrandedShell\\("
    - from: "branded-shell.ts"
      to: "feh/tint2 subprocess"
      via: "spawnFn with env {...process.env, DISPLAY: ':1'}"
      pattern: "DISPLAY: display|DISPLAY: ':1'"
    - from: "update.sh apt block"
      to: "feh tint2 install"
      via: "apt-get install package list"
      pattern: "feh tint2|feh \\\\\\n|tint2"
---

<objective>
Build the §3 in-display LivOS shell: a `bootBrandedShell` helper (modeled on `fluxbox-wm.ts`) that, on the host `:1` display, writes a design-token-themed fluxbox STYLE file, sets the LivOS wallpaper via `feh --bg-fill`, and launches a slim `tint2` dock — all with subprocess-scoped `DISPLAY=:1` and graceful degrade. Ship the wallpaper asset via the livinityd source rsync, wire the helper into boot after the `:1` fluxbox start, and add `feh`/`tint2` to the apt lists. Ends with an operator VNC walk (the visual outcome is human-verify-only).

Purpose: D-255-SHELL-LIVOS-BRANDED — opening `:1` shows a LivOS-branded desktop (wallpaper + dock), not a bare gray fluxbox root. Native feh+fluxbox-style+tint2 (NOT Chromium kiosk — Pitfall 3).
Output: branded-shell helper + wallpaper asset + boot wiring + apt patch; depends on plan 03 (both touch index.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-RESEARCH.md
@.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-PATTERNS.md

<interfaces>
<!-- Extracted from codebase this session — use directly. -->

fluxbox-wm.ts spawn idiom to mirror (L74-104):
```typescript
// idempotent config-file write (L81-86):
try { writeFileSync(rcPath, EMPTY_RC, {encoding: 'utf8'}) } catch (err) { logger?.warn?.(...) }
// subprocess-env-scoped spawn (L96-104):
const child = spawnFn('sudo', ['-n', '-u', user, `DISPLAY=${display}`, 'fluxbox', '-display', display, '-rc', rcPath], {
  detached: true, stdio: ['ignore','pipe','pipe'], env: {...process.env, DISPLAY: display},
}) as ChildProcess
```
EMPTY_RC (fluxbox-wm.ts:63-72) — PRESERVE this behavior (defaultDeco NONE + fullMaximization). The branded shell themes the STYLE, NOT the rc.

Design-token DARK palette (livos/packages/design-tokens/tokens.css, verified):
```
--bg:        #0a0a0c
--bg-2:      #16161a
--card-bg:   #16161a
--fg:        #f5f5f7
--line:      rgb(255 255 255 / .08)
--accent-blue: #2563eb
```

index.ts boot site (index.ts:936-992): startXvfb(:1) → 500ms → startFluxbox({display:':1'}) (L946-950) → :1 registerExisting block (L963-982) — all inside one try; the `} catch (err)` at L983 is the non-fatal fallback. ADD the bootBrandedShell call after L982 (after the :1 register block), still inside the same try.

update.sh apt block (update.sh:359-370): `DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \ ... xvfb fluxbox \` — add feh tint2.
update.sh verify loop (update.sh:386): `for bin in ffmpeg gst-launch-1.0 dbus-send xdotool maim Xvfb fluxbox Xephyr xterm; do` — add feh tint2.
install.sh apt block (livos/install.sh:613-626): `apt-get install -y -qq \ ... xvfb \ fluxbox` — add feh tint2.
install.sh verify loop (livos/install.sh:657): `for bin in ffmpeg gst-launch-1.0 dbus-send vainfo xdotool maim; do` — add feh tint2.

Deploy: livinityd is tsx (no build); source/ rsyncs wholesale (update.sh:433-435), binary assets survive `rsync -a`. Asset reaches `/opt/livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png`.
branded-shell RED test: plan 01 Task 2 (`shell/__tests__/branded-shell.test.ts`) — make GREEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GREEN — branded-shell.ts (feh + themed fluxbox style + tint2, subprocess-scoped, graceful degrade)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/webapps/fluxbox-wm.ts (the WHOLE file — injected spawnFn, idempotent writeFileSync, env {...process.env, DISPLAY}, non-fatal warns)
    - livos/packages/livinityd/source/modules/shell/__tests__/branded-shell.test.ts (plan 01 RED — make GREEN)
    - 255-PATTERNS.md "shell/branded-shell.ts (NEW)" + "Subprocess-scoped DISPLAY env" + "Idempotent boot config-file write" sections
    - 255-RESEARCH.md §3 + Pitfall 3/4/5
  </read_first>
  <files>livos/packages/livinityd/source/modules/shell/branded-shell.ts</files>
  <action>
    Create `bootBrandedShell` mirroring `fluxbox-wm.ts` conventions. Signature:
    ```typescript
    export interface BootBrandedShellOpts {
      display?: string                  // default ':1'
      user?: string                     // default 'bruce'
      wallpaperPath?: string            // default the deployed asset abs path
      stylePath?: string                // default '/tmp/livos-fluxbox-style'
      tint2RcPath?: string              // default '/tmp/livos-tint2rc'
      spawnFn?: typeof import('node:child_process').spawn
      writeFileFn?: (path: string, data: string) => void   // default writeFileSync wrapper
      logger?: {info?: (m: string) => void; warn?: (m: string, e?: unknown) => void; error?: (m: string, e?: unknown) => void}
    }
    export async function bootBrandedShell(opts?: BootBrandedShellOpts): Promise<void>
    ```
    Behavior:
    1. `const display = opts?.display ?? ':1'`. NEVER mutate `process.env` — always pass `env: {...process.env, DISPLAY: display}` to every spawn (the codebase norm at fluxbox-wm.ts:103).
    2. Default `wallpaperPath` = the ABSOLUTE deployed path `/opt/livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png`. (NOT a UI URL — Pitfall 5.) Allow override for tests.
    3. Write the LivOS fluxbox STYLE file idempotently (writeFileFn). Author it STATICALLY from the dark token values (Open Q2 recommends static for v1). Theme menu/toolbar/window colors+fonts using the verified tokens — include literal `#0a0a0c` (bg), `#16161a` (card/toolbar), `#f5f5f7` (fg/text), `#2563eb` (accent/focus). Example keys: `menu.frame.color: #16161a`, `menu.frame.textColor: #f5f5f7`, `menu.hilite.color: #2563eb`, `toolbar.color: #16161a`, `window.focus.title.color: #16161a`, `window.label.focus.textColor: #f5f5f7`, plus a font (use a system font; do not depend on the bundled web fonts which X cannot read). Do NOT re-add window decorations — the STYLE is colors/fonts only; window-management stays governed by EMPTY_RC (Pitfall 4).
    4. Apply the style to fluxbox: write `~/.fluxbox` style is fragile under bruce-ownership (MEMORY feedback_bruce_home_ownership) — instead reference the style via the running fluxbox. Simplest robust path: spawn `sudo -n -u bruce DISPLAY=:1 fluxbox-remote 'setStyle <stylePath>'` (best-effort; if fluxbox-remote unavailable, log warn and continue — the wallpaper+dock still brand the shell). Guard in try/catch.
    5. Set wallpaper: spawn `feh` with args `['--bg-fill', wallpaperPath]` via `sudo -n -u bruce DISPLAY=:1 feh --bg-fill <wallpaperPath>` and `env: {...process.env, DISPLAY: display}`. On spawn error / binary-missing, FALL BACK to `xsetroot -solid '#0a0a0c'` (same env). Re-invoke feh directly each boot — do NOT rely on `~/.fehbg` (Pitfall 5). Best-effort try/catch.
    6. Launch the dock: spawn `tint2` with `-c <tint2RcPath>` via `sudo -n -u bruce DISPLAY=:1 tint2 -c <rc>` and the scoped env. Write a minimal tint2 rc (writeFileFn) themed with the token colors (panel background `#16161a`, font color `#f5f5f7`, a slim height). On spawn error / binary-missing, fall back to leaving the fluxbox toolbar visible (log warn; do not throw). Best-effort try/catch.
    7. Wrap the ENTIRE function body so it NEVER throws out — a binary-missing or spawn failure logs a warn/error and returns normally (graceful degrade; never break boot). Match the plan 01 RED test: assert feh + tint2 spawned with env.DISPLAY === ':1', a style file written containing a token color, process.env.DISPLAY unchanged, and no-throw on spawnFn error. Make the test GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @livos/livinityd test -- branded-shell 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `branded-shell.ts` exists; grep `export async function bootBrandedShell` returns a match.
    - grep `'--bg-fill'` AND `tint2` AND `feh` each return a match.
    - grep `DISPLAY: display` (or `DISPLAY: ':1'`) returns a match for each spawn; grep -c `process.env.DISPLAY =` returns 0 (no global mutation).
    - grep `#0a0a0c` AND `#16161a` AND `#2563eb` each return a match (token-themed style/dock).
    - grep `xsetroot` returns a match (feh fallback).
    - `pnpm --filter @livos/livinityd test -- branded-shell` exits ZERO (GREEN).
  </acceptance_criteria>
  <done>bootBrandedShell themes the fluxbox style + sets feh wallpaper + launches tint2, all subprocess-scoped + graceful-degrade; plan 01 RED test green.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the wallpaper asset + wire bootBrandedShell into boot</name>
  <read_first>
    - livos/packages/livinityd/source/index.ts (L936-992 the :1 boot block; the bootBrandedShell call goes after the :1 registerExisting block at L982, inside the same try)
    - 255-PATTERNS.md "shell/assets/livos-wallpaper.png" + "shell/branded-shell.ts ... Boot wiring" sections
    - Plan 03 already injected displayManager into index.ts — DO NOT revert plan 03's edits; this task only ADDS the bootBrandedShell call + import.
  </read_first>
  <files>livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png, livos/packages/livinityd/source/index.ts</files>
  <action>
    1. Create the asset directory and place a real PNG/JPG at `livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png`. Source image is a design call (Open Q3): copy an existing LivOS-branded wallpaper. Locate a candidate under `livos/packages/ui/public/wallpapers/` (the existing browser wallpapers) and copy ONE into the asset path as `livos-wallpaper.png` (PNG or re-saved JPG named .png is acceptable for feh; prefer copying a .jpg and naming the default path to match the actual file extension you ship — keep branded-shell.ts's default path in exact sync with the shipped filename). Confirm the asset dir is NOT gitignored (`git check-ignore` returns nothing) so it commits + rsyncs.
    2. Add the import at the top of index.ts (with the other module imports): `import {bootBrandedShell} from './modules/shell/branded-shell.js'`.
    3. In the :1 boot try-block, AFTER the `:1` registerExisting block (after index.ts:982, before the `} catch (err)` at L983), add:
       ```typescript
       // Phase 255 (D-255-SHELL-LIVOS-BRANDED) — brand the :1 host shell:
       // feh wallpaper + design-token fluxbox style + slim tint2 dock. Native
       // (NOT Chromium kiosk — Pitfall 3). Non-fatal: bootBrandedShell never
       // throws; a missing feh/tint2 degrades to xsetroot/fluxbox-toolbar.
       await bootBrandedShell({display: ':1', logger: streamingLogger})
       ```
    4. Do NOT touch plan 03's `displayManager: this.displayManager` ctor edit or the disjoint-range allocator change. If a merge conflict on index.ts arises, keep BOTH plan 03's and this plan's additions.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && pnpm exec tsc --noEmit -p . 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The asset file `livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png` exists and is a non-empty binary (size > 1KB).
    - `git check-ignore livos/packages/livinityd/source/modules/shell/assets/livos-wallpaper.png` returns nothing (not ignored).
    - grep `bootBrandedShell({display: ':1'` in index.ts returns a match, inside the :1 boot try-block (after the registerExisting :1 block).
    - grep `displayManager: this.displayManager` in index.ts STILL returns a match (plan 03 edit intact).
    - tsc --noEmit passes for livinityd (import resolves).
  </acceptance_criteria>
  <done>Wallpaper asset shipped under livinityd source; bootBrandedShell called on :1 boot non-fatally; plan 03 edits preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add feh + tint2 to update.sh and install.sh apt lists + verify loops</name>
  <read_first>
    - update.sh (L359-370 apt install block; L386 verify `for bin in` loop)
    - livos/install.sh (L613-626 apt install block; L657 verify `for bin in` loop)
    - 255-PATTERNS.md "update.sh — add feh tint2 to apt" section + Pitfall 7
  </read_first>
  <files>update.sh, livos/install.sh</files>
  <action>
    1. In update.sh, add `feh tint2` to the `apt-get install -y -qq` package list alongside `xvfb fluxbox` (L369). Use a continuation line consistent with the surrounding block style (e.g. add `feh tint2 \` on its own line within the install command).
    2. In update.sh, add `feh tint2` to the verify loop at L386: `for bin in ffmpeg gst-launch-1.0 dbus-send xdotool maim Xvfb fluxbox Xephyr xterm feh tint2; do`.
    3. In livos/install.sh, add `feh` and `tint2` to the apt block (L613-626) alongside `xvfb` / `fluxbox` (same continuation style).
    4. In livos/install.sh, add `feh tint2` to the verify loop at L657: `for bin in ffmpeg gst-launch-1.0 dbus-send vainfo xdotool maim feh tint2; do`.
    5. `apt-get install -y -qq` is a no-op on already-installed packages, so re-running update.sh is safe (no behavior change for boxes that already have them).
  </action>
  <verify>
    <automated>bash -n update.sh && bash -n livos/install.sh && echo "SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep `feh` AND `tint2` in update.sh each return a match in BOTH the apt install block and the verify `for bin in` loop.
    - grep `feh` AND `tint2` in livos/install.sh each return a match in BOTH its apt block and its verify loop.
    - `bash -n` on both scripts reports no syntax errors (prints "SYNTAX OK").
  </acceptance_criteria>
  <done>feh + tint2 are in both deploy scripts' apt lists and verify loops; scripts parse clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator VNC walk — branded :1 shell (wallpaper + dock + themed menus), keys preserved</name>
  <what-built>
    The in-display LivOS shell for the host `:1` display: a LivOS-branded wallpaper (feh), a design-token-themed fluxbox style (dark palette colors/fonts), and a slim tint2 dock — replacing the bare gray fluxbox root. Spawned natively (no Chromium kiosk), subprocess-scoped to DISPLAY=:1, with graceful degrade. feh + tint2 added to the deploy apt lists.
  </what-built>
  <how-to-verify>
    This outcome is VISUAL and human-verify-only (no code-side assertion of the rendered look). Operator runs:
    1. `git push origin master`, then on the Mini PC as root `bash /opt/livos/update.sh` (this installs feh + tint2 and rsyncs the wallpaper asset + restarts livinityd).
    2. After restart, verify feh/tint2 installed: `which feh tint2` returns paths (no ENOENT).
    3. In a browser at https://bruce.livinity.io, open the `:1` display from the new Displays popover (plan 04) — the display opens as an interactive VNC window.
    4. Confirm the `:1` desktop shows: (a) the LivOS wallpaper (NOT a flat gray root), (b) a slim dock (tint2) styled in dark LivOS colors, (c) themed fluxbox menu colors (right-click the root). Confirm it looks like LivOS, NOT Ubuntu/XFCE.
    5. Confirm WebApp keys/clicks still work (open a WebApp display, type into it) — the EMPTY_RC behavior must be preserved (Pitfall 4: themed style must not re-add decorations or swallow keys).
    6. If feh/tint2 somehow failed, confirm graceful degrade: a solid dark `#0a0a0c` root (xsetroot fallback) and the fluxbox toolbar — boot must NOT be broken.
    Take a screenshot of the branded `:1` desktop.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks off (e.g. gray root = feh failed, no dock = tint2 failed, keys swallowed = style re-added decorations).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| livinityd → feh/tint2/fluxbox subprocesses | display id + paths become subprocess argv/env on boot |
| boot helper → server global env | risk of mutating process.env.DISPLAY for all concurrent requests |
| shipped asset → host filesystem | a binary asset rsynced to /opt and read by feh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-255-15 | Information disclosure / tampering | mutating global process.env.DISPLAY server-side leaking display context to concurrent requests | mitigate | Every spawn uses `env: {...process.env, DISPLAY: display}` (the fluxbox-wm.ts:103 norm); process.env is NEVER mutated. The plan 01 RED test asserts process.env.DISPLAY is unchanged after bootBrandedShell. |
| T-255-16 | Tampering | display/path args injecting shell-meta into feh/tint2 argv | mitigate | Display is the literal `:1` (not user input). Paths are server-controlled constants (deployed asset path / /tmp rc paths). Spawn uses argv arrays (execFile-style via spawn, not a shell string) so no shell interpolation. No user input reaches these argv. |
| T-255-17 | Denial of service | a missing binary or failed spawn crashing livinityd boot | mitigate | bootBrandedShell wraps everything best-effort and NEVER throws; the boot call site is already inside a non-fatal try/catch (index.ts:983). feh→xsetroot, tint2→fluxbox-toolbar fallbacks. |
| T-255-18 | Elevation of privilege | spawning as bruce via sudo -n | accept | Mirrors the EXISTING startFluxbox `sudo -n -u bruce` pattern (fluxbox-wm.ts:88-96) already trusted in 100-08; NOPASSWD scope unchanged. No new privilege surface introduced. |
</threat_model>

<verification>
- `pnpm --filter @livos/livinityd test` green including branded-shell; livinityd tsc --noEmit clean.
- No global process.env.DISPLAY mutation (grep). All spawns subprocess-scoped to :1.
- Style themes colors/fonts only; EMPTY_RC behavior untouched (no decorations re-added).
- feh + tint2 in both update.sh and install.sh apt lists + verify loops; scripts parse clean.
- Plan 03's index.ts edits preserved (displayManager DI + disjoint range still present).
- Operator VNC walk approved (visual outcome is human-verify-only).
</verification>

<success_criteria>
Opening `:1` shows a LivOS-branded desktop (wallpaper + slim dock + themed menus), not a bare gray root; WebApp keys/clicks still work; a missing binary degrades gracefully without breaking boot; feh/tint2 ship via the deploy scripts — all confirmed by the operator VNC walk.
</success_criteria>

<output>
After completion, create `.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-05-SUMMARY.md`
</output>
