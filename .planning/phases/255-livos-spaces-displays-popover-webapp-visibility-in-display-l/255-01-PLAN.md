---
phase: 255-livos-spaces-displays-popover-webapp-visibility-in-display-l
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/computer-use/__tests__/trpc-router-screenshot.test.ts
  - livos/packages/livinityd/source/modules/shell/__tests__/branded-shell.test.ts
  - livos/packages/ui/src/modules/desktop/clock-helpers.test.ts
autonomous: true
requirements: [GOAL-255-LIVE-THUMBS, GOAL-255-LIVOS-SHELL, GOAL-255-NAVBAR-GLOWUP]
must_haves:
  truths:
    - "Three new test files exist and FAIL (RED) because the modules they test do not yet exist"
    - "trpc-router-screenshot test asserts FORBIDDEN for a foreign non-admin caller and dataUrl shape for an owned display"
    - "branded-shell test asserts feh/tint2/fluxbox-style argv shape via an injected spawnFn"
    - "clock-helpers test asserts wmoGlyph code→glyph mapping and Turkish greeting by hour"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/computer-use/__tests__/trpc-router-screenshot.test.ts"
      provides: "RED auth + dataUrl tests for displays.screenshot"
      contains: "displays.screenshot"
    - path: "livos/packages/livinityd/source/modules/shell/__tests__/branded-shell.test.ts"
      provides: "RED argv-shape tests for branded shell boot"
      contains: "feh"
    - path: "livos/packages/ui/src/modules/desktop/clock-helpers.test.ts"
      provides: "RED wmoGlyph + greeting tests"
      contains: "wmoGlyph"
  key_links:
    - from: "trpc-router-screenshot.test.ts"
      to: "canAccessDisplay / displays.screenshot"
      via: "import + invocation"
      pattern: "displays\\.screenshot|canAccessDisplay"
    - from: "clock-helpers.test.ts"
      to: "desktop/clock-helpers.ts"
      via: "import { wmoGlyph, greeting }"
      pattern: "from '\\./clock-helpers'"
---

<objective>
Create the Wave 0 RED test scaffolds for the three testable pure/logic seams in Phase 255, so every subsequent GREEN plan has a failing test to satisfy (Nyquist rule). These tests MUST fail on first run because the production modules/handlers they exercise do not yet exist.

Purpose: Lock the observable contracts (screenshot authorization, branded-shell spawn argv, clock helpers) BEFORE implementation, per the 254 TDD norm.
Output: Three failing test files. No production code in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-RESEARCH.md
@.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from codebase this session. -->

canAccessDisplay (livos/packages/livinityd/source/modules/computer-use/trpc-router.ts:64-72) — REUSE, already unit-tested:
```typescript
export function canAccessDisplay(input: {
  ownerSession: string; callerSession: string; callerRole: string
}): boolean {
  if (!input.ownerSession) return true              // host/shared
  if (input.callerRole === 'admin') return true     // 254-06 admin bypass
  return input.callerSession === input.ownerSession // owner only
}
```

displayIdSchema (trpc-router.ts:43): `z.string().regex(/^:\d+(\.\d+)?$/)`

Target NEW handler shape (does not exist yet — this is what the screenshot test drives):
```typescript
// displays.screenshot: query, input {display}, returns {dataUrl, width, height}
// throws UNAUTHORIZED (no ctx.currentUser), SERVICE_UNAVAILABLE (no displayManager),
//   NOT_FOUND (no display record), FORBIDDEN (canAccessDisplay false)
```

Target NEW pure module (does not exist yet — this is what clock-helpers test drives):
```typescript
// livos/packages/ui/src/modules/desktop/clock-helpers.ts
export function wmoGlyph(code: number): string
export function greeting(hour: number, name?: string): string
```

Target NEW boot helper (does not exist yet — this is what branded-shell test drives):
```typescript
// livos/packages/livinityd/source/modules/shell/branded-shell.ts
// bootBrandedShell({display, spawnFn, wallpaperPath, ...}) spawns feh + tint2 with
//   env {...process.env, DISPLAY: display}, writes a fluxbox style file idempotently.
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — displays.screenshot authorization + dataUrl tests</name>
  <read_first>
    - livos/packages/livinityd/source/modules/computer-use/__tests__/trpc-router-authz.test.ts (the model — how canAccessDisplay + the displays router are exercised in isolation)
    - livos/packages/livinityd/source/modules/computer-use/trpc-router.ts (lines 64-148: canAccessDisplay + getVncUrl auth shape to mirror)
    - 255-PATTERNS.md "Source-text invariant test" + "Display authorization" shared-pattern sections
  </read_first>
  <files>livos/packages/livinityd/source/modules/computer-use/__tests__/trpc-router-screenshot.test.ts</files>
  <behavior>
    - Test 1 (auth pure): canAccessDisplay({ownerSession:'userB', callerSession:'userA', callerRole:'member'}) === false → a foreign member cannot screenshot.
    - Test 2 (auth pure): canAccessDisplay({ownerSession:'userB', callerSession:'userA', callerRole:'admin'}) === true → 254-06 admin bypass holds for screenshots.
    - Test 3 (auth pure): canAccessDisplay({ownerSession:'', callerSession:'userA', callerRole:'member'}) === true → host/shared :1 screenshot allowed.
    - Test 4 (handler shape): invoking the screenshot handler with no ctx.currentUser throws a TRPCError code 'UNAUTHORIZED'.
    - Test 5 (handler shape): invoking with a currentUser but ctx.livinityd.displayManager undefined throws 'SERVICE_UNAVAILABLE'.
    - Test 6 (handler shape): a valid owned display returns an object whose dataUrl matches /^data:image\/jpeg;base64,/ and exposes numeric width+height.
    - Tests 4-6 use a fake ctx + a fake displayManager whose list() returns a record with owner_session, and a stubbed captureScreenshot returning {base64:'AAAA', mimeType:'image/jpeg', width:1280, height:720}.
  </behavior>
  <action>
    Create the test file modeled on trpc-router-authz.test.ts. Import canAccessDisplay from '../trpc-router.js' for Tests 1-3 (this export already exists). For Tests 4-6, import the screenshot procedure resolver — since displays.screenshot does NOT exist yet, write the import as `import {displaysRouter} from '../trpc-router.js'` and call `displaysRouter._def.procedures.screenshot` (this will be undefined now → the test that calls it throws/fails, giving RED). Build a fake ctx: `{currentUser: {id:'userA', role:'member'}, livinityd: {displayManager: fakeDm}, logger: {log(){}}}`. fakeDm.list() resolves to `[{display:':10', owner_session:'userA', width:1280, height:720}]`. vi.mock('../native/screenshot.js', () => ({captureScreenshot: vi.fn().mockResolvedValue({base64:'AAAA', mimeType:'image/jpeg', width:1280, height:720})})). Use vitest `describe`/`it`/`expect`. The file MUST fail on `pnpm --filter @livos/livinityd test -- trpc-router-screenshot` because `displaysRouter._def.procedures.screenshot` is undefined.
  </action>
  <verify>
    <automated>pnpm --filter @livos/livinityd test -- trpc-router-screenshot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/livinityd/source/modules/computer-use/__tests__/trpc-router-screenshot.test.ts` exists (grep `displays.screenshot` returns a match in it).
    - Running the vitest command exits NON-ZERO (RED) with at least one failing assertion referencing the missing screenshot procedure.
    - grep `canAccessDisplay` in the file returns ≥3 matches (Tests 1-3).
    - grep `UNAUTHORIZED` AND `SERVICE_UNAVAILABLE` AND `data:image/jpeg;base64` each return a match.
  </acceptance_criteria>
  <done>Test file exists, RED, and asserts the full auth matrix + dataUrl shape.</done>
</task>

<task type="tdd">
  <name>Task 2: RED — branded-shell boot argv tests</name>
  <read_first>
    - livos/packages/livinityd/source/modules/webapps/fluxbox-wm.ts (the analog: injected spawnFn, env {...process.env, DISPLAY: display} at L96-104, idempotent config write at L74-86, and its sibling test file pattern)
    - 255-PATTERNS.md "shell/branded-shell.ts" + "Idempotent boot config-file write + injected spawnFn + non-fatal try/catch" sections
  </read_first>
  <files>livos/packages/livinityd/source/modules/shell/__tests__/branded-shell.test.ts</files>
  <behavior>
    - Test 1: bootBrandedShell({display:':1', spawnFn, wallpaperPath:'/abs/livos-wallpaper.png', writeFileFn, ...}) calls spawnFn with the feh binary and args containing '--bg-fill' and the absolute wallpaperPath, and env.DISPLAY === ':1' (subprocess-scoped, NOT global process.env).
    - Test 2: spawnFn is also called for 'tint2' with env.DISPLAY === ':1'.
    - Test 3: a fluxbox STYLE file is written (writeFileFn called) with a path under the fluxbox styles dir and content containing a LivOS token color (e.g. '#0a0a0c' or '#2563eb').
    - Test 4: process.env.DISPLAY is NOT mutated by bootBrandedShell (assert it is the same before/after).
    - Test 5: if spawnFn throws (binary missing), bootBrandedShell does NOT throw (non-fatal degrade).
  </behavior>
  <action>
    Create the directory `livos/packages/livinityd/source/modules/shell/__tests__/` and the test file. Import `import {bootBrandedShell} from '../branded-shell.js'` — this module does NOT exist yet, so the import resolution / call fails → RED. Pass an injected `spawnFn = vi.fn(() => ({on(){}, unref(){}, pid: 123}))` and `writeFileFn = vi.fn()`. Capture the env argument: assert `spawnFn.mock.calls.find(c => c[0] === 'feh')[2].env.DISPLAY === ':1'`. For Test 4: snapshot `const before = process.env.DISPLAY` then `expect(process.env.DISPLAY).toBe(before)` after the call. For Test 5: `spawnFn.mockImplementation(() => { throw new Error('ENOENT') })` and `await expect(bootBrandedShell({...})).resolves.not.toThrow()` (or wrap and assert no throw). Mirror fluxbox-wm.ts's test conventions. The file MUST fail because branded-shell.ts does not exist.
  </action>
  <verify>
    <automated>pnpm --filter @livos/livinityd test -- branded-shell 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists; grep `feh` and `tint2` and `--bg-fill` each return a match.
    - grep `DISPLAY` shows an assertion that env.DISPLAY === ':1' (subprocess-scoped).
    - grep `before` / a not-mutated assertion exists for process.env.DISPLAY (Test 4).
    - Running the vitest command exits NON-ZERO (RED) — branded-shell.js unresolved.
  </acceptance_criteria>
  <done>Test file exists, RED, asserts feh/tint2 argv + subprocess-scoped DISPLAY + non-global-mutation + non-fatal degrade.</done>
</task>

<task type="tdd">
  <name>Task 3: RED — clock-helpers wmoGlyph + greeting tests</name>
  <read_first>
    - livos/packages/ui/src/modules/desktop/top-bar.tsx (lines 451-567: useLocationWeather + ClockWithLocation — the source of the logic being extracted)
    - 255-PATTERNS.md "desktop/clock-helpers.ts" section (the exact WMO map + Turkish greeting boundaries)
    - 255-RESEARCH.md §5 Code Examples (wmoGlyph mapping)
  </read_first>
  <files>livos/packages/ui/src/modules/desktop/clock-helpers.test.ts</files>
  <behavior>
    - wmoGlyph: 0→'☀️', 1→'⛅', 2→'⛅', 3→'☁️', 45→'🌫️', 48→'🌫️', 51→'🌧️', 67→'🌧️', 71→'❄️', 77→'❄️', 80→'🌧️', 82→'🌧️', 95→'⛈️', 99→'⛈️', and an unknown high code like 200→'☁️' (fallback).
    - greeting(hour, name): hour=2 → 'İyi geceler, Bruce'; hour=9 → 'Günaydın, Bruce'; hour=14 → 'İyi günler, Bruce'; hour=20 → 'İyi akşamlar, Bruce'. With no name: 'Günaydın' (no trailing comma+name).
  </behavior>
  <action>
    Create the vitest file. `import {wmoGlyph, greeting} from './clock-helpers'` — module does NOT exist yet → RED. Write parametrized `it.each([...])` covering every boundary above for wmoGlyph (codes 0,1,2,3,45,48,51,67,71,77,80,82,95,99,200). For greeting use the exact Turkish strings from PATTERNS.md: h<6 'İyi geceler' | h<12 'Günaydın' | h<18 'İyi günler' | else 'İyi akşamlar'; with name → `${greet}, ${name}`, without name → just `${greet}`. Run with the ui vitest config.
  </action>
  <verify>
    <automated>pnpm --filter ui test -- clock-helpers 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/ui/src/modules/desktop/clock-helpers.test.ts` exists.
    - grep `from './clock-helpers'` returns a match.
    - grep `İyi geceler` AND `Günaydın` AND `İyi günler` AND `İyi akşamlar` each return a match.
    - Running the vitest command exits NON-ZERO (RED) — clock-helpers.ts unresolved.
  </acceptance_criteria>
  <done>Test file exists, RED, asserts full WMO map (incl. fallback) + 4 Turkish greeting bands + name/no-name.</done>
</task>

</tasks>

<verification>
- All three test files exist in the listed paths.
- Each fails (RED) for the right reason: the production module/handler it imports does not yet exist.
- No production code was created in this plan (grep confirms no `displays-popover.tsx`, `clock-helpers.ts`, `branded-shell.ts` source files yet).
- `pnpm --filter @livos/livinityd test` and `pnpm --filter ui test` both show NEW failing tests (not pre-existing breakage).
</verification>

<success_criteria>
Three RED test files committed, each failing because its target module/handler is unimplemented. These become the GREEN gates for plans 02, 04, and 05.
</success_criteria>

<output>
After completion, create `.planning/phases/255-livos-spaces-displays-popover-webapp-visibility-in-display-l/255-01-SUMMARY.md`
</output>
