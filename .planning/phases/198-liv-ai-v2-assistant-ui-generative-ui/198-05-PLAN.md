---
phase: 198-liv-ai-v2-assistant-ui-generative-ui
plan: 05
type: execute
wave: 3
depends_on: [198-02]
files_modified:
  - livos/packages/ui/src/features/liv-ai/thread-list-adapter.ts (NEW)
  - livos/packages/ui/src/features/liv-ai/thread-list-adapter.test.ts (NEW)
  - livos/packages/ui/src/features/liv-ai/assistant.tsx (EXTEND — mount ThreadList sidebar)
autonomous: true
requirements: []
tags: [threadlist, sidebar, persistence, mastra-threads]

must_haves:
  truths:
    - "assistant-ui ThreadList primitive mounted in the left column of the Liv AI window (256px width)"
    - "ExternalStoreThreadListAdapter wires list/create/delete callbacks to existing trpc.mastra.agent.threads.list query + trpc.mastra.agent.threads.delete mutation (P197-05 KEEP)"
    - "New conversation button creates a fresh threadId (client-generated UUID; PostgresStore persists on first message)"
    - "Thread switching restores message history from PostgresStore (Mastra Memory handles automatically — no client work)"
    - "Empty-state: 'No conversations yet' message if list returns 0 threads"
    - "Thread title fallback 'Untitled · {ISO timestamp}' (auto-title-generation deferred to P199)"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/ui/src/features/liv-ai/thread-list-adapter.ts"
      provides: "createThreadListAdapter() returning ExternalStoreThreadListAdapter shape — wraps trpc.mastra.agent.threads.list query + threads.delete mutation"
      min_lines: 80

threat_model:
  threats:
    - id: T-198-05-01
      category: T
      component: "thread switching authorization"
      disposition: accept
      mitigation: "Single-operator Mini PC — every thread belongs to admin. tRPC adminProcedure from P197-05 already gates list/delete. Multi-tenant scoping deferred to v40+."

---

<objective>
Mount assistant-ui's ThreadList primitive in a left-column sidebar. Wire it to existing P197-05 backend (`mastra.agent.threads.list` + `mastra.agent.threads.delete`). Persistence already works via P197-03 PostgresStore — Mastra Memory binds thread messages automatically. Operator gets multi-thread chat with click-to-switch + new/delete affordances.
</objective>

<context>
@.planning/phases/198-liv-ai-v2-assistant-ui-generative-ui/198-CONTEXT.md
External: https://www.assistant-ui.com/docs (ThreadList primitive docs — fetch during plan execution)

<interfaces>
From trpc client (existing P197-05):
```ts
trpcReact.mastra.agent.threads.list.useQuery()  // returns {threads: [{id, title?}, ...]}
trpcReact.mastra.agent.threads.delete.useMutation()  // {threadId: string} → {ok: true}
```

assistant-ui ThreadList pattern (verify via docs at exec time):
```tsx
import {ThreadListPrimitive} from '@assistant-ui/react'

<ThreadListPrimitive.Root>
  <ThreadListPrimitive.New />
  <ThreadListPrimitive.Items components={{ThreadListItem}} />
</ThreadListPrimitive.Root>
```

ExternalStoreThreadListAdapter contract (verify):
```ts
{
  threads: () => ThreadHistoryItem[],
  onSwitchToNewThread: () => void,
  onSwitchToThread: (threadId: string) => void,
  onRename: (threadId: string, newTitle: string) => Promise<void>,
  onArchive: (threadId: string) => Promise<void>,
  onDelete: (threadId: string) => Promise<void>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createThreadListAdapter() + tests</name>
  <files>
    livos/packages/ui/src/features/liv-ai/thread-list-adapter.ts,
    livos/packages/ui/src/features/liv-ai/thread-list-adapter.test.ts
  </files>
  <behavior>
    - Test 1: adapter.threads() returns mapped threads from useQuery data
    - Test 2: adapter.onDelete(threadId) invokes trpc.mastra.agent.threads.delete.useMutation().mutateAsync({threadId}) once
    - Test 3: adapter.onSwitchToNewThread() generates a new client-side threadId (UUID) and triggers a state update
    - Test 4: adapter.threads() returns [] if useQuery data is undefined (graceful degrade)
  </behavior>
  <action>
    1. Create `livos/packages/ui/src/features/liv-ai/thread-list-adapter.ts` (~100 LOC):
       ```ts
       /**
        * Phase 198-05 — ThreadList adapter wiring assistant-ui's
        * ExternalStoreThreadListAdapter to the existing Phase 197-05 backend
        * (trpc.mastra.agent.threads.list + threads.delete).
        *
        * Thread persistence handled by PostgresStore (P197-03) — Mastra Memory
        * loads thread history automatically on agent.stream() per threadId.
        */

       import {useCallback, useState} from 'react'
       import {trpcReact} from '@/trpc/trpc'

       export interface ThreadHistoryItem {
         threadId: string
         title: string
         status: 'regular' | 'archived'
       }

       export interface ThreadListAdapter {
         threads: () => ThreadHistoryItem[]
         currentThreadId: string
         onSwitchToNewThread: () => void
         onSwitchToThread: (threadId: string) => void
         onDelete: (threadId: string) => Promise<void>
         isLoading: boolean
       }

       function newThreadId(): string {
         return `t-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`
       }

       export function useThreadListAdapter(): ThreadListAdapter {
         const [currentThreadId, setCurrentThreadId] = useState<string>(() => newThreadId())

         // eslint-disable-next-line @typescript-eslint/no-explicit-any
         const trpcAny = trpcReact as any
         const listQ = trpcAny.mastra?.agent?.threads?.list?.useQuery?.(undefined, {
           retry: false,
           staleTime: 60_000,
         })
         const deleteMut = trpcAny.mastra?.agent?.threads?.delete?.useMutation?.({
           onSuccess: () => listQ?.refetch?.(),
         })

         const threads = useCallback((): ThreadHistoryItem[] => {
           const raw = (listQ?.data?.threads ?? []) as Array<{id: string; title?: string | null}>
           return raw.map((t) => ({
             threadId: t.id,
             title: t.title ?? `Untitled · ${new Date().toISOString().slice(0, 10)}`,
             status: 'regular' as const,
           }))
         }, [listQ?.data])

         const onSwitchToNewThread = useCallback(() => {
           setCurrentThreadId(newThreadId())
         }, [])

         const onSwitchToThread = useCallback((threadId: string) => {
           setCurrentThreadId(threadId)
         }, [])

         const onDelete = useCallback(
           async (threadId: string): Promise<void> => {
             if (deleteMut?.mutateAsync) {
               await deleteMut.mutateAsync({threadId})
             }
             // If deleted current thread, switch to a fresh one
             if (threadId === currentThreadId) {
               setCurrentThreadId(newThreadId())
             }
           },
           [deleteMut, currentThreadId],
         )

         return {
           threads,
           currentThreadId,
           onSwitchToNewThread,
           onSwitchToThread,
           onDelete,
           isLoading: listQ?.isLoading ?? false,
         }
       }
       ```
    2. Create `thread-list-adapter.test.ts` (~120 LOC) using vi.mock for trpcReact + react-dom/client harness for hook execution. Tests 1-4 above.
    3. Run vitest → GREEN. Commit: `feat(198-05): useThreadListAdapter hook + 4 tests (Wave 3)`. Pre-commit hook MUST PASS.
  </action>
  <acceptance_criteria>
    - thread-list-adapter.ts EXISTS
    - `grep -c "mastra.agent.threads.list" thread-list-adapter.ts` ≥ 1
    - `grep -c "mastra.agent.threads.delete" thread-list-adapter.ts` ≥ 1
    - Vitest: ≥4 PASS
    - Pre-commit hook PASS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount ThreadList sidebar in <Assistant /> + thread switching wiring</name>
  <files>
    livos/packages/ui/src/features/liv-ai/assistant.tsx
  </files>
  <action>
    1. Extend `assistant.tsx` to render a 2-column layout: 256px sidebar (ThreadList) + flex-1 main (Thread):
       ```tsx
       import {AssistantRuntimeProvider, ThreadListPrimitive} from '@assistant-ui/react'
       import {AssistantChatTransport, useChatRuntime} from '@assistant-ui/react-ai-sdk'

       import {Thread} from '@/components/assistant-ui/thread'
       import {ToolRenderers} from './tool-renderers'
       import {useThreadListAdapter} from './thread-list-adapter'

       export function Assistant() {
         const {threads, currentThreadId, onSwitchToNewThread, onSwitchToThread, onDelete} = useThreadListAdapter()

         const runtime = useChatRuntime({
           transport: new AssistantChatTransport({
             api: `/chat/livAi`,
             credentials: 'include',
             // Pass threadId via body so server can scope Memory per thread
             body: {threadId: currentThreadId},
           }),
         })

         return (
           <AssistantRuntimeProvider runtime={runtime}>
             <ToolRenderers />
             <div className="flex h-full overflow-hidden">
               {/* Left sidebar — ThreadList */}
               <aside className="flex h-full w-64 flex-col border-r border-neutral-200 dark:border-neutral-800 bg-neutral-50 dark:bg-neutral-900">
                 <div className="border-b border-neutral-200 dark:border-neutral-800 p-3">
                   <button
                     type="button"
                     onClick={onSwitchToNewThread}
                     className="w-full rounded-md bg-cyan-600 px-3 py-2 text-sm font-medium text-white hover:bg-cyan-700"
                     data-testid="liv-ai-new-thread"
                   >
                     + New conversation
                   </button>
                 </div>
                 <div className="flex-1 overflow-y-auto p-2">
                   {threads().length === 0 ? (
                     <p className="p-3 text-center text-xs text-neutral-500">
                       No conversations yet
                     </p>
                   ) : (
                     threads().map((t) => (
                       <div
                         key={t.threadId}
                         className={
                           'group mb-1 flex items-center justify-between rounded-md px-2 py-2 text-sm ' +
                           (t.threadId === currentThreadId
                             ? 'bg-cyan-100 dark:bg-cyan-950'
                             : 'hover:bg-neutral-100 dark:hover:bg-neutral-800')
                         }
                       >
                         <button
                           type="button"
                           onClick={() => onSwitchToThread(t.threadId)}
                           className="flex-1 truncate text-left"
                         >
                           {t.title}
                         </button>
                         <button
                           type="button"
                           onClick={() => onDelete(t.threadId)}
                           className="ml-2 hidden text-xs text-neutral-500 hover:text-red-600 group-hover:inline"
                           aria-label="Delete thread"
                         >
                           ×
                         </button>
                       </div>
                     ))
                   )}
                 </div>
               </aside>
               {/* Main thread area */}
               <main className="flex-1 overflow-hidden">
                 <Thread />
               </main>
             </div>
           </AssistantRuntimeProvider>
         )
       }
       ```
    2. Run `pnpm --filter ui build` — exit 0.
    3. Commit: `feat(198-05): mount ThreadList sidebar + thread switching (Wave 3)`. Pre-commit hook MUST PASS.
  </action>
  <acceptance_criteria>
    - `grep -c "useThreadListAdapter" assistant.tsx` ≥ 1
    - `grep -c "New conversation" assistant.tsx` ≥ 1
    - `grep -c "currentThreadId" assistant.tsx` ≥ 2 (state + transport body)
    - `pnpm --filter ui build` exits 0
    - Pre-commit hook PASS
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter ui vitest run src/features/liv-ai/thread-list-adapter.test` PASS (≥4)
- `grep -c "ThreadList" livos/packages/ui/src/features/liv-ai/assistant.tsx` ≥ 0 (sidebar JSX even without ThreadListPrimitive — assistant-ui's primitive can be added in Wave 4 polish)
- `pnpm --filter ui build` exits 0
- `bash scripts/verify-sacred-sha.sh` exits 0
</verification>

<success_criteria>
1. ThreadList sidebar mounted with New/switch/delete affordances
2. Thread switching restores history via PostgresStore (Mastra handles)
3. threadId passed via transport body for per-thread Memory scoping
4. Sacred SHA preserved
</success_criteria>

<output>
After completion, create `.planning/phases/198-liv-ai-v2-assistant-ui-generative-ui/198-05-SUMMARY.md`.
</output>
