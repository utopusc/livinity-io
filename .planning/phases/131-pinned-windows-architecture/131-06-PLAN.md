---
phase: 131-pinned-windows-architecture
plan: 06
type: execute
wave: 5
depends_on: ["131-01", "131-02", "131-03", "131-04", "131-05"]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/phases/131-pinned-windows-architecture/131-SUMMARY.md
  - .planning/phases/131-pinned-windows-architecture/131-UAT-CHECKLIST.md
  - memory/project_phase_131_complete.md (auto-memory)
autonomous: false
requirements:
  - V36-PIN-06
tags:
  - uat
  - docs
  - close-out
must_haves:
  truths:
    - "Every flow from 131-01 through 131-05 walked on Mini PC successfully (or explicitly deferred to a follow-up phase)."
    - "PROJECT.md has a new chapter `## Pinned-Windows Architecture` describing the data flow + per-app session contracts."
    - "STATE.md current_phase = 131 with status SHIPPED + all 6 plan commit hashes recorded."
    - "Auto-memory has a project_phase_131_complete.md entry summarizing decisions made (D-131-A through G actual values, deviations from the master plan, follow-up phases identified)."
    - "Sacred SHA invariant: f3538e1d811992b782a9bb057d1b7f0a0189f95f."
  artifacts:
    - path: ".planning/phases/131-pinned-windows-architecture/131-SUMMARY.md"
      provides: "Full phase summary with all 6 sub-plan commit hashes + decisions + carry-overs to v37"
      contains: "PHASE_131_SHIPPED"
    - path: ".planning/phases/131-pinned-windows-architecture/131-UAT-CHECKLIST.md"
      provides: "Operator walk checklist with PASS/FAIL boxes per flow"
      contains: "UAT TARGET: Mini PC"
---

<objective>
Close Phase 131. Walk every flow on Mini PC, record results in a UAT checklist, document the new architecture in PROJECT.md, snapshot the decisions to auto-memory, and update STATE.md.

This plan is `autonomous: false` because the operator must walk the live Mini PC tests.
</objective>

<tasks>

<task id="06-01" type="execute">
<action>
Write `131-UAT-CHECKLIST.md` with all flows from 131-01 through 131-05 as PASS/FAIL checkboxes. Each flow lists: precondition, action, expected result, pass/fail box.

Operator runs `bash /opt/livos/update.sh` on Mini PC, then walks each flow, checking boxes.
</action>
<acceptance_criteria>
- Checklist exists with all 5 flows broken into individual steps.
- Contains `UAT TARGET: Mini PC`.
</acceptance_criteria>
</task>

<task id="06-02" type="execute">
<action>
Update `.planning/PROJECT.md` with a new chapter:

```markdown
## Pinned-Windows Architecture (Phase 131, shipped {date})

Pinned windows are an OS-shaped persistence layer:

- **UI:** TopBar shelf shows chips for each pinned window. Drag to pin, click to restore.
- **Persistence (Plan 131-02):** Postgres `pinned_windows` table per user, refresh-survives.
- **Background runtime (Plan 131-03):** livinityd hosts the live session per pinned window. Per-app `PinnedSession` contracts. Tab close survives.
- **AI control (Plan 131-04):** MCP server `livos-pinned-windows` exposes list/read/send-input/wait-for tools. Subscription-only path (sdk-agent-runner unchanged).
- **Shelf UX (Plan 131-05):** Hover preview, drag-off-unpin, right-click menu, reorder.

[Data-flow diagram]
```

Cite each sub-plan + the canonical decision document (CONTEXT.md `## Locked Decisions`).
</action>
<acceptance_criteria>
- PROJECT.md has the new chapter.
- Chapter cites all 6 sub-plans.
</acceptance_criteria>
</task>

<task id="06-03" type="execute">
<action>
Write `131-SUMMARY.md` with all 6 plan commit hashes, decisions made, sacred SHA verification per commit, baseline tsc count delta, follow-ups identified for v37.

Update `STATE.md`:
- `current_phase: 131`
- `current_phase_status: SHIPPED`
- `phase_131_commits: [list of all hashes]`
- `next_phase: 132 (TBD)`

Save auto-memory `project_phase_131_complete.md` with:
- Date shipped
- The locked D-131-A through G values
- What was different from the master plan
- Any carry-overs to v37
</action>
<acceptance_criteria>
- Summary contains `PHASE_131_SHIPPED`.
- STATE.md updated.
- Memory file exists.
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
git ls-tree HEAD liv/packages/core/src/sdk-agent-runner.ts
test -f .planning/phases/131-pinned-windows-architecture/131-SUMMARY.md && echo OK
grep -c "PHASE_131_SHIPPED" .planning/phases/131-pinned-windows-architecture/131-SUMMARY.md
grep -c "Pinned-Windows Architecture" .planning/PROJECT.md
```

Operator UAT walk (all 5 flows from 131-01..131-05).
</verification>
