---
phase: 201-liv-ai-nextjs-iframe
plan: 02
type: execute
status: code-complete
wave: 0
completed: 2026-05-23
duration_min: 4
sacred_sha_verified: f3538e1d811992b782a9bb057d1b7f0a0189f95f
commit_sha: 518c73f3
build_exit: 0
files_modified: 3   # assistant.tsx, package.json, pnpm-lock.yaml
files_deleted: 1    # app/api/chat/route.ts (+ empty parent dirs app/api/chat, app/api)
deps_removed: ["@ai-sdk/openai"]
deviations: 0
tags: [transport, backend-proxy, wave-0]
subsystem: liv-ai-app
requirements: [REQ-201-02]
key-decisions:
  - "AssistantChatTransport api set to relative '/chat/livAi' for same-origin auto-cookie auth (D-201-06)"
  - "Next.js app/api/chat route deleted — subapp does NOT host server-side LLM provider (D-201-07, INV-201-02)"
  - "@ai-sdk/openai pruned from deps; no `openai` package was present"
dependency_graph:
  requires: ["201-01 scaffold (assistant.tsx + thread.tsx + package.json baseline)"]
  provides: ["minimal AssistantChatTransport wired to parent livinityd; Plan 201-04 extension surface"]
  affects: ["livos/packages/liv-ai-app/{app/assistant.tsx, package.json, pnpm-lock.yaml}"]
tech-stack:
  added: []
  removed: ["@ai-sdk/openai@^3.0.61"]
  patterns: ["same-origin iframe → relative API path → credentials:'include' cookie flow"]
key-files:
  modified:
    - "livos/packages/liv-ai-app/app/assistant.tsx (69 → 24 LOC; minimal AssistantRuntimeProvider + Thread shell)"
    - "livos/packages/liv-ai-app/package.json (removed @ai-sdk/openai dep)"
    - "livos/pnpm-lock.yaml (regenerated by pnpm install)"
  deleted:
    - "livos/packages/liv-ai-app/app/api/chat/route.ts (boilerplate Next.js LLM route — superseded by livinityd /chat/livAi)"
    - "livos/packages/liv-ai-app/app/api/chat/ (empty dir removed)"
    - "livos/packages/liv-ai-app/app/api/ (empty dir removed)"
---

# Phase 201 Plan 02: Wire AssistantChatTransport to parent /chat/livAi Summary

**One-liner:** Rewrote scaffolded `app/assistant.tsx` to the minimal `AssistantChatTransport({api:'/chat/livAi', credentials:'include'})` shape pointing at parent livinityd same-origin, deleted boilerplate `app/api/chat/route.ts` (+ empty parent dirs), pruned `@ai-sdk/openai` server-only provider dep — `pnpm build` EXIT 0, sacred SHA PASS, atomic commit `518c73f3`.

## Tasks Executed

### Task 1: Rewrite app/assistant.tsx transport — DONE

Replaced the 69-line 201-01 scaffold (which still had a SidebarProvider + Breadcrumb shell pointing at `/api/chat`) with the 24-line minimal Plan 201-02 spec:

```tsx
"use client";

import {
  AssistantRuntimeProvider,
} from "@assistant-ui/react";
import {
  AssistantChatTransport,
  useChatRuntime,
} from "@assistant-ui/react-ai-sdk";
import { Thread } from "@/components/assistant-ui/thread";

export const Assistant = () => {
  const runtime = useChatRuntime({
    transport: new AssistantChatTransport({
      api: "/chat/livAi",
      credentials: "include",
    }),
  });

  return (
    <AssistantRuntimeProvider runtime={runtime}>
      <Thread />
    </AssistantRuntimeProvider>
  );
};
```

Plan 201-04 will EXTEND this with the LivAiComposer mount + thread-list adapter + body callback (threadId / config.modelName), per D-201-21 / D-201-23. The SidebarProvider + Breadcrumb sidebar from the 201-01 scaffold are intentionally dropped here — 201-04 reintroduces the thread-list adapter on a cleaner footing.

**Acceptance checks (all PASS):**
- `grep -c 'api: "/chat/livAi"' app/assistant.tsx` → `1` 
- `grep -c 'credentials: "include"' app/assistant.tsx` → `1`
- `@/components/assistant-ui/thread` resolves (`components/assistant-ui/thread.tsx` present from 201-01)
- Next.js build re-renders the file with no TypeScript errors

### Task 2: Delete app/api/chat/route.ts — DONE

```bash
rm livos/packages/liv-ai-app/app/api/chat/route.ts
rmdir livos/packages/liv-ai-app/app/api/chat   # empty after deletion
rmdir livos/packages/liv-ai-app/app/api        # empty after subdir removed
```

`livos/packages/liv-ai-app/app/` post-delete contents:
```
assistant.tsx
favicon.ico
globals.css
layout.tsx
page.tsx
```

**Acceptance checks (all PASS):**
- `ls app/api/chat/route.ts` → "No such file or directory"
- `app/api/` dir does not exist
- Build output route table contains only `/` and `/_not-found` — no `/api/chat`

### Task 3: Strip provider deps — DONE

`livos/packages/liv-ai-app/package.json` audit:
- `"@ai-sdk/openai": "^3.0.61"` → **REMOVED**
- `"openai":` → was NOT present (no plain `openai` package installed by the 201-01 scaffold)
- `"ai": "^6.0.175"` → **KEPT** (transitive dep chain via `@assistant-ui/react-ai-sdk → AssistantChatTransport`)

Ran:
```bash
cd livos && pnpm install --ignore-scripts   # lockfile regenerates
cd packages/liv-ai-app && pnpm build         # EXIT 0
```

Build output excerpt:
```
▲ Next.js 16.2.6 (Turbopack)
  Creating an optimized production build ...
✓ Compiled successfully in 3.5s
  Running TypeScript ...
  Finished TypeScript in 3.8s ...
✓ Generating static pages using 5 workers (4/4) in 771ms

Route (app)
┌ ○ /
└ ○ /_not-found
```

**Acceptance checks (all PASS):**
- `grep -E '(@ai-sdk/openai|"openai":)' package.json` → no matches (EXIT 1)
- `pnpm build` → EXIT 0

### Task 4: Commit — DONE

Atomic commit: `518c73f3`

```
feat(201-02): wire AssistantChatTransport to parent /chat/livAi, drop Next.js api/chat

- Rewrite app/assistant.tsx: AssistantChatTransport({api:'/chat/livAi', credentials:'include'})
  pointed at parent livinityd same-origin (relative path, JWT cookie auto-flows)
- DELETE app/api/chat/route.ts + empty app/api/chat + app/api dirs (subapp HTTP-proxies
  to livinityd; no Next.js server-side LLM provider needed per INV-201-02)
- Strip @ai-sdk/openai dep from package.json (server-only provider client, unused after
  api route deletion). No `openai` package was present.
- Minimal scaffold-compatible <Assistant /> — Plan 201-04 extends with LivAiComposer +
  thread-list adapter + body callback (threadId / config.modelName) per D-201-23 / D-201-21
- pnpm install (workspace) + pnpm build (liv-ai-app) EXIT 0
- Sacred SHA preserved (sdk-agent-runner.ts untouched)
```

Pre-commit hook output: `[sacred-sha] PASS: 20 files verified`

Git stats: `4 files changed, 7 insertions(+), 106 deletions(-)` (route.ts boilerplate removal + scaffold-shell trim drives the deletion count).

Deletion audit (`git diff --diff-filter=D --name-only HEAD~1 HEAD`): only `livos/packages/liv-ai-app/app/api/chat/route.ts` — single intentional deletion per plan.

## Invariants Honoured

| ID | Check | Status |
|----|-------|--------|
| INV-201-01 | Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved | PASS (hook output `20 files verified`) |
| INV-201-02 | Backend untouched (no diff in livinityd / liv) | PASS (`git diff HEAD~1 HEAD --stat` shows only `livos/packages/liv-ai-app/*` + `livos/pnpm-lock.yaml`) |
| INV-201-05 | English UI text only | PASS (no Turkish strings introduced; assistant.tsx has no copy) |
| INV-201-06 | Same-origin iframe path | PASS (`api: "/chat/livAi"` is a relative URL → resolves against iframe host = Caddy host = livinityd same-origin once 201-06/07 land) |
| Plan 201-04 surface preserved | `<Assistant />` is minimal enough to extend in 201-04 | PASS (no premature LivAiComposer mount, no body callback, no thread-list adapter — Plan 201-04 will add these per D-201-21/23) |

## Deviations from Plan

**None — plan executed exactly as written.**

Note on `openai` package: Plan Task 3 step 1 said "Check `package.json` for `openai`, `@ai-sdk/openai`, `ai` deps" — only `@ai-sdk/openai` was present (and removed). The plain `openai` package was never added by the 201-01 scaffold, so no separate removal action was needed. The grep regression-lock `grep -E '(@ai-sdk/openai|"openai":)' package.json → no matches` was satisfied.

## Deferred Items

None.

## Build Metrics

| Metric | Value |
|--------|-------|
| `pnpm install --ignore-scripts` time | 11.8 s |
| `pnpm build` time | 3.5 s compile + 3.8 s tsc + 0.771 s SSG = ~8 s wall |
| `pnpm build` EXIT | 0 |
| Route table | `/` (static), `/_not-found` (static) |
| Bundle audit | No new chunks vs 201-01 baseline (no source files added) |

## Acceptance Envelope Touchpoints (carry to operator UAT in 201-08)

This plan does NOT add operator-walkable surface — verification deferred to 201-06 (Caddy + systemd) + 201-08 (browser UAT). The transport wiring is verifiable by browser DevTools network tab once the iframe mounts (Plan 201-07): `/chat/livAi` requests should appear with the JWT cookie, and 401 → 200 once auth flows.

## Self-Check: PASSED

- `livos/packages/liv-ai-app/app/assistant.tsx` → FOUND (24 LOC, matches Plan Task 1 verbatim shape)
- `livos/packages/liv-ai-app/app/api/chat/route.ts` → MISSING (intentional delete — PASS)
- `livos/packages/liv-ai-app/app/api/chat/` → MISSING (intentional rmdir — PASS)
- `livos/packages/liv-ai-app/app/api/` → MISSING (intentional rmdir — PASS)
- `livos/packages/liv-ai-app/package.json` → no `@ai-sdk/openai`, no `"openai":` (PASS via grep EXIT 1)
- `pnpm build` → EXIT 0 (PASS)
- Commit `518c73f3` → FOUND in `git log --oneline -5`
- Sacred SHA hook → `PASS: 20 files verified` (PASS)
