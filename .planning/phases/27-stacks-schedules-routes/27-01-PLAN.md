---
phase: 27-stacks-schedules-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/docker/resource-store.ts
  - livos/packages/ui/src/routes/docker/sections/stacks.tsx
  - livos/packages/ui/src/routes/docker/resources/action-button.tsx
autonomous: false
requirements: [DOC-11]
must_haves:
  truths:
    - "User opens Docker app, clicks Stacks in sidebar, sees the live stack list with status badges and per-row actions (start/stop/restart/redeploy/edit/remove) — same actions as v27.0 server-control Stacks tab."
    - "User clicks 'Deploy Stack' and sees a 3-tab dialog: Deploy from YAML / Deploy from Git / Generate from prompt — all three paths work end-to-end (YAML accepts compose, Git creates webhook URL after deploy, AI generates compose from natural-language prompt)."
    - "User expands a stack row and sees Containers tab (constituent containers list) and Graph tab (Phase 19 ComposeGraphViewer) — same tabs as v27.0."
    - "User clicks a constituent container in the Stack expanded row and sees the existing ContainerDetailSheet open with its Logs (Phase 17 xterm) and Files (Phase 18 file browser) tabs — preserving Phase 17 + Phase 18 features via the existing per-container detail sheet."
    - "User types in the search input and the stack list filters client-side by stack name; empty search shows all stacks."
    - "External code can call useDockerResource.getState().setSelectedStack(name) to programmatically expand a stack row — DOC-20 programmatic deep-link half closed for stacks."
  artifacts:
    - path: "livos/packages/ui/src/routes/docker/resource-store.ts"
      provides: "selectedStack slot + setSelectedStack + useSelectedStack — 5th slot in ResourceStore"
      contains: "selectedStack: string | null"
    - path: "livos/packages/ui/src/routes/docker/stacks/stack-section.tsx"
      provides: "StackSection — full Stacks body (list + search + dialogs + expandable rows + per-row actions)"
      min_lines: 400
    - path: "livos/packages/ui/src/routes/docker/stacks/deploy-stack-form.tsx"
      provides: "DeployStackForm — full-page overlay with YAML/Git/AI tabs"
      min_lines: 350
    - path: "livos/packages/ui/src/routes/docker/stacks/ai-compose-tab.tsx"
      provides: "AiComposeTab (Phase 23 AID-03)"
    - path: "livos/packages/ui/src/routes/docker/stacks/add-git-credential-dialog.tsx"
      provides: "AddGitCredentialDialog (HTTPS / SSH credential entry)"
    - path: "livos/packages/ui/src/routes/docker/stacks/stack-dialogs.tsx"
      provides: "RemoveStackDialog + RedeployStackDialog"
    - path: "livos/packages/ui/src/routes/docker/sections/stacks.tsx"
      provides: "1-line re-export of StackSection (replaces Phase 24 placeholder)"
  key_links:
    - from: "src/routes/docker/sections/stacks.tsx"
      to: "src/routes/docker/stacks/stack-section.tsx"
      via: "named re-export `Stacks`"
      pattern: "export \\{.*StackSection as Stacks.*\\}"
    - from: "src/routes/docker/stacks/stack-section.tsx"
      to: "src/hooks/use-stacks.ts"
      via: "useStacks() hook (env-aware via Phase 22)"
      pattern: "useStacks\\(\\)"
    - from: "src/routes/docker/stacks/stack-section.tsx"
      to: "src/routes/server-control/compose-graph-viewer.tsx"
      via: "ComposeGraphViewer cross-import (verbatim — Plan 24-02 D-09 precedent; Plan 27-02 will move it)"
      pattern: "from '@/routes/server-control/compose-graph-viewer'"
    - from: "src/routes/docker/stacks/stack-section.tsx"
      to: "src/routes/docker/resource-store.ts"
      via: "useSelectedStack + setSelectedStack (replaces local expandedStack useState)"
      pattern: "useSelectedStack|setSelectedStack"
    - from: "src/routes/docker/stacks/stack-section.tsx"
      to: "src/routes/server-control/container-detail-sheet.tsx"
      via: "ContainerDetailSheet cross-import — clicking a constituent container row opens it (Phase 17 logs + Phase 18 files preserved via the existing per-container sheet)"
      pattern: "from '@/routes/server-control/container-detail-sheet'"
    - from: "src/routes/docker/stacks/deploy-stack-form.tsx"
      to: "src/hooks/use-stacks.ts"
      via: "deployStack / editStack / lastDeployResult / clearLastDeployResult"
      pattern: "deployStack|editStack"
---

<objective>
Replace the Phase 24 Stacks placeholder with the full live Stacks section ported from `routes/server-control/index.tsx`. Preserves every v27.0 Stacks feature: list with summary, per-row controls (start/stop/restart/redeploy/edit/remove), expandable rows showing Containers + Graph tabs, full Deploy Stack overlay with YAML / Git (Phase 21) / AI (Phase 23) tabs, AddGitCredentialDialog, RemoveStackDialog, RedeployStackDialog. ADDS a search input. Wires `expandedStack` through a new `selectedStack` slot in `useDockerResource` for DOC-20 programmatic deep-link parity with containers/images/volumes/networks. Stack constituent containers route into the existing `ContainerDetailSheet` from `routes/server-control/` (cross-import — Plan 27-02 will relocate post-cleanup) so Phase 17 logs + Phase 18 files keep working.

Purpose: Close DOC-11. Stacks is the largest sub-section by feature surface (full overlay form with 3 deploy modes, nested credential dialog, webhook display, vuln-scan-on-stack-images via the per-container sheet). Final cleanup of `routes/server-control/` is Plan 27-02.

Output: Live Stacks section reachable from `Sidebar > Stacks`; legacy `StacksTab()` in server-control still on disk (deleted by Plan 27-02 directory delete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-stacks-schedules-routes/27-CONTEXT.md
@.planning/phases/24-docker-app-skeleton/24-01-SUMMARY.md
@.planning/phases/26-resource-routes/26-01-SUMMARY.md
@.planning/phases/26-resource-routes/26-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Source files to read (extract — do not re-read)
@livos/packages/ui/src/routes/server-control/index.tsx     # Lines 2674-3855: AiComposeTab + DeployStackForm + AddGitCredentialDialog + RemoveStackDialog + RedeployStackDialog + StacksTab
@livos/packages/ui/src/routes/server-control/compose-graph-viewer.tsx
@livos/packages/ui/src/hooks/use-stacks.ts
@livos/packages/ui/src/routes/docker/resource-store.ts
@livos/packages/ui/src/routes/docker/sections/stacks.tsx
@livos/packages/ui/src/routes/docker/resources/container-section.tsx  # Reference shape for section structure
@livos/packages/ui/src/routes/docker/resources/filter-rows.ts          # Pure helper, reuse
@livos/packages/ui/src/routes/docker/resources/action-button.tsx       # Reuse — already ported

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/routes/docker/resource-store.ts (Plan 26-01):
```typescript
export interface ResourceStore {
  selectedContainer: string | null
  selectedImage: string | null
  selectedVolume: string | null
  selectedNetwork: string | null
  // selectedStack: string | null   // ADD in this plan
  setSelectedContainer: (name: string | null) => void
  setSelectedImage: (id: string | null) => void
  setSelectedVolume: (name: string | null) => void
  setSelectedNetwork: (id: string | null) => void
  // setSelectedStack: (name: string | null) => void  // ADD
  clearAllSelections: () => void  // include selectedStack reset
}
export const useSelectedContainer = () => useDockerResource((s) => s.selectedContainer)
// ... add useSelectedStack to match
```

From src/hooks/use-stacks.ts (Phase 22 env-aware):
```typescript
export type DeployStackGitInput = {url: string; branch?: string; credentialId?: string | null; composePath?: string}
export type DeployStackInput = {name: string; composeYaml?: string; git?: DeployStackGitInput; envVars?: Array<{key: string; value: string; secret?: boolean}>}
export function useStacks(): {
  stacks: Array<{name: string; status: 'running' | 'stopped' | 'partial'; containerCount: number; containers: Array<{id: string; name: string; image: string; state: string}>}>
  isLoading, isError, error, isFetching, refetch,
  deployStack, isDeploying, editStack, isEditing,
  controlStack: (name: string, action: 'up' | 'stop' | 'restart' | 'pull-and-up') => void,
  isControlling, removeStack, isRemoving, actionResult,
  lastDeployResult, clearLastDeployResult,
}
```

From src/routes/server-control/compose-graph-viewer.tsx:
```typescript
export function ComposeGraphViewer({stackName}: {stackName: string}): JSX.Element
```

From src/routes/server-control/container-detail-sheet.tsx (cross-import target — Plan 27-02 will relocate):
```typescript
// Used inside Plan 26-01's ContainerSection — same component opens here.
// Phase 17 logs xterm + Phase 18 files browser + Phase 19 vuln-scan all live inside this sheet.
import {ContainerDetailSheet} from '@/routes/server-control/container-detail-sheet'
```

From src/routes/docker/resources/filter-rows.ts:
```typescript
export function filterByQuery<T>(rows: T[], query: string, getHaystack: (row: T) => string): T[]
// Empty-string query returns SAME array reference (Plan 26-01 perf contract).
```

From src/routes/docker/resources/action-button.tsx:
```typescript
export function ActionButton({icon, onClick, disabled, color, title}: {
  icon: Icon  // @tabler/icons-react Icon type (Plan 26-01 D-08)
  onClick: () => void
  disabled?: boolean
  color: 'emerald' | 'amber' | 'blue' | 'red'
  title: string
}): JSX.Element
```

From src/routes/docker/store.ts (Plan 24-01):
```typescript
export const useSetDockerSection = (): (section: SectionId) => void
// Available if a downstream plan needs to flip from Stacks → another section.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selectedStack slot to useDockerResource store</name>
  <files>
    livos/packages/ui/src/routes/docker/resource-store.ts,
    livos/packages/ui/src/routes/docker/resource-store.unit.test.ts
  </files>
  <action>
Extend the existing `ResourceStore` interface from Plan 26-01 with a 5th slot for stacks. Mirrors the existing 4-slot pattern exactly — same null-default, same setter shape, same clearAllSelections inclusion, same selector hook export.

**Edit `resource-store.ts`:**
1. Add `selectedStack: string | null` to the `ResourceStore` interface (between `selectedNetwork` and the setters block).
2. Add `setSelectedStack: (name: string | null) => void` to the interface.
3. Add `selectedStack: null` to the `create<ResourceStore>()((set) => ({...}))` initial state.
4. Add `setSelectedStack: (name) => set({selectedStack: name})` to the setters block.
5. Update `clearAllSelections` to include `selectedStack: null` in the reset object.
6. Export `useSelectedStack` selector hook at the bottom: `export const useSelectedStack = () => useDockerResource((s) => s.selectedStack)`.

**Edit `resource-store.unit.test.ts`:**
Add 3 cases following the existing 4-slot test patterns from Plan 26-01:
- `setSelectedStack writes the slot` — call `setSelectedStack('mystack')`, expect `selectedStack === 'mystack'`.
- `setSelectedStack does not clobber other slots` — set selectedContainer first, then setSelectedStack, expect both populated.
- `clearAllSelections resets selectedStack to null` — set all 5 slots, call clearAllSelections, expect all 5 null.

**Why this design (per D-XX in Plan 26-01):** Single `useDockerResource` store with N slots, NOT N mini-stores — same trust model and re-render scope as the 4 existing slots. Selector hook (`useSelectedStack`) bounds subscriptions so the StackSection won't re-render when selectedContainer changes.

**Use existing pattern; do NOT introduce a new store, persist middleware, or different slot shape.** Reference: `livos/packages/ui/src/routes/docker/resource-store.ts:21-58` and `resource-store.unit.test.ts` Test G (identity-stable subscriber).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui exec vitest run src/routes/docker/resource-store.unit.test.ts --environment jsdom</automated>
  </verify>
  <done>10 tests pass (7 existing + 3 new); `useSelectedStack` exported; `selectedStack` slot present in `clearAllSelections` reset.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port StacksTab body + DeployStackForm + dialogs to routes/docker/stacks/</name>
  <files>
    livos/packages/ui/src/routes/docker/stacks/ai-compose-tab.tsx,
    livos/packages/ui/src/routes/docker/stacks/add-git-credential-dialog.tsx,
    livos/packages/ui/src/routes/docker/stacks/stack-dialogs.tsx,
    livos/packages/ui/src/routes/docker/stacks/deploy-stack-form.tsx,
    livos/packages/ui/src/routes/docker/stacks/stack-section.tsx,
    livos/packages/ui/src/routes/docker/sections/stacks.tsx
  </files>
  <action>
Verbatim port from `routes/server-control/index.tsx` into a new `routes/docker/stacks/` subdirectory (matching the `routes/docker/resources/` precedent from Plan 26-01 — sections re-export, complex components live in a sibling directory). Per Plan 26-01 D-06 + Plan 24-02 D-09: cross-imports from legacy `routes/server-control/*` for `ComposeGraphViewer` and `ContainerDetailSheet` are CORRECT during the transition; Plan 27-02 will relocate them post-server-control delete.

**File 1 — `stacks/ai-compose-tab.tsx`:**
- Verbatim port of `AiComposeTab` from `server-control/index.tsx` lines 2680-2796.
- Imports needed: `IconLoader2, IconSparkles, IconCheck` from `@tabler/icons-react`; `Button, Label` from `@/shadcn-components/ui/{button,label}`; `useAiDiagnostics` from `@/hooks/use-ai-diagnostics` (or wherever it lives — match the legacy import).
- Export named `AiComposeTab`.
- Comment header: `// Phase 27-01 — ported verbatim from routes/server-control/index.tsx:2680-2796 (Phase 23 AID-03 — Kimi natural-language compose generator).`

**File 2 — `stacks/add-git-credential-dialog.tsx`:**
- Verbatim port of `AddGitCredentialDialog` from `server-control/index.tsx` lines 3314-3448.
- Imports: `useState`, all Dialog primitives from `@/shadcn-components/ui/dialog`, `Button, Input, Label`, `cn`, `trpcReact`.
- Export named `AddGitCredentialDialog`.
- Comment header citing legacy lines.

**File 3 — `stacks/stack-dialogs.tsx`:**
- Verbatim port of BOTH `RemoveStackDialog` (legacy 3451-3502) AND `RedeployStackDialog` (legacy 3505-3539) into one file (consumer-of-one each, but keeping them paired matches `image-dialogs.tsx` precedent from Plan 26-01).
- Imports: useState, Dialog primitives, Button, Checkbox.
- Exports: `RemoveStackDialog`, `RedeployStackDialog`.
- Comment header citing legacy line ranges for each.

**File 4 — `stacks/deploy-stack-form.tsx`:**
- Verbatim port of `DeployStackForm` from `server-control/index.tsx` lines 2799-3307.
- This is the largest sub-component (~510 lines) — full-page overlay with three Tabs (yaml/git/ai), env-vars repeater (with secret toggle + hasValue placeholder logic), git credential picker, webhook URL panel after deploy.
- Imports: useState, useEffect from React; many `@tabler/icons-react` icons (`IconRefresh, IconX, IconPlus, IconLock, IconCheck, IconCopy`); shadcn primitives (`Button, Input, Label, Tabs, TabsList, TabsTrigger, TabsContent, Checkbox`); `cn`, `trpcReact`, `useStacks` from `@/hooks/use-stacks`.
- Internal references: `<AiComposeTab>` import from `./ai-compose-tab`; `<AddGitCredentialDialog>` import from `./add-git-credential-dialog`.
- Export named `DeployStackForm`.
- DO NOT alter the YAML / Git / AI tab logic, env-var secret handling (`hasValue` UI-only flag), webhook URL panel, or "keep existing stored secret" semantics — the comment headers reference Plan 21-02 D-X and Phase 23 AID-03 which are required preservation points.

**File 5 — `stacks/stack-section.tsx`:**
- Port of `StacksTab` from `server-control/index.tsx` lines 3542-3856 with these structural changes:
  - Wrap the entire `<>...</>` body in `<div className='flex h-full flex-col overflow-y-auto p-4 sm:p-6'>` to match the `routes/docker/resources/*-section.tsx` shape from Plan 26-01.
  - Replace `const [expandedStack, setExpandedStack] = useState<string | null>(null)` with `const expandedStack = useSelectedStack()` + `const setExpandedStack = useDockerResource((s) => s.setSelectedStack)` (use the new slot from Task 1).
  - **ADD search input** with `maxLength={200}` (defensive bound — Plan 26-01 T-26-03 precedent): place above the action row, filtering `stacks` by name via `filterByQuery(stacks, searchQuery, (s) => s.name)`. Render `filteredStacks` instead of `stacks` in the table.
  - **ADD `noFilterResults` empty-state branch** mirroring Plan 26-02 D-05: shows `No stacks match "<query>"` with the search query echoed in mono font when filter is active and empty but unfiltered list is non-empty.
  - **Replace the constituent-container row** (legacy lines 3771-3791 — the inner `<TableRow key={container.id}>` rendering name/image/state) with a clickable row: `<TableRow onClick={() => setSelectedContainer(container.name)} className='cursor-pointer hover:bg-surface-1/50'>`. This wires the constituent containers into the existing `ContainerDetailSheet` (rendered conditionally at the bottom of the section, identical to Plan 26-01's container-section.tsx). Phase 17 logs + Phase 18 files + Phase 19 vuln-scan are all already inside `ContainerDetailSheet` — no new components needed at the stack level.
  - Render `<ContainerDetailSheet containerName={selectedContainer} onClose={() => setSelectedContainer(null)} ...>` block at the bottom (copy the wiring from `container-section.tsx`'s sheet wiring; use `useDockerResource` for `selectedContainer` to keep the deep-link contract).
  - Imports needed:
    - React: `Fragment, useState`
    - icons: `IconChevronDown, IconChevronRight, IconStack2, IconRefresh, IconPlayerPlay, IconPlayerStop, IconRotateClockwise, IconCloudDownload, IconPencil, IconTrash, IconPlus, IconSearch`
    - framer: `motion, AnimatePresence`
    - shadcn: `Button, Input, Tabs, TabsList, TabsTrigger, TabsContent, Table, TableBody, TableCell, TableHead, TableHeader, TableRow`
    - cn
    - hooks: `useStacks` from `@/hooks/use-stacks`
    - resource store: `useDockerResource, useSelectedStack` from `../resource-store`
    - filter helper: `filterByQuery` from `../resources/filter-rows`
    - action button: `ActionButton` from `../resources/action-button`
    - Stack components: `DeployStackForm` from `./deploy-stack-form`, `RemoveStackDialog, RedeployStackDialog` from `./stack-dialogs`
    - Cross-imports (transition — to be relocated by Plan 27-02): `ComposeGraphViewer` from `@/routes/server-control/compose-graph-viewer`; `ContainerDetailSheet` from `@/routes/server-control/container-detail-sheet`
  - Inline a local `statusBadge` helper identical to legacy lines 3563-3574.
  - Export `StackSection` (named export — matches `ContainerSection` / `ImageSection` / etc. from Plan 26-01).
  - Comment header: `// Phase 27-01 (DOC-11) — ported from routes/server-control/index.tsx StacksTab() (lines 3542-3856) with: (1) search input (NEW — maxLength=200, T-27-01 mitigation); (2) expandedStack migrated to useDockerResource.selectedStack zustand store (DOC-20 partial); (3) constituent-container rows clickable → opens ContainerDetailSheet (preserves Phase 17 logs + Phase 18 files + Phase 19 vuln-scan via existing per-container sheet); (4) wrapped in flex h-full overflow-y-auto p-4 sm:p-6.`

**File 6 — `sections/stacks.tsx` (modify, replacing Phase 24 placeholder):**
Replace the current placeholder body with a 1-line re-export:
```tsx
// Phase 27-01 — replaces the Phase 24 placeholder.
// Body lives in routes/docker/stacks/stack-section.tsx (DOC-11 + DOC-20 partial).
export {StackSection as Stacks} from '../stacks/stack-section'
```

**Per CONVENTIONS.md / STRUCTURE.md:** Use Tabler icons (`@tabler/icons-react`); use `cn` for class merging; use shadcn primitives. Match indentation (tabs not spaces) and quoting style with neighboring files.

**Do NOT** alter `use-stacks.ts`, `compose-graph-viewer.tsx`, `container-detail-sheet.tsx`, or any backend code. Do NOT delete `routes/server-control/index.tsx` — Plan 27-02 owns the directory delete after schedules migration verifies zero remaining cross-imports.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/config build && pnpm --filter ui build</automated>
    <manual>
1. `cd livos/packages/ui && pnpm dev` — open Docker app via dock icon, click Stacks in sidebar.
2. Stack list renders with the same status badges, container counts, and per-row buttons (start/stop/restart/redeploy/edit/remove) as the v27.0 server-control Stacks tab.
3. Click 'Deploy Stack' → 3-tab dialog opens (YAML / Git / AI). Type a YAML compose and click Deploy → success toast + new stack appears in list.
4. Click 'Deploy Stack' again → switch to Git tab → enter a public repo URL + branch + composePath → Deploy. Webhook URL + secret panel renders after success.
5. Switch to AI tab → enter prompt "nginx with redis on port 8080" → Generate → preview YAML appears → "Use this YAML" → tab switches to YAML and Deploy works.
6. Click 'Add Credential' from Git tab → AddGitCredentialDialog opens → HTTPS / SSH toggle works → create flow returns id and auto-selects in picker.
7. Expand a running stack row → Containers + Graph tabs visible. Containers tab shows constituent containers; clicking a container opens ContainerDetailSheet with Logs (xterm) + Files (browser) + vuln-scan tabs visible.
8. Graph tab renders the Phase 19 ComposeGraphViewer (react-flow diagram).
9. Type in search input → list filters by stack name; clear search → all stacks return.
10. Click Edit on a stack → DeployStackForm opens in edit mode (YAML-only) with existing compose + env vars pre-loaded.
11. Click Redeploy → RedeployStackDialog → confirm → controlStack('pull-and-up') runs.
12. Click Remove → RemoveStackDialog with "also remove volumes" checkbox → confirm → stack removed.
13. Open browser devtools console: `useDockerResource.getState().setSelectedStack('<existing-stack-name>')` → stack row programmatically expands.
    </manual>
  </verify>
  <done>
- `pnpm --filter ui build` green.
- Phase 24 placeholder string ("Coming in Phase 27 — Compose stacks (YAML / Git / AI)") removed from sections/stacks.tsx.
- All v27.0 stack actions (start/stop/restart/redeploy/edit/remove) work as before.
- All 3 deploy modes (YAML / Git / AI) functional including webhook display and credential creation.
- Constituent container rows in expanded stacks are clickable and open ContainerDetailSheet (Phase 17 logs + Phase 18 files + Phase 19 vuln-scan available via the existing per-container sheet).
- Search filters by name; `setSelectedStack(name)` deep-link works programmatically.
- ComposeGraphViewer renders in Graph tab unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- New `selectedStack` slot in `useDockerResource` zustand store + `useSelectedStack` selector hook + 3 new vitest cases.
- `routes/docker/stacks/` subdirectory with 5 ported components: `AiComposeTab`, `AddGitCredentialDialog`, `RemoveStackDialog` + `RedeployStackDialog` (paired in `stack-dialogs.tsx`), `DeployStackForm`, `StackSection`.
- `sections/stacks.tsx` placeholder replaced with 1-line re-export.
- Cross-imports retained from `routes/server-control/` for `ComposeGraphViewer` and `ContainerDetailSheet` — Plan 27-02 will relocate.
- All v27.0 features preserved: list + status badges + per-row controls + 3-tab Deploy dialog + AddGitCredentialDialog + webhook URL + RemoveStackDialog + RedeployStackDialog + ComposeGraphViewer Graph tab + click-through to ContainerDetailSheet (Phase 17 logs + Phase 18 files + Phase 19 vuln-scan).
- NEW: search input (maxLength=200), `selectedStack` deep-link, `noFilterResults` empty-state branch.
  </what-built>
  <how-to-verify>
1. `cd livos/packages/ui && pnpm dev` (or rebuild on server: `pnpm --filter @livos/config build && pnpm --filter ui build && systemctl restart livos`).
2. Open Docker app from dock → click Stacks in sidebar.
3. **Functional smoke (must all pass):**
   - [ ] Stack list renders with same shape as v27.0.
   - [ ] Search input filters by name; clearing returns all stacks; `noFilterResults` empty card shows when filter has no matches.
   - [ ] Per-row actions work: Start (stopped/partial only), Stop (running/partial only), Restart, Redeploy (opens RedeployStackDialog), Edit (opens form in edit mode), Remove (opens RemoveStackDialog with volumes checkbox).
   - [ ] Deploy Stack dialog: YAML tab deploys a compose; Git tab deploys from public repo + shows webhook URL panel after; AI tab generates compose from prompt; "Use this YAML" pipes generated YAML into YAML tab.
   - [ ] Add Credential nested dialog: creates HTTPS PAT credential; creates SSH key credential (with PEM ≥ 50 chars); auto-selects in picker after create.
   - [ ] Expand stack row → Containers + Graph tabs render; clicking a constituent container opens ContainerDetailSheet (verify Logs + Files + vuln-scan tabs all work inside the sheet).
   - [ ] In browser devtools: `useDockerResource.getState().setSelectedStack('<stack-name>')` programmatically expands the stack row.
4. **Build/test green:**
   - [ ] `pnpm --filter ui build` (UI bundle): success.
   - [ ] `pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom`: all tests pass (84 baseline from Phase 26 + 3 new = 87 expected).
5. **Cross-import sanity:** `grep -rn "from '@/routes/server-control" livos/packages/ui/src/routes/docker/` returns ONLY: ContainerDetailSheet (container-section + stack-section), ContainerCreateForm (container-section), ComposeGraphViewer (stack-section), AlertsBell (status-bar), EnvironmentSelector (status-bar). NO new cross-imports beyond these (Plan 27-02 will relocate).
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 27-02 (Schedules + final server-control delete), or describe issues to address before plan ships.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→docker.deployStack | Compose YAML / git URL / env-var values cross from browser to livinityd; YAML is parsed server-side; secret env-vars encrypted to Redis. Same trust model as v27.0 server-control. |
| user→docker.createGitCredential | username + PAT or SSH private key cross to livinityd; encrypted at rest. Same as v27.0. |
| user→ai.generateComposeFromPrompt | Free-text prompt sent to Kimi; generated YAML returned. Validated client-side as YAML before deploy. Same as Phase 23 AID-03. |
| user→useDockerResource.setSelectedStack | Stack name written to in-memory zustand only — same trust model as Plan 26-01 setSelectedContainer (T-26-01 accept). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | D (Denial of Service) | StackSection search input — query of length 100k chars across N-stack list | mitigate | `<Input maxLength={200}>` on search input — same defensive bound as Plan 26-01 T-26-03. |
| T-27-02 | T (Tampering) | useDockerResource.setSelectedStack — store tampering | accept | Same trust model as Plan 26-01 T-26-01 (in-memory zustand, no escalation, identical to legacy useState). |
| T-27-03 | I (Information Disclosure) | search reveals env-scoped data | accept | Same data the legacy StacksTab already exposed; env scoping enforced at tRPC layer (Phase 22-01 D-08), not at UI filter step. |
| T-27-04 | I (Info Disclosure) | webhook secret displayed in DOM after git deploy | accept | Same as Plan 21-02 D-X — secret only retrievable once, displayed in password input + copy button; user warned via copy. Form remains open until Done click. |
| T-27-05 | E (Elevation of Privilege) | new tRPC mutations introduced | n/a | Zero new tRPC routes — re-uses `docker.deployStack`, `docker.editStack`, `docker.controlStack`, `docker.removeStack`, `docker.createGitCredential`, `docker.listGitCredentials`, `docker.getStackCompose`, `docker.getStackEnv`, `docker.generateComposeFromPrompt`. All env-aware via Phase 22-01 D-08. |
| T-27-06 | S (Spoofing) | AI compose generator output executed as compose YAML | accept | User reviews generated YAML in read-only preview before clicking "Use this YAML"; Deploy button still requires explicit click. Same as Phase 23 AID-03 mitigation. |
| T-27-07 | T (Tampering) | container-detail-sheet cross-import path traversal — Plan 27-02 will move it | accept | Imported by file path, no user input flows into the import path. Cross-import is a transition concern, not a security one. |
</threat_model>

<verification>
- `pnpm --filter ui build` succeeds.
- `pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom` succeeds (84 + 3 = 87 expected).
- Phase 24 placeholder string removed: `grep -F "Coming in Phase 27 — Compose stacks" livos/packages/ui/src/routes/docker/sections/stacks.tsx` returns empty.
- All v27.0 stack features verified via the manual smoke checklist in Task 2.
- Cross-imports from `routes/server-control/*` limited to: `ComposeGraphViewer`, `ContainerDetailSheet`, `ContainerCreateForm`, `AlertsBell`, `EnvironmentSelector` (these 5 are the relocation targets Plan 27-02 will handle).
</verification>

<success_criteria>
- DOC-11 closed (per ROADMAP success criteria 1, 2, 4 — partial 5 via programmatic deep-link; URL-bar deep-link is Phase 29 DOC-20 final).
- StackSection mounted; sections/stacks.tsx no longer a placeholder.
- DeployStackForm renders all three deploy modes (YAML / Git / AI) with full feature parity to v27.0.
- ComposeGraphViewer (Phase 19) preserved in Graph tab.
- Constituent containers click through to ContainerDetailSheet which carries Phase 17 logs + Phase 18 files + Phase 19 vuln-scan unchanged.
- `useDockerResource` 5-slot store extended; selectedStack contract test passes.
- Build green; all UI vitest tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/27-stacks-schedules-routes/27-01-SUMMARY.md` documenting:
- Final file list (created vs modified).
- Decisions made (especially: stack-detail Logs+Files surfaced via per-container ContainerDetailSheet click-through, NOT new stack-level Logs/Files tabs — see Open Item below).
- Deviations from plan (Rule 1/2/3 auto-fixes if any).
- Threat-model status (T-27-01 mitigated via maxLength=200; rest accept/n.a.).
- Phase 27-02 readiness: `routes/server-control/` cross-imports remaining: `ComposeGraphViewer`, `ContainerDetailSheet`, `ContainerCreateForm`, `AlertsBell`, `EnvironmentSelector`. Plan 27-02 must relocate ALL FIVE before deleting the directory.
- **Open Item to flag in SUMMARY.md (executor note):** ROADMAP success criterion 2 for Phase 27 reads "Stack detail panel preserves Graph / Logs / Files tabs (Phase 19 + Phase 17 + Phase 18)". The legacy v27.0 server-control StacksTab has only Containers + Graph tabs — Phase 17 (real-time container logs) and Phase 18 (container file browser) were per-container features, not per-stack. This plan preserves Phase 17 + 18 features by making constituent-container rows clickable to open the existing `ContainerDetailSheet` (which already contains Logs + Files tabs). If Phase 27 reviewers determine that ROADMAP intended dedicated stack-level Logs/Files tabs (aggregating across constituent containers), that would be NEW backend + UI work not present in v27.0 source — flag for Phase 28 or follow-up phase. The current plan ships the closest no-new-backend interpretation.
</output>
