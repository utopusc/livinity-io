---
phase: 27-stacks-schedules-routes
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - livos/packages/ui/src/routes/docker/schedules/scheduler-section.tsx
  - livos/packages/ui/src/routes/docker/schedules/add-backup-dialog.tsx
  - livos/packages/ui/src/routes/docker/schedules/job-card.tsx
  - livos/packages/ui/src/routes/docker/sections/schedules.tsx
  - livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  - livos/packages/ui/src/routes/settings/_components/scheduler-section.tsx
  - livos/packages/ui/src/modules/window/window-content.tsx
  - livos/packages/ui/src/modules/window/app-contents/server-control-content.tsx
  - livos/packages/ui/src/routes/docker/_components/environment-selector.tsx
  - livos/packages/ui/src/routes/docker/_components/ai-alerts-bell.tsx
  - livos/packages/ui/src/routes/docker/_components/compose-graph-viewer.tsx
  - livos/packages/ui/src/routes/docker/resources/container-create-form.tsx
  - livos/packages/ui/src/routes/docker/resources/container-detail-sheet.tsx
  - livos/packages/ui/src/routes/docker/resources/container-files-tab.tsx
  - livos/packages/ui/src/routes/docker/resources/domains-tab.tsx
  - livos/packages/ui/src/routes/docker/status-bar.tsx
  - livos/packages/ui/src/routes/docker/resources/container-section.tsx
  - livos/packages/ui/src/routes/docker/stacks/stack-section.tsx
autonomous: false
requirements: [DOC-12, DOC-03]
must_haves:
  truths:
    - "User clicks Schedules in Docker app sidebar and sees the scheduler job list — same job rows, badges, schedule, last run, next run, status, Run Now / toggle / Delete buttons as the v27.0 Settings > Scheduler page."
    - "User clicks 'Add Backup' from the Schedules section and sees a dialog with name + volume picker + cron + destination type (Local / S3 / SFTP) + Test Destination + Save — same fields as v27.0."
    - "When the user navigates from Volumes section via per-row 'Schedule backup' link, the Add Backup dialog opens automatically with the volume field pre-filled — closes the Phase 26-02 cross-section navigation seam contract."
    - "The legacy Settings > Scheduler menu item is removed (DOC-12 owns scheduler now); existing users hitting the old menu see no broken link because the menu item itself is gone from the MENU_ITEMS array."
    - "After the cleanup task: `livos/packages/ui/src/routes/server-control/` directory does not exist on disk, and `grep -rn 'from.*server-control' livos/packages/ui/src/` returns ZERO results."
    - "The `ServerControlWindowContent` lazy import + the `app-contents/server-control-content.tsx` adapter are deleted; the deprecation banner code path is gone."
    - "All previously cross-imported components (ComposeGraphViewer, ContainerDetailSheet, ContainerCreateForm, ContainerFilesTab, DomainsTab, EnvironmentSelector, AiAlertsBell) live in their final homes inside `routes/docker/`."
    - "DOC-03 fully closed (legacy Server Control removed entirely from disk and routing)."
  artifacts:
    - path: "livos/packages/ui/src/routes/docker/schedules/scheduler-section.tsx"
      provides: "SchedulerSection — full Schedules body (job list + Add Backup button + volume pre-fill from useSelectedVolume)"
      min_lines: 200
    - path: "livos/packages/ui/src/routes/docker/schedules/add-backup-dialog.tsx"
      provides: "AddBackupDialog (volume + dest type + cron + Test + Save) with volume pre-fill from useSelectedVolume()"
      min_lines: 250
    - path: "livos/packages/ui/src/routes/docker/schedules/job-card.tsx"
      provides: "JobCard sub-component (Run Now / Toggle / Delete per-row)"
    - path: "livos/packages/ui/src/routes/docker/sections/schedules.tsx"
      provides: "1-line re-export of SchedulerSection (replaces Phase 24 placeholder)"
    - path: "livos/packages/ui/src/routes/docker/_components/environment-selector.tsx"
      provides: "EnvironmentSelector — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/_components/ai-alerts-bell.tsx"
      provides: "AlertsBell — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/_components/compose-graph-viewer.tsx"
      provides: "ComposeGraphViewer — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/resources/container-detail-sheet.tsx"
      provides: "ContainerDetailSheet — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/resources/container-create-form.tsx"
      provides: "ContainerCreateForm — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/resources/container-files-tab.tsx"
      provides: "ContainerFilesTab (FilesTab) — relocated from server-control/"
    - path: "livos/packages/ui/src/routes/docker/resources/domains-tab.tsx"
      provides: "DomainsTab — relocated from server-control/"
  key_links:
    - from: "src/routes/docker/sections/schedules.tsx"
      to: "src/routes/docker/schedules/scheduler-section.tsx"
      via: "named re-export `Schedules`"
      pattern: "export \\{.*SchedulerSection as Schedules.*\\}"
    - from: "src/routes/docker/schedules/scheduler-section.tsx"
      to: "src/routes/docker/resource-store.ts"
      via: "useSelectedVolume() — Phase 26-02 contract; opens AddBackupDialog with volume pre-filled when arriving from Volumes section"
      pattern: "useSelectedVolume"
    - from: "src/routes/docker/schedules/add-backup-dialog.tsx"
      to: "src/trpc/trpc.ts (scheduler.testBackupDestination + scheduler.upsertJob + docker.listVolumes)"
      via: "trpcReact mutations + queries — unchanged from legacy"
      pattern: "scheduler\\.(testBackupDestination|upsertJob)|docker\\.listVolumes"
    - from: "src/routes/docker/status-bar.tsx"
      to: "src/routes/docker/_components/{environment-selector,ai-alerts-bell}"
      via: "import path updated from '@/routes/server-control/...' to '@/routes/docker/_components/...'"
      pattern: "from '@/routes/docker/_components/"
    - from: "src/routes/docker/resources/container-section.tsx"
      to: "src/routes/docker/resources/{container-detail-sheet,container-create-form}"
      via: "import path updated to relative './...' inside resources/"
      pattern: "from '\\./(container-detail-sheet|container-create-form)'"
    - from: "src/routes/docker/stacks/stack-section.tsx"
      to: "src/routes/docker/_components/compose-graph-viewer + src/routes/docker/resources/container-detail-sheet"
      via: "import paths updated post-relocation"
      pattern: "from '\\.\\./_components/compose-graph-viewer'|from '\\.\\./resources/container-detail-sheet'"
---

<objective>
Replace the Phase 24 Schedules placeholder with the full live Schedules section ported from `routes/settings/_components/scheduler-section.tsx`. Add the Phase 26-02 contract: when the user navigates from Volumes via per-row "Schedule backup" link, `useSelectedVolume()` is non-null on mount → automatically open the AddBackupDialog with the volume picker pre-set. Then perform the FINAL CLEANUP that closes DOC-03: relocate the 7 cross-imported components into their permanent homes in `routes/docker/`, update all import paths, then DELETE `livos/packages/ui/src/routes/server-control/` directory + the `ServerControlWindowContent` lazy import + the `server-control-content.tsx` adapter, AND remove the now-orphaned Settings > Scheduler menu entry (since Schedules is now in Docker).

Purpose: Closes DOC-12 (Schedules route + Run Now + Test Destination + AddJob dialog with Volume pre-fill) AND DOC-03 (final server-control directory delete). After this plan ships, ZERO references to `routes/server-control` remain in the codebase.

Output: Live Schedules section reachable from `Sidebar > Schedules` with Volume pre-fill from `useSelectedVolume()`. Legacy server-control entirely gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-stacks-schedules-routes/27-CONTEXT.md
@.planning/phases/27-stacks-schedules-routes/27-01-SUMMARY.md
@.planning/phases/26-resource-routes/26-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Source files to read (extract — do not re-read)
@livos/packages/ui/src/routes/settings/_components/scheduler-section.tsx   # 792 lines: SchedulerSection + JobCard + AddBackupDialog + StatusBadge + relTime + emptyDestination + types
@livos/packages/ui/src/routes/settings/_components/settings-content.tsx    # Find MENU_ITEMS scheduler entry + case 'scheduler' switch
@livos/packages/ui/src/routes/docker/sections/schedules.tsx                # Phase 24 placeholder to replace
@livos/packages/ui/src/routes/docker/resource-store.ts                     # useSelectedVolume contract (Plan 26-02)
@livos/packages/ui/src/routes/docker/resources/volume-section.tsx          # Read inline comment around line 70-77 for the pre-fill contract
@livos/packages/ui/src/routes/server-control/index.tsx                     # Confirm exports list before delete
@livos/packages/ui/src/routes/server-control/compose-graph-viewer.tsx
@livos/packages/ui/src/routes/server-control/container-detail-sheet.tsx
@livos/packages/ui/src/routes/server-control/container-create-form.tsx
@livos/packages/ui/src/routes/server-control/container-files-tab.tsx
@livos/packages/ui/src/routes/server-control/domains-tab.tsx
@livos/packages/ui/src/routes/server-control/environment-selector.tsx
@livos/packages/ui/src/routes/server-control/ai-alerts-bell.tsx
@livos/packages/ui/src/modules/window/window-content.tsx                   # Find ServerControlWindowContent lazy import
@livos/packages/ui/src/modules/window/app-contents/server-control-content.tsx  # 1-line adapter to delete

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/routes/docker/resource-store.ts (Plan 26-02 contract):
```typescript
export const useSelectedVolume = () => useDockerResource((s) => s.selectedVolume)
// When VolumeSection's "Schedule backup" link fires onScheduleBackup(volumeName):
//   setSelectedVolume(volumeName); setSection('schedules')
// SchedulerSection MUST: on mount, if useSelectedVolume() is non-null, open
// AddBackupDialog with volumeName pre-filled. Phase 26-02 D-02 contract.
```

From src/routes/settings/_components/scheduler-section.tsx (current implementation to port):
```typescript
type DestinationType = 's3' | 'sftp' | 'local'
type SftpAuthMethod = 'password' | 'privateKey'
interface S3Form { type: 's3'; endpoint, region, bucket, prefix, accessKeyId, forcePathStyle, secretAccessKey }
interface SftpForm { type: 'sftp'; host, port, username, remotePath, authMethod, password, privateKey, passphrase }
interface LocalForm { type: 'local'; path }
type DestinationForm = S3Form | SftpForm | LocalForm
interface JobRow { id, name, schedule, type, config, enabled, lastRun, lastRunStatus, lastRunError, lastRunOutput, nextRun, createdAt, updatedAt }
function emptyDestination(type): DestinationForm
function StatusBadge({status}): JSX.Element
function relTime(d): string
export function SchedulerSection(): JSX.Element  // top-level — port verbatim
function JobCard({job, onRunNow, onToggle, onDelete, isRunning}): JSX.Element
function AddBackupDialog({open, onOpenChange, onSaved}): JSX.Element  // ADD: initialVolumeName?: string prop
```

From src/routes/settings/_components/settings-content.tsx:
```typescript
// Line 142: 'scheduler' in SettingsSection union
// Line 172: { id: 'scheduler', icon: TbServerCog, label: 'Scheduler', description: 'Maintenance + backup tasks', adminOnly: true }
// Line 459-460: case 'scheduler': return <SchedulerSectionLazy />
// Line 1409-1411: const SchedulerSectionLazy = React.lazy(() => import('./scheduler-section').then((m) => ({default: m.SchedulerSection})))
// REMOVE all four locations.
```

From src/modules/window/window-content.tsx:
```typescript
// Line 12-15: deprecation banner adapter import
const ServerControlWindowContent = React.lazy(() => import('./app-contents/server-control-content'))
// REMOVE this lazy import + any reference to it (eslint-disable comment too).
```

From src/routes/docker/status-bar.tsx (existing Plan 24-02 cross-imports):
```typescript
import {AlertsBell} from '@/routes/server-control/ai-alerts-bell'
import {EnvironmentSelector} from '@/routes/server-control/environment-selector'
// UPDATE both to '@/routes/docker/_components/ai-alerts-bell' and '@/routes/docker/_components/environment-selector'
```

From src/routes/docker/resources/container-section.tsx (existing Plan 26-01 cross-imports):
```typescript
import {ContainerCreateForm} from '@/routes/server-control/container-create-form'
import {ContainerDetailSheet} from '@/routes/server-control/container-detail-sheet'
// UPDATE to './container-create-form' and './container-detail-sheet' (siblings inside resources/)
```

From src/routes/docker/stacks/stack-section.tsx (Plan 27-01 cross-imports):
```typescript
import {ComposeGraphViewer} from '@/routes/server-control/compose-graph-viewer'
import {ContainerDetailSheet} from '@/routes/server-control/container-detail-sheet'
// UPDATE to '../_components/compose-graph-viewer' and '../resources/container-detail-sheet'
```

From src/routes/server-control/container-detail-sheet.tsx:
```typescript
import {FilesTab} from './container-files-tab'  // becomes './container-files-tab' relative path inside the new resources/ home (still relative — no change needed if both move together)
```
</interfaces>

# Cross-import inventory (sources to update post-relocation)

After file relocation, the following files have import paths that need updating:

| File needing update | Old path | New path |
|---|---|---|
| `routes/docker/status-bar.tsx` | `@/routes/server-control/ai-alerts-bell` | `@/routes/docker/_components/ai-alerts-bell` |
| `routes/docker/status-bar.tsx` | `@/routes/server-control/environment-selector` | `@/routes/docker/_components/environment-selector` |
| `routes/docker/resources/container-section.tsx` | `@/routes/server-control/container-create-form` | `./container-create-form` |
| `routes/docker/resources/container-section.tsx` | `@/routes/server-control/container-detail-sheet` | `./container-detail-sheet` |
| `routes/docker/stacks/stack-section.tsx` | `@/routes/server-control/compose-graph-viewer` | `../_components/compose-graph-viewer` |
| `routes/docker/stacks/stack-section.tsx` | `@/routes/server-control/container-detail-sheet` | `../resources/container-detail-sheet` |
| `routes/docker/resources/container-detail-sheet.tsx` (relocated) | `./container-files-tab` | `./container-files-tab` (NO CHANGE — both move into resources/, relative still resolves) |
| `routes/docker/resources/container-detail-sheet.tsx` (relocated) | `./domains-tab` | `./domains-tab` (NO CHANGE — both move into resources/) |

Run `grep -rn "from.*server-control" livos/packages/ui/src/` after Task 3 to ensure ZERO matches remain.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port SchedulerSection + AddBackupDialog with volume pre-fill from useSelectedVolume()</name>
  <files>
    livos/packages/ui/src/routes/docker/schedules/scheduler-section.tsx,
    livos/packages/ui/src/routes/docker/schedules/job-card.tsx,
    livos/packages/ui/src/routes/docker/schedules/add-backup-dialog.tsx,
    livos/packages/ui/src/routes/docker/sections/schedules.tsx
  </files>
  <action>
Port the existing `SchedulerSection` from `routes/settings/_components/scheduler-section.tsx` (792 lines) into a new `routes/docker/schedules/` subdirectory split across 3 files for clarity, plus a 1-line section re-export. Add ONE new behavior: AddBackupDialog accepts an `initialVolumeName?: string` prop, and SchedulerSection reads `useSelectedVolume()` on mount to auto-open the dialog with that volume pre-selected — closes the Phase 26-02 contract.

**File 1 — `schedules/job-card.tsx`:**
- Verbatim port of `JobCard` sub-component (legacy `scheduler-section.tsx` lines 299-384) PLUS the supporting `StatusBadge` (lines 117-129), `relTime` (lines 131-148), and `TYPE_LABELS`, `STATUS_STYLES`, `JobRow` interface (lines 87-115).
- Imports: `TbDatabase, TbEdit, TbLoader2, TbPlayerPlay, TbPlus, TbServerCog, TbTrash` from `react-icons/tb`; `Button, Switch` shadcn; `cn`.
- Exports: `JobCard`, `JobRow` (type), `StatusBadge`, `relTime`, `TYPE_LABELS`.
- Comment header: `// Phase 27-02 (DOC-12) — ported verbatim from routes/settings/_components/scheduler-section.tsx (lines 87-148, 299-384). Sub-components extracted to a sibling file for SchedulerSection clarity.`

**File 2 — `schedules/add-backup-dialog.tsx`:**
- Port of `AddBackupDialog` (legacy lines 390-792 — bulk of the dialog body including the destination type form switching).
- Port the type definitions: `DestinationType, SftpAuthMethod, S3Form, SftpForm, LocalForm, DestinationForm` (legacy lines 54-85), and `emptyDestination` factory (lines 154-179).
- **ADD a new prop `initialVolumeName?: string`** to the component signature:
  ```typescript
  function AddBackupDialog({open, onOpenChange, onSaved, initialVolumeName}: {
    open: boolean
    onOpenChange: (open: boolean) => void
    onSaved: () => void
    initialVolumeName?: string
  })
  ```
- **WIRE the pre-fill:** Inside the existing `useEffect(() => { if (!open) { ...reset } }, [open])` block, ADD a separate `useEffect(() => { if (open && initialVolumeName) setVolumeName(initialVolumeName) }, [open, initialVolumeName])`. Keep the existing reset semantics intact (close → blank).
- Imports: `useEffect, useMemo, useState` from React; many `react-icons/tb` icons; shadcn primitives (`Button, Checkbox, Dialog*, Input, Label, RadioGroup*, Select*, Switch`); `toast` from sonner; `cn`, `trpcReact`.
- Exports: `AddBackupDialog`.
- Comment header: `// Phase 27-02 (DOC-12) — ported from routes/settings/_components/scheduler-section.tsx AddBackupDialog (lines 390-792). NEW: initialVolumeName prop pre-fills the volume picker when SchedulerSection opens it via the Phase 26-02 useSelectedVolume() seam.`

**File 3 — `schedules/scheduler-section.tsx`:**
- Port of `SchedulerSection` top-level export (legacy lines 185-293).
- **Add the Phase 26-02 seam:** at the top of the component, read `useSelectedVolume()` and the `setSelectedVolume` setter from `useDockerResource`. Add a `useEffect` that opens the AddBackupDialog with the volume pre-filled WHEN there's a non-null `selectedVolume` AND the dialog is currently closed. After consuming, clear the slot:
  ```typescript
  const selectedVolume = useSelectedVolume()
  const setSelectedVolume = useDockerResource((s) => s.setSelectedVolume)
  const [showAddDialog, setShowAddDialog] = useState(false)
  const [pendingVolumeName, setPendingVolumeName] = useState<string | null>(null)

  useEffect(() => {
    if (selectedVolume && !showAddDialog) {
      setPendingVolumeName(selectedVolume)
      setShowAddDialog(true)
      setSelectedVolume(null) // consume the slot — Phase 26-02 contract
    }
  }, [selectedVolume, showAddDialog, setSelectedVolume])
  ```
  Pass `initialVolumeName={pendingVolumeName ?? undefined}` to AddBackupDialog. Clear `pendingVolumeName` when the dialog closes.
- **Wrap in flex h-full overflow-y-auto p-4 sm:p-6** to match other section components from Plan 26.
- Imports: `useEffect, useState` from React; `trpcReact`; `Button` shadcn; `TbLoader2, TbPlus, TbServerCog`; `toast` from sonner; `JobCard`, `JobRow` from `./job-card`; `AddBackupDialog` from `./add-backup-dialog`; `useSelectedVolume, useDockerResource` from `../resource-store`.
- Export: `SchedulerSection`.
- Comment header: `// Phase 27-02 (DOC-12) — ported from routes/settings/_components/scheduler-section.tsx (lines 185-293). NEW: useSelectedVolume() seam — when arriving from Volumes section's "Schedule backup" link, AddBackupDialog auto-opens with volume pre-filled (Phase 26-02 contract).`

**File 4 — `sections/schedules.tsx` (replace Phase 24 placeholder):**
```tsx
// Phase 27-02 — replaces the Phase 24 placeholder.
// Body lives in routes/docker/schedules/scheduler-section.tsx (DOC-12).
export {SchedulerSection as Schedules} from '../schedules/scheduler-section'
```

**Per CONVENTIONS.md / STRUCTURE.md:** tabs not spaces; single-quotes; Tabler/react-icons usage matches neighbors; shadcn primitives.

**Do NOT** alter the legacy `routes/settings/_components/scheduler-section.tsx` yet — Task 2 removes the Settings > Scheduler menu entry and deletes the file.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/config build && pnpm --filter ui build</automated>
    <manual>
1. `cd livos/packages/ui && pnpm dev`. Open Docker app → Schedules in sidebar.
2. Job list renders with the same shape as v27.0 Settings > Scheduler — built-in jobs flagged "Built-in", per-row Run Now / toggle / Delete (Delete only on user jobs).
3. Click 'Add Backup' → dialog opens. Switch destination types (Local / S3 / SFTP). Fill SFTP form, click Test Destination — toast shows latency or error. Save creates job; list refreshes.
4. Navigate to Volumes section → click any volume's "Schedule backup" icon. Section flips to Schedules AND AddBackupDialog opens with the volume pre-selected. Close dialog without saving → returning to Volumes shows no stale state.
5. Run Now on a built-in job → toast confirms; Last run + Status badge update on next 10s poll.
6. Toggle a job — Switch flips, toast confirms.
7. Delete a user job — confirm modal works, job removed.
    </manual>
  </verify>
  <done>
- `pnpm --filter ui build` green.
- Schedules section renders job list + AddBackup dialog with all v27.0 features (Run Now, toggle, Delete, Test Destination, S3/SFTP/Local destination forms).
- Phase 26-02 volume pre-fill seam works end-to-end (Volumes click → Schedules AddBackup dialog opens with volume selected).
- Phase 24 placeholder string ("Coming in Phase 27 — Scheduler jobs + Run Now") removed from sections/schedules.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relocate cross-imported components from routes/server-control to routes/docker; update import paths in 3 dependent files; remove Settings > Scheduler menu entry</name>
  <files>
    livos/packages/ui/src/routes/docker/_components/environment-selector.tsx,
    livos/packages/ui/src/routes/docker/_components/ai-alerts-bell.tsx,
    livos/packages/ui/src/routes/docker/_components/compose-graph-viewer.tsx,
    livos/packages/ui/src/routes/docker/resources/container-create-form.tsx,
    livos/packages/ui/src/routes/docker/resources/container-detail-sheet.tsx,
    livos/packages/ui/src/routes/docker/resources/container-files-tab.tsx,
    livos/packages/ui/src/routes/docker/resources/domains-tab.tsx,
    livos/packages/ui/src/routes/docker/status-bar.tsx,
    livos/packages/ui/src/routes/docker/resources/container-section.tsx,
    livos/packages/ui/src/routes/docker/stacks/stack-section.tsx,
    livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  </files>
  <action>
**Phase A — Move 7 files** from `routes/server-control/` into their permanent docker homes. Use `git mv` so the file history follows. The split is:
- `_components/` (chrome / shared components used by `routes/docker/*` chrome): `environment-selector.tsx`, `ai-alerts-bell.tsx`, `compose-graph-viewer.tsx`.
- `resources/` (container-related sub-components, sit alongside `container-section.tsx`): `container-create-form.tsx`, `container-detail-sheet.tsx`, `container-files-tab.tsx`, `domains-tab.tsx`.

Commands (Bash):
```bash
mkdir -p livos/packages/ui/src/routes/docker/_components
git mv livos/packages/ui/src/routes/server-control/environment-selector.tsx livos/packages/ui/src/routes/docker/_components/environment-selector.tsx
git mv livos/packages/ui/src/routes/server-control/ai-alerts-bell.tsx       livos/packages/ui/src/routes/docker/_components/ai-alerts-bell.tsx
git mv livos/packages/ui/src/routes/server-control/compose-graph-viewer.tsx livos/packages/ui/src/routes/docker/_components/compose-graph-viewer.tsx
git mv livos/packages/ui/src/routes/server-control/container-create-form.tsx   livos/packages/ui/src/routes/docker/resources/container-create-form.tsx
git mv livos/packages/ui/src/routes/server-control/container-detail-sheet.tsx  livos/packages/ui/src/routes/docker/resources/container-detail-sheet.tsx
git mv livos/packages/ui/src/routes/server-control/container-files-tab.tsx     livos/packages/ui/src/routes/docker/resources/container-files-tab.tsx
git mv livos/packages/ui/src/routes/server-control/domains-tab.tsx             livos/packages/ui/src/routes/docker/resources/domains-tab.tsx
```

**Phase B — Update import paths inside the moved files** so internal relative imports still resolve:
- `_components/compose-graph-viewer.tsx` — verify it does NOT import any other server-control siblings (per legacy structure it's standalone). If it imports anything via `'./'` from server-control, update to absolute `@/routes/docker/...`. Read the file before editing.
- `resources/container-detail-sheet.tsx` — internal imports `./container-files-tab` and (likely) `./domains-tab` STILL resolve since both also moved into `resources/` — leave as-is. Verify by reading.
- `resources/container-create-form.tsx`, `container-files-tab.tsx`, `domains-tab.tsx` — same: `./container-detail-sheet` etc. still resolve. Verify by reading after move.
- `_components/environment-selector.tsx` and `_components/ai-alerts-bell.tsx` — read; if they import siblings (unlikely — they're standalone chrome), update.

**Phase C — Update import paths in 3 consumer files** that previously cross-imported from `routes/server-control`:

In `livos/packages/ui/src/routes/docker/status-bar.tsx`:
- Replace `import {AlertsBell} from '@/routes/server-control/ai-alerts-bell'` with `import {AlertsBell} from '@/routes/docker/_components/ai-alerts-bell'`.
- Replace `import {EnvironmentSelector} from '@/routes/server-control/environment-selector'` with `import {EnvironmentSelector} from '@/routes/docker/_components/environment-selector'`.

In `livos/packages/ui/src/routes/docker/resources/container-section.tsx`:
- Replace `import {ContainerCreateForm} from '@/routes/server-control/container-create-form'` with `import {ContainerCreateForm} from './container-create-form'`.
- Replace `import {ContainerDetailSheet} from '@/routes/server-control/container-detail-sheet'` with `import {ContainerDetailSheet} from './container-detail-sheet'`.

In `livos/packages/ui/src/routes/docker/stacks/stack-section.tsx`:
- Replace `import {ComposeGraphViewer} from '@/routes/server-control/compose-graph-viewer'` with `import {ComposeGraphViewer} from '../_components/compose-graph-viewer'`.
- Replace `import {ContainerDetailSheet} from '@/routes/server-control/container-detail-sheet'` with `import {ContainerDetailSheet} from '../resources/container-detail-sheet'`.

**Phase D — Remove the Settings > Scheduler menu entry** in `livos/packages/ui/src/routes/settings/_components/settings-content.tsx`:
1. Remove `'scheduler'` from the `SettingsSection` union (line ~142).
2. Remove the menu entry on line ~172: `{id: 'scheduler', icon: TbServerCog, label: 'Scheduler', description: 'Maintenance + backup tasks', adminOnly: true}`.
3. Remove the case on line ~459-460: `case 'scheduler': return <SchedulerSectionLazy />`.
4. Remove the lazy import on line ~1409-1411: `const SchedulerSectionLazy = React.lazy(() => import('./scheduler-section').then((m) => ({default: m.SchedulerSection})))`.
5. If `TbServerCog` is no longer used in this file after the menu entry removal, remove its import line. (Keep otherwise.)

**Do NOT delete the legacy `routes/settings/_components/scheduler-section.tsx` yet** — Task 3 deletes it together with `routes/server-control/`.

**Per CONVENTIONS.md:** Use `git mv` for renames so blame follows. Update imports atomically — keep build green at every commit boundary.

**Build verification at end of Task 2 is critical:** `pnpm --filter ui build` MUST succeed before Task 3 (delete) runs. If a build error reveals a missed import path, fix it before proceeding.
  </action>
  <verify>
    <automated>
cd livos && pnpm --filter @livos/config build && pnpm --filter ui build
    </automated>
    <manual>
1. `grep -rn "from '@/routes/server-control" livos/packages/ui/src/` returns ZERO matches except inside `routes/server-control/` itself (which is still present until Task 3 deletes it).
2. `git status` shows the 7 git-renamed files + 4 modified files (status-bar.tsx, container-section.tsx, stack-section.tsx, settings-content.tsx) + 0 deletions yet.
3. UI dev server still works: open Docker app → Containers / Images / Stacks all functional; expanded container row opens detail sheet (Logs / Files / Domains tabs all work — they're imported from new resources/ paths now).
4. Open Settings via UI — Scheduler menu item is gone from the sidebar (admin user). Schedules functionality only accessible via Docker app sidebar.
    </manual>
  </verify>
  <done>
- 7 files git-mv'd to their new homes.
- Internal relative imports inside moved files still resolve (verified via build).
- 3 consumer files updated with new import paths.
- Settings > Scheduler menu entry + lazy import + switch case removed (4 edits in `settings-content.tsx`).
- `pnpm --filter ui build` green.
- `grep -rn "from '@/routes/server-control" livos/packages/ui/src/` returns matches ONLY inside `routes/server-control/` itself (no external consumers).
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete server-control directory + ServerControlWindowContent + legacy scheduler-section + verify zero references remain (DOC-03 final close)</name>
  <files>
    livos/packages/ui/src/routes/server-control/index.tsx,
    livos/packages/ui/src/modules/window/window-content.tsx,
    livos/packages/ui/src/modules/window/app-contents/server-control-content.tsx,
    livos/packages/ui/src/routes/settings/_components/scheduler-section.tsx
  </files>
  <action>
**Phase A — Pre-delete verification (FAIL FAST if anything is still referencing legacy):**

Run these commands FIRST. If ANY return a non-empty result outside `routes/server-control/` itself, FIX before deleting:
```bash
grep -rn "from.*server-control" livos/packages/ui/src/ 2>/dev/null | grep -v "/server-control/index.tsx" | grep -v "// We keep the ServerControlWindowContent" || echo "OK"
grep -rn "ServerControlWindowContent" livos/packages/ui/src/ 2>/dev/null
grep -rn "LIVINITY_server-control" livos/packages/ui/src/ 2>/dev/null
grep -rn "from.*scheduler-section" livos/packages/ui/src/ 2>/dev/null | grep -v "/_components/scheduler-section.tsx" || echo "OK"
```

Expected: only references inside the files about to be deleted, or zero. If anything else surfaces, the executor must update it before proceeding.

**Phase B — Delete the legacy files:**

```bash
# 1. Delete the entire server-control directory (only index.tsx remains after Task 2's git-mv)
git rm -r livos/packages/ui/src/routes/server-control/

# 2. Delete the ServerControlWindowContent adapter
git rm livos/packages/ui/src/modules/window/app-contents/server-control-content.tsx

# 3. Delete the legacy Settings scheduler-section (replaced by routes/docker/schedules/scheduler-section.tsx in Task 1)
git rm livos/packages/ui/src/routes/settings/_components/scheduler-section.tsx
```

**Phase C — Edit `livos/packages/ui/src/modules/window/window-content.tsx`:**

Remove the `ServerControlWindowContent` lazy import and any associated `eslint-disable-next-line @typescript-eslint/no-unused-vars` line. Read the file first to find the exact lines (Plan 24-01 SUMMARY noted lines ~12-15 contain the deprecation-rollback import comment + lazy-load line). Delete:
- The 1-line eslint-disable comment (if present).
- The 1-line `const ServerControlWindowContent = React.lazy(() => import('./app-contents/server-control-content'))` declaration.
- Any related comment block explaining "kept for one-phase rollback safety".

Make sure NO switch case in the file references `ServerControlWindowContent` (Plan 24-01 already removed the case — verify by reading the switch block; if any leftover, remove).

**Phase D — Post-delete verification:**

```bash
# All four MUST return zero matches:
grep -rn "from.*server-control" livos/packages/ui/src/
grep -rn "ServerControlWindowContent" livos/packages/ui/src/
grep -rn "LIVINITY_server-control" livos/packages/ui/src/
grep -rn "from.*scheduler-section" livos/packages/ui/src/
ls livos/packages/ui/src/routes/server-control/ 2>/dev/null  # MUST return "No such file or directory"
ls livos/packages/ui/src/modules/window/app-contents/server-control-content.tsx 2>/dev/null  # MUST return "No such file or directory"
ls livos/packages/ui/src/routes/settings/_components/scheduler-section.tsx 2>/dev/null  # MUST return "No such file or directory"
```

**Phase E — Final build + test:**
```bash
cd livos && pnpm --filter @livos/config build && pnpm --filter ui build
pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom
```

If any test or build fails, fix the cause (likely a stale import path or a missed reference) before this task is "done".

**DOC-03 closure justification:** Plan 24-01 made `LIVINITY_server-control` unreachable from dock/desktop/mobile/spotlight. Plan 24-02 closed the deprecation banner. This task removes the file from disk + every reference to it. DOC-03 is now fully closed.
  </action>
  <verify>
    <automated>
cd livos && pnpm --filter @livos/config build && pnpm --filter ui build && pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom
    </automated>
    <manual>
1. `git status` shows: 1 deleted directory (`routes/server-control/`), 2 deleted files (`server-control-content.tsx` adapter + legacy `scheduler-section.tsx`), 1 modified (`window-content.tsx` no-longer-imports the adapter).
2. UI dev server still works end-to-end: open Docker app → all 12 sections render correctly. Containers / Images / Stacks / Volumes / Networks / Schedules all functional. ContainerDetailSheet opens with Logs / Files / Domains tabs working. ComposeGraphViewer renders in Stack Graph tab. EnvironmentSelector + AlertsBell + ThemeToggle all render in StatusBar.
3. Settings page: open Settings — no Scheduler menu entry; no console errors about missing module.
4. Try to navigate to `/server-control` URL directly — 404 (route was never registered post-Plan-24-01; just sanity-check).
5. `git log --stat` shows reasonable file history preservation via git-mv (the moved files retain blame from server-control/).
    </manual>
  </verify>
  <done>
- `routes/server-control/` directory does not exist on disk.
- `app-contents/server-control-content.tsx` adapter deleted.
- Legacy `routes/settings/_components/scheduler-section.tsx` deleted.
- `window-content.tsx` no longer references `ServerControlWindowContent`.
- All four post-delete grep checks return ZERO matches.
- `pnpm --filter ui build` green.
- `pnpm --filter ui exec vitest run src/routes/docker/` all tests pass.
- DOC-03 fully closed (legacy server-control gone from disk + zero references in codebase).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- `routes/docker/schedules/` subdirectory with `scheduler-section.tsx` + `add-backup-dialog.tsx` + `job-card.tsx` (DOC-12).
- `sections/schedules.tsx` placeholder replaced with re-export.
- `useSelectedVolume()` consumed in SchedulerSection: clicking "Schedule backup" on a volume now flips to Schedules section AND auto-opens AddBackupDialog with the volume pre-selected (closes Phase 26-02 contract).
- 7 components relocated from `routes/server-control/` to their final homes: `_components/{environment-selector,ai-alerts-bell,compose-graph-viewer}.tsx` + `resources/{container-create-form,container-detail-sheet,container-files-tab,domains-tab}.tsx`.
- 3 consumer files updated with new import paths (status-bar.tsx, container-section.tsx, stack-section.tsx).
- Settings > Scheduler menu entry + lazy import removed (4 edits in `settings-content.tsx`).
- `routes/server-control/` directory + `app-contents/server-control-content.tsx` + legacy `scheduler-section.tsx` DELETED.
- `ServerControlWindowContent` lazy import removed from `window-content.tsx`.
- DOC-03 fully closed; ZERO references to `routes/server-control` remain anywhere in the codebase.
  </what-built>
  <how-to-verify>
1. `cd livos/packages/ui && pnpm dev` (or rebuild on server: `pnpm --filter @livos/config build && pnpm --filter ui build && systemctl restart livos`).
2. **Schedules section functional smoke (must all pass):**
   - [ ] Open Docker app → Schedules in sidebar. Job list renders. Built-in jobs show "Built-in" badge; user jobs do not.
   - [ ] Per-job: Run Now button works (toast + status badge updates on next poll). Switch toggles enabled state. Delete (user jobs only) prompts and removes the job.
   - [ ] Add Backup button → dialog opens with default Local destination, blank volume picker.
   - [ ] Switch destination type to S3 → conditional fields render (endpoint, region, bucket, prefix, accessKey, secretAccessKey, forcePathStyle).
   - [ ] Switch to SFTP → host, port, username, remotePath, authMethod radio (password / privateKey), conditional auth field render.
   - [ ] Test Destination button: toast confirms latency for valid creds, error toast for invalid.
   - [ ] Save creates job, dialog closes, list refreshes.
3. **Volume pre-fill seam (Phase 26-02 contract):**
   - [ ] Navigate to Volumes section → click any volume's "Schedule backup" icon (calendar icon).
   - [ ] Section flips to Schedules AND AddBackupDialog auto-opens with the volume pre-selected in the picker.
   - [ ] Close dialog without saving → re-navigate to Volumes → no stale state.
4. **Cleanup verification (DOC-03 final close):**
   - [ ] `ls livos/packages/ui/src/routes/server-control/` returns "No such file or directory".
   - [ ] `grep -rn "from.*server-control" livos/packages/ui/src/` returns zero matches.
   - [ ] `grep -rn "ServerControlWindowContent" livos/packages/ui/src/` returns zero matches.
   - [ ] `grep -rn "LIVINITY_server-control" livos/packages/ui/src/` returns zero matches.
   - [ ] `grep -rn "from.*scheduler-section" livos/packages/ui/src/` returns zero matches.
5. **Functional regression sanity (relocated components still work):**
   - [ ] StatusBar renders: EnvironmentSelector dropdown opens (relocated to `_components/environment-selector.tsx`); AlertsBell opens panel (relocated to `_components/ai-alerts-bell.tsx`).
   - [ ] Containers section: clicking a container row opens ContainerDetailSheet (relocated to `resources/container-detail-sheet.tsx`); Logs / Files / Domains tabs all render and function.
   - [ ] Stacks section: expand row → Graph tab renders ComposeGraphViewer (relocated to `_components/compose-graph-viewer.tsx`); clicking a constituent container opens ContainerDetailSheet from new path.
6. **Settings page:**
   - [ ] Open Settings as admin user → no Scheduler menu entry (it lived between AI Configuration and Environments; verify it is gone).
   - [ ] No console errors about missing module / lazy-load failure.
7. **Build + tests:**
   - [ ] `pnpm --filter ui build` green.
   - [ ] `pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom` — all tests pass (87 from Plan 27-01 baseline; this plan adds 0 new tests).
8. **Git history sanity:**
   - [ ] `git log --follow livos/packages/ui/src/routes/docker/_components/environment-selector.tsx` shows pre-relocation history (verifies git-mv preserved blame).
  </how-to-verify>
  <resume-signal>Type "approved" to commit the plan + Phase 27 verifier runs next, or describe issues to fix before commit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→scheduler.upsertJob | Backup destination config + cleartext credentials cross to livinityd; secrets routed via `creds` field, encrypted at rest. Same trust model as v27.0 Settings > Scheduler. |
| user→scheduler.testBackupDestination | Same destination config + cleartext creds; the test path uploads a 1-byte test file to verify connectivity. Same as v27.0. |
| useDockerResource→SchedulerSection | useSelectedVolume() value is a volume name (string from in-memory zustand); pre-fills the volume picker. Same trust as Plan 26-02 T-26-09 (no escalation; same data path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | I (Information Disclosure) | AddBackupDialog displays SFTP private key + S3 secret key in DOM | accept | Same trust model as v27.0 Settings > Scheduler — secrets entered by user, password input type used; same as legacy. |
| T-27-09 | T (Tampering) | useSelectedVolume() pre-fill writes a volume name into the form | accept | Same data the volume picker would otherwise show; pre-fill is a UX shortcut, no escalation. Plan 26-02 T-26-09 precedent. |
| T-27-10 | R (Repudiation) | scheduler.upsertJob audit | accept | Pre-existing audit_log behavior unchanged; this plan only relocates the UI. |
| T-27-11 | I (Information Disclosure) | git history of moved files (was readable before, still readable) | accept | git-mv preserves blame; the only concern would be if moved files contained secrets — they don't (chrome components only). |
| T-27-12 | E (Elevation of Privilege) | new tRPC routes | n/a | Zero new tRPC routes — re-uses `scheduler.listJobs`, `scheduler.upsertJob`, `scheduler.deleteJob`, `scheduler.runNow`, `scheduler.testBackupDestination`, `docker.listVolumes`. Settings > Scheduler menu removal does NOT remove the tRPC routes themselves (other internal callers may exist; routes stay). |
| T-27-13 | T (Tampering) | server-control directory delete leaves dangling references | mitigate | Phase A pre-delete grep verification + Phase D post-delete grep verification + final build + final vitest run all gate the task. If any check fails, the executor MUST fix before completion. |
</threat_model>

<verification>
- `pnpm --filter ui build` succeeds after each task (Tasks 1, 2, 3 each gated by build green).
- `pnpm --filter ui exec vitest run src/routes/docker/ --environment jsdom` succeeds (87 tests from Plan 27-01).
- Phase 24 placeholder string removed: `grep -F "Coming in Phase 27 — Scheduler" livos/packages/ui/src/routes/docker/sections/schedules.tsx` returns empty.
- Phase 26-02 volume pre-fill seam works (manual smoke test #3 passes).
- All 4 post-delete grep checks return zero matches.
- `routes/server-control/` directory absent from disk.
- `routes/settings/_components/scheduler-section.tsx` absent from disk.
- `app-contents/server-control-content.tsx` absent from disk.
- DOC-03 fully closed.
</verification>

<success_criteria>
- DOC-12 closed (per ROADMAP success criteria 3 + 4).
- DOC-03 closed completely (legacy server-control entirely removed from disk + zero references).
- 7 cross-imported components live in their final homes inside `routes/docker/_components/` or `routes/docker/resources/`.
- Settings > Scheduler menu entry removed; admin users no longer see a duplicate scheduler entry in Settings.
- Volume backup pre-fill from `useSelectedVolume()` confirms the Phase 26-02 cross-section navigation seam contract.
- Build green; all UI vitest tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/27-stacks-schedules-routes/27-02-SUMMARY.md` documenting:
- Final file list — created (4 schedules + 0 new in cleanup), modified (3 consumer import path updates + settings-content.tsx + window-content.tsx), deleted (1 directory + 2 standalone files), git-renamed (7 files).
- Decisions made (especially: `_components/` vs `resources/` split for the 7 relocated files; volume-prefill consume-and-clear semantics in SchedulerSection's mount effect; Settings > Scheduler menu removal vs keeping it as a redirect).
- Deviations from plan (Rule 1/2/3 auto-fixes if any).
- Threat-model status — T-27-13 (dangling references) mitigated via 4-grep gate; rest accept/n.a.
- Phase 27 closure: DOC-11 (Plan 27-01) + DOC-12 (this plan) + DOC-03 (this plan) all closed. Ready for Phase 27 verifier.
- Confirmation that the legacy `routes/server-control/` and `routes/settings/_components/scheduler-section.tsx` files are gone.
</output>
