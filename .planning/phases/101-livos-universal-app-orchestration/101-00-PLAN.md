---
phase: 101
plan: 00
title: Wave 0 — Test Stub Scaffolding + Dependency Install
type: execute
wave: 0
depends_on: []
files_modified:
  - livos/packages/livinityd/package.json
  - livos/packages/livinityd/source/modules/chrome-cdp/bootstrap.test.ts
  - livos/packages/livinityd/source/modules/chrome-cdp/client.test.ts
  - livos/packages/livinityd/source/modules/streaming/port-allocator.test.ts
  - livos/packages/livinityd/source/modules/apps/native-app-spawner.test.ts
  - livos/packages/livinityd/source/modules/apps/native-app-binder.test.ts
  - livos/packages/livinityd/source/modules/ai/agent-prompt-builder.test.ts
  - livos/packages/ui/src/modules/dock/native-app-form.test.tsx
  - livos/packages/ui/src/modules/dock/native-app-icon.test.tsx
  - livos/packages/ui/src/modules/window/teach-popover.test.tsx
  - livos/packages/ui/src/modules/window/app-contents/webapp-teach-popup-host.test.tsx
  - .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md
autonomous: true
requirements: [D-101-CHROME-CDP, D-101-PORT-ALLOC, D-101-NATIVE-APPS, D-101-LUSE-CONTEXT, D-101-TEACH-V3]
estimated_tasks: 3

must_haves:
  truths:
    - "chrome-remote-interface@^0.34.0 + @types/chrome-remote-interface@^0.33.0 are installed in livinityd workspace BEFORE Wave 1 runs"
    - "All 10 stub test files exist as compilable vitest skeletons so Wave 1+ TDD tasks can begin in RED with the test file already on disk (Nyquist contract)"
    - "VALIDATION.md frontmatter `wave_0_complete` flips from `false` to `true` upon completion of this plan"
    - "Stub files are minimum-passing (use `it.skip(...)` placeholders) so `pnpm -r test:run` exits 0 immediately after Wave 0 lands"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/chrome-cdp/bootstrap.test.ts"
      provides: "Stub for Plan 101-01 Task 3 RED phase"
    - path: "livos/packages/livinityd/source/modules/chrome-cdp/client.test.ts"
      provides: "Stub for Plan 101-01 Task 2 RED phase"
    - path: "livos/packages/livinityd/source/modules/streaming/port-allocator.test.ts"
      provides: "Stub for Plan 101-02 Task 1 RED phase"
    - path: "livos/packages/livinityd/source/modules/apps/native-app-spawner.test.ts"
      provides: "Stub for Plan 101-03 Task 2 RED phase"
    - path: "livos/packages/livinityd/source/modules/apps/native-app-binder.test.ts"
      provides: "Stub for Plan 101-05 RED phase"
    - path: "livos/packages/livinityd/source/modules/ai/agent-prompt-builder.test.ts"
      provides: "Stub for Plan 101-06 Task 2 RED phase"
    - path: "livos/packages/ui/src/modules/dock/native-app-form.test.tsx"
      provides: "Stub for Plan 101-07 RED phase"
    - path: "livos/packages/ui/src/modules/dock/native-app-icon.test.tsx"
      provides: "Stub for Plan 101-07 RED phase"
    - path: "livos/packages/ui/src/modules/window/teach-popover.test.tsx"
      provides: "Stub for Plan 101-08 Task 2 RED phase"
    - path: "livos/packages/ui/src/modules/window/app-contents/webapp-teach-popup-host.test.tsx"
      provides: "Stub for Plan 101-08 Task 3 behavior tests (rapid-click queueing, commit-clears-pending, mount-with-event)"
    - path: "livos/packages/livinityd/package.json"
      contains: '"chrome-remote-interface":'
  key_links:
    - from: ".planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md"
      to: "Wave 0 stub list"
      via: "wave_0_complete: true frontmatter flag flipped after this plan ships"
      pattern: "wave_0_complete: true"
---

<objective>
Pre-condition Wave 1 onward: install the runtime + types deps for chrome-remote-interface and create the 10 stub test files that the rest of Phase 101's TDD tasks open with in RED phase. Without this Wave-0 plan, every TDD task in 101-01..101-08 has a literal "open test file that does not yet exist" prerequisite — which the existing plans assume away. This plan makes the Nyquist contract executable: the test scaffolding is on disk before Wave 1 begins.

Purpose: VALIDATION.md ships with `wave_0_complete: false` because 10 entries in the "Wave 0 Requirements" checklist are unchecked. This plan checks them. After this plan ships, Wave 1 TDD tasks can RED-phase against real files instead of asking "do I create the file or extend?".

Output: 10 stub `*.test.ts(x)` files + chrome-remote-interface installed + VALIDATION.md flipped to `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md
@.planning/phases/101-livos-universal-app-orchestration/101-PATTERNS.md
@.planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md

<interfaces>
<!-- vitest 2.1.9 minimum-passing skeleton (TS): -->
```typescript
import {describe, it, expect} from 'vitest'

describe('TODO — filled by Plan 101-NN Task M', () => {
  it.skip('placeholder', () => {
    expect(true).toBe(true)
  })
})
```

<!-- vitest 2.1.9 minimum-passing skeleton (TSX, for ui package): -->
```tsx
import {describe, it, expect} from 'vitest'

describe('TODO — filled by Plan 101-NN Task M', () => {
  it.skip('placeholder', () => {
    expect(true).toBe(true)
  })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chrome-remote-interface + @types in livinityd workspace</name>
  <read_first>
    - livos/packages/livinityd/package.json (verify current deps; confirm no existing CDP dep)
    - .planning/phases/101-livos-universal-app-orchestration/101-RESEARCH.md §Standard Stack lines 105-152 (version specs)
  </read_first>
  <action>
    1. From `livos/packages/livinityd/`:
       ```
       pnpm add chrome-remote-interface@^0.34.0
       pnpm add -D @types/chrome-remote-interface@^0.33.0
       ```
       (Use `pnpm` because livos workspace is pnpm; `liv/` is npm but this is in `livos/`.)
    2. Verify both keys appear in `livos/packages/livinityd/package.json` under `dependencies` and `devDependencies` respectively.
    3. Run `pnpm --filter @livos/livinityd install` (or root `pnpm install`) — must exit 0.
    4. Do NOT touch `liv/packages/core/src/sdk-agent-runner.ts` (sacred file).
  </action>
  <acceptance_criteria>
    - `grep -q "chrome-remote-interface" livos/packages/livinityd/package.json` returns 0
    - `grep -q "@types/chrome-remote-interface" livos/packages/livinityd/package.json` returns 0
    - `pnpm --filter @livos/livinityd install` exits 0
    - Sacred SHA unchanged: `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @livos/livinityd install && node -e "require.resolve('chrome-remote-interface', {paths:['./livos/packages/livinityd']})"</automated>
  </verify>
  <done>Both packages installed; require.resolve succeeds against livinityd workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 10 stub test files (vitest min-passing skeletons)</name>
  <read_first>
    - .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md §"Wave 0 Requirements" list (10 stubs)
    - livos/packages/livinityd/vitest.config.ts (verify test glob covers `source/modules/**/*.test.ts`)
    - livos/packages/ui/vitest.config.ts (verify test glob covers `src/**/*.test.tsx`)
  </read_first>
  <action>
    1. Create the following 10 stub files. Each is a minimum-passing vitest skeleton with one `it.skip(...)` placeholder so `pnpm -r test:run` exits 0 after Wave 0:
       - `livos/packages/livinityd/source/modules/chrome-cdp/bootstrap.test.ts`
       - `livos/packages/livinityd/source/modules/chrome-cdp/client.test.ts`
       - `livos/packages/livinityd/source/modules/streaming/port-allocator.test.ts`
       - `livos/packages/livinityd/source/modules/apps/native-app-spawner.test.ts`
       - `livos/packages/livinityd/source/modules/apps/native-app-binder.test.ts`
       - `livos/packages/livinityd/source/modules/ai/agent-prompt-builder.test.ts`
       - `livos/packages/ui/src/modules/dock/native-app-form.test.tsx`
       - `livos/packages/ui/src/modules/dock/native-app-icon.test.tsx`
       - `livos/packages/ui/src/modules/window/teach-popover.test.tsx`
       - `livos/packages/ui/src/modules/window/app-contents/webapp-teach-popup-host.test.tsx`

    2. For `.test.ts` files (livinityd), use this exact body — but name the describe block after the target Plan/Task it stubs out (replace `<Plan-NN Task M>`):
       ```typescript
       import {describe, it, expect} from 'vitest'

       describe('TODO — filled by Plan 101-NN Task M', () => {
         it.skip('placeholder — Wave 0 stub; real cases added by owning plan', () => {
           expect(true).toBe(true)
         })
       })
       ```
    3. For `.test.tsx` files (ui), use the same body with `.tsx` extension (no JSX in stub itself — just the skip placeholder).
    4. Mkdir any missing parent directories (e.g. `livos/packages/livinityd/source/modules/chrome-cdp/`, `livos/packages/livinityd/source/modules/ai/`, `livos/packages/ui/src/modules/dock/`) — these may not exist yet.
    5. Verify each file via `test -f` AND run `pnpm -r test:run` to confirm vitest discovers all stubs as skipped (no fail).
    6. Do NOT touch `liv/packages/core/src/sdk-agent-runner.ts` (sacred file).
  </action>
  <acceptance_criteria>
    - `test -f livos/packages/livinityd/source/modules/chrome-cdp/bootstrap.test.ts` exits 0
    - `test -f livos/packages/livinityd/source/modules/chrome-cdp/client.test.ts` exits 0
    - `test -f livos/packages/livinityd/source/modules/streaming/port-allocator.test.ts` exits 0
    - `test -f livos/packages/livinityd/source/modules/apps/native-app-spawner.test.ts` exits 0
    - `test -f livos/packages/livinityd/source/modules/apps/native-app-binder.test.ts` exits 0
    - `test -f livos/packages/livinityd/source/modules/ai/agent-prompt-builder.test.ts` exits 0
    - `test -f livos/packages/ui/src/modules/dock/native-app-form.test.tsx` exits 0
    - `test -f livos/packages/ui/src/modules/dock/native-app-icon.test.tsx` exits 0
    - `test -f livos/packages/ui/src/modules/window/teach-popover.test.tsx` exits 0
    - `test -f livos/packages/ui/src/modules/window/app-contents/webapp-teach-popup-host.test.tsx` exits 0
    - `pnpm -r test:run` exits 0 (all skipped tests pass)
    - Sacred SHA unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r test:run -- --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 stub test files exist; full test suite passes with stubs as skipped placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Flip VALIDATION.md wave_0_complete to true + commit</name>
  <read_first>
    - .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md (full file — focus on frontmatter line 6 `wave_0_complete: false` and the §"Wave 0 Requirements" checklist)
    - .planning/STATE.md "Sacred Constraints (v32-wide)" section
    - .husky/pre-commit
  </read_first>
  <action>
    1. Edit `.planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md`:
       - In frontmatter: change `wave_0_complete: false` → `wave_0_complete: true`.
       - In §"Wave 0 Requirements" checklist: tick all 10 `- [ ]` to `- [x]` (the 9 stub files + the chrome-remote-interface install line + this plan's webapp-teach-popup-host.test.tsx stub which is also added here).
       - Add a new checked entry for the new `webapp-teach-popup-host.test.tsx` stub (mentioned in BLOCKER #4 fix — see Plan 101-08 Task 3).
    2. Verify Sacred SHA: `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST output `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If not, ABORT.
    3. `pnpm -r test:run` MUST exit 0.
    4. `git add livos/packages/livinityd/package.json livos/packages/livinityd/source/modules/chrome-cdp/ livos/packages/livinityd/source/modules/streaming/port-allocator.test.ts livos/packages/livinityd/source/modules/apps/ livos/packages/livinityd/source/modules/ai/agent-prompt-builder.test.ts livos/packages/ui/src/modules/dock/ livos/packages/ui/src/modules/window/teach-popover.test.tsx livos/packages/ui/src/modules/window/app-contents/webapp-teach-popup-host.test.tsx .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md`. Also stage `pnpm-lock.yaml` (workspace lockfile at repo root) if it was updated.
    5. Commit `chore(101-00): wave 0 stub scaffolding + chrome-remote-interface install`. NEVER use `--no-verify`.
  </action>
  <acceptance_criteria>
    - `grep -q "wave_0_complete: true" .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md`
    - `grep -c "^- \[x\]" .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md` >= 10 (10+ checked Wave 0 items)
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - `git log -1 --format=%s` contains `101-00`
    - Pre-commit hook passed (no `--no-verify`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "wave_0_complete: true" .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md && git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>VALIDATION.md flag flipped; commit landed; sacred SHA unchanged; Wave 1 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry → livinityd node_modules | New deps brought in (chrome-remote-interface, @types/chrome-remote-interface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-00 | Supply-chain Tampering | chrome-remote-interface@^0.34.0 from npm registry | accept | Pinned to ^0.34.0 (semver-major locked); pnpm-lock.yaml records integrity hash. Same risk profile as every other Phase 101 dep. |
</threat_model>

<verification>
1. `pnpm -r test:run -- --reporter=dot` — all stubs discovered as skipped; suite passes
2. `node -e "require.resolve('chrome-remote-interface', {paths:['./livos/packages/livinityd']})"` resolves
3. `grep "wave_0_complete:" .planning/phases/101-livos-universal-app-orchestration/101-VALIDATION.md` outputs `wave_0_complete: true`
4. Sacred SHA unchanged
</verification>

<success_criteria>
- 10 stub test files exist on disk and are discovered (as skipped) by vitest
- chrome-remote-interface@^0.34.0 + @types installed in livinityd workspace
- VALIDATION.md `wave_0_complete: true` flipped + all 10 Wave 0 list items checked
- Sacred SHA unchanged
- One commit landed (`chore(101-00): wave 0 stub scaffolding ...`)
</success_criteria>

<output>
After completion, create `.planning/phases/101-livos-universal-app-orchestration/101-00-SUMMARY.md` covering:
- 10 stub file paths created
- chrome-remote-interface + @types versions installed
- pnpm-lock.yaml diff line count
- VALIDATION.md flag flip confirmation
- Sacred SHA verification result
</output>
