---
phase: 101
plan: 10
title: Mini PC Deploy + 20-Row UAT Walk
type: execute
wave: 4
depends_on: [101-04, 101-05, 101-06, 101-07, 101-08, 101-09]
files_modified:
  - .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md
autonomous: false
requirements: [D-101-SACRED, D-101-NO-SERVER4]
estimated_tasks: 6

must_haves:
  truths:
    - "GitHub master HEAD contains all 9 prior plan commits AND the sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` is intact on disk"
    - "Mini PC `bruce@10.69.31.68` has been re-deployed via `bash /opt/livos/update.sh` and all 4 systemd services are active"
    - "20-row UAT walk (CONTEXT.md `Success Criteria`) completed and recorded in UAT-CHECKLIST.md with PASS/FAIL per row"
    - "Sacred SHA on Mini PC matches the constraint after deploy"
    - "ROADMAP.md Phase 101 entry is flipped to `[x]` (Shipped) ONLY IF all 20 rows PASS; otherwise documented as PARTIAL-PASS with deferred rows"
    - "STATE.md current position updated to reflect Phase 101 close"
  artifacts:
    - path: ".planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md"
      provides: "20-row checklist with PASS/FAIL/Notes per row"
    - path: ".planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md"
      provides: "Phase 101 close-out summary"
  key_links:
    - from: ".planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md"
      to: ".planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md §Success Criteria (20-row table)"
      via: "each ## Row N section maps 1:1 to a Success Criteria row; pass criteria copied verbatim"
      pattern: "^## Row [0-9]+"
    - from: ".planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md"
      to: ".planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md"
      via: "PHASE-SUMMARY 20-row UAT result snapshot section embeds PASS/FAIL aggregated from the checklist"
      pattern: "20-row UAT result"
    - from: ".planning/ROADMAP.md Phase 101 entry"
      to: ".planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md outcome"
      via: "Status line (Shipped vs PARTIAL-PASS) is set from the UAT walk result (Task 5)"
      pattern: "Status:.*(Shipped|PARTIAL-PASS)"
    - from: ".planning/STATE.md Phase 101 section"
      to: ".planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md"
      via: "STATE entry records sacred-SHA verification + UAT summary line"
      pattern: "Phase 101"
---

<objective>
User-walked Wave 4. Push all Wave 1+2+3 commits to GitHub, deploy to Mini PC, walk the 20-row UAT (CONTEXT.md `Success Criteria`), record results, and update STATE + ROADMAP based on the outcome (full-pass → ship, partial-pass → next phase plant).

This plan is `autonomous: false` because it requires:
- The user to run `bash /opt/livos/update.sh` on the Mini PC (the executor agent is not authorized to ssh on the user's behalf for deploys per "USER WALK" pattern in STATE.md)
- The user to drive the desktop UAT (clicking dock icons, watching streams, talking to chat, recording teach steps)
- Sacred SHA re-verification on the deployed Mini PC

Server4/Server5 are off-limits per D-101-NO-SERVER4 and the hard rule in MEMORY.md.

Output: A finalized PHASE-SUMMARY.md + UAT-CHECKLIST.md + updated ROADMAP/STATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md
@.planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-deploy push to GitHub + sacred-SHA pre-check</name>
  <what-built>
    All 9 Wave 1+2+3 commits (101-01 .. 101-09) should be on master. Pre-commit hooks already enforced sacred SHA at each commit; we double-check on disk.
  </what-built>
  <how-to-verify>
    Run on your laptop (Windows PowerShell or bash):
    1. `git log --oneline master | head -20` — confirm 101-01..101-09 commits are present in order.
    2. `git hash-object liv/packages/core/src/sdk-agent-runner.ts` — MUST output `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    3. `git push origin master` — push all to GitHub.
    4. Confirm GitHub Actions deploy.yml passes (if applicable).
  </how-to-verify>
  <resume-signal>Type "pushed and sacred-SHA verified" or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Mini PC deploy via update.sh</name>
  <what-built>
    Mini PC update.sh pulls from GitHub `utopusc/livinity-io` to /tmp, rsyncs source to `/opt/livos/` and `/opt/liv/`, runs `pnpm install` + `npm install`, builds packages, then `systemctl restart livos liv-core liv-worker liv-memory`.
  </what-built>
  <how-to-verify>
    On the Mini PC (via SSH or terminal):
    1. `bash /opt/livos/update.sh` — wait for it to finish (deploy log shows "deployed SHA: <hash>").
    2. `systemctl status livos liv-core liv-worker liv-memory` — all 4 must report `active (running)`.
    3. `cat /opt/livos/.deployed-sha` — must match what you pushed in Task 1.
    4. `ssh bruce@10.69.31.68 'git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts'` — MUST output `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. (Per VALIDATION.md row 101-10-01.)
    5. Smoke test: `curl -s http://localhost:9222/json/version` on Mini PC — MUST return Chrome version JSON (UAT row 1, CDP up).
    6. `pgrep -af 'google-chrome.*--remote-debugging-port=9222'` — exactly one PID (UAT row 2).
    7. Per CLAUDE.md memory `update.sh pnpm-store quirk`: `ls /opt/livos/node_modules/.pnpm/@liv+core*` — if multiple dirs, manually copy `/opt/liv/packages/core/dist` into the correct one and `systemctl restart livos`.
  </how-to-verify>
  <resume-signal>Type "deployed, sacred SHA verified on Mini PC, CDP up" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Generate UAT-CHECKLIST.md scaffold</name>
  <read_first>
    - .planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md (template — copy section structure)
    - .planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md `Success Criteria` (20-row table)
  </read_first>
  <action>
    1. Create `.planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md` with the 20 rows from CONTEXT.md Success Criteria. Each row becomes a section with:
       ```markdown
       ## Row N — <Test name>
       **Pass criteria:** <from CONTEXT>
       **Steps:**
       1. ...
       **Result:** ⬜ pending | ✅ PASS | ❌ FAIL
       **Notes:** _(filled during walk)_
       ```
    2. Pre-fill each row's Steps from the CONTEXT pass-criteria. For rows that require terminal commands (1, 2, 5, 19), include exact commands.
    3. Add a header section with date, deployed SHA placeholder, and Mini PC IP.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md`
    - `grep -c "## Row" .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md` outputs 20
    - `grep -q "Sacred SHA" .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md` (row 19 present)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^## Row' .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md</automated>
  </verify>
  <done>20-row checklist scaffolded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Walk the 20 UAT rows on Mini PC</name>
  <what-built>
    Phase 101 features (single Chrome + multi-port WebApps, native apps, Luse auto-context, Teach v3, chat animations, Hermes phrase relay) are now live on Mini PC.
  </what-built>
  <how-to-verify>
    Open https://bruce.livinity.io (or the local LivOS URL) in a browser. Walk each of the 20 rows from `.planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md` in order. For each row, record ✅ PASS or ❌ FAIL with notes. Concentrate especially on:

    1. **Row 1, 2:** CDP up + single Chrome process (already verified in Task 2, just re-record).
    2. **Row 3, 4:** Click WebApp `livinity.io` then `google.com` — distinct stream windows, distinct ports (15900, 15901), SAME Google login (top-right shows `lucyfeilu123@gmail.com` in both).
    3. **Row 5:** `DISPLAY=:1 wmctrl -l` shows 2 distinct windows.
    4. **Row 6, 7:** Add Antigravity IDE via dock "Add Native App" form, click icon, stream window opens 1280x720.
    5. **Row 8:** In a WebApp's chat, ask "What window am I in?" — agent should answer with the title/URL without explicit list_windows call (D-101-LUSE-CONTEXT).
    6. **Row 9-12:** Teach v3 — click in stream, popover appears at click point, instruction input + Save records step, name "Search Test", replay works.
    7. **Row 13:** v2 (existing pre-101) skills still replay (D-101-BACKWARDS-COMPAT).
    8. **Row 14:** Chat thinking dots animation visible while waiting for first token.
    9. **Row 15:** Chat input idle pulse visible when unfocused + empty.
    10. **Row 16:** Per-tool status line shows real Hermes phrase ("Listing windows...") not just `currentTool` (Pillar F closed).
    11. **Row 17, 18:** Close WebApp window → stream port released + CDP target destroyed. Same for native.
    12. **Row 19:** `ssh bruce@10.69.31.68 'git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts'` returns `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    13. **Row 20:** Open 5 WebApps + 2 native apps concurrently — all 7 streams active, no overlap, no crash, RAM ~1.5GB.
  </how-to-verify>
  <resume-signal>Type "all 20 PASS" or "PARTIAL-PASS — rows N,M failed: <reason>" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md + STATE.md based on UAT outcome</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 101 entry lines 475-509)
    - .planning/STATE.md
    - .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md (the just-walked file)
  </read_first>
  <action>
    Branch on the user's reply to Task 4:

    **If "all 20 PASS":**
    1. In `.planning/ROADMAP.md` Phase 101 entry, flip the status line to `**Status:** ✅ Shipped 2026-05-NN` and check off the sub-goal A/B/C boxes.
    2. In `.planning/STATE.md`:
       - Update `## Current Position` to indicate Phase 101 closed.
       - Add a `## Phase 101 — SHIPPED (2026-05-NN)` section with the deployed SHA + UAT summary (20/20 PASS).
       - Note Sacred SHA verified on Mini PC.

    **If "PARTIAL-PASS — rows N,M failed: <reason>":**
    1. In `.planning/ROADMAP.md` Phase 101 entry, add `**Status:** PARTIAL-PASS 2026-05-NN — rows N,M deferred to Plan 101-11 (or Phase 102 if architectural).`
    2. In `.planning/STATE.md`, document the residual gaps with row numbers, reasons, and proposed next step (`/gsd-discuss-phase 101-gap` or `/gsd-plan-phase 101 --gaps`).

    In BOTH cases:
    3. Verify sacred SHA on disk: `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST output `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 101 entry has been updated with a Status line reflecting the UAT outcome (PASS/PARTIAL-PASS)
    - `.planning/STATE.md` has a new section for Phase 101
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 101" .planning/STATE.md && grep -q "Phase 101" .planning/ROADMAP.md && git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>ROADMAP + STATE updated.</done>
</task>

<task type="auto">
  <name>Task 6: PHASE-SUMMARY.md + final commit</name>
  <read_first>
    - .planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md (template — copy section structure)
    - All 9 prior 101-NN-SUMMARY.md files in the phase directory
    - The just-walked UAT-CHECKLIST.md
  </read_first>
  <action>
    1. Create `.planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md` with:
       - Header: phase number, dates, deployed SHA, outcome (PASS / PARTIAL-PASS)
       - Per-plan summary table (101-01 through 101-10 — what each shipped)
       - 6 Pillar outcomes (A-F: shipped/partial/deferred)
       - 20-row UAT result snapshot
       - Sacred SHA verification result
       - Deferred items list (any partial-pass row that goes to a follow-on phase)
    2. `git add .planning/phases/101-livos-universal-app-orchestration/UAT-CHECKLIST.md .planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md .planning/ROADMAP.md .planning/STATE.md`
    3. Commit `docs(101-10): UAT walk results + Phase 101 close-out`. No `--no-verify`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md`
    - `git log -1 --format=%s` contains `101-10`
    - `grep -q "Sacred SHA" .planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md`
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - Pre-commit hook passed
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/101-livos-universal-app-orchestration/PHASE-SUMMARY.md && git log -1 --format=%s | grep -q '101-10'</automated>
  </verify>
  <done>Phase 101 closed (or PARTIAL-PASS documented). Commit landed.</done>
</task>

</tasks>

<verification>
1. Mini PC `/opt/livos/.deployed-sha` matches GitHub master HEAD
2. `ssh bruce@10.69.31.68 'git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts'` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
3. All 4 systemd services active
4. 20-row UAT-CHECKLIST.md filled (PASS or PARTIAL-PASS recorded)
5. ROADMAP + STATE updated
6. PHASE-SUMMARY.md committed
</verification>

<success_criteria>
- Mini PC deployed and live with Phase 101 features
- Sacred SHA preserved on Mini PC
- 20-row UAT walked
- ROADMAP + STATE reflect outcome (Shipped / PARTIAL-PASS)
- PHASE-SUMMARY.md ships
- Server4 + Server5 untouched (D-101-NO-SERVER4 enforced)
</success_criteria>

<output>
After completion of this plan, Phase 101 either closes (full pass) or surfaces gap-closure recommendations (partial pass). Either way, STATE.md is the canonical next-step reference for the next session.
</output>
