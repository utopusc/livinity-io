---
phase: 101
plan: 09
title: Chat Animations + Hermes Phrase Relay (Pillar E + F)
type: execute
wave: 3
depends_on: []
files_modified:
  - livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx
  - livos/packages/ui/src/modules/window/webapp-floating-action-bar.test.tsx
  - livos/packages/ui/src/index.css
  - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts
  - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.test.ts
  - livos/packages/livinityd/source/modules/ai/index.ts
autonomous: true
requirements: [D-101-CHAT-ANIMS, D-101-SACRED]
estimated_tasks: 5

must_haves:
  truths:
    - "When chat is streaming AND no assistant token has arrived yet, ChatInputBar (inside webapp-floating-action-bar.tsx) shows 3 staggered-pulse dots (0ms / 150ms / 300ms animation delays)"
    - "When chat input is unfocused AND empty AND not streaming, input border breathes via @keyframes idleBreath (4s cycle, opacity 0.3-0.8)"
    - "prefers-reduced-motion: reduce suppresses BOTH animations (a11y)"
    - "Hermes status_detail chunks emitted by RunStore in liv-core are RELAYED to the WS clients by the livinityd broker layer — closing the Pillar F backend gap from 100-10-10"
    - "Per PATTERNS.md risk note #1: webapp-chat-bottom-bar.tsx is DEPRECATED — animations land in `webapp-floating-action-bar.tsx` `ChatInputBar`, NOT in webapp-chat-bottom-bar.tsx"
  artifacts:
    - path: "livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx"
      provides: "Updated ChatInputBar with thinking-dots + idle-pulse"
    - path: "livos/packages/ui/src/index.css"
      provides: "@keyframes idleBreath + .chat-input-idle utility + prefers-reduced-motion override"
    - path: "livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts"
      provides: "Extended generator that re-yields status_detail chunks from liv-core SSE"
  key_links:
    - from: "liv-core RunStore"
      to: "livinity-broker agent-runner-factory.ts"
      via: "SSE chunk forwarding (already exists for other chunk types; add status_detail discriminant)"
      pattern: "status_detail"
    - from: "livinityd WS"
      to: "useAgentSocket"
      via: "ChatMessage union already supports status_detail per 100-10-10"
      pattern: "status_detail"
---

<objective>
Ship two parallel concerns in one plan because they touch the same chat UI surface and are file-disjoint from every other Wave-3 plan:

**Pillar E (UI):** Add the 3-dot thinking pulse and idle-breathe pulse to `ChatInputBar` inside `webapp-floating-action-bar.tsx` (NOT to `webapp-chat-bottom-bar.tsx` — that file is DEPRECATED per its own header / PATTERNS.md risk note #1). Honor `prefers-reduced-motion`.

**Pillar F (backend bridge):** Close the 100-10-10 gap where Hermes-style `status_detail` chunks emitted by RunStore in `liv-core` never reach the WebApp chat UI because the livinityd broker (`agent-runner-factory.ts`) does not re-yield them on the WS. Find the SSE chunk forwarding loop and add `status_detail` to the recognized chunk types so the existing UI status line ("Calling list_windows..." etc.) shows real phrase content instead of just the tool name.

Per orchestrator instruction 5: this plan may touch `livinity-broker/agent-runner-factory.ts` and `ai/index.ts`, but MUST NOT touch `liv/packages/core/src/sdk-agent-runner.ts` (sacred file). All RunStore writes happen inside liv-core untouched; we only re-yield existing chunks at the broker layer.

Purpose: Pillar E completes the chat-UX polish. Pillar F closes the open gap from 100-10 sub-goal C.

Output: A green test suite + visible animations + actual phrase strings showing in the per-tool status line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md
@.planning/phases/101-livos-universal-app-orchestration/101-RESEARCH.md
@.planning/phases/101-livos-universal-app-orchestration/101-PATTERNS.md
@livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx
@livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts
@livos/packages/livinityd/source/modules/ai/index.ts

<interfaces>
<!-- Current single-dot pattern (webapp-floating-action-bar.tsx:452-457) — REPLACE: -->
```typescript
{agent.isStreaming && (agent.agentStatus?.phrase || agent.agentStatus?.currentTool) ? (
  <div className='text-caption-xs text-text-tertiary flex items-center gap-1.5'>
    <span className='inline-block w-1 h-1 rounded-full bg-text-tertiary animate-pulse' aria-hidden='true' />
    <span>{agent.agentStatus.phrase ?? `Using ${agent.agentStatus.currentTool}…`}</span>
  </div>
) : null}
```

<!-- D-101-CHAT-ANIMS thinking dots (CONTEXT lines 122-129 verbatim): -->
```tsx
<span className='inline-flex gap-1'>
  <span className='w-1.5 h-1.5 rounded-full bg-text-tertiary animate-pulse [animation-delay:0ms]' />
  <span className='w-1.5 h-1.5 rounded-full bg-text-tertiary animate-pulse [animation-delay:150ms]' />
  <span className='w-1.5 h-1.5 rounded-full bg-text-tertiary animate-pulse [animation-delay:300ms]' />
</span>
```

<!-- Gating condition (CONTEXT line 122 + 132): -->
- Thinking dots: `isStreaming && messages.length === lastSentCount` (user sent, no response token yet)
- Idle pulse: input unfocused + empty + !isStreaming

<!-- Idle-breath CSS: -->
```css
@keyframes idleBreath {
  0%, 100% { opacity: 0.3; }
  50% { opacity: 0.8; }
}
.chat-input-idle {
  animation: idleBreath 4s ease-in-out infinite;
}
@media (prefers-reduced-motion: reduce) {
  .chat-input-idle, .animate-pulse {
    animation: none !important;
  }
}
```

<!-- Pillar F: agent-runner-factory.ts SSE forward loop — find the existing chunk parser and add status_detail discriminant. Existing pattern from agent-runs.ts (already has chunk dispatch for token/tool/result types). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thinking-dots + idle-pulse in webapp-floating-action-bar.tsx (RED → GREEN)</name>
  <read_first>
    - livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx (lines ~440-470 ChatInputBar + lines 452-457 single-dot pattern)
    - livos/packages/ui/src/modules/window/webapp-floating-action-bar.test.tsx (existing tests — must keep passing)
    - .planning/phases/101-livos-universal-app-orchestration/101-PATTERNS.md "webapp-floating-action-bar.tsx" section + risk note #1
  </read_first>
  <behavior>
    Tests:
    - `thinking-dots rendered when isStreaming && messages.length === lastSentCount`
    - `thinking-dots NOT rendered when assistant has produced at least one token (lastAssistantMessage truthy)`
    - `thinking-dots NOT rendered when !isStreaming`
    - `dots have animation-delay arbitrary values [animation-delay:0ms], [animation-delay:150ms], [animation-delay:300ms]`
    - `existing phrase/currentTool status line is preserved (renders when phrase present)`
    - `idle-pulse class applied to input border when unfocused && empty && !isStreaming`
    - `idle-pulse removed once user focuses or types`
  </behavior>
  <action>
    1. Locate the single-pulse dot block (lines 452-457). Replace it with the 3-dot pattern shown in `<interfaces>`. Gate the dots on `isStreaming && messages.length === lastSentCount` (you may need to surface `lastSentCount` from the agent store; if not already exposed, expose it via the existing chat store).
    2. Keep the phrase-or-currentTool status line that follows; it should render when `phrase` exists. The new flow:
       - When dots gating is true AND no phrase yet → show only the 3 dots.
       - When phrase arrives → show the existing phrase line.
       - When `isStreaming` ends → both disappear.
    3. Add the idle-pulse class to the chat input's border:
       ```typescript
       <Input
         ...
         className={cn(
           'existing-classes',
           !isFocused && value.length === 0 && !isStreaming ? 'chat-input-idle' : '',
         )}
       />
       ```
    4. Extend the test file with the 7 cases.
    5. Iterate until GREEN. Existing tests must still pass.
  </action>
  <acceptance_criteria>
    - `grep -q "animation-delay:0ms" livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx`
    - `grep -q "animation-delay:150ms" livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx`
    - `grep -q "animation-delay:300ms" livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx`
    - `grep -q "chat-input-idle" livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx`
    - `pnpm --filter @livos/ui test:run window/webapp-floating-action-bar.test.tsx` exits 0
    - Sacred SHA unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @livos/ui test:run window/webapp-floating-action-bar.test.tsx -- --reporter=dot</automated>
  </verify>
  <done>3-dot pulse + idle-breathe shipped; existing phrase line preserved; 7 new tests + existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: idleBreath @keyframes + prefers-reduced-motion override in index.css</name>
  <read_first>
    - livos/packages/ui/src/index.css (find an existing `@keyframes` block — copy convention; do NOT replace existing rules)
    - .planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md D-101-CHAT-ANIMS idle pulse spec
  </read_first>
  <action>
    1. Append to `livos/packages/ui/src/index.css`:
       ```css
       @keyframes idleBreath {
         0%, 100% { opacity: 0.3; }
         50% { opacity: 0.8; }
       }
       .chat-input-idle {
         animation: idleBreath 4s ease-in-out infinite;
       }
       @media (prefers-reduced-motion: reduce) {
         .chat-input-idle {
           animation: none !important;
         }
         .animate-pulse {
           animation: none !important;
         }
       }
       ```
    2. Verify `pnpm --filter @livos/ui build` succeeds (Tailwind passes raw CSS through).
  </action>
  <acceptance_criteria>
    - `grep -q "idleBreath" livos/packages/ui/src/index.css`
    - `grep -q "prefers-reduced-motion: reduce" livos/packages/ui/src/index.css`
    - `pnpm --filter @livos/ui build` exits 0
    - Sacred SHA unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @livos/ui build 2>&1 | tail -10</automated>
  </verify>
  <done>CSS keyframes + a11y override in tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Relay status_detail chunks in agent-runner-factory.ts (Pillar F backend bridge)</name>
  <read_first>
    - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts (full file — the SSE chunk parse loop is the relay point)
    - livos/packages/livinityd/source/modules/ai/index.ts lines 717-780 (parallel chunk forwarder; verify both paths handle status_detail)
    - .planning/phases/101-livos-universal-app-orchestration/101-CONTEXT.md Pillar F section (page lines 47-51) + sub-goal C reference
    - .planning/phases/101-livos-universal-app-orchestration/101-PATTERNS.md agent-runner-factory.ts entry
  </read_first>
  <behavior>
    Tests:
    - `when liv-core SSE emits {type: 'status_detail', phase, phrase, elapsed}, agent-runner-factory.ts re-yields it as an AgentEvent with the same shape`
    - `existing chunk types (token, tool_call, result) are still relayed unchanged`
    - `unknown chunk types fall through unchanged (forward-compat)`
  </behavior>
  <action>
    1. In `agent-runner-factory.ts`, find the SSE parse loop (it iterates over events from the upstream `fetch(...)` to liv-core). Find the switch/dispatcher on chunk type. Add a branch:
       ```typescript
       case 'status_detail':
         yield {
           type: 'status_detail',
           phase: chunk.phase,
           phrase: chunk.phrase,
           elapsed: chunk.elapsed,
         } as AgentEvent
         break
       ```
       If the union type `AgentEvent` does not have a `status_detail` variant yet, add one. (Reference: the UI's `useAgentSocket` already accepts `status_detail` per 100-10-10; backend just needs to emit it.)
    2. In `livinityd/source/modules/ai/index.ts:717-780` chatStream — verify it also re-broadcasts status_detail to the WS. If it's a generic chunk-forward (`for await chunk of upstream { ws.send(JSON.stringify(chunk)) }`), no change needed; if it filters chunk types, add status_detail to the allowed list.
    3. Add 3 tests in `agent-runner-factory.test.ts` covering the relay.
    4. Iterate until GREEN.
  </action>
  <acceptance_criteria>
    - `grep -q "status_detail" livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts`
    - `grep -q "status_detail" livos/packages/livinityd/source/modules/ai/index.ts` (either pre-existing or newly added)
    - `pnpm --filter @livos/livinityd test:run livinity-broker/ -- --reporter=dot` exits 0
    - `pnpm --filter @livos/livinityd build` exits 0
    - Sacred SHA unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @livos/livinityd test:run livinity-broker/ -- --reporter=dot</automated>
  </verify>
  <done>status_detail chunks now reach the WebApp chat UI via the broker; Pillar F gap closed.</done>
</task>

<task type="auto">
  <name>Task 4: Verify webapp-chat-bottom-bar.tsx UNCHANGED (per PATTERNS.md risk note #1)</name>
  <read_first>
    - livos/packages/ui/src/modules/window/webapp-chat-bottom-bar.tsx (file header — confirm "DEPRECATED 2026-05-10 (P100-09-08)")
    - .planning/phases/101-livos-universal-app-orchestration/101-PATTERNS.md "webapp-chat-bottom-bar.tsx" entry (risk note)
  </read_first>
  <action>
    1. Open `webapp-chat-bottom-bar.tsx` and confirm it still carries the DEPRECATED header. DO NOT add the idle pulse or thinking dots here (CONTEXT D-101-CHAT-ANIMS originally targeted this file but PATTERNS.md / orchestrator correction 2a says apply to `webapp-floating-action-bar.tsx` `ChatInputBar` instead).
    2. Add a one-line code comment near the top reaffirming "Phase 101-09: animations land in webapp-floating-action-bar.tsx, NOT here. See 101-PATTERNS.md risk note #1." This is a future-proofing note for the next reader.
  </action>
  <acceptance_criteria>
    - `grep -q "DEPRECATED" livos/packages/ui/src/modules/window/webapp-chat-bottom-bar.tsx`
    - `grep -q "Phase 101-09\|101-09" livos/packages/ui/src/modules/window/webapp-chat-bottom-bar.tsx`
    - No production behavior change in this file (`git diff --stat` shows comment-only changes)
    - Sacred SHA unchanged.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat livos/packages/ui/src/modules/window/webapp-chat-bottom-bar.tsx</automated>
  </verify>
  <done>Deprecation reaffirmed with a Phase 101-09 forward-pointer comment.</done>
</task>

<task type="auto">
  <name>Task 5: Sacred SHA verify + commit</name>
  <read_first>
    - .planning/STATE.md "Sacred Constraints" section
  </read_first>
  <action>
    1. `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST output `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    2. Verify `liv/packages/core/src/sdk-agent-runner.ts` is NOT in `git diff --name-only HEAD` (sacred file untouched).
    3. `pnpm --filter @livos/ui test:run` AND `pnpm --filter @livos/livinityd test:run` must exit 0.
    4. `git add` all modified files.
    5. Commit `feat(101-09): chat thinking-dots + idle-pulse + Hermes status_detail relay`. No `--no-verify`.
  </action>
  <acceptance_criteria>
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - `git diff --name-only HEAD~1 HEAD | grep -q sdk-agent-runner.ts` returns NON-zero (file NOT in the commit)
    - `git log -1 --format=%s` contains `101-09`
    - Pre-commit hook passed
  </acceptance_criteria>
  <verify>
    <automated>git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>Commit landed; sacred SHA unchanged.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @livos/ui test:run window/webapp-floating-action-bar.test.tsx -- --reporter=dot` — green
2. `pnpm --filter @livos/livinityd test:run livinity-broker/ -- --reporter=dot` — green
3. `pnpm --filter @livos/ui build` + `pnpm --filter @livos/livinityd build` — clean
4. UAT rows 14, 15, 16 (101-10)
5. Sacred SHA preserved
</verification>

<success_criteria>
- Thinking dots: 3 staggered-pulse spans with 0/150/300ms delays
- Idle pulse: 4s ease-in-out @keyframes idleBreath; honors prefers-reduced-motion
- Animations land in ChatInputBar (webapp-floating-action-bar.tsx), NOT webapp-chat-bottom-bar.tsx (PATTERNS.md risk #1 applied)
- status_detail chunks relayed end-to-end from liv-core RunStore through livinityd broker to WS clients
- Sacred SDK runner file untouched
- 10+ new tests green
- Sacred SHA unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/101-livos-universal-app-orchestration/101-09-SUMMARY.md` covering:
- Before/after of the single-dot → 3-dot replacement (line range)
- CSS keyframe additions
- status_detail relay diff in agent-runner-factory.ts
- Test counts per file
- Confirmation that liv/packages/core/src/sdk-agent-runner.ts hash matches f3538e1d811992b782a9bb057d1b7f0a0189f95f and file was NOT in the commit diff
</output>
