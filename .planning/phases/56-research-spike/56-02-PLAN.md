---
phase: 56-research-spike
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - .planning/phases/56-research-spike/SPIKE-FINDINGS.md
  - .planning/phases/56-research-spike/notes-q3-agent-mode.md
  - .planning/phases/56-research-spike/notes-q4-public-endpoint.md
  - .planning/phases/56-research-spike/notes-q5-key-rotation.md
  - .planning/phases/56-research-spike/notes-q6-rate-limit.md
autonomous: true
requirements: []
tags: [research, spike, auth, public-endpoint, caddy, cloudflare, rate-limit]

must_haves:
  truths:
    - "SPIKE-FINDINGS.md Q3 verdict picks header OR URL path for agent-mode opt-in with worked curl example"
    - "SPIKE-FINDINGS.md Q4 verdict picks Server5 Caddy OR Cloudflare Worker, paired with TLS strategy AND rate-limit primitive name"
    - "SPIKE-FINDINGS.md Q5 verdict states API key rotation policy (manual revoke+recreate vs auto-90-day) AND default-keyed-vs-opt-in axis"
    - "SPIKE-FINDINGS.md Q6 verdict states whether broker forwards Anthropic rate limits verbatim or imposes broker-side per-key bucket (or both)"
    - "Each verdict has >=2 alternatives evaluated, >=3 rationale reasons, and a risk/mitigation pair"
    - "Sacred file SHA unchanged at end of plan execution"
  artifacts:
    - path: ".planning/phases/56-research-spike/SPIKE-FINDINGS.md"
      provides: "Verdict blocks for Q3, Q4, Q5, Q6"
      contains: "## Q3"
    - path: ".planning/phases/56-research-spike/notes-q3-agent-mode.md"
      provides: "Client-compat matrix for X-Livinity-Mode header support"
    - path: ".planning/phases/56-research-spike/notes-q4-public-endpoint.md"
      provides: "Caddy vs CF Worker comparison + TLS + rate-limit primitive evidence"
    - path: ".planning/phases/56-research-spike/notes-q5-key-rotation.md"
      provides: "Stripe/OpenAI/Anthropic key rotation pattern citations"
    - path: ".planning/phases/56-research-spike/notes-q6-rate-limit.md"
      provides: "Anthropic + OpenAI rate-limit header specs"
  key_links:
    - from: "Q3 verdict"
      to: "livinity-broker/router.ts:36 (current URL-path identity) + livinity-broker/agent-runner-factory.ts:82 (X-LivOS-User-Id header precedent)"
      via: "explicit file:line cite"
      pattern: "router\\.ts:[0-9]+|agent-runner-factory\\.ts:[0-9]+"
    - from: "Q4 verdict"
      to: "platform/relay/Caddyfile (current Server5 Caddy posture)"
      via: "verdict cites the file path"
      pattern: "platform/relay/Caddyfile"
    - from: "Q6 verdict"
      to: "livinity-broker/router.ts:158-185 (existing 429+Retry-After forward)"
      via: "verdict references existing pattern"
      pattern: "router\\.ts:15[0-9]|router\\.ts:18[0-5]"
---

<objective>
Produce verdict blocks for Q3 (agent mode opt-in mechanism), Q4 (public endpoint architecture), Q5 (API key rotation policy), and Q6 (rate-limit policy) — the four questions covering the auth / public-perimeter cluster.

Purpose:
- Phase 57 (passthrough mode) needs Q3 to know how clients opt-in to agent mode.
- Phase 60 (public endpoint) needs Q4 to know whether to write a Caddy block or build a CF Worker.
- Phase 59 (Bearer auth) needs Q5 to know lifecycle expectations for `api_keys` table.
- Phase 60+61 need Q6 to know whether a broker-side token bucket exists.

Output:
- `SPIKE-FINDINGS.md` (Q3 + Q4 + Q5 + Q6 sections — verdict blocks)
- `notes-q3-agent-mode.md`, `notes-q4-public-endpoint.md`, `notes-q5-key-rotation.md`, `notes-q6-rate-limit.md` (intermediate research notes)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-research-spike/56-CONTEXT.md
@.planning/phases/56-research-spike/56-RESEARCH.md

# Existing-code references (read; do NOT edit)
@livos/packages/livinityd/source/modules/livinity-broker/router.ts
@livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts
@livos/packages/livinityd/source/modules/livinity-broker/auth.ts
@platform/relay/Caddyfile
</context>

<sacred_boundary>
**STRICT RULE for this plan:** `nexus/packages/core/src/sdk-agent-runner.ts` is the sacred file (SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`). Tasks in this plan should NOT need to read this file at all — Q3/Q4/Q5/Q6 are perimeter concerns, not internal-runner concerns. If you find yourself wanting to Read it, double-check whether your verdict actually requires it. NO Edit/Write under any circumstance.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Q3 + Q5 verdicts — Agent-mode opt-in mechanism + API key rotation policy</name>
  <files>.planning/phases/56-research-spike/notes-q3-agent-mode.md, .planning/phases/56-research-spike/notes-q5-key-rotation.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q3 + Q5 source inventory + decision frameworks)
    - livos/packages/livinityd/source/modules/livinity-broker/router.ts (line 36 — URL-path identity precedent)
    - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts (line 82 — X-LivOS-User-Id header precedent)
    - .planning/REQUIREMENTS.md (FR-BROKER-A2-01 — header OR URL path; FR-BROKER-B1-01..05 — api_keys table schema)
  </read_first>
  <action>
**Q3 (agent-mode opt-in):**

1. Read all `read_first` files.
2. WebFetch https://docs.anthropic.com/en/api/versioning and extract the precedent for `X-*` request headers (e.g., `anthropic-version`).
3. WebFetch https://platform.openai.com/docs/api-reference/authentication and extract the OpenAI auth pattern — confirm OpenAI uses Bearer + base URL with no opt-in headers (precedent: don't fragment URL).
4. WebSearch each of: "Bolt.diy custom headers", "Open WebUI custom request headers", "Continue.dev custom headers", "Cline custom HTTP headers" — for each client, find at minimum one piece of evidence in their docs/GitHub indicating whether custom headers can be configured. Capture URL + snippet in `notes-q3-agent-mode.md`.
5. WebFetch each client's official docs page (or relevant GitHub README) discovered above.
6. Build a client-compat matrix in `notes-q3-agent-mode.md`:
   ```
   | Client          | Custom headers? | base_url switching? | Evidence (URL + snippet) |
   |-----------------|-----------------|---------------------|--------------------------|
   | Bolt.diy        | yes/no          | yes/no              | ...                      |
   | Open WebUI      | yes/no          | yes/no              | ...                      |
   | Continue.dev    | yes/no          | yes/no              | ...                      |
   | Cline           | yes/no          | yes/no              | ...                      |
   ```
7. Choose verdict among RESEARCH.md candidates A (Header `X-Livinity-Mode: agent`), B (URL path `/u/<id>/agent/v1/messages`), C (Both supported).
8. Write the worked example: a `curl` invocation that triggers agent mode AND a `curl` invocation that defaults to passthrough.
9. Append `## Q3` section to SPIKE-FINDINGS.md with Verdict / Rationale (>=3) / Alternatives Considered (>=2) / Worked Example (the two curl commands) / Integration Point (cite `router.ts:36` and/or `agent-runner-factory.ts:82` as the new opt-in detection site) / Risk + Mitigation.

**Q5 (API key rotation policy):**

10. WebFetch https://docs.stripe.com/keys (or the Stripe-keys docs page) and extract Stripe's rotate model + grace period.
11. WebFetch https://platform.openai.com/docs/quickstart (or the OpenAI keys page) and extract OpenAI's rotate model — manual, no auto rotation.
12. WebFetch https://docs.anthropic.com/en/api/managing-api-keys and extract Anthropic's key lifecycle.
13. WebSearch "OWASP API Security 2023 credential rotation" and capture top result snippet on credential-lifecycle guidance.
14. Cross-reference REQUIREMENTS.md FR-BROKER-B1-01 schema (`revoked_at` column) — verdict must reconcile with this column existing.
15. Write `notes-q5-key-rotation.md` with the three vendor citations + OWASP guidance + a candidate-evaluation table for: A (manual revoke+recreate), B (auto 90-day rotation), C (default-keyed users on signup vs opt-in).
16. Choose ONE rotation-policy verdict AND ONE default-keyed-vs-opt-in verdict (these are two axes per RESEARCH.md).
17. Append `## Q5` section to SPIKE-FINDINGS.md with Verdict (TWO axes named) / Rationale (>=3 reasons total spanning both axes) / Alternatives Considered (>=2) / Lifecycle Flow (text-diagram of: created → last_used_at updates → revoked_at set → 401 returned, referencing api_keys table columns from FR-BROKER-B1-01) / Risk + Mitigation.

18. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append SHA check to BOTH notes files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const okQ3=s.includes('## Q3') && /curl/.test(s.split('## Q3')[1] || '') && /(router\.ts:|agent-runner-factory\.ts:)/.test(s); const okQ5=s.includes('## Q5') && /revoked_at/.test(s); if(!(okQ3&&okQ5)){console.error('Q3 or Q5 missing structure'); process.exit(1)} else {console.log('Q3+Q5 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-q3-agent-mode.md` exists with: client-compat matrix containing all 4 client rows (Bolt.diy / Open WebUI / Continue.dev / Cline), evidence URL per row, sacred SHA check line.
    - `.planning/phases/56-research-spike/notes-q5-key-rotation.md` exists with: Stripe + OpenAI + Anthropic + OWASP citations, candidate-evaluation table, sacred SHA check line.
    - SPIKE-FINDINGS.md contains `## Q3` AND `## Q5` headings.
    - Q3 section contains a `curl` invocation (substring `curl ` appears).
    - Q3 section cites `router.ts:` followed by digit(s) OR `agent-runner-factory.ts:` followed by digit(s).
    - Q5 section references the `revoked_at` column (substring `revoked_at` appears).
    - Q5 section explicitly addresses BOTH the rotation-policy axis AND the default-keyed-vs-opt-in axis (substrings "rotation" and "default-keyed" or "opt-in" both appear).
    - WebFetch was invoked for at least 4 distinct external URLs across this task.
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>Q3 + Q5 verdicts written; client-compat matrix complete; lifecycle flow described; sacred SHA unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Q4 verdict — Public endpoint platform (Caddy vs CF Worker) + TLS + rate-limit primitive</name>
  <files>.planning/phases/56-research-spike/notes-q4-public-endpoint.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q4 source inventory, common pitfall P4 about CF DNS-only posture, P5 about Server4-vs-Server5)
    - .planning/STATE.md (Server5 routing topology — Cloudflare DNS-only NOT proxied)
    - platform/relay/Caddyfile (current Server5 Caddy config — on-demand TLS, *.livinity.io wildcards)
  </read_first>
  <action>
1. Read all `read_first` files. Confirm the Caddyfile path resolves and current contents include on-demand TLS + `*.livinity.io` wildcards.
2. WebFetch https://caddyserver.com/docs/modules/http.handlers.rate_limit and extract: is the `rate_limit` handler in the stock Caddy v2.11.x binary, OR does it require a custom build with `mholt/caddy-ratelimit`?
3. WebFetch https://github.com/mholt/caddy-ratelimit (the plugin's README) and extract installation method — confirm whether `xcaddy build` is required.
4. WebFetch https://caddyserver.com/docs/automatic-https and confirm on-demand TLS still supported in Caddy v2.11.x (already running on Server5).
5. WebFetch https://developers.cloudflare.com/workers/platform/pricing/ and extract: free-tier limits (10ms CPU per request? 50ms?), paid-tier upgrade cost.
6. WebFetch https://developers.cloudflare.com/workers/runtime-apis/streams/ and confirm whether Cloudflare Workers can passthrough Server-Sent Events streams without buffering.
7. WebFetch https://developers.cloudflare.com/waf/rate-limiting-rules/ and extract: WAF rate-limit rule shape (threshold, period, action).
8. WebFetch https://developers.cloudflare.com/durable-objects/ and extract: per-key token-bucket viability via Durable Objects (vs KV's eventual-consistency).
9. WebSearch "Cloudflare DNS-only Worker route" — confirm whether a CF Worker can be attached to a domain that's currently DNS-only (orange cloud OFF). Document the finding (Workers requires proxied/orange-cloud — verify or refute).
10. Note STATE.md's hard rule: Cloudflare is currently DNS-only for `*.livinity.io`. Posture flip is a cost.
11. Write `notes-q4-public-endpoint.md` with:
    - Caddy `rate_limit` plugin install method (stock vs xcaddy)
    - CF Worker free-tier CPU cap and SSE viability
    - CF DNS posture cost (DNS-only → proxied flip)
    - Candidate evaluation table: A (Server5 Caddy + new `api.livinity.io` block + `caddy-ratelimit` plugin), B (Cloudflare Worker + Durable Objects rate-limit), C (Server5 Caddy native primitives only — no plugin)
    - Cold-start latency numbers cited from each source
    - Pair-of-three required by RESEARCH.md: platform + TLS strategy (LE on-demand vs CF-managed) + rate-limit primitive name
12. Choose ONE verdict.
13. Append `## Q4` section to SPIKE-FINDINGS.md with Verdict (naming all three of: platform / TLS / rate-limit primitive) / Rationale (>=3 reasons) / Alternatives Considered (>=2) / Cold-Start + Latency Notes / Integration Point (cite `platform/relay/Caddyfile` if Caddy chosen, OR cite a new file path under `platform/cf-workers/` if CF Worker chosen) / Risk + Mitigation. Risk MUST include the CF DNS posture cost if CF Worker chosen, OR the xcaddy custom build burden if Caddy + plugin chosen.
14. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append SHA check.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const sec=(s.split('## Q4')[1]||'').split(/\n## /)[0]; const has=sec && /Verdict/.test(sec) && /TLS/.test(sec) && /rate.?limit/i.test(sec); if(!has){console.error('Q4 missing platform+TLS+rate-limit triplet'); process.exit(1)} else {console.log('Q4 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-q4-public-endpoint.md` exists with: candidate-evaluation table, all 7 fetched URLs listed under "Sources Fetched", CF DNS posture finding, sacred SHA check.
    - SPIKE-FINDINGS.md contains `## Q4` heading.
    - Q4 section names all three of: a platform (Caddy or CF Worker), a TLS strategy (LE on-demand or CF-managed), a rate-limit primitive (substrings: "TLS" AND ("rate-limit" or "rate_limit" or "ratelimit") both appear in the Q4 section).
    - Q4 section cites either `platform/relay/Caddyfile` OR a `platform/cf-workers/` path as integration point.
    - Risk section names CF DNS posture cost (if CF chosen) OR xcaddy custom build burden (if Caddy+plugin chosen) — substring "DNS" or "xcaddy" or "custom build" appears in the risk section.
    - WebFetch was invoked for at least 5 of the listed Caddy + Cloudflare URLs.
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>Q4 verdict written with platform+TLS+rate-limit triplet; cold-start notes captured; CF DNS posture cost or xcaddy burden documented as risk.</done>
</task>

<task type="auto">
  <name>Task 3: Q6 verdict — Rate-limit policy (forward verbatim vs broker-side bucket)</name>
  <files>.planning/phases/56-research-spike/notes-q6-rate-limit.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q6 source inventory + decision framework)
    - .planning/phases/56-research-spike/notes-q4-public-endpoint.md (Q4 verdict — Q6 must align with Q4's perimeter rate-limit primitive)
    - livos/packages/livinityd/source/modules/livinity-broker/router.ts (lines 158-185 — existing 429 + Retry-After forwarding pattern from Phase 45)
    - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts (lines 92-106 — UpstreamHttpError with retryAfter capture)
    - .planning/REQUIREMENTS.md (FR-BROKER-B2-02 — baseline policy says "TBD pending Phase 56 spike"; FR-BROKER-C3-01..03 header forwarding spec)
  </read_first>
  <action>
1. Read all `read_first` files.
2. WebFetch https://docs.anthropic.com/en/api/rate-limits and extract verbatim:
   - Header names: `anthropic-ratelimit-requests-limit`, `anthropic-ratelimit-requests-remaining`, `anthropic-ratelimit-requests-reset`, `anthropic-ratelimit-tokens-limit`, `anthropic-ratelimit-tokens-remaining`, `anthropic-ratelimit-tokens-reset`
   - 429 response shape
   - Retry-After format
3. WebFetch https://platform.openai.com/docs/guides/rate-limits and extract OpenAI's `x-ratelimit-*` header names + 429 + Retry-After format.
4. WebFetch https://datatracker.ietf.org/doc/html/rfc7231#section-7.1.3 (or rfc9110 if it has succeeded rfc7231 by spike date) and confirm Retry-After format options (delta-seconds OR HTTP-date).
5. WebFetch https://en.wikipedia.org/wiki/Token_bucket and capture algorithm reference (parameters: capacity, refill rate).
6. Verify in repo that `ioredis` is a known dep (already confirmed in RESEARCH.md "Standard Stack" — `^5.4.0` in both nexus/livinityd) — no new dep needed for token bucket.
7. Write `notes-q6-rate-limit.md` with:
   - Anthropic rate-limit headers (verbatim from doc)
   - OpenAI rate-limit headers (verbatim from doc)
   - Retry-After spec
   - Token bucket algorithm reference + Redis schema sketch (`bucket:<keyId>` with `INCR` + TTL, atomic ops)
   - Candidate evaluation: A (forward verbatim, no broker bucket), B (broker-side per-key token bucket + forward upstream when more restrictive), C (Server5 Caddy/CF perimeter coarse + forward verbatim fine)
   - Alignment with Q4 verdict (cite Q4's rate-limit primitive and explain how Q6 layers on top or replaces)
8. Choose verdict that resolves REQUIREMENTS.md FR-BROKER-B2-02's "TBD."
9. Append `## Q6` section to SPIKE-FINDINGS.md with: Verdict / Rationale (>=3) / Alternatives Considered (>=2) / Header Forwarding List (the 6 Anthropic header names + the 6 OpenAI header names + Retry-After) / Integration Point (must cite `router.ts:158-185` as existing forward pattern; if broker-side bucket chosen, cite Redis schema `bucket:<keyId>` with TTL and refill rate constants) / Risk + Mitigation / Q4 Alignment paragraph.
10. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append SHA check.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const sec=(s.split('## Q6')[1]||'').split(/\n## /)[0]; const has=sec && /anthropic-ratelimit-requests-remaining/.test(sec) && /x-ratelimit-remaining-requests/.test(sec) && /Retry-After/.test(sec) && /router\.ts:15[0-9]|router\.ts:18[0-5]/.test(s); if(!has){console.error('Q6 missing required header names or integration point'); process.exit(1)} else {console.log('Q6 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-q6-rate-limit.md` exists with: Anthropic + OpenAI rate-limit header lists, Retry-After spec, token-bucket Redis schema sketch, candidate-evaluation table, Q4 alignment paragraph, sacred SHA check line.
    - SPIKE-FINDINGS.md contains `## Q6` heading.
    - Q6 section contains all six Anthropic header names AND all six OpenAI header names (substrings: `anthropic-ratelimit-requests-remaining`, `anthropic-ratelimit-tokens-remaining`, `x-ratelimit-remaining-requests`, `x-ratelimit-remaining-tokens`).
    - Q6 section contains `Retry-After`.
    - Q6 section cites `router.ts:158` (or any line in 158-185 — regex `router\.ts:15[0-9]|router\.ts:18[0-5]`).
    - Q6 section explicitly aligns with Q4 verdict (substring "Q4" appears in Q6 with an alignment statement).
    - WebFetch was invoked for at least 4 of the listed URLs.
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>Q6 verdict written; all 12 rate-limit header names cited; integration point at router.ts:158-185; Q4 alignment documented.</done>
</task>

</tasks>

<verification>
- Q3, Q4, Q5, Q6 verdict blocks all present in SPIKE-FINDINGS.md
- All four intermediate notes files exist
- Sacred file SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b` at end (validated by `git hash-object`)
- No Write/Edit tool calls targeted `nexus/packages/core/src/sdk-agent-runner.ts`
- Q4 names platform + TLS + rate-limit primitive triplet
- Q6 cites all 12 rate-limit header names + Retry-After
</verification>

<success_criteria>
- ROADMAP Phase 56 success criterion #1 partially satisfied: Q3, Q4, Q5, Q6 have verdicts (no "TBD")
- ROADMAP Phase 56 success criterion #3 satisfied: Q3 verdict has worked curl example for opt-in vs default
- ROADMAP Phase 56 success criterion #4 satisfied: Q4 verdict pairs platform + TLS strategy + rate-limit primitive
</success_criteria>

<output>
After completion, create `.planning/phases/56-research-spike/56-02-SUMMARY.md` summarizing: which verdicts were reached, key external evidence URLs cited, any [ASSUMED] flags from RESEARCH.md (A3 caddy plugin, A4 CF DNS posture) that flipped to [VERIFIED: <source>] during this plan.
</output>
