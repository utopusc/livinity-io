---
phase: 56-research-spike
plan: 03
type: execute
wave: 3
depends_on: [56-01, 56-02]
files_modified:
  - .planning/phases/56-research-spike/SPIKE-FINDINGS.md
  - .planning/phases/56-research-spike/notes-cross-cuts.md
autonomous: true
requirements: []
tags: [research, spike, audit, cross-cut, dependencies, sacred-file, d-51-03]

must_haves:
  truths:
    - "SPIKE-FINDINGS.md contains a Cross-Cuts section with three sub-sections: D-NO-NEW-DEPS audit, Sacred-file SHA stability, D-51-03 re-evaluation"
    - "D-NO-NEW-DEPS audit lists every package the Q1-Q7 verdicts imply, classified as already-present or new-needed"
    - "Sacred file SHA stability subsection records the live `git hash-object` output and confirms it equals 4f868d318abff71f8c8bfbcf443b2393a553018b"
    - "D-51-03 re-evaluation subsection states one of the three verdicts: not-needed-in-v30, still-needed-as-v30.1-hot-patch, or re-evaluate-after-Phase-63"
  artifacts:
    - path: ".planning/phases/56-research-spike/SPIKE-FINDINGS.md"
      provides: "Cross-Cuts section appended"
      contains: "## Cross-Cuts"
    - path: ".planning/phases/56-research-spike/notes-cross-cuts.md"
      provides: "Raw audit data: package.json grep output, sacred SHA logs, D-51-03 reasoning trace"
  key_links:
    - from: "Cross-Cuts D-NO-NEW-DEPS"
      to: "nexus/packages/core/package.json (verified via Read + grep)"
      via: "explicit version cite per package referenced in any verdict"
      pattern: "@anthropic-ai/sdk.*0\\.[0-9]+"
    - from: "Cross-Cuts D-51-03"
      to: "Q7 verdict (in 56-01) + Q1 verdict (in 56-01)"
      via: "explicit reference to Q1+Q7 outcomes"
      pattern: "Q1.*Q7|Q7.*Q1"
---

<objective>
Audit the Q1-Q7 verdicts for cross-cutting constraints:
1. **D-NO-NEW-DEPS:** does any verdict imply a new npm package? Classify each implied package as already-present (cite version + path) or new-needed (force Phase 57+ to budget the dep).
2. **Sacred SHA stability:** confirm `nexus/packages/core/src/sdk-agent-runner.ts` is byte-identical at SHA `4f868d318abff71f8c8bfbcf443b2393a553018b` after plans 56-01 and 56-02 ran. Drift = phase blocker.
3. **D-51-03 re-evaluation:** based on Q1 (passthrough handles external identity-line) + Q7 (Agent SDK aggregation behavior), verdict on whether Branch N reversal is still needed in v30, deferred to v30.1 hot-patch, or re-evaluate after Phase 63 live verification.

Purpose: ROADMAP Phase 56 success criterion #5 ("D-51-03 is re-evaluated with explicit verdict"). Plus the cross-cutting D-NO-NEW-DEPS gate that Phase 57+ planning depends on.

Output: `## Cross-Cuts` section appended to SPIKE-FINDINGS.md + `notes-cross-cuts.md` with raw audit data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-research-spike/56-CONTEXT.md
@.planning/phases/56-research-spike/56-RESEARCH.md
@.planning/phases/56-research-spike/SPIKE-FINDINGS.md

# Existing-code references
@nexus/packages/core/package.json
@livos/packages/livinityd/package.json

# Q1+Q7 verdicts (consumed by D-51-03 re-evaluation)
@.planning/phases/56-research-spike/notes-q1-passthrough.md
@.planning/phases/56-research-spike/notes-q7-streaming.md
@.planning/milestones/v29.5-phases/51-a2-streaming-fix/51-01-SUMMARY.md
</context>

<sacred_boundary>
**STRICT RULE:** This plan's entire purpose includes verifying `nexus/packages/core/src/sdk-agent-runner.ts` SHA. The plan MUST run `git hash-object` on the sacred file but MUST NOT Edit/Write it. Any drift detected is a phase-level blocker — stop and surface to the operator, do not "fix" by editing.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: D-NO-NEW-DEPS audit — classify every package implied by Q1-Q7 verdicts</name>
  <files>.planning/phases/56-research-spike/notes-cross-cuts.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Standard Stack section listing already-present libraries)
    - .planning/phases/56-research-spike/SPIKE-FINDINGS.md (Q1-Q7 verdicts produced by 56-01 and 56-02 — read every verdict's "D-NO-NEW-DEPS Implications" subsection if present, else infer from integration point)
    - nexus/packages/core/package.json (full)
    - livos/packages/livinityd/package.json (full)
  </read_first>
  <action>
1. Read all `read_first` files. Confirm SPIKE-FINDINGS.md has Q1-Q7 verdict blocks present. If any are missing, fail and surface (this plan depends on 56-01 + 56-02 completing).
2. Walk every Q1-Q7 verdict in SPIKE-FINDINGS.md. For each verdict, list every package or runtime feature the verdict's integration point will use. Examples to look for:
   - `@anthropic-ai/sdk` (Q1 — likely if SDK-direct chosen)
   - `@anthropic-ai/claude-agent-sdk` (Q7 — agent mode keeps using this; should NOT appear in passthrough path)
   - `node:crypto` (Q5 — base62 random + SHA-256 for keys)
   - `ioredis` (Q6 — token bucket if broker-side chosen)
   - `pg` (Q5 — api_keys table)
   - `caddy-ratelimit` plugin (Q4 — only if Caddy + plugin chosen; this is a Caddy module, not an npm package — flag accordingly)
   - `cloudflare-workers` runtime (Q4 — only if CF Worker chosen; not an npm dep but a separate deployment target)
3. For each package, run grep on `nexus/packages/core/package.json` and `livos/packages/livinityd/package.json` to classify:
   - already-present (cite version + path: e.g., `@anthropic-ai/sdk: ^0.80.0 at nexus/packages/core/package.json`)
   - new-needed (force Phase 57+ to budget; flag as a delta to D-NO-NEW-DEPS)
4. Build the audit table in `notes-cross-cuts.md`:
   ```
   | Package / Runtime | Implied by Verdict | Status | Cite |
   |-------------------|--------------------|--------|------|
   | @anthropic-ai/sdk | Q1 (if A) | already-present | nexus/packages/core/package.json:NN, version ^0.80.0 |
   | ioredis | Q6 (if B) | already-present | both package.jsons, ^5.4.0 |
   | caddy-ratelimit | Q4 (if A) | NOT npm — Caddy module | requires xcaddy build per Q4 risk |
   | ... | ... | ... | ... |
   ```
5. Compute the D-NO-NEW-DEPS verdict for v30 Phase 57:
   - GREEN: zero new npm deps required by any Q-verdict's primary path
   - YELLOW: zero new npm deps but a non-npm dep (Caddy module, CF Worker runtime) is implied — Phase 57+ budget
   - RED: at least one new npm dep is required — D-NO-NEW-DEPS exception requested for Phase 57+
6. Append `## Cross-Cuts` heading to SPIKE-FINDINGS.md with `### D-NO-NEW-DEPS Audit` subsection containing:
   - Verdict color (GREEN / YELLOW / RED)
   - Audit table (cited in step 4)
   - For each package classified "already-present", cite the package.json line with version
   - For each package classified "new-needed", cite the verdict that requires it AND the rationale ("Phase 57 must budget X because Q1 chose strategy B which depends on it")
7. Append findings to `notes-cross-cuts.md` with raw grep output as evidence.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const ok=s.includes('## Cross-Cuts') && /D-NO-NEW-DEPS/i.test(s) && /(GREEN|YELLOW|RED)/.test(s.split('## Cross-Cuts')[1] || ''); if(!ok){console.error('D-NO-NEW-DEPS audit missing or no verdict color'); process.exit(1)} else {console.log('D-NO-NEW-DEPS OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-cross-cuts.md` exists with: package audit table covering all 7 verdicts, raw grep output evidence per "already-present" claim, D-NO-NEW-DEPS verdict color justification.
    - SPIKE-FINDINGS.md contains `## Cross-Cuts` heading.
    - Cross-Cuts section contains `### D-NO-NEW-DEPS Audit` subsection.
    - The D-NO-NEW-DEPS subsection contains exactly one of: `GREEN`, `YELLOW`, or `RED` as the overall verdict color.
    - The D-NO-NEW-DEPS subsection cites at minimum `@anthropic-ai/sdk` with version (regex `@anthropic-ai/sdk.*0\.[0-9]+`).
    - For every Q-verdict (Q1-Q7), the audit table either lists at least one package implied by that verdict OR explicitly states "no new packages implied" for that Q.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>D-NO-NEW-DEPS audit complete with verdict color; every Q-verdict accounted for in the table; raw grep evidence in notes file.</done>
</task>

<task type="auto">
  <name>Task 2: Sacred file SHA stability check (start vs end of phase)</name>
  <files>.planning/phases/56-research-spike/notes-cross-cuts.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md (sacred boundary rules)
    - .planning/phases/56-research-spike/56-RESEARCH.md (P3 pitfall about sacred-file edits)
    - .planning/STATE.md (Sacred file integrity section — current SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`)
  </read_first>
  <action>
1. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and capture output.
2. Run `git status -- nexus/packages/core/src/sdk-agent-runner.ts` and confirm the file is NOT in modified state. Capture output.
3. Run `git diff --stat -- nexus/packages/core/src/sdk-agent-runner.ts` and confirm zero lines changed. Capture output.
4. Compare hash-object output to the canonical SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`.
5. If the SHA matches: append a stability-confirmed entry to `notes-cross-cuts.md` with the three command outputs.
6. If the SHA does NOT match: STOP. Write a phase-blocker entry to `notes-cross-cuts.md` (with all three command outputs) and append a `### SACRED FILE DRIFT — PHASE BLOCKER` warning section to SPIKE-FINDINGS.md naming the actual SHA, the expected SHA, and the file's modification source. Do NOT proceed to D-51-03 re-evaluation. Do NOT attempt to "fix" by editing.
7. If the SHA matches, append `### Sacred File SHA Stability` subsection to SPIKE-FINDINGS.md Cross-Cuts containing:
   - Expected SHA: `4f868d318abff71f8c8bfbcf443b2393a553018b`
   - Observed SHA (from `git hash-object` output)
   - `git status` output (must show clean)
   - `git diff --stat` output (must show 0 lines changed)
   - Verdict: PASS — sacred file untouched throughout phase 56-01 + 56-02
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process'); const sha=execSync('git hash-object nexus/packages/core/src/sdk-agent-runner.ts').toString().trim(); if(sha!=='4f868d318abff71f8c8bfbcf443b2393a553018b'){console.error('SACRED FILE DRIFT: expected 4f868d318abff71f8c8bfbcf443b2393a553018b, got '+sha); process.exit(1)} const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); if(!/Sacred File SHA Stability/.test(s)){console.error('Sacred SHA Stability subsection missing from SPIKE-FINDINGS.md'); process.exit(1)} console.log('Sacred SHA OK: '+sha)"</automated>
  </verify>
  <acceptance_criteria>
    - `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git status -- nexus/packages/core/src/sdk-agent-runner.ts` shows the file is NOT in modified, deleted, or staged state.
    - `git diff --stat -- nexus/packages/core/src/sdk-agent-runner.ts` shows zero lines changed.
    - SPIKE-FINDINGS.md contains a `### Sacred File SHA Stability` subsection under `## Cross-Cuts`.
    - That subsection contains the expected SHA `4f868d318abff71f8c8bfbcf443b2393a553018b` AND a verdict line containing `PASS`.
    - `notes-cross-cuts.md` has captured the three command outputs verbatim.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts` performed by this task (or by any earlier task in the phase — validated by the git diff --stat being 0 across the entire phase).
  </acceptance_criteria>
  <done>Sacred file SHA stability PASS recorded; three command outputs captured; no edits made.</done>
</task>

<task type="auto">
  <name>Task 3: D-51-03 re-evaluation verdict (Branch N reversal still needed?)</name>
  <files>.planning/phases/56-research-spike/notes-cross-cuts.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (D-51-03 sub-question section, P3 pitfall)
    - .planning/phases/56-research-spike/SPIKE-FINDINGS.md (Q1 + Q7 verdicts produced earlier in this phase)
    - .planning/phases/56-research-spike/notes-q1-passthrough.md
    - .planning/phases/56-research-spike/notes-q7-streaming.md
    - .planning/milestones/v29.5-phases/51-a2-streaming-fix/51-01-SUMMARY.md (D-51-03 deferral rationale from Phase 51)
    - .planning/STATE.md (D-51-03 reference in Locked decisions)
  </read_first>
  <action>
1. Read all `read_first` files. Confirm Q1 verdict is present in SPIKE-FINDINGS.md and Q7 verdict is present.
2. Extract from Q1 verdict: does the chosen passthrough strategy bypass the sacred file's identity-line (lines 264-270) for external clients? (Almost certainly YES if any of strategies A/B/C from Q1's framework is chosen — passthrough by definition bypasses sacred file.)
3. Extract from Q7 verdict: does Agent mode keep current behavior (sacred file untouched, identity-line still emitted in agent path)? (Almost certainly YES per RESEARCH.md framework candidate A.)
4. Combine: external clients see no Nexus identity (passthrough path); internal LivOS AI Chat sees Nexus identity (agent path). Question: does that satisfy v30 needs without reversing Branch N?
5. Apply RESEARCH.md "D-51-03 sub-question" verdict criteria:
   - If Q1 eliminates external identity contamination → external use case resolved → Branch N reversal NOT needed for external.
   - Internal LivOS AI Chat still goes through agent mode → identity-line still emitted there.
   - Verdict options:
     a. "Not needed in v30 — passthrough handles external; agent mode internal-only and identity-line acceptable there"
     b. "Still needed as v30.1 hot-patch — sacred-edit batched separately"
     c. "Re-evaluate after Phase 63 live verification with internal AI Chat self-identification test"
6. Choose ONE verdict. Per Phase 56 boundary, sacred-file edits are out of scope for v30 regardless — so verdict (a) is most aligned with v30 scope; (b) is acceptable if there's a clear internal-identity-leak risk; (c) is a punt.
7. Append `### D-51-03 Re-Evaluation` subsection to SPIKE-FINDINGS.md Cross-Cuts containing:
   - The original D-51-03 statement (quoted from STATE.md or Phase 51 summary)
   - How Q1 affects it (cite Q1 verdict outcome)
   - How Q7 affects it (cite Q7 verdict outcome)
   - Re-evaluation verdict (one of a/b/c)
   - Rationale (>=3 reasons)
   - Action item routing: if (a), no further action; if (b), file as v30.1 hot-patch deferred decision row; if (c), add to Phase 63 UAT script — the test "internal LivOS AI Chat self-identifies as Nexus, NOT as 'Bolt' or other external persona"
8. Add a "Decisions Log Entry" line at the end of the D-51-03 subsection in the format used by STATE.md decisions:
   ```
   D-30-XX: D-51-03 re-evaluation — <verdict>. Rationale: <one sentence>. (Phase 56 spike outcome.)
   ```
   The XX number can be assigned during Plan 56-04 synthesis if a sequential decision number is needed; for this plan, XX = `01` placeholder.
9. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` once more and append SHA-confirms-still-untouched line to `notes-cross-cuts.md`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const sec=(s.split('### D-51-03 Re-Evaluation')[1]||'').split(/\n### |\n## /)[0]; const okVerdict=/Not needed in v30|v30\.1 hot-patch|Re-evaluate after Phase 63/.test(sec); const okRefs=/Q1/.test(sec) && /Q7/.test(sec); const okLog=/D-30-/.test(sec); if(!(okVerdict&&okRefs&&okLog)){console.error('D-51-03 re-eval missing verdict, Q1+Q7 refs, or decision-log entry'); process.exit(1)} else {console.log('D-51-03 re-eval OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - SPIKE-FINDINGS.md contains a `### D-51-03 Re-Evaluation` subsection under `## Cross-Cuts`.
    - That subsection contains exactly one of: "Not needed in v30", "v30.1 hot-patch", or "Re-evaluate after Phase 63" as the verdict.
    - That subsection references both Q1 verdict outcome AND Q7 verdict outcome (substrings "Q1" and "Q7" both appear).
    - That subsection contains at least 3 numbered/bulleted rationale reasons.
    - That subsection contains a decisions-log entry starting with `D-30-` (numbered placeholder).
    - `notes-cross-cuts.md` contains a final-end-of-task sacred-SHA confirmation line equal to `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>D-51-03 re-evaluated with explicit verdict (one of three options); rationale references Q1+Q7 outcomes; decisions-log entry placed; sacred SHA still pristine.</done>
</task>

</tasks>

<verification>
- `## Cross-Cuts` section present in SPIKE-FINDINGS.md with three subsections (D-NO-NEW-DEPS Audit, Sacred File SHA Stability, D-51-03 Re-Evaluation)
- D-NO-NEW-DEPS audit has verdict color (GREEN/YELLOW/RED)
- Sacred SHA verified == `4f868d318abff71f8c8bfbcf443b2393a553018b` AT END of all three tasks
- D-51-03 verdict is one of three explicit options (no "TBD")
- `notes-cross-cuts.md` has raw evidence for all three subsections
</verification>

<success_criteria>
- ROADMAP Phase 56 success criterion #5 satisfied: D-51-03 re-evaluated with explicit verdict
- Cross-cutting D-NO-NEW-DEPS gate audited (Phase 57+ planning unblocked OR exception flagged)
- Sacred file SHA stability proven (no drift introduced by spike)
</success_criteria>

<output>
After completion, create `.planning/phases/56-research-spike/56-03-SUMMARY.md` summarizing: D-NO-NEW-DEPS verdict color, sacred SHA stability outcome, D-51-03 re-evaluation outcome with rationale.
</output>
