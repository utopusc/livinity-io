---
phase: 100-multi-stream-window-redesign
plan: 08-01
subsystem: webapps
tags: [xvfb, fluxbox, lifecycle, livinityd, selfclaude]
dependency_graph:
  requires: []
  provides: [Xvfb-on-:1, fluxbox-on-:1, helper-modules-xvfb-display, helper-modules-fluxbox-wm]
  affects: [livos/packages/livinityd/source/index.ts, livos/install.sh, update.sh]
tech_stack:
  added: [Xvfb (apt), fluxbox (apt)]
  patterns: [sudo-n-u-bruce spawn, SIGTERM→2s→SIGKILL stop, exited-promise lifecycle]
key_files:
  created:
    - livos/packages/livinityd/source/modules/webapps/xvfb-display.ts
    - livos/packages/livinityd/source/modules/webapps/xvfb-display.test.ts
    - livos/packages/livinityd/source/modules/webapps/fluxbox-wm.ts
    - livos/packages/livinityd/source/modules/webapps/fluxbox-wm.test.ts
    - .planning/phases/100-multi-stream-window-redesign/100-08-01-SUMMARY.md
  modified:
    - livos/packages/livinityd/source/index.ts
    - livos/install.sh
    - update.sh
decisions:
  - D-100-08-A locked: dedicated Xvfb :1 + fluxbox WM under livinityd lifecycle (selfclaude pattern, bare-metal adaptation).
  - Auto-respawn deferred (risk #3 mitigation) — operator restart via systemctl restart livos.
  - Empty-keybindings stub rc /tmp/livos-fluxbox.cfg so fluxbox doesn't swallow Alt+F1/Tab keys destined for WebApp.
metrics:
  duration: ~25 minutes
  completed_date: 2026-05-09
  commits: 4
  tasks: 5
---

# Phase 100-08-01 SUMMARY

**One-liner:** Stand up dedicated Xvfb display `:1` + fluxbox WM under livinityd lifecycle so subsequent 100-08-* plans can target a clean X server instead of bruce's GNOME `:0` (D-100-08-A).

**Date:** 2026-05-09
**Goal:** Stand up Xvfb :1 + fluxbox WM under livinityd lifecycle (D-100-08-A) so 100-08-02 cutover, 100-08-04 per-WebApp MCP, and 100-08-06 deploy have a working dedicated display.

## Diff

| File | Change |
|---|---|
| `livos/packages/livinityd/source/modules/webapps/xvfb-display.ts` | NEW (86 lines) — `startXvfb({display, resolution, user})` helper |
| `livos/packages/livinityd/source/modules/webapps/xvfb-display.test.ts` | NEW (80 lines) — 4 unit tests (export, argv, sudo, stop signal chain) |
| `livos/packages/livinityd/source/modules/webapps/fluxbox-wm.ts` | NEW (101 lines) — `startFluxbox({display, user, rcPath})` helper with empty-keybindings stub rc |
| `livos/packages/livinityd/source/modules/webapps/fluxbox-wm.test.ts` | NEW (65 lines) — 4 unit tests (export, argv+env, sudo, stop signal chain) |
| `livos/packages/livinityd/source/index.ts` | +49 — imports + class fields + start()/stop() wiring |
| `livos/install.sh` | +6 / -2 (xvfb + fluxbox to streaming apt block, with comment) |
| `update.sh` | +5 / -2 (xvfb fluxbox apt install + Xvfb fluxbox bin verification loop) |

Total: +390 / -2 across 7 files.

## Test results

- `npx vitest run xvfb-display.test fluxbox-wm.test`: **8/8 PASS** (RED → GREEN gate satisfied).
  - `xvfb-display.test.ts` (4 tests): exports startXvfb, spawns Xvfb with `:1` + `1920x1080x24` + `-nolisten tcp` + `-ac`, spawns under `sudo -n -u bruce`, stop() sends SIGTERM then SIGKILL after 2s.
  - `fluxbox-wm.test.ts` (4 tests): exports startFluxbox, spawns fluxbox with `-display :1` + `DISPLAY=:1` env, spawns under `sudo -n -u bruce`, stop() signal chain.

## Sacred SHA gate

- Pre commits:  `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- Post commits: `f3538e1d811992b782a9bb057d1b7f0a0189f95f`

Verified via `git hash-object liv/packages/core/src/sdk-agent-runner.ts` after each of the 4 task commits. Pre-commit hook (`.husky/pre-commit` calling `scripts/check-sacred.sh`) fired green on every commit.

## Commits

- `6e0e028e` test(100-08-01): RED - assert xvfb-display + fluxbox-wm helper contracts
- `de494d90` feat(100-08-01): xvfb-display + fluxbox-wm helper modules
- `9beeb0ce` feat(100-08-01): wire Xvfb :1 + fluxbox into livinityd lifecycle
- `48f04f18` feat(100-08-01): add xvfb + fluxbox to install.sh + update.sh apt deps

(SUMMARY commit appended after this file is committed.)

## Deviations from Plan

### Rule 1 — Bug: test file imports needed `.js` extensions

- **Found during:** Task 2 (post-RED, after writing helper modules and re-running typecheck).
- **Issue:** The plan literal showed `await import('./xvfb-display')` (no `.js` extension), which fails `tsc --noEmit` against the package's `moduleResolution: nodenext` config (TS2835).
- **Fix:** Updated all 8 dynamic-import lines across `xvfb-display.test.ts` and `fluxbox-wm.test.ts` to use `'./xvfb-display.js'` and `'./fluxbox-wm.js'`. Tests still GREEN (vitest's bundler resolves `.js` to the `.ts` source via vite).
- **Files modified:** `livos/packages/livinityd/source/modules/webapps/xvfb-display.test.ts`, `livos/packages/livinityd/source/modules/webapps/fluxbox-wm.test.ts`.
- **Commit:** rolled into `de494d90` (Task 2 GREEN commit) — the test files were re-staged alongside the new source files.

### Note — `tsc --noEmit` exits non-zero against pre-existing errors

- The plan's Task 2 acceptance criterion says `pnpm --filter @livos/livinityd exec tsc --noEmit` must exit 0. The repo state at start of this plan had **378 pre-existing TS errors** in unrelated files (`pipewire-portal.test.ts`, `trpc-router.ts`, `widgets/routes.ts`, `window-manager.test.ts`, `trpc-streams.test.ts`, etc.).
- **Action:** per executor scope-boundary rule, only typecheck errors directly caused by this plan's changes are in-scope. Confirmed total error count UNCHANGED at 378 before and after my edits — my contribution introduced **0 new TS errors**. The pre-existing 378 are out-of-scope and logged here for the verifier.
- **Filter command used:** `npx tsc --noEmit 2>&1 | grep -E "(xvfb-display|fluxbox-wm|source/index.ts)"` returned 0 matches after the `.js` extension fix. My files type-check clean.

### Note — pnpm filter syntax

- The plan literal uses `pnpm --filter @livos/livinityd test`. The actual package name in `livos/packages/livinityd/package.json` is `livinityd` (not `@livos/livinityd`). I ran tests directly via `cd livos/packages/livinityd && npx vitest run` to bypass the workspace filter mismatch. No code change needed — purely a documentation note.

## Threat Flags

None — this plan creates Xvfb/fluxbox lifecycle helpers under livinityd's existing `sudo -n -u bruce` trust boundary (matching Chrome / x11vnc spawn pattern). No new network surface (`-nolisten tcp` enforced), no new file-access patterns, no schema changes. STRIDE register entries T-100-08-01-01..04 from the plan all unchanged in disposition.

## Self-Check: PASSED

- [x] `livos/packages/livinityd/source/modules/webapps/xvfb-display.ts` exists.
- [x] `livos/packages/livinityd/source/modules/webapps/xvfb-display.test.ts` exists.
- [x] `livos/packages/livinityd/source/modules/webapps/fluxbox-wm.ts` exists.
- [x] `livos/packages/livinityd/source/modules/webapps/fluxbox-wm.test.ts` exists.
- [x] Commit `6e0e028e` exists in `git log`.
- [x] Commit `de494d90` exists in `git log`.
- [x] Commit `9beeb0ce` exists in `git log`.
- [x] Commit `48f04f18` exists in `git log`.
- [x] Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` matches `git hash-object liv/packages/core/src/sdk-agent-runner.ts` at HEAD.
- [x] 8/8 unit tests PASS.

## Carryover for 100-08-02

- Now that `:1` exists, switch `WEBAPPS_X11_ENV.DISPLAY` from `:0` → `:1` in `window-discovery.ts:49-52` (single-point change covers ~90% of paths via `WEBAPPS_X11_ENV` re-export).
- Native bytebot input.ts handled by 100-08-03 descriptor extension (DISPLAY passed through `BYTEBOT_TARGET_DISPLAY` env, not the global default).
- Mini PC apt-install of `xvfb` + `fluxbox` happens at 100-08-06 deploy time (idempotent on already-installed; first-time installs ~10MB xvfb + ~1MB fluxbox).
- The 500ms grace between `startXvfb` and `startFluxbox` is empirical (selfclaude README cites it). If 100-08-06 UAT shows fluxbox crashes immediately on `:1`, increase to 1s and re-deploy.
