---
phase: 100-multi-stream-window-redesign
plan: 09-05
subsystem: ui
tags: [ui, chat, drawer, inline, zustand, webapp]
requires:
  - 100-04-PLAN (Sheet drawer host pattern)
  - 100-06-PLAN (floating action bar)
provides:
  - WebAppChatBottomBar (inline chat anchored at bottom of stream window)
  - chatLogExpandedByWebappId + toggleChatLog (Zustand store extension)
affects:
  - webapp-stream-window.tsx (drops Sheet `chat` branch; renders bottom bar)
  - webapp-floating-action-bar.tsx (Chat icon repurposed)
  - webapp-chat-drawer.tsx (DEPRECATED banner; export retained)
tech-stack-added: []
patterns:
  - "Inline overlay band: absolute inset-x-0 bottom-0 z-10 over stream wrapper's pb-9 reservation"
  - "Per-webappId Zustand state via Record<string, T> map"
key-files-created:
  - livos/packages/ui/src/modules/window/app-contents/webapp-chat-bottom-bar.tsx
key-files-modified:
  - livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx
  - livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx
  - livos/packages/ui/src/modules/window/webapp-drawer-store.ts
  - livos/packages/ui/src/modules/window/app-contents/webapp-chat-drawer.tsx
  - livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx
decisions:
  - "Chat surface moves from Sheet drawer (35% slide-in) → inline persistent bottom bar (~44px collapsed, ~204px expanded)"
  - "Chat icon active state mirrors chatLogExpanded (NOT drawer state)"
  - "WebAppChatDrawer file kept as DEPRECATED reference target — backwards-compat for any downstream importers"
metrics:
  duration: ~4min
  completed: 2026-05-09
  tasks: 5
  commits: 4
---

# Phase 100 Plan 09-05: Inline Chat Bottom Bar Summary

**One-liner:** Replaces the Chat shadcn Sheet drawer with a persistent slim text input + collapsible message log anchored at the bottom of every WebApp stream window — per user explicit "Chat penceresi olmasin sadece yazi yazalim. Yazilar sadece Alt kisimda gozuksun. Butonlar kalsin o sirada."

## Diff

| File                                         | Change                                                                                                                                                                              |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| webapp-chat-bottom-bar.tsx                   | NEW — slim text input + collapsible message log; `absolute inset-x-0 bottom-0 z-10` band overlaying stream wrapper's `pb-9` reservation; consumes `useWebAppAgent` + drawer store. |
| webapp-stream-window.tsx                     | +import `WebAppChatBottomBar`; render inside stream wrapper; drop `WebAppChatDrawer` import; Sheet `open` prop excludes `'chat'`; Sheet body removes the chat branch.              |
| webapp-floating-action-bar.tsx               | +`toggleChatLog` + `chatLogExpanded` selectors; Chat icon `onClick` routes to `toggleChatLog` (not drawer toggle); active state uses `chatLogExpanded` for chat.                    |
| webapp-drawer-store.ts                       | +`chatLogExpandedByWebappId: Record<string, boolean>` state; +`toggleChatLog(webappId)` action.                                                                                     |
| webapp-chat-drawer.tsx                       | DEPRECATED banner at top; export retained for backwards-compat.                                                                                                                     |
| webapp-stream-window.unit.test.tsx           | +5 invariants (T-09-05-01..05) across 2 describe blocks; existing 21 invariants preserved.                                                                                          |

## Test results

- `pnpm --filter ui exec vitest run webapp-stream-window.unit`: **26/26 PASS** (5 new + 21 pre-existing).
- `pnpm --filter ui build`: vite build clean (37.9s, 212 precache entries).

## Mini PC verification (post-deploy)

1. SSH `bruce@10.69.31.68`; deploy via `bash /opt/livos/update.sh`.
2. Open WebApp Gmail.
3. Expected: NO Chat slide-in drawer ever appears.
4. Bottom of stream window has a slim text input row (~44px) with a chevron-up button on the left.
5. Click the chevron OR click the floating Chat icon → message log expands above the input (max-h ~160px).
6. Click again → log collapses.
7. Type "hello" + send → message appears in the log → agent reply renders inline.
8. Floating Teach + Auto icons still open their respective drawers (unchanged from 100-04).

## Sacred SHA gate

- **Pre commits:**  `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- **Post commits:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- Pre-commit hook fired green on all 4 commits in this plan.

## Commits

- `16a6140d` test(100-09-05): RED - assert inline chat bottom-bar invariants
- `b8ba6410` feat(100-09-05): drawer store +toggleChatLog +chatLogExpandedByWebappId
- `c331b931` feat(100-09-05): WebAppChatBottomBar component (inline chat at bottom)
- `8934b92a` feat(100-09-05): integrate WebAppChatBottomBar; drop Sheet chat branch; repurpose Chat icon

## Deviations from Plan

None — plan executed exactly as written. The only minor delta: the plan acceptance criterion expected `grep -c "WebAppChatDrawer" webapp-stream-window.tsx == 0`, but the dropped-import comment retains the literal name once. This is documentation-only (no import statement, no JSX usage), and the canonical T-09-05-02 invariant (which asserts `<WebAppChatDrawer` JSX is absent) passes.

## TDD Gate Compliance

- RED gate: `16a6140d test(100-09-05): ...` — 5 failing invariants asserted before implementation.
- GREEN gate: `b8ba6410`, `c331b931`, `8934b92a` — incremental implementation flips invariants one slice at a time:
  - After `b8ba6410`: 4 RED → 3 RED (T-09-05-03 GREEN).
  - After `c331b931`: 3 RED → 2 RED (T-09-05-05 GREEN).
  - After `8934b92a`: 0 RED (all 26 tests PASS).
- REFACTOR gate: not applicable (no cleanup needed).

## Carryover for 100-09-06

- 09-06 will rework Teach drawer next: drop the Sheet `teach` branch, inline a `<TeachPopupHost/>` (toast-per-event) + `<SkillsPopover/>` (top-right popover) + save modal. The pattern established here (drop drawer → inline component → add Zustand store action) is the template.
- `webapp-chat-drawer.tsx` remains as a DEPRECATED reference target. v34 cleanup may delete it if no consumers surface.
- `WEBAPP_MODE_CHANGE_EVENT` dispatch preserved on Chat icon click — Phase 96/97 listeners unaffected.

## Self-Check: PASSED

- File `livos/packages/ui/src/modules/window/app-contents/webapp-chat-bottom-bar.tsx`: FOUND
- File `.planning/phases/100-multi-stream-window-redesign/100-09-05-SUMMARY.md`: FOUND (this file)
- Commit `16a6140d`: FOUND
- Commit `b8ba6410`: FOUND
- Commit `c331b931`: FOUND
- Commit `8934b92a`: FOUND
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`: PRESERVED (verified post-Task-4)
