---
phase: 100-multi-stream-window-redesign
plan: 05
type: execute
wave: 5
depends_on:
  - 100-02
  - 100-03
  - 100-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/98-uat-polish/UAT-CHECKLIST.md
  - .planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md
  - .planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md
autonomous: false
requirements_addressed:
  - V33-MULTI-05
summary: >-
  Mini PC deploy + user-walked end-to-end UAT closing the v33 ship gate.
  Push to GitHub `utopusc/livinity-io`, run `bash /opt/livos/update.sh` on
  bruce@10.69.31.68 (long operation: backgrounded with nohup per
  reference_zerotier_unstable.md), then walk UAT-CHECKLIST.md sections A-J
  with the user covering all 11 CONTEXT success criteria. On PASS: write
  100-05-SUMMARY.md + PHASE-SUMMARY.md, flip v33 milestone to ✅ Shipped
  in ROADMAP.md's Project-Level Milestone Index, update STATE.md to mark
  Phase 100 complete + v33 closed.
must_haves:
  truths:
    - "Mini PC has the Phase 100 build deployed (livinityd + UI restart green; deployed SHA recorded)."
    - "Two concurrent WebApp clicks produce two distinct stream windows on independent x11vnc ports (15900 + 15901), each rendering its own captured Chrome window."
    - "Each stream window has no URL bar, the stream is full-bleed, and the bottom 4-icon row renders the Chat/Teach/Watch/Auto buttons."
    - "Clicking each icon opens its drawer; second click closes; switching swaps content."
    - "Bytebot Auto mode in WebApp A does not interfere with WebApp B (per-window MCP env confirmed via journal log)."
    - "All 11 success criteria from 100-CONTEXT.md `<success_criteria>` are checked PASS in UAT-CHECKLIST.md."
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` is unchanged AT HEAD AND on the deployed Mini PC `/opt/liv/packages/core/src/sdk-agent-runner.ts`."
    - "v33 milestone flips to ✅ Shipped in ROADMAP.md Project-Level Milestone Index."
    - "STATE.md is updated: Phase 100 marked complete + Current Position advances + Last shipped milestone updated to v33.0."
  artifacts:
    - path: ".planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md"
      provides: "Deploy + UAT walk record"
    - path: ".planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md"
      provides: "Phase 100 close — covers all 5 plans, sacred SHA timeline, v33 ship signal"
    - path: ".planning/ROADMAP.md"
      provides: "v33 milestone flipped ✅"
    - path: ".planning/STATE.md"
      provides: "Phase 100 complete; current position advanced"
    - path: ".planning/phases/98-uat-polish/UAT-CHECKLIST.md"
      provides: "Phase 100 row added with PASS / live evidence"
  key_links:
    - from: "Mini PC deployed build"
      to: "GitHub HEAD"
      via: "bash /opt/livos/update.sh"
      pattern: "update\\.sh"
    - from: "ROADMAP.md v33 row"
      to: "Project-Level Milestone Index"
      via: "shipping flip"
      pattern: "v33\\.0.*Shipped"
---

<objective>
Close Phase 100 by deploying to Mini PC and walking the UAT with the user. The four code plans (100-01..100-04) shipped the diagnosis + fix + UI rewire; this plan ships the bits to bruce@10.69.31.68 and gates the v33 milestone on a user-confirmed end-to-end PASS covering all 11 CONTEXT success criteria.

Purpose: V33-MULTI-05 — v33 cannot flip to ✅ Shipped without a user-walked Mini PC UAT (per memory `feedback_milestone_uat_gate.md` — "v29.4 audit said 'passed' with 4× human_needed verifications, shipped broken; UAT is not optional"). 100-05 is that gate.

Output: 100-05-SUMMARY.md + PHASE-SUMMARY.md + ROADMAP.md + STATE.md + UAT-CHECKLIST.md updates. ZeroTier-aware execution: every Mini PC operation expected to take >30s is backgrounded with `nohup … >log 2>&1 &` and polled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md
@.planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md
@.planning/phases/100-multi-stream-window-redesign/100-02-SUMMARY.md
@.planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md
@.planning/phases/100-multi-stream-window-redesign/100-04-SUMMARY.md

<interfaces>
<!-- Operational facts the executor needs. -->

Mini PC SSH:
  ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
      -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      bruce@10.69.31.68

Sacred SHA: f3538e1d811992b782a9bb057d1b7f0a0189f95f

Deploy command (per memory MEMORY.md "Mini PC > Deployment is bash /opt/livos/update.sh"):
  Long operation. Always backgrounded:
    ssh ... 'nohup sudo bash /opt/livos/update.sh >/tmp/p100-update.log 2>&1 & echo $!'
  Poll completion:
    ssh ... 'tail -20 /tmp/p100-update.log; ls -la /tmp/livinity-update-* 2>/dev/null | tail -5; systemctl is-active livos liv-core liv-worker liv-memory'

Services to verify after deploy:
  livos.service     (livinityd, port 8080)        — REQUIRED for Phase 100
  liv-core.service  (port 3200)                   — REQUIRED for Phase 100
  liv-worker.service                              — REQUIRED for Phase 100
  liv-memory.service                              — OPTIONAL (known carry-over, see below)

**KNOWN CARRY-OVER:** Per memory MEMORY.md "Pre-existing breakage as of 2026-04-25",
`liv-memory.service` runs in a restart loop because `/opt/liv/packages/memory/dist/index.js`
never gets compiled — `update.sh` builds core/worker/mcp-server but NOT memory. This is a
pre-existing breakage that Phase 100 does NOT introduce and does NOT fix. If liv-memory
is inactive after deploy AND the journal shows the known build-omission signature, the
deploy gate PASSES anyway (documented as carry-over). Any OTHER inactive reason aborts.

Sacred SHA verification on Mini PC:
  ssh ... 'git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts'
  Expected: f3538e1d811992b782a9bb057d1b7f0a0189f95f

UAT-CHECKLIST.md location: .planning/phases/98-uat-polish/UAT-CHECKLIST.md (per ROADMAP.md line 11 — sections A-J)

ROADMAP.md flip targets:
  - Line 11: `- ✅ **v33.0 WebApp Launcher + Teach/Auto Modes** — Phases 92-100 (CODE-COMPLETE 2026-05-08; pending Mini PC UAT signoff …)` becomes `… (Shipped <YYYY-MM-DD> after Mini PC UAT signoff)`.
  - Project-Level Milestone Index near line 503: change `**v33.0 WebApp Launcher + Teach/Auto Modes** (CODE-COMPLETE …)` to `v33.0 WebApp Launcher + Teach/Auto Modes (shipped local <YYYY-MM-DD>)` and remove the bold-active formatting.
  - Phase 100 entry near line 446-473: append `- [x] PHASE-SUMMARY.md (closed <YYYY-MM-DD>)` and `Status: Shipped`.

STATE.md flip targets:
  - `last_shipped_milestone: v33.0 WebApp Launcher + Teach/Auto Modes`
  - `current_milestone: <next>` (or note that no active milestone follows; user invokes /gsd-new-milestone for v34).
  - Phase 100 status row added: `Phase 100 — SHIPPED (PASS, <YYYY-MM-DD>)`.

UAT-CHECKLIST.md row template (append a Phase 100 section):
  ```markdown
  ## Phase 100 — Multi-Stream + Stream-Window Redesign

  | # | Criterion | Result | Evidence |
  |---|-----------|--------|----------|
  | 1 | WebApp A → port 15900, RFB handshake, Chrome visible | PASS | <user note + screenshot ref> |
  | 2 | WebApp B → port 15901, independent stream | PASS | <user note> |
  | 3 | Both stream windows render simultaneously, no cross-talk | PASS | <user note> |
  | 4 | No URL bar (only top drag-strip + close-X) | PASS | <user note> |
  | 5 | Stream area fills the window | PASS | <user note> |
  | 6 | Bottom 4-icon row visible (Chat/Teach/Watch/Auto) | PASS | <user note> |
  | 7 | Chat icon opens slide-in drawer; close returns full stream | PASS | <user note> |
  | 8 | Teach icon opens recorder UI in drawer | PASS | <user note> |
  | 9 | Bytebot Auto in WebApp A doesn't disturb WebApp B | PASS | <user note + journal excerpt> |
  | 10 | Sacred SHA unchanged on Mini PC | PASS | git hash-object output: f3538e1d… |
  | 11 | UAT signoff documented here | PASS | this row |
  ```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to GitHub + run Mini PC update.sh (backgrounded) + verify services (with liv-memory carry-over tolerance)</name>
  <files>.planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md sections "Mini PC > Deployment" and "ZeroTier link to Mini PC unstable" — every long operation MUST be backgrounded.
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md "Mini PC SSH" section for the SSH command.
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md note "Pre-existing breakage as of 2026-04-25: liv-memory.service in restart loop because /opt/liv/packages/memory/dist/index.js never gets compiled — update.sh builds core/worker/mcp-server but NOT memory." Phase 100 inherits this carry-over.
    - .planning/phases/100-multi-stream-window-redesign/100-04-SUMMARY.md (to confirm 100-04 commits landed on master).
  </read_first>
  <action>
    Step 1 — confirm local state is clean and pushable:
    ```
    git status
    git log --oneline -n 10
    git hash-object liv/packages/core/src/sdk-agent-runner.ts
    ```
    Sacred SHA must equal `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. Working tree must be clean (else commit any pending SUMMARY).

    Step 2 — push to GitHub `utopusc/livinity-io` master:
    ```
    git push origin master
    ```
    Record the resulting `git rev-parse HEAD` in 100-05-SUMMARY.md as the deploy-source SHA.

    Step 3 — fire update.sh on the Mini PC (backgrounded):
    ```
    ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
        -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
        bruce@10.69.31.68 \
        'nohup sudo bash /opt/livos/update.sh >/tmp/p100-update.log 2>&1 & echo $!'
    ```
    Capture the printed PID. ZeroTier may drop while this runs (~3-6 min). Do NOT hold the SSH session open.

    Step 4 — poll completion (single-shot SSH every ~30s; max 10 attempts; do not chain sleeps locally — use the Bash tool's run_in_background parameter for the polling loop):
    ```
    ssh ... 'ps -p <PID> >/dev/null 2>&1 && echo RUNNING || echo DONE; tail -15 /tmp/p100-update.log'
    ```
    Wait until `DONE`.

    Step 5 — verify services + deployed SHA + sacred SHA on Mini PC (one batched SSH call to avoid fail2ban). The batch ALSO classifies any liv-memory failure as carry-over vs. investigate:
    ```
    ssh ... '
        echo "=== systemd ===";
        systemctl is-active livos liv-core liv-worker liv-memory;
        echo "=== liv-memory carry-over classifier ===";
        journalctl -u liv-memory --since "5 min ago" --no-pager | grep -q "Cannot find module .*memory/dist/index.js" && echo CARRY-OVER || echo INVESTIGATE;
        echo "=== deployed SHA ===";
        cat /opt/livos/.deployed-sha 2>/dev/null || git -C /opt/livos rev-parse HEAD 2>/dev/null;
        echo "=== sacred SHA ===";
        git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts;
        echo "=== last 30 livos log lines ===";
        sudo journalctl -u livos --since "5 minutes ago" --no-pager | tail -30;
    '
    ```

    Acceptance:
    - The 3 REQUIRED services (livos, liv-core, liv-worker) report `active`.
    - liv-memory: if `active` → great. If inactive AND classifier prints `CARRY-OVER` → document as pre-existing breakage carry-over (memory.dist build gap) and PROCEED. If inactive AND classifier prints `INVESTIGATE` → ABORT.
    - Deployed SHA equals the local `git rev-parse HEAD` from Step 2.
    - Sacred SHA equals `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    - No ERROR / FATAL lines in the livos log tail (warnings tolerated; document them).

    If any REQUIRED service (livos / liv-core / liv-worker) is `inactive` or `failed`, OR if liv-memory's classifier returns `INVESTIGATE`:
    - `ssh ... 'sudo journalctl -u <service> --since "5 minutes ago" --no-pager | tail -50'`
    - Do NOT proceed to UAT. Capture the failure in 100-05-SUMMARY.md as a deploy-blocker, escalate to user.

    Begin writing 100-05-SUMMARY.md with the "Deploy" section now (UAT section comes after Task 2). Include a `## liv-memory carry-over` sub-section recording either "active" or "inactive — CARRY-OVER (pre-existing breakage carry-over: memory.dist build gap)".
  </action>
  <verify>
    <automated>git rev-parse HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin master` succeeded; `git status` shows "up to date with origin/master".
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (local).
    - update.sh completed (PID no longer running on Mini PC).
    - `systemctl is-active livos liv-core liv-worker` returns 3 lines all `active` (these 3 are required for Phase 100).
    - **liv-memory.service is OPTIONAL** — if inactive AND `journalctl -u liv-memory --since '5 min ago'` shows the known build-omission signature (`Cannot find module '/opt/liv/packages/memory/dist/index.js'`), document in 100-05-SUMMARY.md as 'pre-existing breakage carry-over (memory.dist build gap)' and PROCEED. If liv-memory is inactive for ANY OTHER reason, abort and investigate.
    - Deployed SHA on Mini PC matches local HEAD.
    - Mini PC `git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (deployed sacred file unchanged).
    - 100-05-SUMMARY.md "Deploy" section written with: deploy-source SHA, deployed SHA, all 4 service statuses (3 required + liv-memory carry-over disposition), sacred SHA pre/post, last log tail.
  </acceptance_criteria>
  <done>
    Mini PC is on the Phase 100 build, all REQUIRED services green, liv-memory carry-over classified, sacred SHA preserved on disk.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-walked UAT — 11 success criteria from 100-CONTEXT.md</name>
  <files>.planning/phases/98-uat-polish/UAT-CHECKLIST.md, .planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md `<success_criteria>` section (11 items).
    - .planning/phases/98-uat-polish/UAT-CHECKLIST.md (current state — append a Phase 100 section, do NOT overwrite Phase 98 sections).
    - .planning/phases/100-multi-stream-window-redesign/100-04-SUMMARY.md "Carryover for 100-05" — note the layout contract (pb-9 + z-20 overlay; no fallback).
  </read_first>
  <what-built>
    The user opens https://bruce.livinity.io/ on a desktop browser (or sits at the Mini PC monitor) and walks through the 11 criteria below. Each is checked PASS or FAIL with a one-line note + screenshot reference where useful. The executor narrates each step; the user reports results.
  </what-built>
  <action>
    Append a `## Phase 100 — Multi-Stream + Stream-Window Redesign` section to `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` using the template in `<interfaces>`. Walk each row with the user:

    **Setup (one-time):** the user logs into the Mini PC desktop (https://bruce.livinity.io or local monitor), confirms there are at least 2 WebApps registered (e.g. duckduckgo.com + example.com); if not, right-click desktop → Add WebApp twice.

    **Row 1 — WebApp A → port 15900, RFB handshake, Chrome visible:**
    - User clicks WebApp A icon on desktop.
    - Stream window opens; user sees the captured Chrome window content.
    - On Mini PC: `ssh ... 'sudo journalctl -u livos --since "1 minute ago" --no-pager | grep -E "(rfbport|streamId)"' ` should show port 15900 (or first allocation in 15900..16099 ring).
    - Record PASS/FAIL.

    **Row 2 — WebApp B → port 15901, independent stream:**
    - User clicks WebApp B icon (different URL).
    - Second stream window opens; visually different captured page.
    - Verify port: `journalctl ... | grep rfbport` shows 15901 (or N+1 of row 1).
    - **CRITICAL CHECK — V33-MULTI-01 verification:** if both windows show the same content, the multi-stream fix did NOT work — STOP UAT, mark FAIL, escalate to user. Re-investigate via 100-01 probe procedure.
    - Record PASS/FAIL.

    **Row 3 — Both render simultaneously, no cross-talk:**
    - User moves mouse + clicks in WebApp A's stream; verify pointer + clicks affect ONLY WebApp A's captured Chrome.
    - User moves mouse + clicks in WebApp B's stream; verify ONLY WebApp B reacts.
    - Record PASS/FAIL.

    **Row 4 — No URL bar (only top drag-strip + close-X):**
    - User visually inspects each stream window. The only chrome above the stream pane should be the standard window-chrome.tsx top strip with the X close button.
    - Record PASS/FAIL.

    **Row 5 — Stream area fills the window:**
    - User confirms the stream pane reaches all the way to the bottom (above the 36px bottom action-bar).
    - The Plan A layout reserves 36px (pb-9) so the stream pixels never sit beneath the overlay bar — verify visually that no stream content is obscured.
    - If occlusion occurs: STOP, mark FAIL, escalate (per 100-04 "no fallback layout" contract — do NOT silently swap layouts).
    - Record PASS/FAIL.

    **Row 6 — Bottom 4-icon row visible:**
    - User sees 4 icons at bottom: chat-bubble (Chat), graduation-cap (Teach), eye (Watch), bot-head (Auto). Tooltips on hover show labels.
    - Record PASS/FAIL.

    **Row 7 — Chat icon opens slide-in drawer; close returns full stream:**
    - User clicks Chat icon. Drawer slides in from right at ~35% window width. v32 chat surface visible (input + message thread placeholder).
    - User types a message, sends. Confirm message round-trips through useWebAppAgent (some response or "sent" indicator).
    - User clicks Chat icon again. Drawer closes; stream is full-bleed again.
    - Record PASS/FAIL.

    **Row 8 — Teach icon opens recorder UI in drawer:**
    - User clicks Teach icon. Drawer opens; user sees skills sidebar + replay scrubber + Record/Stop button.
    - User clicks Record. Status changes; recording overlay appears on stream (TeachRecordingOverlay from existing wiring).
    - User clicks Stop. Save dialog appears (existing 96-* wiring).
    - User cancels save (or saves a 0-action skill, doesn't matter).
    - User clicks Teach icon again to close drawer.
    - Record PASS/FAIL.

    **Row 9 — Bytebot Auto in WebApp A doesn't disturb WebApp B:**
    - User clicks Auto icon in WebApp A's drawer.
    - User instructs "click somewhere harmless" via the Auto drawer or Chat drawer.
    - User watches WebApp B simultaneously — its captured Chrome should NOT change cursor/click activity.
    - On Mini PC: `journalctl -u livos | grep BYTEBOT_TARGET_WINDOW_ID` should show distinct wid values per spawn.
    - Record PASS/FAIL.

    **Row 10 — Sacred SHA unchanged on Mini PC:**
    - `ssh ... 'git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts'` returns `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    - Record literal output. PASS only if exact match.

    **Row 11 — UAT signoff:**
    - User types "approved" or "approved with notes: <details>" in the resume signal.
    - Record PASS only if rows 1-10 all PASS and user explicitly approved.

    Update both `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` (append section) and `.planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md` (full UAT log) as the walk progresses.

    On any FAIL: stop, write the failure into 100-05-SUMMARY.md, do NOT proceed to Task 3 (the milestone flip). The user decides whether to (a) hot-fix in a follow-up plan, (b) revert the deploy, or (c) accept the FAIL with a tracked carry-over.
  </action>
  <how-to-verify>
    1. Open `.planning/phases/98-uat-polish/UAT-CHECKLIST.md`. The new "Phase 100 — Multi-Stream + Stream-Window Redesign" section exists with 11 rows, each marked PASS or FAIL with concrete evidence.
    2. Open `.planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md`. The "UAT" section mirrors the 11 rows with full transcripts.
    3. User's "approved" line exists at the resume signal.
    4. If all 11 rows are PASS: proceed to Task 3.
    5. If any FAIL: Task 3 is BLOCKED — milestone does NOT flip; user decides next step.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 100 UAT 11/11 PASS" or describe failures (which row, what observed, recommended next step)</resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` contains the literal section heading `## Phase 100 — Multi-Stream + Stream-Window Redesign`.
    - The section has 11 rows (Criterion 1 through Criterion 11).
    - Each row's `Result` column contains either `PASS` or `FAIL`.
    - 100-05-SUMMARY.md "UAT" section contains the same 11 rows with one-line evidence each.
    - User has typed "approved — Phase 100 UAT 11/11 PASS" on the resume signal IF all rows are PASS, OR has typed a FAIL description naming the failing rows.
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` locally.
    - Mini PC `git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (verified during Row 10).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Flip v33 milestone + close Phase 100 in ROADMAP/STATE/SUMMARY (PASS only)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md, .planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (line 11 v33 milestone row + Project-Level Milestone Index ~line 503 + Phase 100 entry ~line 446).
    - .planning/STATE.md (Current Position section + Last shipped milestone field).
    - .planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md (the in-progress SUMMARY with deploy + UAT sections).
    - $HOME/.claude/get-shit-done/templates/summary.md.
    - All 4 prior plan SUMMARYs (100-01..100-04) — to roll up into PHASE-SUMMARY.md.
  </read_first>
  <action>
    **PRECONDITION:** This task runs ONLY if Task 2 returned all 11 PASS. If any FAIL exists in UAT-CHECKLIST.md Phase 100 section, ABORT and write the abort reason into 100-05-SUMMARY.md.

    Step 1 — finalize 100-05-SUMMARY.md by appending the close section:
    ```markdown
    ## Close

    All 11 success criteria PASS. Deploy + UAT shipped <YYYY-MM-DD>.

    ### Sacred SHA timeline
    - 100-01 commits: f3538e1d811992b782a9bb057d1b7f0a0189f95f (gate hook installed; SHA preserved)
    - 100-02 commits: f3538e1d811992b782a9bb057d1b7f0a0189f95f
    - 100-03 commits: f3538e1d811992b782a9bb057d1b7f0a0189f95f
    - 100-04 commits: f3538e1d811992b782a9bb057d1b7f0a0189f95f
    - 100-05 deploy:  f3538e1d811992b782a9bb057d1b7f0a0189f95f (verified on Mini PC)

    ### v33 ship signal
    Phase 100 closes the four UAT gaps from Phase 99. v33 milestone now has all 9 phases (92-100) shipped.
    ```

    Step 2 — write `.planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md`:
    ```markdown
    # Phase 100 — PHASE-SUMMARY

    **Closed:** <YYYY-MM-DD>
    **Outcome:** PASS
    **Verified hypothesis (100-01):** H<N>
    **Backend fix (100-02):** <B1 / matcher-tighten / frontend-rewire>
    **Plans shipped:** 5 (100-01..100-05)
    **Commits:** ~12 (sacred SHA gate fired on every one)
    **Sacred SHA at every commit + at deploy:** f3538e1d811992b782a9bb057d1b7f0a0189f95f

    ## Plan-by-plan

    | Plan | Title | Status | SUMMARY |
    |------|-------|--------|---------|
    | 100-01 | Mini PC kill-gate + sacred-SHA hook | SHIPPED | 100-01-SUMMARY.md |
    | 100-02 | Backend argv fix (--app=URL) | SHIPPED | 100-02-SUMMARY.md |
    | 100-03 | Frontend full-bleed (drop URL bar) | SHIPPED | 100-03-SUMMARY.md |
    | 100-04 | Bottom action-bar + drawers | SHIPPED | 100-04-SUMMARY.md |
    | 100-05 | Mini PC deploy + UAT | SHIPPED | 100-05-SUMMARY.md |

    ## v33 close
    All 9 phases (92-100) shipped. v33 milestone is now ✅ Shipped.

    ## Carry-over (none expected)
    - Multi-tab same-WebApp coordination — out of scope per CONTEXT (deferred to v34).
    - WebRTC upgrade — out of scope per v33-DRAFT.md (deferred to v34).
    - Per-WebApp Chrome profile isolation — out of scope per D-100-SHARED-PROFILE.
    - **liv-memory.service build-omission** — pre-existing carry-over from before Phase 100 (memory.dist build gap); not introduced by this phase, not fixed by this phase.

    ## Memory updates pending
    The user (or `/gsd-cleanup`) should write a `feedback_p100_complete.md` memory note capturing:
    - The verified root cause from 100-01.
    - The sacred-SHA pre-commit hook is now live; future phases inherit it.
    - The 4-drawer pattern (`webapp-{chat,teach,watch,auto}-drawer.tsx`) is the canonical shape for WebApp panels.
    ```

    Step 3 — update ROADMAP.md:
    - Line 11: change `(CODE-COMPLETE 2026-05-08; pending Mini PC UAT signoff …)` to `(shipped local <YYYY-MM-DD> — Phases 92-100 closed; UAT PASS recorded in .planning/phases/98-uat-polish/UAT-CHECKLIST.md)`.
    - Project-Level Milestone Index (~line 503): change `**v33.0 WebApp Launcher + Teach/Auto Modes** (CODE-COMPLETE 2026-05-08 — Phases 92-98 — pending UAT)` to `v33.0 WebApp Launcher + Teach/Auto Modes (shipped local <YYYY-MM-DD>)` (drop bold-active formatting since milestone is shipped).
    - Phase 100 entry (~line 446-473): append a `**Status:** ✅ Shipped <YYYY-MM-DD> — UAT PASS` line and check off the 5 plan boxes (`- [x]` each).

    Step 4 — update STATE.md:
    - `last_updated`: ISO8601 now.
    - `progress.completed_phases`: increment by 1 (or count actual completed phases).
    - `progress.completed_plans`: increment by 5.
    - `progress.percent`: recompute.
    - Body: replace the "Current milestone" sentence with `**Last shipped milestone:** v33.0 WebApp Launcher + Teach/Auto Modes — closed <YYYY-MM-DD> (Phases 92-100 all complete; UAT PASS).`
    - Body "Current Position" section: replace with `Phase: (between milestones — run `/gsd-new-milestone` for v34 when ready)` and `Plan: n/a`.
    - Append a "Phase 100 — SHIPPED" block mirroring the existing "Phase 99 — PARTIAL-PASS" style:
      ```markdown
      ## Phase 100 — SHIPPED (<YYYY-MM-DD>)
      - **Shipped:** all 5 plans (~12 commits); pushed to GitHub; deployed to Mini PC; UAT PASS 11/11.
      - **What works:** two concurrent WebApp clicks → two independent stream windows on independent x11vnc ports; full-bleed stream; bottom 4-icon row; slide-in drawers; Bytebot per-window MCP env preserved.
      - **Sacred SHA:** f3538e1d811992b782a9bb057d1b7f0a0189f95f UNTOUCHED across all commits + deploy.
      - **v33 milestone:** ✅ Shipped (Phases 92-100 all closed).
      ```

    Step 5 — commit (sacred SHA gate auto-runs):
    ```
    git add .planning/ROADMAP.md .planning/STATE.md \
            .planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md \
            .planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md \
            .planning/phases/98-uat-polish/UAT-CHECKLIST.md
    git commit -m "$(cat <<'EOF'
    docs(100-05): close Phase 100 + flip v33 milestone ✅ Shipped

    - 11/11 UAT PASS recorded in 98-uat-polish/UAT-CHECKLIST.md
    - PHASE-SUMMARY.md + 100-05-SUMMARY.md document the deploy + walk
    - ROADMAP.md v33 row + Project-Level Milestone Index flipped
    - STATE.md current position advanced; last_shipped_milestone = v33.0
    - Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f UNTOUCHED throughout

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git push origin master
    ```
  </action>
  <verify>
    <automated>git log --oneline -n 1 && grep -c "v33.0 WebApp Launcher.*shipped\|v33\.0.*Shipped" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md` exists.
    - File `.planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md` has both `## Deploy` and `## UAT` and `## Close` sections.
    - `grep -c "v33\.0 WebApp Launcher.*shipped local" .planning/ROADMAP.md` >= 1 (at least one shipped flip in ROADMAP).
    - `grep -c "Phase 100" .planning/ROADMAP.md` >= 1.
    - `grep -c "Last shipped milestone:.*v33\.0" .planning/STATE.md` >= 1.
    - `grep -c "Phase 100 — SHIPPED" .planning/STATE.md` == 1.
    - `grep -c "## Phase 100 — Multi-Stream" .planning/phases/98-uat-polish/UAT-CHECKLIST.md` == 1.
    - `git log --oneline -n 1` shows the close commit.
    - `git status` shows working tree clean and "up to date with origin/master".
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (final post-Phase-100 verification).
  </acceptance_criteria>
  <done>
    Phase 100 closed; v33 ✅ Shipped; ROADMAP + STATE + PHASE-SUMMARY all reflect the milestone close; sacred SHA preserved end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → GitHub | git push over HTTPS (or SSH); standard auth. |
| GitHub → Mini PC | bash /opt/livos/update.sh clones from GitHub master. |
| local dev → Mini PC | contabo_master SSH key auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05-01 | Tampering | Sacred file (deployed) | mitigate | Verified on Mini PC after deploy: `git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts` == locked SHA. UAT Row 10 is the explicit gate. |
| T-100-05-02 | Denial of Service | Mini PC services post-deploy | mitigate | Task 1 verifies the 3 REQUIRED services (livos, liv-core, liv-worker) `active`. liv-memory carry-over classified separately (CARRY-OVER vs. INVESTIGATE) — only INVESTIGATE blocks. If any REQUIRED service fails OR liv-memory classifier returns INVESTIGATE, UAT does NOT proceed; user decides revert vs. hot-fix. |
| T-100-05-03 | Information Disclosure | UAT screenshots | accept | User's own WebApps; no third-party data; Mini PC is single-user. |
</threat_model>

<verification>
- The 3 REQUIRED systemd services on Mini PC report `active` post-deploy.
- liv-memory classified as either active OR carry-over (memory.dist build gap) — both are PASS dispositions; only "INVESTIGATE" blocks.
- Mini PC deployed sacred SHA == `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- All 11 UAT-CHECKLIST.md Phase 100 rows mark PASS with concrete evidence.
- ROADMAP.md v33 row + Project-Level Milestone Index reflect "shipped local <date>".
- STATE.md last_shipped_milestone == v33.0 + Phase 100 marked SHIPPED.
- PHASE-SUMMARY.md + 100-05-SUMMARY.md complete and committed.
- Working tree clean, master pushed.
- Sacred SHA preserved at HEAD AND on Mini PC.
</verification>

<success_criteria>
- v33 milestone is ✅ Shipped (the explicit goal of Phase 100 per CONTEXT line 159 V33-MULTI-05).
- The user can demonstrably open 2 concurrent WebApps on Mini PC, each with its own stream + own bottom-bar + own drawers, with no cross-talk and no URL bar.
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` is byte-identical at HEAD AND on Mini PC.
- All planning artifacts (ROADMAP / STATE / PHASE-SUMMARY / UAT-CHECKLIST) reflect the close.
</success_criteria>

<output>
After completion, the following files exist + are committed + pushed:
- `.planning/phases/100-multi-stream-window-redesign/100-05-SUMMARY.md`
- `.planning/phases/100-multi-stream-window-redesign/PHASE-SUMMARY.md`
- `.planning/ROADMAP.md` (updated v33 row + Project-Level Milestone Index + Phase 100 entry)
- `.planning/STATE.md` (Phase 100 SHIPPED block + last_shipped_milestone advanced)
- `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` (Phase 100 section appended with 11 PASS rows)
</output>
</content>
