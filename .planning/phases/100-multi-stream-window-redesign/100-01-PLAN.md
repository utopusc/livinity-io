---
phase: 100-multi-stream-window-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md
  - .planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md
  - scripts/check-sacred.sh
  - .husky/pre-commit
autonomous: false
requirements_addressed:
  - V33-MULTI-01
summary: >-
  Mini PC live-verification kill-gate. Empirically pin which of H1..H4
  (Chrome --new-window IPC merge / xdotool matcher race / frontend single-render /
  wid-collision-via-merge) is the real cause of the multi-stream concurrency
  bug. NO code edits to product surfaces. Output: a single sentence in
  100-01-SUMMARY.md naming the verified root cause + which fix path 100-02
  must take. Also installs the project-wide sacred-SHA pre-commit hook so
  every subsequent Phase 100 commit is automatically gated against
  liv/packages/core/src/sdk-agent-runner.ts SHA f3538e1d…
must_haves:
  truths:
    - "Mini PC empirical evidence (xdotool wid count + xprop _NET_WM_PID + LivOS server log scrape) recorded in 100-01-SUMMARY.md."
    - "One of H1, H2, H3, H4 is named as the verified root cause; others marked falsified."
    - "Recommendation in 100-01-SUMMARY.md tells 100-02 exactly which fix to apply (B1 / matcher tightening / frontend fix)."
    - "Sacred-SHA pre-commit hook is installed and refuses commits when liv/packages/core/src/sdk-agent-runner.ts SHA != f3538e1d811992b782a9bb057d1b7f0a0189f95f."
    - "scripts/check-sacred.sh exists and exits non-zero on SHA drift; is executable."
  artifacts:
    - path: ".planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md"
      provides: "Root-cause finding + 100-02 fix recommendation"
    - path: "scripts/check-sacred.sh"
      provides: "Sacred SHA gate helper script"
    - path: ".husky/pre-commit"
      provides: "Project-wide pre-commit hook invoking check-sacred.sh"
  key_links:
    - from: ".husky/pre-commit"
      to: "scripts/check-sacred.sh"
      via: "shell call"
      pattern: "scripts/check-sacred\\.sh"
    - from: "scripts/check-sacred.sh"
      to: "liv/packages/core/src/sdk-agent-runner.ts"
      via: "git hash-object"
      pattern: "git hash-object .*sdk-agent-runner\\.ts"
---

<objective>
Empirically pin the multi-stream root cause on the Mini PC before any code change ships (D-100-LIVE-VERIFY-FIRST). Test all four CONTEXT G-100-A hypotheses (H1: Chrome --new-window IPC merge; H2: xdotool title-matcher race; H3: frontend single-render bug; H4: wid-collision via merge) by running probe commands on bruce@10.69.31.68 against the deployed Phase 99 build, then write a single-sentence root-cause finding into 100-01-SUMMARY.md so 100-02 can apply the locked fix without ambiguity.

Also bootstrap the sacred-SHA pre-commit hook (no existing .husky/ today per PATTERNS.md "No Analog Found" — Sacred SHA Gate row) so Phase 100's remaining 4 plans cannot accidentally edit liv/packages/core/src/sdk-agent-runner.ts.

Purpose: Without empirical verification, 100-02 risks shipping the wrong fix (e.g., changing Chrome argv when the actual bug is in window-discovery.ts:findNewWindowMatching, or vice-versa). CONTEXT D-100-LIVE-VERIFY-FIRST is non-negotiable.

Output: 100-01-SUMMARY.md (verified root cause + fix recommendation) + .husky/pre-commit + scripts/check-sacred.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md
@.planning/phases/100-multi-stream-window-redesign/100-PATTERNS.md

<interfaces>
<!-- Key facts the executor needs without exploring the codebase. -->

Mini PC SSH (per memory reference_minipc_ssh.md):
  ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
      -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
      bruce@10.69.31.68

ZeroTier link is unstable (memory reference_zerotier_unstable.md): ANY Mini PC
operation expected to take >30s MUST run with `nohup … >/tmp/log 2>&1 &` and
poll for completion via short follow-up SSH calls. Never hold a foreground SSH
session for the full duration.

Current Chrome spawn (livos/packages/livinityd/source/modules/webapps/window-manager.ts:222-237):
  sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority \
    google-chrome --user-data-dir=/home/bruce/.config/livos-chrome \
    --new-window <url>

Current matcher (livos/packages/livinityd/source/modules/webapps/window-discovery.ts:152-193):
  poll loop: xdotool search --name <hostname>; falls back at 60% elapsed to
  "any new window with chrome/firefox in title".

Sacred SHA constant (D-100-SACRED): f3538e1d811992b782a9bb057d1b7f0a0189f95f
Verify via: git hash-object liv/packages/core/src/sdk-agent-runner.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sacred-SHA pre-commit hook (D-100-SACRED enforcement)</name>
  <files>scripts/check-sacred.sh, .husky/pre-commit, package.json</files>
  <read_first>
    - .planning/phases/100-multi-stream-window-redesign/100-PATTERNS.md (section "Sacred SHA Gate (NO EXISTING HOOK)" + "No Analog Found" table) so the executor knows there is currently no .husky/ directory and no existing hook to merge with.
    - package.json (project root) to check whether husky is already a devDependency or whether the hook can be a plain shell-script gitHooksPath fallback.
    - liv/packages/core/src/sdk-agent-runner.ts (do NOT modify — only confirm path exists).
  </read_first>
  <action>
    Create the sacred-SHA gate as a hand-rolled `core.hooksPath` setup (no husky dependency required — works on Windows PowerShell shell + bash):

    1. Write `scripts/check-sacred.sh` (executable, POSIX-sh-only, no bashisms beyond `[ ]`):

       ```sh
       #!/usr/bin/env sh
       # D-100-SACRED gate: refuse any commit that mutates
       # liv/packages/core/src/sdk-agent-runner.ts away from the locked SHA.
       set -e
       LOCKED_SHA="f3538e1d811992b782a9bb057d1b7f0a0189f95f"
       SACRED_FILE="liv/packages/core/src/sdk-agent-runner.ts"
       if [ ! -f "$SACRED_FILE" ]; then
         echo "[sacred-sha] ERROR: $SACRED_FILE not found" >&2
         exit 1
       fi
       ACTUAL_SHA="$(git hash-object "$SACRED_FILE")"
       if [ "$ACTUAL_SHA" != "$LOCKED_SHA" ]; then
         echo "[sacred-sha] ABORT: D-100-SACRED violated." >&2
         echo "[sacred-sha]   expected: $LOCKED_SHA" >&2
         echo "[sacred-sha]   actual:   $ACTUAL_SHA" >&2
         echo "[sacred-sha]   file:     $SACRED_FILE" >&2
         exit 1
       fi
       exit 0
       ```

    2. Write `.husky/pre-commit` (creating .husky/ if absent — note: this is the conventional path husky uses, but we wire it via `core.hooksPath` so husky itself is NOT required):

       ```sh
       #!/usr/bin/env sh
       exec sh "$(git rev-parse --show-toplevel)/scripts/check-sacred.sh"
       ```

    3. Run once locally to wire the hooks dir (this is a developer-side setup — record the command in the SUMMARY but do NOT modify .git/config in CI):

       ```
       git config core.hooksPath .husky
       ```

    4. Make scripts/check-sacred.sh executable. On Windows the bit is recorded in the index via `git update-index --chmod=+x scripts/check-sacred.sh` and `git update-index --chmod=+x .husky/pre-commit`.

    5. Sanity-test by running `sh scripts/check-sacred.sh` directly — it must print nothing and exit 0. Then temporarily corrupt the file path (e.g., `LOCKED_SHA="0000…"`), run again, confirm non-zero exit + the ABORT message; revert.

    6. Commit ONLY scripts/check-sacred.sh + .husky/pre-commit (do NOT modify package.json unless husky was already a dep — per D-NO-NEW-DEPS we keep this dep-free).

    Use the Write tool, not heredoc.
  </action>
  <verify>
    <automated>sh scripts/check-sacred.sh; echo $?</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/check-sacred.sh exists.
    - File .husky/pre-commit exists.
    - `sh scripts/check-sacred.sh` exits 0 (output: nothing) when liv/packages/core/src/sdk-agent-runner.ts hashes to f3538e1d811992b782a9bb057d1b7f0a0189f95f.
    - `git ls-files --stage scripts/check-sacred.sh | grep -q "^100755"` (executable bit set in the git index).
    - `git ls-files --stage .husky/pre-commit | grep -q "^100755"` (executable bit set in the git index).
    - Sacred SHA pre/post: `[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ]` (true before AND after this task's commit).
  </acceptance_criteria>
  <done>
    Hook is installed, gate fires on SHA drift, the sacred file's SHA is byte-identical to the locked value at HEAD.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mini PC empirical probe — verify which of H1..H4 is the real root cause</name>
  <files>.planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md (entire `<gray_areas>` G-100-A table, so the executor runs the exact probes the gray-area lists).
    - livos/packages/livinityd/source/modules/webapps/window-manager.ts (lines 220-238 spawn block + lines 363-366 windowkill, so the executor knows what it's testing against).
    - livos/packages/livinityd/source/modules/webapps/window-discovery.ts (lines 152-193 findNewWindowMatching poll loop + lines 217-244 getWindowGeometry — the matcher under test).
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md sections "Mini PC SSH" + "ZeroTier link to Mini PC unstable" (memory `reference_zerotier_unstable.md`) — every long Mini PC operation MUST be backgrounded with `nohup … >log 2>&1 &`.
  </read_first>
  <what-built>
    Live empirical verification on bruce@10.69.31.68 of which root cause produces the single-stream-only symptom (Phase 99 G-99-UAT-1). Two probes run sequentially; results are tabulated against the four hypotheses; one hypothesis is named as VERIFIED, others as FALSIFIED, in 100-01-SUMMARY.md.
  </what-built>
  <action>
    SSH session is user-walked (autonomous: false). The executor narrates each probe; the user runs them OR confirms the executor may run them (the executor has the SSH key per memory). Use `nohup … >/tmp/log 2>&1 &` for any command expected to take >30s; otherwise inline is fine.

    SSH command (one-shot batch — see memory `feedback_ssh_rate_limit.md`):
    ```
    ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
        -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
        bruce@10.69.31.68 '<batch>'
    ```

    Probe sequence (run as one SSH batch):

    **Pre-flight:**
    1. `sudo -n -u bruce pgrep -af google-chrome` — note any baseline Chrome processes; record count.
    2. `xdotool search --class chrome 2>/dev/null | wc -l` — record baseline wid count.
    3. `git -C /opt/livos/livos rev-parse HEAD 2>/dev/null || cat /opt/livos/livos/.deployed-sha 2>/dev/null` — confirm deployed SHA matches `cd6f442a` (Phase 99 ship).

    **Probe A — current spawn (--new-window) twice:**
    4. Spawn first WebApp via the LivOS UI (user clicks WebApp A icon on desktop). Wait 3s.
    5. `sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority xdotool search --class chrome 2>&1 | tee /tmp/probe-A-after-1.txt` — count wids.
    6. For each wid: `sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority xprop -id <wid> _NET_WM_PID WM_NAME` — record (pid, name) tuples.
    7. Spawn second WebApp via the LivOS UI (user clicks WebApp B icon, different URL). Wait 3s.
    8. Repeat #5+#6 → tee to `/tmp/probe-A-after-2.txt`.
    9. `sudo journalctl -u livos --since '2 minutes ago' --no-pager | grep -E '(window-manager|window-discovery|findNewWindowMatching|stream-manager)' | tee /tmp/probe-A-livos.log`.

    **Probe B — manual --app= twice (compares H1):**
    10. Kill all Chromes: `sudo -n -u bruce pkill -f "google-chrome.*livos-chrome" || true`. Wait 2s.
    11. ```
        sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority \
          nohup google-chrome --user-data-dir=/home/bruce/.config/livos-chrome \
          --app=https://duckduckgo.com >/tmp/chrome-app-1.log 2>&1 &
        ```
        Wait 4s.
    12. `sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority xdotool search --class chrome 2>&1 | tee /tmp/probe-B-after-1.txt`.
    13. ```
        sudo -n -u bruce DISPLAY=:0 XAUTHORITY=/run/user/1000/gdm/Xauthority \
          nohup google-chrome --user-data-dir=/home/bruce/.config/livos-chrome \
          --app=https://example.com >/tmp/chrome-app-2.log 2>&1 &
        ```
        Wait 4s.
    14. Repeat #12 → tee to `/tmp/probe-B-after-2.txt`.

    **Decision matrix:**

    | Observation | Verdict |
    |---|---|
    | Probe A step 8 wid count == step 5 wid count (no new wid for WebApp B) | **H1 VERIFIED** (--new-window IPC merge). Fix = B1 (`--app=URL`). |
    | Probe A wids increase by 1 but stream-manager startStream NOT called twice in livos.log | **H3 VERIFIED** (frontend single-render). Fix = frontend rewire. |
    | Probe A wids increase, stream-manager called twice, but findNewWindowMatching returns same wid both times | **H2 or H4 VERIFIED**. Fix = matcher tightening (xprop _NET_WM_PID + creation-time filter) OR B1. |
    | Probe B wid count increases by 1 each invocation | Confirms B1 produces independent windows; even if H3 is the real cause, B1 still solves G-99-UAT-2/3 for free. |

    **Write `100-01-SUMMARY.md`** with the following exact structure:

    ```markdown
    # Phase 100-01 SUMMARY

    **Date:** 2026-05-08 (or actual probe date)
    **Branch / SHA at probe time:** <git rev-parse HEAD>
    **Mini PC deployed SHA at probe time:** <from preflight #3>

    ## Root cause: H<N> VERIFIED — <one-sentence statement>

    ## 100-02 fix recommendation: <B1 | matcher-tighten | frontend-rewire>

    ## Evidence

    | Probe step | Result |
    |---|---|
    | A.5 (1st wid count) | <n> |
    | A.8 (2nd wid count) | <n> |
    | A.6 / A.8 xprop output | <pids + names> |
    | A.9 livos log excerpt | <relevant lines> |
    | B.12 (1st --app wid count) | <n> |
    | B.14 (2nd --app wid count) | <n> |

    ## Hypothesis verdicts
    - H1 (--new-window IPC merge): VERIFIED | FALSIFIED — <reason>
    - H2 (matcher race):           VERIFIED | FALSIFIED — <reason>
    - H3 (frontend single-render): VERIFIED | FALSIFIED — <reason>
    - H4 (wid collision via merge): VERIFIED | FALSIFIED — <reason>

    ## Sacred SHA pre-probe / post-probe
    pre:  $(git hash-object liv/packages/core/src/sdk-agent-runner.ts) == f3538e1d811992b782a9bb057d1b7f0a0189f95f
    post: $(git hash-object liv/packages/core/src/sdk-agent-runner.ts) == f3538e1d811992b782a9bb057d1b7f0a0189f95f

    ## CONTEXT.md update
    The G-100-A table in 100-CONTEXT.md is amended at the end with the verified hypothesis row.
    ```

    Also update 100-CONTEXT.md `<gray_areas>` G-100-A table with a final row noting the verified hypothesis (single line, e.g. `**Verified 2026-05-08:** H1 — Chrome merges; B1 fix locked.`).

    Cleanup: `sudo -n -u bruce pkill -f "google-chrome.*livos-chrome" || true` (so the next probe / 100-02 deploy starts clean).
  </action>
  <how-to-verify>
    1. Open `100-01-SUMMARY.md`. The `## Root cause: H<N>` heading must name a single verified hypothesis.
    2. The `## 100-02 fix recommendation` line must name exactly one of: `B1`, `matcher-tighten`, `frontend-rewire`.
    3. The `## Hypothesis verdicts` section must classify ALL four (H1, H2, H3, H4) as VERIFIED or FALSIFIED with a 1-line reason.
    4. `## Evidence` table must show concrete numbers from the probe logs (not placeholders).
    5. Re-run on Mini PC: `git hash-object /opt/livos/liv/packages/core/src/sdk-agent-runner.ts` returns `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (deployed file unchanged — gate verified).
    6. 100-CONTEXT.md G-100-A table has been amended with a "Verified" row at the bottom.
    7. User confirms with "approved" or describes any anomalies (e.g., probe inconclusive — escalate to a follow-up probe before unblocking 100-02).
  </how-to-verify>
  <resume-signal>Type "approved — root cause H&lt;N&gt; verified, 100-02 fix path = &lt;B1|matcher-tighten|frontend-rewire&gt;" or describe issues</resume-signal>
  <acceptance_criteria>
    - File 100-01-SUMMARY.md exists at .planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md.
    - `grep -E '^## Root cause: H[1-4] VERIFIED' .planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md` matches exactly one line.
    - `grep -E '^## 100-02 fix recommendation: (B1|matcher-tighten|frontend-rewire)' .planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md` matches exactly one line.
    - `grep -cE '^- H[1-4] \(.+\): (VERIFIED|FALSIFIED)' .planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md` == 4.
    - 100-CONTEXT.md contains `Verified 2026-` as a row in the G-100-A table.
    - Sacred SHA gate passes: `sh scripts/check-sacred.sh` exits 0.
    - User has typed an "approved" line on the resume signal that names the same H<N> + fix path written in the SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → Mini PC SSH | contabo_master key authenticates root-equivalent (`bruce` user with `sudo -n -u bruce`) operations. |
| Mini PC bash → host kernel | xdotool / xprop / sudo / google-chrome operate on the live X11 session of the only Mini PC user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-01 | Tampering | Sacred file `liv/packages/core/src/sdk-agent-runner.ts` | mitigate | scripts/check-sacred.sh + .husky/pre-commit reject any commit when SHA != `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (D-100-SACRED). Pre/post-commit gate. |
| T-100-01-02 | Information Disclosure | Probe logs `/tmp/probe-*.txt` on Mini PC | accept | Logs are non-secret (X11 wid counts + xprop pid/name); Mini PC is single-user. Cleanup with `rm /tmp/probe-* /tmp/chrome-app-*.log` is recommended but not required. |
| T-100-01-03 | Denial of Service | `pkill -f "google-chrome.*livos-chrome"` in cleanup | accept | Cleanup is bounded to Chrome processes spawned with the LivOS profile dir; user is mid-probe so the disruption is intended. |
</threat_model>

<verification>
- 100-01-SUMMARY.md exists, has the four required sections, classifies all 4 hypotheses, names the fix path.
- 100-CONTEXT.md G-100-A table has the "Verified" amendment row.
- Sacred-SHA pre-commit hook is installed and rejects SHA-drift commits (manual test: temporarily corrupt LOCKED_SHA value in scripts/check-sacred.sh → commit attempt fails → revert).
- `git hash-object liv/packages/core/src/sdk-agent-runner.ts` is byte-identical to `f3538e1d811992b782a9bb057d1b7f0a0189f95f` at this plan's HEAD.
- `sh scripts/check-sacred.sh; echo $?` prints `0`.
</verification>

<success_criteria>
- One hypothesis (H1, H2, H3, or H4) named as VERIFIED with empirical evidence.
- 100-02 has an unambiguous fix-path recommendation (B1, matcher-tighten, or frontend-rewire).
- Sacred-SHA pre-commit hook is in place for the rest of Phase 100.
- All commits in this plan preserve `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
</success_criteria>

<output>
After completion, create `.planning/phases/100-multi-stream-window-redesign/100-01-SUMMARY.md` per the template above. Also amend `.planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md` G-100-A table with one verification row.
</output>
