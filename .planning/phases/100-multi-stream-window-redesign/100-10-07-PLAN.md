---
plan_id: 100-10-07
phase: 100-10
wave: 4
depends_on: [100-10-01, 100-10-02, 100-10-03, 100-10-04, 100-10-05, 100-10-06]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md
autonomous: false
requirements: [V33-MULTI-01, V33-MULTI-02, V33-MULTI-03, V33-MULTI-04, V33-MULTI-05]
goal: User-walked Mini PC deploy (`bash /opt/livos/update.sh`) + 15-row UAT walk per CONTEXT success criteria + ROADMAP/STATE flip to mark 100-10 complete (and v33 milestone closed if 99-* and earlier 100-* UATs are also green).
---

# Plan 100-10-07: Mini PC deploy + 15-row UAT walk

<objective>
Plans 100-10-01 through 100-10-06 are CODE-COMPLETE on the dev workstation. Nothing has been deployed to the Mini PC (`bruce@10.69.31.68`) yet. This plan is the **user-walked** wave — Claude triggers the deploy, then the user runs through the 15-row UAT table from `100-10-CONTEXT.md` (lines 343-361) on the Mini PC, marking each row PASS / FAIL. After all rows green, Claude flips ROADMAP/STATE to mark 100-10 complete; if any row fails, Claude opens hot-fix sub-plans (100-10-08+) before flipping anything.

This is the ONLY plan in 100-10 with `autonomous: false` — it requires the user at the keyboard to verify visual / functional behavior on the live Mini PC. The other 6 plans are autonomous and run via /gsd-execute-phase before this one.

D-100-NO-SERVER4 carries forward — Mini PC ONLY. Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` MUST match on the Mini PC's `/opt/liv/packages/core/src/sdk-agent-runner.ts` after deploy (deploy is rsync — sacred file SHA is a deploy invariant).
</objective>

<must_haves>
- Mini PC deploy executed via `bash /opt/livos/update.sh` (the only sanctioned deploy command per memory `feedback_update_sh_drift.md` + project commands).
- All 4 systemd services (`livos`, `liv-core`, `liv-worker`, `liv-memory`) restarted and `active (running)` after deploy.
- Sacred SHA on Mini PC: `git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts` equals `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- 15-row UAT-CHECKLIST.md walked with the user; each row marked PASS/FAIL with evidence (command output, screenshot reference, or live observation).
- ROADMAP.md updated to reflect 100-10 complete (and v33 milestone closed if appropriate).
- STATE.md updated to record the new milestone position.
- If any UAT row FAILS, a `100-10-08+` follow-up sub-plan is opened (or 100-10 stays open with the failure documented) — NEVER mark complete with red rows.
</must_haves>

## Tasks

### Task 1: Pre-deploy checks (LOCAL)

<read_first>
- `.planning/phases/100-multi-stream-window-redesign/100-10-01-SUMMARY.md` through `100-10-06-SUMMARY.md` (verify all 6 plans landed)
- `.planning/STATE.md` (current position)
- `.husky/pre-commit` + `scripts/check-sacred.sh` (sanity: hook is live)
- Memory: `reference_minipc_ssh.md` (SSH connection cmd for Mini PC)
</read_first>

<action>
**Step 1.** Verify all 6 prior plan SUMMARYs exist locally:
```bash
ls .planning/phases/100-multi-stream-window-redesign/100-10-0{1,2,3,4,5,6}-SUMMARY.md
```
All 6 must list. If any missing, the corresponding plan didn't ship — go back to that plan's executor before deploying.

**Step 2.** Verify sacred SHA still matches LOCALLY:
```bash
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# MUST be f3538e1d811992b782a9bb057d1b7f0a0189f95f
```

**Step 3.** Verify git tree is clean:
```bash
git status -sb
git log --oneline -10
```
Confirm the last 10+ commits are from 100-10-01..06 (no uncommitted work).

**Step 4.** Push to origin so Mini PC's `update.sh` can pull:
```bash
git push origin master
```
(Per memory `update.sh` clones from `utopusc/livinity-io` — push is required for the deploy.)
</action>

<acceptance_criteria>
- [ ] `ls .planning/phases/100-multi-stream-window-redesign/100-10-0{1,2,3,4,5,6}-SUMMARY.md 2>&1 | wc -l` returns 6.
- [ ] `git hash-object liv/packages/core/src/sdk-agent-runner.ts` equals `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- [ ] `git status -sb` shows working tree clean (no `M ` or `??` lines for code files).
- [ ] `git push origin master` succeeded (last commits visible on `https://github.com/utopusc/livinity-io/commits/master`).
</acceptance_criteria>

### Task 2: Mini PC deploy (USER-WALKED — Claude runs the SSH commands; user is at the keyboard)

<read_first>
- Memory: `reference_minipc_ssh.md` (`ssh -i .../minipc bruce@10.69.31.68`)
- Memory: `feedback_ssh_rate_limit.md` (batch commands into ONE ssh invocation; fail2ban bans rapid probes)
- Memory: `reference_zerotier_unstable.md` (detach long Mini PC ops via `nohup` + log + background poll; never hold foreground SSH for >30s)
- Memory: `feedback_update_sh_drift.md` (use update.sh — do NOT manually rsync)
- Memory: `reference_minipc_redis.md` (capability registry prefix is `liv:cap:*`; Redis password lives in /opt/livos/.env)
</read_first>

<action>
**Step 1 — Run update.sh on Mini PC.** Use ONE batched SSH session, with the deploy detached via `nohup` per ZeroTier-instability rule:

```bash
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
    -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
    bruce@10.69.31.68 \
    'nohup bash /opt/livos/update.sh > /tmp/100-10-update.log 2>&1 & echo $!'
```

This returns the PID. The deploy proceeds in the background (~3-5 min — clones + rsync + npm install + builds + systemctl restart).

**Step 2 — Poll for completion.** Every 30s, tail the log:
```bash
ssh -i .../contabo_master bruce@10.69.31.68 'tail -50 /tmp/100-10-update.log'
```
Look for either `Deployment complete` (success) or any `error` / `failed` / non-zero exit. Per `update.sh` flow: `git clone → rsync → pnpm install → npm install → pnpm --filter @livos/config build → pnpm --filter ui build → npm run build (liv core/worker/mcp-server) → systemctl restart livos liv-core liv-worker liv-memory`.

**Step 3 — Verify all 4 services active after deploy:**
```bash
ssh ... bruce@10.69.31.68 'systemctl is-active livos liv-core liv-worker liv-memory'
```
Expected: 4 lines, each `active`. If `liv-memory` is `failed` (per memory: pre-existing breakage where update.sh doesn't build memory dist), document as a known issue and continue — UAT rows don't depend on liv-memory.

**Step 4 — Verify sacred SHA on Mini PC:**
```bash
ssh ... bruce@10.69.31.68 'cd /opt/liv && git hash-object packages/core/src/sdk-agent-runner.ts'
```
Expected: `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If not — STOP. Sacred SHA was breached by the deploy; this is a critical regression. Do not proceed to UAT until investigated.

**Step 5 — Verify pnpm-store quirk** (memory `update.sh pnpm-store quirk` — multiple `@liv+core*` dirs may exist; livinityd may import the stale one):
```bash
ssh ... bruce@10.69.31.68 'ls /opt/livos/node_modules/.pnpm/@liv+core*'
```
If multiple dirs appear, manually `cp -r /opt/liv/packages/core/dist` into the one livinityd's `node_modules/@liv/core` symlink resolves to (run `readlink` to confirm), then `systemctl restart livos`.
</action>

<acceptance_criteria>
- [ ] `tail -1 /tmp/100-10-update.log` on Mini PC contains `Deployment complete` (or equivalent success marker).
- [ ] `systemctl is-active livos liv-core liv-worker` returns `active` on each line (3 of 3 — `liv-memory` may be `failed` per known pre-existing breakage; document).
- [ ] `cd /opt/liv && git hash-object packages/core/src/sdk-agent-runner.ts` on Mini PC equals `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- [ ] `pgrep -af luse | wc -l` on Mini PC returns at least 0 (zero before any WebApp opens; will be ≥1 after UAT row 5 opens a WebApp).
- [ ] `xdpyinfo -display :10 2>/dev/null` returns non-empty output AFTER opening a WebApp (UAT row 1 verifies this; here we just confirm `xdpyinfo` binary exists).
</acceptance_criteria>

### Task 3: 15-row UAT walk (USER-AT-KEYBOARD)

<read_first>
- `.planning/phases/100-multi-stream-window-redesign/100-10-CONTEXT.md` lines 343-361 (THE 15-row table — verbatim source of truth)
- `.planning/phases/100-multi-stream-window-redesign/100-10-01-SUMMARY.md` through `100-10-06-SUMMARY.md` (what was shipped per row)
</read_first>

<action>
**Step 1.** Create `.planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md`. Header:

```markdown
# Phase 100-10 UAT Checklist

**Mini PC:** bruce@10.69.31.68
**Deploy commit:** <fill in: HEAD SHA>
**Sacred SHA:** f3538e1d811992b782a9bb057d1b7f0a0189f95f
**Date:** <fill in>

| # | Test | Pass Criteria | Result | Evidence |
|---|------|---------------|--------|----------|
| 1 | xdpyinfo -display :10 returns valid info | Per-WebApp Xvfb spawned for WebApp A | [ ] | <command output> |
| ... | ... | ... | [ ] | ... |
```

**Step 2.** Walk through each of the 15 rows from CONTEXT.md verbatim. Each row corresponds to one of D-100-10-A..G + the sacred SHA constraint. The literal table content (already locked by CONTEXT and quoted here so executors don't re-derive):

The 15 rows ARE (copy them verbatim into UAT-CHECKLIST.md):

1. `xdpyinfo -display :10` returns valid info → Per-WebApp Xvfb spawned for WebApp A.
2. Open WebApp A then WebApp B → Two Chromes on `:10` and `:11` respectively. NO overlap (different displays). NO black areas.
3. Chrome on `:10` is fullscreen-ish → `--app=URL` + window-size matches Xvfb display size (1920x1080 or whatever picked).
4. Stream area in LivOS WebApp window fills entire window → NO black border below (CSS-cover applied).
5. `pgrep -af luse` shows per-WebApp Luse MCP children → Naming reflects rename; not `bytebot` anymore.
6. `mcp__luse__list_windows` callable from chat → Returns windows on caller's display.
7. `mcp__luse__screenshot_window` callable → Returns base64 PNG of specified wid.
8. `mcp__luse__create_stream` callable → Returns new wsUrl + port. **Pre-test on Mini PC (per memory `reference_minipc_redis.md` — password lives in `/opt/livos/.env` REDIS_URL via sudo; legacy `LivRedis2024!` is STALE):**
   ```bash
   ssh ... bruce@10.69.31.68      "sudo bash -c 'source /opt/livos/.env && redis-cli -u \"\$REDIS_URL\" SET liv:config:luse_can_create_streams true'"
   ```
   Then invoke the tool from chat. Expect `{streamId, wsUrl, port}` in the tool response. Without this flag set, the handler returns `{isError: true}` with text matching `/luse_can_create_streams disabled/` per G-100-10-E privilege gate. Post-test, you may flip it back to `false` to restore production-default security.
9. Skill button visible OUTSIDE WebApp window at top-right → Click opens popover with skills.
10. Auto button gone from floating action bar → Only Chat + Teach (2 icons — Skill is OUTSIDE separately).
11. Click Chat icon → input shows. 09-08 state machine intact.
12. Type + Enter → response streams IN PLACE. Input area replaced by response area.
13. Stop button visible during streaming → Click Stop → response halts.
14. Old action_log v2 skills still replayable post-rename → Backwards-compat shim translates `mcp__bytebot__*` → `mcp__luse__*`.
15. Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNTOUCHED → `git -C /opt/liv hash-object packages/core/src/sdk-agent-runner.ts` matches.

For each row, Claude runs the verification command on the Mini PC (via SSH) when applicable (rows 1, 2, 5, 6, 7, 8, 14, 15 are command-driven; rows 3, 4, 9, 10, 11, 12, 13 require user visual confirmation in the LivOS UI). The user marks PASS/FAIL.

For rows that are command-driven, capture the literal stdout/stderr in the Evidence column.

For rows that are visual, the user types `pass` / `fail` after observing the behavior; if `fail`, the user describes what they saw.

**Step 3.** After all 15 rows walked, count PASS:

- 15/15 PASS → proceed to Task 4 (ROADMAP flip).
- 14/15 with one minor fail → user decides: open hot-fix sub-plan (Task 4 alternate) or accept as known issue.
- < 14/15 PASS → DO NOT flip ROADMAP. Open hot-fix plan(s) `100-10-08`, `100-10-09` etc.

**Step 4.** Commit the UAT-CHECKLIST.md:
```
git commit -m "docs(100-10-07): UAT-CHECKLIST <result> - <X>/15 rows pass"
```
</action>

<acceptance_criteria>
- [ ] File `.planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md` exists.
- [ ] UAT-CHECKLIST.md contains all 15 row entries (literal text from CONTEXT.md lines 343-361).
- [ ] Each row has a `Result` field marked `[x] PASS` or `[ ] FAIL` with evidence text in the Evidence column.
- [ ] Row 15 (Sacred SHA) is PASS — `git hash-object` on Mini PC matched `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- [ ] Latest commit message matches `^docs\(100-10-07\): UAT-CHECKLIST`.
- [ ] If any row is FAIL, the SUMMARY (Task 5) documents the failure and either (a) opens a follow-up plan number OR (b) flags for user decision before ROADMAP flip.
</acceptance_criteria>

### Task 4: ROADMAP + STATE flip (only if 100-10 PASSED)

<read_first>
- `.planning/ROADMAP.md` (current shape — find Phase 100 entry)
- `.planning/STATE.md` (current position)
- `.planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md` (Task 3 result — must be PASS)
</read_first>

<action>
**Conditional execution: only run this task if Task 3 ended with all 15 rows PASS (or the user explicitly accepted the partial result).**

**Step 1.** Update `.planning/ROADMAP.md`:

- Find the Phase 100 entry (the umbrella phase: "Multi-stream window redesign").
- Find sub-plan 100-10 marker (likely `[ ] 100-10 — per-WebApp Xvfb + Bytebot→Luse + Luse tools + UI polish`).
- Flip the `[ ]` to `[x]` for 100-10.
- If the entire Phase 100 (all of 100-01 through 100-10) is now complete, flip the parent Phase 100 marker to `[x]`.
- If v33 milestone (which Phase 100 closes per V33-MULTI-05) is now ship-ready, update the v33 milestone marker.

**Step 2.** Update `.planning/STATE.md`:

- Update the "Current position" line to reflect 100-10 complete.
- Append a new entry to the "Recent ships" section: `2026-05-XX: Phase 100-10 shipped — per-WebApp Xvfb + Bytebot→Luse rename + Luse window/stream tools + UI cleanup. Sacred SHA preserved. <X>/15 UAT rows pass.`

**Step 3.** Commit:
```
git commit -m "docs(state): flip 100-10 complete; v33 milestone <closed/in-progress>"
```

**Step 4.** Push to origin:
```bash
git push origin master
```
</action>

<acceptance_criteria>
- [ ] `.planning/ROADMAP.md` shows `[x] 100-10` (or whatever literal pattern the file uses to mark complete).
- [ ] `.planning/STATE.md` "Recent ships" section has a new entry mentioning 100-10 + sacred SHA preserved + UAT count.
- [ ] Latest commit message matches `^docs\(state\): flip 100-10`.
- [ ] `git push origin master` succeeded.
</acceptance_criteria>

### Task 5: Phase SUMMARY + carryover docs

<read_first>
- All 6 sub-plan SUMMARYs (100-10-01 through 100-10-06).
- `.planning/phases/100-multi-stream-window-redesign/UAT-CHECKLIST.md` (Task 3 result).
</read_first>

<action>
Create `.planning/phases/100-multi-stream-window-redesign/100-10-PHASE-SUMMARY.md` consolidating all 6 sub-plans + the UAT result:

- Frontmatter: `phase: 100-multi-stream-window-redesign`, `plan: 10-PHASE`, `subsystem: multi`, `tags: [phase-summary, per-webapp-xvfb, luse-rename, luse-tools, ui-cleanup, chat-response]`, `sacred_sha_pre/post: f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- One-liner summarizing all 8 issues addressed (Issues 1-8 from CONTEXT.md).
- Per-sub-plan diff summary (one paragraph each).
- UAT result: X/15 rows PASS.
- Sacred SHA preserved across all <N> commits.
- Sacred SHA literal `f3538e1d811992b782a9bb057d1b7f0a0189f95f` appears at least 2 times.
- Carryover for v34: list the deferred items from CONTEXT.md "Deferred Ideas" + any UAT-discovered issues that became 100-10-08+ follow-ups.

Commit:
```
git commit -m "docs(100-10): PHASE-SUMMARY - per-WebApp Xvfb + Luse rename + tools + UI cleanup shipped"
```
</action>

<acceptance_criteria>
- [ ] File `.planning/phases/100-multi-stream-window-redesign/100-10-PHASE-SUMMARY.md` exists.
- [ ] `grep -c "f3538e1d811992b782a9bb057d1b7f0a0189f95f" .planning/phases/100-multi-stream-window-redesign/100-10-PHASE-SUMMARY.md` returns at least 2.
- [ ] PHASE-SUMMARY references all 6 sub-plans (100-10-01 through 100-10-06).
- [ ] PHASE-SUMMARY references the 8 user-reported issues from CONTEXT.md.
- [ ] Latest commit message matches `^docs\(100-10\): PHASE-SUMMARY`.
- [ ] `git hash-object liv/packages/core/src/sdk-agent-runner.ts` equals `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
</acceptance_criteria>
