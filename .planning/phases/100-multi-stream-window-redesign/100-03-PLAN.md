---
phase: 100-multi-stream-window-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 100-02
files_modified:
  - livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx
  - livos/packages/ui/src/modules/window/webapp-toolbar.tsx
  - livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx
autonomous: true
requirements_addressed:
  - V33-MULTI-02
summary: >-
  Frontend: drop the top WebAppToolbar (URL bar + back/forward/refresh chord)
  and the inline ResizablePanelGroup vertical split that used to host the agent
  panel beneath the stream. Stream area becomes full-bleed (`flex-1` of a single
  column). webapp-toolbar.tsx is deleted. The stream window now exposes ONLY
  the standard top drag-strip + close-X (from window-chrome.tsx) above the
  stream pane. Test invariants are flipped to assert the absence of the dropped
  imports and to remove the persistence-key + 70/30-layout assertions.
must_haves:
  truths:
    - "Stream pane fills the visible window (no URL bar above, no inline agent panel below)."
    - "WebAppToolbar import is gone from webapp-stream-window.tsx; webapp-toolbar.tsx file is deleted."
    - "ResizablePanelGroup imports + persistence helpers (SPLIT_KEY_PREFIX, DEFAULT_TOP_PCT, DEFAULT_BOTTOM_PCT, MIN_PCT, MAX_PCT, readPersistedLayout, writePersistedLayout, initialLayout, onLayoutChange) are removed."
    - "Stream-window unit test no longer asserts on ResizablePanelGroup, persistence keys, or 70/30 + [20,90] layout guards; new invariants assert the drops are real."
    - "Spawn lifecycle (spawnedForRef, closeMutationRef, spawn idempotency / retry from 95-07.B / 99-04 / 95-08 hotfixes) is preserved byte-for-byte."
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged across all commits in this plan."
  artifacts:
    - path: "livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx"
      provides: "Full-bleed stream layout (V33-MULTI-02)"
    - path: "livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx"
      provides: "Updated source-text invariants (no toolbar / no resizable / full-bleed)"
    - path: ".planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md"
      provides: "Diff + invariant changes + sacred SHA"
  key_links:
    - from: "livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx"
      to: "stream pane (NoVncCanvas / VNC overlays)"
      via: "single flex column, stream is flex-1"
      pattern: "flex-1"
    - from: "livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx"
      to: "webapp-stream-window.tsx source text"
      via: "readFileSync + regex"
      pattern: "expect\\(SRC\\)\\.not\\.toMatch\\(/ResizablePanelGroup/"
---

<objective>
Land the V33-MULTI-02 layout change: stream area fills the window, no URL bar (the top WebAppToolbar is gone), no inline agent panel below (the ResizablePanelGroup vertical split is gone). The standard top drag-strip + close-X (provided by window-chrome.tsx, rendered by window-content.tsx) is the only chrome above the stream pane.

Purpose: With Phase 100-02's `--app=URL` Chrome shipping chromeless windows at the X11 layer, the LivOS-side URL bar is doubly redundant. Dropping it (G-100-E E1 locked default) hands the full window to the stream and aligns with the user's "stream pencere boyutu kadar" requirement.

This plan does NOT add the bottom action bar or the drawer host — those are 100-04's responsibility (file-disjoint enough that 100-03 and 100-04 can run in parallel within Wave 3, but the final-integration test in 100-04's last task validates BOTH plans together).

Output: webapp-toolbar.tsx deleted; webapp-stream-window.tsx slimmed of the toolbar + ResizablePanelGroup machinery; webapp-stream-window.unit.test.tsx invariants updated. Plus 100-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-multi-stream-window-redesign/100-CONTEXT.md
@.planning/phases/100-multi-stream-window-redesign/100-PATTERNS.md

<interfaces>
<!-- Concrete editing instructions — executor should not need to re-derive any of this. -->

LINES TO READ + KEEP (preserve byte-for-byte) in webapp-stream-window.tsx:
  - Spawn lifecycle: ~lines 132-198 (spawnedForRef, closeMutationRef, spawn idempotency + retry — 2026-05-08 hotfix v2)
  - VNC hook + agent hook + teach recorder hook usage (~ lines 132-275)
  - Stream pane body: ~lines 422-452 (NoVncCanvas + SpawnErrorBanner + VncOverlay + TeachRecordingOverlay + TeachAutoStopBanner) — these stay, dropping the wrapping ResizablePanel only.
  - Skill replay scrubber render (~lines 446-451) — preserve, but may relocate to top-level absolute overlay or to Teach drawer (left to 100-04). 100-03 only ensures it still renders without breaking the layout.

LINES TO DROP in webapp-stream-window.tsx:
  - Imports lines 54-58: `import {ResizableHandle, ResizablePanel, ResizablePanelGroup} from '@/shadcn-components/ui/resizable'` (or wherever the resizable barrel is — confirm via Read first).
  - Imports for WebAppToolbar (around line 33-50, exact line confirmed by Read).
  - Constants + helpers ~lines 67-119: `SPLIT_KEY_PREFIX`, `DEFAULT_TOP_PCT`, `DEFAULT_BOTTOM_PCT`, `MIN_PCT`, `MAX_PCT`, `readPersistedLayout`, `writePersistedLayout`, `initialLayout`.
  - `onLayoutChange` callback ~lines 366-376.
  - The JSX wrapper `<ResizablePanelGroup direction="vertical" ...>` and the two `<ResizablePanel>` children + `<ResizableHandle />` — the body of the top panel becomes the direct stream area; the bottom panel (which holds WebAppAgentPanel + WebAppModeSelector + WebAppSkillsSidebar) is removed entirely (these components relocate to 100-04 drawers).
  - JSX `<WebAppToolbar ... />` render call (currently atop the stream area).
  - The full WebAppAgentPanel inner-component implementation block ~lines 555-635 — relocated to 100-04 (`<WebAppChatDrawer>`). For 100-03, simply DELETE the block here; 100-04 picks it up from this commit's history (`git show <SHA>:webapp-stream-window.tsx`).

WRAPPER CHANGE (lines ~404-405):
  Current: `<div className='flex h-full w-full flex-row bg-surface-base'>`
  Target:  `<div className='relative flex h-full w-full flex-col bg-surface-base'>`
  Rationale: `flex-col` so a future bottom-bar (100-04) can sit at the bottom; `relative` so future absolutely-positioned children (drawer host, bottom-bar) anchor correctly. NOTE 100-04 will further edit this; 100-03 just makes the column orientation correct now.

STREAM-WRAPPER LAYOUT (locked Plan A — bottom-bar reserves space via `pb-9`):
  When wrapping the stream pane content (NoVncCanvas + overlays) in the
  `relative flex-1 min-h-0 overflow-hidden` div, ALSO add `pb-9` to that div.
  Final className: `relative flex-1 min-h-0 overflow-hidden pb-9`.
  Rationale: 100-04's bottom action-bar overlays at `absolute inset-x-0 bottom-0 z-20`
  with `h-9` (36px). Reserving `pb-9` on the stream wrapper guarantees the noVNC
  canvas pixels never sit beneath the bar — gives the bar visual prominence without
  occluding stream pixels. This is the canonical layout (Plan B / sibling-after-stream
  is REMOVED as an option). If the executor finds Plan A doesn't work on inspection,
  escalate before flipping; do NOT silently fall back.

webapp-toolbar.tsx (125 lines):
  DELETE the file. Per CONTEXT G-100-E E1, the WebAppToolbar is dropped entirely. The copy-URL action moves to a future right-click-menu task (DEFERRED — out of Phase 100 scope; the desktop right-click menu from Phase 94 already exists and can host this in a future micro-phase).

webapp-stream-window.unit.test.tsx (existing source-text invariants test):
  Per PATTERNS.md "Invariants TO REMOVE":
    - Drop the ResizablePanelGroup assertion (~line 43-44).
    - Drop the persistence key shape assertion (~line 47-48).
    - Drop the 70/30 + [20,90] guard assertions (~lines 51-55).
    - Drop the toolbar import assertion (~lines 36-39).
  Per PATTERNS.md "Invariants TO ADD" (a subset — the others are 100-04's responsibility):
    - `it('drops WebAppToolbar import (V33-MULTI-02 / G-100-E E1)') { expect(SRC).not.toMatch(/from\s+['"]\.\.\/webapp-toolbar['"]/); expect(SRC).not.toMatch(/<WebAppToolbar\b/) }`
    - `it('drops ResizablePanelGroup vertical split') { expect(SRC).not.toMatch(/ResizablePanelGroup/) }`
    - `it('uses flex-col root container (full-bleed prep for 100-04 bottom-bar)') { expect(SRC).toMatch(/flex h-full w-full flex-col/) }`
    - `it('reserves bottom space via pb-9 (Plan A locked — bottom-bar overlay)') { expect(SRC).toMatch(/pb-9/) }`

Sacred SHA (D-100-SACRED): f3538e1d811992b782a9bb057d1b7f0a0189f95f
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update unit-test invariants (RED — assertions reflect the not-yet-applied changes)</name>
  <files>livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx (full file — to see existing assertions and the readFileSync convention).
    - .planning/phases/100-multi-stream-window-redesign/100-PATTERNS.md (sections "webapp-stream-window.unit.test.tsx" — Invariants TO ADD + TO REMOVE).
    - livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx (full file — confirm current toolbar + resizable usage so the "TO REMOVE" assertions actually exist today).
  </read_first>
  <behavior>
    - After this commit, running the test suite must produce failing assertions for: "drops WebAppToolbar import" (because import still present in source), "drops ResizablePanelGroup vertical split" (still present), "uses flex-col root container" (still flex-row), and "reserves bottom space via pb-9" (no pb-9 yet).
    - All other assertions in the file must pass — i.e. the existing invariants that we are NOT removing should keep their behavior.
  </behavior>
  <action>
    Open `livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx`. Apply both edits:

    REMOVE these existing `it(...)` blocks (use Read to find exact line ranges, then Edit):
    - The `it(...)` that asserts `expect(SRC).toMatch(/ResizablePanelGroup/)` (~line 43-44 area).
    - The `it(...)` that asserts on the persistence key shape (the one matching `SPLIT_KEY_PREFIX` or `livos:webapp-stream-split:`).
    - The `it(...)` blocks that assert on the 70/30 default layout and the [20,90] min/max guards.
    - The `it(...)` that asserts on the WebAppToolbar import being PRESENT.

    ADD these new `it(...)` blocks at the end of the existing `describe(...)` block (use exact text):

    ```typescript
    it('drops WebAppToolbar import (V33-MULTI-02 / G-100-E E1)', () => {
        expect(SRC).not.toMatch(/from\s+['"]\.\.\/webapp-toolbar['"]/)
        expect(SRC).not.toMatch(/<WebAppToolbar\b/)
    })

    it('drops ResizablePanelGroup vertical split (no inline agent panel below stream)', () => {
        expect(SRC).not.toMatch(/ResizablePanelGroup/)
    })

    it('uses flex-col root container (full-bleed; 100-04 bottom-bar will anchor here)', () => {
        expect(SRC).toMatch(/flex h-full w-full flex-col/)
    })

    it('reserves bottom space via pb-9 (Plan A locked — bottom-bar overlay anchored over reserved 36px)', () => {
        // Locks the canonical bottom-bar layout: stream wrapper has pb-9 so the
        // overlay bar (absolute inset-x-0 bottom-0 z-20 h-9 from 100-04) never
        // occludes stream pixels. Failing this guard means a future edit removed
        // the bottom reservation and UAT Row 5 will surface the regression.
        expect(SRC).toMatch(/pb-9/)
    })
    ```

    Run the test: `cd livos && pnpm --filter ui test -- webapp-stream-window.unit`. Expect: 4 new tests FAIL (RED). Removed-test count is reflected (no failures from those that no longer exist). All untouched tests still PASS.

    Commit (sacred SHA gate auto-runs): `test(100-03): RED - flip stream-window invariants for full-bleed layout`
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui test -- webapp-stream-window.unit 2>&1 | grep -E "(drops WebAppToolbar|drops ResizablePanelGroup|flex-col root|pb-9|FAIL|PASS|✓|✗)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "drops WebAppToolbar import" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1.
    - `grep -c "drops ResizablePanelGroup vertical split" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1.
    - `grep -c "uses flex-col root container" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1.
    - `grep -c "reserves bottom space via pb-9" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1.
    - `grep -c "expect(SRC).toMatch(/pb-9/)" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1.
    - `grep -c "ResizablePanelGroup" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 1 (the new "not.toMatch" assertion is the only remaining occurrence).
    - `grep -c "SPLIT_KEY_PREFIX\|livos:webapp-stream-split:" livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx` == 0.
    - Running `cd livos && pnpm --filter ui test -- webapp-stream-window.unit` shows the four new `it(...)` blocks FAIL (RED expected) and zero unrelated test regressions.
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </acceptance_criteria>
  <done>
    Test invariants reflect the target source-text shape; expected RED state achieved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop top toolbar + ResizablePanelGroup; switch root to flex-col (GREEN)</name>
  <files>livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx, livos/packages/ui/src/modules/window/webapp-toolbar.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx (full file — must see exact line numbers for every removal; the file is ~776 lines).
    - livos/packages/ui/src/modules/window/webapp-toolbar.tsx (full file — confirm it has no other importers before deleting; run `grep -r "webapp-toolbar" livos/packages/ui/src/` to verify).
    - .planning/phases/100-multi-stream-window-redesign/100-PATTERNS.md (sections "Pattern Assignments" → "webapp-stream-window.tsx" + "webapp-toolbar.tsx").
    - livos/packages/ui/src/modules/window/window-content.tsx (read-only reference — it provides the top drag-strip + close-X around our content; confirm it stays unchanged).
  </read_first>
  <action>
    Step 1 — webapp-stream-window.tsx edits (use Edit tool with multiple targeted patches, NOT a full rewrite):

    1.1. Remove the import:
       ```typescript
       import {WebAppToolbar} from '../webapp-toolbar'
       ```
       (exact import path may differ — confirm via Read; remove the matching line).

    1.2. Remove the import for the resizable barrel:
       ```typescript
       import {ResizableHandle, ResizablePanel, ResizablePanelGroup} from '@/shadcn-components/ui/resizable'
       ```
       (or whatever shadcn path is in use — confirm via Read).

    1.3. Remove the constants block (lines ~67-119) — every line of `SPLIT_KEY_PREFIX`, `DEFAULT_TOP_PCT`, `DEFAULT_BOTTOM_PCT`, `MIN_PCT`, `MAX_PCT`, `readPersistedLayout`, `writePersistedLayout`, `initialLayout`. Remove their declarations and any helper types they reference.

    1.4. Remove the `onLayoutChange` callback (lines ~366-376) and any `useState` / `useMemo` it depended on.

    1.5. Edit the root JSX wrapper (lines ~404-405):
       ```typescript
       // BEFORE:
       <div className='flex h-full w-full flex-row bg-surface-base'>
       // AFTER:
       <div className='relative flex h-full w-full flex-col bg-surface-base'>
       ```

    1.6. Remove the `<WebAppToolbar ... />` JSX render (find the literal `<WebAppToolbar` and remove the whole element including any wrapping div that exists ONLY to host it — preserve siblings).

    1.7. Replace the `<ResizablePanelGroup direction='vertical' ...>` block: keep the contents of the TOP panel (the stream pane: NoVncCanvas, SpawnErrorBanner, VncOverlay, TeachRecordingOverlay, TeachAutoStopBanner, SkillReplayScrubber), wrap them in a single `<div className='relative flex-1 min-h-0 overflow-hidden pb-9'>...</div>` (the `pb-9` reserves 36px at the bottom for 100-04's overlay action-bar — Plan A locked layout), and DELETE the BOTTOM panel entirely (which contained `<WebAppAgentPanel>` + `<WebAppModeSelector>` + the skills sidebar — these get re-introduced in 100-04 inside drawers). DELETE all `<ResizableHandle />` siblings.

    1.8. Remove the `WebAppAgentPanel` inner component (lines ~555-635) — every line of its function body and its associated helper types/constants if not used elsewhere. This component is reborn as `<WebAppChatDrawer>` in 100-04. For now, it is dead and must be deleted to avoid orphan exports.

    1.9. Remove any imports that are now unused (e.g. `WebAppModeSelector`, `WebAppSkillsSidebar` — KEEP the type import for `WebAppMode` if 100-04 needs it; specifically `import type {WebAppMode} from '../webapp-mode-selector'` should stay because 100-04 will reference it). Run `pnpm --filter ui exec tsc --noEmit` to find unused imports if needed.

    Step 2 — Delete webapp-toolbar.tsx:

    Confirm no remaining importers via:
    ```
    grep -rn "webapp-toolbar\|WebAppToolbar" livos/packages/ui/src/
    ```
    Expect: only the test-file's `not.toMatch` assertions reference the string. If any production importer remains, STOP and re-check Task 1.

    Then delete the file:
    ```
    git rm livos/packages/ui/src/modules/window/webapp-toolbar.tsx
    ```

    Step 3 — Build + test:
    ```
    cd livos && pnpm --filter ui exec tsc --noEmit
    cd livos && pnpm --filter ui test -- webapp-stream-window.unit
    cd livos && pnpm --filter ui build
    ```

    All three must succeed. Build (vite) is the heaviest signal because it runs the full module graph; if a stray `WebAppAgentPanel` reference survived elsewhere, it errors here.

    Commit (sacred SHA gate auto-runs):
    `feat(100-03): drop URL bar + ResizablePanelGroup; stream is full-bleed (V33-MULTI-02)`
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui test -- webapp-stream-window.unit 2>&1 | grep -E "(drops WebAppToolbar|drops ResizablePanelGroup|flex-col|pb-9|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/ui/src/modules/window/webapp-toolbar.tsx` does NOT exist (`test ! -f livos/packages/ui/src/modules/window/webapp-toolbar.tsx`).
    - `grep -c "WebAppToolbar" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` returns 0.
    - `grep -c "ResizablePanelGroup\|ResizablePanel\b\|ResizableHandle" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` returns 0.
    - `grep -c "SPLIT_KEY_PREFIX\|DEFAULT_TOP_PCT\|readPersistedLayout\|writePersistedLayout" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` returns 0.
    - `grep -c "WebAppAgentPanel" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` returns 0 (relocated to 100-04 drawer).
    - `grep -c "flex h-full w-full flex-col" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` returns 1 (root wrapper switched to flex-col).
    - **Plan A bottom-space reservation locked:** `grep -qE "pb-9.*min-h-0|min-h-0.*pb-9|relative flex-1 min-h-0 overflow-hidden pb-9" livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` exits 0 (the stream-content wrapper has `pb-9` reserving the 36px bottom-bar overlay space).
    - `grep -rn "from '\.\./webapp-toolbar'\|from '\.\.\/webapp-toolbar'" livos/packages/ui/src/` returns 0 lines (no surviving importers).
    - `cd livos && pnpm --filter ui exec tsc --noEmit` exits 0.
    - `cd livos && pnpm --filter ui test -- webapp-stream-window.unit` shows the 4 new it-blocks PASS (including the `pb-9` reservation check) and 0 failures across the file.
    - `cd livos && pnpm --filter ui build` exits 0.
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (sacred pre-AND-post).
    - Spawn-lifecycle invariants from earlier tests still PASS: the test file's existing `it(...)` blocks for `spawnedForRef` / `closeMutationRef` / spawn idempotency continue to pass (verify by running the full file: `pnpm --filter ui test -- webapp-stream-window.unit` shows 0 failures).
  </acceptance_criteria>
  <done>
    URL bar dropped; webapp-toolbar.tsx deleted; layout is single-column flex with the stream pane as flex-1 + pb-9 reservation; tests + build green; sacred SHA preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 100-03-SUMMARY.md (with pre-100-03 SHA pin for 100-04 lift)</name>
  <files>.planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md.
    - This plan's commit log: `git log --oneline -n 5`.
    - The diff stats from the two commits: `git diff --stat <RED-SHA>~ <GREEN-SHA>`.
  </read_first>
  <action>
    Step 1 — capture the pre-100-03 SHA for 100-04 to lift the deleted WebAppAgentPanel body from.

    Determine N = the number of 100-03 commits BEFORE this SUMMARY commit (typically 2: the RED test commit + the GREEN implementation commit). Then run:
    ```
    git rev-parse HEAD~$N
    ```
    The output is `pre_100_03_sha` — the commit immediately BEFORE 100-03's first source-deleting commit. This is the SHA 100-04 reads to access the deleted `WebAppAgentPanel` body via `git show ${pre_100_03_sha}:livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx`.

    Step 2 — write 100-03-SUMMARY.md with these sections (use Write tool). The frontmatter MUST include the `pre_100_03_sha` key:

    ```markdown
    ---
    pre_100_03_sha: <SHA from Step 1>  # The commit immediately BEFORE 100-03's first source-deleting commit. 100-04 reads this to lift the WebAppAgentPanel body via `git show <SHA>:livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx`.
    ---

    # Phase 100-03 SUMMARY

    **Date:** <YYYY-MM-DD>
    **Goal:** V33-MULTI-02 — drop URL bar + inline agent panel; full-bleed stream.

    ## Diff
    | File | Change |
    |---|---|
    | livos/packages/ui/src/modules/window/webapp-toolbar.tsx | DELETED (125 lines gone) |
    | livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx | -<N> +<M> (toolbar + ResizablePanelGroup + WebAppAgentPanel removed; root switched to flex-col; stream wrapper gains pb-9) |
    | livos/packages/ui/src/modules/window/webapp-stream-window.unit.test.tsx | -<N> +<M> (4 invariants removed; 4 added — including pb-9 guard) |

    ## Test results
    - `pnpm --filter ui test -- webapp-stream-window.unit`: 4 new it-blocks PASS; 0 failures.
    - `pnpm --filter ui exec tsc --noEmit`: clean.
    - `pnpm --filter ui build`: vite build OK.

    ## Sacred SHA gate
    - Pre commits:  f3538e1d811992b782a9bb057d1b7f0a0189f95f
    - Post commits: f3538e1d811992b782a9bb057d1b7f0a0189f95f

    ## Commits
    - <SHA1> test(100-03): RED - flip stream-window invariants for full-bleed layout
    - <SHA2> feat(100-03): drop URL bar + ResizablePanelGroup; stream is full-bleed (V33-MULTI-02)

    ## Carryover for 100-04
    - The deleted `WebAppAgentPanel` inner component is preserved in git history at `pre_100_03_sha` (recorded in this SUMMARY's frontmatter). 100-04 lifts its body via `git show ${pre_100_03_sha}:livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx` into a new `<WebAppChatDrawer>` component file.
    - The `<WebAppSkillsSidebar>` + `<SkillReplayScrubber>` components still exist; 100-04 relocates their render sites into the Teach drawer.
    - Type import `import type {WebAppMode} from '../webapp-mode-selector'` is preserved (100-04 may delete or repurpose webapp-mode-selector.tsx — D-locked option (a): keep as constants module).
    - **Layout contract for 100-04:** stream wrapper is `relative flex-1 min-h-0 overflow-hidden pb-9`. 100-04 adds the bottom action-bar as `absolute inset-x-0 bottom-0 z-20` overlay (Plan A locked). Do NOT remove the `pb-9` — the unit-test invariant guards it.
    ```

    Commit: `docs(100-03): SUMMARY — full-bleed stream, toolbar deleted; pin pre_100_03_sha for 100-04 lift`
  </action>
  <verify>
    <automated>test -f .planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md && grep -q "f3538e1d811992b782a9bb057d1b7f0a0189f95f" .planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md && grep -q "^pre_100_03_sha:" .planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File 100-03-SUMMARY.md exists.
    - File contains the literal sacred-SHA string twice (Pre + Post).
    - File contains real commit SHAs (not `<SHA1>` placeholder).
    - File frontmatter (between the leading `---` fences) contains a `pre_100_03_sha:` key whose value is a 40-char git SHA (verified by `grep -E '^pre_100_03_sha: [0-9a-f]{40}' .planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md` matching exactly one line).
    - The `pre_100_03_sha` value, when fed into `git show ${pre_100_03_sha}:livos/packages/ui/src/modules/window/app-contents/webapp-stream-window.tsx`, returns a non-empty file containing the literal string `WebAppAgentPanel` (sanity-check that the lift target is valid).
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` == `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </acceptance_criteria>
  <done>
    SUMMARY committed; carryover notes give 100-04 enough to lift the agent panel + skills sidebar into drawers; pre_100_03_sha pinned in frontmatter for 100-04's `git show` lift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ui bundle → user | Source-only changes; no new exfil surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | Tampering | Sacred file | mitigate | .husky/pre-commit hook from 100-01 fires on every commit (3 commits in this plan). |
| T-100-03-02 | Information Disclosure | URL bar removal | accept | URL was previously displayed in-app for the user's own WebApps; removing it does not expose anything new. |
</threat_model>

<verification>
- webapp-toolbar.tsx file is deleted (`test ! -f livos/packages/ui/src/modules/window/webapp-toolbar.tsx`).
- webapp-stream-window.tsx has zero references to ResizablePanelGroup / WebAppToolbar / WebAppAgentPanel / SPLIT_KEY_PREFIX / DEFAULT_TOP_PCT / readPersistedLayout / writePersistedLayout.
- webapp-stream-window.tsx root container is `flex flex-col` (verified by grep).
- webapp-stream-window.tsx stream wrapper has `pb-9` reserving 36px for 100-04 bottom-bar overlay (Plan A locked).
- pnpm --filter ui test webapp-stream-window.unit: 4 new tests pass; existing spawn-lifecycle tests still pass.
- pnpm --filter ui exec tsc --noEmit clean.
- pnpm --filter ui build clean.
- 100-03-SUMMARY.md frontmatter has `pre_100_03_sha:` pinned for 100-04 to lift WebAppAgentPanel body.
- Sacred SHA preserved across all 3 commits.
</verification>

<success_criteria>
- Stream area visually fills the WebApp window (no URL bar, no inline agent panel beneath).
- Standard top drag-strip + close-X (from window-content.tsx + window-chrome.tsx) is the only chrome above the stream.
- Stream wrapper reserves 36px (pb-9) at the bottom for 100-04's overlay bottom-bar.
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` byte-identical at HEAD.
- 100-04 has a clean handoff: it can add the bottom action bar + drawers using the pinned `pre_100_03_sha` to lift WebAppAgentPanel; pb-9 reservation is already in place.
</success_criteria>

<output>
After completion, create `.planning/phases/100-multi-stream-window-redesign/100-03-SUMMARY.md`.
</output>
</content>
