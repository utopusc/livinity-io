---
phase: 42-openai-compatible-broker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-openai-compatible-broker/42-AUDIT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Plans 42-02..05 know exactly which Phase 41 helpers to import and where to mount the new OpenAI route"
    - "Reuse vs new-file decisions are pinned with line numbers (not vague hand-waves)"
    - "Sacred file SHA is re-verified at the start of Phase 42 (not assumed from Phase 41 SUMMARY)"
  artifacts:
    - path: ".planning/phases/42-openai-compatible-broker/42-AUDIT.md"
      provides: "Concrete extension points (file paths + line numbers + reuse decisions) for Plans 42-02..05"
      min_lines: 200
  key_links:
    - from: ".planning/phases/42-openai-compatible-broker/42-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/livinity-broker/index.ts"
      via: "documents mountBrokerRoutes() extension pattern (line-precise)"
      pattern: "mountBrokerRoutes"
    - from: ".planning/phases/42-openai-compatible-broker/42-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/livinity-broker/router.ts"
      via: "documents middleware chain to reuse + handler pattern to mirror"
      pattern: "containerSourceIpGuard|resolveAndAuthorizeUserId"
    - from: ".planning/phases/42-openai-compatible-broker/42-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/livinity-broker/sse-adapter.ts"
      via: "documents writeSseChunk pattern + decision: extend vs new file for OpenAI SSE"
      pattern: "writeSseChunk"
---

<objective>
Phase 42 must extend Phase 41's `livinity-broker` module with one new route (`POST /v1/chat/completions`) and bidirectional OpenAI ↔ Anthropic translation. Before any implementation, this plan produces a read-only `42-AUDIT.md` that pins the EXACT extension points so Plans 42-02..05 don't have to re-discover them.

Purpose: Phase 41 finished 5 days ago with 5 source files + 4 test files in `livinity-broker/`. The next 3 implementation plans need precise answers — not "look at the broker module" — to: (a) which helpers to import vs duplicate; (b) where to add the OpenAI router mount in `index.ts`; (c) whether to extend the existing `sse-adapter.ts` or create a parallel `openai-sse-adapter.ts`; (d) the exact middleware order in the new router; (e) how to wire the OpenAI route to reuse `createSdkAgentRunnerForUser` (the same upstream proxy Phase 41 already uses).

Output: `.planning/phases/42-openai-compatible-broker/42-AUDIT.md` with 8 sections (mount surface; helpers to reuse; sse-adapter strategy; router middleware order; SdkAgentRunner reuse path; sacred-file baseline; test infrastructure; risks). One atomic commit (audit doc only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-openai-compatible-broker/42-CONTEXT.md
@.planning/phases/41-anthropic-messages-broker/41-SUMMARY.md
@.planning/phases/41-anthropic-messages-broker/41-CONTEXT.md
@livos/packages/livinityd/source/modules/livinity-broker/index.ts
@livos/packages/livinityd/source/modules/livinity-broker/router.ts
@livos/packages/livinityd/source/modules/livinity-broker/types.ts
@livos/packages/livinityd/source/modules/livinity-broker/auth.ts
@livos/packages/livinityd/source/modules/livinity-broker/translate-request.ts
@livos/packages/livinityd/source/modules/livinity-broker/sse-adapter.ts
@livos/packages/livinityd/source/modules/livinity-broker/sync-response.ts
@livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts

<interfaces>
<!-- Key Phase 41 contracts the audit must reference. -->

From livos/packages/livinityd/source/modules/livinity-broker/index.ts (line 25):
```typescript
export function mountBrokerRoutes(app: express.Application, livinityd: Livinityd): void {
  const router = createBrokerRouter({livinityd})
  app.use('/u', router)
  // ...
}
```

From livos/packages/livinityd/source/modules/livinity-broker/router.ts (line 25):
```typescript
export function createBrokerRouter(deps: BrokerDeps): express.Router {
  const router = express.Router()
  router.use(containerSourceIpGuard)            // Line 29
  router.use(express.json({limit: '10mb'}))     // Line 32
  router.post('/:userId/v1/messages', ...)      // Line 35 (sync + SSE in one handler)
  return router
}
```

From livos/packages/livinityd/source/modules/livinity-broker/auth.ts:
```typescript
export function containerSourceIpGuard(req, res, next): void
export async function resolveAndAuthorizeUserId(req, res, livinityd): Promise<{userId: string} | undefined>
```

From livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts:
```typescript
export async function* createSdkAgentRunnerForUser(opts: {
  livinityd: Livinityd
  userId: string
  task: string
  contextPrefix?: string
  systemPromptOverride?: string
  maxTurns?: number
  signal?: AbortSignal
}): AsyncGenerator<AgentEvent, AgentResult, void>
```

From livos/packages/livinityd/source/modules/livinity-broker/sse-adapter.ts:
```typescript
export function writeSseChunk(res: Response, chunk: AnthropicSseChunk): void
// Note: writeSseChunk is Anthropic-spec specific (event: <name>\ndata: <json>\n\n)
// OpenAI SSE wants data:-only (no event: prefix). Decision needed.
```

From livos/packages/livinityd/source/modules/server/index.ts (line 1215):
```typescript
mountBrokerRoutes(this.app, this.livinityd)  // Already mounted — no changes needed in Phase 42
```

Sacred file (NOT modified): nexus/packages/core/src/sdk-agent-runner.ts
Baseline SHA: 623a65b9a50a89887d36f770dcd015b691793a7f (from Phase 41 SUMMARY)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sacred file baseline + write 42-AUDIT.md</name>
  <files>.planning/phases/42-openai-compatible-broker/42-AUDIT.md</files>
  <action>
Step A — Sacred file integrity check (mandatory before audit body):
Run: `git log -1 --format="%H" -- nexus/packages/core/src/sdk-agent-runner.ts`
Run: `git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts`
The first command must return a SHA that produces a git blob equal to baseline `623a65b9a50a89887d36f770dcd015b691793a7f` for the file's last-touched commit content; the second must produce empty output. If either fails, STOP — Phase 42 cannot proceed (sacred file drift means Phase 41 baseline is broken; report and abort).

Step B — Write the audit. Create `.planning/phases/42-openai-compatible-broker/42-AUDIT.md` with these 8 sections (use markdown headers `## Section N: <name>`):

1. **Sacred File Baseline Re-Verification** — record the current SHA from Step A, the git diff output (empty), and the path the audit looked at. State explicitly: "Phase 42 baseline = `623a65b9...` (Phase 41 final = Phase 40 baseline; byte-identical chain since Phase 40)."

2. **Mount Surface** — document that `mountBrokerRoutes(app, livinityd)` in `livos/packages/livinityd/source/modules/livinity-broker/index.ts:25` is the single mount point. Phase 42 does NOT add a new mount call in `server/index.ts:1215` — instead, the existing `createBrokerRouter()` factory in `router.ts:25` will be extended to also register the `/v1/chat/completions` route. Conclusion: ZERO edits to `livos/packages/livinityd/source/modules/server/index.ts` in Phase 42. The audit must include the verbatim 5 lines around line 1215 from server/index.ts as evidence.

3. **Helpers to Reuse Verbatim** — list with import paths:
   - `containerSourceIpGuard` (auth.ts:23) — re-applied via `router.use()` once, covers BOTH `/v1/messages` and `/v1/chat/completions`
   - `resolveAndAuthorizeUserId(req, res, livinityd)` (auth.ts:69) — called inside the OpenAI handler the same way `/v1/messages` calls it
   - `express.json({limit: '10mb'})` body parser — already applied at router level (router.ts:32), no need to re-apply
   - `createSdkAgentRunnerForUser` (agent-runner-factory.ts:29) — the SAME async generator the Anthropic route uses; OpenAI route reuses it identically (only the input translation + output adapter differ)
   - `aggregateChunkText()` (sync-response.ts:51) — reusable as-is for OpenAI sync response (text concatenation is format-agnostic)

4. **OpenAI SSE Strategy Decision** — Phase 41's `writeSseChunk` writes BOTH `event:` and `data:` lines (Anthropic-spec). OpenAI Chat Completions streaming wants `data: <json>\n\n` ONLY (no `event:` prefix), terminated by literal `data: [DONE]\n\n`. Decision: CREATE a new file `openai-sse-adapter.ts` rather than parametrizing the existing `sse-adapter.ts`. Rationale:
     (a) Phase 41's adapter is a tightly-coupled discriminated union of 8 Anthropic event shapes — adding OpenAI shapes would balloon the union and risk regressing Phase 41 tests.
     (b) Two adapters = two narrow files, each with its own type-safe chunk discriminator, easier to test independently.
     (c) Phase 41's sse-adapter.ts is feature-frozen — touching it could break the integrity test.
   The audit must explicitly state: "Phase 41's `sse-adapter.ts` is NOT modified in Phase 42." A new sibling file `openai-sse-adapter.ts` will house OpenAI chunk writer + adapter.

5. **Router Middleware Order in OpenAI Handler** — document that `createBrokerRouter()` in router.ts:25 must be extended to add a SECOND `router.post('/:userId/v1/chat/completions', ...)` handler AFTER the existing `/v1/messages` handler (line 35). Both handlers reuse the same router-level `containerSourceIpGuard` + `express.json` middleware. Inside the OpenAI handler the order MUST be: (1) `resolveAndAuthorizeUserId`; (2) body validation (`model`, `messages`, ignore `tools`/`tool_choice`/`function_call` with warn log); (3) translate OpenAI → Anthropic-style SDK args via new `openai-translator.ts`; (4) branch on `body.stream` — sync uses `aggregateChunkText` + new `buildSyncOpenAIResponse`; SSE uses new `createOpenAISseAdapter`; (5) reuse `createSdkAgentRunnerForUser` for the upstream call (same as `/v1/messages`).

6. **SdkAgentRunner Reuse Path (Strategy B inheritance)** — Phase 41 chose Strategy B (HTTP proxy to nexus `/api/agent/stream`). The OpenAI route MUST inherit Strategy B unchanged — no direct SdkAgentRunner instantiation in livinityd. Reason: the X-LivOS-User-Id header → homeOverride wiring (Plan 41-04) is already proven in nexus's api.ts; the OpenAI route benefits transparently. The audit must explicitly state: "OpenAI route does NOT touch nexus/packages/core/src/api.ts. The X-LivOS-User-Id header path established by Plan 41-04 carries forward unchanged."

7. **Test Infrastructure (vitest? bare tsx?)** — Document Phase 41's pattern: `node:assert/strict` + bare `tsx` execution per file (no Vitest). Test files run individually as `npx tsx ./source/modules/livinity-broker/<name>.test.ts`. The integration test pattern in `integration.test.ts:1-302` uses (a) pg.Pool prototype patching BEFORE database module import (lines 39-55), (b) per-test fetch monkey-patch scoped to `/api/agent/stream` (lines 108-129), (c) ephemeral 127.0.0.1 port via `http.createServer` (lines 131-152). Phase 42's integration test MUST mirror this pattern. Run command for Phase 42 tests: same as Phase 41 (`npx tsx ./source/modules/livinity-broker/openai-translator.test.ts` etc.). For the chained `npm run test:phase42` script: it must add a NEW nexus-side test file, OR (preferred) chain `npm run test:phase41` and rely on the broker-side tsx tests being run separately. Decision: Phase 42 adds NO new nexus tests (no nexus changes in Phase 42); `test:phase42` chains `npm run test:phase41` then re-asserts sacred SHA.

8. **Risks & Open Questions** — pin these for Plans 42-02..05:
   - Risk R1: SDK args translator currently produces `{task, contextPrefix, systemPromptOverride}`. The OpenAI translator can RETURN THE SAME SHAPE and reuse `createSdkAgentRunnerForUser` directly — confirm by reading `agent-runner-factory.ts:29` opts shape. (Confirmed: identical.)
   - Risk R2: `aggregateChunkText` operates on `AgentEvent` with `type === 'chunk'`. OpenAI sync response just needs the concatenated text — same call, format-agnostic. Safe to reuse.
   - Risk R3: OpenAI requires `delta.role: "assistant"` on the FIRST chunk only. The new SSE adapter must track a `firstChunkSent` boolean. Document this so Plan 42-03 implements it.
   - Risk R4: OpenAI spec forbids the `event:` prefix in SSE — only `data:` lines. The new `writeOpenAISseChunk` helper must emit `data: <json>\n\n` (no `event:` line) and the terminal `data: [DONE]\n\n`.
   - Risk R5: Model alias table is hardcoded per D-42-11. The `model` field RETURNED in the OpenAI response must echo the CALLER'S requested model (e.g., `gpt-4`), NOT the resolved Claude model. Internal logging records both. Document the table verbatim.

End of audit. Total: ~250-350 lines.

Step C — Commit:
Run: `git add .planning/phases/42-openai-compatible-broker/42-AUDIT.md`
Run: `git commit -m "docs(42-01): codebase audit — extension points for Phase 42 OpenAI broker (Phase 42 prep)"`
  </action>
  <verify>
    <automated>test -f .planning/phases/42-openai-compatible-broker/42-AUDIT.md && wc -l .planning/phases/42-openai-compatible-broker/42-AUDIT.md | awk '{exit ($1 < 200)}' && grep -q "623a65b9a50a89887d36f770dcd015b691793a7f" .planning/phases/42-openai-compatible-broker/42-AUDIT.md && grep -q "openai-sse-adapter" .planning/phases/42-openai-compatible-broker/42-AUDIT.md && grep -q "createSdkAgentRunnerForUser" .planning/phases/42-openai-compatible-broker/42-AUDIT.md && git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l | awk '{exit ($1 != 0)}'</automated>
  </verify>
  <done>42-AUDIT.md created with 8 sections, ≥ 200 lines, sacred-file SHA verified, decision pinned (extend vs new file = NEW openai-sse-adapter.ts), and committed atomically.</done>
</task>

</tasks>

<verification>
- 42-AUDIT.md exists in `.planning/phases/42-openai-compatible-broker/`
- Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` is byte-identical to baseline `623a65b9a50a89887d36f770dcd015b691793a7f` (`git diff HEAD` produces zero output)
- Audit pins all 8 sections with concrete file paths + line numbers
- Audit explicitly decides: NEW `openai-sse-adapter.ts` (do NOT modify `sse-adapter.ts`)
- Audit explicitly decides: ZERO edits to `livos/.../server/index.ts` (mount surface unchanged)
- Audit explicitly decides: ZERO edits to nexus (X-LivOS-User-Id path inherited from Plan 41-04)
- One atomic commit
</verification>

<success_criteria>
- Plans 42-02..05 can begin without re-reading Phase 41 source files
- Every "should we extend or create new" question is answered with a rationale
- Sacred file integrity is re-verified at Phase 42 start (not assumed)
</success_criteria>

<output>
After completion, create `.planning/phases/42-openai-compatible-broker/42-01-SUMMARY.md` with: commit SHA, audit section count, line count, sacred-file SHA verification result, and the 5 risk items (R1-R5) for downstream plans.
</output>
