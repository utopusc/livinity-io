---
phase: 185
plan: 04
type: execute
wave: 4
depends_on:
  - 185-03
files_modified:
  - .planning/phases/184-v38-deploy-uat/184-03-probes-log.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - V38.1-01
must_haves:
  truths:
    - "184-03-probes-log.md has a Phase 185 UAT probe section appended"
    - "ROADMAP.md Phase 185 entry reflects the actual scope (AI Chat window, not global shell)"
    - "ROADMAP.md Phase 185 status marker is updated to CODE-COMPLETE"
  artifacts:
    - path: ".planning/phases/184-v38-deploy-uat/184-03-probes-log.md"
      provides: "Manual UAT walk for Phase 185 sidebar mount"
      contains: "Phase 185"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 185 description + CODE-COMPLETE marker"
      contains: "CODE-COMPLETE"
  key_links: []
---

<objective>
Documentation close-out for Phase 185. No code changes.

1. Append a manual UAT probe section to `184-03-probes-log.md` describing exactly how
   an operator verifies the sidebar is working end-to-end in the AI Chat window.

2. Update ROADMAP.md Phase 185 entry:
   - Fix the misleading description ("global dashboard shell" → actual scope: "AI Chat window left pane")
   - Set status to CODE-COMPLETE
   - Record plan count + linked plan files

Purpose: v38.1 UAT loop requires a documented probe so the operator can verify the
fix on the Mini PC without re-reading source code.

Output: Updated probe log + ROADMAP marker. No vitest assertions.
</objective>

<execution_context>
@C:\Users\hello\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hello\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/185-sidebar-mount-shell/185-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 185 UAT probe to 184-03-probes-log.md</name>
  <files>.planning/phases/184-v38-deploy-uat/184-03-probes-log.md</files>
  <action>
    Read the existing `184-03-probes-log.md` to understand its format (each probe has a
    heading, command or browser steps, and an expected result section).

    Append the following section AT THE END of the file (after the last existing probe):

    ---

    ### P-185 — SidebarTree in AI Chat Window (Phase 185 UAT)

    **What to verify:** The AI Chat window now has a left sidebar pane (~280px) with
    the SidebarTree mounted inside it. This resolves the v38.0 UAT finding:
    "Open a Chat from the sidebar to attach a terminal" (no sidebar was visible).

    **Browser steps (Mini PC — open AI Chat window from Dock):**

    1. Open the LivOS desktop in a browser (e.g. `https://bruce.livinity.live` or `http://10.69.31.68:3000`).
    2. Click the AI Chat icon in the Dock to open the AI Chat window.
    3. EXPECT: The window body is split horizontally — a ~280px left pane is visible
       containing a tree/list of items (Projects, Agents, Chats), and the right pane
       shows the Terminal / Vault Graph tab nav.
    4. EXPECT: A "Workspace" label and a `+` button are visible at the top of the left pane.
    5. Click the `+` button. EXPECT: An "Add new item" modal opens with three type cards
       (Project, Agent, Chat).
    6. Close the modal (press Escape or click outside). EXPECT: Modal closes, sidebar still visible.
    7. In the SidebarTree, if any Chat items exist: click one.
       EXPECT: The right pane switches from the empty-state hint to a terminal
       (CcTerminal attach for that Chat's ccSessionId).
    8. If no Chat items exist yet: use the `+` button to create a new Chat item.
       EXPECT: After creation, the new Chat row appears in the tree.
       Click it. EXPECT: Terminal mounts in the right pane.
    9. Click the "Vault Graph" tab.
       EXPECT: Vault Graph renders; the terminal/detail view is replaced.
    10. Click the "Terminal" tab.
        EXPECT: The selected item's terminal re-appears (if item was selected) OR
        the empty-state hint / LivWelcomeTerminal (if no item selected).

    **Mobile (optional — narrow the browser to < 768px):**
    - EXPECT: Sidebar is hidden by default; a hamburger icon appears in the tab nav row.
    - Click the hamburger. EXPECT: Sidebar slides into view.

    **Pass criteria:** Steps 1-10 all produce the expected results with no console errors.

    ---
  </action>
  <verify>
    <automated>grep -c "P-185" ".planning/phases/184-v38-deploy-uat/184-03-probes-log.md"</automated>
  </verify>
  <done>`grep -c "P-185" 184-03-probes-log.md` returns 1 (section present). File modified date is today.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 185 entry to CODE-COMPLETE</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Read the current Phase 185 block in ROADMAP.md (around line 2413).
    The current text reads:

    ```
    ### Phase 185: Mount SidebarTree in Global Dashboard Shell — 🔴 PLANNED 2026-05-20
    ```

    Replace the entire Phase 185 block (from `### Phase 185:` down to the `---` separator
    before Phase 186) with:

    ```markdown
    ### Phase 185: Mount SidebarTree in AI Chat Window (Left Pane) — 🟡 CODE-COMPLETE 2026-05-20

    **Goal:** Wire `<SidebarTree>`, `<SidebarFooter>`, and `<AddItemModal>` into the
    AI Chat window content (`routes/ai-chat/index.tsx`) as a persistent left pane.
    Per [feedback_livos_window_logic_no_url_routing.md], the sidebar belongs INSIDE the
    AI Chat window — not in the global desktop shell. Resolves v38.0 UAT finding #1:
    "Open a Chat from the sidebar to attach a terminal" (no sidebar was visible).

    **Scope:**
    - 185-01: Split AI Chat window into left pane (SidebarTree ~280px) + right pane (tab content)
    - 185-02: Right-pane item routing — Chat click → ChatDetail, Project → ProjectDetail, Agent → AgentDetail
    - 185-03: Mobile collapse via useIsMobile + AddItemModal trigger at top of sidebar
    - 185-04: UAT probe doc + ROADMAP status marker

    **Depends on:** Phase 174 (SidebarTree), Phase 175 (AddItemModal + Detail views), Phase 176 (open_item), Phase 183 (gear button)
    **Wave:** 1 (file-disjoint with Phase 186)
    **Plans:** 4 plans

    Plans:
    - [x] 185-01-PLAN.md — Split layout + SidebarTree mount (6 assertions)
    - [x] 185-02-PLAN.md — Right-pane item routing (8 assertions)
    - [x] 185-03-PLAN.md — Mobile collapse + AddItemModal trigger (4 assertions)
    - [x] 185-04-PLAN.md — UAT probe + ROADMAP marker (docs only)

    **Test delta:** +18 vitest assertions (32 PASS in ai-chat.test.tsx after phase close)
    **Sacred SHA preserved:** yes (no liv/packages/core changes)

    ---
    ```

    Preserve the Phase 186 block immediately after (do not modify it).
  </action>
  <verify>
    <automated>grep -c "CODE-COMPLETE" ".planning/ROADMAP.md"</automated>
  </verify>
  <done>Phase 185 heading contains "CODE-COMPLETE"; all 4 plan checkboxes marked [x]; Phase 186 block unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | Plain markdown — no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-04-01 | N/A | Documentation files | accept | No code, no attack surface |
</threat_model>

<verification>
```bash
grep "CODE-COMPLETE" ".planning/ROADMAP.md" | grep "185"
# Expect: one matching line

grep "P-185" ".planning/phases/184-v38-deploy-uat/184-03-probes-log.md"
# Expect: the probe heading
```
</verification>

<success_criteria>
- `184-03-probes-log.md` has P-185 probe section with 10-step browser walk
- `ROADMAP.md` Phase 185 heading shows CODE-COMPLETE
- Phase 185 ROADMAP block describes AI Chat window scope (not global shell)
- All 4 plan checkboxes marked [x]
- No code files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/185-sidebar-mount-shell/185-04-SUMMARY.md`

Then create the phase SUMMARY at `.planning/phases/185-sidebar-mount-shell/185-SUMMARY.md`:
- Phase 185 complete
- 18 new vitest assertions (32 PASS in ai-chat.test.tsx)
- Files modified: routes/ai-chat/index.tsx, ai-chat.test.tsx
- Sacred SHA preserved
</output>
