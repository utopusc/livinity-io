---
phase: 247-luse-skill-v2-docs
plan: 02
type: execute
wave: 2
depends_on: [247-01]
files_modified:
  - .claude/skills/luse/SKILL.md
  - .claude/skills/luse/click.md
  - .claude/skills/luse/type.md
  - .claude/skills/luse/screenshot.md
  - .claude/skills/luse/key.md
  - .claude/skills/luse/scroll.md
  - .aion/skills/luse.md
  - .opencode/skills/luse.md
  - .openclaw/skills/luse.md
autonomous: true
requirements: []
tags: [docs, luse, shim-sync, sha256, idempotency]

must_haves:
  truths:
    - "Re-running `bash scripts/sync-luse-skills.sh` after Plan 247-01's canonical doc additions reports a non-zero UPDATED count (new files added to canonical set + per-tool cross-ref edits MUST propagate to shims)."
    - "After the first sync, a second consecutive run reports `0 new / 0 updated / N unchanged` — idempotency invariant from Phase 242 is preserved."
    - "All 4 shim targets (`.claude/skills/luse/`, `.aion/skills/luse.md`, `.opencode/skills/luse.md`, `.openclaw/skills/luse.md`) carry source-sha markers matching the post-247-01 canonical payload."
    - "Spot-checking a representative shim (e.g. `.aion/skills/luse.md`) shows at least one PATTERNS.md cross-reference line propagated through the bundled payload (proving per-tool doc edits travel into shims)."
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` for `liv/packages/core/src/sdk-agent-runner.ts` preserved through both Plan 02 commits (pre-commit hook enforces)."
  artifacts:
    - path: .claude/skills/luse/SKILL.md
      provides: "Claude Code skill entry with refreshed source-sha"
      contains: "source-sha:"
    - path: .aion/skills/luse.md
      provides: "Aion CLI shim with refreshed source-sha and bundled v2 payload"
      contains: "source-sha:"
    - path: .opencode/skills/luse.md
      provides: "OpenCode shim with refreshed source-sha"
      contains: "source-sha:"
    - path: .openclaw/skills/luse.md
      provides: "OpenClaw shim with refreshed source-sha"
      contains: "source-sha:"
  key_links:
    - from: docs/luse/PATTERNS.md
      to: .aion/skills/luse.md
      via: "scripts/sync-luse-skills.sh concatenated payload"
      pattern: "Pattern 1"
    - from: docs/luse/tools/click.md
      to: .claude/skills/luse/click.md
      via: "scripts/sync-luse-skills.sh per-tool shim"
      pattern: "PATTERNS\\.md"
---

<objective>
Propagate Plan 247-01's canonical doc additions (PATTERNS / TROUBLESHOOTING / ANTI-PATTERNS / INTEGRATION-RECIPES / KNOWN-LIMITS / CHEAT-SHEET + 5 per-tool cross-reference edits) to all 4 agent shims via `scripts/sync-luse-skills.sh`. Verify Phase 242's sha256-marker idempotency invariant still holds after the v2 docs land, then commit the shim deltas.

Purpose: Phase 242 deliberately split canonical docs (source of truth) from per-agent shims (auto-generated). v2 docs are only useful to agents if the shim sync actually carries them. This plan is the propagation + verification step.

Output: 4 regenerated shim payloads + 1 commit, no canonical-doc edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/247-luse-skill-v2-docs/247-CONTEXT.md
@.planning/phases/247-luse-skill-v2-docs/247-01-PLAN.md
@.planning/phases/242-luse-skill-set-universal/242-SUMMARY.md
@scripts/sync-luse-skills.sh

<sync_script_contract>
<!-- From Phase 242 D-242-B + script structure (lines 133-207): -->
<!-- - generate_claude_skill writes .claude/skills/luse/SKILL.md + 5 per-tool files (click/type/screenshot/key/scroll). -->
<!-- - generate_generic_shim writes 1 concatenated .md per agent (Aion / OpenCode / OpenClaw). -->
<!-- - Idempotency: first 5 lines of each shim carry `source-sha: <hex>`; re-run computes payload sha, compares, writes only on mismatch. -->
<!-- - Final stdout line: `Synced N shims (M new / K updated / P unchanged)`. -->
<!-- - 4 shim targets total. Gemini is intentionally skipped (D-242-C). -->
<!-- - PHASE 247 DOES NOT EXTEND the script. New canonical files (PATTERNS.md / TROUBLESHOOTING.md / etc.) -->
<!--   live under docs/luse/ (NOT docs/luse/tools/) and currently are NOT in the script's payload manifest. -->
<!--   If a new canonical file is meant to be shipped INTO the shim payload, the script's -->
<!--   build_*_payload function (and the per-tool list, if relevant) must be extended in Task 1. -->
<!--   Verify which canonical files the existing script reads BEFORE assuming the sync picks them up. -->
</sync_script_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit sync script payload manifest + extend if needed + run sync</name>
  <files>scripts/sync-luse-skills.sh, .claude/skills/luse/SKILL.md, .claude/skills/luse/click.md, .claude/skills/luse/type.md, .claude/skills/luse/screenshot.md, .claude/skills/luse/key.md, .claude/skills/luse/scroll.md, .aion/skills/luse.md, .opencode/skills/luse.md, .openclaw/skills/luse.md</files>
  <read_first>
    - scripts/sync-luse-skills.sh (FULL file — needed to identify which canonical paths feed the payload; lines 1-213)
    - docs/luse/PATTERNS.md, docs/luse/TROUBLESHOOTING.md, docs/luse/ANTI-PATTERNS.md, docs/luse/INTEGRATION-RECIPES.md, docs/luse/KNOWN-LIMITS.md, docs/luse/CHEAT-SHEET.md (Plan 247-01 outputs — confirm they exist)
    - .claude/skills/luse/click.md (current shim — to see HTML-comment header + body shape before editing the script touches it)
    - .aion/skills/luse.md head -20 (current source-sha marker + payload start)
  </read_first>
  <action>
    **Step 1 — Audit (decision gate, no edits yet):** Open `scripts/sync-luse-skills.sh`. Find the `build_*_payload` / payload-assembly functions and the per-tool list. Determine: which canonical paths under `docs/luse/` does the script currently concatenate into the generic-shim payload, and which paths does it copy as standalone per-tool files for the Claude skill?

    - Phase 242's script (per 242-SUMMARY.md) bundles `LUSE.md` + 5 tool files + `LUSE-WORKFLOW.md` for the generic shim, and copies the 5 tool files for the Claude skill.
    - The new canonical files (PATTERNS / TROUBLESHOOTING / ANTI-PATTERNS / INTEGRATION-RECIPES / KNOWN-LIMITS / CHEAT-SHEET) are NEW top-level `docs/luse/*.md` files NOT in the original manifest.

    **Decision:**
    - If the existing script's generic-shim payload already does a `cat docs/luse/*.md` glob → no script edit needed, the new files travel automatically.
    - If it lists files explicitly → extend the explicit list to add all 6 new canonical files (PATTERNS.md, TROUBLESHOOTING.md, ANTI-PATTERNS.md, INTEGRATION-RECIPES.md, KNOWN-LIMITS.md, CHEAT-SHEET.md) into the generic-shim payload, AND copy them as standalone files into `.claude/skills/luse/` for the Claude skill (analogous to how it copies the 5 tool files).
    - For Claude skill: add a parallel pattern for the 6 new top-level docs OR concatenate them into SKILL.md body (prefer standalone files matching the per-tool shape — one .claude/skills/luse/PATTERNS.md per canonical file — for parity with Phase 242 style).
    - **Cross-reference edits inside `docs/luse/tools/*.md`** (Plan 247-01 Task 2) propagate naturally because the script already reads those 5 files.

    **Step 2 — Extend script if needed:** If the audit determined explicit-list extension is required, edit `scripts/sync-luse-skills.sh` to:
    - Add a new variable `LUSE_TOP_DOCS=("PATTERNS.md" "TROUBLESHOOTING.md" "ANTI-PATTERNS.md" "INTEGRATION-RECIPES.md" "KNOWN-LIMITS.md" "CHEAT-SHEET.md")`.
    - Extend the generic-shim payload builder to append these in a deterministic order after the existing payload, each with a `## <NAME>` h2 header.
    - Extend the Claude skill generator to emit `.claude/skills/luse/<name>.md` for each of the 6 top-level docs (HTML-comment header + source-sha marker + body, same shape as the existing per-tool shim emitter).
    - Preserve `set -euo pipefail`, no jq, sha256sum/shasum fallback semantics.
    - Update the final stdout `Synced N shims (...)` count to include the 6 new Claude skill files (4 generic + 1 Claude SKILL.md + 5 per-tool + 6 new top-level = 16, NOT 9 as Phase 242 reported).
    - If the script ALREADY globs `docs/luse/*.md`, NO script edit; document this in the task SUMMARY.

    **Step 3 — Run sync:** `bash scripts/sync-luse-skills.sh` from repo root. Capture stdout. Expected first-run output: includes new file counts (e.g. `Synced 16 shims (7 new / 9 updated / 0 unchanged)` if script was extended; OR `Synced N shims (0 new / N updated / 0 unchanged)` if script already globbed).

    **Step 4 — Idempotency check:** Immediately run `bash scripts/sync-luse-skills.sh` a second time. MUST report `0 new / 0 updated / N unchanged`. This proves the source-sha marker invariant from Phase 242 D-242-B still holds.

    **Step 5 — Cross-reference smoke test:** `grep -c 'PATTERNS.md' .aion/skills/luse.md` MUST be ≥1 (proves the per-tool doc edits with PATTERNS.md cross-refs landed in the bundle). `grep -c 'Pattern 1' .aion/skills/luse.md` MUST be ≥1 if the new top-level docs are in the bundle (Aion/OpenCode/OpenClaw use the concatenated-payload shape).

    **Step 6 — Sacred SHA gate:** Verify `git hash-object liv/packages/core/src/sdk-agent-runner.ts` returns `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. The pre-commit hook will reject the commit otherwise.

    Do NOT hand-edit any shim file. All shim changes flow through the script.
  </action>
  <verify>
    <automated>bash scripts/sync-luse-skills.sh > /tmp/sync-second.log 2>&1 && grep -qE '0 new / 0 updated / [0-9]+ unchanged' /tmp/sync-second.log && grep -q 'source-sha:' .claude/skills/luse/SKILL.md && grep -q 'source-sha:' .aion/skills/luse.md && grep -q 'source-sha:' .opencode/skills/luse.md && grep -q 'source-sha:' .openclaw/skills/luse.md && grep -q 'PATTERNS.md' .aion/skills/luse.md && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ]</automated>
  </verify>
  <done>Sync ran successfully, second invocation reported `0 new / 0 updated / N unchanged` (idempotency intact), all 4 shim targets carry source-sha markers, `.aion/skills/luse.md` contains at least one `PATTERNS.md` reference (proving Plan 247-01 cross-ref edits propagated), sacred SHA preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Commit shim deltas + SUMMARY + STATE/ROADMAP rollover</name>
  <files>.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md head -60 (Current Position block — needed to know current line numbers for rollover)
    - .planning/ROADMAP.md grep "Phase 247" -A 5 -B 1 (target row for status flip)
    - Plan 247-01's SUMMARY at `.planning/phases/247-luse-skill-v2-docs/247-01-SUMMARY.md` if it exists (for commit SHA references)
    - `git status --short` (verify only expected shim files + script-edit + planning files are staged)
  </read_first>
  <action>
    **Step 1 — Pre-commit hygiene:** Run `git status --short`. Expected modified/untracked:
    - 4 generic shims (`.aion/skills/luse.md`, `.opencode/skills/luse.md`, `.openclaw/skills/luse.md`)
    - `.claude/skills/luse/SKILL.md` + the existing 5 per-tool shims + (if script extended) up to 6 new `.claude/skills/luse/<NAME>.md` top-level shims
    - Possibly `scripts/sync-luse-skills.sh` if Task 1's audit required extension
    - This SUMMARY + STATE.md + ROADMAP.md updates

    NO unexpected files (no Mini PC deploy artifacts, no compiled JS, no .env touched). If anything else appears, investigate before committing.

    **Step 2 — Commit shim deltas (Commit A):**
    ```
    git add scripts/sync-luse-skills.sh .claude/skills/luse/ .aion/skills/luse.md .opencode/skills/luse.md .openclaw/skills/luse.md
    git commit -m "feat(247-02): propagate luse v2 docs to all 4 agent shims via sync-luse-skills.sh

    - <if script extended> Extended sync-luse-skills.sh manifest to include the 6 new top-level
      canonical docs (PATTERNS, TROUBLESHOOTING, ANTI-PATTERNS, INTEGRATION-RECIPES, KNOWN-LIMITS,
      CHEAT-SHEET) in the generic-shim payload and as standalone files under .claude/skills/luse/.
    - Regenerated all 4 shim targets via bash scripts/sync-luse-skills.sh.
    - source-sha markers refreshed; idempotency verified on second run (0 new / 0 updated / N unchanged).
    - Phase 242 D-242-B invariant preserved.
    - Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f for sdk-agent-runner.ts unchanged."
    ```
    Pre-commit hook (`[sacred-sha] PASS`) MUST succeed. If it fails, STOP — do not bypass with `--no-verify`.

    **Step 3 — Write `.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md`** using the standard SUMMARY template at `$HOME/.claude/get-shit-done/templates/summary.md`. Required content:
    - Frontmatter: `phase: 247-luse-skill-v2-docs`, `plan: 02`, `status: complete`, `requires: [{phase: 247, plan: 01, provides: "6 new canonical docs + per-tool cross-refs"}]`, `provides: ["Regenerated agent shims carrying v2 docs", "Verified idempotency post-extension"]`.
    - Performance block (started/completed/duration).
    - Accomplishments: which sync mode the script ran in (glob vs explicit-list-extended), file counts per shim, before/after source-sha values for at least one shim as evidence the marker rotated, second-run idempotency stdout line verbatim.
    - Decisions: D-247-02-A "Honored Phase 242 D-242-C — no Gemini shim added (orchestrator's `additional_context` mentioned `.gemini/...` but 247-CONTEXT.md and D-242-C both treat Gemini as MCP-discovery-only)."
    - Sacred SHA evidence: pre-commit hook output line + final `git hash-object liv/packages/core/src/sdk-agent-runner.ts`.
    - Commit SHA from Step 2.

    **Step 4 — STATE.md rollover:** Update the "Current Position" block to mark Phase 247 SHIPPED (or `2/2 plans`), preserve the previous phase entry one slot down. If milestone v44.0 has a tracker row, increment its completion count.

    **Step 5 — ROADMAP.md flip:** Find `### Phase 247:` row. Change status from `🟡 PLANNED 2026-05-28 (0/? plans)` to `✅ SHIPPED 2026-05-28 (2/2 plans)`. Add a ship-evidence one-liner referencing Commit A's SHA.

    **Step 6 — Commit bookkeeping (Commit B):**
    ```
    git add .planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(247-02): SUMMARY + STATE/ROADMAP rollover — Phase 247 SHIPPED"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md && grep -qE 'status: complete' .planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md && grep -qE 'Phase 247.*SHIPPED' .planning/ROADMAP.md && git log --oneline -2 | grep -qE 'feat\(247-02\)' && git log --oneline -2 | grep -qE 'docs\(247-02\)' && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ]</automated>
  </verify>
  <done>Two commits exist (`feat(247-02): ...` shim propagation + `docs(247-02): ...` SUMMARY/STATE/ROADMAP). SUMMARY file complete with frontmatter, sync evidence, sacred SHA preservation. ROADMAP shows Phase 247 ✅ SHIPPED. STATE.md Current Position rolled to Phase 247.</done>
</task>

</tasks>

<verification>
- `bash scripts/sync-luse-skills.sh` is idempotent on the second run (`0 new / 0 updated / N unchanged`) — Phase 242 D-242-B invariant intact.
- All 4 shim targets carry refreshed source-sha markers reflecting the post-247-01 canonical payload.
- At least one `PATTERNS.md` cross-reference visible in `.aion/skills/luse.md` (proves per-tool doc edits flowed through the concatenated payload).
- No `.gemini/skills/luse.md` created (D-242-C honored — Gemini discovers via MCP tool-discovery only, INTEGRATION-RECIPES.md documents this).
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` for `liv/packages/core/src/sdk-agent-runner.ts` preserved through both commits (pre-commit hook PASS on both).
- No Mini PC deploy artifacts, no compiled JS, no `.env` touches.
</verification>

<success_criteria>
- 4 shim targets regenerated with refreshed source-sha markers.
- 2 commits exist with correct shapes (`feat(247-02): ...` + `docs(247-02): ...`).
- `.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md` exists with status: complete.
- STATE.md Current Position reflects Phase 247 shipped.
- ROADMAP.md Phase 247 row flipped to ✅ SHIPPED 2026-05-28 (2/2 plans).
- Sacred SHA preserved through both commits.
- Idempotency self-check passes (second sync run reports 0 new / 0 updated).
</success_criteria>

<output>
After completion, `.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md` exists and is committed (Step 3 above is the artifact creation step).
</output>
