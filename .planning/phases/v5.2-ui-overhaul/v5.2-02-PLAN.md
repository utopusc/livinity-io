---
phase: v5.2-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/features/files/index.tsx
  - livos/packages/ui/src/features/files/components/sidebar/index.tsx
  - livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/index.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/path-bar/index.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/view-toggle.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/sort-dropdown.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/search-input.tsx
autonomous: true

must_haves:
  truths:
    - "Files layout has a refined sidebar with wider width, section headers with icons, and subtle depth"
    - "Files toolbar has a clean unified bar design with frosted glass background and grouped controls"
    - "Sidebar items have clear active/hover states with smooth transitions"
    - "Navigation controls (back/forward) have polished icon-only buttons"
    - "Path breadcrumbs display with chevron separators and hover highlights"
    - "All visual changes follow white/light theme palette"
  artifacts:
    - path: "livos/packages/ui/src/features/files/index.tsx"
      provides: "Updated Files layout with wider sidebar and refined spacing"
      contains: "grid-cols-1 lg:grid-cols"
    - path: "livos/packages/ui/src/features/files/components/sidebar/index.tsx"
      provides: "Redesigned sidebar with enhanced section styling"
      contains: "SidebarSection"
    - path: "livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx"
      provides: "Refined sidebar items with improved active/hover states"
      contains: "isActive"
    - path: "livos/packages/ui/src/features/files/components/listing/actions-bar/index.tsx"
      provides: "Redesigned toolbar with unified bar aesthetic"
      contains: "backdrop-blur"
  key_links:
    - from: "files/index.tsx"
      to: "sidebar/index.tsx"
      via: "component rendering in grid layout"
      pattern: "<Sidebar"
    - from: "files/index.tsx"
      to: "actions-bar/index.tsx"
      via: "component rendering"
      pattern: "<ActionsBar"
---

<objective>
Redesign the Files manager layout structure, sidebar, and toolbar for a premium, modern file manager aesthetic. This plan handles the structural shell: layout proportions, sidebar redesign, and toolbar redesign.

Purpose: Transform the file manager from its current functional-but-plain design into a professional, macOS-Finder-inspired file manager with refined spacing, frosted glass toolbar, and a sidebar with clear visual hierarchy.
Output: Updated layout, sidebar, and toolbar files with premium visual styling.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@livos/packages/ui/src/features/files/index.tsx
@livos/packages/ui/src/features/files/components/sidebar/index.tsx
@livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/index.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/view-toggle.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/sort-dropdown.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/search-input.tsx
@livos/packages/ui/src/features/files/components/listing/actions-bar/path-bar/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Files layout and sidebar</name>
  <files>
    livos/packages/ui/src/features/files/index.tsx
    livos/packages/ui/src/features/files/components/sidebar/index.tsx
    livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
  </files>
  <action>
  **A) Files layout (index.tsx)**

  Redesign the main layout grid for a more spacious, premium feel:

  1. Widen the sidebar from 188px to 220px: Change `lg:grid-cols-[188px_1fr]` to `lg:grid-cols-[220px_1fr]` and the SheetTitle `lg:min-w-[188px]` to `lg:min-w-[220px]`.

  2. Add a subtle separator between sidebar and content. After the Sidebar component (desktop only), add a vertical divider:
     ```tsx
     <div className='hidden lg:block w-px bg-border-subtle self-stretch' />
     ```
     Update the grid to `lg:grid-cols-[220px_1px_1fr]` to accommodate the divider.

  3. Increase the gap between sidebar and content area: change `gap-3 lg:gap-6` to `gap-3 lg:gap-0` (the divider handles separation).

  4. Keep ALL functional imports and logic (FilesDndWrapper, RewindOverlayProvider, FileViewer, Outlet, all dialogs, all hooks, all state management) EXACTLY as-is. Only change Tailwind classes and layout structure.

  **B) Sidebar (sidebar/index.tsx)**

  Redesign the sidebar for a cleaner, more refined look:

  1. Update the `SidebarSection` component styling:
     - Add slightly more vertical padding: change `gap-0.5 pr-4` to `gap-0.5 px-3`
     - Make section labels more prominent: change `px-2 py-1 text-caption-sm font-medium text-text-tertiary` to `px-1 py-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-tertiary/70`

  2. Update the `SidebarDivider` styling for a cleaner look:
     - Change from the radial gradient to a simple line: `className='my-2 mx-3 h-px bg-border-subtle'`

  3. Add more breathing room at top of sidebar: Add `pt-1` to the nav element className.

  4. Keep ALL functional logic (AnimatePresence, capabilities checks, visibility flags, hooks) EXACTLY as-is.

  **C) Sidebar items (sidebar-item.tsx)**

  Redesign sidebar items for clearer active/hover states:

  1. Update the outer Droppable wrapper classes. Replace the current className logic with:
     ```tsx
     className={cn(
       'flex w-full rounded-10 border border-transparent text-caption transition-all duration-150',
       disabled
         ? 'cursor-default opacity-50'
         : 'hover:bg-surface-1',
       isActive && !disabled
         ? 'bg-surface-1 border-border-subtle shadow-sm font-medium text-text-primary'
         : disabled
           ? 'text-text-tertiary'
           : 'text-text-secondary hover:text-text-primary',
     )}
     ```

  2. Update the inner button padding: change `px-2 py-1.5` to `px-2.5 py-[7px]` for slightly more breathing room.

  3. Increase icon-to-text gap: change `gap-1.5` to `gap-2`.

  4. Keep ALL functional logic (Droppable, FileItemIcon, onClick, disabled handling) EXACTLY as-is.
  </action>
  <verify>
  1. Read all three files and confirm class changes applied correctly.
  2. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20` to check for type errors.
  3. Confirm no functional code was altered (hooks, state, event handlers, imports of functional components).
  </verify>
  <done>
  - Files layout uses 220px sidebar with vertical divider
  - Sidebar sections have refined typography (uppercase tracking labels)
  - Sidebar dividers are clean horizontal lines
  - Sidebar items have clear active state (bg-surface-1 + border + shadow) and smooth hover
  - All functional behavior preserved (drag-drop, selection, navigation)
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign Files toolbar (actions bar)</name>
  <files>
    livos/packages/ui/src/features/files/components/listing/actions-bar/index.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/view-toggle.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/sort-dropdown.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/search-input.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/path-bar/index.tsx
  </files>
  <action>
  Redesign the toolbar for a unified, premium look — inspired by macOS Finder's toolbar:

  **A) Actions bar container (actions-bar/index.tsx)**

  1. Replace the current nav className with a unified frosted-glass toolbar bar:
     ```tsx
     <nav
       className={cn(
         'flex h-10 w-full min-w-0 items-center gap-2 rounded-xl',
         'bg-white/60 backdrop-blur-md',
         'border border-border-subtle',
         'px-2',
         !isEmbedded && 'lg:-mt-14',
       )}
       aria-label='File browser actions'
     >
     ```

  2. Keep the left/right split structure. Adjust gap from `gap-2` to `gap-1.5` in the right-side controls for tighter grouping.

  3. Add a thin separator between the navigation controls and the path bar. After `<NavigationControls />` and before the PathBar conditional, add:
     ```tsx
     <div className='h-5 w-px bg-border-subtle mx-1' />
     ```

  **B) Navigation controls (navigation-controls.tsx)**

  Read the file first, then update the back/forward buttons to be cleaner icon-only buttons:
  - Ensure buttons have `h-7 w-7 rounded-lg` sizing
  - Use `hover:bg-surface-1` for hover state
  - Remove any existing background on idle state
  - Icons should be `h-4 w-4 text-text-secondary`
  - Disabled state: `opacity-30 cursor-default` (no pointer events)

  **C) Path bar (path-bar/index.tsx)**

  Read the file first, then refine the breadcrumb styling:
  - Path segments should use `text-body-sm text-text-secondary hover:text-text-primary transition-colors`
  - Active/last segment: `text-text-primary font-medium`
  - Separator chevrons: use a small `>` or TbChevronRight icon sized `h-3 w-3 text-text-tertiary`
  - The overall path bar should have `flex items-center gap-0.5 min-w-0 overflow-hidden` to truncate nicely

  **D) View toggle (view-toggle.tsx)**

  Read the file first, then update for a segmented-control look:
  - Wrap the two toggle buttons in a container: `flex items-center rounded-lg bg-surface-1 p-0.5`
  - Active button: `bg-white shadow-sm rounded-md`
  - Inactive button: `hover:text-text-primary text-text-tertiary`
  - Both buttons: `h-6 w-7 flex items-center justify-center transition-all duration-150`
  - Icons: `h-3.5 w-3.5`

  **E) Sort dropdown (sort-dropdown.tsx)**

  Read the file first, then make the trigger button match the toolbar aesthetic:
  - Trigger: `h-7 px-2 rounded-lg text-caption text-text-secondary hover:bg-surface-1 hover:text-text-primary transition-colors`
  - Icon: `h-3.5 w-3.5`

  **F) Search input (search-input.tsx)**

  Read the file first, then refine:
  - Use a compact search with a magnifying glass icon
  - Container: `flex items-center gap-1.5 h-7 px-2 rounded-lg bg-surface-1 border border-border-subtle focus-within:border-brand/50 focus-within:ring-1 focus-within:ring-brand/20 transition-all`
  - Input: `bg-transparent text-caption text-text-primary placeholder:text-text-tertiary outline-none w-28 focus:w-36 transition-all`

  For all files: keep ALL functional logic (click handlers, state, hooks, dropdown items, search filtering) EXACTLY as-is. Only change Tailwind classes and minimal structural JSX.
  </action>
  <verify>
  1. Read all modified files and confirm class changes applied.
  2. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20` to check for type errors.
  3. Confirm functional behavior preserved: NavigationControls still calls navigate, ViewToggle still toggles view, SortDropdown still sorts, SearchInput still filters.
  </verify>
  <done>
  - Toolbar has unified frosted-glass bar container (rounded, backdrop-blur, border)
  - Navigation controls are clean icon-only buttons
  - Path bar has refined breadcrumbs with chevron separators
  - View toggle has segmented-control appearance
  - Sort dropdown matches toolbar aesthetic
  - Search input is compact with focus expansion
  - All functional behavior preserved
  </done>
</task>

</tasks>

<verification>
1. Files layout shows wider sidebar (220px) with vertical divider separator
2. Sidebar sections have uppercase tracking labels and clean dividers
3. Sidebar items have distinct active state with subtle shadow and border
4. Toolbar is a unified frosted-glass bar with grouped controls
5. All functional behavior preserved: navigation, view switching, sorting, search, drag-drop
6. TypeScript compiles cleanly
</verification>

<success_criteria>
- Files layout structure is visually premium with clear separation between sidebar and content
- Sidebar has refined typography hierarchy and clear item states
- Toolbar is a unified bar with consistent control styling
- No regression in any file management functionality
- White/light theme palette used throughout
</success_criteria>

<output>
After completion, create `.planning/phases/v5.2-ui-overhaul/v5.2-02-SUMMARY.md`
</output>
