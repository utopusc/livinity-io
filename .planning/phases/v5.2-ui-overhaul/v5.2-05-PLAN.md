---
phase: v5.2-ui-overhaul
plan: 05
type: execute
wave: 2
depends_on: ["v5.2-04"]
files_modified:
  - livos/packages/ui/src/modules/window/app-contents/app-store-routes/discover-window.tsx
  - livos/packages/ui/src/modules/window/app-contents/app-store-routes/shared-components.tsx
  - livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx
  - livos/packages/ui/src/modules/window/app-contents/app-store-routes/category-page-window.tsx
autonomous: true

must_haves:
  truths:
    - "App Store window version matches the sheet version's premium light-theme design"
    - "Window hero cards use the same pastel gradients as the sheet version"
    - "Window grid section uses bento layout with refined light-theme cards"
    - "Window panoramic cards have the same elevated design as sheet version"
    - "Window fan showcase cards match the sheet three-column section"
    - "App detail page in window has polished light-theme styling"
    - "Category page in window has consistent light-theme styling"
  artifacts:
    - path: "livos/packages/ui/src/modules/window/app-contents/app-store-routes/discover-window.tsx"
      provides: "Redesigned window discover page with light-theme hero cards"
      contains: "FeaturedHeroCard"
    - path: "livos/packages/ui/src/modules/window/app-contents/app-store-routes/shared-components.tsx"
      provides: "Redesigned window shared components with light-theme styling"
      contains: "FeaturedGridCard"
    - path: "livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx"
      provides: "Redesigned window app detail page"
      contains: "AppPageWindow"
    - path: "livos/packages/ui/src/modules/window/app-contents/app-store-routes/category-page-window.tsx"
      provides: "Redesigned window category page"
      contains: "CategoryPageWindow"
  key_links:
    - from: "discover-window.tsx"
      to: "shared-components.tsx"
      via: "component imports"
      pattern: "AppsGridSectionWindow|AppsRowSectionWindow|AppsThreeColumnSectionWindow"
    - from: "shared-components.tsx"
      to: "shared.tsx (module)"
      via: "style imports"
      pattern: "import.*from.*shared"
---

<objective>
Redesign the App Store window version to match the premium light-theme design applied to the sheet version in Plan 04. The window version has its own components (shared-components.tsx, discover-window.tsx) that mirror the sheet versions but use WindowAwareLink instead of Link.

Purpose: Ensure visual consistency between sheet and window versions of the App Store. The window version must match the premium editorial aesthetic established in Plan 04.
Output: Updated window App Store components with matching light-theme design.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v5.2-ui-overhaul/v5.2-04-SUMMARY.md
@livos/packages/ui/src/modules/window/app-contents/app-store-routes/discover-window.tsx
@livos/packages/ui/src/modules/window/app-contents/app-store-routes/shared-components.tsx
@livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx
@livos/packages/ui/src/modules/window/app-contents/app-store-routes/category-page-window.tsx
@livos/packages/ui/src/modules/app-store/shared.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign window discover page and hero cards</name>
  <files>
    livos/packages/ui/src/modules/window/app-contents/app-store-routes/discover-window.tsx
  </files>
  <action>
  Mirror the exact same design changes from the sheet version (discover.tsx from Plan 04) to discover-window.tsx.

  1. **GRADIENTS**: Update to match the sheet version's light-theme pastel gradients:
     ```tsx
     const GRADIENTS = [
       {
         from: 'from-blue-50', via: 'via-indigo-50/80', to: 'to-violet-50/50',
         orb1: 'bg-blue-200/40', orb2: 'bg-indigo-200/30',
         accent: 'text-blue-600',
         badge: 'bg-blue-100 text-blue-700 ring-blue-200',
         dot: 'bg-blue-500', glow: 'bg-blue-200/50',
       },
       {
         from: 'from-amber-50', via: 'via-orange-50/80', to: 'to-rose-50/50',
         orb1: 'bg-amber-200/40', orb2: 'bg-orange-200/30',
         accent: 'text-amber-600',
         badge: 'bg-amber-100 text-amber-700 ring-amber-200',
         dot: 'bg-amber-500', glow: 'bg-amber-200/50',
       },
     ]
     ```

  2. **FeaturedHeroCard**: Mirror the sheet version's changes:
     - Card border: `border border-neutral-200/80`
     - Remove or lighten the dark overlay: change `from-black/20 via-black/10` to `from-white/30 via-transparent`
     - App name: `text-xl font-bold tracking-tight text-neutral-900 md:text-2xl`
     - Tagline: `text-body-sm text-neutral-600`
     - Badge: ensure readable on light bg with `ring-1`
     - Shine sweep: `from-transparent via-white/40 to-transparent`
     - Arrow accent: use the gradient's accent color (text-blue-600 or text-amber-600)

  3. Keep ALL functional logic: WindowAwareLink (NOT Link — this is the window version), useWindowRouter, useAvailableApps, useDiscoverQuery, getAppById, Tilt, Spotlight, all section type routing, ErrorBoundary.

  CRITICAL: This file uses `WindowAwareLink` while the sheet version uses `Link`. Do NOT accidentally change WindowAwareLink to Link. The `to` paths in window version are relative (e.g., `/${app.id}`) not absolute (e.g., `/app-store/${app.id}`).
  </action>
  <verify>
  1. Read discover-window.tsx and confirm GRADIENTS match light-theme palette.
  2. Confirm all navigation uses WindowAwareLink, not Link.
  3. Confirm relative paths (/${app.id}, not /app-store/${app.id}).
  4. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20`.
  </verify>
  <done>
  - Window hero cards use matching pastel gradients
  - Light overlay replaces dark overlay
  - All navigation correctly uses WindowAwareLink with relative paths
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign window shared components and detail pages</name>
  <files>
    livos/packages/ui/src/modules/window/app-contents/app-store-routes/shared-components.tsx
    livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx
    livos/packages/ui/src/modules/window/app-contents/app-store-routes/category-page-window.tsx
  </files>
  <action>
  **A) Shared components (shared-components.tsx)**

  Mirror the light-theme design from Plan 04's grid/row/three-column sections:

  1. **AppWithDescriptionWindow**: Update hover styles for light theme:
     ```tsx
     className={cn(
       'group flex w-full items-start gap-3 rounded-xl p-3',
       'transition-all duration-200 ease-out',
       'hover:bg-neutral-50',
       'outline-none focus-visible:ring-2 focus-visible:ring-brand/30',
     )}
     ```
     - Icon shadow: `shadow-[0_1px_4px_rgba(0,0,0,0.08)]`
     - Name: `text-neutral-900`, Tagline: `text-neutral-500`

  2. **FeaturedGridCard**: Update for light theme:
     - Card: `bg-neutral-50/50 border-neutral-200/60 hover:border-neutral-300 hover:shadow-[0_4px_12px_rgba(0,0,0,0.06)]`
     - Accent bar: keep ColorThief accent color
     - Icon shadow: `shadow-[0_1px_4px_rgba(0,0,0,0.08)]`
     - Name: `text-neutral-900`, Tagline: `text-neutral-600`

  3. **CompactAppCard**: Update:
     - Hover: `hover:bg-neutral-50`
     - Icon shadow: `shadow-[0_1px_3px_rgba(0,0,0,0.06)]`
     - Name: `text-neutral-800`

  4. **AppsGridSectionWindow**: Update section container:
     - `bg-white border-neutral-200/80 shadow-[0_1px_3px_rgba(0,0,0,0.04)]`
     - Featured grid: keep layout, update card styles per above

  5. **PanoramicCard** (in AppsRowSectionWindow): Mirror the sheet row-section changes:
     - Card dimensions: change `h-[110px] w-[270px] md:h-[120px] md:w-[310px]` to `h-[120px] w-[280px] md:h-[140px] md:w-[320px]`
     - Border: `border-neutral-200/80 group-hover:border-neutral-300 group-hover:shadow-[0_8px_24px_rgba(0,0,0,0.08)]`
     - Background gradient: softer `linear-gradient(135deg, ${c1}15, ${c2}08, rgba(255,255,255,0.95))`
     - Decorative blobs: `bg-neutral-100/60` instead of `bg-surface-base`
     - Spotlight: `from-white/30 via-white/15 to-transparent`
     - Icon: `shadow-[0_2px_8px_rgba(0,0,0,0.1)]`
     - Name: `text-neutral-900`, Tagline: `text-neutral-500`

  6. **ShowcaseCard** (in AppsThreeColumnSectionWindow): Mirror fan showcase:
     - Background gradient: `linear-gradient(180deg, ${c1}25, ${c2}15, rgba(255,255,255,0.98))`
     - Border: `border-neutral-200/80`
     - Icon: `shadow-[0_4px_12px_rgba(0,0,0,0.1)]`
     - Bottom info area: `bg-white/90 backdrop-blur-sm` with border-t
     - "View" button: `bg-neutral-100 text-neutral-700 group-hover:bg-neutral-200`

  7. **AppsThreeColumnSectionWindow**: Update container:
     - `bg-gradient-to-br from-white to-neutral-50/80 border-neutral-200/80`
     - Overline: `text-[11px] font-bold uppercase tracking-[0.08em] text-brand`

  Keep ALL functional logic: WindowAwareLink, useColorThief, useIsMobile, Tilt, Spotlight, preloadFirstFewGalleryImages.

  **B) App page window (app-page-window.tsx)**

  Read the file first. Then update for light theme:
  - Card backgrounds: `bg-white` with `border-neutral-200/80`
  - Text colors: use `text-neutral-900` for headings, `text-neutral-500` for secondary text
  - Buttons: ensure they use the brand color system consistently
  - Screenshots/gallery: ensure borders use neutral-200
  - Keep ALL functional logic (app data, install/uninstall, gallery, description rendering)

  **C) Category page window (category-page-window.tsx)**

  Read the file first. Then update for light theme:
  - Page header: `text-neutral-900` for title
  - App list: consistent with AppWithDescriptionWindow styling
  - Keep ALL functional logic (category filtering, app listing, navigation)
  </action>
  <verify>
  1. Read shared-components.tsx and confirm light-theme colors throughout.
  2. Read app-page-window.tsx and category-page-window.tsx for light-theme consistency.
  3. Confirm all navigation uses WindowAwareLink, not Link.
  4. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20`.
  </verify>
  <done>
  - All window App Store components use light-theme palette
  - Visual consistency between sheet and window versions
  - Bento grid, panoramic cards, fan showcase all updated
  - App detail and category pages use consistent light-theme styling
  - All navigation correctly uses WindowAwareLink
  </done>
</task>

</tasks>

<verification>
1. Window discover page visually matches sheet discover page design
2. All window components use neutral/pastel light-theme colors
3. No dark-theme remnants in window components
4. WindowAwareLink used throughout (not Link)
5. Relative paths used for navigation within window context
6. TypeScript compiles cleanly
</verification>

<success_criteria>
- App Store window version is visually indistinguishable in quality from sheet version
- Both versions share the same premium editorial aesthetic
- Cards, gradients, and typography are consistent between versions
- All functional behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/v5.2-ui-overhaul/v5.2-05-SUMMARY.md`
</output>
