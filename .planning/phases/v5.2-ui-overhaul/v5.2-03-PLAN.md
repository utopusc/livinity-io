---
phase: v5.2-ui-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
  - livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx
  - livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx
  - livos/packages/ui/src/features/files/components/listing/directory-listing/empty-state.tsx
autonomous: true

must_haves:
  truths:
    - "Grid view file items have larger icons (72px), more padding, and hover states with subtle background"
    - "List view file items have cleaner row styling with proper column alignment"
    - "Empty directory state has a refined, centered design"
    - "Virtualization still works correctly with updated dimensions"
    - "All file types render correctly (folders, files, uploads, external drives, network)"
  artifacts:
    - path: "livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx"
      provides: "Redesigned grid view items with larger icons and hover states"
      contains: "h-[72px] w-[72px]"
    - path: "livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx"
      provides: "Redesigned list view rows with refined spacing"
      contains: "items-center"
    - path: "livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx"
      provides: "Updated virtualization dimensions matching new item sizes"
      contains: "itemWidth"
    - path: "livos/packages/ui/src/features/files/components/listing/directory-listing/empty-state.tsx"
      provides: "Refined empty state design"
      contains: "items-center justify-center"
  key_links:
    - from: "virtualized-list.tsx"
      to: "icons-view-file-item.tsx"
      via: "FileItem rendering in grid cells"
      pattern: "getGridDimensions"
    - from: "virtualized-list.tsx"
      to: "list-view-file-item.tsx"
      via: "FileItem rendering in list rows"
      pattern: "renderListRow"
---

<objective>
Redesign the Files grid view items, list view items, and empty states for a premium file manager look. Update virtualization dimensions to match new item sizes.

Purpose: The file items are the core of the file manager visual experience. Larger icons, better spacing, and refined hover states transform the feel from functional to premium.
Output: Updated file item components and virtualization config with consistent premium styling.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
@livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx
@livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx
@livos/packages/ui/src/features/files/components/listing/directory-listing/empty-state.tsx
@livos/packages/ui/src/features/files/components/listing/file-item/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign grid view file items with larger icons</name>
  <files>
    livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
    livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx
  </files>
  <action>
  **A) Icons view file item (icons-view-file-item.tsx)**

  Redesign for a more spacious, premium grid item:

  1. Increase overall item width from 112px (w-28) to 128px (w-32). Change the outer div:
     ```tsx
     <div
       className='relative flex h-full w-32 flex-col items-center gap-1.5 overflow-hidden text-ellipsis break-all rounded-xl p-3 text-center transition-colors duration-150 hover:bg-surface-1'
       onMouseEnter={() => setIsHovered(true)}
       onMouseLeave={() => setIsHovered(false)}
     >
     ```
     Note the additions: `rounded-xl`, `p-3` (was `p-2`), `gap-1.5` (was `gap-1`), `transition-colors duration-150 hover:bg-surface-1` for hover effect.

  2. Increase icon size from 56px to 72px. Change the FileItemIcon:
     ```tsx
     <FileItemIcon item={item} className='h-[72px] w-[72px]' useAnimatedIcon={!isTouchDevice} isHovered={isHovered} />
     ```
     Note: The `flex justify-center` wrapper div stays as-is.

  3. Update filename text styling. Change `text-caption` to `text-[12px]` for slightly better readability, and keep `line-clamp-2 leading-tight`.

  4. Update the type/size label: keep `text-caption text-text-tertiary` as-is.

  5. Update upload overlay: change `rounded-lg` to `rounded-xl` to match new item rounding.

  Keep ALL functional logic: isUploading checks, CircularProgress, EditableName, TruncatedFilename, isDirectoryAnExternalDrivePartition, etc.

  **B) Virtualized list - grid dimensions (virtualized-list.tsx)**

  Update `getGridDimensions` to match new 128px item width:

  1. Change `const itemWidth = 112` to `const itemWidth = 128`
  2. Change `const itemHeight = 120` to `const itemHeight = 140` (taller to accommodate larger icon + text)
  3. Change `const fixedVerticalGap = 24` to `const fixedVerticalGap = 16` (slightly less gap since items now have internal padding)

  Keep ALL other virtualization logic exactly as-is: InfiniteLoader, FixedSizeGrid, FixedSizeList, AutoSizer, scroll fade, overscan amounts, everything.
  </action>
  <verify>
  1. Read icons-view-file-item.tsx and confirm icon size is 72px, item width is w-32 (128px), hover state added.
  2. Read virtualized-list.tsx getGridDimensions and confirm itemWidth=128, itemHeight=140.
  3. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20` to check for type errors.
  </verify>
  <done>
  - Grid items are 128px wide with 72px icons
  - Items have rounded-xl corners and hover:bg-surface-1
  - Virtualization dimensions updated to match
  - All functional behavior preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign list view items and empty state</name>
  <files>
    livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx
    livos/packages/ui/src/features/files/components/listing/directory-listing/empty-state.tsx
  </files>
  <action>
  **A) List view file item (list-view-file-item.tsx)**

  Redesign for a cleaner, more refined list view:

  1. **Desktop view** (the non-mobile section starting around line 82):
     - Update `tableStyles` to: `'text-[13px] px-3 py-2.5 whitespace-nowrap overflow-hidden text-ellipsis'`
     - Change the outer row div: `className={cn('flex items-center rounded-lg transition-colors duration-100', isUploading && 'opacity-70')}` — note the added `rounded-lg` and `transition-colors duration-100` for future hover styling (hover comes from parent via CSS).
     - Increase icon size from `h-6 w-6` to `h-5 w-5` (actually the current 6 is fine, but ensure consistency). Change icon gap from `gap-1.5` to `gap-2.5` for more breathing room between icon and filename.
     - Make the name column slightly bolder for the filename: keep `text-caption` but ensure good contrast.
     - For the date, size, and type columns, use `text-text-tertiary` instead of `text-text-secondary` for a more subdued secondary data feel.

  2. **Mobile view** (lines 38-76):
     - Update the outer div: `className={cn('flex cursor-pointer items-center gap-3 rounded-xl px-3 py-2.5', isUploading && 'opacity-70')}` — increase gap to `gap-3` (was `gap-2`), use `rounded-xl`, adjust `py-2.5`.
     - Increase mobile icon size from `h-7 w-7` to `h-8 w-8`.
     - Mobile filename: keep `text-caption`, but ensure `font-medium` is applied for better readability.
     - Mobile subtitle: keep `text-caption-sm text-text-tertiary`.

  3. Keep ALL functional logic: isUploading, uploadingProgress, EditableName, TruncatedFilename, formatFilesystemDate, formatFilesystemSize, FILE_TYPE_MAP, all directory type checks — EXACTLY as-is.

  **B) Empty state (empty-state.tsx)**

  Refine the empty directory state for a premium, friendly feel:

  1. Update the EmptyStateDirectory outer div: `className='flex h-full flex-col items-center justify-center gap-4 p-8 pt-0 text-center'` (increase gap to 4, padding to p-8).

  2. Increase the empty folder icon size: change `h-15 w-15` to `h-20 w-20` for the EmptyFolderIcon, and `h-12 w-12` to `h-16 w-16` for the NAS icon.

  3. Update the text styling: change `text-caption text-text-tertiary` to `text-body-sm text-text-tertiary` for better readability.

  4. Add a subtle message below: wrap the text in a div with max-width for centered text:
     ```tsx
     <div className='max-w-[240px]'>
       <p className='text-body-sm text-text-tertiary'>
         {isOfflineNetworkHost ? t('files-empty.network-host-offline') : t('files-empty.directory')}
       </p>
     </div>
     ```

  5. Keep ALL functional logic: upload button, new folder button, UploadInput ref, read-only checks — EXACTLY as-is.

  6. For `EmptyStateNetwork`, similarly update: increase padding to `p-8`, text to `text-body-sm text-text-tertiary`.
  </action>
  <verify>
  1. Read list-view-file-item.tsx and confirm desktop row has rounded-lg and transition, mobile has gap-3 and rounded-xl.
  2. Read empty-state.tsx and confirm icon sizes increased, text uses text-body-sm.
  3. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20`.
  </verify>
  <done>
  - List view desktop rows have cleaner typography and consistent spacing
  - List view mobile rows have larger icons and more breathing room
  - Empty directory state has larger icons and friendlier text
  - All functional behavior preserved (upload, folder creation, read-only checks)
  </done>
</task>

</tasks>

<verification>
1. Grid items display at 128px width with 72px icons and hover effects
2. List view has refined spacing and typography
3. Empty states have larger, friendlier icons and improved text
4. Virtualization works correctly with new dimensions (no overlapping, no gaps)
5. All file types render correctly
6. TypeScript compiles cleanly
</verification>

<success_criteria>
- Grid view items are larger and more spacious with hover states
- List view items have clear column alignment with refined typography
- Empty states feel premium and friendly
- Virtualization parameters match new item dimensions
- No regression in file management functionality
</success_criteria>

<output>
After completion, create `.planning/phases/v5.2-ui-overhaul/v5.2-03-SUMMARY.md`
</output>
