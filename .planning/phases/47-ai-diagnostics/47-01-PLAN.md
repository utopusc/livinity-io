---
phase: 47-ai-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md
autonomous: false
requirements:
  - FR-MODEL-01
user_setup: []

must_haves:
  truths:
    - "47-01-DIAGNOSTIC.md captures live broker response.model literal from Mini PC"
    - "Document captures pnpm-store @nexus+core* dir count (>1 = drift signal)"
    - "Document captures readlink -f resolved /opt/livos/node_modules/@nexus/core path"
    - "Document captures grep result for systemPrompt identity-line marker in resolved dist"
    - "Document captures /proc/<claude-pid>/environ snapshot for any active claude CLI subprocess (or NONE if not running)"
    - "Document declares one of four verdicts: dist-drift / source-confabulation / both / neither"
    - "Verdict drives Plan 47-03 branch decision (A / B / C / N) at executor runtime"
  artifacts:
    - path: ".planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md"
      provides: "Pre-flight diagnostic ground truth + verdict for Plan 47-03"
      contains: "verdict: dist-drift"
  key_links:
    - from: ".planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md"
      to: ".planning/phases/47-ai-diagnostics/47-03-PLAN.md"
      via: "verdict field consumed by Plan 47-03 branch selector"
      pattern: "verdict:\\s*(dist-drift|source-confabulation|both|neither|clean)"
---

<objective>
Run the FR-MODEL-01 6-step diagnostic on the Mini PC ahead of any backend code so that Plan 47-03 ships the correct remediation branch (A/B/C/N) with zero guesswork. This plan is read-only — NO source changes, NO server-side mutations. It captures ground truth and writes a verdict document the executor consults at the start of Plan 47-03.

Purpose: Prevent the B-05 catastrophic pitfall (shipping a sacred-file edit when the bug is actually in `update.sh` dist-copy). Pitfall B-05 is rated BLOCKER. The 6-step diagnostic is the only safe way to choose between branches.

Output: `.planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md` with verdict (`dist-drift` / `source-confabulation` / `both` / `neither`) plus full evidence (raw command outputs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-ai-diagnostics/47-CONTEXT.md
@.planning/phases/47-ai-diagnostics/47-PATTERNS.md
@.planning/research/v29.4-PITFALLS.md
</context>

<gotchas>
- G-10 D-NO-SERVER4 hard-wall: target ONLY `bruce@10.69.31.68`. Refuse if any command resolves to `45.137.194.103` (Server4) or `45.137.194.102` (Server5). Memory hard-rule (2026-04-27): "Server 4 is NOT yours."
- B-05 (BLOCKER): wrong-root-cause fix. Source-edit when bug is in dist-copy = silent failure. The 6 steps MUST all complete before the executor decides on a branch.
- Memory: `update.sh` pnpm-store quirk — copies dist into FIRST `@nexus+core*` dir matched by `find -maxdepth 1`. If multiple resolution dirs exist (sharp version drift), wrong dir gets the fresh dist.
- M-09: pnpm 9.x may resolve `@nexus/core` symlink layout differently. Capture `readlink -f` AND `ls -la` of the path so Plan 47-03 has fallback evidence.
- The diagnostic uses SSH from the developer workstation (Windows host) into the Mini PC. SSH key path: `C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master`. Use `/c/Windows/System32/OpenSSH/ssh.exe -i ... -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 '<cmd>'` per memory.
- Step 1 broker probe: livinityd listens on port 8080. The broker route requires a userId path segment. Check existing route shape via `grep -n 'v1/messages' /opt/livos/packages/livinityd/source/modules/livinity-broker/router.ts` first to confirm the URL template before curling.
- `/proc/<claude-pid>/environ` may be empty if claude CLI is not currently running. That is FINE — record "NONE (claude CLI not active at probe time)". Do not fail the plan.
</gotchas>

<tasks>

<task type="auto">
  <name>Task 1: SSH pre-flight + capture pnpm-store + dist resolution + identity-line marker presence</name>
  <files>
    - .planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md (new)
  </files>
  <read_first>
    - .planning/phases/47-ai-diagnostics/47-CONTEXT.md sections "Implementation Decisions > FR-MODEL-01/02"
    - .planning/research/v29.4-PITFALLS.md B-05 (the 6-step diagnostic recipe)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md "Mini PC" + "update.sh pnpm-store quirk" + "Server4 hard rule"
  </read_first>
  <action>
    Execute the following SSH commands sequentially against `bruce@10.69.31.68` ONLY (per D-NO-SERVER4 / G-10) and capture verbatim stdout into `47-01-DIAGNOSTIC.md`. Refuse to run if any command target resolves to Server4 (`45.137.194.103`) or Server5 (`45.137.194.102`).

    SSH wrapper (PowerShell host, run via Bash tool):
    `SSH='/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68'`

    The 6 diagnostic steps (capture each output BLOCK verbatim into the document):

    **Step 1: Broker probe — fire deterministic identity prompt.**
    First confirm broker route shape: `$SSH 'sudo grep -n "v1/messages" /opt/livos/packages/livinityd/source/modules/livinity-broker/router.ts | head -5'`
    Identify a real userId (admin) from PG: `$SSH "sudo -u postgres psql -d livos -tAc \"SELECT id FROM users WHERE role='admin' ORDER BY created_at LIMIT 1;\""`
    Then curl the broker (use the userId from previous step): `$SSH "curl -sS -X POST http://localhost:8080/u/<USERID>/v1/messages -H 'Content-Type: application/json' -H 'anthropic-version: 2023-06-01' -d '{\"model\":\"claude-opus-4-7\",\"max_tokens\":50,\"messages\":[{\"role\":\"user\",\"content\":\"What model are you? Respond with only the exact model ID and nothing else.\"}]}'"` — capture FULL response JSON. Extract `response.model` literal string.

    **Step 2: Inspect response.model field literal.**
    Pull `model` field from Step 1 JSON. Record verbatim. Flag if it disagrees with `claude-opus-4-7` (the deterministic expected value per memory's tier mapping commit `9f1562be feat(43.12): bump tierToModel to Claude 4.X`).

    **Step 3: /proc/<claude-pid>/environ snapshot.**
    `$SSH "pgrep -af claude || echo NONE"` — capture pid list (or NONE).
    For each pid (best-effort, swallow ENOENT): `$SSH "sudo cat /proc/<PID>/environ 2>/dev/null | tr '\0' '\n' | grep -E '^(ANTHROPIC|CLAUDE|HOME|PATH)=' || true"`. If no pids, record "NONE (claude CLI not active at probe time)".

    **Step 4: pnpm-store dir count for @nexus+core*.**
    `$SSH 'ls -la /opt/livos/node_modules/.pnpm/ | grep -E "@nexus\+core" || echo NONE_FOUND'` — capture verbatim. Count the matching lines. If count > 1 → flag `pnpm_store_drift_risk: true`. Record count.

    **Step 5: readlink -f resolved nexus/core path.**
    `$SSH 'readlink -f /opt/livos/node_modules/@nexus/core 2>&1; echo --- ; ls -la /opt/livos/node_modules/@nexus/core 2>&1'` — capture both outputs. The resolved path is the one livinityd actually imports from at runtime.

    **Step 6: Grep deployed dist for system-prompt identity-line marker.**
    Identity-line marker per `nexus/packages/core/src/sdk-agent-runner.ts:269` is `_identityLine`. The DEPLOYED dist will have it tree-shaken or inlined. Use a stable substring instead:
    `$SSH 'RESOLVED=$(readlink -f /opt/livos/node_modules/@nexus/core); echo "Resolved dir: $RESOLVED"; ls -la "$RESOLVED/dist/" 2>&1 | head -5; echo "---"; grep -c "You are powered by the model named" "$RESOLVED/dist/index.js" 2>&1 || echo "GREP_MISS"; echo "---"; stat -c "%Y %n" "$RESOLVED/dist/index.js" 2>&1'`
    Record marker count + dist file mtime. If grep returns 0 / GREP_MISS → marker MISSING in deployed dist (signals pnpm-store drift OR a build that excluded it).

    Now compute the verdict per Pattern Map's 4-bucket rule:
    - `dist-drift`: Step 4 reports >1 `@nexus+core*` dir AND Step 6 marker count is 0/GREP_MISS in resolved dir.
    - `source-confabulation`: Step 6 marker count > 0 in resolved dir BUT Step 1's `response.model` field disagrees with deterministic expected `claude-opus-4-7`.
    - `both`: both conditions met.
    - `neither` (or `clean`): Step 1 returns expected model AND marker present AND only 1 pnpm-store dir → no remediation needed; Plan 47-03 ships diagnostic surface only and exits without source changes.

    Write `.planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md` with this exact structure:
    ```markdown
    # Phase 47 Pre-Flight Diagnostic — 6-Step On-Mini-PC Capture

    **Captured:** <ISO date>
    **Target:** bruce@10.69.31.68 (Mini PC ONLY — D-NO-SERVER4)
    **Operator:** Plan 47-01 executor

    ## Verdict

    **verdict: <dist-drift|source-confabulation|both|neither>**

    Reasoning: <1-3 sentences keyed to the steps below>

    ## Step 1 — Broker probe (response.model)
    - URL: http://localhost:8080/u/<userId>/v1/messages
    - Request body: <prompt JSON>
    - Response (full JSON):
      ```json
      <verbatim>
      ```
    - response.model literal: `<value>`
    - Expected (per tierToModel 43.12): `claude-opus-4-7`
    - Match: <yes|no>

    ## Step 2 — response.model interpretation
    <interpretation>

    ## Step 3 — /proc/<claude-pid>/environ
    - pgrep -af claude output:
      ```
      <verbatim>
      ```
    - environ snapshots: <or NONE>

    ## Step 4 — pnpm-store dir count
    - ls -la output:
      ```
      <verbatim>
      ```
    - @nexus+core* dir count: <N>
    - drift risk: <true|false>

    ## Step 5 — readlink -f resolved path
    - resolved: <path>
    - ls -la output:
      ```
      <verbatim>
      ```

    ## Step 6 — Identity-line marker grep + dist mtime
    - resolved dist: <path>
    - grep "You are powered by the model named" count: <N>
    - dist/index.js mtime: <epoch + path>
    - marker present: <yes|no>

    ## Branch Decision (consumed by Plan 47-03)
    - Branch A (dist-drift): <yes|no>
    - Branch B (source-confabulation): <yes|no>
    - Branch C (both): <yes|no>
    - Branch N (neither): <yes|no>

    ## Raw command transcript
    <verbatim full SSH session output for audit>
    ```

    Per D-09 (B-05 mitigation): if any step fails to execute (SSH timeout, command not found, livinityd not running), record the failure in the doc and set verdict to `inconclusive` instead of guessing. Do NOT proceed to Plan 47-03 in `inconclusive` state — instead surface the failure to the operator.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md` exists.
    - File contains the literal string `verdict:` followed by exactly one of: `dist-drift`, `source-confabulation`, `both`, `neither`, `inconclusive`.
    - File contains all 6 step headings (`## Step 1`, `## Step 2`, ... `## Step 6`).
    - File contains literal `bruce@10.69.31.68` in target line; file does NOT contain `45.137.194.103` or `45.137.194.102`.
    - Grep verifiable: `grep -E 'verdict:\s*(dist-drift|source-confabulation|both|neither|inconclusive)' 47-01-DIAGNOSTIC.md` returns 1 match.
    - Grep verifiable: `grep -c 'bruce@10.69.31.68' 47-01-DIAGNOSTIC.md` returns >= 1.
    - Grep verifiable: `grep -c '45.137.194' 47-01-DIAGNOSTIC.md` returns 0 (no Server4/5 references).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md','utf8'); const v=t.match(/verdict:\s*(dist-drift|source-confabulation|both|neither|inconclusive)/); if(!v){process.exit(1)} if(t.includes('45.137.194')){process.exit(2)} if(!t.includes('bruce@10.69.31.68')){process.exit(3)} for(let i=1;i<=6;i++){if(!t.includes('## Step '+i)){process.exit(10+i)}} console.log('OK verdict='+v[1]);"</automated>
  </verify>
  <done>
    47-01-DIAGNOSTIC.md exists with valid verdict line, all 6 step sections, references Mini PC only (no Server4/5). The verdict value will drive Plan 47-03 branch selection.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms verdict before Plan 47-03 enters Branch B (sacred-file edit)</name>
  <files>(checkpoint — no files modified by this task)</files>
  <action>This is a checkpoint:human-verify gate. Pause execution and present the captured 47-01-DIAGNOSTIC.md verdict to the operator for explicit approval. See <how-to-verify> below for the operator-facing instructions and <resume-signal> for the expected response. The executor MUST NOT proceed past this task without an explicit "approved" signal (or rerun on "rerun-47-01").</action>
  <verify><automated>echo "checkpoint-task-no-automated-verify"</automated></verify>
  <done>Operator typed "approved" (verdict locked, Plan 47-03 branch selection unblocked) OR "rerun-47-01" (this plan re-runs).</done>
  <what-built>
    47-01-DIAGNOSTIC.md with one of {dist-drift, source-confabulation, both, neither, inconclusive}.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/47-ai-diagnostics/47-01-DIAGNOSTIC.md`.
    2. Read the **Verdict** section AND the **Branch Decision** table.
    3. Cross-check the raw command transcript at the bottom of the doc — does the evidence support the verdict? Specifically:
       - If verdict = `source-confabulation` or `both`: Plan 47-03 will edit the sacred file `nexus/packages/core/src/sdk-agent-runner.ts` per D-40-01 ritual. This is a SECOND v29.4 re-pin (Phase 45 was the first, audit-only). Confirm you accept this.
       - If verdict = `dist-drift`: Plan 47-03 edits `update.sh` ONLY. No sacred-file change.
       - If verdict = `neither`: Plan 47-03 ships diagnostic surface only — no remediation commits.
       - If verdict = `inconclusive`: STOP. Do not run Plan 47-03 yet. Re-run Plan 47-01 after fixing the failed step (likely SSH or livinityd availability).
    4. Confirm the diagnostic was actually executed against `bruce@10.69.31.68` (search the transcript) and NOT against Server4 (`45.137.194.103`).
  </how-to-verify>
  <resume-signal>
    Type "approved" to lock the verdict and unblock Plan 47-02 (FR-TOOL is independent of verdict) AND Plan 47-03 branch selection. If verdict = `inconclusive`, type "rerun-47-01" instead.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → SSH → Mini PC | Operator credentials cross. SSH key `contabo_master` is the only auth. |
| livinityd broker /v1/messages | Reads from broker; broker re-issues to Anthropic via Agent SDK. No new attack surface — read-only probe. |
| /proc filesystem read | Best-effort root read on Mini PC. Already root per D-LIVINITYD-IS-ROOT. No new privilege. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Information Disclosure | Diagnostic transcript leaks internal IPs / userIds | mitigate | Document is in `.planning/` (committed-to-git) — strip any tokens / OAuth headers from environ snapshot before writing. Capture only `^(ANTHROPIC\|CLAUDE\|HOME\|PATH)=` envs. |
| T-47-01-02 | Tampering | Operator edits 47-01-DIAGNOSTIC.md to fake a verdict | accept | Plan 47-03 executor reads verdict + raw transcript; mismatched verdict surfaces in review. Operator-trust assumption is baseline (single-developer + Claude workflow). |
| T-47-01-03 | Denial of Service | Broker probe under high load delays livinityd response | accept | Single low-volume probe (50 max tokens, single message). Negligible. |
| T-47-01-04 | Spoofing | Diagnostic accidentally targets Server4/5 | mitigate | Hard-coded target string `bruce@10.69.31.68`; acceptance criteria grep refuses Server4/5 IPs in output. G-10. |
</threat_model>

<verification>
The verdict drives Plan 47-03 branch selection. There is no other consumer in this phase. UAT (Plan 47-05's 47-UAT.md) re-runs FR-MODEL-01 on Mini PC AFTER Plan 47-03's remediation lands and asserts post-fix verdict == `clean`.
</verification>

<success_criteria>
- 47-01-DIAGNOSTIC.md committed to `.planning/phases/47-ai-diagnostics/`.
- Verdict declared in the file matches the evidence in the raw transcript section.
- Operator approval logged via Task 2 checkpoint resume-signal.
- No Server4 / Server5 references in any captured output.
</success_criteria>

<output>
After completion, create `.planning/phases/47-ai-diagnostics/47-01-SUMMARY.md` recording:
- Verdict
- Plan 47-03 branch that will run (A / B / C / N)
- Whether Plan 47-03 will touch the sacred file (true if verdict ∈ {source-confabulation, both})
- Top 3 raw evidence lines that drove the verdict
</output>
