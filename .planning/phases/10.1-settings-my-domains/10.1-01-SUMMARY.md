---
phase: 10.1-settings-my-domains
plan: 01
subsystem: ui
tags: [react, settings, domains, caddy, trpc, shadcn-dialog]

# Dependency graph
requires:
  - phase: 09-tunnel-sync-livos-domain-receiver
    provides: domain.platform.listCustomDomains, updateAppMapping, removeCustomDomain tRPC routes
  - phase: 10-livos-domain-ui
    provides: DomainsTab reference implementation in server-control
provides:
  - MyDomainsSection component in Settings with Configure dialog
  - Settings menu item "My Domains" replacing "Domain & HTTPS"
  - Conditional Caddy integration in updateAppMapping (direct mode only)
affects: [settings, custom-domains, caddy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Configure dialog pattern: button opens Dialog with Select + Apply (not inline mutation)"
    - "Conditional Caddy: check tunnel status before reconfiguring Caddy"

key-files:
  created:
    - livos/packages/ui/src/routes/settings/_components/my-domains-section.tsx
  modified:
    - livos/packages/livinityd/source/modules/platform/routes.ts
    - livos/packages/ui/src/routes/settings/_components/settings-content.tsx

key-decisions:
  - "Reused existing domain.platform.* tRPC routes instead of creating duplicate domain.myDomains.* routes"
  - "Caddy reconfiguration gated on tunnel status: only in direct mode (relay handles TLS in tunnel mode)"
  - "Configure button + Dialog pattern instead of inline Select mutation (per CONTEXT.md)"

patterns-established:
  - "Settings Configure dialog: button per card opens Dialog with form + Apply button"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-03-27
---

# Phase 10.1 Plan 01: Settings My Domains Summary

**Replaced "Domain & HTTPS" wizard in Settings with "My Domains" section showing tunnel-synced domains from livinity.io, with Configure dialog for app mapping and conditional Caddy integration**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-27T04:37:55Z
- **Completed:** 2026-03-27T04:45:16Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Settings menu now shows "My Domains" with description "Domains synced from livinity.io" instead of old "Domain & HTTPS" wizard
- MyDomainsSection component displays domains from domain.platform.listCustomDomains with status badges, current mapping, and Configure button
- Configure button opens shadcn Dialog with app mapping Select and Apply button (mutation fires only on Apply, not inline)
- Empty state shows "No domains yet" with link to livinity.io dashboard
- updateAppMapping backend now conditionally calls applyCaddyConfig() only when tunnel is NOT connected

## Task Commits

Each task was committed atomically:

1. **Task 1: Add conditional Caddy integration to updateAppMapping** - `3a478de` (feat)
2. **Task 2: Create MyDomainsSection component with Configure dialog** - `6c034d5` (feat)
3. **Task 3: Swap Settings menu item and section routing** - `e75a328` (feat)

## Files Created/Modified
- `livos/packages/livinityd/source/modules/platform/routes.ts` - Added applyCaddyConfig import and conditional Caddy reconfiguration after app mapping update
- `livos/packages/ui/src/routes/settings/_components/my-domains-section.tsx` - New MyDomainsSection component with domain list, status badges, Configure dialog, empty state
- `livos/packages/ui/src/routes/settings/_components/settings-content.tsx` - Replaced 'domain' with 'my-domains' in type union, menu item, and switch; removed old DomainSection/DomainSetupInner

## Decisions Made
- Reused existing `domain.platform.*` tRPC routes (listCustomDomains, updateAppMapping) instead of creating duplicate `domain.myDomains.*` routes per CONTEXT.md -- routes are functionally identical, already in httpOnlyPaths
- Caddy integration gated on `livos:platform:status` Redis key -- in tunnel mode the relay handles TLS, so Caddy stays on `:80`; only reconfigure Caddy in direct mode
- Used `motion/react` import (matching settings-content pattern) instead of `framer-motion` (used in domains-tab reference)
- No remove button in Settings -- per CONTEXT.md deferred decisions, domain removal done on livinity.io

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed CaddyConfig interface field name**
- **Found during:** Task 1 (Caddy integration)
- **Issue:** Plan code block used `domain` field but CaddyConfig interface uses `mainDomain`
- **Fix:** Used `mainDomain: mainConfig.domain` to match CaddyConfig interface
- **Files modified:** livos/packages/livinityd/source/modules/platform/routes.ts
- **Verification:** TypeScript compiles without new errors
- **Committed in:** 3a478de (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minor field name correction. No scope creep.

## Issues Encountered
- Pre-existing TypeScript errors in `ai/routes.ts` (ctx.livinityd possibly undefined) -- out of scope, not related to this plan's changes
- `tsc` not available at workspace root -- used package-local tsc binary for type checking; UI build verification used as primary check

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Settings My Domains section is fully wired to existing backend routes
- Domains will appear once livinity.io tunnel sync populates Redis/PG
- Old domain-setup.tsx and domain-setup-dialog.tsx preserved (still used by /settings/domain-setup route)

## Self-Check: PASSED

All 3 created/modified files verified on disk. All 3 task commits verified in git log.

---
*Phase: 10.1-settings-my-domains*
*Completed: 2026-03-27*
