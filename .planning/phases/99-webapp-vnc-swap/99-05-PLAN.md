---
phase: 99-webapp-vnc-swap
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/98-uat-polish/UAT-CHECKLIST.md
  - .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - V33-VNC-04
  - V33-VNC-05
tags:
  - deploy
  - uat
  - mini-pc
  - rollup
  - sacred-sha
user_setup:
  - service: minipc-deploy
    why: "Mini PC live deploy via update.sh + user-walked UAT row signoff. The phase cannot ship without observable end-to-end success on bruce@10.69.31.68."
    env_vars: []
    dashboard_config:
      - task: "Open ZeroTier client and confirm Mini PC peer (10.69.31.68) is reachable"
        location: "Local machine — ZeroTier UI"
      - task: "Have Chrome/Brave open at https://bruce.livinity.io ready for the WebApp click test"
        location: "Local machine — browser"
must_haves:
  truths:
    - "git push to origin/master succeeds (already-deployed via update.sh after rsync)"
    - "Mini PC `bash /opt/livos/update.sh` completes without errors and restarts livos.service cleanly"
    - "User-walked UAT: clicking a WebApp icon in the LivOS UI opens a stream window where noVNC's RFB handshake succeeds (no 'Invalid server version ftypiso' error)"
    - "User-walked UAT: clicking inside the noVNC canvas fires xdotool input on the Mini PC and Chrome reacts (mouse + keyboard pass through)"
    - "UAT-CHECKLIST.md gains one new row for 'WebApp window opens with live RFB stream + bidirectional input' marked PASS"
    - "99-SUMMARY.md captures the full phase rollup (5 plans, ~7-9 commits, sacred SHA preserved throughout)"
    - "ROADMAP.md Phase 99 entry flips from `[ ]` to `[x]`"
    - "STATE.md updated to reflect v33 milestone CODE-COMPLETE-PLUS-HOTFIX (P99 closed)"
  artifacts:
    - path: ".planning/phases/99-webapp-vnc-swap/99-SUMMARY.md"
      provides: "Phase rollup: tasks shipped + commit ranges + tests added + sacred SHA verification + carryovers"
      min_lines: 60
      contains: "f3538e1d811992b782a9bb057d1b7f0a0189f95f"
    - path: ".planning/phases/98-uat-polish/UAT-CHECKLIST.md"
      provides: "New row appended documenting the post-99-04 UAT walk outcome"
      contains: "WebApp window opens with live RFB stream"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 99 entry marked [x]"
      contains: "[x] **Phase 99"
    - path: ".planning/STATE.md"
      provides: "Updated to reflect P99 close + all-of-v33 status"
      contains: "v33"
  key_links:
    - from: "git push origin master"
      to: "GitHub utopusc/livinity-io"
      via: "https push"
      pattern: "remote.*origin"
    - from: "bash /opt/livos/update.sh on Mini PC"
      to: "/tmp/livinity-update-* clone + rsync to /opt/livos + systemctl restart livos liv-core liv-worker liv-memory"
      via: "ssh bruce@10.69.31.68"
      pattern: "update\\.sh"
    - from: "user clicks WebApp icon in LivOS UI"
      to: "noVNC RFB handshake against /ws/stream/:streamId"
      via: "use-webapp-vnc.ts → WebSocket → bridge → x11vnc"
      pattern: "RFB 003\\.008"
---

<objective>
Close Phase 99: push commits to GitHub, walk the user through the Mini PC deploy + end-to-end UAT, write the phase rollup, append the UAT row, flip ROADMAP + STATE.

Purpose: Phases 99-01 through 99-04 ship code (and one canonical-recipe doc) but do NOT prove the swap actually works in production. This plan is the verification + closure step. Per `feedback_milestone_uat_gate.md`, no phase ships without a user-walked UAT — and per the live UAT mismatch that triggered Phase 99 in the first place, we cannot trust the test suite alone to confirm RFB↔noVNC compatibility.

Per D-99-12 (D-NO-SERVER4), Mini PC `bruce@10.69.31.68` is the ONLY deploy target. Server4 is NOT touched.

Per `reference_zerotier_unstable.md`, the deploy command MUST be detached (nohup + log + bg poll), NOT held in foreground SSH for >30s.

Per D-99-10 (sacred SHA), every commit through this phase has been verified, and this plan re-verifies one final time post-deploy + post-UAT.

Output:
  - GitHub `master` advanced to include all Phase 99 commits.
  - Mini PC at `/opt/livos/` running the new code (livos.service restarted cleanly).
  - One new row in `.planning/phases/98-uat-polish/UAT-CHECKLIST.md`.
  - `99-SUMMARY.md` written.
  - `ROADMAP.md` Phase 99 flipped to `[x]`.
  - `STATE.md` updated.
  - Final commit landing the rollup docs.

This plan is `autonomous: false` because the deploy + UAT walk requires the user (Mini PC SSH, browser-based observation of the noVNC handshake, mouse/keyboard test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/99-webapp-vnc-swap/99-CONTEXT.md
@.planning/phases/99-webapp-vnc-swap/99-RESEARCH.md
@.planning/phases/99-webapp-vnc-swap/99-VALIDATION.md
@.planning/phases/99-webapp-vnc-swap/99-01-SUMMARY.md
@.planning/phases/99-webapp-vnc-swap/99-02-SUMMARY.md
@.planning/phases/99-webapp-vnc-swap/99-03-SUMMARY.md
@.planning/phases/99-webapp-vnc-swap/99-04-SUMMARY.md
@.planning/phases/93-window-manager/93-SUMMARY.md
@.planning/phases/98-uat-polish/UAT-CHECKLIST.md

<interfaces>
<!-- The git push remote (existing). -->
git remote: origin → utopusc/livinity-io (master)

<!-- The Mini PC deploy script (existing — NOT modified by this phase). -->
/opt/livos/update.sh
  - clones from GitHub utopusc/livinity-io to /tmp/livinity-update-*
  - rsyncs source to /opt/livos
  - runs pnpm install + npm install
  - builds @livos/config, UI (vite), liv core/worker/mcp-server (tsc)
  - systemctl restart livos liv-core liv-worker liv-memory
  - ⚠ MEMORY note: livinityd runs TypeScript directly via tsx — no compilation
    needed for livinityd. liv-core MUST be rebuilt (compiled JS at dist/).
    Phase 99 only touches livinityd → no liv-core rebuild needed; update.sh
    handles all of this automatically.

<!-- Mini PC SSH (memory pattern): -->
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@10.69.31.68
  with sudo for /opt/livos/ paths

<!-- ZeroTier instability mitigation: -->
Detach update.sh into nohup + log file; do NOT hold foreground SSH for >30s.

<!-- UAT-CHECKLIST.md row format (from .planning/phases/98-uat-polish/UAT-CHECKLIST.md
     — read existing rows for the exact column shape): -->
| section | step | result | notes |

<!-- ROADMAP.md Phase 99 entry (lines 144 of current ROADMAP.md): -->
- [ ] **Phase 99: WebApp VNC Swap — fMP4 → x11vnc** (V33-VNC-01..05) — ...

<!-- POST-99-05: -->
- [x] **Phase 99: WebApp VNC Swap — fMP4 → x11vnc** (V33-VNC-01..05) — ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git push origin master + draft 99-SUMMARY.md (pre-deploy state)</name>
  <files>.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md</files>
  <read_first>
    - .planning/phases/99-webapp-vnc-swap/99-01-SUMMARY.md
    - .planning/phases/99-webapp-vnc-swap/99-02-SUMMARY.md
    - .planning/phases/99-webapp-vnc-swap/99-03-SUMMARY.md
    - .planning/phases/99-webapp-vnc-swap/99-04-SUMMARY.md
    - .planning/phases/93-window-manager/93-SUMMARY.md (the SUMMARY format to mirror)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
**Step A — verify the commit chain locally before push:**

```bash
git log --oneline master..HEAD 2>/dev/null || git log --oneline -10
# Confirm the commit subjects:
#   docs(99-01): record Mini PC x11vnc -id <wid> live verification (PASS)
#   test(99-02): add failing vitest spec for vnc-bridge.ts
#   feat(99-02): implement vnc-bridge.ts (spawn x11vnc + WS↔TCP byte pipe)
#   refactor(99-03): introduce discriminated-union StreamSession (kind:'fmp4')
#   test(99-03): add 5 failing vitest cases for vnc-window kind
#   feat(99-03): startStream({mode:"vnc-window"}) + getSession + stopStream(vnc)
#   feat(99-04): WebAppWindowManager.spawn() swaps to mode:'vnc-window'
#   feat(99-04): /ws/stream/:streamId dispatches on session.kind (fmp4|vnc)
```

Verify sacred SHA across the commit range:
```bash
for SHA in $(git log --format=%H master..HEAD 2>/dev/null || git log --format=%H -8); do
  echo "$SHA $(git show $SHA:liv/packages/core/src/sdk-agent-runner.ts | git hash-object --stdin)"
done
# Every line must end with f3538e1d811992b782a9bb057d1b7f0a0189f95f
```

If any line shows a different SHA → ABORT. The sacred file was modified somewhere — escalate to user with the offending commit's `git show` output.

**Step B — push to GitHub:**

```bash
git push origin master 2>&1 | tail -10
# Expected: branch is up-to-date OR commits pushed cleanly.
# If push is rejected with non-fast-forward → STOP, escalate to user
# (probably means master moved on remote; do not force).
```

**Step C — draft `.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md`** (write a placeholder; the post-UAT update lands in Task 4 after the user signs off):

Use the Write tool to create the file with this content:

```markdown
# Phase 99 — WebApp VNC Swap (fMP4 → x11vnc) — SUMMARY

**Status:** CODE-COMPLETE <today's ISO date>; PENDING Mini PC deploy + user-walked UAT (this plan).
**Sacred SHA:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNTOUCHED before AND after every commit.
**Pushed:** Pending Task 1B push.
**Phase trigger:** UAT-discovered protocol mismatch on 2026-05-08 — backend was emitting fMP4 (`ftypisom...`) over `/ws/stream/:streamId` but frontend `use-webapp-vnc.ts` is a `@novnc/novnc 1.6.0` RFB client expecting `RFB 003.008\n`. The swap replaces the per-WebApp window backend with `x11vnc -id <wid>` so the existing wire endpoint speaks RFB.

## Plans shipped

| Plan | Status | Commit range | Headline |
|------|--------|--------------|----------|
| 99-01 | DONE | <sha> | Mini PC live verification PASS — canonical x11vnc argv locked |
| 99-02 | DONE | <sha>..<sha> | vnc-bridge.ts + 9 vitest cases (RED→GREEN) |
| 99-03 | DONE | <sha>..<sha>..<sha> | StreamSession discriminated union + vnc-window mode + getSession + stopStream cascade (refactor + RED + GREEN) |
| 99-04 | DONE | <sha>..<sha> | WebAppWindowManager swap to mode:'vnc-window' + /ws/stream/:streamId dispatch on session.kind |
| 99-05 | IN PROGRESS | (this plan) | Push + Mini PC deploy + UAT + close |

(Fill in the actual commit SHAs from `git log --format=%h --abbrev-commit master..HEAD` or local equivalent.)

## Files touched

### Created
- `livos/packages/livinityd/source/modules/streaming/vnc-bridge.ts` — ~150 lines (spawnVncForWindow + attachVncBridge + BACKPRESSURE_BYTES + 3×100ms ECONNREFUSED retry per Pitfall 4)
- `livos/packages/livinityd/source/modules/streaming/vnc-bridge.test.ts` — ~200 lines (9 cases)

### Modified
- `livos/packages/livinityd/source/modules/streaming/stream-manager.ts` — discriminated-union StreamSession; vnc-window branch in startStream; getSession; stopStream cascade
- `livos/packages/livinityd/source/modules/streaming/stream-manager.test.ts` — +5 new cases (15/15 green)
- `livos/packages/livinityd/source/modules/streaming/encoder-args.ts` — StreamMode union extended with 'vnc-window'; defensive throw in buildFfmpegArgs/buildGstWindowArgs
- `livos/packages/livinityd/source/modules/webapps/window-manager.ts` — spawn() hard-cuts to mode:'vnc-window'; PipeWire portal + GeometryTracker for WebApp use case removed; geometry-clamp preserved per D-99-05
- `livos/packages/livinityd/source/modules/webapps/window-manager.test.ts` — +3 new cases (14/14 green)
- `livos/packages/livinityd/source/modules/server/index.ts` — /ws/stream/:streamId dispatches on session.kind: fmp4 → addSubscriber; vnc → attachVncBridge
- `livos/packages/livinityd/source/modules/server/ws-stream.test.ts` — +2 new source-string assertions (15/15 green)

### Untouched (D-99-04 + D-99-10 + D-NO-BYOK preservation)
- `liv/packages/core/src/sdk-agent-runner.ts` — sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- `livos/packages/livinityd/source/modules/streaming/fmp4-fanout.ts` — fMP4 fanout for mode:'desktop'
- `livos/packages/livinityd/source/modules/webapps/pipewire-portal.ts` — orphaned for fMP4 path; not deleted
- `livos/packages/livinityd/source/modules/webapps/geometry-tracker.ts` — orphaned for fMP4 path; not deleted
- `livos/packages/ui/source/...` — frontend (`use-webapp-vnc.ts`, `webapp-stream-window.tsx`, etc.) — UNCHANGED

## Tests added

| File | New cases | Total |
|------|-----------|-------|
| streaming/vnc-bridge.test.ts | 9 | 9 |
| streaming/stream-manager.test.ts | +5 | 15 |
| webapps/window-manager.test.ts | +3 | 14 |
| server/ws-stream.test.ts | +2 | 15 |
| **Total** | **+19** | **+19 net** |

## Decisions diff vs CONTEXT

All 12 LOCKED decisions (D-99-01..D-99-12) honored as written. No drift.

- D-99-01 canonical argv: implemented verbatim in vnc-bridge.ts spawnVncForWindow
- D-99-02 pure-Node bridge (no websockify subprocess): implemented in vnc-bridge.ts attachVncBridge
- D-99-03 -localhost bind + per-stream port: in-process counter starting at 15900
- D-99-04 fMP4 path stays alive: Fmp4Fanout, encoder-args, pipewire-portal, geometry-tracker all preserved
- D-99-05 geometry-clamp preserved as no-op: explicit comment "preserved for ffmpeg-fallback path; unused under x11vnc mode"
- D-99-06 loud-fail (no auto-fallback): no fallback wired; bridge surfaces `vnc backend unreachable` via ws.close(1011)
- D-99-07 stderr-tail diagnostic: spawnVncForWindow tails last 50 lines + dumps on non-zero exit
- D-99-08 + D-99-09 lifecycle cascade: stopStream(vnc kind) SIGTERMs x11vnc; idleCleanupTick → close → stopStream
- D-99-10 sacred SHA preserved: verified across all commits (see table below)
- D-99-11 D-NO-BYOK: no @anthropic-ai/sdk edits, no broker changes
- D-99-12 D-NO-SERVER4: Mini PC bruce@10.69.31.68 only

## Sacred SHA verification

| Commit | sdk-agent-runner.ts SHA |
|--------|-------------------------|
| 99-01 doc commit | f3538e1d811992b782a9bb057d1b7f0a0189f95f |
| 99-02 RED | same |
| 99-02 GREEN | same |
| 99-03 refactor | same |
| 99-03 RED | same |
| 99-03 GREEN | same |
| 99-04 window-manager swap | same |
| 99-04 ws-stream dispatch | same |
| 99-05 docs (this commit) | same |

`liv/packages/core/src/sdk-agent-runner.ts` UNTOUCHED across the entire phase.

## UAT outcome

(filled in by Task 4 after the user-walked UAT signs off)

## Carryovers

- **No new carryovers.** The frontend's noVNC reconnect-on-disconnect loop hammers the WS endpoint at 1s/2s/4s/8s backoff after a window dies; this is bounded and self-resolving (Pitfall 3 in 99-RESEARCH.md). Documented here for future maintenance.
- **fMP4 path orphaned for WebApp use case but ALIVE for mode:'desktop'** — desktop-stream native app continues to work via Fmp4Fanout. If a future cleanup phase decides to retire fMP4, this is the file inventory: `streaming/fmp4-fanout.ts`, `streaming/encoder-args.ts` (fmp4 branches), `webapps/pipewire-portal.ts`, `webapps/geometry-tracker.ts`.
- **WebRTC upgrade for the desktop fMP4 path** — deferred to v34 per v33-DRAFT.md.
- **Multi-tab same-WebApp UX nit** (`-shared` allows multiple tabs to drive the same x11vnc → cursor jitter if both move at once) — out of scope; documented in 99-RESEARCH.md §"Open Questions" Q3.

---

**Closing sacred-SHA gate:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
```

After writing, sacred-SHA verify + commit:
```bash
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# MUST equal f3538e1d811992b782a9bb057d1b7f0a0189f95f
git add .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md
git commit -m "docs(99-05): draft 99-SUMMARY.md (pending Mini PC UAT)

Phase rollup with file/test/decision inventory. UAT outcome section
left blank — filled by 99-05 task 4 after user-walked Mini PC UAT
signs off.

Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f UNTOUCHED."

git push origin master 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>git log --oneline -1 -- .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] && grep -q "f3538e1d811992b782a9bb057d1b7f0a0189f95f" .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format=%H master..HEAD` (or local equivalent showing the phase 99 commits) includes commits for 99-01..99-04.
    - For every commit in the range: `git show <sha>:liv/packages/core/src/sdk-agent-runner.ts | git hash-object --stdin` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    - `git push origin master` succeeds.
    - File `.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md` exists.
    - SUMMARY contains the literal sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    - SUMMARY contains a "Plans shipped" table with rows for 99-01, 99-02, 99-03, 99-04, 99-05.
    - SUMMARY contains a "UAT outcome" section (empty placeholder OK at this point — Task 4 fills it).
    - One commit landed: `docs(99-05): draft 99-SUMMARY.md (pending Mini PC UAT)`.
    - The new commit also pushed.
  </acceptance_criteria>
  <done>
    - GitHub master advanced.
    - Phase summary draft committed + pushed.
    - Sacred SHA verified across all 99-01..99-05 commits.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User walks Mini PC deploy via update.sh (detached)</name>
  <read_first>
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_minipc_ssh.md (SSH command + key path)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_zerotier_unstable.md (detach long ops; never hold foreground SSH for >30s)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md (one batched session)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_update_sh_drift.md (diff Mini PC update.sh against repo before patching — but this phase doesn't patch update.sh, only ships through it)
  </read_first>
  <files>(no files modified by this task — Mini PC deploy via update.sh is run on the Mini PC; the repo is unchanged)</files>
  <what>
    The user runs the Mini PC deploy. update.sh clones from GitHub, rsyncs, builds, restarts services. update.sh runs ~5-10 min on a typical Mini PC; ZeroTier link can flap during that window, so the run MUST be detached (nohup + log file + bg poll).
  </what>
  <action>
**Step A — kick off update.sh detached:**

```
"/c/Windows/System32/OpenSSH/ssh.exe" -i "C:/Users/hello/Desktop/Projects/contabo/pem/minipc" -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'sudo nohup bash /opt/livos/update.sh > /tmp/p99-update.log 2>&1 < /dev/null & echo "kicked off pid=$!"; disown'
```

This returns within ~1 second with `kicked off pid=NNNNN`. The actual update runs in the background.

**Step B — poll log for completion (separate SSH calls, single command each — fail2ban-safe spacing):**

Wait ~3 minutes, then poll:
```
"/c/Windows/System32/OpenSSH/ssh.exe" -i "C:/Users/hello/Desktop/Projects/contabo/pem/minipc" -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'tail -30 /tmp/p99-update.log; echo ---; pgrep -f /opt/livos/update.sh > /dev/null && echo "STILL_RUNNING" || echo "DONE"'
```

Expected eventual stdout: shows the tail of update.sh including a final `DONE` block. If `STILL_RUNNING` appears, wait 60-120s and re-poll.

**Step C — verify livos.service health after the deploy completes:**

```
"/c/Windows/System32/OpenSSH/ssh.exe" -i "C:/Users/hello/Desktop/Projects/contabo/pem/minipc" -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'sudo systemctl status livos.service --no-pager | head -20; echo ---; sudo journalctl -u livos.service --since "5 minutes ago" --no-pager | tail -30'
```

Look for:
- `Active: active (running)` in the systemctl status output.
- Recent journal lines do NOT contain `streamManager not available`, `vnc-bridge`, `Cannot open display`, or fatal startup errors.
- (Bonus) journal might log `WebApp window-manager: ...` noise — that's normal.

If livos.service is `failed` or `inactive` → STOP. Capture journalctl output, paste into chat. Common causes: missing dependency at runtime (unlikely — Phase 99 added no new deps), TypeScript error in vnc-bridge.ts (would have been caught by vitest pre-deploy — escalate).

**Step D — report back to assistant:**

Paste into chat:
1. The final 30 lines of `/tmp/p99-update.log`.
2. The systemctl status block.
3. The recent journalctl tail.

The assistant will inspect for: clean systemctl restart, no vnc-bridge / x11vnc startup errors, no missing-symbol crash.
  </action>
  <how-to-verify>
    1. Run kickoff command (Step A) — get back `pid=NNNNN`.
    2. Wait 3 minutes (or check log earlier — `tail /tmp/p99-update.log` is safe to call multiple times).
    3. Run poll command (Step B) until `DONE` appears.
    4. Run health-check (Step C).
    5. Paste outputs into chat.
  </how-to-verify>
  <acceptance_criteria>
    - `/tmp/p99-update.log` exists on the Mini PC and ends with deployment-completion lines (rsync done, build done, systemctl restart done).
    - `systemctl status livos.service` reports `Active: active (running)`.
    - Recent journalctl for livos.service does NOT contain fatal errors, missing-module errors, or `Cannot open display`.
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` (run locally) outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </acceptance_criteria>
  <resume-signal>Paste the three SSH outputs (update log tail + systemctl status + journalctl tail) into chat. Type "abort: &lt;reason&gt;" if any of them indicate a failed deploy.</resume-signal>
  <verify>
    <automated>echo "MANUAL — user runs Mini PC SSH commands and reports outcome to assistant"</automated>
  </verify>
  <done>
    - update.sh ran to completion on Mini PC.
    - livos.service is active (running).
    - No fatal errors in journalctl since the restart.
    - Phase 99 code is now live on bruce@10.69.31.68.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User-walked end-to-end UAT — open WebApp + RFB handshake + bidirectional input</name>
  <read_first>
    - .planning/phases/99-webapp-vnc-swap/99-CONTEXT.md (Goal sentence — proves the live UAT was the trigger)
    - .planning/phases/99-webapp-vnc-swap/99-RESEARCH.md §"Common Pitfalls" → Pitfall 2 (black-frame symptom) and Pitfall 3 (reconnect spam — benign)
    - .planning/phases/98-uat-polish/UAT-CHECKLIST.md (existing format)
  </read_first>
  <files>(no files modified by this task — purely user observation against live https://bruce.livinity.io)</files>
  <action>This is a checkpoint:human-verify task. The user is the actor. The user follows the &lt;how-to-verify&gt; steps below in their local browser pointed at https://bruce.livinity.io and reports PASS or FAIL with evidence. The assistant does NOT execute any commands during this task — it only reads the user-pasted result and decides whether Task 4 (close) can proceed. If FAIL: do NOT proceed to Task 4. Either roll forward with a debug step (run Pitfall 2 mitigations on the Mini PC: try `-rawfb` or `-onetile` x11vnc flags on a single Chrome window via SSH; OR compare DevTools console errors against Pitfall 3 reconnect-loop signatures) OR roll back to fMP4 by reverting the four 99-04 + 99-03 commits and pushing fMP4 back to the Mini PC. The roll-back path is intentionally documented but unlikely to be needed given 99-01 already proved the x11vnc handshake works empirically.</action>
  <what>
    The user opens https://bruce.livinity.io in a browser, opens (or creates) a WebApp, clicks its desktop icon, and observes the stream window. The acceptance criteria are concrete and observable: the noVNC handshake succeeds (no `Invalid server version ftypiso` error), the captured Chrome window's pixels are visible (not black, not frozen), mouse clicks inside the canvas reach the Mini PC (Chrome reacts), keyboard input reaches the Mini PC (typing in an input field on the captured page works).
  </what>
  <how-to-verify>
    1. Open https://bruce.livinity.io in your local browser. Sign in if needed.
    2. On the LivOS desktop, locate (or create via right-click → Add WebApp) a WebApp icon. A simple URL like https://duckduckgo.com works for the test.
    3. Click the WebApp icon.
    4. **EXPECTED:** A stream window opens. Within ~1-3 seconds, you see the live captured Chrome window (the right side of the LivOS UI shows the v32 chat panel; the left/top shows the captured Chrome).
    5. **DO NOT EXPECT:** Any "Invalid server version ftypiso" error overlay. Any "Stream ended" banner that doesn't auto-recover. Any black/frozen rectangle.
    6. Click somewhere inside the noVNC canvas (e.g., the address bar of the captured Chrome). The cursor should land. Type some text. The captured Chrome's address bar should fill in real-time.
    7. Move your mouse around inside the canvas. The captured Chrome should show the cursor moving.
    8. Open browser DevTools (F12) → Console tab on the LivOS UI. Look for noVNC connection logs. Expected: `[RFB] connected to wss://...`, `[RFB] handshake state: started → ProtocolVersion → ... → connected`. Should NOT see repeated reconnect-loop spam (a few reconnects on first open are OK; sustained 1s/2s/4s/8s spam means the bridge isn't holding the connection).
    9. Close the captured Chrome window directly (kill it from the captured Chrome's window controls, OR use the LivOS UI's "close WebApp" button). Within ~5-10 seconds, the stream window should disappear (idleCleanupTick fires).
    10. Open another WebApp from a different domain (e.g., https://example.com). Verify the same flow works for a SECOND concurrent stream.
    
    Report PASS or FAIL with specific evidence:
      - PASS: paste DevTools console snippet showing successful RFB handshake.
      - FAIL: paste the exact error overlay text + DevTools console + screenshot link.
  </how-to-verify>
  <what-built>
    The Phase 99 swap from fMP4 to x11vnc per-window streaming. After this task PASSES, the wire-format mismatch from the original UAT (2026-05-08) is resolved. The frontend is unchanged; only the backend serves a different protocol now (RFB instead of fMP4) over the same `/ws/stream/:streamId` endpoint.
  </what-built>
  <acceptance_criteria>
    - WebApp click opens a stream window without any "Invalid server version" or "ftypiso" error.
    - Captured Chrome window's pixels are visible (not black).
    - Mouse click inside the canvas: captured Chrome cursor lands at clicked position.
    - Keyboard input inside an input field of the captured Chrome: characters appear in real-time.
    - Closing the captured Chrome window cascades — the stream window auto-removes within 10 seconds.
    - A SECOND WebApp on a different URL also works concurrently (no port collision, no cross-WebApp pointer-fight in the simple single-tab case).
    - DevTools console shows successful RFB handshake (no sustained reconnect-loop spam).
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` (run locally) outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </acceptance_criteria>
  <resume-signal>Paste either: (a) "PASS" with DevTools snippet showing RFB handshake; OR (b) "FAIL: &lt;evidence&gt;" with screenshot/console output.</resume-signal>
  <verify>
    <automated>echo "MANUAL — user observes browser behavior end-to-end and reports PASS/FAIL"</automated>
  </verify>
  <done>
    - User confirms PASS with DevTools evidence.
    - Two concurrent WebApps observed working.
    - Cleanup-on-close observed.
    - No fmp4 ftypiso error.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append UAT row + finalize 99-SUMMARY.md + flip ROADMAP/STATE + final commit</name>
  <files>.planning/phases/98-uat-polish/UAT-CHECKLIST.md, .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/98-uat-polish/UAT-CHECKLIST.md (existing format — examine the table column shape)
    - .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md (post-Task-1 draft — needs the UAT outcome section filled in)
    - .planning/ROADMAP.md lines 108-170 (the v33 milestone block — Phase 99 entry to flip)
    - .planning/STATE.md (the milestone status block — update v33 status to reflect P99 close)
  </read_first>
  <action>
**Step A — append the UAT row to UAT-CHECKLIST.md.**

Locate the appropriate section in `.planning/phases/98-uat-polish/UAT-CHECKLIST.md`. The existing checklist is structured as sections A-J + sign-off. Phase 99 is a hot-fix, so a single new row under the existing "WebApp Launcher" section is appropriate. Use the EXISTING column shape (read the first row to confirm the column count + format).

Append a row similar to:
```markdown
| (P99) | WebApp window opens with live RFB stream + bidirectional input | PASS | 2026-05-08 — Phase 99 swap from fMP4 to x11vnc -id <wid>; user-walked UAT confirmed handshake + mouse/keyboard pass-through across 2 concurrent WebApps; close cascade auto-removes stream window. Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved throughout. |
```

(Adapt to whatever column shape the existing table uses.)

**Step B — finalize 99-SUMMARY.md.**

Replace the placeholder "UAT outcome" section in `.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md` with the actual outcome:

```markdown
## UAT outcome

**Result:** PASS (date: <today's ISO>; user-walked).

**Evidence:**
- Mini PC deploy via `bash /opt/livos/update.sh` completed cleanly; `livos.service` reports `Active: active (running)`; no fatal journalctl errors.
- WebApp click → stream window opens within ~1-3s. noVNC RFB handshake succeeds (no "Invalid server version ftypiso" error).
- Mouse + keyboard input pass through to captured Chrome window.
- Two concurrent WebApps on different URLs both render correctly (port allocation collision-free; no cross-WebApp pointer fight).
- Captured Chrome window close → idleCleanupTick fires within ~5-10s; stream window auto-removes; x11vnc PID is reaped.
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNTOUCHED before AND after the deploy + UAT.

**Status flip:** Phase 99 — `[ ]` → `[x]` in ROADMAP.md.
**v33 milestone status:** all 8 phases (92-99) now CODE-COMPLETE-AND-UAT-PASSED at the WebApp Launcher feature level. Final v33 milestone signoff happens via the existing v33 UAT-CHECKLIST.md sections A-J once those are walked separately.
```

Update the closing line of 99-SUMMARY.md to read:
```markdown
**Closing sacred-SHA gate:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (verified post-deploy + post-UAT).
```

**Step C — flip ROADMAP.md.**

Find the line in `.planning/ROADMAP.md` that reads:
```
- [ ] **Phase 99: WebApp VNC Swap — fMP4 → x11vnc** (V33-VNC-01..05) — UAT (2026-05-08) found protocol mismatch...
```

Change `[ ]` → `[x]` and append a closing note at the END of the existing description:
```
... (existing description). Shipped <today's ISO>: code-complete + Mini PC UAT PASSED.
```

Also update the v33 milestone status line (line 11):
```
- ✅ **v33.0 WebApp Launcher + Teach/Auto Modes** — Phases 92-99 (CODE-COMPLETE 2026-05-08; pending Mini PC UAT signoff — see [.planning/phases/98-uat-polish/UAT-CHECKLIST.md](phases/98-uat-polish/UAT-CHECKLIST.md))
```

becomes:
```
- ✅ **v33.0 WebApp Launcher + Teach/Auto Modes** — Phases 92-99 (CODE-COMPLETE 2026-05-08, P99 hot-fix UAT PASSED <today's ISO>; full v33 UAT-CHECKLIST.md sections A-J still pending separately)
```

**Step D — update STATE.md.**

In `.planning/STATE.md`, update the YAML frontmatter:
```yaml
status: v33 milestone CODE-COMPLETE 2026-05-08; P99 hot-fix UAT PASSED <today>; full v33 UAT-CHECKLIST.md sections A-J still pending separately. Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f preserved through all 8 phases (92-99).
last_updated: "<today's ISO datetime>"
last_activity: <today's ISO> — v33 Phase 99 (WebApp VNC swap from fMP4 → x11vnc) shipped. ...
progress:
  total_phases: 8
  completed_phases: 8
  total_plans: 12
  completed_plans: 12
  percent: 100
```

(Adjust the plan/phase counts to whatever STATE.md currently uses — the structure is the lock, the exact counts depend on how STATE.md is currently keyed for v33.)

In the "Current Position" section, append a paragraph noting P99 close.

**Step E — sacred SHA verify + final commit + push.**

```bash
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# MUST equal f3538e1d811992b782a9bb057d1b7f0a0189f95f

git add .planning/phases/98-uat-polish/UAT-CHECKLIST.md \
        .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md \
        .planning/ROADMAP.md \
        .planning/STATE.md
git commit -m "$(cat <<'EOF'
docs(99-05): close Phase 99 — UAT PASS, ROADMAP/STATE updated

Phase 99 (WebApp VNC swap fMP4 → x11vnc) shipped:
- Mini PC deploy via update.sh completed cleanly
- User-walked UAT PASS: noVNC handshake, bidirectional input, two
  concurrent WebApps, close cascade all verified
- UAT-CHECKLIST.md row appended under WebApp Launcher section
- ROADMAP.md Phase 99 entry [x] flipped
- STATE.md updated with P99 close + v33 phase count

Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f preserved across
all ~9 commits in the phase 99 range. Verified pre + post deploy +
post UAT.

D-99-12 honored: no Server4 contact. D-99-11 honored: no BYOK / no
@anthropic-ai/sdk edits. D-99-10 honored: liv/packages/core/ untouched.

Closes Phase 99 of v33.0 milestone (WebApp Launcher hot-fix wave).
EOF
)"

git push origin master 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>grep -q "WebApp window opens with live RFB stream" .planning/phases/98-uat-polish/UAT-CHECKLIST.md && grep -qE "^\s*-\s*\[x\]\s*\*\*Phase 99" .planning/ROADMAP.md && grep -q "PASS" .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` contains the literal string `WebApp window opens with live RFB stream` and `PASS`.
    - `.planning/ROADMAP.md` Phase 99 entry now starts with `- [x] **Phase 99` (was `- [ ]`).
    - `.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md` contains the literal `**Result:** PASS` in the UAT outcome section.
    - `.planning/STATE.md` reflects P99 close (status field mentions P99).
    - `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
    - Final commit landed: `docs(99-05): close Phase 99 — UAT PASS, ROADMAP/STATE updated`.
    - `git push origin master` succeeds.
  </acceptance_criteria>
  <done>
    - UAT row appended.
    - 99-SUMMARY.md finalized.
    - ROADMAP.md Phase 99 = [x].
    - STATE.md updated.
    - Final commit pushed.
    - Sacred SHA verified one last time.
    - Phase 99 SHIPPED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine ↔ GitHub | git push over HTTPS, repo `utopusc/livinity-io` |
| Local machine ↔ Mini PC SSH | OpenSSH key auth (contabo_master.minipc); only the user has the key |
| Mini PC ↔ GitHub | update.sh clones over HTTPS to /tmp/livinity-update-* |
| livos.service (root) ↔ x11vnc child (bruce, runtime) | sudo -u bruce + DISPLAY/XAUTHORITY env injection (already in vnc-bridge.ts spawnVncForWindow per 99-02) |
| Browser ↔ /ws/stream/:streamId | JWT-from-query auth + ownership check (P93 pattern, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-Sacred | Backend Module Integrity (V14.2) | liv/packages/core/src/sdk-agent-runner.ts | mitigate | Pre-deploy sacred SHA verify across the entire phase 99 commit range; post-deploy + post-UAT re-verify. Final SHA recorded in 99-SUMMARY.md closing line. |
| T-99-Direct | Spoofing / Bypass (V5) | x11vnc rfbport on Mini PC | mitigate | `-localhost` in spawn argv (verified in 99-01 + 99-02); UAT step verifies the noVNC handshake against the bridged endpoint, NOT directly against x11vnc — proves the bridge is the only access path |
| T-99-Mutter | Configuration (V11) | x11vnc capture under bruce's Mutter session | mitigate | 99-01 live verification gate; UAT in this plan is the final empirical confirmation that the swap works on the real Mini PC |
| T-99-CrossUser | Access Control (V4) | /ws/stream/:streamId ownership lookup | mitigate | UNCHANGED from P93; UAT does not exercise multi-user (single Mini PC user only in v33); D-99-12 + memory note "Single Mini PC user only in v33" |
</threat_model>

<verification>
- All Phase 99 commits present on local + GitHub master.
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` present in EVERY commit's `liv/packages/core/src/sdk-agent-runner.ts` blob.
- `bash /opt/livos/update.sh` ran cleanly on Mini PC; livos.service active.
- User-walked UAT PASS: noVNC handshake + bidirectional input + 2 concurrent WebApps + close cascade.
- UAT-CHECKLIST.md row appended.
- 99-SUMMARY.md finalized with PASS evidence.
- ROADMAP.md Phase 99 = `[x]`.
- STATE.md reflects P99 close.
- No edits to liv/packages/core/, livos/packages/ui/, or any frontend code throughout the phase.
- D-NO-BYOK preserved (no @anthropic-ai/sdk paths added).
- D-NO-SERVER4 preserved (Mini PC bruce@10.69.31.68 only).
</verification>

<success_criteria>
- `git log --grep="99-05" --oneline` shows two commits: draft SUMMARY + close commit.
- `grep -qE "WebApp window opens with live RFB stream" .planning/phases/98-uat-polish/UAT-CHECKLIST.md` exits 0.
- `grep -qE "^- \[x\] \*\*Phase 99" .planning/ROADMAP.md` exits 0.
- `grep -q "PASS" .planning/phases/99-webapp-vnc-swap/99-SUMMARY.md` exits 0.
- `git hash-object liv/packages/core/src/sdk-agent-runner.ts` outputs `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
- `git diff --name-only HEAD~9..HEAD -- liv/packages/core/` outputs nothing (entire phase 99 commit range).
- `git diff --name-only HEAD~9..HEAD -- livos/packages/ui/` outputs nothing.
- `git push origin master` exit 0 (final push at end of Task 4).
</success_criteria>

<output>
This plan IS the closing rollup. Files updated:
  - `.planning/phases/99-webapp-vnc-swap/99-SUMMARY.md` (drafted in Task 1, finalized in Task 4)
  - `.planning/phases/98-uat-polish/UAT-CHECKLIST.md` (one row appended in Task 4)
  - `.planning/ROADMAP.md` (Phase 99 = [x] in Task 4)
  - `.planning/STATE.md` (P99 close in Task 4)
</output>
