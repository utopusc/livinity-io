---
phase: 63-mandatory-live-verification
plan: 11
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/63-mandatory-live-verification/63-final-gate.log
  - .planning/phases/63-mandatory-live-verification/63-deploy-cleanup.md
  - .planning/phases/63-mandatory-live-verification/63-VERIFICATION.md
autonomous: false
requirements:
  - FR-VERIFY-V30-08
tags:
  - phase-63
  - milestone-close
  - sacred-file
  - gate-clean-pass
  - no-accept-debt

must_haves:
  truths:
    - "git hash-object nexus/packages/core/src/sdk-agent-runner.ts equals 4f868d318abff71f8c8bfbcf443b2393a553018b (canonical sacred SHA)"
    - "git diff 4f868d31...HEAD -- nexus/packages/core/src/sdk-agent-runner.ts is empty"
    - "npx vitest run nexus/packages/core/src/__tests__/sdk-agent-runner-integrity.test.ts PASSES (BASELINE_SHA constant unchanged)"
    - "All test liv_sk_* keys minted during Phase 63 (phase-63-smoke-tests, phase-63-open-webui, phase-63-continue-dev) are REVOKED via Settings UI"
    - "Phase 63 *-SUMMARY.md exists for each of plans 01-10 (10 SUMMARYs)"
    - "Phase 63 *-VERIFICATION.md exists with status: passed (this plan creates it)"
    - "/gsd-complete-milestone v30.0 returns audit `passed` (NOT human_needed) on FIRST invocation"
    - "ZERO --accept-debt invocation occurs"
    - "ZERO new override row appended to MILESTONES.md 'Live-Verification Gate Overrides' section for v30.0"
    - "Forensic trail: all evidence files preserved; no orphaned ephemeral keys; STATE.md transitions to milestone-shipped"
  artifacts:
    - path: ".planning/phases/63-mandatory-live-verification/63-final-gate.log"
      provides: "Captured output of sacred SHA gate + integrity test + cleanup + milestone close attempt"
      min_lines: 40
    - path: ".planning/phases/63-mandatory-live-verification/63-deploy-cleanup.md"
      provides: "Documentation of test-key revocation actions taken at Wave 5"
      contains: "revoked"
    - path: ".planning/phases/63-mandatory-live-verification/63-VERIFICATION.md"
      provides: "Phase 63 official verification document with status: passed"
      contains: "status: passed"
  key_links:
    - from: "git hash-object sacred file"
      to: "BASELINE_SHA in sdk-agent-runner-integrity.test.ts"
      via: "constant pin assertion"
      pattern: "4f868d318abff71f8c8bfbcf443b2393a553018b"
    - from: "/gsd-complete-milestone v30.0 invocation"
      to: ".planning/MILESTONES.md 'Live-Verification Gate Overrides'"
      via: "gate logic -- `passed` audit means NO new row appended (clean close)"
      pattern: "Live-Verification Gate Overrides"
---

<objective>
Wave 5 -- FINAL gate. Sacred SHA assertion + cleanup + milestone clean-close. Closes FR-VERIFY-V30-08.

Purpose: This is the headline test of v30: the FIRST milestone to close cleanly under D-LIVE-VERIFICATION-GATE without `--accept-debt`. Everything Phase 63 has done so far (deploy + 3 external clients + 16 UAT walks) was preparation for this single invocation. If `/gsd-complete-milestone v30.0` returns `human_needed`, Phase 63 has NOT actually completed its job -- and `--accept-debt` is **strictly forbidden** by FR-VERIFY-V30-08.

Output:
- `63-final-gate.log` -- sacred SHA assertion + integrity test + cleanup actions + milestone close output.
- `63-deploy-cleanup.md` -- documentation of revoked test keys.
- `63-VERIFICATION.md` -- Phase 63's own verification doc (status: passed).
- v30.0 milestone shipped via `/gsd-complete-milestone` workflow (which writes its own milestone records).

This plan implements ONE phase requirement (FR-VERIFY-V30-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/workflows/complete-milestone.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-mandatory-live-verification/63-CONTEXT.md
@.planning/phases/63-mandatory-live-verification/63-RESEARCH.md
@.planning/phases/63-mandatory-live-verification/63-01-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-02-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-03-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-04-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-05-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-06-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-07-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-08-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-09-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-10-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md

<interfaces>
<!-- Sacred file gate (canonical, repeated across all v30 phases): -->
<!--   git hash-object nexus/packages/core/src/sdk-agent-runner.ts MUST equal 4f868d318abff71f8c8bfbcf443b2393a553018b -->
<!--   git diff 4f868d31...HEAD -- nexus/packages/core/src/sdk-agent-runner.ts MUST be empty -->
<!--   npx vitest run nexus/packages/core/src/__tests__/sdk-agent-runner-integrity.test.ts MUST PASS -->

<!-- Test keys to revoke (minted during Phase 63 -- Wave 2 + Wave 3a/3b/3c + possibly Wave 4): -->
<!--   - phase-63-smoke-tests   (minted in 63-03 task 1) -->
<!--   - phase-63-open-webui    (minted in 63-05 task 2) -->
<!--   - phase-63-continue-dev  (minted in 63-06 task 1) -->
<!--   - any ad-hoc keys minted during UAT walks -->
<!-- Revoke procedure: Settings → AI Configuration → API Keys → click each → "Revoke" → confirm. -->
<!-- Each revocation hits Phase 59 tRPC apiKeys.revoke. -->

<!-- Milestone close workflow: -->
<!--   /gsd-complete-milestone v30.0 -->
<!--   - Workflow scans .planning/phases/{56..63}-*/*-VERIFICATION.md for status: human_needed -->
<!--   - If count > 0 AND no --accept-debt flag → returns "human_needed" with phase list -->
<!--   - If count == 0 → returns "passed" → archives phases to .planning/milestones/v30.0-phases/ -->
<!--   - On `--accept-debt`: appends a row to MILESTONES.md "Live-Verification Gate Overrides" section -->
<!--     and proceeds to archive (THIS IS WHAT V29.5 DID; FORBIDDEN HERE) -->

<!-- MILESTONES.md baseline (current state per Read at planning time): -->
<!-- - 1 existing override row for v29.5 -->
<!-- - Phase 63 close MUST NOT add a v30.0 row (the row count of overrides table = 1, both before and after Phase 63) -->

<!-- AskUserQuestion default = "No" in complete-milestone workflow per Phase 54: -->
<!-- prevents accidental Enter-key bypass; if `human_needed` returns, the user must explicitly type "yes" or "--accept-debt" -- and they MUST NOT do so per FR-VERIFY-V30-08. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Sacred file final gate -- hash-object + git diff + integrity test</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-final-gate.log
  </files>
  <what-built>
The canonical sacred file gate triplet:

```bash
{
  echo "=== SACRED FILE FINAL GATE ==="
  date -u +%Y-%m-%dT%H:%M:%SZ

  echo "--- A: git hash-object ---"
  EXPECTED=4f868d318abff71f8c8bfbcf443b2393a553018b
  ACTUAL=$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)
  echo "expected: $EXPECTED"
  echo "actual:   $ACTUAL"
  if [ "$ACTUAL" = "$EXPECTED" ]; then echo "OK: hash-object matches"; else echo "BLOCK: hash-object DRIFTED"; fi

  echo ""
  echo "--- B: git diff 4f868d31..HEAD -- sacred ---"
  DIFF_LINES=$(git diff 4f868d31..HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l)
  echo "diff line count (expected 0): $DIFF_LINES"
  if [ "$DIFF_LINES" -eq 0 ]; then echo "OK: diff empty"; else echo "BLOCK: diff has $DIFF_LINES lines"; git diff 4f868d31..HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | head -30; fi

  echo ""
  echo "--- C: integrity test ---"
  npx vitest run nexus/packages/core/src/__tests__/sdk-agent-runner-integrity.test.ts 2>&1 | tail -20
  echo "vitest exit: $?"

  echo "=== END SACRED FILE FINAL GATE ==="
} | tee .planning/phases/63-mandatory-live-verification/63-final-gate.log
```

If ANY of A/B/C fails:
- The sacred file has been edited (or the BASELINE_SHA constant in the integrity test is mismatched).
- This is a **catastrophic milestone-blocking event**.
- Identify the offending commit: `git log --oneline 4f868d31..HEAD -- nexus/packages/core/src/sdk-agent-runner.ts` and `nexus/packages/core/src/__tests__/sdk-agent-runner-integrity.test.ts`.
- Revert sacred file: `git checkout 4f868d31 -- nexus/packages/core/src/sdk-agent-runner.ts`.
- If integrity test BASELINE_SHA was edited: `git checkout 4f868d31 -- nexus/packages/core/src/__tests__/sdk-agent-runner-integrity.test.ts`.
- Re-deploy via `update.sh` (Wave 1 reprise -- single batched SSH).
- Re-run this gate.

After successful gate: do NOT proceed to Task 2 if any check shows BLOCK. v30.0 cannot close with sacred file drift.
  </what-built>
  <how-to-verify>
1. Open `63-final-gate.log`.
2. Section A: "OK: hash-object matches".
3. Section B: "OK: diff empty" (DIFF_LINES = 0).
4. Section C: vitest exit 0; test name `sdk-agent-runner-integrity` PASSED.
  </how-to-verify>
  <resume-signal>Type "sacred-gate-PASS" to advance, OR "BLOCK: <which-of-A-B-C>" + revert plan</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "sacred-gate-PASS" to advance, OR "BLOCK: <which-of-A-B-C>" + revert plan"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cleanup -- revoke ALL Phase 63 test liv_sk_* keys via Settings UI</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-deploy-cleanup.md
    .planning/phases/63-mandatory-live-verification/63-final-gate.log
  </files>
  <what-built>
Browser:
1. Open https://&lt;test-user&gt;.livinity.io → Settings → AI Configuration → API Keys.
2. List should show keys with names matching `phase-63-*` (likely 3-5 keys depending on what was minted).
3. For EACH `phase-63-*` key:
   - Click "Revoke".
   - Confirm in modal.
   - Confirm row updates with `revoked_at` timestamp.
4. After all revocations, single SSH to verify:
   ```bash
   ssh -i ... bruce@10.69.31.68 \
     "source /opt/livos/.env; \
      psql \"\$DATABASE_URL\" -c \"\
        SELECT id, key_prefix, name, created_at, revoked_at \
        FROM api_keys \
        WHERE name LIKE 'phase-63-%' \
        ORDER BY created_at DESC;\""
   ```
   Confirm ALL phase-63-* rows have `revoked_at` populated.

Write `63-deploy-cleanup.md`:

```markdown
# Phase 63 Deploy Cleanup

**Executed:** {ISO-8601 timestamp}

## Test API Keys Revoked

| key_id | key_prefix | name | revoked_at | verified_via |
|--------|-----------|------|------------|--------------|
| {id} | {prefix} | phase-63-smoke-tests | {ts} | psql row + Settings UI |
| {id} | {prefix} | phase-63-open-webui | {ts} | psql row + Settings UI |
| {id} | {prefix} | phase-63-continue-dev | {ts} | psql row + Settings UI |
| ...

## Open WebUI Cleanup (optional)

If desired, stop the local Open WebUI container:
docker stop open-webui-phase63 && docker rm open-webui-phase63 && docker volume rm open-webui-phase63

## Continue.dev Cleanup (optional)

The `~/.continue/config.json` retains the now-revoked liv_sk_*. Delete the model entry OR
rotate apiKey to a non-Livinity provider. Sanitized copy in
`evidence/63-continue-dev-config.json` already redacts.

## Bolt.diy Cleanup (optional)

Bolt.diy per-user container can be uninstalled via Marketplace → bolt-diy → Uninstall.
The container's revoked API key is now invalid; subsequent chat attempts will fail.
```

Append a confirmation block to 63-final-gate.log:
```bash
{
  echo ""
  echo "=== CLEANUP TASK 2: test keys revoked ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  ssh -i ... bruce@10.69.31.68 \
    "source /opt/livos/.env; \
     psql \"\$DATABASE_URL\" -c \"\
       SELECT COUNT(*) AS total_phase63_keys, \
              COUNT(*) FILTER (WHERE revoked_at IS NOT NULL) AS revoked, \
              COUNT(*) FILTER (WHERE revoked_at IS NULL) AS still_active \
       FROM api_keys \
       WHERE name LIKE 'phase-63-%';\""
  echo "(still_active MUST be 0)"
  echo "=== END ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-final-gate.log
```

Confirm `still_active = 0`.
  </what-built>
  <how-to-verify>
1. `63-deploy-cleanup.md` exists with revoked-keys table.
2. SSH psql query returns `still_active = 0` for `phase-63-*` keys.
3. UI shows revoked badges on all phase-63 keys.
  </how-to-verify>
  <resume-signal>Type "all-test-keys-revoked" with count, OR list any active phase-63-* keys + plan</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "all-test-keys-revoked" with count, OR list any active phase-63-* keys + pl"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="auto">
  <name>Task 3: Generate 63-VERIFICATION.md (status: passed)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-VERIFICATION.md
  </files>
  <action>
Create `.planning/phases/63-mandatory-live-verification/63-VERIFICATION.md` with the canonical phase-verification structure:

```markdown
~~~
phase: 63-mandatory-live-verification
status: passed
verified_at: {ISO-8601 timestamp}
verifier: gsd-executor + human-in-the-loop (orchestrator)
sacred_file_sha: 4f868d318abff71f8c8bfbcf443b2393a553018b
sacred_file_status: byte-identical (canonical)
must_haves_satisfied: 8/8
must_haves_partial: 0
must_haves_blocked: 0
uat_rows_walked: {count from 63-UAT-RESULTS.md grep}
uat_pass: {count}
uat_fail: 0
uat_obsolete: {count}  # Phase 43 sections C-F + any others
uat_blocked: 0
test_keys_revoked: {count}
hot_patches_applied: {phase + commit + uat-id list, or "none"}
~~~

# Phase 63 Verification

**Phase:** 63 - Mandatory Live Verification (D-LIVE-VERIFICATION-GATE)
**Date:** {ISO-8601}
**Status:** passed
**Verifier:** gsd-executor + human-in-the-loop

## Phase Requirements (8)

| Req | Description | Status | Evidence |
|-----|-------------|--------|----------|
| FR-VERIFY-V30-01 | Mini PC deployed; 4 services active; Past Deploys panel shows row | PASS | 63-02-SUMMARY.md; 63-deploy-output.log; 63-postdeploy.log; evidence/63-past-deploys-panel.png |
| FR-VERIFY-V30-02 | Bolt.diy: install + chat + ≥3 deltas + identity = Bolt + broker_usage row | PASS | 63-04-SUMMARY.md; evidence/63-bolt-diy-identity.png; evidence/63-bolt-diy-streaming.mp4; evidence/63-bolt-diy-broker-usage.txt |
| FR-VERIFY-V30-03 | Open WebUI: external client + system prompt honor + token counts | PASS | 63-05-SUMMARY.md; evidence/63-open-webui-streaming.png; evidence/63-open-webui-french-response.png; evidence/63-open-webui-broker-usage.txt |
| FR-VERIFY-V30-04 | Continue.dev: <5s response + tool-cleanliness | PASS | 63-06-SUMMARY.md; evidence/63-continue-dev-completion.png; evidence/63-continue-dev-config.json (sanitized) |
| FR-VERIFY-V30-05 | Raw curl streaming smoke (orchestrator-local + Server5) | PASS | 63-03-SUMMARY.md; 63-smoke-curl.log; evidence/63-curl-streaming-events.txt |
| FR-VERIFY-V30-06 | Anthropic Python SDK smoke in clean venv | PASS | 63-03-SUMMARY.md; 63-smoke-python.log; scripts/sdk-smoke.py |
| FR-VERIFY-V30-07 | All 16 UATs walked; ZERO BLOCKED | PASS | 63-07/08/09/10-SUMMARY.md; 63-UAT-RESULTS.md ({count} rows; PASS={N}/FAIL=0/OBSOLETE={N}/BLOCKED=0) |
| FR-VERIFY-V30-08 | /gsd-complete-milestone v30.0 returns audit `passed` first invocation; ZERO --accept-debt | PASS | 63-final-gate.log Task 4 invocation log; MILESTONES.md unchanged in "Live-Verification Gate Overrides" |

## Must-Haves (per phase ROADMAP success criteria) -- 7

(Mirror the 7 ROADMAP Phase 63 success criteria; each cross-references plan summaries)

## Sacred File Integrity

- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` = `4f868d318abff71f8c8bfbcf443b2393a553018b` ✓
- `git diff 4f868d31..HEAD -- nexus/packages/core/src/sdk-agent-runner.ts` = empty ✓
- `npx vitest run sdk-agent-runner-integrity.test.ts` = PASS ✓

## UAT Results Summary

- 16 source UAT/VERIFICATION files walked.
- Total rows: {N}
- PASS: {N}
- FAIL: 0
- OBSOLETE: {N} (documented closures, primarily Phase 43 sections C-F MiroFish per Phase 52 migration 0010)
- BLOCKED: 0

## Hot-Patch Loops Triggered

{list of any hot-patches applied during Phase 63, with phase + commit hash + UAT-id reference, OR "None -- Phase 63 closed cleanly with no hot-patch loops"}

## Cleanup Verification

- All phase-63-* test API keys revoked: confirmed via psql `still_active = 0`.
- Open WebUI / Continue.dev / Bolt.diy local artifacts handled per 63-deploy-cleanup.md.

## Conclusion

Phase 63 closes cleanly under D-LIVE-VERIFICATION-GATE without `--accept-debt`. v30.0 milestone is ready for `/gsd-complete-milestone v30.0` invocation expected to return audit `passed`.

**Status:** passed
```

Fill in placeholders by reading 63-UAT-RESULTS.md (grep counts) and prior plan SUMMARYs.
  </action>
  <verify>
    <automated>powershell -Command "$f = '.planning/phases/63-mandatory-live-verification/63-VERIFICATION.md'; if ((Test-Path $f) -and (Select-String 'status: passed' $f) -and (Select-String 'sacred_file_sha: 4f868d318abff71f8c8bfbcf443b2393a553018b' $f) -and ((Select-String 'uat_blocked: 0' $f) -ne $null)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>63-VERIFICATION.md exists with status: passed; all 8 reqs PASS; sacred_file_sha pinned; uat_blocked=0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Invoke /gsd-complete-milestone v30.0 -- expect `passed` (NO --accept-debt)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-final-gate.log
  </files>
  <what-built>
THIS IS THE HEADLINE MILESTONE-CLOSE INVOCATION. The single most important moment of the v30.0 milestone.

Pre-flight checklist (mental, before invocation):
- [ ] Sacred file SHA = canonical (Task 1 PASS).
- [ ] All test keys revoked (Task 2 PASS).
- [ ] 63-VERIFICATION.md exists with status: passed (Task 3 done).
- [ ] All 10 prior plans (63-01 .. 63-10) have SUMMARY files.
- [ ] All Phase 56-62 VERIFICATION files have status: passed (verified at Wave 0; should not have changed).

Invocation:
```
/gsd-complete-milestone v30.0
```

The workflow will:
1. Scan `.planning/phases/{56..63}-*/*-VERIFICATION.md` for `status: human_needed` count.
2. If count > 0 → AskUserQuestion default "No"; if user answers "no" → audit `human_needed`; if user types `--accept-debt` (FORBIDDEN) → forensic-trail row appended.
3. If count == 0 → audit `passed` → phases archived to `.planning/milestones/v30.0-phases/` → STATE.md transitions to `milestone-shipped` → tag `v30.0` may be created.

Capture the FULL workflow output to 63-final-gate.log:

```bash
{
  echo ""
  echo "=== /gsd-complete-milestone v30.0 INVOCATION ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  echo "(this is the headline FR-VERIFY-V30-08 invocation; --accept-debt FORBIDDEN)"
  echo ""
  # The invocation is performed via the slash command in the orchestrator session.
  # Capture stdout/stderr from the workflow in this log.
  # ... (paste full workflow output here)
  echo ""
  echo "audit_result: passed | human_needed"
  echo "accept_debt_used: false | true (FORBIDDEN if true)"
  echo "=== END INVOCATION ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-final-gate.log
```

EXPECTED OUTCOME:
- audit_result: `passed`.
- accept_debt_used: `false`.
- Workflow archives phases 56-63 to `.planning/milestones/v30.0-phases/`.
- MILESTONES.md gains a new "v30.0 Livinity Broker Professionalization" section but DOES NOT add a row to "Live-Verification Gate Overrides".

ABSOLUTE FORBIDDEN OUTCOME (per FR-VERIFY-V30-08):
- ❌ User passes `--accept-debt`.
- ❌ A new row is appended to MILESTONES.md "Live-Verification Gate Overrides" for v30.0.

If audit returns `human_needed`:
- DO NOT pass `--accept-debt`.
- Identify which phase's VERIFICATION.md has `status: human_needed`.
- That phase's gap is the actual blocker.
- Either:
  (a) Walk the missing UATs in that phase (likely Phase 63 itself if 63-VERIFICATION.md is missing or misset), OR
  (b) File v30.1 carry-forward decision (which is itself a milestone-blocking event for v30.0 -- file a separate decision document; v30.0 does NOT close).
- Re-run `/gsd-complete-milestone v30.0` until clean OR the v30.1 decision is documented.

Verification of clean close:
```bash
{
  echo ""
  echo "=== POST-CLOSE VERIFICATION ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  echo "--- A: MILESTONES.md override count for v30.0 (must be 0) ---"
  V30_OVERRIDE_COUNT=$(grep -c '^| .* | v30.0 |' .planning/MILESTONES.md 2>/dev/null || echo 0)
  echo "v30.0 override row count: $V30_OVERRIDE_COUNT"
  if [ "$V30_OVERRIDE_COUNT" -eq 0 ]; then echo "OK: clean close, no override"; else echo "BLOCK: v30.0 override row appended"; fi

  echo "--- B: phases archived to v30.0-phases/ ---"
  ls -d .planning/milestones/v30.0-phases/*/ 2>/dev/null | wc -l
  echo "(expected: 8 -- phases 56-63)"

  echo "--- C: STATE.md status ---"
  grep "^status:" .planning/STATE.md | head -1
  echo "(expected: milestone-shipped or similar terminal state)"

  echo "=== END POST-CLOSE ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-final-gate.log
```
  </what-built>
  <how-to-verify>
1. `63-final-gate.log` shows audit_result: `passed` and accept_debt_used: `false`.
2. POST-CLOSE A: v30.0 override row count = 0.
3. POST-CLOSE B: 8 phase dirs archived to v30.0-phases/.
4. POST-CLOSE C: STATE.md transitioned to terminal state.
5. MILESTONES.md "Live-Verification Gate Overrides" table count is unchanged (still 1 row, the v29.5 entry -- no v30.0 entry).
  </how-to-verify>
  <resume-signal>Type "milestone-CLOSED-CLEAN" with timestamp + tag, OR "BLOCKED: human_needed for Phase {N}" + remediation plan</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "milestone-CLOSED-CLEAN" with timestamp + tag, OR "BLOCKED: human_needed fo"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="auto">
  <name>Task 5: Final integrity sweep -- sacred file SHA still holds + plan close</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-final-gate.log
  </files>
  <action>
Final assertion that the sacred file is STILL pinned after milestone close (the close workflow may have triggered git operations like archiving -- verify nothing edited the sacred file):

```bash
{
  echo ""
  echo "=== PLAN 63-11 FINAL CLOSE: post-milestone-close sacred file SHA ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  EXPECTED=4f868d318abff71f8c8bfbcf443b2393a553018b
  ACTUAL=$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)
  echo "expected: $EXPECTED"
  echo "actual:   $ACTUAL"
  if [ "$ACTUAL" = "$EXPECTED" ]; then echo "OK: sacred SHA holds post-close"; else echo "ALERT: sacred SHA drifted post-close -- investigate"; fi
  echo ""
  echo "=== v30.0 MILESTONE STATE ==="
  echo "ROADMAP: $(head -1 .planning/ROADMAP.md 2>/dev/null || echo 'archived')"
  echo "STATE.md status: $(grep '^status:' .planning/STATE.md 2>/dev/null | head -1 || echo 'archived')"
  echo "v30.0 phase archives: $(ls -d .planning/milestones/v30.0-phases/*/ 2>/dev/null | wc -l)"
  echo "Live-Verification Gate Overrides v30.0 entries: $(grep -c '^|.*v30.0' .planning/MILESTONES.md 2>/dev/null || echo 'file moved')"
  echo "=== END ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-final-gate.log
```
  </action>
  <verify>
    <automated>powershell -Command "$sha = git hash-object nexus/packages/core/src/sdk-agent-runner.ts; if ($sha -eq '4f868d318abff71f8c8bfbcf443b2393a553018b') { exit 0 } else { Write-Output \"sha=$sha\"; exit 1 }"</automated>
  </verify>
  <done>v30.0 closed cleanly; sacred SHA confirmed; Phase 63 PASS; no override row added; milestone shipped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → /gsd-complete-milestone workflow | invoke once; receive audit verdict |
| orchestrator → MILESTONES.md (read-only check) | post-close verification |
| orchestrator → 63-VERIFICATION.md (write) | declares Phase 63 status |
| orchestrator → Settings UI revoke endpoint | revokes test keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-11-01 | Repudiation | Operator passes `--accept-debt` from muscle memory | mitigate | Task 4 explicit "FORBIDDEN" guard; AskUserQuestion default = "No" per Phase 54; plan text repeats the prohibition |
| T-63-11-02 | Tampering | Sacred file edited between Task 1 gate and Task 4 milestone close | mitigate | Task 5 re-runs the sacred SHA check post-close to detect any drift introduced by archive operations |
| T-63-11-03 | Information Disclosure | Test liv_sk_* keys remain active post-Phase-63 (forgotten in cleanup) | mitigate | Task 2 SSH psql verifies `still_active = 0` for `phase-63-*` keys |
| T-63-11-04 | Denial-of-Service | /gsd-complete-milestone workflow has bug; archive operation fails mid-way | accept | Workflow is itself v29.5 Phase 54 work; bugs would have surfaced earlier |
| T-63-11-05 | Spoofing | 63-VERIFICATION.md has status: passed but underlying UAT-RESULTS shows BLOCKED rows | mitigate | Task 3 uat_blocked=0 is asserted; Wave 4 plans (63-07..10) already enforce zero BLOCKED |
</threat_model>

<verification>
**Plan-level verification:**
```bash
test -f .planning/phases/63-mandatory-live-verification/63-final-gate.log && \
  test -f .planning/phases/63-mandatory-live-verification/63-VERIFICATION.md && \
  test -f .planning/phases/63-mandatory-live-verification/63-deploy-cleanup.md && \
  grep -q "status: passed" .planning/phases/63-mandatory-live-verification/63-VERIFICATION.md && \
  grep -q "uat_blocked: 0" .planning/phases/63-mandatory-live-verification/63-VERIFICATION.md && \
  grep -q "OK: hash-object matches" .planning/phases/63-mandatory-live-verification/63-final-gate.log && \
  grep -q "OK: diff empty" .planning/phases/63-mandatory-live-verification/63-final-gate.log && \
  grep -q "audit_result: passed" .planning/phases/63-mandatory-live-verification/63-final-gate.log && \
  grep -q "accept_debt_used: false" .planning/phases/63-mandatory-live-verification/63-final-gate.log && \
  test "$(grep -c '^|.* v30.0 ' .planning/MILESTONES.md 2>/dev/null || echo 0)" -eq 0 && \
  test "$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)" = "4f868d318abff71f8c8bfbcf443b2393a553018b"
```

**Sacred file SHA assertion (final, canonical):**
```bash
git hash-object nexus/packages/core/src/sdk-agent-runner.ts
# expected: 4f868d318abff71f8c8bfbcf443b2393a553018b
```
</verification>

<success_criteria>
- Sacred file gate triplet PASSES (hash-object + git diff empty + integrity test PASS).
- All `phase-63-*` test API keys REVOKED (still_active = 0).
- `63-VERIFICATION.md` exists with `status: passed`, `uat_blocked: 0`, sacred_file_sha pinned.
- `63-deploy-cleanup.md` documents revoked keys.
- `/gsd-complete-milestone v30.0` invocation returns audit `passed`.
- ZERO `--accept-debt` invocation occurs.
- ZERO new row in MILESTONES.md "Live-Verification Gate Overrides" for v30.0.
- 8 phases (56-63) archived to `.planning/milestones/v30.0-phases/`.
- STATE.md transitions to terminal state (milestone-shipped or equivalent).
- Sacred file SHA = `4f868d318abff71f8c8bfbcf443b2393a553018b` post-close.
- D-LIVE-VERIFICATION-GATE closes its FIRST clean pass -- v30 raison d'être validated.
</success_criteria>

<output>
After completion, create `.planning/phases/63-mandatory-live-verification/63-11-SUMMARY.md` with:
- Sacred file gate result (3 sub-checks).
- Cleanup result (count of revoked keys).
- 63-VERIFICATION.md status: passed confirmation.
- /gsd-complete-milestone outcome: passed | human_needed.
- accept_debt used: false.
- v30.0 override row delta: 0 (CONFIRMED clean close).
- Phases archived: 8.
- Final sacred SHA confirmation.
- Conclusion: "v30.0 Livinity Broker Professionalization SHIPPED -- D-LIVE-VERIFICATION-GATE first clean pass achieved" OR "BLOCKED -- see remediation plan in 63-final-gate.log".
</output>
