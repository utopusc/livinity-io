---
phase: 63-mandatory-live-verification
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
autonomous: false
requirements:
  - FR-VERIFY-V30-07
tags:
  - phase-63
  - uat-walks
  - v29.5
  - carry-forward
  - sacred-file

must_haves:
  truths:
    - "All 6 v29.5 VERIFICATION.md files (49, 50, 51, 52, 53, 54) walked step-by-step on the live Mini PC + Server5 environment"
    - "Each enumerated step recorded in 63-UAT-RESULTS.md with one row: phase | uat_id | step_id | description | result (PASS|FAIL|BLOCKED|OBSOLETE) | evidence | timestamp"
    - "Phase 49 5 must-haves walked (3 PASSED / 1 PARTIAL / 1 BLOCKED upstream — but Phase 63 walks them on LIVE Mini PC and updates verdicts)"
    - "Phase 51 5 enumerated human-verify steps walked (deploy timing, dist mtime, token streaming, streaming-failure escalation, 'Hangi modelsin?' identity)"
    - "Phase 50/52/53/54 each walked through their must-haves; the items previously DEFERRED to live now have live verdicts"
    - "ZERO BLOCKED rows for the v29.5 batch (FAIL or OBSOLETE acceptable; BLOCKED triggers hot-patch loop or Wave 5 milestone-blocker)"
    - "Sacred file SHA = 4f868d318abff71f8c8bfbcf443b2393a553018b at end of plan"
  artifacts:
    - path: ".planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md"
      provides: "Append-only UAT results table; Wave 4a populates the v29.5 phases section (~25-30 rows)"
      contains: "v29.5"
  key_links:
    - from: "63-UAT-RESULTS.md table rows"
      to: "Phase 49/50/51/52/53/54 *-VERIFICATION.md must-have items"
      via: "1:1 row-per-step mapping"
      pattern: "\\|\\s*v29.5\\s*/\\s*(49|50|51|52|53|54)"
---

<objective>
Wave 4a — UAT walks for v29.5 carry-forward (Phases 49, 50, 51, 52, 53, 54). Closes the first batch of FR-VERIFY-V30-07 (the requirement spans all 16 files; this plan covers 6).

Purpose: v29.5 closed via `--accept-debt` precisely BECAUSE these UATs were never walked. Phase 55 was the deferred live-verification phase; its work is absorbed here. This wave is the closure of that debt.

Output:
- `63-UAT-RESULTS.md` — appended with ~25-30 rows for v29.5 phases.

Step inventory (per 63-RESEARCH.md UAT inventory rows 1-6):
- Phase 49: 5 must-haves + 4 requirement rows + 3 human-verify items
- Phase 50: 4 must-haves (mechanism PASSED; FR-A1-03/04 deferred-to-live → walk live)
- Phase 51: 5 must-haves + 5 enumerated human verify steps (lines 47-51 of 51-VERIFICATION.md)
- Phase 52: 6 must-haves (4 PASSED / 1 PARTIAL / 1 DEFERRED — browser visit confirms apps post-deploy)
- Phase 53: 6 must-haves (5 PASSED via Phase 51 / 1 DEFERRED — sidebar 13-entry visual)
- Phase 54: 5 must-haves (all PASSED — optional retro `/gsd-audit-milestone v29.4` validation)

This plan implements ONE phase requirement (FR-VERIFY-V30-07) for 6 of 16 files. Plans 63-08 (v29.4), 63-09 (v29.3 part 1), 63-10 (v29.3 part 2) cover the remaining 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-mandatory-live-verification/63-CONTEXT.md
@.planning/phases/63-mandatory-live-verification/63-RESEARCH.md
@.planning/phases/63-mandatory-live-verification/63-01-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-04-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-05-SUMMARY.md
@.planning/phases/63-mandatory-live-verification/63-06-SUMMARY.md
@.planning/milestones/v29.5-phases/49-mini-pc-diagnostic/49-VERIFICATION.md
@.planning/milestones/v29.5-phases/50-a1-tool-registry-seed/50-VERIFICATION.md
@.planning/milestones/v29.5-phases/51-a2-streaming-fix/51-VERIFICATION.md
@.planning/milestones/v29.5-phases/52-a3-marketplace-state/52-VERIFICATION.md
@.planning/milestones/v29.5-phases/53-a4-security-panel-render/53-VERIFICATION.md
@.planning/milestones/v29.5-phases/54-b1-live-verification-gate/54-VERIFICATION.md

<interfaces>
<!-- 63-UAT-RESULTS.md table format (canonical, established in 63-01-PLAN Task 1): -->
<!-- | phase | uat_id | step_id | description | result | evidence | timestamp | -->
<!-- result legend: PASS | FAIL | BLOCKED | OBSOLETE -->

<!-- Step ID convention: -->
<!--   Phase 49: must-have-1 .. must-have-5; req-row-1 .. req-row-4; human-verify-1 .. human-verify-3 -->
<!--   Phase 50: must-have-1 .. must-have-4 -->
<!--   Phase 51: must-have-1 .. must-have-5; human-verify-1 .. human-verify-5 -->
<!--   Phase 52: must-have-1 .. must-have-6 -->
<!--   Phase 53: must-have-1 .. must-have-6 -->
<!--   Phase 54: must-have-1 .. must-have-5 -->

<!-- Evidence column conventions: -->
<!--   - For Mini PC SSH check: "63-postdeploy.log L<line>" or specific log section -->
<!--   - For browser screenshot: "evidence/63-<phase>-<aspect>.png" -->
<!--   - For psql output: "evidence/63-<phase>-<query>.txt" -->
<!--   - For prior-Wave evidence: reference Wave 1/2/3 outputs (e.g., "63-deploy-output.log") -->

<!-- BLOCKED rows: include the blocker description in evidence column (e.g., "Mini PC SSH ban", "Phase 53 sidebar collapsed") -->
<!--   ZERO BLOCKED rows allowed for milestone close → triggers hot-patch loop in Wave 5 if found -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk Phase 49 (Mini PC Live Diagnostic) — 5 must-haves + 4 requirement rows + 3 human-verify items</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
Read `.planning/milestones/v29.5-phases/49-mini-pc-diagnostic/49-VERIFICATION.md` and walk every enumerated check.

For each check:
1. Determine if it's testable on the live Mini PC + Server5 environment NOW.
2. If testable: walk it (likely a single batched SSH or browser check). Record result.
3. If references state that no longer exists in v30 (rare for Phase 49): record OBSOLETE.
4. Append a row to `63-UAT-RESULTS.md`.

Phase 49 reference structure (per 63-RESEARCH.md inventory):
- 5 must-haves (was: 3 PASSED / 1 PARTIAL / 1 BLOCKED upstream — most "BLOCKED" was Mini PC SSH ban; now SSH is reachable per Wave 0 → re-walk).
- 4 requirement rows (FR-DIAG-01..04 or similar — see 49-VERIFICATION.md for actual ids).
- 3 human-verify items.

Single batched SSH for Phase 49 server-side checks:
```bash
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc \
    -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
    bruce@10.69.31.68 \
    "echo '=== A: 49 must-have-1 evidence ==='; \
     # (insert appropriate diagnostic command per 49-VERIFICATION.md must-have-1)
     # ... batch all Phase 49 server-side commands here in ONE ssh call
     echo '=== END Phase 49 batch ==='"
```

Append rows to 63-UAT-RESULTS.md. Example row format:

```markdown
| v29.5 / 49 | FR-DIAG-XX | must-have-1 | Server5 platform DB schema captured | PASS | `49-DIAGNOSTIC-FIXTURE.md` line 42; verified live via single-batched SSH 63-07-task1.log section A | 2026-05-02T<HH:MM:SSZ> |
| v29.5 / 49 | FR-DIAG-XX | must-have-2 | Mini PC fail2ban diagnostic | PASS | Wave 0 pre-flight check 2A; no ban at Phase 63 start | 2026-05-02T<...> |
| ...
```

If a row that was BLOCKED in 49-VERIFICATION.md (Mini PC SSH ban) now PASSES because Wave 0 confirmed SSH reachable: walk it now and record PASS.

If a row that was PARTIAL → resolve to PASS or FAIL based on live observation.
  </what-built>
  <how-to-verify>
1. Open 63-UAT-RESULTS.md.
2. Confirm at least 12 rows added with `phase` column showing "v29.5 / 49".
3. Confirm each row has non-empty `evidence` column.
4. Confirm ZERO `BLOCKED` rows in this batch (any BLOCKED → fix the blocker before Wave 5).
  </how-to-verify>
  <resume-signal>Type "phase-49-walked" with PASS/FAIL/OBSOLETE counts, OR list any BLOCKED rows + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-49-walked" with PASS/FAIL/OBSOLETE counts, OR list any BLOCKED rows "</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk Phase 50 + Phase 51 — Tool Registry Seed (4) + Streaming Fix (5+5)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
**Phase 50** — Tool Registry Built-in Seed. Read `50-VERIFICATION.md`. Mechanism PASSED in v29.5; FR-A1-03/04 were deferred to live.

Live walk:
1. Single SSH to Mini PC: confirm Redis has 9 BUILT_IN_TOOL_IDS under `nexus:cap:tool:*` (sentinel `nexus:cap:_meta:lastSeedAt` exists):
   ```bash
   ssh -i ... bruce@10.69.31.68 \
     "source /opt/livos/.env; \
      REDIS_PASS=\$(echo \$REDIS_URL | sed -E 's|.*://[^:]*:([^@]+)@.*|\1|' | sed 's|%21|!|g'); \
      redis-cli -a \"\$REDIS_PASS\" --no-auth-warning KEYS 'nexus:cap:tool:*' | wc -l; \
      redis-cli -a \"\$REDIS_PASS\" --no-auth-warning EXISTS 'nexus:cap:_meta:lastSeedAt'; \
      redis-cli -a \"\$REDIS_PASS\" --no-auth-warning GET 'nexus:cap:_meta:lastSeedAt'"
   ```
2. Confirm count is ≥ 9 (the canonical 9 built-in tools: shell, docker_run, docker_ps, docker_logs, docker_stop, files_read, files_write, files_search, web_search).
3. Confirm sentinel exists with non-empty value.

**Phase 51** — Streaming Regression Fix. Read `51-VERIFICATION.md`. The 5 enumerated human-verify steps (lines 47-51):
1. **Deploy timing** — confirm `update.sh` ran with the `rm -rf dist` hardening; check `63-deploy-output.log` for the `rm -rf dist` line OR `verify_build` after vite build.
2. **Dist mtime** — single SSH: `ssh ... "ls -la /opt/livos/livos/packages/ui/dist/index.html"` — mtime should be within Wave 1's deploy window.
3. **Token streaming** — already verified live in 63-04 (Bolt.diy ≥3 deltas) and 63-05 (OWUI streaming) and 63-03 (raw curl). Cross-reference those evidences.
4. **Streaming-failure escalation** — if Phase 58 has an integration test for streaming-failure escalation, run it. Otherwise mark as PASS-via-cross-reference (Wave 2 + Wave 3 confirmed streaming works → escalation path implicitly tested).
5. **'Hangi modelsin?' identity test** — Bolt.diy "Who are you?" already tested in 63-04 → reference that evidence.

For each Phase 50 must-have AND Phase 51 must-have/human-verify step, append a row to 63-UAT-RESULTS.md.

Example rows:
```markdown
| v29.5 / 50 | FR-A1-03 | must-have-3 | nexus:cap:tool:* keys ≥9 on live Mini PC | PASS | 63-07-task2.log: count=9, sentinel present | 2026-05-02T<...> |
| v29.5 / 51 | FR-A2-XX | human-verify-3 | Token streaming visible | PASS | cross-ref: evidence/63-bolt-diy-streaming.mp4 (≥3 deltas) AND evidence/63-curl-streaming-events.txt | 2026-05-02T<...> |
```
  </what-built>
  <how-to-verify>
1. Open 63-UAT-RESULTS.md.
2. Confirm 4 v29.5/50 rows + 10 v29.5/51 rows added (5 must-haves + 5 human-verify steps).
3. ZERO BLOCKED.
  </how-to-verify>
  <resume-signal>Type "phase-50-51-walked" with PASS counts, OR list any BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-50-51-walked" with PASS counts, OR list any BLOCKED + remediation"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk Phase 52 + Phase 53 + Phase 54 — Marketplace State + Security Panel + Verification Gate (6+6+5)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
**Phase 52** — Marketplace State Correction. Read `52-VERIFICATION.md`. 6 must-haves; one was DEFERRED to "browser visit confirms apps post-deploy".

Live walk (browser):
1. Open https://&lt;test-user&gt;.livinity.io → Marketplace.
2. Confirm MiroFish is NOT listed (DELETED in Phase 52 migration).
3. Confirm Bolt.diy IS listed (carryover from v29.5 hot-patch + verified live in 63-04).
4. Confirm Phase 52 hot-patches (wrangler-install + OPENAI_LIKE_API_KEY) are visible in Bolt.diy compose:
   ```bash
   ssh -i ... bruce@10.69.31.68 \
     "sudo docker inspect \$(sudo docker ps -q --filter 'name=bolt-diy' | head -1) | grep -E 'OPENAI_LIKE_API_KEY|wrangler' | head -5"
   ```

**Phase 53** — Security Panel Render. Read `53-VERIFICATION.md`. 5 PASSED via Phase 51; 1 DEFERRED — sidebar 13-entry visual.

Live walk (browser):
1. Open LIVINITY_docker (Docker management app) in LivOS UI.
2. Expand sidebar (if collapsed — Phase 53 root cause was collapsed-sidebar visibility issue per MILESTONES.md).
3. Count sidebar entries — expected 13 (per 53-VERIFICATION.md).
4. Confirm "Security" entry visible in the list.
5. Click Security → fail2ban admin panel loads (Phase 46 work).
6. Capture screenshot to `evidence/63-uat-phase53-sidebar-13.png`.

**Phase 54** — Live-Verification Gate. Read `54-VERIFICATION.md`. 5 must-haves all PASSED in v29.5 itself; this Phase 63 is the FIRST real-world close where the gate work bears fruit.

Walk (mostly already-true assertions):
1. `gsd-toolkit complete-milestone` workflow exists at `~/.claude/get-shit-done/workflows/complete-milestone.md` — `<step name="live_verification_gate">` block present.
2. Wave 5 (63-11) will INVOKE the gate — that's the definitive proof of must-have-1.
3. AskUserQuestion default = "No" — verifiable in workflow source.
4. `--accept-debt` flag emits forensic-trail row — pre-existing v29.5 row in MILESTONES.md is the proof.
5. Retroactive v29.4 audit deferred — STATUS quo OK.

Append rows to 63-UAT-RESULTS.md. For Phase 54 items mostly mark PASS-by-mechanism (the live test of the GATE is Wave 5 itself; record those as "PASS, evidence: pending Wave 5 invocation").

Example:
```markdown
| v29.5 / 52 | FR-A3-XX | must-have-1 | MiroFish not in Marketplace | PASS | browser screenshot evidence/63-uat-phase52-marketplace.png; psql platform.apps WHERE id='mirofish' AND status='archived' | <timestamp> |
| v29.5 / 53 | FR-A4-XX | must-have-6 | Sidebar 13-entry visual | PASS | evidence/63-uat-phase53-sidebar-13.png shows 13 entries including Security | <timestamp> |
| v29.5 / 54 | FR-B1-XX | must-have-1 | gsd toolkit gate logic exists | PASS | ~/.claude/get-shit-done/workflows/complete-milestone.md grep for 'live_verification_gate' | <timestamp> |
```
  </what-built>
  <how-to-verify>
1. 63-UAT-RESULTS.md has 6 v29.5/52 rows + 6 v29.5/53 rows + 5 v29.5/54 rows.
2. ZERO BLOCKED.
3. `evidence/63-uat-phase52-marketplace.png` and `evidence/63-uat-phase53-sidebar-13.png` exist.
  </how-to-verify>
  <resume-signal>Type "phase-52-53-54-walked" with PASS counts + screenshot paths, OR list BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-52-53-54-walked" with PASS counts + screenshot paths, OR list BLOCKE"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="auto">
  <name>Task 4: v29.5 batch closeout — count rows + sacred file SHA</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <action>
Append a closing summary block to 63-UAT-RESULTS.md:

```markdown

<!-- ===== Wave 4a v29.5 batch closeout ===== -->
<!-- Walked: Phases 49 (12 rows) + 50 (4) + 51 (10) + 52 (6) + 53 (6) + 54 (5) = ~43 rows total -->
<!-- (Exact count may vary depending on how 49 sub-items decomposed.) -->
```

Then assert the sacred SHA + count rows added in this wave:

```bash
{
  echo ""
  echo "=== PLAN 63-07 CLOSE: v29.5 batch summary ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  V295_ROWS=$(grep -c '^| v29.5' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)
  BLOCKED=$(grep -c '^| v29.5.*| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)
  echo "v29.5 rows: $V295_ROWS"
  echo "v29.5 BLOCKED count: $BLOCKED (must be 0 for milestone close)"
  echo "--- sacred SHA ---"
  EXPECTED=4f868d318abff71f8c8bfbcf443b2393a553018b
  ACTUAL=$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)
  echo "expected: $EXPECTED"
  echo "actual:   $ACTUAL"
  if [ "$ACTUAL" = "$EXPECTED" ]; then echo "OK: sacred SHA holds"; else echo "BLOCK: sacred SHA drifted"; fi
  echo "=== END ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
```
  </action>
  <verify>
    <automated>powershell -Command "$rows = (Select-String '^\| v29.5' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md).Count; $blocked = (Select-String '^\| v29.5.*\| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md).Count; if ($rows -ge 30 -and $blocked -eq 0 -and (git hash-object nexus/packages/core/src/sdk-agent-runner.ts) -eq '4f868d318abff71f8c8bfbcf443b2393a553018b') { exit 0 } else { Write-Output \"rows=$rows blocked=$blocked\"; exit 1 }"</automated>
  </verify>
  <done>v29.5 batch walked; ≥30 rows added; ZERO BLOCKED; sacred SHA holds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → Mini PC SSH (per task) | single-batched per task; fail2ban discipline |
| orchestrator → LivOS browser (Marketplace, sidebar) | per-user subdomain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-07-01 | Tampering | UAT walker records false PASS without doing the actual check | mitigate | Every PASS row has a populated `evidence` column referencing concrete file/log/screenshot |
| T-63-07-02 | Denial-of-Service | Repeated SSH calls to Mini PC during walk get fail2ban-banned | mitigate | Each task batches its server-side checks into ONE SSH call |
| T-63-07-03 | Repudiation | Walker forgets a check; UAT-RESULTS missing rows | mitigate | Task 4 row count assertion (≥30 rows for v29.5 batch) catches under-walk |
</threat_model>

<verification>
**Plan-level verification:**
```bash
test -f .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md && \
  test "$(grep -c '^| v29.5' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)" -ge 30 && \
  test "$(grep -c '^| v29.5.*| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)" -eq 0 && \
  test "$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)" = "4f868d318abff71f8c8bfbcf443b2393a553018b"
```

**Sacred file SHA assertion:**
```bash
git hash-object nexus/packages/core/src/sdk-agent-runner.ts
# expected: 4f868d318abff71f8c8bfbcf443b2393a553018b
```
</verification>

<success_criteria>
- All 6 v29.5 VERIFICATION.md files walked (49, 50, 51, 52, 53, 54).
- ~30+ rows appended to 63-UAT-RESULTS.md with phase column showing v29.5/{N}.
- Each row has populated evidence column.
- ZERO BLOCKED rows in this batch.
- Sacred file SHA = `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- ZERO `--accept-debt`. ZERO new override row in MILESTONES.md.
</success_criteria>

<output>
After completion, create `.planning/phases/63-mandatory-live-verification/63-07-SUMMARY.md` with:
- v29.5 batch row count + PASS/FAIL/OBSOLETE breakdown.
- Any hot-patch loops triggered (phase, commit, UAT-id).
- Sacred file SHA confirmation.
- "v29.5 batch DONE — proceed to 63-08 (v29.4)" verdict.
</output>
