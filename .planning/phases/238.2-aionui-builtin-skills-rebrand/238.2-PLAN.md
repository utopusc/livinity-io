---
phase: 238.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-liv-assistant.sh
  - docs/liv-assistant-install.md
autonomous: true
requirements: [FR-V43-BUILTIN-SKILLS-REBRAND-01]
tags: [v43, hot-fix, builtin-skills, sed-replace, idempotent, sacred-sha-preserved]
---

# Phase 238.2 — Built-in skill `SKILL.md` Aion → Liv rebrand

## Context

Phase 238.1 (footer URL redirect) shipped 2026-05-27 evening. Phase 238 closure DEPLOY-LOG documented a KNOWN LIMITATION: "Built-in skill names + descriptions baked into the 94MB AionUi Bun ELF binary". Operator continued "Tamamdır devam et" → continuation work to close the residual.

### Discovery (single batched Mini PC SSH probe)

The KNOWN LIMITATION was **wrong**. Phase 238.2 probe found:

- `/opt/liv-assistant/data/builtin-skills/` exists on disk (24 SKILL.md files + references + scripts across 24 skill subdirs in 2 layers: top-level + `auto-inject/` sub-tier).
- Owner: `bruce:bruce`. mtime locked to install time (`2026-05-27 01:50:15`) — NOT updated by subsequent `systemctl restart liv-assistant` cycles. AionUi backend extracts builtin-skills only when missing.
- Tarball does NOT ship `builtin-skills/` (D.1 probe empty); the 94MB Bun binary extracts them on first start.
- `/api/skills` endpoint returns the FULL list with `location` field pointing at on-disk SKILL.md paths — confirming the renderer reads disk-side files.
- 11 files contain word-boundary or compound Aion variants. 8 are `.md` content (skill descriptions, references) — IN SCOPE. 3 are scripts (`.js`, `.py`, `.sh`) — OUT OF SCOPE (compound `AionUi → Liv AI` substitution has a SPACE that breaks code identifiers).

### Affected `.md` files (8)

```
auto-inject/aionui-skills/SKILL.md             (AionUI Skills registry text)
aionui-webui-setup/SKILL.md                    (AionUi WebUI config expert)
aionui-webui-setup/references/aionui-webui.md
star-office-helper/SKILL.md
star-office-helper/references/troubleshooting.md
star-office-helper/references/discovery.md
morph-ppt/SKILL.md
openclaw-setup/SKILL.md
```

### `/api/agents` Aion CLI entry — OUT OF SCOPE this phase

Probe E shows `/api/agents` returns TWO agents: `Aion CLI` (id=632f31d2, agent_type=`aionrs`, internal) + `Claude Code` (id=2d23ff1c, builtin ACP). The Aion CLI agent name comes from the Bun binary — actual binary-embedded. Operator's earlier "Aion CLI haricinde bir gözükmüyor" might or might not refer to this — needs separate investigation if operator clarifies. Phase 238.2 does NOT touch agent surface.

## Fix scope

**IN scope (Phase 238.2)**:
- Extend `scripts/install-liv-assistant.sh` with **Step 238.2-D** — idempotent sed pass over `${INSTALL_ROOT}/data/builtin-skills/**/*.md`. 5 substitutions chained (longest patterns first to preserve compound matches):
  1. `AionUi` → `Liv AI` (mirrors Phase 234-03)
  2. `AionUI` → `Liv AI` (case variant — NEW for skills "AionUI Skills" text)
  3. `aionui-web` → `liv-ai-web` (mirrors Phase 234-03)
  4. `aionui` → `liv-ai` (mirrors Phase 234-03)
  5. `\b(Aion|AION|aion)\b` → `Liv` (word-boundary catch-all from Phase 238-B)
- Idempotent grep pre/post wrapped with `set +o pipefail` (Phase 238 hot-fix precedent).
- Scoped to `.md` files only — code files (`.js`, `.py`, `.sh`) deliberately EXCLUDED (compound `Liv AI` has SPACE, breaks code identifiers).

**D-V42-NO-DATA-LOSS preserved**: target path strictly `${INSTALL_ROOT}/data/builtin-skills/`. Never touch `data/skills/` (user import dir), `data/sessions/`, `data/secrets/`, `data/aionui-backend.db`, or any other operator-state subdir.

### Sacred SHA gate

- Repo: `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- Mini PC: `62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe`

## Success criteria

- [ ] `scripts/install-liv-assistant.sh` Step 238.2-D block present and parses OK via `bash -n`
- [ ] Mini PC `bash /opt/livos/update.sh` EXIT 0 with `LivOS updated successfully!` + Step 238.2-D log line "Skill rebrand: applying ..."
- [ ] PRE Aion-variant grep count in `data/builtin-skills/*.md` > 0 → POST = 0 (proves Step 238.2-D fired)
- [ ] `/api/skills` JSON response no longer contains `AionUi`/`aionui`/standalone-Aion word-boundary tokens in description fields (excluding skill `name:` field — that's the directory name, unchanged)
- [ ] D-V42-NO-DATA-LOSS preserved — `data/skills/` (user import dir), `data/sessions/`, `data/aionui-backend.db` untouched
- [ ] Phase 238 + 238.1 non-regression: word-boundary Aion in `static/`=0; `iOfficeAI/*` URLs in `static/`=0
- [ ] LICENSE + NOTICE sha256 byte-identical (D-V43-APACHE-NOTICE)
- [ ] All 6 services active POST
- [ ] Sacred SHA `f3538e1d...` UNCHANGED 4-snapshot agreement
- [ ] Phase 237 `/liv/ws` 101 + Phase 234-04 `/liv-login` 302 non-regressed
- [ ] Single atomic feat commit pushed to origin
- [ ] DEPLOY-LOG.md authored
