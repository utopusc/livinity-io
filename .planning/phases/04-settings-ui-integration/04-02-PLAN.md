---
phase: 04-settings-ui-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - livos/packages/ui/src/routes/settings/ai-config.tsx
  - livos/packages/ui/src/routes/ai-chat/index.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Settings AI Configuration page shows a provider selector at the top to switch between Kimi and Claude"
    - "Both Kimi and Claude provider cards are visible with their individual auth status"
    - "Claude card has API key input with save button and OAuth PKCE login button"
    - "Switching provider in Settings calls setPrimaryProvider and updates the UI"
    - "AI chat sidebar header shows the active provider name as a small badge"
    - "After switching providers, the provider badge in chat updates to reflect the new provider"
  artifacts:
    - path: "livos/packages/ui/src/routes/settings/ai-config.tsx"
      provides: "Provider toggle + Claude auth card + provider cards"
      contains: "getClaudeStatus"
    - path: "livos/packages/ui/src/routes/ai-chat/index.tsx"
      provides: "Provider indicator badge in chat sidebar"
      contains: "getProviders"
  key_links:
    - from: "livos/packages/ui/src/routes/settings/ai-config.tsx"
      to: "tRPC ai.getProviders"
      via: "trpcReact.ai.getProviders.useQuery"
      pattern: "trpcReact\\.ai\\.getProviders"
    - from: "livos/packages/ui/src/routes/settings/ai-config.tsx"
      to: "tRPC ai.setPrimaryProvider"
      via: "trpcReact.ai.setPrimaryProvider.useMutation"
      pattern: "trpcReact\\.ai\\.setPrimaryProvider"
    - from: "livos/packages/ui/src/routes/settings/ai-config.tsx"
      to: "tRPC ai.getClaudeStatus"
      via: "trpcReact.ai.getClaudeStatus.useQuery"
      pattern: "trpcReact\\.ai\\.getClaudeStatus"
    - from: "livos/packages/ui/src/routes/ai-chat/index.tsx"
      to: "tRPC ai.getProviders"
      via: "trpcReact.ai.getProviders.useQuery"
      pattern: "trpcReact\\.ai\\.getProviders"
---

<objective>
Add provider selection toggle and Claude auth card to Settings AI Configuration page, and add an active provider indicator badge to the AI chat interface.

Purpose: Users need to see and switch between Claude and Kimi providers from Settings, manage Claude authentication (API key or OAuth), and see which provider is currently active in the chat interface. This completes the v16.0 Multi-Provider AI milestone.

Output: Updated ai-config.tsx with provider toggle + Claude card, updated ai-chat index.tsx with provider badge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-ui-integration/04-CONTEXT.md
@.planning/phases/04-settings-ui-integration/04-01-PLAN.md

<interfaces>
<!-- tRPC routes available after Plan 01 -->

trpcReact.ai.getClaudeStatus.useQuery()
  -> { authenticated: boolean; method?: string; provider: string }

trpcReact.ai.setClaudeApiKey.useMutation({ apiKey: string })
  -> { success: true }

trpcReact.ai.claudeStartLogin.useMutation()
  -> OAuth PKCE auth data

trpcReact.ai.claudeSubmitCode.useMutation({ code: string })
  -> { success: true }

trpcReact.ai.claudeLogout.useMutation()
  -> { success: boolean }

trpcReact.ai.getProviders.useQuery()
  -> { providers: Array<{ name: string; available: boolean }>; primaryProvider: string; fallbackOrder: string[] }

trpcReact.ai.setPrimaryProvider.useMutation({ provider: 'claude' | 'kimi' })
  -> { success: true; primaryProvider: string; fallbackOrder: string[] }

<!-- Existing Kimi routes (unchanged) -->
trpcReact.ai.getKimiStatus.useQuery() -> { authenticated: boolean; provider: string }
trpcReact.ai.kimiLogin.useMutation() -> { sessionId, verificationUrl, userCode }
trpcReact.ai.kimiLogout.useMutation() -> { success: boolean }
trpcReact.ai.kimiLoginPoll.useQuery({ sessionId }) -> { status: ... }

<!-- Existing UI patterns from ai-config.tsx -->
SettingsPageLayout: wrapper with title + description props
Kimi card pattern: bordered div, green border when connected, status icons (TbCircleCheck, TbAlertCircle, TbLoader2)
Connected state: green-400 text, TbCircleCheck icon, "Connected to Kimi"
Not connected: amber-400 text, TbAlertCircle icon, "Not connected"
Button variants: 'primary', 'secondary'. Sizes: 'sm'
Icons from react-icons/tb: TbLoader2, TbCircleCheck, TbAlertCircle, TbLogout, TbLogin, TbBrain, TbCopy, TbCheck

<!-- AI chat sidebar header (from ai-chat/index.tsx lines 343-358) -->
Sidebar header: div with flex, items-center, gap-2, containing IconBrain + "Liv AI" title
Mobile header (lines 681-697): "Liv AI" centered span
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider toggle and Claude auth card to Settings AI Configuration</name>
  <files>livos/packages/ui/src/routes/settings/ai-config.tsx</files>
  <read_first>
    - livos/packages/ui/src/routes/settings/ai-config.tsx (full file -- the complete existing Kimi-only page)
    - livos/packages/ui/src/routes/settings/_components/settings-page-layout.tsx (layout wrapper)
  </read_first>
  <action>
    Rewrite ai-config.tsx to add provider selection and Claude authentication alongside existing Kimi auth. The page structure becomes:

    **1. Provider Selector (top of page, before provider cards):**
    - Section title "Primary Provider" with `text-body font-semibold`
    - Two radio-style buttons side by side in a flex row, each being a clickable div:
      - Kimi button: shows "Kimi" label, when active has `border-brand bg-brand/10` styling
      - Claude button: shows "Claude" label, when active has `border-brand bg-brand/10` styling
      - Inactive buttons: `border-border-default bg-surface-base hover:bg-surface-1`
      - Each button: `rounded-radius-md border p-3 flex-1 cursor-pointer transition-colors`
      - Inside each: provider name in `text-body-sm font-medium` and availability dot (green if available, gray if not)
    - Uses `trpcReact.ai.getProviders.useQuery()` to read current primaryProvider
    - On click: call `trpcReact.ai.setPrimaryProvider.useMutation()` then invalidate getProviders query
    - Show the current primary with a filled radio indicator

    **2. Kimi Account Section (keep existing, unchanged):**
    - All existing Kimi auth logic stays exactly as-is
    - The entire existing Kimi section (lines 86-203 of current file) is preserved

    **3. Claude Account Section (NEW, after Kimi):**
    - Section title "Claude Account" with `text-body font-semibold` (same as Kimi's "Kimi Account")
    - Card with same border/bg pattern as Kimi card:
      - `border-brand/50 bg-brand/5` when connected
      - `border-border-default bg-surface-base` when not connected
    - Uses `trpcReact.ai.getClaudeStatus.useQuery()` for status
    - **Connected state:**
      - Green TbCircleCheck + "Connected to Claude" text
      - Show auth method: "Authenticated via API key" or "Authenticated via OAuth"
      - Sign Out button using `trpcReact.ai.claudeLogout.useMutation()`
    - **Not connected state:**
      - Two auth options stacked vertically:
        - **API Key section:** Input field (placeholder "sk-ant-...") + Save button
          - Uses `trpcReact.ai.setClaudeApiKey.useMutation()`
          - On save success: invalidate getClaudeStatus + show brief "Saved" state
          - On error: show red error text below input
        - **OAuth section:** "Or sign in with Claude" button
          - Uses `trpcReact.ai.claudeStartLogin.useMutation()`
          - On success, shows auth URL to open (same UX as Kimi login flow if applicable)
          - If OAuth returns a code prompt: shows input for code + submit button using claudeSubmitCode

    **4. Active Model Section (UPDATE):**
    - Change from hardcoded "Kimi for Coding" to dynamic based on primaryProvider
    - If primaryProvider is 'kimi': show "Kimi for Coding" with description "Thinking model with 128K context..."
    - If primaryProvider is 'claude': show "Claude (Anthropic)" with description "Claude AI model. Used for all AI tasks including chat, tool calling, and background agents."

    **Update SettingsPageLayout description** from "Configure how LivOS connects to Kimi AI" to "Configure AI providers for LivOS"

    **New imports needed:** TbKey icon from react-icons/tb (for API key section), Input from @/shadcn-components/ui/input

    **State management:**
    - Keep all existing Kimi state (loginSession, copied, pollIntervalRef)
    - Add Claude state: `claudeApiKey` (string), `claudeApiKeySaved` (boolean), `claudeOAuthCode` (string, for OAuth code input if needed)
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getClaudeStatus" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "setClaudeApiKey" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "claudeLogout" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "getProviders" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "setPrimaryProvider" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "Primary Provider" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "Claude Account" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "Kimi Account" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "Configure AI providers" livos/packages/ui/src/routes/settings/ai-config.tsx matches
    - grep "sk-ant-" livos/packages/ui/src/routes/settings/ai-config.tsx matches (API key placeholder)
    - grep "getKimiStatus" livos/packages/ui/src/routes/settings/ai-config.tsx matches (Kimi section preserved)
  </acceptance_criteria>
  <done>Settings AI Configuration page shows provider selector at top, existing Kimi card preserved, new Claude card with API key + OAuth auth, Active Model section dynamically reflects selected provider</done>
</task>

<task type="auto">
  <name>Task 2: Add active provider indicator badge to AI chat</name>
  <files>livos/packages/ui/src/routes/ai-chat/index.tsx</files>
  <read_first>
    - livos/packages/ui/src/routes/ai-chat/index.tsx (lines 340-390 for sidebar header, lines 680-697 for mobile header)
  </read_first>
  <action>
    Add a small provider badge to the AI chat interface that shows which provider is currently active. This uses the `trpcReact.ai.getProviders.useQuery()` route.

    **1. Inside the main AiChatPage component (the default export function):**
    Add a query at the top of the component:
    ```
    const providersQuery = trpcReact.ai.getProviders.useQuery(undefined, { refetchInterval: 30_000 })
    const activeProvider = providersQuery.data?.primaryProvider ?? 'kimi'
    ```

    **2. Sidebar header badge (inside the ChatSidebar component):**
    The ChatSidebar component needs to receive `activeProvider` as a prop.

    In the sidebar header area (after the "Liv AI" h2 text on line 350), add a small badge:
    ```tsx
    <span className='rounded-full bg-surface-2 px-2 py-0.5 text-caption-sm font-medium text-text-secondary capitalize'>
      {activeProvider}
    </span>
    ```

    So the header becomes: [brain icon] Liv AI [kimi badge] ... [+ button]

    **3. Mobile header badge:**
    In the mobile header area (line 689), change the "Liv AI" span to include the badge:
    ```tsx
    <div className='flex items-center gap-2'>
      <span className='text-body font-semibold text-text-primary'>Liv AI</span>
      <span className='rounded-full bg-surface-2 px-2 py-0.5 text-caption-sm font-medium text-text-secondary capitalize'>
        {activeProvider}
      </span>
    </div>
    ```

    **ChatSidebar prop threading:**
    - Add `activeProvider: string` to ChatSidebar's props (the function at ~line 335)
    - Pass it from the parent: `<ChatSidebar ... activeProvider={activeProvider} />`

    **Minimal changes only.** Do NOT restructure or modify any other part of the chat component. Only add the query, prop, and two badge elements.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "getProviders" livos/packages/ui/src/routes/ai-chat/index.tsx matches
    - grep "primaryProvider" livos/packages/ui/src/routes/ai-chat/index.tsx matches
    - grep "activeProvider" livos/packages/ui/src/routes/ai-chat/index.tsx matches
    - grep "capitalize" livos/packages/ui/src/routes/ai-chat/index.tsx matches (badge styling)
    - grep "Liv AI" livos/packages/ui/src/routes/ai-chat/index.tsx still matches (title preserved)
  </acceptance_criteria>
  <done>AI chat sidebar and mobile header show the active provider name as a small badge that updates when the provider is switched in Settings</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes for UI: `cd livos && npx tsc --noEmit --project packages/ui/tsconfig.json`
2. Settings page has provider selector, Kimi card, Claude card with auth, dynamic Active Model
3. AI chat shows provider badge in sidebar header and mobile header
4. Existing Kimi auth functionality is preserved and unchanged
5. All three requirements met: UI-01 (toggle + status), UI-02 (chat indicator), UI-03 (switching works)
</verification>

<success_criteria>
- UI compiles without TypeScript errors
- Provider selector toggle visible at top of AI Configuration page
- Claude auth card shows API key input and OAuth login option
- Active provider badge visible in both desktop sidebar and mobile header of AI chat
- Switching provider in Settings updates the badge in chat (via query refetch)
- Kimi auth flow unchanged and still working
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-ui-integration/04-02-SUMMARY.md`
</output>
