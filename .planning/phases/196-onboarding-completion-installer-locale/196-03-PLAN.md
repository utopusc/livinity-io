---
phase: 196-onboarding-completion-installer-locale
plan: 03
type: execute
wave: 2
depends_on: [196-01]
files_modified:
  - livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx
  - livos/packages/ui/src/features/onboarding-flow/steps/provider-step.test.tsx
  - livos/packages/ui/src/features/onboarding-flow/constants.ts
  - livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx
autonomous: true
requirements: []
tags: [onboarding, ui, provider-selection, auto-route, xai]

user_setup: []

must_haves:
  truths:
    - "When the operator clicks the xAI card in the new provider-selection step, the wizard advances to ConnectAiStep within one render tick (no intermediate Continue click required)"
    - "Non-xAI providers (Claude, Anthropic API, OpenAI) render as disabled cards with a `Coming soon` badge — they do NOT advance the wizard when clicked"
    - "The wizard's existing wave-navigation Props contract `{onContinue, onSkip, onBack}` is preserved exactly; provider-step exposes the same shape"
    - "Selecting xAI persists the choice to OnboardingData (provider: 'xai') so step 5 (ConnectAiStep) and step 6 (Done) can read it"
    - "ConnectAiStep is now reached at stepper index 5 (was 4) — every reference in the codebase that hard-coded 4 is updated"
  artifacts:
    - path: "livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx"
      provides: "Provider selection step component; xAI card auto-routes; other providers disabled with badge"
      min_lines: 80
    - path: "livos/packages/ui/src/features/onboarding-flow/steps/provider-step.test.tsx"
      provides: "Vitest + react-dom/client harness (D-NO-NEW-DEPS) covering the three behavioural truths above"
      min_lines: 100
    - path: "livos/packages/ui/src/features/onboarding-flow/constants.ts"
      provides: "TOTAL = 7 (was 6); STEP_NAMES extended with 'Provider' before 'Connect AI'; OnboardingData adds `provider?: 'xai' | 'claude' | 'openai' | 'anthropic'`; STEP_WEIGHT entry added"
      contains: "Provider"
    - path: "livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx"
      provides: "ProviderStep mounted at stepIndex 4; ConnectAiStep moved to stepIndex 5; DoneStep moved to stepIndex 6"
  key_links:
    - from: "provider-step.tsx (xAI card onClick handler)"
      to: "props.onContinue()"
      via: "single tick — no intermediate state"
      pattern: "onClick\\s*=\\s*\\{?\\s*\\(\\)\\s*=>"
    - from: "setup-wizard-v2.tsx"
      to: "ProviderStep"
      via: "<Step stepIndex={4}>"
      pattern: "<ProviderStep"
    - from: "OnboardingData.provider"
      to: "ConnectAiStep / DoneStep (downstream consumers)"
      via: "data.provider"
      pattern: "data\\.provider"

sacred_constraints:
  - "liv/packages/core/src/sdk-agent-runner.ts SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f MUST remain byte-identical"
  - "scripts/install/sudoers.d/livinityd SHA MUST remain byte-identical (Plan 196-03 is UI-only; does not touch sudoers)"
  - "Do NOT touch liv/packages/*; this is a livos/packages/ui change only"
  - "Do NOT add a new dep — use the existing react-dom/client + vi.mock harness pattern from connect-ai-step.test.tsx and inline-tool-pill.unit.test.tsx (D-NO-NEW-DEPS preserved per Phase 195-04)"
  - "Do NOT touch ConnectAiStep internals — only its mount position in setup-wizard-v2.tsx changes"
---

<objective>
Introduce a provider-selection step ahead of ConnectAiStep so that picking xAI single-clicks the operator into the OAuth flow (vs the current "click provider → click Continue" two-click implied by the operator directive).

Purpose: CONTEXT.md decision 196-03 — "selecting xAI immediately advances to ConnectAiStep (no intermediate confirmation)." This sets up the funnel for Phase 197+ providers (Claude, OpenAI, Anthropic) which currently ship as `Coming soon` placeholders, and locks the auto-route pattern as the canonical UX.

Output:
- New provider-step.tsx with 4 cards (xAI ENABLED + auto-routing, three others disabled).
- New provider-step.test.tsx with at least 4 vitest assertions.
- TOTAL bumped from 6 → 7 in constants.ts; STEP_NAMES extended; OnboardingData gains a `provider` field.
- setup-wizard-v2.tsx mounts ProviderStep at stepIndex 4 and shifts ConnectAiStep/DoneStep to 5/6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-onboarding-completion-installer-locale/196-CONTEXT.md
@.planning/phases/195-xai-oauth-onboarding/195-04-SUMMARY.md

<interfaces>
<!-- The existing wizard step contract. -->

From livos/packages/ui/src/features/onboarding-flow/constants.ts (CURRENT):
```ts
export const TOTAL = 6
export const STEP_NAMES = ['Welcome','Account','Wallpaper','Personalize','Connect AI','All set'] as const
export const STEP_WEIGHT = [15, 60, 20, 45, 25, 5] as const
export type OnboardingData = {
  lang: string; name: string; password: string; confirm: string;
  authMode: 'password' | '2fa'; otpSecret?: string; otpCode?: string;
  wallpaper: string; role: string; style: 'concise' | 'direct' | 'detailed';
  useCases: string[]; useCasesTouched: boolean; tone: number;
  memory: 'off' | 'session' | 'persistent';
}
```

From connect-ai-step.tsx (CURRENT — Phase 195-04):
```ts
type Props = { onContinue: () => void; onSkip: () => void; onBack: () => void }
```
ConnectAiStep DOES NOT depend on a provider prop — it is xAI-only. This plan does NOT pass provider into it; the data.provider field is for downstream / future use.

From connect-ai-step.test.tsx (canonical D-NO-NEW-DEPS harness):
- Uses `import {createRoot} from 'react-dom/client'`
- Uses `vi.mock('@/trpc/trpc')` to stub the tRPC client
- NO @testing-library/react

From setup-wizard-v2.tsx (CURRENT — see lines 198-243 of the file):
- 6 Step components stacked, stepIndex 0..5
- ConnectAiStep is at stepIndex 4
- DoneStep is at stepIndex 5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ProviderStep component + vitest covering auto-route + disabled-card + props contract</name>
  <files>livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx, livos/packages/ui/src/features/onboarding-flow/steps/provider-step.test.tsx</files>

  <read_first>
    - livos/packages/ui/src/features/onboarding-flow/steps/connect-ai-step.test.tsx (MIRROR this harness — react-dom/client + vi.mock pattern; NO @testing-library/react)
    - livos/packages/ui/src/features/onboarding-flow/steps/connect-ai-step.tsx lines 1-80 (style: discriminated unions, hooks, FooterBar wiring)
    - livos/packages/ui/src/features/onboarding-flow/footer-bar.tsx (Continue/Skip/Back contract)
    - livos/packages/ui/src/features/onboarding-flow/icon.tsx (existing icon set; check for an xAI/Grok icon, fallback to a text label if absent)
  </read_first>

  <behavior>
    provider-step.tsx:
    - Props: `{data: OnboardingData; setData: (d: OnboardingData) => void; onContinue: () => void; onSkip: () => void; onBack: () => void}` — same contract shape as the other steps that mutate OnboardingData (account-step, wallpaper-step, personalize-step).
    - Renders a header: "Choose your AI provider".
    - Renders 4 cards in a grid:
      - **xAI (Grok)** — ENABLED, primary border-cyan styling, no badge. onClick: `setData({...data, provider: 'xai'}); onContinue();` in the SAME synchronous handler (single tick, no setTimeout).
      - **Claude (Anthropic)** — DISABLED, opacity-50, "Coming soon" badge top-right. onClick is a noop (or absent; do not register handler).
      - **OpenAI** — DISABLED, "Coming soon" badge.
      - **Anthropic API** — DISABLED, "Coming soon" badge.
    - FooterBar at the bottom with `continueDisabled={true}` ALWAYS — provider selection is the one-click path; the footer Continue button is intentionally inert so the only way forward is via the xAI card. Back button still works.
    - No tRPC calls; this step is pure UI.

    provider-step.test.tsx:
    - Mirror connect-ai-step.test.tsx harness verbatim (createRoot, vi.mock('@/trpc/trpc'), act, etc.).
    - Test 1 "idle render": all 4 cards rendered; xAI has data-testid="provider-card-xai"; 3 disabled cards have aria-disabled="true" or class containing "is-disabled" / data-disabled.
    - Test 2 "xAI auto-route fires onContinue exactly once in the same handler": create the component with vi.fn() spies for onContinue + setData; simulate click on the xAI card; assert setData called once with `{provider: 'xai'}` shape and onContinue called once; assert NO Continue button click was needed.
    - Test 3 "disabled cards do NOT advance": click each of the 3 disabled cards; assert onContinue spy call count is 0 after all three clicks.
    - Test 4 "footer Continue is inert, Back still works": Continue button is disabled (or absent); clicking the Back button calls onBack spy exactly once.
  </behavior>

  <action>
    Implement provider-step.tsx with the cards listed in the behavior block. The xAI card's onClick handler is verbatim:
    ```ts
    const handleSelectXai = () => {
      setData({...data, provider: 'xai'})
      onContinue()
    }
    ```
    No setTimeout, no useEffect, no requestAnimationFrame — single synchronous handler so vitest assertions hold deterministically.

    Disabled card markup must include:
    - `aria-disabled="true"`
    - `className="provider-card is-disabled"` (re-use existing onb-style hooks if present in onboarding.css)
    - A `<span className="provider-card-badge">Coming soon</span>` element
    - NO onClick handler (omit the prop entirely; pointer-events:none in CSS is also acceptable)

    Tests must be PURE behaviour assertions on the rendered DOM + spy invocations. NO snapshot tests. NO @testing-library/react import.

    Add a 5-line header docblock to provider-step.tsx citing Phase 196-03 + the rationale ("xAI selection is the funnel; others are Coming soon placeholders for Phase 197+").

    Verify the test runs in isolation via `npx vitest run livos/packages/ui/src/features/onboarding-flow/steps/provider-step.test.tsx`.
  </action>

  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/features/onboarding-flow/steps/provider-step.test.tsx --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run on provider-step.test.tsx reports 4 PASS / 0 FAIL
    - grep -c "@testing-library" livos/packages/ui/src/features/onboarding-flow/steps/provider-step.test.tsx → 0 (D-NO-NEW-DEPS)
    - grep -c "Phase 196-03" livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx → ≥ 1
    - grep -cE "Coming soon" livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx → 3 (exactly the three disabled cards)
    - grep -c "provider: 'xai'" livos/packages/ui/src/features/onboarding-flow/steps/provider-step.tsx → ≥ 1
    - bash scripts/verify-sacred-sha.sh exits 0
  </acceptance_criteria>

  <done>
    provider-step.tsx renders 4 cards (1 active, 3 disabled), the xAI card auto-routes in a single handler, the test file passes 4 assertions, sacred guard passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ProviderStep into setup-wizard-v2.tsx (TOTAL=7, step shift) + constants update</name>
  <files>livos/packages/ui/src/features/onboarding-flow/constants.ts, livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx</files>

  <read_first>
    - livos/packages/ui/src/features/onboarding-flow/constants.ts (entire file)
    - livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx lines 195-260 (the Step block + Done navigation)
    - All other places in livos/packages/ui/ that import TOTAL or STEP_NAMES (grep first):
      `grep -rn "TOTAL\\|STEP_NAMES\\|STEP_WEIGHT" livos/packages/ui/src/features/onboarding-flow/ livos/packages/ui/src/routes/onboarding/`
  </read_first>

  <behavior>
    constants.ts changes:
    - TOTAL: 6 → 7
    - STEP_NAMES: insert 'Provider' before 'Connect AI' → `['Welcome','Account','Wallpaper','Personalize','Provider','Connect AI','All set']`
    - STEP_WEIGHT: insert 10 (lightweight one-click step) before the 25 (Connect AI weight) → `[15, 60, 20, 45, 10, 25, 5]`
    - OnboardingData type: add `provider?: 'xai' | 'claude' | 'openai' | 'anthropic'`
    - DEFAULT_DATA: no change (provider stays undefined until selection)

    setup-wizard-v2.tsx changes:
    - Import ProviderStep from `@/features/onboarding-flow/steps/provider-step`.
    - Insert a `<Step stepIndex={4}>` block containing `<ProviderStep data={data} setData={setData} onContinue={stepper.next} onSkip={stepper.next} onBack={stepper.back} />` between the existing PersonalizeStep (stepIndex 3) and ConnectAiStep blocks.
    - Bump ConnectAiStep's stepIndex from 4 to 5.
    - Bump DoneStep's stepIndex from 5 to 6.
    - Bump DoneStep's `isActive={stepper.idx === 5}` to `isActive={stepper.idx === 6}`.

    No other call sites should break — TOTAL is read dynamically by useStepper so the bump propagates correctly.
  </behavior>

  <action>
    1. Update constants.ts exactly as described (4 numeric/string edits + 1 type extension).

    2. In setup-wizard-v2.tsx, add the new Step block. The diff must touch ONLY:
       - The import block at top (one new import line)
       - The Step component grid in the JSX return (one insertion + two stepIndex shifts + one isActive shift)

    3. Run a full grep audit AFTER the edits:
       ```
       grep -rn "stepIndex={5}\\|stepIndex={6}\\|stepper.idx === 5\\|stepper.idx === 6" livos/packages/ui/src/
       ```
       Resolve any hits that need adjustment. (The wizard is the only file that should reference these indices.)

    4. Re-run the existing onboarding test suite to confirm nothing regresses:
       ```
       npx vitest run livos/packages/ui/src/features/onboarding-flow
       ```
  </action>

  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/features/onboarding-flow --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "^export const TOTAL = 7" livos/packages/ui/src/features/onboarding-flow/constants.ts → 1
    - grep -c "'Provider'," livos/packages/ui/src/features/onboarding-flow/constants.ts → 1
    - grep -c "provider?:" livos/packages/ui/src/features/onboarding-flow/constants.ts → 1
    - grep -c "<ProviderStep" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx → 1
    - grep -c "stepIndex={4}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx → 1
    - grep -c "stepIndex={5}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx → 1
    - grep -c "stepIndex={6}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx → 1
    - grep -c "stepper.idx === 6" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx → 1
    - All existing onboarding-flow vitest files (account-step / wallpaper-step / personalize-step / connect-ai-step) continue to pass: `npx vitest run src/features/onboarding-flow` exit 0
    - pnpm --filter ui build exits 0 (no TS errors introduced)
    - bash scripts/verify-sacred-sha.sh exits 0
  </acceptance_criteria>

  <done>
    Wizard renders 7 steps; ProviderStep at index 4; ConnectAiStep at index 5; DoneStep at index 6; build green; sacred guard passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-browser → React onClick handlers | Pure client-side. Phase 196-03 adds no new network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-03-01 | Tampering | A disabled-card onClick is wired by mistake and a user picks `claude` → ConnectAiStep crashes (it's xAI-only) | mitigate | Test 3 asserts onContinue is NEVER called for the 3 disabled cards. Disabled cards omit the onClick prop entirely (not just `disabled` styling). |
| T-196-03-02 | Spoofing | An attacker injects a provider value via OnboardingData → ConnectAiStep displays a wrong tier | accept | OnboardingData is client-state only; even if poisoned, the eventual auth call goes to `trpc.auth.xai.start` which is server-validated. The `provider` field is informational. |
| T-196-03-03 | Repudiation | The wizard advances without recording the provider choice | mitigate | setData({...data, provider: 'xai'}) executes BEFORE onContinue(), and Phase 137-04 backend resume persists OnboardingData via preferences.set within 500ms — the choice survives a reload. |
| T-196-03-04 | Information Disclosure | provider-step displays sensitive provider metadata (API keys, OAuth secrets) | accept | The step renders only logo + name + status badge. No secrets. |
</threat_model>

<verification>
- npx vitest run src/features/onboarding-flow/steps/provider-step.test.tsx → 4 PASS
- npx vitest run src/features/onboarding-flow → 4 + (existing test count) PASS, 0 NEW failures
- pnpm --filter ui build → exit 0
- bash scripts/verify-sacred-sha.sh → exit 0 (every commit)

**Operator UAT (deferred):** load `/onboarding`, walk Welcome → Account → Wallpaper → Personalize → Provider; clicking xAI card single-clicks to ConnectAiStep; clicking the back button returns to Provider; non-xAI cards visibly grey + show "Coming soon" badge.
</verification>

<success_criteria>
- One new component (provider-step.tsx), one new test (provider-step.test.tsx), one updated constants.ts, one updated setup-wizard-v2.tsx.
- 4 vitest assertions passing on provider-step.test.tsx; existing onboarding-flow tests unaffected.
- Wizard step count is 7; ConnectAiStep at stepIndex 5; DoneStep at stepIndex 6.
- Sacred SHA + sudoers fragment SHA preserved.
- 1-2 atomic commits: (a) feat(196-03): ProviderStep component + auto-route + vitest; (b) feat(196-03): wire ProviderStep into setup-wizard-v2 (TOTAL=7, step shift).
- No deferred-scope language; no placeholder selection logic.
</success_criteria>

<output>
After completion, create `.planning/phases/196-onboarding-completion-installer-locale/196-03-SUMMARY.md` documenting:
- Commit SHAs
- vitest output (4 PASS for provider-step.test.tsx + full onboarding-flow suite still green)
- pnpm --filter ui build output
- sacred SHA fingerprints pre/post
</output>
