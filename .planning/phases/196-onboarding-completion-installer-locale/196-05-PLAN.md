---
phase: 196-onboarding-completion-installer-locale
plan: 05
type: execute
wave: 3
depends_on: [196-04, 196-03]
files_modified:
  - livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx
  - livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.test.tsx
  - livos/packages/ui/src/lib/intl.ts
  - livos/packages/ui/src/lib/intl.test.ts
  - livos/packages/livinityd/source/modules/locale/timezone-service.ts
  - livos/packages/livinityd/source/modules/locale/timezone-service.test.ts
  - livos/packages/livinityd/source/modules/locale/index.ts
  - livos/packages/livinityd/source/modules/server/trpc/setup-router.ts
  - livos/packages/livinityd/source/modules/server/trpc/setup-router.test.ts
  - livos/packages/livinityd/source/modules/server/trpc/common.ts
  - livos/packages/livinityd/source/index.ts
  - livos/packages/ui/src/features/onboarding-flow/constants.ts
  - livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx
  - scripts/install/sudoers.d/livinityd
  - scripts/sacred-shas-v38.json
autonomous: true
requirements: []
tags: [onboarding, locale, timezone, intl, sudoers, sacred-registry-update, wizard-wire-up]

user_setup:
  - service: system-clock
    why: "Plan 196-05 invokes `sudo /usr/bin/timedatectl set-timezone <iana-zone>` via a narrow Cmnd_Alias extension to the Phase 192 sudoers fragment. The boundary widens by exactly one entry; the sacred-shas-v38.json registry pin MUST be updated atomically in the same commit."
    env_vars: []
    dashboard_config:
      - task: "After install, verify `cat /etc/timezone` matches the operator's selection from the onboarding step"
        location: "Mini PC shell, post onboarding completion"

must_haves:
  truths:
    - "LocaleTimezoneStep auto-detects timezone via `Intl.DateTimeFormat().resolvedOptions().timeZone` and locale via `navigator.language` on mount"
    - "Operator can override timezone via a searchable select listing values from `Intl.supportedValuesOf('timeZone')`"
    - "Operator can override locale via a searchable select listing 6 supported codes (en-US, tr-TR, de-DE, fr-FR, es-ES, ar-SA)"
    - "Clicking Continue calls `trpc.setup.setLocaleTimezone({timezone, locale})` which persists to Redis `liv:user:timezone` + `liv:user:locale` AND invokes `sudo /usr/bin/timedatectl set-timezone <zone>` server-side"
    - "Backend re-validates timezone against `Intl.supportedValuesOf('timeZone')` BEFORE invoking timedatectl (defense-in-depth — even though the UI only offers valid options)"
    - "After Continue with timezone=Europe/Istanbul, `cat /etc/timezone` on the Mini PC reports `Europe/Istanbul`"
    - "Sudoers TIMEDATECTL Cmnd_Alias allows ONLY `/usr/bin/timedatectl set-timezone *` — not the full timedatectl surface; no other commands granted"
    - "scripts/sacred-shas-v38.json is updated in the SAME commit that mutates scripts/install/sudoers.d/livinityd; the pre-commit sacred-sha hook passes"
    - "The wizard now has 8 steps (Welcome / Account / Wallpaper / Personalize / Provider / Region / Locale-Timezone / ConnectAi / Done — wait, that is 9). CORRECTION: After Plans 196-03 (provider) + 196-04 (region) + 196-05 (locale): TOTAL = 9; final step order is [Welcome, Account, Wallpaper, Personalize, Provider, Region, Locale-Timezone, ConnectAi, Done]"
    - "Existing tests for connect-ai-step + provider-step + region-step continue to pass after the wizard rewire"
  artifacts:
    - path: "livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx"
      provides: "Locale + timezone step; auto-detect + searchable selects; Continue persists + calls timedatectl"
      min_lines: 140
    - path: "livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.test.tsx"
      provides: "Vitest covering auto-detect, override, Continue, Skip, invalid-input rejection"
      min_lines: 140
    - path: "livos/packages/ui/src/lib/intl.ts"
      provides: "Three formatter helpers: formatDate, formatTime, formatNumber — used in dashboard/settings post-onboarding"
      min_lines: 30
    - path: "livos/packages/ui/src/lib/intl.test.ts"
      provides: "Smoke tests for the 3 formatters across the 6 supported locales"
    - path: "livos/packages/livinityd/source/modules/locale/timezone-service.ts"
      provides: "Server-side service: validate(zone) using Intl.supportedValuesOf + setSystemTimezone(zone) shelling out to `sudo /usr/bin/timedatectl set-timezone <zone>`"
      min_lines: 60
    - path: "livos/packages/livinityd/source/modules/locale/timezone-service.test.ts"
      provides: "Vitest: validate happy/invalid paths + setSystemTimezone via mocked execFile (no real timedatectl invocation in CI)"
    - path: "livos/packages/livinityd/source/modules/server/trpc/setup-router.ts"
      provides: "MOD: adds setLocaleTimezone procedure alongside setRegion (Plan 196-04 left the file open)"
      contains: "setLocaleTimezone"
    - path: "livos/packages/livinityd/source/modules/server/trpc/common.ts"
      provides: "MOD: adds 'setup.setLocaleTimezone' to httpOnlyPaths"
      contains: "setup.setLocaleTimezone"
    - path: "livos/packages/livinityd/source/index.ts"
      provides: "MOD: production wire-up — construct timezoneService + extend createSetupRouter call to include both setRegion (196-04) + setLocaleTimezone (196-05); pass into createAppRouter via setup slot"
    - path: "livos/packages/ui/src/features/onboarding-flow/constants.ts"
      provides: "MOD: TOTAL = 9; STEP_NAMES extended with 'Region' + 'Locale & Time' before 'Connect AI'; STEP_WEIGHT entries added; OnboardingData gains `region?, country?, timezone?, locale?` fields"
    - path: "livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx"
      provides: "MOD: mounts RegionStep at stepIndex 5 + LocaleTimezoneStep at stepIndex 6; ConnectAiStep shifts to 7; DoneStep shifts to 8"
    - path: "scripts/install/sudoers.d/livinityd"
      provides: "MOD: adds Cmnd_Alias LIVINITYD_TIMEDATECTL = /usr/bin/timedatectl set-timezone * + bruce ALL=(root) NOPASSWD entry"
    - path: "scripts/sacred-shas-v38.json"
      provides: "MOD: updates the SHA pin for scripts/install/sudoers.d/livinityd to reflect the new fragment content; same commit as the sudoers diff"
  key_links:
    - from: "locale-timezone-step.tsx Continue handler"
      to: "trpc.setup.setLocaleTimezone.mutate"
      via: "useMutation"
      pattern: "setLocaleTimezone"
    - from: "setup-router.ts setLocaleTimezone procedure body"
      to: "timezoneService.setSystemTimezone"
      via: "execFile('sudo', ['/usr/bin/timedatectl', 'set-timezone', zone])"
      pattern: "timedatectl"
    - from: "timezone-service.ts validate"
      to: "Intl.supportedValuesOf('timeZone')"
      via: "Set membership check"
      pattern: "Intl\\.supportedValuesOf"
    - from: "sudoers.d/livinityd"
      to: "scripts/sacred-shas-v38.json (locked SHA entry for the same path)"
      via: "sha1sum of the new file written back into the JSON registry"
      pattern: "scripts/install/sudoers.d/livinityd"

sacred_constraints:
  - "liv/packages/core/src/sdk-agent-runner.ts SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f MUST remain byte-identical"
  - "Plan 196-05 is the ONLY plan in Phase 196 authorised to mutate scripts/install/sudoers.d/livinityd; the diff MUST be exactly one new Cmnd_Alias + one NOPASSWD entry; the registered SHA in scripts/sacred-shas-v38.json MUST be updated atomically in the same commit"
  - "Sudoers TIMEDATECTL alias MUST be tight: `/usr/bin/timedatectl set-timezone *` — not `/usr/bin/timedatectl *`; glob only in the argument position per the Phase 192-01 precedent"
  - "Do NOT add a new top-level dependency to package.json (Intl APIs are built-in; execFile is from node:child_process)"
  - "Do NOT touch liv/packages/* — locale logic stays inside livos/packages/livinityd + livos/packages/ui"
  - "Mini PC sole deployment target; no Server4/Server5 references"
---

<objective>
Add a locale + timezone configuration step to the onboarding wizard and wire the system clock to honour the operator's selection. This is the deepest plan in Phase 196: it ships UI + backend service + tRPC procedure extension + livinityd production injection + wizard re-wiring + sacred sudoers fragment extension + atomic sacred-registry update.

Purpose: CONTEXT.md decision 196-05. After Phase 196-05 closes, the onboarding flow proves end-to-end across 9 steps and the Mini PC's system clock is in the operator's actual local time zone (not whatever UTC default the install image shipped with).

Output:
- LocaleTimezoneStep React component + test
- intl.ts UI formatter helpers + test
- timezone-service.ts server module + test
- setup-router.ts extension (adds setLocaleTimezone alongside setRegion)
- livinityd/source/index.ts production wire-up for setupRouter
- Wizard constants + setup-wizard-v2.tsx step re-wiring (TOTAL bumps from 7 → 9, integrating both 196-04's RegionStep and this plan's LocaleTimezoneStep)
- Sudoers fragment extension + sacred-shas-v38.json re-pin in the same commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-onboarding-completion-installer-locale/196-CONTEXT.md
@.planning/phases/196-onboarding-completion-installer-locale/196-04-SUMMARY.md
@.planning/phases/196-onboarding-completion-installer-locale/196-03-SUMMARY.md
@.planning/phases/196-onboarding-completion-installer-locale/196-01-SUMMARY.md

<interfaces>
<!-- Plan-196-04 outputs (Wave 2 input to this plan) -->

From livos/packages/livinityd/source/modules/locale/index.ts (196-04):
```ts
export {Region, REGIONS, countryToRegion, timezoneToRegion} from './region-suggestion.js'
// Plan 196-05 ADDS: export {createTimezoneService, TimezoneService} from './timezone-service.js'
```

From livos/packages/livinityd/source/modules/server/trpc/setup-router.ts (196-04):
```ts
export function createSetupRouter(deps: {redis: RedisLike}) {
  return router({
    setRegion: adminProcedure.input(z.object({...})).mutation(...),
    // Plan 196-05 ADDS: setLocaleTimezone: adminProcedure.input(z.object({timezone: z.string(), locale: z.enum([...])})).mutation(...)
  })
}
```
The setup-router factory needs an extra dep for 196-05: `timezoneService: TimezoneService`. Plan 196-05 will extend createSetupRouter's deps signature.

From livos/packages/ui/src/features/onboarding-flow/constants.ts (after 196-03 + 196-04 outputs land):
- After 196-03: TOTAL = 7 (Welcome, Account, Wallpaper, Personalize, Provider, ConnectAi, Done)
- 196-04 ships RegionStep but does NOT mount it (file-disjoint with 196-03 by design).
- Plan 196-05 mounts both RegionStep + LocaleTimezoneStep in setup-wizard-v2.tsx and bumps TOTAL to 9.

From scripts/install/sudoers.d/livinityd (Phase 192-01):
- Current SHA pin in sacred-shas-v38.json: entry #N (path: scripts/install/sudoers.d/livinityd)
- File ends at line 53; Plan 196-05 appends 3 new lines (Cmnd_Alias + comment + NOPASSWD entry).

From scripts/sacred-shas-v38.json:
- Entry shape: `{path, expected_sha, frozen_in_phase, rationale}` — Plan 196-05 updates the `expected_sha` for the sudoers file AND adds a rationale note referencing Phase 196-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sudoers fragment extension + sacred-registry re-pin (SAME commit)</name>
  <files>scripts/install/sudoers.d/livinityd, scripts/sacred-shas-v38.json</files>

  <read_first>
    - scripts/install/sudoers.d/livinityd (entire current file — 53 lines)
    - scripts/sacred-shas-v38.json (locate the entry with path == "scripts/install/sudoers.d/livinityd")
    - scripts/verify-sacred-sha.sh (understand how the pre-commit hook compares SHAs)
    - scripts/verify-sacred-sha.cjs (the Node-side verifier)
  </read_first>

  <behavior>
    Append exactly 3 new lines to scripts/install/sudoers.d/livinityd:

    ```
    # 23. Phase 196-05 — onboarding locale-timezone step ships timedatectl set-timezone
    Cmnd_Alias LIVINITYD_TIMEDATECTL = /usr/bin/timedatectl set-timezone *
    bruce ALL=(root) NOPASSWD: LIVINITYD_TIMEDATECTL
    ```

    The argument-position glob is intentional (matches the Phase 192-01 precedent on lines 28-39). The new NOPASSWD entry is a SEPARATE line, not merged into the existing line 46 — keeping additions discoverable in git blame.

    THEN: compute the new SHA via `sha1sum scripts/install/sudoers.d/livinityd | cut -d' ' -f1` and update scripts/sacred-shas-v38.json:
    - Update `expected_sha` for the matching entry to the new value.
    - Update `frozen_in_phase` to `"196-05-locale-timezone (extended from 192-01)"`.
    - Append a sentence to `rationale`: "Plan 196-05 added LIVINITYD_TIMEDATECTL Cmnd_Alias allowing only `/usr/bin/timedatectl set-timezone *`."

    Both files MUST be committed atomically (one git commit). Pre-commit sacred-sha hook MUST pass on that commit (i.e. the new SHA in the JSON matches the file content).
  </behavior>

  <action>
    1. Read the current sudoers file with the Read tool.
    2. Edit it to append the 3 lines verbatim.
    3. Compute sha1sum of the new file (use Bash: `sha1sum scripts/install/sudoers.d/livinityd`).
    4. Read scripts/sacred-shas-v38.json.
    5. Edit the matching entry (`path: "scripts/install/sudoers.d/livinityd"`) — update expected_sha + frozen_in_phase + rationale per the behavior block.
    6. Run `bash scripts/verify-sacred-sha.sh` — MUST exit 0.
    7. Run `visudo -c -f scripts/install/sudoers.d/livinityd` (syntax check; should report "parsed OK").
    8. Commit both files together: `git add scripts/install/sudoers.d/livinityd scripts/sacred-shas-v38.json && git commit -m "feat(196-05): extend sudoers fragment with TIMEDATECTL Cmnd_Alias + re-pin sacred SHA"`.
  </action>

  <verify>
    <automated>visudo -c -f scripts/install/sudoers.d/livinityd && bash scripts/verify-sacred-sha.sh</automated>
  </verify>

  <acceptance_criteria>
    - `visudo -c -f scripts/install/sudoers.d/livinityd` outputs "parsed OK"
    - `bash scripts/verify-sacred-sha.sh` exits 0
    - `grep -c "LIVINITYD_TIMEDATECTL" scripts/install/sudoers.d/livinityd` == 2 (Cmnd_Alias definition + NOPASSWD reference)
    - `grep -c "LIVINITYD_TIMEDATECTL" scripts/sacred-shas-v38.json` == 0 (NOT named in registry — only the file's SHA matters)
    - `grep -c "196-05" scripts/sacred-shas-v38.json` >= 1 (rationale references the phase)
    - `sha1sum scripts/install/sudoers.d/livinityd | cut -d' ' -f1` matches the new value in scripts/sacred-shas-v38.json `expected_sha`
    - One atomic commit containing BOTH files; `git diff HEAD~1 --name-only | sort` returns exactly `scripts/install/sudoers.d/livinityd\nscripts/sacred-shas-v38.json`
  </acceptance_criteria>

  <done>
    Sudoers fragment extended with the narrowest possible TIMEDATECTL entry; sacred registry re-pinned in the same commit; visudo parses OK; sacred guard passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: timezone-service.ts + tests (server-side validate + setSystemTimezone)</name>
  <files>livos/packages/livinityd/source/modules/locale/timezone-service.ts, livos/packages/livinityd/source/modules/locale/timezone-service.test.ts, livos/packages/livinityd/source/modules/locale/index.ts</files>

  <read_first>
    - livos/packages/livinityd/source/modules/xai-auth/opencode-spawner.ts (canonical model for execFile invocation with security gates)
    - livos/packages/livinityd/source/modules/locale/region-suggestion.ts (Plan 196-04 utility module style)
    - The barrel index.ts written in 196-04 Task 1 (Plan 196-05 adds exports)
  </read_first>

  <behavior>
    timezone-service.ts exports:
    - `export type TimezoneService = { validate(zone: string): boolean; setSystemTimezone(zone: string): Promise<{ok: true}> }`
    - `export function createTimezoneService(opts?: {execFile?: typeof import('node:child_process').execFile}): TimezoneService`
    - `validate(zone)`: returns true iff `zone` is in `Intl.supportedValuesOf('timeZone')`. Cache the Set on module load to avoid recomputing on every call. Returns false for empty string, undefined, or any unknown zone.
    - `setSystemTimezone(zone)`:
      - FIRST: assert `validate(zone) === true`; if not, throw `new Error('Phase 196-05: invalid IANA timezone: ' + zone)` with a documented error class name.
      - THEN: shell out via `execFile` (NOT exec, NOT spawn shell): `execFile('sudo', ['/usr/bin/timedatectl', 'set-timezone', zone], {timeout: 10000})`.
      - On non-zero exit, throw a typed error including stderr.
      - On success, return `{ok: true as const}`.
    - The `opts.execFile` parameter is for test injection — vitest passes a mock `execFile` that records invocations.

    Tests (timezone-service.test.ts):
    - Test 1: `validate('Europe/Istanbul')` → true.
    - Test 2: `validate('Mars/Olympus')` → false.
    - Test 3: `validate('')` → false.
    - Test 4: `validate(undefined as any)` → false (defensive).
    - Test 5: `setSystemTimezone('Europe/Istanbul')` with mock execFile that succeeds → calls execFile once with `['sudo', ['/usr/bin/timedatectl', 'set-timezone', 'Europe/Istanbul'], expect.any(Object), expect.any(Function)]` and returns `{ok: true}`.
    - Test 6: `setSystemTimezone('Mars/Olympus')` → throws BEFORE execFile is invoked (validate gate fires first). Assert execFile.mock.calls.length === 0.
    - Test 7: `setSystemTimezone('Europe/Istanbul')` with mock execFile that fails with stderr 'permission denied' → throws an Error mentioning "permission denied".
    - Test 8: `setSystemTimezone('; rm -rf /')` (injection attempt) → throws (validate rejects the semicolon-containing string).

    Update livos/packages/livinityd/source/modules/locale/index.ts to also export `createTimezoneService` and `TimezoneService`.
  </behavior>

  <action>
    Write the three files per the behavior block. timezone-service.ts must use `execFile` (NOT `exec` or `spawn` with `shell: true`) — this is the security boundary. The 10-second timeout is mandatory.

    Header docblock for timezone-service.ts must reference:
    - Phase 196-05 + the sudoers TIMEDATECTL extension
    - The validate-before-execFile gate as a defense-in-depth layer

    Tests inject `opts.execFile` so no real timedatectl is invoked in CI.
  </action>

  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/locale/timezone-service.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run reports 8 PASS / 0 FAIL
    - grep -c "execFile" livos/packages/livinityd/source/modules/locale/timezone-service.ts → ≥ 2 (import + call site)
    - grep -c "Intl.supportedValuesOf" livos/packages/livinityd/source/modules/locale/timezone-service.ts → ≥ 1
    - grep -c "Phase 196-05" livos/packages/livinityd/source/modules/locale/timezone-service.ts → ≥ 1
    - grep -cE "shell:\s*true|exec\s*\(" livos/packages/livinityd/source/modules/locale/timezone-service.ts → 0 (no shell-based invocation paths)
    - bash scripts/verify-sacred-sha.sh exits 0
  </acceptance_criteria>

  <done>
    Service implements validate + setSystemTimezone with two-layer defense (Intl validate before execFile); 8 tests pass; barrel re-exports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend setup-router.ts with setLocaleTimezone + httpOnlyPaths + tests</name>
  <files>livos/packages/livinityd/source/modules/server/trpc/setup-router.ts, livos/packages/livinityd/source/modules/server/trpc/setup-router.test.ts, livos/packages/livinityd/source/modules/server/trpc/common.ts</files>

  <read_first>
    - livos/packages/livinityd/source/modules/server/trpc/setup-router.ts (Plan 196-04 output — entire file)
    - livos/packages/livinityd/source/modules/server/trpc/setup-router.test.ts (Plan 196-04 output)
    - livos/packages/livinityd/source/modules/server/trpc/common.ts (httpOnlyPaths Set)
    - livos/packages/livinityd/source/modules/locale/index.ts (after Task 2 — TimezoneService export)
  </read_first>

  <behavior>
    setup-router.ts changes:
    - Extend `createSetupRouter` deps: add `timezoneService: TimezoneService` (alongside the existing `redis: RedisLike`).
    - Add a `setLocaleTimezone` procedure:
      ```ts
      setLocaleTimezone: adminProcedure
        .input(z.object({
          timezone: z.string().min(1),
          locale: z.enum(['en-US','tr-TR','de-DE','fr-FR','es-ES','ar-SA']),
        }))
        .mutation(async ({ctx, input}) => {
          // Defense-in-depth: re-validate timezone against Intl even though UI only offers valid options
          if (!deps.timezoneService.validate(input.timezone)) {
            throw new TRPCError({code: 'BAD_REQUEST', message: 'Phase 196-05: invalid IANA timezone'})
          }
          await deps.timezoneService.setSystemTimezone(input.timezone)
          await deps.redis.set('liv:user:timezone', input.timezone)
          await deps.redis.set('liv:user:locale', input.locale)
          return {ok: true as const}
        }),
      ```
    - The default `setupRouter` empty-injection Proxy needs no change (the Proxy throws on any key access; adding a key doesn't change behavior).

    common.ts changes:
    - Append `'setup.setLocaleTimezone'` to the httpOnlyPaths Set with a Phase 196-05 comment.

    setup-router.test.ts changes (add to the existing 6 tests from 196-04):
    - Test 7: `setLocaleTimezone({timezone: 'Europe/Istanbul', locale: 'tr-TR'})` with mocks → calls timezoneService.validate once, timezoneService.setSystemTimezone once with 'Europe/Istanbul', then redis.set twice (timezone + locale), returns `{ok: true}`.
    - Test 8: `setLocaleTimezone({timezone: 'Mars/Olympus', locale: 'tr-TR'})` → throws BAD_REQUEST. Assert setSystemTimezone NOT called.
    - Test 9: `setLocaleTimezone({timezone: 'Europe/Istanbul', locale: 'klingon'})` → throws (zod enum rejects). Assert validate NOT called.
    - Test 10: `setLocaleTimezone` is adminProcedure-gated; non-admin ctx → UNAUTHORIZED.
  </behavior>

  <action>
    Apply the diffs per the behavior block. The createSetupRouter factory signature now requires `{redis, timezoneService}` — this is a breaking change to Plan 196-04's signature; that's intentional, and Task 5 in this plan wires both deps in livinityd/source/index.ts.

    httpOnlyPaths block:
    ```ts
    // Existing Plan 196-04 entry:
    'setup.setRegion',
    // Plan 196-05 adds:
    // Mutation invokes systemd timedatectl + Redis double-write; MUST survive WS reconnect.
    'setup.setLocaleTimezone',
    ```

    Re-run the existing 6 196-04 tests to confirm they still pass (the factory signature change requires updating the test's factory invocations to supply a stub timezoneService).
  </action>

  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/server/trpc/setup-router.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run reports 10 PASS / 0 FAIL (6 from 196-04 + 4 new)
    - grep -c "setLocaleTimezone" livos/packages/livinityd/source/modules/server/trpc/setup-router.ts → ≥ 2 (procedure name + factory body)
    - grep -c "setup.setLocaleTimezone" livos/packages/livinityd/source/modules/server/trpc/common.ts → 1
    - grep -c "timezoneService" livos/packages/livinityd/source/modules/server/trpc/setup-router.ts → ≥ 2 (deps + call sites)
    - grep -cE "'en-US'|'tr-TR'|'de-DE'|'fr-FR'|'es-ES'|'ar-SA'" livos/packages/livinityd/source/modules/server/trpc/setup-router.ts → 6
    - npx tsc --noEmit on livinityd → zero new errors
  </acceptance_criteria>

  <done>
    setLocaleTimezone procedure validates twice (zod + Intl), calls timezoneService.setSystemTimezone, persists to Redis, adminProcedure-gated; 10 setup-router tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: UI — LocaleTimezoneStep component + intl.ts helpers + tests</name>
  <files>livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx, livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.test.tsx, livos/packages/ui/src/lib/intl.ts, livos/packages/ui/src/lib/intl.test.ts</files>

  <read_first>
    - livos/packages/ui/src/features/onboarding-flow/steps/connect-ai-step.tsx (state machine reference)
    - livos/packages/ui/src/features/onboarding-flow/steps/connect-ai-step.test.tsx (D-NO-NEW-DEPS test harness)
    - The yet-to-be-merged region-step.tsx from Plan 196-04 (similar 6-card layout will inform select-element styling)
  </read_first>

  <behavior>
    intl.ts (livos/packages/ui/src/lib/intl.ts):
    ```ts
    /**
     * Phase 196-05 — Intl helpers. Used by dashboard/settings to render
     * dates/times/numbers per the operator's locale (selected at
     * onboarding step `locale-timezone-step`).
     */
    export function formatDate(date: Date | number | string, locale: string): string {
      return new Intl.DateTimeFormat(locale, {dateStyle: 'medium'}).format(new Date(date))
    }
    export function formatTime(date: Date | number | string, locale: string): string {
      return new Intl.DateTimeFormat(locale, {timeStyle: 'short'}).format(new Date(date))
    }
    export function formatNumber(n: number, locale: string): string {
      return new Intl.NumberFormat(locale).format(n)
    }
    ```

    intl.test.ts:
    - Test 1: formatDate(new Date('2026-05-22'), 'tr-TR') → contains '22' AND a Turkish month abbreviation OR digits-only pattern matching Turkish locale.
    - Test 2: formatNumber(1234567.89, 'tr-TR') → contains the Turkish thousands grouping (period or non-breaking space).
    - Test 3: formatNumber(1234567.89, 'en-US') → contains "1,234,567".
    - Test 4: formatTime(new Date('2026-05-22T14:30:00Z'), 'en-US') → matches /\d{1,2}:\d{2}/.
    - Test 5: formatDate(new Date('2026-05-22'), 'de-DE') → contains 'Mai' OR matches DE locale pattern.
    - At least 5 tests total covering 4 locales.

    locale-timezone-step.tsx:
    - Props: `{data: OnboardingData; setData: (d: OnboardingData) => void; onContinue: () => void; onSkip: () => void; onBack: () => void}`.
    - On mount: auto-detect:
      - `const detectedTz = Intl.DateTimeFormat().resolvedOptions().timeZone` (e.g. 'Europe/Istanbul')
      - `const detectedLocale = navigator.language` (e.g. 'tr-TR'); normalize via supported list — if not in our 6, fall back to 'en-US'.
      - Initialize local state with detected values + "Suggested" pills next to each select.
    - Timezone select: searchable combobox listing `Intl.supportedValuesOf('timeZone')` (~400 entries). Use a simple `<input> + filtered <ul>` pattern (no react-select / downshift / cmdk — D-NO-NEW-DEPS).
    - Locale select: a `<select>` with 6 `<option>` elements (en-US, tr-TR, de-DE, fr-FR, es-ES, ar-SA).
    - Continue calls `trpc.setup.setLocaleTimezone.mutate({timezone: selectedTz, locale: selectedLocale})` then `onContinue()` on success. On error, display the error message inline (no toast lib needed).
    - Skip calls `onSkip()` directly.
    - Back calls `onBack()`.

    locale-timezone-step.test.tsx:
    - Test 1 "auto-detect renders": stub `Intl.DateTimeFormat().resolvedOptions` to return `{timeZone: 'Europe/Istanbul'}` and `navigator.language` to return 'tr-TR'; assert the displayed values + Suggested pills.
    - Test 2 "operator can override timezone": type 'America/New_York' in the combobox, click matching option, click Continue, assert mutate called with `{timezone: 'America/New_York', locale: 'tr-TR'}`.
    - Test 3 "operator can override locale": change locale select to 'en-US', click Continue, assert mutate called with `{timezone: 'Europe/Istanbul', locale: 'en-US'}`.
    - Test 4 "Continue calls trpc + onContinue on success": vi.mock trpcReact with a success-returning useMutation; assert sequence.
    - Test 5 "Skip bypasses mutate": click Skip; assert mutate NOT called; onSkip called once.
    - Test 6 "displays error inline on mutate failure": mock mutate to reject with Error('timedatectl exit 1: permission denied'); assert the inline error region contains 'permission denied'.

    All tests use the react-dom/client + vi.mock harness (no @testing-library/react).
  </behavior>

  <action>
    Write all 4 files per the behavior block. The combobox pattern for the timezone select is a minimal vanilla React implementation:
    ```tsx
    const [query, setQuery] = useState(detectedTz)
    const matches = useMemo(() =>
      Intl.supportedValuesOf('timeZone').filter(z => z.toLowerCase().includes(query.toLowerCase())).slice(0, 20),
      [query]
    )
    // render <input value={query} onChange={...} /> + <ul>{matches.map(z => <li onClick={()=>setSelectedTz(z)}>{z}</li>)}</ul>
    ```

    Add a comment header to locale-timezone-step.tsx:
    ```
    /**
     * Phase 196-05 — Locale + timezone configuration step.
     * Auto-detect via Intl + navigator.language; operator can override.
     * Continue triggers timedatectl via the narrow sudoers TIMEDATECTL alias
     * extended in this same plan (see scripts/install/sudoers.d/livinityd).
     */
    ```
  </action>

  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/features/onboarding-flow/steps/locale-timezone-step.test.tsx src/lib/intl.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - npx vitest run reports ≥ 11 PASS / 0 FAIL (6 step + 5 intl)
    - grep -c "@testing-library" livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.test.tsx → 0
    - grep -c "@testing-library" livos/packages/ui/src/lib/intl.test.ts → 0
    - grep -c "Intl.supportedValuesOf" livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx → ≥ 1
    - grep -c "trpcReact.setup.setLocaleTimezone" livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx → 1
    - grep -cE "'en-US'|'tr-TR'|'de-DE'|'fr-FR'|'es-ES'|'ar-SA'" livos/packages/ui/src/features/onboarding-flow/steps/locale-timezone-step.tsx → 6
    - pnpm --filter ui build exits 0
    - bash scripts/verify-sacred-sha.sh exits 0
  </acceptance_criteria>

  <done>
    LocaleTimezoneStep auto-detects + allows override; intl helpers ship the 3 formatters with locale support; 11 tests pass; sacred guard passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire RegionStep + LocaleTimezoneStep into wizard + livinityd production setupRouter</name>
  <files>livos/packages/ui/src/features/onboarding-flow/constants.ts, livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx, livos/packages/livinityd/source/index.ts</files>

  <read_first>
    - livos/packages/ui/src/features/onboarding-flow/constants.ts (current state after Plan 196-03 lands: TOTAL=7, STEP_NAMES has 'Provider')
    - livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx (current state after 196-03: ProviderStep at index 4, ConnectAiStep at 5, DoneStep at 6)
    - livos/packages/livinityd/source/index.ts lines 820-870 (the Plan 196-01 DI block — Plan 196-05 adds the setupRouter wire-up at the same site)
    - All call sites of TOTAL / STEP_NAMES across livos/packages/ui/
  </read_first>

  <behavior>
    constants.ts changes (on top of 196-03 baseline of TOTAL=7):
    - TOTAL: 7 → 9
    - STEP_NAMES: insert 'Region' and 'Locale & Time' before 'Connect AI'. Final order:
      `['Welcome','Account','Wallpaper','Personalize','Provider','Region','Locale & Time','Connect AI','All set']`
    - STEP_WEIGHT: insert 10 (region) + 25 (locale-timezone) before the existing 25 (Connect AI). Final: `[15, 60, 20, 45, 10, 10, 25, 25, 5]`
    - OnboardingData type: add `region?: Region; country?: string; timezone?: string; locale?: 'en-US'|'tr-TR'|'de-DE'|'fr-FR'|'es-ES'|'ar-SA'`. Import `Region` from a shared types file or define inline matching the 6 region slugs.

    setup-wizard-v2.tsx changes:
    - Import RegionStep + LocaleTimezoneStep from their respective step files.
    - Insert two new `<Step stepIndex={5}>` and `<Step stepIndex={6}>` blocks between ProviderStep (stepIndex 4) and ConnectAiStep (which moves from 5 to 7).
    - Shift ConnectAiStep stepIndex 5 → 7.
    - Shift DoneStep stepIndex 6 → 8; update isActive check from `stepper.idx === 6` to `stepper.idx === 8`.

    livinityd/source/index.ts changes (extending the Plan 196-01 block):
    - Add imports:
      ```ts
      import {createSetupRouter} from './modules/server/trpc/setup-router.js'
      import {createTimezoneService} from './modules/locale/index.js'
      ```
    - Inside the same try-block that hosts the xAI singleton construction (Plan 196-01), append:
      ```ts
      // Phase 196-05 — setup.* (region + locale-timezone) production wire-up.
      // 196-04 shipped the router factory with setRegion; 196-05 extends with
      // setLocaleTimezone and the timezoneService dep. The default Proxy stub
      // throws on call until this swap lands.
      const timezoneService = createTimezoneService()
      const setupRouterProductionInstance = createSetupRouter({
        redis: this.ai.redis,
        timezoneService,
      })
      ```
    - Extend the existing createAppRouter call to pass `setup: setupRouterProductionInstance` alongside `chromeMaster` and `xaiAuth`.
    - Add a one-line webappLogger.info: "Phase 196-05 — setup router wired (setRegion + setLocaleTimezone)".

    Confirm setupRouter slot exists in createAppRouter's opts (was added in Plan 196-04 Task 2).
  </behavior>

  <action>
    Apply the three diffs. Be precise about ordering:
    - Wave 2 finished by Plan 196-03 + 196-04: 196-03 left TOTAL=7 + ProviderStep mounted; 196-04 shipped RegionStep + setup-router (with setRegion only) but DID NOT mount in wizard.
    - This task picks up TOTAL=7 baseline and bumps to 9.

    After edits, run the full onboarding-flow vitest suite to confirm no regression:
    ```
    npx vitest run livos/packages/ui/src/features/onboarding-flow
    ```

    Also run the livinityd vitest suite:
    ```
    npx vitest run livos/packages/livinityd/source/modules/server/trpc livos/packages/livinityd/source/modules/locale
    ```

    Also run tsc on both packages.
  </action>

    <automated>cd livos/packages/ui &amp;&amp; npx vitest run src/features/onboarding-flow --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^export const TOTAL = 9" livos/packages/ui/src/features/onboarding-flow/constants.ts` returns 1
    - `grep -c "'Region'," livos/packages/ui/src/features/onboarding-flow/constants.ts` returns 1
    - `grep -c "'Locale &amp; Time'," livos/packages/ui/src/features/onboarding-flow/constants.ts` returns 1
    - `grep -c "timezone?:" livos/packages/ui/src/features/onboarding-flow/constants.ts` returns 1
    - `grep -c "<RegionStep" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "<LocaleTimezoneStep" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "stepIndex={5}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "stepIndex={6}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "stepIndex={7}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "stepIndex={8}" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "stepper.idx === 8" livos/packages/ui/src/routes/onboarding/setup-wizard-v2.tsx` returns 1
    - `grep -c "createSetupRouter" livos/packages/livinityd/source/index.ts` returns 1
    - `grep -c "createTimezoneService" livos/packages/livinityd/source/index.ts` returns 1
    - `grep -cE "setup:\s*setupRouterProductionInstance" livos/packages/livinityd/source/index.ts` returns 1
    - `grep -c "Phase 196-05" livos/packages/livinityd/source/index.ts` returns at least 2 (block header + log line)
    - All existing onboarding-flow vitest files (account / wallpaper / personalize / connect-ai / provider / region / locale-timezone) pass: `npx vitest run src/features/onboarding-flow` exits 0
    - `pnpm --filter ui build` exits 0
    - `npx tsc --noEmit` on livinityd produces zero NEW errors attributable to source/index.ts
    - `bash scripts/verify-sacred-sha.sh` exits 0 (sacred SHA + the NEW sudoers SHA both intact)
  </acceptance_criteria>

  <done>
    Wizard now renders 9 steps with RegionStep at index 5 and LocaleTimezoneStep at index 6; livinityd boot constructs timezoneService + createSetupRouter and injects into createAppRouter via the setup slot; build green; sacred guard passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-browser to tRPC setup.setLocaleTimezone | New ingress; admin-only; zod-validated locale; server-side re-validated timezone. |
| livinityd to sudo /usr/bin/timedatectl | Privilege escalation gate via narrow Cmnd_Alias; sacred sudoers boundary. |
| livinityd execFile invocation of sudo + timedatectl set-timezone + zone | execFile (NOT shell:true), 10s timeout, no shell metacharacter risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-05-01 | Tampering | Client injects shell metacharacters into timezone string (e.g. semicolons + rm -rf) | mitigate | Two layers: (a) zod input shape; (b) `Intl.supportedValuesOf('timeZone').includes(zone)` in timezoneService.validate runs BEFORE execFile; (c) execFile (not exec/spawn-with-shell) so even an Intl-bypass attempt cannot inject arguments. Test 8 in timezone-service.test.ts regression-locks the rejection. |
| T-196-05-02 | Elevation of Privilege | Sudoers TIMEDATECTL Cmnd_Alias is widened by mistake to `/usr/bin/timedatectl *` granting clock-control surface beyond set-timezone | mitigate | (a) The alias body is exactly `/usr/bin/timedatectl set-timezone *` (sub-command in fixed position); (b) sacred-shas-v38.json re-pin in the same commit means any future widening must explicitly update the registered SHA + survive code review. |
| T-196-05-03 | Elevation of Privilege | Non-admin caller invokes setup.setLocaleTimezone | mitigate | adminProcedure gate (mirrors Phase 195-03 / 196-04 pattern). Test 10 in setup-router.test.ts regression-locks. |
| T-196-05-04 | Information Disclosure | Locale/timezone selection leaks via logs | accept | Non-sensitive data; standard logging applies. |
| T-196-05-05 | Denial of Service | setLocaleTimezone repeatedly invoked, hammering timedatectl | mitigate | adminProcedure gate + execFile 10s timeout prevents long-running invocations. A flood from an authenticated admin is a higher-severity compromise already. |
| T-196-05-06 | Repudiation | Operator selection lost on browser crash mid-step | mitigate | Existing wizard backend resume (Phase 137-04) writes OnboardingData to Redis preferences within 500ms; selection survives reload. |
| T-196-05-07 | Spoofing | Attacker spoofs `navigator.language` to mislead suggestion | accept | Suggestion is advisory; operator can override; persisted value is whatever operator confirms. |
</threat_model>

<verification>
**Module-level (this plan tests):**
- Task 1: `visudo -c -f scripts/install/sudoers.d/livinityd` reports parsed OK; `bash scripts/verify-sacred-sha.sh` exits 0.
- Task 2: 8 timezone-service vitest PASS.
- Task 3: 10 setup-router vitest PASS (6 from 196-04 + 4 new).
- Task 4: 11 vitest PASS (6 locale-timezone + 5 intl).
- Task 5: full onboarding-flow vitest suite PASS, full livinityd locale + trpc suite PASS, `pnpm --filter ui build` exit 0, tsc clean.

**System-level (operator UAT, deferred):**
- On Mini PC: `bash /opt/livos/update.sh` succeeds; new sudoers fragment installed via the existing update flow (or via install.sh on a fresh box).
- Visit `/onboarding`; walk 9 steps; on Locale &amp; Time step, leave the auto-detected values (Europe/Istanbul + tr-TR for a Turkish operator); click Continue.
- `cat /etc/timezone` on Mini PC reports `Europe/Istanbul`.
- `redis-cli get liv:user:timezone` returns "Europe/Istanbul".
- `redis-cli get liv:user:locale` returns "tr-TR".
- Submit a request with an invalid timezone via direct tRPC call (curl with admin JWT and body containing `Mars/Olympus`) returns HTTP 400 BAD_REQUEST.
- Pre-commit hook on operator's machine still passes (sacred SHA verifier sees the new pinned value).
</verification>

<success_criteria>
- 16 file modifications (10 new + 6 mods).
- ~29+ new vitest assertions across the plan (8 timezone-service + 4 setup-router additions + 6 locale-timezone-step + 5 intl).
- All existing tests still pass (provider-step 4 + region-step 6 + connect-ai-step 7 + others — zero regressions).
- Sacred SHA f3538e1d... for sdk-agent-runner.ts byte-identical end-to-end.
- Sudoers fragment SHA updated atomically in scripts/sacred-shas-v38.json; pre-commit hook passes.
- Wizard renders 9 steps; system clock honours operator selection; Redis keys persist locale+timezone+region+country.
- 5 atomic commits expected: (1) feat(196-05): sudoers TIMEDATECTL + sacred-registry re-pin; (2) feat(196-05): timezone-service + tests; (3) feat(196-05): setup.setLocaleTimezone + httpOnlyPaths + tests; (4) feat(196-05): LocaleTimezoneStep + intl helpers + tests; (5) feat(196-05): wizard wire-up + livinityd setupRouter injection.
- No deferred-scope language. The CONTEXT.md "real i18n library" deferred item is honoured: Phase 196-05 ships Intl-based formatting only; no react-intl / i18next.
</success_criteria>

<output>
After completion, create `.planning/phases/196-onboarding-completion-installer-locale/196-05-SUMMARY.md` documenting:
- All 5 commit SHAs
- Vitest output across all 4 test files (~29 PASS minimum)
- `pnpm --filter ui build` + tsc output (clean)
- sha1sum of `scripts/install/sudoers.d/livinityd` matching the new entry in `scripts/sacred-shas-v38.json`
- `visudo -c -f scripts/install/sudoers.d/livinityd` output ("parsed OK")
- A handoff block listing the operator-UAT walkthrough (9-step wizard on Mini PC, `cat /etc/timezone` verification, Redis key spot-check)
- Phase 196 closure notes — at this point all 5 plans of Phase 196 are CODE-COMPLETE and the operator UAT pass closes both Phase 195 HUMAN-UAT items and Phase 196's own checklist
</output>
