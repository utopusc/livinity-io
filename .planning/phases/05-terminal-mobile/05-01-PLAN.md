---
phase: 05-terminal-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/settings/terminal/_shared.tsx
  - livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03]

must_haves:
  truths:
    - "Terminal fills mobile viewport width without horizontal scrollbar"
    - "Terminal text is readable on mobile (12px minimum font size)"
    - "Rotating to landscape recalculates terminal cols/rows and terminal remains functional"
  artifacts:
    - path: "livos/packages/ui/src/routes/settings/terminal/_shared.tsx"
      provides: "Mobile-fitted XTermTerminal with 12px font, no min-width on mobile"
      contains: "isMobile ? 12 : 13"
    - path: "livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx"
      provides: "Touch-friendly terminal tab header on mobile"
      contains: "isMobile"
  key_links:
    - from: "livos/packages/ui/src/routes/settings/terminal/_shared.tsx"
      to: "useMeasure container dimensions"
      via: "ResizeObserver triggers useEffect on orientation change"
      pattern: "containerWidth.*containerHeight"
---

<objective>
Make the terminal fully usable on mobile: viewport-fitted xterm.js (no horizontal scroll), readable 12px font, proper landscape resize support, and touch-friendly tab controls.

Purpose: Terminal is the last app in the v24.0 Mobile Responsive UI milestone. Users need to run basic server commands on mobile without fighting horizontal scrolling or unreadable text.
Output: Two modified files delivering all three TERM requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-terminal-mobile/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From livos/packages/ui/src/routes/settings/terminal/_shared.tsx:
```typescript
export const XTermTerminal = ({appId}: {appId?: string}) => {
  // Uses useMeasure() for container dimensions
  // Uses useIsMobile() for responsive behavior
  // Creates Terminal with fontSize, fontFamily
  // Calculates cols/rows from containerWidth/characterWidth
  // Connects WebSocket with cols/rows in URL params
  // Has fitAddon loaded but min-w-[980px] prevents mobile fit
}
```

From livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx:
```typescript
export default function TerminalWindowContent() {
  // Tab header for LivOS/App mode switching
  // Renders XTermTerminal in flex-1 overflow-hidden container
  // Has mode tabs with px-3 py-1.5 text-[12px] buttons (too small for touch)
}
```

From livos/packages/ui/src/hooks/use-is-mobile.ts:
```typescript
export function useIsMobile(): boolean  // true when viewport < 1024px (lg breakpoint)
```

From livos/packages/ui/src/modules/mobile/mobile-app-renderer.tsx:
```typescript
// Mobile shell: MobileNavBar (h-11 = 44px) at top + pb-[72px] for tab bar at bottom
// Terminal content area = viewport height - 44px (nav) - 72px (tab bar padding)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile-fitted XTermTerminal with 12px font and landscape resize</name>
  <read_first>livos/packages/ui/src/routes/settings/terminal/_shared.tsx</read_first>
  <files>livos/packages/ui/src/routes/settings/terminal/_shared.tsx</files>
  <action>
Modify the XTermTerminal component in _shared.tsx to make the terminal fit mobile viewports. CRITICAL: Desktop UI must NOT change -- all modifications gated on isMobile (already imported).

1. **Font size (TERM-02):** Change line 28 from `const fontSize = isMobile ? 11 : 13` to `const fontSize = isMobile ? 12 : 13`. The 12px minimum ensures readability on mobile screens.

2. **Remove min-width on mobile (TERM-01):** The inner terminal div (line 119) currently has `min-w-[980px]` which forces horizontal scrolling on mobile. Change the className to be conditional on isMobile:
   - Mobile: NO min-w-[980px], use `px-1 py-1` (tight padding to maximize terminal space)
   - Desktop: Keep existing `min-w-[980px] px-4 py-3`
   Use a conditional className with cn() utility (already available via import).

3. **Fix container styles on mobile (TERM-01):** The parent div (line 106) has `overflow-x-auto rounded-xl pb-4 pr-3` which are desktop-oriented. On mobile:
   - Remove `overflow-x-auto` (no side-scrolling on mobile, terminal fits viewport)
   - Remove `rounded-xl` (terminal fills full width on mobile, no rounded corners)
   - Remove `pb-4 pr-3` (no extra padding eating into terminal space on mobile)
   - Keep `overflow-hidden` on both
   Use conditional className with cn().

4. **Landscape resize (TERM-03):** The existing useMeasure + useEffect pattern already handles this:
   - useMeasure uses ResizeObserver which fires on orientation change
   - containerWidth/containerHeight update triggers the useEffect (line 101 dependency array)
   - useEffect recalculates cols/rows and reconnects WebSocket with correct dimensions
   - No additional code needed -- removing min-w-[980px] is what enables this to work correctly on mobile

   The only concern is that on mobile, the useMeasure parent container must properly resize on orientation change. Since the parent uses `h-full w-full` and is inside a flex container, it will naturally follow the viewport change.

Import cn from '@/shadcn-lib/utils' (check if already imported, it is on line 9).
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "isMobile ? 12 : 13" livos/packages/ui/src/routes/settings/terminal/_shared.tsx returns 1
    - grep -c "min-w-\[980px\]" livos/packages/ui/src/routes/settings/terminal/_shared.tsx returns 1 (still present for desktop, inside conditional)
    - grep -c "isMobile" livos/packages/ui/src/routes/settings/terminal/_shared.tsx returns at least 3 (font, container styles, inner div styles)
  </acceptance_criteria>
  <done>XTermTerminal fits mobile viewport width (no min-w-[980px] on mobile), uses 12px font on mobile, and properly resizes on orientation change via existing useMeasure/ResizeObserver pattern. Desktop terminal is completely unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Touch-friendly terminal tab header on mobile</name>
  <read_first>livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx</read_first>
  <files>livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx</files>
  <action>
Modify TerminalWindowContent in terminal-content.tsx to make the tab header (LivOS/App mode toggle and app selector) touch-friendly on mobile. CRITICAL: Desktop UI must NOT change.

1. **Import useIsMobile:** Add `import {useIsMobile} from '@/hooks/use-is-mobile'` and call `const isMobile = useIsMobile()` inside the component.

2. **Touch-friendly mode tabs (44px targets):** The mode tab buttons (lines 32-55) currently use `px-3 py-1.5 text-[12px]`. On mobile, increase to 44px minimum touch targets:
   - Mobile: `px-4 py-2.5 text-[13px]` (h-11 equivalent via padding)
   - Desktop: Keep existing `px-3 py-1.5 text-[12px]`
   Use isMobile conditional in className.

3. **Touch-friendly app selector:** The select dropdown (line 65) currently uses `px-3 py-1.5 text-[12px]`. On mobile:
   - Mobile: `px-4 py-2.5 text-[14px]` for 44px touch target + readability
   - Desktop: Keep existing styles

4. **Tab header spacing on mobile:** The tab header container (line 29) uses `px-4 py-2.5`. On mobile, keep the same or slightly increase: `px-3 py-2` on mobile is fine since the buttons themselves have the 44px targets.

5. **App selector layout on mobile:** The "Run in:" label + select on mobile should stack or be full-width. On mobile, change the app selector wrapper (line 60) from `ml-auto` to full-width below the tabs:
   - Mobile: Remove `ml-auto`, add `w-full mt-2`
   - Desktop: Keep `ml-auto`
   And wrap the entire header in `flex-wrap` on mobile so the app selector wraps to a new line.

Import cn from '@/shadcn-lib/utils' if not already imported.
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "useIsMobile" livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx returns at least 1
    - grep -c "isMobile" livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx returns at least 2
    - grep -c "py-2.5" livos/packages/ui/src/modules/window/app-contents/terminal-content.tsx returns at least 1 (mobile touch targets)
  </acceptance_criteria>
  <done>Terminal tab header has 44px touch targets on mobile for mode toggle buttons and app selector. App selector wraps to full-width on mobile. Desktop UI is completely unchanged.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd livos && npx tsc --noEmit` passes without errors
2. Visual check (mobile): Terminal fills viewport width, no horizontal scrollbar, 12px readable text
3. Landscape check: Rotate device/emulator, terminal recalculates cols/rows, text reflows correctly
4. Desktop check: Terminal looks identical to before (min-w-[980px] still active, 13px font, same padding)
5. Touch targets: Tab header buttons have adequate touch area on mobile
</verification>

<success_criteria>
- TERM-01: Terminal fills mobile viewport width without horizontal scroll (min-w-[980px] removed on mobile, container padding reduced)
- TERM-02: Terminal font is 12px on mobile (changed from 11px)
- TERM-03: Landscape rotation triggers resize via existing useMeasure/ResizeObserver, terminal reconnects with correct cols/rows
- Desktop terminal is completely unchanged in appearance and behavior
- All 44px touch target patterns consistent with Phase 01-04 decisions
</success_criteria>

<output>
After completion, create `.planning/phases/05-terminal-mobile/05-01-SUMMARY.md`
</output>
