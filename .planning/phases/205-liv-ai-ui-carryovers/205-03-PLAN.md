---
phase: 205-liv-ai-ui-carryovers
plan: 03
type: execute
wave: 2
depends_on: [205-01, 205-02]
files_modified:
  - livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx
  - livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts
  - livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.test.ts
  - livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts
  - livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.live-reload.test.ts
  - livos/packages/livinityd/source/index.ts
autonomous: true
requirements: [R2, R3, R6]
requirements_addressed: [R2, R3]
tags: [mcp, redis-pubsub, live-reload, claw-client, livinityd]

must_haves:
  truths:
    - "Operator can open Settings → MCP Servers in claw-client and see the list of currently configured MCP servers (consumes existing mcp.config.list — INV-203-09 contract unchanged)"
    - "Adding an MCP server via the new form writes a row via mcp.config.set; the row appears in the tab without page reload"
    - "Removing an MCP server via the new row Delete button calls mcp.config.delete; the row disappears without reload"
    - "mcp-config-router publishes liv:mcp:updated on every set/delete (and toggle/update if those exist)"
    - "mcp-bridge subscribes to liv:mcp:updated on a duplicated ioredis connection and reconciles its spawned-MCP-client map on each message (spawn new entries, disconnect removed entries)"
    - "Adding an MCP server propagates to openclaw agent tool list within 10s WITHOUT any systemctl restart liv-claw-gateway line in journalctl during the test window"
    - "Env vars in mcp.config.list responses are displayed REDACTED in McpServersTab (KEY=********) — never raw"
  artifacts:
    - path: "livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx"
      provides: "MCP Servers tab UI — list + add form + per-row delete + redacted env display"
      min_lines: 120
      contains: "callMutation('mcp.config.set'"
    - path: "livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts"
      provides: "Existing CRUD + new redis.publish('liv:mcp:updated', ...) call after each mutation"
      contains: "liv:mcp:updated"
    - path: "livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.test.ts"
      provides: "Existing tests + new case: 'publishes liv:mcp:updated on set' + 'publishes liv:mcp:updated on delete'"
      contains: "liv:mcp:updated"
    - path: "livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts"
      provides: "Existing bridge + subscribe loop on liv:mcp:updated via redis.duplicate() + reconcileServers() diff/spawn/disconnect"
      contains: "redis.duplicate()"
    - path: "livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.live-reload.test.ts"
      provides: "Unit test: simulating a publish on liv:mcp:updated causes reconcileServers to spawn the new server within 1s (fake redis + factory injection)"
      contains: "reconcileServers"
  key_links:
    - from: "McpServersTab.tsx"
      to: "livinityd /trpc/mcp.config.{list,set,delete}"
      via: "callMutation/callQuery from src/lib/livinityd-client.ts"
      pattern: "mcp\\.config\\.(list|set|delete)"
    - from: "mcp-config-router.ts mutations"
      to: "Redis channel liv:mcp:updated"
      via: "deps.redis.publish('liv:mcp:updated', JSON.stringify({op,name,ts}))"
      pattern: "publish\\(['\"]liv:mcp:updated"
    - from: "mcp-bridge.ts subscribe loop"
      to: "current spawned-MCP-client map reconciliation"
      via: "redis.duplicate() → subscribe → on('message') → reconcileServers()"
      pattern: "subscribe\\(['\"]liv:mcp:updated"
    - from: "livinityd/source/index.ts boot"
      to: "mcp-bridge construction"
      via: "wire a Redis instance into McpBridge deps so it can .duplicate() for subscribe"
      pattern: "createMcpBridge|new McpBridge"
---

<objective>
Mount the MCP Servers tab inside the claw-client SettingsDialog (consuming the existing `mcp.config.{list,set,delete}` tRPC namespace via the locked browser auth path from 205-01) AND make MCP config changes propagate to running openclaw agents within 10s with zero `systemctl restart`. The propagation is the hard SPEC R3 constraint — Redis pub on writer side (`mcp-config-router.ts`) + Redis sub + reconcile on consumer side (`mcp-bridge.ts`).

Purpose: SPEC R2 + R3 — operator must manage MCP servers without leaving the chat shell AND without restarting the gateway. Today the chat shell has zero MCP UI and adding/removing an MCP server requires `systemctl restart liv-claw-gateway` (because mcp-bridge.ts has no live-reload).

Output: A claw-client build whose Settings → MCP Servers tab can add/list/delete MCP servers; the changes show up in the openclaw agent's tool list within 10s with no gateway restart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/205-liv-ai-ui-carryovers/205-SPEC.md
@.planning/phases/205-liv-ai-ui-carryovers/205-CONTEXT.md
@.planning/phases/205-liv-ai-ui-carryovers/205-RESEARCH.md
@.planning/phases/205-liv-ai-ui-carryovers/205-PATTERNS.md
@.planning/phases/205-liv-ai-ui-carryovers/205-01-SPIKE-NOTES.md

<interfaces>
<!-- Existing tRPC contracts consumed — DO NOT mutate (INV-203-09) -->

From livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts:
```typescript
// Existing namespace mcp.config.*. INV-203-09 = contract stays unchanged at the wire level.
// New work in this plan: add Redis publish AFTER each existing hset/hdel, no schema changes.
export interface McpConfigRedisClient {
  hgetall(key: string): Promise<Record<string, string>>;
  hget(key: string, field: string): Promise<string | null>;
  hset(key: string, field: string, value: string): Promise<unknown>;
  hdel(key: string, field: string): Promise<unknown>;
  publish(channel: string, message: string): Promise<number>;  // NEW — Phase 205
}
```

From livos/packages/livinityd/source/modules/apps/native-app-config.ts:127,144 (publisher template — verbatim mirror):
```typescript
const REDIS_CHANNEL = 'liv:config:updated';
await this.redis.publish(REDIS_CHANNEL, JSON.stringify({kind:'native-app', id:parsed.id, op:'upsert'}));
```

From livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts (current):
```typescript
// Reads liv:mcp:luse:enabled. NO knowledge of liv:mcp:config today. Greenfield extension.
// Construction spawns Luse stdio once at line ~369-372; no subscribe loop, no reconciliation.
```

From src/lib/livinityd-client.ts (created in Wave 1):
```typescript
export async function callMutation<I, O>(path: string, input: I): Promise<O>;
export async function callQuery<I, O>(path: string, input: I): Promise<O>;
// Auth path locked in 205-01-SPIKE-NOTES.md AUTH PATH section
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch mcp-config-router.ts to publish liv:mcp:updated + extend McpConfigRedisClient interface + add 2 vitest cases</name>
  <read_first>
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts (full file — find every set/delete/toggle/update mutation handler)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.test.ts (full file — pattern for existing test cases)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/apps/native-app-config.ts (lines 120-150 — publisher template)
  </read_first>
  <behavior>
    - Test 1: When `mcp.config.set` mutation is called with valid input, `deps.redis.publish` is invoked exactly once with channel `'liv:mcp:updated'` and a JSON string body containing `{"op":"set","name":"<input.name>","ts":"<ISO-8601>"}`.
    - Test 2: When `mcp.config.delete` mutation is called with valid input, `deps.redis.publish` is invoked exactly once with channel `'liv:mcp:updated'` and body `{"op":"delete","name":"<input.name>","ts":"..."}`.
    - Test 3 (if `toggle` or `update` mutation exists in current router): same publish behavior with `op: 'set'` (any state mutation counts as `op:set` since the consumer is going to hgetall + diff anyway).
    - Test 4: Existing tests in `mcp-config-router.test.ts` still pass — fake redis client extended with `publish: vi.fn().mockResolvedValue(1)` does not break any existing assertion.
  </behavior>
  <files>livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts, livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.test.ts</files>
  <action>
**Step A — Extend `McpConfigRedisClient` interface** in `mcp-config-router.ts` (the existing interface — see RESEARCH § Code Examples):
```typescript
export interface McpConfigRedisClient {
  hgetall(key: string): Promise<Record<string, string>>;
  hget(key: string, field: string): Promise<string | null>;
  hset(key: string, field: string, value: string): Promise<unknown>;
  hdel(key: string, field: string): Promise<unknown>;
  publish(channel: string, message: string): Promise<number>;  // NEW — Phase 205
}
```

**Step B — Add publish call** at the tail of every state-mutating procedure handler. Identify them by grep `\.mutation\(` in the file. For each one, after the `hset`/`hdel` succeeds and before the response is returned, append:
```typescript
try {
  await deps.redis.publish(
    'liv:mcp:updated',
    JSON.stringify({op: '<set|delete>', name: input.name, ts: new Date().toISOString()}),
  );
} catch (err) {
  deps.logger.warn('Failed to publish liv:mcp:updated', err); // best-effort, never throw
}
```
- `op: 'set'` for add/upsert/toggle/update
- `op: 'delete'` for delete/remove
- Use `input.name` (or whatever field identifies the server — match the existing schema)

**Step C — Add 2 vitest cases** to `mcp-config-router.test.ts`. RED → GREEN → REFACTOR:
- RED: Write test asserting `publish` is called on `set`; run, expect failure (publish not called yet).
- GREEN: Confirm Step B implementation passes the test.
- Add the corresponding `delete` test.
- Update the fake redis builder helper used by existing tests to include `publish: vi.fn().mockResolvedValue(1)` so existing tests stay green.

**Step D — Update every existing caller of `createMcpConfigRouter`** (grep `createMcpConfigRouter\|McpConfigRedisClient` across livinityd source). Any place that constructs the deps object needs `publish` on its redis client. The production wiring in `index.ts` uses ioredis which already has `publish` natively — only the test fake objects need patching.

**Sacred SHA:** None of these files are in the 20-blob registry. Pre-commit must show `[sacred-sha] PASS: 20 files verified`.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd &amp;&amp; pnpm vitest run source/modules/server/trpc/mcp-config-router.test.ts 2>&amp;1 | tail -20 ; cd ../../.. &amp;&amp; grep -q "liv:mcp:updated" livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts &amp;&amp; grep -q "publish(channel: string" livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts &amp;&amp; grep -c "liv:mcp:updated" livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.test.ts | grep -qE "^[2-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp-config-router.ts` literal string `'liv:mcp:updated'` appears ≥ 2 times (once per state-mutating procedure)
    - `McpConfigRedisClient` interface declares `publish(channel: string, message: string): Promise<number>` (grep `publish\\(channel: string`)
    - `mcp-config-router.test.ts` contains ≥ 2 new test cases referencing `liv:mcp:updated`
    - `pnpm --filter livinityd vitest run source/modules/server/trpc/mcp-config-router.test.ts` exits 0 with all cases passing
    - Existing wire contracts unchanged: `mcp.config.list/set/delete` input/output schemas have NOT been mutated (INV-203-09)
    - `grep -P '[^\x00-\x7F]'` returns empty on the modified files (English-only)
  </acceptance_criteria>
  <done>
    Every state-mutating MCP config tRPC call publishes `liv:mcp:updated` after the hash mutation. Tests prove it. Existing contracts unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend mcp-bridge.ts with live-reload subscribe loop + reconcileServers() + wire ioredis instance via index.ts boot</name>
  <read_first>
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts (full file — find constructor / createMcpBridge factory, the Luse spawn site at ~line 369, the destroy() teardown)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/index.ts (find the McpBridge construction site)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts (find `parseEntry`/`listServers` exports for re-use)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/apps/native-app-config.ts (subscribe pattern — though this file may be publish-only; look for the consumer side in another file or follow the ioredis duplicate-subscribe idiom from RESEARCH § Pitfall 2)
  </read_first>
  <behavior>
    - Test 1: On bridge construction with non-empty `liv:mcp:config` hash, all entries with `enabled:true` are spawned via the injected `mcpClientFactory`; spawned clients are stored in an internal `Map<name, McpClient>`.
    - Test 2: Publishing a fake `liv:mcp:updated` message (op:set, name:foo) where `liv:mcp:config[foo]` is a new enabled entry → `reconcileServers()` spawns a new client for `foo` within 1s; the map size increments by 1.
    - Test 3: Publishing op:delete (or op:set with `liv:mcp:config[foo]` removed via hdel) → `reconcileServers()` calls `disconnect()` on the previously spawned client for `foo` and removes it from the map.
    - Test 4: Two rapid publishes (set then delete) are serialized — no double-spawn race. (Implement via simple in-flight Promise chain or `reconciling/pending` flags per RESEARCH § MCP Propagation race-condition warning.)
    - Test 5: `destroy()` closes BOTH the main connection AND the duplicated subscribe connection (no socket leak).
  </behavior>
  <files>livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts, livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.live-reload.test.ts, livos/packages/livinityd/source/index.ts</files>
  <action>
**Step A — Extend `mcp-bridge.ts`:**

1. Add an internal `Map<string, McpClient>` keyed by server `name` (separate from Luse).
2. At construction (after the existing Luse spawn), call `await this.reconcileServers()` to spawn all currently-enabled external MCP servers from `liv:mcp:config`.
3. Implement `reconcileServers(): Promise<void>`:
   - Read `liv:mcp:config` via `redis.hgetall`.
   - Parse each entry (reuse `parseEntry` from mcp-config-router.ts — export it if needed, OR inline the parse logic).
   - For each entry with `enabled:true` NOT in the current map → spawn via `mcpClientFactory({id:`mcp:${name}`, servers:{[name]:{command,args,env}}})` and `set(name, client)`.
   - For each entry in the map NOT in the new enabled set (or with `enabled:false`) → `client.disconnect()` then `map.delete(name)`.
4. Add subscribe loop:
   ```typescript
   const sub = (this.redis as Redis).duplicate();
   await sub.subscribe('liv:mcp:updated');
   sub.on('message', () => { void this.scheduleReconcile(); });
   this.subConnection = sub;
   ```
5. Implement `scheduleReconcile()` with serialization:
   ```typescript
   private reconciling = false;
   private pending = false;
   private async scheduleReconcile() {
     if (this.reconciling) { this.pending = true; return; }
     this.reconciling = true;
     try { await this.reconcileServers(); } finally {
       this.reconciling = false;
       if (this.pending) { this.pending = false; void this.scheduleReconcile(); }
     }
   }
   ```
6. Update `destroy()` to also `await this.subConnection?.quit()`.

**Step B — Wire ioredis into McpBridge via `index.ts` boot:**

Find the existing McpBridge construction site. Ensure the ioredis instance the rest of livinityd uses is passed in as a dep (the bridge calls `.duplicate()` internally). If a Redis dep is already injected, this is a no-op; if only specific methods were injected (hgetall, hget), broaden the dep to the full Redis client.

**Step C — Tool namespacing:**

When listing tools from external MCP servers, prefix each tool name with `<serverName>_` per RESEARCH § MCP Propagation Mechanism step 1. Verify how the existing Luse-side tool surfacing names tools and mirror the same convention so the openclaw agent loop discovers them uniformly.

**Step D — Create test file `mcp-bridge.live-reload.test.ts`:**

Use vitest fake timers + a fake Redis client that exposes:
- `hgetall(key)` — backed by an in-memory `Map<string, Map<string, string>>` per hash
- `publish(channel, msg)` — invokes registered `message` listeners synchronously
- `duplicate()` — returns a copy that shares the listener registry for `subscribe(channel)` + `on('message', fn)`
- `quit()` — no-op

Inject a fake `mcpClientFactory` that returns objects with `disconnect: vi.fn()`. Assert the spawn/disconnect behaviors from <behavior> above.

RED → GREEN → REFACTOR per behavior bullets.

**Sacred SHA:** None of these 3 files (`mcp-bridge.ts`, `mcp-bridge.live-reload.test.ts`, `index.ts`) are in the 20-blob registry per RESEARCH § Pitfall 6.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd &amp;&amp; pnpm vitest run source/modules/agent-runtime/mcp-bridge.live-reload.test.ts 2>&amp;1 | tail -20 ; cd ../../.. &amp;&amp; grep -q "redis.duplicate()" livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts &amp;&amp; grep -q "subscribe('liv:mcp:updated'" livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts &amp;&amp; grep -q "reconcileServers" livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.ts &amp;&amp; grep -q "reconcileServers" livos/packages/livinityd/source/modules/agent-runtime/mcp-bridge.live-reload.test.ts
  </verify>
  <acceptance_criteria>
    - `mcp-bridge.ts` contains literal `redis.duplicate()` exactly once
    - `mcp-bridge.ts` contains literal `subscribe('liv:mcp:updated'` exactly once
    - `mcp-bridge.ts` exports/defines a method named `reconcileServers`
    - `mcp-bridge.live-reload.test.ts` contains at least 5 test cases corresponding to the 5 behavior bullets (grep `^\s*(test|it)\(` returns at least 5)
    - `pnpm --filter livinityd vitest run source/modules/agent-runtime/mcp-bridge.live-reload.test.ts` exits 0
    - `livinityd/source/index.ts` constructs the McpBridge with the full ioredis instance (grep the construction site — Redis client argument is present)
    - `mcp-bridge.ts` `destroy()` method calls `this.subConnection?.quit()` (grep `subConnection?.quit\(\)` or equivalent)
    - English-only invariant holds
  </acceptance_criteria>
  <done>
    Bridge has a live-reload subscribe loop that spawns/disconnects external MCP servers within 1s of a publish on `liv:mcp:updated`. Unit test proves the diff/spawn/disconnect behavior with fake redis. No restart of liv-claw-gateway needed for MCP changes to land.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement McpServersTab.tsx — list + add form + per-row delete + redacted env display</name>
  <read_first>
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/liv-ai-app/components/settings/McpTab.tsx (full file — port the visual shell, sections, row component, redacted env display; DO NOT copy the `?batch=1` envelope per D-205-06 + RESEARCH Pitfall 1)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/liv-ai-app/components/settings/ProvidersTab.tsx (full file — port the working `callMutation` envelope pattern + restart-banner state idiom)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/liv-claw-os/packages/claw-client/src/lib/livinityd-client.ts (created in Wave 1 — use callMutation + callQuery from here)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts (the exact input shape for set/delete + the list response shape)
    - C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos/packages/livinityd/source/modules/provider/key-store.ts (redactKey pattern — Phase 204 INV-204-04)
  </read_first>
  <files>livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx</files>
  <action>
Replace the Wave 1 placeholder `McpServersTab.tsx` with a working implementation that:

1. **State + lifecycle:** Fetch `mcp.config.list` on mount via `callQuery` from `'../../lib/livinityd-client'`. Store as `McpEntry[]`. Track `loading`, `error`, `saving`, and Add-form state (`pendingName`, `pendingTransport`, `pendingCommand`, `pendingUrl`, `pendingEnv` raw text).

2. **List section** (port visual shell from `McpTab.tsx`, drop "Built-in tools" + restart-required banner + enabled checkbox per D-205-04 / D-205-06):
   - Header: `MCP Servers (N)` + helper paragraph `Add MCP servers to expose new tools to the agent. Changes take effect within ~10 seconds — no restart required.`
   - Empty state: `No MCP servers configured.` (dashed border).
   - Loaded state: bordered ul with row per server. Each row shows `name`, transport-prefixed connection target (stdio: command OR http: url), and env-vars-as-list with EVERY VALUE shown as literal `********` (eight asterisks). Per-row Trash2 button calls `callMutation('mcp.config.delete', {name})` then refreshes.

3. **Add form section:**
   - Inputs: name (text), transport (select stdio/http), command OR url (conditional on transport), env (textarea, KEY=value newline-separated raw text).
   - Submit button labelled `Add MCP Server` with `Plus` lucide icon. Disabled while `saving`. On click: parse env textarea into `Record<string,string>`, call `callMutation('mcp.config.set', {name, transport, command|url, env, enabled:true})`, then refresh list.

4. **Critical constraints:**
   - ENV VARS DISPLAY: Always render env values as literal `********`. Never render raw env values. (Phase 204 INV-204-04 carried forward.)
   - Use `callMutation` / `callQuery` from `'../../lib/livinityd-client'` — DO NOT inline a fetch.
   - Match the EXACT tRPC procedure names exported by `mcp-config-router.ts`. If the router uses `add` instead of `set`, or `remove` instead of `delete`, MATCH THE ROUTER. Read `mcp-config-router.ts` first to confirm names + input schemas.
   - No `?batch=1` envelope (RESEARCH Pitfall 1).
   - English-only strings (INV-203-05).

**Sacred SHA:** None of these files are in the 20-blob registry.
  </action>
  <verify>
    <automated>cd livos/packages/liv-claw-os/packages/claw-client &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -20 &amp;&amp; pnpm --filter @openuidev/claw-client build 2>&amp;1 | tail -20 ; cd ../../../../.. &amp;&amp; grep -q "mcp.config.set\|mcp.config.add\|mcp.config.upsert" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx &amp;&amp; grep -q "mcp.config.delete\|mcp.config.remove" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx &amp;&amp; grep -q "mcp.config.list" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx &amp;&amp; grep -q "callMutation" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx &amp;&amp; grep -q "callQuery" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx &amp;&amp; grep -q "\*\*\*\*\*\*\*\*" livos/packages/liv-claw-os/packages/claw-client/src/components/settings/McpServersTab.tsx</automated>
    <!-- B-4 fix: Next.js bundle build (next build) catches issues `tsc --noEmit` misses (output:export static-export quirks). Workspace filter `@openuidev/claw-client` per package.json. -->
    <!-- B-3 note: Sacred SHA hook lives at .husky/pre-commit -> scripts/check-sacred.sh — auto-runs on every commit (validated 2026-05-24). -->
  </verify>
  <acceptance_criteria>
    - `McpServersTab.tsx` imports `callMutation` AND `callQuery` from `'../../lib/livinityd-client'`
    - File contains the literal `'mcp.config.list'`
    - File contains the literal `'mcp.config.set'` (or `'mcp.config.add'` / `'mcp.config.upsert'` — whichever the router exports)
    - File contains the literal `'mcp.config.delete'` (or `'mcp.config.remove'` — whichever the router exports)
    - File contains the literal `'********'` (eight asterisks) for env-var redaction display
    - File contains `<Trash2` icon usage AND `<Plus` icon usage from lucide-react
    - File does NOT contain `?batch=1` (Pitfall 1 negative check)
    - File does NOT contain `process.env.LIV_API_KEY` (auth path lives in livinityd-client.ts only)
    - `pnpm tsc --noEmit` in `claw-client` exits 0
    - File contains the exact string `'No MCP servers configured.'` (empty state) AND `'Add MCP Server'` (button label)
    - Min file length: 120 lines
    - English-only: `grep -P '[^\x00-\x7F]' McpServersTab.tsx` returns empty
  </acceptance_criteria>
  <done>
    Opening Settings → MCP Servers in claw-client lists current MCP servers; clicking "Add MCP Server" with name+transport+command writes a row via `mcp.config.set` and refreshes the list; clicking the Trash2 button on a row removes it. Env vars in list rows are rendered as `KEY=********`. Within 10s of an add, the agent's tool list (via Task 2 live-reload) includes a tool prefixed with the new server name — SPEC R3 acceptance.
  </done>
</task>

</tasks>

<verification>
- All 3 task verify commands pass
- `pnpm --filter livinityd vitest run` for the two test files exits 0
- **Mini PC live R3 gate (AUTOMATED — W-5 fix, MUST PASS):** After Wave 2 deploy completes (executor runs `bash /opt/livos/update.sh` on Mini PC), executor immediately SSHs back and runs:
  ```
  ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@100.112.68.1 'sudo journalctl -u liv-claw-gateway --since "5 minutes ago" --no-pager | grep -cE "Started|systemctl restart"'
  ```
  Assert: output is exactly `0`. Any non-zero count means a gateway restart happened during the deploy window — R3 (live-reload without restart) is NOT satisfied; the executor MUST fail the plan and surface the offending journal lines.
  (SSH command shape per memory `reference_minipc_ssh.md`. The Mini PC ZeroTier IP `100.112.68.1` is preferred over the LAN `10.69.31.68` when ZT is up; if ZT-down per `reference_zerotier_unstable.md`, fall back to `bruce@10.69.31.68`.)
- **Mini PC live R3 functional check (still manual UAT — agent tool surfacing within 10s):** SSH to Mini PC, watch `journalctl -u livos -f` while adding an MCP server through the UI; observe the bridge log line confirming reconcileServers spawned the new tool within 10s.
- Pre-commit hook shows `[sacred-sha] PASS: 20 files verified` (.husky/pre-commit -> scripts/check-sacred.sh — B-3 note, validated 2026-05-24)
- INV-203-09 preserved: `mcp.config.*` input/output schemas unchanged at the wire level
</verification>

<success_criteria>
- SPEC R2 acceptance: Add/Remove via MCP Servers tab round-trips through `mcp.config.list` without page reload
- SPEC R3 acceptance: New MCP server tools visible in agent within 10s, no `systemctl restart liv-claw-gateway` line in journalctl during the test window
- INV-203-09 preserved: contract schemas untouched
- Sacred SHA: every commit shows `[sacred-sha] PASS: 20 files verified`
</success_criteria>

<output>
After completion, create `.planning/phases/205-liv-ai-ui-carryovers/205-03-SUMMARY.md` documenting the Mini PC live-smoke results (journalctl absence + agent-tool-list visibility timing).
</output>
