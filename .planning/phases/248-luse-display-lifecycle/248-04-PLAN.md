---
phase: 248
plan: 04
type: execute
wave: 3
depends_on: ['248-02']
autonomous: true
gap_closure: false
requirements: []
files_modified:
  - docs/luse/DISPLAY-LIFECYCLE.md
  - docs/luse/tools/create_display.md
  - docs/luse/tools/list_displays.md
  - docs/luse/tools/kill_display.md
  - docs/luse/tools/launch_app_in_display.md
  - docs/luse/LUSE.md
  - scripts/sync-luse-skills.sh
  - .claude/skills/luse/DISPLAY-LIFECYCLE.md
  - .claude/skills/luse/create_display.md
  - .claude/skills/luse/list_displays.md
  - .claude/skills/luse/kill_display.md
  - .claude/skills/luse/launch_app_in_display.md
  - .aion/skills/luse.md
  - .opencode/skills/luse.md
  - .openclaw/skills/luse.md
task_count: 3
objective: Canonical agent-agnostic display-lifecycle docs + per-tool refs + extend sync-luse-skills.sh to propagate them to all 4 agent shim dirs
tags: [v44, luse, docs, agent-agnostic, sync-script, idempotency, owner-scoped]
must_haves:
  truths:
    - "docs/luse/DISPLAY-LIFECYCLE.md exists — covers when-to-create, cleanup discipline, isolation guarantees, Xephyr-vs-Xvfb decision matrix, app-placement recipes"
    - "docs/luse/tools/create_display.md exists — inputs (mode/name/width/height), output {display, name, pid}, when-to-use (visible vs headless)"
    - "docs/luse/tools/list_displays.md exists — output shape, owner_session field, role-in-cleanup-workflow"
    - "docs/luse/tools/kill_display.md exists — owner-scope rule (D-V44-DISPLAY-OWNER-SCOPED), killed_apps_count return, error responses"
    - "docs/luse/tools/launch_app_in_display.md exists — display arg required, app catalog resolution, args[] passthrough"
    - "docs/luse/LUSE.md gains a '## Display lifecycle (Phase 248)' section linking to DISPLAY-LIFECYCLE.md + the 4 new tool refs"
    - "scripts/sync-luse-skills.sh updated: 5 new read_canonical calls + 5 new ## sections in CONCAT_PAYLOAD + 5 new lines in the per-tool generate_claude_skill loop"
    - "First sync run reports >=10 changes (5 new + 5 updated — Claude tool files new, generic shim CONCAT_PAYLOAD updated for all 3 placeholders + .claude/skills/luse/SKILL.md updated due to LUSE.md edit)"
    - "Second sync run reports 0 new / 0 updated / N unchanged — D-242-B idempotency invariant preserved"
    - "Agent-agnostic prose (no Claude/Aion/Gemini/OpenCode/OpenClaw names in tool docs body — only allowed in INTEGRATION-RECIPES per Phase 247 D-242-C)"
    - "Tone matches Phase 247 — terse factual command-style; cite tool names with backticks; reference D-V44-DISPLAY-XEPHYR-DEFAULT + D-V44-DISPLAY-OWNER-SCOPED explicitly"
  artifacts:
    - path: docs/luse/DISPLAY-LIFECYCLE.md
      provides: top-level lifecycle guide
      min_lines: 120
    - path: docs/luse/tools/create_display.md
      provides: per-tool reference
      min_lines: 50
    - path: docs/luse/tools/list_displays.md
      min_lines: 40
    - path: docs/luse/tools/kill_display.md
      min_lines: 50
    - path: docs/luse/tools/launch_app_in_display.md
      min_lines: 55
    - path: scripts/sync-luse-skills.sh
      provides: extended manifest for 5 new canonical files
      min_lines_added: 30
  key_links:
    - from: scripts/sync-luse-skills.sh
      to: docs/luse/DISPLAY-LIFECYCLE.md + docs/luse/tools/{create,list,kill,launch_app_in}_display.md
      via: "read_canonical calls + CONCAT_PAYLOAD sections + generate_claude_skill per-tool loop entries"
      pattern: "DISPLAY-LIFECYCLE|create_display|list_displays|kill_display|launch_app_in_display"
---

<objective>
Ship the canonical, agent-agnostic Luse display-lifecycle documentation set and propagate it to every shimmed CLI agent (Claude Code, Aion CLI, OpenCode, OpenClaw) via the existing Phase 242/247 sync script. Gemini is intentionally NOT shimmed — agents discover the prose via MCP tool-discovery only (Phase 247 D-247-A).

Purpose: An agent that can't decide between Xephyr (visible) and Xvfb (headless), forgets to kill what it created, or violates the owner-scope rule produces bad UX. The docs scaffold built in 242/247 is the right surface to add this guidance. Per Phase 247 D-242-B (idempotency via source-sha markers), the sync script's first run after canonical edits emits a healthy non-zero change set and the second run reports 0 changes — that's the regression gate.

Output:
- 1 new top-level canonical doc (`DISPLAY-LIFECYCLE.md`)
- 4 new per-tool refs under `docs/luse/tools/`
- Updated `docs/luse/LUSE.md` with new "## Display lifecycle" section
- Updated `scripts/sync-luse-skills.sh` manifest (3 surgical hunks per Phase 247 pattern)
- Sync run #1: non-zero new+updated count
- Sync run #2: 0 new / 0 updated / N unchanged (D-242-B drift-lock holds)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/248-luse-display-lifecycle/248-CONTEXT.md
@.planning/phases/248-luse-display-lifecycle/248-02-PLAN.md
@.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md
@.planning/phases/242-luse-skill-set-universal/242-SUMMARY.md
@docs/luse/LUSE.md
@docs/luse/tools/click.md
@scripts/sync-luse-skills.sh

<interfaces>
<!-- Existing patterns to mirror — Phase 242 baseline + Phase 247 extension -->

From scripts/sync-luse-skills.sh (existing manifest pattern — Phase 247 extension):
- `read_canonical "docs/luse/PATTERNS.md"` — reads + sha256s a canonical file
- `CONCAT_PAYLOAD` — accumulates `## <NAME>\n---\n<body>\n---\n` blocks
- `generate_claude_skill` — emits per-file .claude/skills/luse/<NAME>.md files for each top-level canonical doc + per-tool files
- `generate_generic_shim` — emits single concatenated .aion/.opencode/.openclaw shims

From docs/luse/tools/click.md (per-tool ref structure — terse factual, ~50 lines):
- ## Inputs (table or bullets)
- ## Output (success + error shapes)
- ## When to use
- ## When NOT to use
- ## Safety (preconditions / postconditions)
- ## Example (single JSON call)
- ## See also (Phase 247 — cross-links to PATTERNS / ANTI-PATTERNS)

From docs/luse/LUSE.md (top-level format):
- H1 title
- Capability overview
- Tool index table
- Cross-references to deeper docs
</interfaces>
</context>

<read_first>
1. `docs/luse/LUSE.md` — top-level pattern (will add a "## Display lifecycle" section pointing at new docs)
2. `docs/luse/tools/click.md` — per-tool ref structure to mirror for the 4 new tools
3. `scripts/sync-luse-skills.sh` — the 3 surgical hunks needed (read_canonical + CONCAT_PAYLOAD section + generate_claude_skill per-tool loop). Phase 247-02 SUMMARY documents the exact pattern.
4. `.planning/phases/247-luse-skill-v2-docs/247-02-SUMMARY.md` — proof the manifest-extension branch works (`Synced 15 shims (6 new / 8 updated / 1 unchanged)` on first run; `0 new / 0 updated / 15 unchanged` on second)
5. `.planning/phases/248-luse-display-lifecycle/248-02-PLAN.md` — exact MCP tool names + input/output shapes to cite in the per-tool refs
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Author canonical docs — DISPLAY-LIFECYCLE.md + 4 per-tool refs + LUSE.md section</name>
  <files>
    docs/luse/DISPLAY-LIFECYCLE.md
    docs/luse/tools/create_display.md
    docs/luse/tools/list_displays.md
    docs/luse/tools/kill_display.md
    docs/luse/tools/launch_app_in_display.md
    docs/luse/LUSE.md
  </files>
  <action>
    **DISPLAY-LIFECYCLE.md (~120 lines):**
    - H1: "Luse Display Lifecycle"
    - ## When to create a display — bullet list: isolated visual UAT walk, multi-app side-by-side comparison, batch screenshot without disturbing operator desktop, dry-run an unknown app safely
    - ## When NOT to create — single-app interaction on the existing operator desktop (use computer_application), need to interact with windows already open on :1 (list_windows on :1)
    - ## Mode decision matrix — Xephyr vs Xvfb table (default Xephyr, columns: visible / op-can-watch / GPU-needed / common-use); cite **D-V44-DISPLAY-XEPHYR-DEFAULT** explicitly
    - ## Lifecycle protocol — numbered: (1) create_display → (2) launch_app_in_display → (3) work / take screenshot — pass `display: ":N"` to computer_screenshot / computer_click_mouse / etc → (4) when finished: kill_display
    - ## Cleanup discipline — every successful create must have a matching kill in the same agent session; the 4h TTL GC is a safety net not a license to skip cleanup; reference Phase 248-03 1h sweep / 4h idle
    - ## Owner-scope rule — only the session that called create_display can call kill_display for that display; list_displays is global (any caller); cite **D-V44-DISPLAY-OWNER-SCOPED**
    - ## Isolation guarantees — apps in created displays don't leak input events into :1; killing a display kills every tracked app pid (`killed_apps_count` in response)
    - ## App-placement recipes — 3 worked examples:
      1. "Open Firefox in a fresh Xephyr, screenshot, close"
      2. "Side-by-side: 2 displays, libreoffice in :10, firefox in :11"
      3. "Headless batch: Xvfb display, launch app, capture N screenshots, kill"
       Each recipe = numbered JSON calls (computer_create_display → computer_launch_app_in_display → computer_screenshot with display arg → computer_kill_display).
    - ## Failure modes — display creation race (binary missing → error), kill on not-owner (error response shape), app failed to spawn
    - Agent-agnostic prose — no Claude/Aion/Gemini/OpenCode/OpenClaw names in body.

    **docs/luse/tools/create_display.md (~50 lines):** inputs (mode/name/width/height — all optional, defaults `xephyr` + `1920x1080`), output `{display:":N", name, pid}`, when-to-use (visible vs headless decision), example JSON. Cite D-V44-DISPLAY-XEPHYR-DEFAULT.

    **docs/luse/tools/list_displays.md (~40 lines):** no inputs, output shape `[{display, name, mode, created_at, owner_session, running_apps:[pid...]}]`, role in cleanup workflow (poll before each create to avoid duplicate names; check own owner_session entries before exiting agent session).

    **docs/luse/tools/kill_display.md (~50 lines):** input `{display:":N"}`, output `{ok:true, killed_apps_count}` OR `{ok:false, error}`. Owner-scope rule explained: only creator session can kill; agents under different sessions get `error: "not-owner"`. Side effect: all tracked app pids SIGTERM'd. Cite D-V44-DISPLAY-OWNER-SCOPED.

    **docs/luse/tools/launch_app_in_display.md (~55 lines):** inputs `{display, app, args?}`, output `{pid, app_name}`, behavior (resolves app via the same LivOS catalog used by computer_application, spawns with DISPLAY=<display>), examples (firefox / libreoffice / chromium).

    **LUSE.md — add new section before the existing tool index OR at the appropriate place** (just before "## Prerequisites"):
    ```markdown
    ## Display lifecycle (Phase 248)

    Beyond direct interaction with the host desktop, agents can create isolated
    nested X servers (Xephyr — visible default, Xvfb — headless opt-in) to run
    apps without disturbing the operator's main session. Four tools:
    `computer_create_display`, `computer_list_displays`, `computer_kill_display`,
    `computer_launch_app_in_display`. See [DISPLAY-LIFECYCLE.md](DISPLAY-LIFECYCLE.md)
    for full workflow + cleanup discipline. Per-tool refs:
    [create_display](tools/create_display.md) ·
    [list_displays](tools/list_displays.md) ·
    [kill_display](tools/kill_display.md) ·
    [launch_app_in_display](tools/launch_app_in_display.md).
    ```

    Tone: factual, command-style, no emoji, no marketing voice. Match Phase 247 docs.

    Commit message: `docs(248-04): canonical display lifecycle docs — DISPLAY-LIFECYCLE.md + 4 per-tool refs + LUSE.md section`.
  </action>
  <verify>
    <automated>ls docs/luse/DISPLAY-LIFECYCLE.md docs/luse/tools/create_display.md docs/luse/tools/list_displays.md docs/luse/tools/kill_display.md docs/luse/tools/launch_app_in_display.md && grep -c "Display lifecycle" docs/luse/LUSE.md</automated>
  </verify>
  <done>All 5 new files exist; LUSE.md contains the new section heading. Agent-agnostic invariant: `grep -rE "Claude|Aion|Gemini|OpenCode|OpenClaw" docs/luse/DISPLAY-LIFECYCLE.md docs/luse/tools/{create,list,kill,launch_app_in}_display.md` returns 0 hits. Sacred SHA preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extend sync-luse-skills.sh manifest for the 5 new canonical files</name>
  <files>
    scripts/sync-luse-skills.sh
  </files>
  <action>
    Edit `scripts/sync-luse-skills.sh` with 3 surgical hunks (same shape as Phase 247-02):

    **Hunk 1 — Add 5 new `read_canonical` calls** alongside the existing PATTERNS/ANTI-PATTERNS/CHEAT-SHEET/etc calls:
    ```bash
    DISPLAY_LIFECYCLE_PAYLOAD="$(read_canonical "$CANONICAL_DIR/DISPLAY-LIFECYCLE.md")"
    CREATE_DISPLAY_PAYLOAD="$(read_canonical "$CANONICAL_DIR/tools/create_display.md")"
    LIST_DISPLAYS_PAYLOAD="$(read_canonical "$CANONICAL_DIR/tools/list_displays.md")"
    KILL_DISPLAY_PAYLOAD="$(read_canonical "$CANONICAL_DIR/tools/kill_display.md")"
    LAUNCH_APP_PAYLOAD="$(read_canonical "$CANONICAL_DIR/tools/launch_app_in_display.md")"
    ```

    **Hunk 2 — Add 5 new sections to CONCAT_PAYLOAD** (for the generic shims):
    ```bash
    CONCAT_PAYLOAD+="
    ## DISPLAY-LIFECYCLE
    ---
    $DISPLAY_LIFECYCLE_PAYLOAD
    ---
    ## TOOL: create_display
    ---
    $CREATE_DISPLAY_PAYLOAD
    ---
    ## TOOL: list_displays
    ---
    $LIST_DISPLAYS_PAYLOAD
    ---
    ## TOOL: kill_display
    ---
    $KILL_DISPLAY_PAYLOAD
    ---
    ## TOOL: launch_app_in_display
    ---
    $LAUNCH_APP_PAYLOAD
    ---
    "
    ```

    **Hunk 3 — Add 5 new emissions in `generate_claude_skill`** (one .claude/skills/luse/<NAME>.md per new canonical file). Follow the EXACT pattern Phase 247 used for top-level docs (e.g. PATTERNS.md → .claude/skills/luse/PATTERNS.md). For the per-tool files use the existing per-tool loop pattern. Concretely:
    ```bash
    write_shim ".claude/skills/luse/DISPLAY-LIFECYCLE.md" "$DISPLAY_LIFECYCLE_PAYLOAD"
    write_shim ".claude/skills/luse/create_display.md" "$CREATE_DISPLAY_PAYLOAD"
    write_shim ".claude/skills/luse/list_displays.md" "$LIST_DISPLAYS_PAYLOAD"
    write_shim ".claude/skills/luse/kill_display.md" "$KILL_DISPLAY_PAYLOAD"
    write_shim ".claude/skills/luse/launch_app_in_display.md" "$LAUNCH_APP_PAYLOAD"
    ```
    Or if Phase 247 generalized this via a per-doc loop, extend the loop's input array — read the file first and match the existing structure exactly (don't fork the script logic).

    Commit message: `feat(248-04): extend sync-luse-skills.sh manifest with 5 display-lifecycle docs`.
  </action>
  <verify>
    <automated>grep -c "DISPLAY_LIFECYCLE_PAYLOAD\|CREATE_DISPLAY_PAYLOAD\|KILL_DISPLAY_PAYLOAD" scripts/sync-luse-skills.sh</automated>
  </verify>
  <done>grep returns >= 6 (3 vars referenced 2x each minimum). `bash scripts/sync-luse-skills.sh` runs without error.</done>
</task>

<task type="auto">
  <name>Task 3: Run sync script — verify first-run change set + second-run idempotency</name>
  <files>
    .claude/skills/luse/DISPLAY-LIFECYCLE.md
    .claude/skills/luse/create_display.md
    .claude/skills/luse/list_displays.md
    .claude/skills/luse/kill_display.md
    .claude/skills/luse/launch_app_in_display.md
    .aion/skills/luse.md
    .opencode/skills/luse.md
    .openclaw/skills/luse.md
  </files>
  <action>
    1. **First sync run:** `bash scripts/sync-luse-skills.sh > /tmp/sync-run-1.log 2>&1; cat /tmp/sync-run-1.log`. Expected output line: `Synced N shims (M new / K updated / P unchanged)` where M+K >= 8 (5 new Claude per-doc files + 3 generic shims updated). The `.claude/skills/luse/SKILL.md` may also update because LUSE.md was edited in Task 1.
    2. **Second sync run:** `bash scripts/sync-luse-skills.sh > /tmp/sync-run-2.log 2>&1; cat /tmp/sync-run-2.log`. Expected output: `Synced N shims (0 new / 0 updated / N unchanged)` — Phase 242 D-242-B drift-lock invariant. If non-zero updates appear → the source-sha marker is broken; bisect by re-reading the read_existing_sha output for each file.
    3. **Cross-agent prose probe:** `grep -c "DISPLAY-LIFECYCLE" .aion/skills/luse.md && grep -c "DISPLAY-LIFECYCLE" .opencode/skills/luse.md && grep -c "DISPLAY-LIFECYCLE" .openclaw/skills/luse.md` — all 3 should return identical non-zero counts (proves the concatenated payload propagated identically to all 3 generic shims).
    4. **Gemini negation:** `ls .gemini/ 2>&1 | grep -c luse` MUST be 0 (D-242-C / D-247-A).

    Stage and commit all generated shims. Commit message: `feat(248-04): generated agent shims + sync script idempotency invariant verified`.
  </action>
  <verify>
    <automated>bash scripts/sync-luse-skills.sh 2>&1 | grep -E "0 new / 0 updated"</automated>
  </verify>
  <done>Second sync run reports `0 new / 0 updated / N unchanged`. Cross-agent prose probe shows identical DISPLAY-LIFECYCLE counts in all 3 generic shims. Gemini absent. Sacred SHA preserved. Wave 4 (deploy) unblocked.</done>
</task>

</tasks>

<verification>
- 5 new canonical files exist under `docs/luse/`
- LUSE.md has the new "## Display lifecycle (Phase 248)" section
- sync-luse-skills.sh manifest extended (grep verification)
- First sync run: non-zero new+updated count
- Second sync run: `0 new / 0 updated / N unchanged` (D-242-B drift-lock)
- All 3 generic shims have identical DISPLAY-LIFECYCLE prose
- `.gemini/skills/luse.md` absent (D-242-C / D-247-A)
- Agent-agnostic invariant: no agent names in tool docs body
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved
</verification>

<success_criteria>
- 5 new agent-agnostic canonical files
- LUSE.md updated with new section
- sync-luse-skills.sh extended with 5 new payload manifests
- All 4 agent shim dirs (.claude / .aion / .opencode / .openclaw) refreshed
- Idempotency invariant holds on second run
- Gemini deliberately untouched
</success_criteria>

<output>
After completion, create `.planning/phases/248-luse-display-lifecycle/248-04-SUMMARY.md` documenting:
- The 5 new canonical files + line counts
- Sync run #1 outcome (N new + K updated)
- Sync run #2 outcome (0 / 0 / N — drift-lock proof)
- Cross-agent prose probe counts
- Reference to 248-05 (Mini PC deploy + UAT)
</output>
