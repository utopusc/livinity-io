---
phase: 248
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
gap_closure: false
requirements: []
files_modified:
  - livos/packages/livinityd/source/modules/computer-use/displays/display-manager.ts
  - livos/packages/livinityd/source/modules/computer-use/displays/types.ts
  - livos/packages/livinityd/source/modules/computer-use/displays/redis-keys.ts
  - livos/packages/livinityd/source/modules/computer-use/displays/index.ts
  - livos/packages/livinityd/source/modules/computer-use/displays/__tests__/display-manager.test.ts
task_count: 3
objective: Backend display module — Xephyr/Xvfb spawn factory + display-number allocator (:10+) + Redis HSET state + apps LIST tracker, with owner_session enforcement
tags: [v44, luse, displays, xephyr, xvfb, redis, owner-scoped, backend-module]
must_haves:
  truths:
    - "createDisplayManager({redis, spawnFn, nowFn, allocatorStart}) returns {create, list, kill, attachApp, listAppsForDisplay, isOwner}"
    - "create({mode:'xephyr'|'xvfb', name?, width?, height?, ownerSession}) spawns Xephyr (visible) or Xvfb (headless), allocates :N starting at :10, writes Redis HSET luse:display:<display>"
    - "Default width=1920, default height=1080 when omitted"
    - "Display allocation never reuses :0 or :1; starts at :10 monotonic via in-memory counter seeded from highest existing key in Redis SCAN luse:display:*"
    - "create() returns {display: ':N', name, pid}"
    - "list() returns [{display, name, mode, created_at, owner_session, running_apps: [pid...]}] reading from Redis SCAN luse:display:* + per-display luse:display:<d>:apps LIST"
    - "kill({display, callerSession}) refuses with {ok:false, error:'not-owner'} if HGET luse:display:<display> owner_session != callerSession"
    - "kill() when owner-match: SIGTERM the X server pid, SIGTERM every pid in luse:display:<display>:apps, DEL both Redis keys, returns {ok:true, killed_apps_count}"
    - "attachApp({display, pid, app_name}) RPUSH pid into luse:display:<display>:apps and updates luse:display:<display> last_app_at field"
    - "All time + spawn functions are DI'd — vitest uses fake spawnFn returning {pid, kill} stub; never spawns real Xephyr"
    - "D-V44-DISPLAY-XEPHYR-DEFAULT honored: when mode arg omitted the create() default is 'xephyr'"
    - "D-V44-DISPLAY-OWNER-SCOPED enforced inside kill() — list() is global (any session can read)"
  artifacts:
    - path: livos/packages/livinityd/source/modules/computer-use/displays/display-manager.ts
      provides: createDisplayManager factory + DisplayManager type
      min_lines: 180
    - path: livos/packages/livinityd/source/modules/computer-use/displays/types.ts
      provides: DisplayRecord, DisplayMode, CreateDisplayInput, KillDisplayResult types
      min_lines: 30
    - path: livos/packages/livinityd/source/modules/computer-use/displays/redis-keys.ts
      provides: redisKeyForDisplay() + redisKeyForDisplayApps() + DISPLAY_REDIS_PREFIX constant
      min_lines: 20
    - path: livos/packages/livinityd/source/modules/computer-use/displays/__tests__/display-manager.test.ts
      provides: 12+ vitest cases covering create / list / kill / owner-scope / allocator / mode-default
      min_lines: 250
  key_links:
    - from: livos/packages/livinityd/source/modules/computer-use/displays/display-manager.ts
      to: child_process.spawn (DI'd via deps.spawnFn)
      via: "spawnFn('Xephyr', ['-screen', '<w>x<h>', ':<N>', '-ac', '-noreset'])"
      pattern: "spawnFn\\(.Xephyr."
    - from: livos/packages/livinityd/source/modules/computer-use/displays/display-manager.ts
      to: ioredis Redis client
      via: "redis.hset / redis.hgetall / redis.rpush / redis.lrange / redis.del / redis.scan"
      pattern: "redis\\.(hset|hgetall|rpush|lrange|del|scan)"
---

<objective>
Ship the backend foundation for display lifecycle: a `createDisplayManager` factory that owns Xephyr/Xvfb spawn, display-number allocation (`:10+`), Redis-backed state, and owner-scoped kill semantics. Everything else in Phase 248 (MCP tool registrations in 248-02, TTL GC in 248-03, docs in 248-04, deploy in 248-05) consumes this module.

Purpose: D-V44-DISPLAY-XEPHYR-DEFAULT (Xephyr visible default — operator can watch what AI does) + D-V44-DISPLAY-OWNER-SCOPED (only the creator session can kill its own displays) must be enforced at the lowest layer so neither MCP wrappers nor the TTL GC can accidentally violate them. Mirrors the Phase 246-01 SessionManager shape (composition over inheritance, DI'd timer/spawn, Redis SCAN-based listing).

Output:
- 4 new files under `livos/packages/livinityd/source/modules/computer-use/displays/` (module dir + tests subdir)
- 12+ vitest cases drift-locking allocator start (:10), default mode (xephyr), default geometry (1920x1080), owner-scope on kill, and Redis key shapes
- Public surface exported via `displays/index.ts` barrel for 248-02 to import
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/248-luse-display-lifecycle/248-CONTEXT.md
@.planning/phases/246-terminal-v2-multi-session/246-SUMMARY.md
@livos/packages/livinityd/source/modules/pty-sessions/ttl-gc.ts
@livos/packages/livinityd/source/modules/pty-sessions/index.ts

<interfaces>
<!-- Phase 246 patterns the new displays/ module should mirror.
     Extracted from livos/packages/livinityd/source/modules/pty-sessions/ -->

From pty-sessions/ttl-gc.ts (DI shape to copy):
```typescript
export interface TtlGcDeps {
  sessionManager: SessionManager
  nowFn?: () => number
  idleMs?: number
  sweepMs?: number
  setIntervalFn?: typeof setInterval
  clearIntervalFn?: typeof clearInterval
  logger?: {info: (msg: string, ctx?: object) => void}
}
```

From pty-sessions/metadata.ts pattern (Redis prefix constant):
```typescript
export const PTY_SESSION_REDIS_PREFIX = 'livos:pty:session:'
```
Mirror as: `export const DISPLAY_REDIS_PREFIX = 'luse:display:'`

From mcp/tools.ts (existing Luse module child_process.spawn import):
```typescript
import {spawn} from 'node:child_process'
```

ioredis API surface used (Redis class is a NAMED export per project memory):
- `redis.hset(key, field, value)` — HSET fields
- `redis.hgetall(key)` — read all fields of a hash
- `redis.rpush(key, value)` — append to LIST
- `redis.lrange(key, 0, -1)` — read full LIST
- `redis.del(key)` — delete key
- `redis.scan(cursor, 'MATCH', pattern, 'COUNT', 100)` — incremental key scan
</interfaces>
</context>

<read_first>
1. `livos/packages/livinityd/source/modules/pty-sessions/ttl-gc.ts` — DI factory shape to mirror (`createDisplayManager({deps}) → DisplayManager`)
2. `livos/packages/livinityd/source/modules/pty-sessions/metadata.ts` — Redis prefix-constant + per-key helpers convention
3. `livos/packages/livinityd/source/modules/pty-sessions/__tests__/ttl-gc.test.ts` — vitest scaffolding (DI'd spawnFn, drift-lock assertions on defaults)
4. `livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` lines 35 (`import {spawn} from 'node:child_process'`) — existing computer-use spawn import style
5. `.planning/phases/248-luse-display-lifecycle/248-CONTEXT.md` — full domain spec including Redis key shapes + owner-scope decision
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — drift-lock test scaffold for display-manager (failing)</name>
  <files>
    livos/packages/livinityd/source/modules/computer-use/displays/__tests__/display-manager.test.ts
  </files>
  <behavior>
    - Test 1 (drift-lock): `DISPLAY_REDIS_PREFIX === 'luse:display:'` (exact literal)
    - Test 2 (drift-lock): `redisKeyForDisplay(':12') === 'luse:display::12'`
    - Test 3 (drift-lock): `redisKeyForDisplayApps(':12') === 'luse:display::12:apps'`
    - Test 4 (allocator): with empty Redis, first `create({mode:'xephyr', ownerSession:'s1'})` returns `display: ':10'`
    - Test 5 (allocator): second create returns `:11` (monotonic, no reuse)
    - Test 6 (default-mode): `create({ownerSession:'s1'})` defaults to mode='xephyr' (D-V44-DISPLAY-XEPHYR-DEFAULT); assert spawnFn called with first arg 'Xephyr'
    - Test 7 (xvfb-opt-in): `create({mode:'xvfb', ownerSession:'s1'})` calls spawnFn with first arg 'Xvfb'
    - Test 8 (default-geometry): width=1920, height=1080 when omitted; spawnFn args contain '1920x1080'
    - Test 9 (redis-write): after create, redis.hset called with key 'luse:display::10' and fields owner_session='s1', mode='xephyr', created_at=<iso>, name=<auto-if-omitted>, width='1920', height='1080'
    - Test 10 (list): list() returns array reflecting all displays in Redis with running_apps populated from luse:display:<d>:apps LIST
    - Test 11 (owner-scope deny): kill({display:':10', callerSession:'s2'}) when owner_session='s1' returns {ok:false, error:'not-owner'} and never calls process kill
    - Test 12 (owner-scope allow): kill({display:':10', callerSession:'s1'}) calls spawn-handle.kill('SIGTERM') for both X server pid AND every pid in apps LIST, DELs both Redis keys, returns {ok:true, killed_apps_count:<n>}
    - Test 13 (attachApp): attachApp({display:':10', pid:1234, app_name:'firefox'}) calls redis.rpush('luse:display::10:apps', '1234')
    - Test 14 (allocator-seeding): when Redis already has luse:display::15, next create returns ':16' (allocator seeds from SCAN max + 1)
    All tests use:
      - Fake `spawnFn` returning `{pid: <unique>, kill: vi.fn()}` shape
      - Fake `redis` exposing a Map-backed `hset/hgetall/rpush/lrange/del/scan` surface
      - Fake `nowFn` returning fixed epoch
      - vitest singleThread to serialize allocator state
    Each `.test.ts` case asserts BOTH the return value AND the side-effect (which fake fn was called with which args).
  </behavior>
  <action>
    Create `__tests__/display-manager.test.ts` importing from `'../display-manager.js'` + `'../redis-keys.js'` + `'../types.js'`. The imports MUST FAIL — that's the RED. Write the 14 vitest cases using `describe('display-manager', () => { ... })` + `describe('redis-keys drift-lock', () => { ... })`. Use vi.fn() for spawnFn / redis methods; build a Map-backed fake redis client whose `hset` writes to the map, `hgetall` reads back, `scan` returns Array.from(map.keys()).filter(matchesPattern). Set up the fakes in a `beforeEach`. Commit message: `test(248-01): RED display-manager — drift-locks + create/list/kill/owner-scope`.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && pnpm vitest run source/modules/computer-use/displays/__tests__/display-manager.test.ts 2>&1 | grep -E "(FAIL|Failed to load url|Cannot find module)"</automated>
  </verify>
  <done>vitest reports "Failed to load url" or equivalent module-not-found for `../display-manager.js` + `../redis-keys.js` + `../types.js` — classic RED. Commit landed with `[sacred-sha] PASS: 20 files verified`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement display-manager + types + redis-keys + barrel</name>
  <files>
    livos/packages/livinityd/source/modules/computer-use/displays/types.ts
    livos/packages/livinityd/source/modules/computer-use/displays/redis-keys.ts
    livos/packages/livinityd/source/modules/computer-use/displays/display-manager.ts
    livos/packages/livinityd/source/modules/computer-use/displays/index.ts
  </files>
  <behavior>All 14 Task 1 tests pass. Module exports the public surface enumerated in must_haves.</behavior>
  <action>
    Implement the minimum code to make all 14 tests GREEN.

    `types.ts` — Export `DisplayMode = 'xephyr' | 'xvfb'`, `DisplayRecord = {display: string; name: string; mode: DisplayMode; created_at: string; owner_session: string; width: number; height: number; running_apps: number[]}`, `CreateDisplayInput = {mode?: DisplayMode; name?: string; width?: number; height?: number; ownerSession: string}`, `KillDisplayResult = {ok: true; killed_apps_count: number} | {ok: false; error: 'not-owner' | 'not-found'}`, `DisplayManagerDeps` (with `redis`, `spawnFn?`, `nowFn?`, `allocatorStart?`, `logger?` — DI seams), `DisplayManager` interface (the methods listed in truths).

    `redis-keys.ts` — Export `DISPLAY_REDIS_PREFIX = 'luse:display:'` (drift-lock literal), `redisKeyForDisplay(display: string) → \`${DISPLAY_REDIS_PREFIX}${display}\``, `redisKeyForDisplayApps(display: string) → \`${redisKeyForDisplay(display)}:apps\``. Keep this file tiny — it's the drift-lock surface for tests 1-3.

    `display-manager.ts` — `createDisplayManager(deps): DisplayManager`. Defaults: `spawnFn = (await import('node:child_process')).spawn`, `nowFn = Date.now`, `allocatorStart = 10`, `logger = noop`. The manager maintains:
      - In-memory `Map<string, {pid: number; handle: ChildProcess}>` for spawn-handles (so kill() can SIGTERM by display)
      - An async-init phase that SCANs `luse:display:*` in Redis to seed the allocator counter (highest existing :N + 1)
      - `create()`: pick next :N via counter, build spawn args per mode:
        * xephyr: `['Xephyr', [':<N>', '-screen', '<W>x<H>', '-ac', '-noreset']]`
        * xvfb: `['Xvfb', [':<N>', '-screen', '0', '<W>x<H>x24', '-ac', '-noreset']]`
        Call `spawnFn(cmd, args, {stdio: 'ignore', detached: false})`. Store handle in map. HSET `luse:display:<display>` with all fields. Return `{display, name: input.name ?? \`display-<N>\`, pid: handle.pid}`.
      - `list()`: SCAN for `luse:display:*` keys (filter out `:apps` suffix), HGETALL each, LRANGE apps key, map to `DisplayRecord[]`. Read-only — any caller session.
      - `kill({display, callerSession})`: HGET owner_session, compare to callerSession. If mismatch → `{ok:false, error:'not-owner'}`. If match: LRANGE apps, SIGTERM each app pid (best-effort; ignore ESRCH), SIGTERM the X server handle from the in-memory map, DEL both Redis keys, drop the map entry, return `{ok:true, killed_apps_count}`.
      - `attachApp({display, pid, app_name})`: RPUSH pid as string into the apps key + HSET `last_app_at` = nowFn() ISO on the display key.
      - `isOwner({display, session})`: HGET owner_session, return boolean.

    Use `'displays'` child logger like Phase 246 modules.

    `index.ts` (barrel) — Re-export `createDisplayManager`, types, `DISPLAY_REDIS_PREFIX`, `redisKeyForDisplay`, `redisKeyForDisplayApps`.

    Commit message: `feat(248-01): GREEN display-manager — Xephyr/Xvfb spawn + allocator + Redis state + owner-scoped kill`.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && pnpm vitest run source/modules/computer-use/displays/__tests__/display-manager.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All 14 vitest cases GREEN. Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved (pre-commit hook PASS).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Typecheck + barrel verification + commit</name>
  <files>
    livos/packages/livinityd/source/modules/computer-use/displays/index.ts
  </files>
  <behavior>
    - `pnpm tsc --noEmit` adds zero new errors compared to baseline
    - `import {createDisplayManager, DISPLAY_REDIS_PREFIX} from '../displays/index.js'` resolves from any sibling module under `computer-use/`
  </behavior>
  <action>
    Run `cd livos/packages/livinityd && pnpm tsc --noEmit 2>&1 | grep -E "displays/" | wc -l` — must be 0. If non-zero, fix the types until clean. Then write a tiny ephemeral import smoke check: `node -e "import('./packages/livinityd/source/modules/computer-use/displays/index.js').then(m => console.log(Object.keys(m).sort().join(',')))"` (or equivalent via tsx) to confirm the barrel exports the expected surface (`DISPLAY_REDIS_PREFIX,createDisplayManager,redisKeyForDisplay,redisKeyForDisplayApps`). Document the verified barrel surface in commit body. Commit message: `chore(248-01): typecheck clean + barrel surface verified`.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && pnpm tsc --noEmit 2>&1 | grep -cE "computer-use/displays/"</automated>
  </verify>
  <done>Output is `0` — zero typecheck errors under `displays/`. Sacred SHA preserved. Wave 2 unblocked.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run source/modules/computer-use/displays` → 14/14 GREEN
- `pnpm tsc --noEmit | grep displays/` → 0 errors
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved on every commit
- D-V44-DISPLAY-XEPHYR-DEFAULT drift-locked via Test 6
- D-V44-DISPLAY-OWNER-SCOPED drift-locked via Tests 11 + 12
- Allocator drift-locked via Tests 4 + 5 + 14
</verification>

<success_criteria>
- All 4 module files exist and export the public surface
- 14/14 vitest GREEN, including all drift-locks
- Zero new typecheck errors
- Barrel `displays/index.ts` re-exports the 5 public symbols
- Module is self-contained — no edits to existing computer-use files (those happen in 248-02)
</success_criteria>

<output>
After completion, create `.planning/phases/248-luse-display-lifecycle/248-01-SUMMARY.md` documenting:
- The 4 created files + line counts
- The 14 drift-locked test cases + literal values they pin
- Why owner-scope is enforced at the manager layer (so MCP wrappers + TTL GC inherit it for free)
- Reference to the next plan (248-02 wires this into MCP tools)
</output>
