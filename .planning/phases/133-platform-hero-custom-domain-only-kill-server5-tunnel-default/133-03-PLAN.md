---
phase: 133-platform-hero-custom-domain-only-kill-server5-tunnel-default
plan: 03
type: execute
wave: 2
depends_on: ["133-01", "133-02"]
files_modified:
  - .planning/phases/133-platform-hero-custom-domain-only-kill-server5-tunnel-default/133-UAT-CHECKLIST.md
  - .planning/phases/133-platform-hero-custom-domain-only-kill-server5-tunnel-default/133-03-SUMMARY.md
  - .planning/STATE.md
  - memory/project_phase_133_complete.md
autonomous: false
requirements:
  - V34-PLATFORM-03
tags:
  - uat
  - fresh-user
  - end-to-end
blocking_questions:
  - "Operator must have a livinity.io account they can register fresh from (different from any existing user), OR a way to create one for the UAT walk."
must_haves:
  truths:
    - "A newly-registered livinity.io user with NO install yet sees the empty-state hero ('let's connect your computer') — no `${username}.livinity.io` URL anywhere."
    - "After that user completes a hybrid-mode wizard Generate, a row exists in `custom_domains` for them with status='pending_dns'."
    - "lucylu (post-hotfix) sees her chosen domain in the hero, NOT `lucylu.livinity.io`."
    - "Sacred SHA invariant holds."
  artifacts:
    - path: ".planning/phases/133-.../133-UAT-CHECKLIST.md"
      provides: "PASS/FAIL grid for the three flows (empty-state, post-Generate, lucylu)"
      contains: "UAT TARGET: TUNNEL-URL KILLED"
    - path: ".planning/phases/133-.../133-03-SUMMARY.md"
      provides: "Verdict + STATE flip + memory entry"
      contains: "PHASE_133_TUNNEL_KILLED_VERIFIED"
---

<objective>
Operator-walked UAT to verify Phase 133 closes both bugs end-to-end
and that `${username}.livinity.io` no longer appears on any
user-visible platform surface for users with custom domains.

This is the gate that flips Phase 133 from CODE-COMPLETE to SHIPPED.
</objective>

<context>
@.planning/phases/133-platform-hero-custom-domain-only-kill-server5-tunnel-default/133-CONTEXT.md
@.planning/phases/133-platform-hero-custom-domain-only-kill-server5-tunnel-default/133-PLAN.md
</context>

<tasks>

<task id="03-01" type="execute">
<action>
Write `133-UAT-CHECKLIST.md` with three flows:

**Flow A — Empty-state hero (fresh user, no install):**

| # | Action | Expected | PASS | FAIL |
|---|--------|----------|:----:|:----:|
| A1 | Register a NEW livinity.io account (any username) | Verify email if needed; reach `/dashboard` | ☐ | ☐ |
| A2 | View `/dashboard` hero | Shows "let's connect your computer" + "Go to install wizard" CTA. NO `${username}.livinity.io` anywhere in the visible page | ☐ | ☐ |
| A3 | View page source / DOM | No string `${username}.livinity.io` rendered for THIS user | ☐ | ☐ |

**Flow B — Wizard Generate auto-registers domain:**

| # | Action | Expected | PASS | FAIL |
|---|--------|----------|:----:|:----:|
| B1 | From the empty-state hero, click "Go to install wizard" | Wizard loads | ☐ | ☐ |
| B2 | Select Hybrid mode, fill domain (e.g. `uat133-fresh.bruceoz.com`), CF token, zone ID | All fields editable | ☐ | ☐ |
| B3 | Click Generate API Key | Returns 200 with `liv_k_*` token | ☐ | ☐ |
| B4 | SSH into Server5 + check DB | `SELECT * FROM custom_domains WHERE domain = 'uat133-fresh.bruceoz.com';` returns 1 row, status='pending_dns', user_id matches the fresh account | ☐ | ☐ |
| B5 | Reload `/dashboard` | Hero now shows `uat133-fresh.bruceoz.com` (status pending_dns may render with a pending badge — OK) | ☐ | ☐ |

**Flow D — Wizard subdomain pre-fill (Bug C):**

| # | Action | Expected | PASS | FAIL |
|---|--------|----------|:----:|:----:|
| D1 | Log in as a fresh user (any username, e.g. `freshuat`) | Dashboard loads | ☐ | ☐ |
| D2 | Navigate to `/dashboard/install` → select Hybrid mode | Wizard hybrid form renders | ☐ | ☐ |
| D3 | Inspect Domain input field on first mount | Field is pre-filled with `freshuat.` (username + dot), cursor positioned at end | ☐ | ☐ |
| D4 | Type `bruceoz.com` after the dot | Field reads `freshuat.bruceoz.com`, fully editable | ☐ | ☐ |
| D5 | Clear the field entirely then re-focus | Pre-fill does NOT re-fire (single-shot only — user can override to a different subdomain) | ☐ | ☐ |

**Flow C — Existing-user empty state (no lucylu hotfix):**

| # | Action | Expected | PASS | FAIL |
|---|--------|----------|:----:|:----:|
| C1 | Log in as lucylu (or any existing user with no `custom_domains` row) | Dashboard loads | ☐ | ☐ |
| C2 | View hero | Shows the empty state ("let's connect your computer"), NOT `${username}.livinity.io` | ☐ | ☐ |
| C3 | Click "Go to install wizard" → re-run Generate API Key with hybrid + their domain | Wizard auto-inserts `custom_domains` row (Bug A); reload dashboard shows the new domain in hero | ☐ | ☐ |

(Per user directive 2026-05-17, no per-user data hotfix shipped — existing users complete the round-trip by re-running wizard Generate, which the 133-01 fix makes self-registering.)

**Sacred SHA check:**

| # | Check | Expected | PASS | FAIL |
|---|-------|----------|:----:|:----:|
| S1 | `git ls-tree HEAD liv/packages/core/src/sdk-agent-runner.ts` | `f3538e1d811992b782a9bb057d1b7f0a0189f95f` | ☐ | ☐ |

**Verdict:**

- All PASS → write `133-03-SUMMARY.md` with `PHASE_133_TUNNEL_KILLED_VERIFIED`, flip `phase_133_status` to SHIPPED in STATE.md, write `memory/project_phase_133_complete.md`.
- Any FAIL → open `133.X` hotfix sub-plan and re-walk.
</action>
<acceptance_criteria>
- `133-UAT-CHECKLIST.md` exists with `UAT TARGET: TUNNEL-URL KILLED`.
- All 3 flows (A/B/C) have explicit PASS/FAIL rows.
</acceptance_criteria>
</task>

<task id="03-02" type="execute">
<action>
**After operator walks the checklist + all PASS:** write the SHIPPED-
flavor summary + STATE flip + memory entry.

(Defer to operator-walked completion; this task is a stub that fires
only when 03-01 walks green.)
</action>
<acceptance_criteria>
- `133-03-SUMMARY.md` with `PHASE_133_TUNNEL_KILLED_VERIFIED`.
- STATE.md `phase_133_status: SHIPPED`.
- `memory/project_phase_133_complete.md` written.
</acceptance_criteria>
</task>

</tasks>

<verification>
Operator walks `133-UAT-CHECKLIST.md` end-to-end. If all PASS → ship.
If any FAIL → open hotfix sub-plan.
</verification>
