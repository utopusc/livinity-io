---
phase: 119-reusable-component-library
plan: 03
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - livos/packages/ui-kit/src/components/Stepper.tsx
  - livos/packages/ui-kit/src/components/Stepper.types.ts
  - livos/packages/ui-kit/src/components/Stepper.stories.tsx
  - livos/packages/ui-kit/src/components/Stepper.test.tsx
  - livos/packages/ui-kit/src/components/CommandBox.tsx
  - livos/packages/ui-kit/src/components/CommandBox.types.ts
  - livos/packages/ui-kit/src/components/CommandBox.stories.tsx
  - livos/packages/ui-kit/src/components/CommandBox.test.tsx
  - livos/packages/ui-kit/src/components/Modal.tsx
  - livos/packages/ui-kit/src/components/Modal.types.ts
  - livos/packages/ui-kit/src/components/Modal.stories.tsx
  - livos/packages/ui-kit/src/components/Modal.test.tsx
  - livos/packages/ui-kit/src/components/Toast.tsx
  - livos/packages/ui-kit/src/components/Toast.types.ts
  - livos/packages/ui-kit/src/components/Toast.stories.tsx
  - livos/packages/ui-kit/src/components/Toast.test.tsx
  - livos/packages/ui-kit/src/components/NavBar.tsx
  - livos/packages/ui-kit/src/components/NavBar.types.ts
  - livos/packages/ui-kit/src/components/NavBar.stories.tsx
  - livos/packages/ui-kit/src/components/NavBar.test.tsx
  - livos/packages/ui-kit/src/components/ThemeToggle.tsx
  - livos/packages/ui-kit/src/components/ThemeToggle.stories.tsx
  - livos/packages/ui-kit/src/components/ThemeToggle.test.tsx
  - livos/packages/ui-kit/src/styles/composites.css
  - livos/packages/ui-kit/src/index.ts
autonomous: true
requirements:
  - D-119-DASHBOARD-HTML-IS-SOURCE
  - D-119-NO-CONSUMER-CHANGES
  - D-119-LIGHT-DARK-IRIDESCENT-PARITY
  - D-119-A11Y-FOCUS-RINGS
must_haves:
  truths:
    - "6 composite components exported from @livinity/ui-kit: Stepper, CommandBox, Modal, Toast, NavBar, ThemeToggle"
    - "Stepper renders steps array with current index, marking previous as done and current as active per dashboard.html .stepper visual"
    - "CommandBox shows a mono-font command string with optional copy button using navigator.clipboard.writeText"
    - "Modal is controlled (open + onClose), traps focus while open, closes on Escape, restores focus on close"
    - "Toast provides a ToastProvider + imperative toast.success/warn/error/info APIs that render in a fixed bottom-right region with role='status'"
    - "NavBar renders a brand wordmark + actions slot + ThemeToggle"
    - "ThemeToggle cycles light -> dark -> iridescent and persists to localStorage['liv_theme']"
  artifacts:
    - path: "livos/packages/ui-kit/src/components/Stepper.tsx"
      provides: "<Stepper steps={Array<{label,id?}>} current={number} /> with ARIA"
    - path: "livos/packages/ui-kit/src/components/CommandBox.tsx"
      provides: "<CommandBox text={string} copyButton={boolean} /> clipboard API"
    - path: "livos/packages/ui-kit/src/components/Modal.tsx"
      provides: "<Modal open onClose title> controlled w/ focus trap + ESC close"
    - path: "livos/packages/ui-kit/src/components/Toast.tsx"
      provides: "<ToastProvider /> + useToast() hook + imperative toast object"
    - path: "livos/packages/ui-kit/src/components/NavBar.tsx"
      provides: "<NavBar brand actions /> using b-card-tight visual + composites.css .navbar"
    - path: "livos/packages/ui-kit/src/components/ThemeToggle.tsx"
      provides: "<ThemeToggle /> cycling 3 themes via applyLivTheme + localStorage"
    - path: "livos/packages/ui-kit/src/styles/composites.css"
      provides: ".stepper / .cmd-box / .modal / .toast / .navbar canonical classes using design-tokens vars"
    - path: "livos/packages/ui-kit/src/index.ts"
      provides: "Public exports for all 6 composites + their *Props types + useToast hook + ToastProvider"
      contains: "export { Stepper } from"
  key_links:
    - from: "livos/packages/ui-kit/src/components/Stepper.tsx"
      to: "@livinity/design-tokens tokens.css"
      via: "composites.css .stepper .step.active uses var(--accent-blue), .step.done uses var(--accent-green)"
      pattern: "var\\(--accent-(blue|green)\\)"
    - from: "livos/packages/ui-kit/src/components/CommandBox.tsx"
      to: "navigator.clipboard"
      via: "copy button onClick"
      pattern: "navigator\\.clipboard\\.writeText"
    - from: "livos/packages/ui-kit/src/components/Modal.tsx"
      to: "document.activeElement focus restoration"
      via: "useEffect on open transition + cleanup"
      pattern: "previousActiveElement|document\\.activeElement"
    - from: "livos/packages/ui-kit/src/components/ThemeToggle.tsx"
      to: "lib/theme-classes (applyLivTheme + LIV_THEME_STORAGE_KEY)"
      via: "import from ../lib/theme-classes"
      pattern: "applyLivTheme"
---

<objective>
Ship the 6 composite components for `@livinity/ui-kit`: Stepper, CommandBox, Modal, Toast, NavBar, ThemeToggle. These are composed (where possible) on top of the atoms shipped in 119-02. They run in parallel with 119-02 because composites.css is a separate stylesheet and the composite `.tsx` files don't import the atom components by source (Modal uses Card-style classes directly, NavBar uses raw HTML + composites.css). The Toast pattern is the global provider variant locked in CONTEXT.md open question.

Purpose: These six are the user-interaction primitives that the install wizard, dashboard, and Mini PC settings flows depend on (dashboard.html `.stepper`, `.cmd-box`, theme cycle, future modal/toast). Per D-119-DASHBOARD-HTML-IS-SOURCE: Stepper + CommandBox + ThemeToggle match dashboard.html / dashboard-install.html exactly; Modal + Toast are NEW canonical visuals derived from card tokens (dashboard.html has no modal/toast yet, see Phase 115 COMPONENT-MAP.md rows for Modal and Toast).

Output: 6 components × {Component.tsx, [Component.types.ts], Component.stories.tsx, Component.test.tsx} = 23 files (ThemeToggle has no .types.ts — props are trivial), plus composites.css, plus updated `src/index.ts` (full library re-export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-reusable-component-library/119-CONTEXT.md
@.planning/phases/119-reusable-component-library/119-01-PLAN.md
@.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/design-tokens/tokens.css

<interfaces>
<!-- Provided by 119-01 chassis -->

From livos/packages/ui-kit/src/lib/cn.ts:
```ts
export function cn(...inputs: ClassValue[]): string;
```

From livos/packages/ui-kit/src/lib/theme-classes.ts:
```ts
export const LIV_THEME_STORAGE_KEY: "liv_theme";
export type LivTheme = "light" | "dark" | "iridescent";
export const LIV_THEMES: readonly LivTheme[]; // ["light","dark","iridescent"]
export function applyLivTheme(theme: LivTheme, body?: HTMLElement): void;
export function readLivTheme(): LivTheme;
```

<!-- Will exist after 119-02 finishes — do NOT depend on these for parallel execution. -->
<!-- 119-03 ONLY relies on the 119-01 chassis (cn, theme-classes), NOT on 119-02 atoms. -->
<!-- src/index.ts merge in Task 4 below assumes 119-02 has landed its exports; if not, append-only. -->
</interfaces>

<canonical_visual_contracts>
<!-- Locked to dashboard.html / dashboard-install.html (Phase 115 COMPONENT-MAP.md). -->

`.stepper` (Stepper):
  - Horizontal flex, gap 12px
  - Each `.step`: padding 8px 14px; radius 999px; mono font 11px uppercase letter-spacing 0.08em
  - `.step` (idle): background var(--card-bg-2); color rgba(0,0,0,0.5)
  - `.step.active`: background var(--accent-blue); color #fff
  - `.step.done`: background var(--accent-green); color #fff; prefix a ✓ glyph (Unicode or inline SVG)

`.cmd-box` (CommandBox):
  - Background: dark surface in light mode (the dashboard-install.html visual uses #0b0b0b on a light page); but token-driven: use var(--card-bg-2) inverted via an inline `style={{ background: 'var(--cmd-box-bg, #0b0b0b)' }}` fallback OR introduce a new token. SHIP IT WITH: `background: #0b0b0b; color: #f0f0f0;` directly in composites.css — this is dashboard-install.html canonical visual. (One exception to "tokens only" because dashboard-install.html literally hardcodes this surface contrast for legibility. Document the exception in composites.css comments.)
  - Padding 14px 16px; radius 12px
  - Font-family var(--font-mono); font-size 13px
  - Optional `.copy` button inline-right: small ghost button with eye/clipboard SVG; aria-label "Copy to clipboard"; on copy, briefly swap aria-label to "Copied"

`.modal` (Modal — NEW canonical visual):
  - Backdrop: `position: fixed; inset: 0; background: rgba(0,0,0,0.5); backdrop-filter: blur(4px);`
  - Panel: centered via flex; background var(--card-bg); border-radius var(--dash-radius); padding var(--dash-pad); max-width 480px; box-shadow var(--card-shadow)
  - Close X button: top-right, ghost style, aria-label "Close"
  - `:focus-visible` rings as established in atoms.css

`.toast-region` (Toast — NEW canonical visual):
  - Region: `position: fixed; right: 24px; bottom: 24px; display: flex; flex-direction: column; gap: 8px;` with `role="region" aria-label="Notifications"`
  - Each toast: `role="status"` (or `role="alert"` for error variant); composition uses the same accent tints as `.pill` for tone parity (D-119-CONTEXT mentions Toast composed from .pill — keep the visual aligned, but `.toast` has more padding + width than `.pill`)
  - Variants: success (var(--accent-green)), warn (var(--accent-amber)), error (var(--accent-red)), info (var(--accent-blue))
  - Auto-dismiss after 4000ms (configurable)
  - Slide-in via `transform: translateX(100%) -> translateX(0)` over 180ms ease (matches dashboard.html 0.18s motion token)

`.navbar` (NavBar):
  - Horizontal flex, padding 16px var(--dash-pad); border-bottom 1px solid var(--dash-line)
  - Brand left, actions slot right, optional theme toggle in actions
  - Background var(--card-bg) (matches `.hero-card`-ish dashboard.html convention)
  - F-115-MAP-05: token-driven only — NO hardcoded #e5e5e7 or bg-white/80

`ThemeToggle`:
  - Small ghost button with sun/moon/sparkle SVGs cycling
  - Reads localStorage on mount via `readLivTheme()`, applies via `applyLivTheme()`
  - On click: cycle light -> dark -> iridescent -> light; persist; aria-label reflects "Switch to dark theme" / "Switch to iridescent theme" / "Switch to light theme"
</canonical_visual_contracts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: composites.css + Stepper + CommandBox + ThemeToggle (3 simpler composites)</name>
  <files>
    livos/packages/ui-kit/src/styles/composites.css,
    livos/packages/ui-kit/src/components/Stepper.types.ts,
    livos/packages/ui-kit/src/components/Stepper.tsx,
    livos/packages/ui-kit/src/components/Stepper.stories.tsx,
    livos/packages/ui-kit/src/components/Stepper.test.tsx,
    livos/packages/ui-kit/src/components/CommandBox.types.ts,
    livos/packages/ui-kit/src/components/CommandBox.tsx,
    livos/packages/ui-kit/src/components/CommandBox.stories.tsx,
    livos/packages/ui-kit/src/components/CommandBox.test.tsx,
    livos/packages/ui-kit/src/components/ThemeToggle.tsx,
    livos/packages/ui-kit/src/components/ThemeToggle.stories.tsx,
    livos/packages/ui-kit/src/components/ThemeToggle.test.tsx
  </files>
  <behavior>
- Stepper.test.tsx:
  - Test 1: renders all step labels in order
  - Test 2: step at index < current gets class `done`; index === current gets `active`; index > current gets no state class
  - Test 3: each step has role='listitem' inside a role='list' parent with aria-label='Progress'
  - Test 4: current step has aria-current='step'
  - Test 5: done step renders a ✓ prefix
- CommandBox.test.tsx:
  - Test 1: renders the text in a <code> or <pre> element with `.cmd-box` class
  - Test 2: when copyButton=true, renders a button with aria-label='Copy to clipboard'
  - Test 3: when copyButton=false (default), NO copy button is rendered
  - Test 4: clicking copy calls navigator.clipboard.writeText with the text (mock the clipboard API)
  - Test 5: after a successful copy, the button aria-label briefly changes to 'Copied' (then reverts after timeout — use vi.useFakeTimers)
- ThemeToggle.test.tsx:
  - Test 1: renders a button with an aria-label naming the NEXT theme
  - Test 2: initial label reflects readLivTheme() (mock localStorage with 'light' -> label 'Switch to dark theme')
  - Test 3: clicking cycles light -> dark -> iridescent -> light; each click writes to localStorage and toggles body classes
  - Test 4: button has type='button' (not 'submit') and a :focus-visible-compatible class
  </behavior>
  <action>
**Read first:** `livos/packages/ui-kit/src/lib/theme-classes.ts` (the cycle source of truth), `.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md` § Stepper + CommandBox + ThemeToggle rows.

Create `livos/packages/ui-kit/src/styles/composites.css` with `.stepper`, `.step`, `.step.active`, `.step.done`, `.cmd-box`, `.cmd-box-copy`, `.theme-toggle` rules. Every color/spacing/radius MUST be a `var(--*)` reference from design-tokens — with ONE documented exception: `.cmd-box` ships `background: #0b0b0b; color: #f0f0f0;` per dashboard-install.html canonical visual (call this exception out in a CSS comment block with rationale "matches dashboard-install.html canonical contrast surface; do not change without consulting Phase 116 token-extension PR"). Modal + Toast + NavBar styles are deferred to Task 2 and Task 3 — composites.css will be appended.

Create `Stepper.types.ts`:
```ts
export interface StepperStep {
  id?: string;
  label: string;
}
export interface StepperProps {
  steps: StepperStep[];
  current: number;
  className?: string;
}
```

Create `Stepper.tsx`:
```tsx
import React, { forwardRef } from "react";
import { cn } from "../lib/cn";
import type { StepperProps } from "./Stepper.types";
import "../styles/composites.css";

export const Stepper = forwardRef<HTMLOListElement, StepperProps>(
  ({ steps, current, className }, ref) => (
    <ol
      ref={ref}
      role="list"
      aria-label="Progress"
      className={cn("stepper", className)}
    >
      {steps.map((step, i) => {
        const state = i < current ? "done" : i === current ? "active" : undefined;
        return (
          <li
            key={step.id ?? `${i}-${step.label}`}
            role="listitem"
            aria-current={state === "active" ? "step" : undefined}
            className={cn("step", state)}
          >
            {state === "done" && <span aria-hidden="true">✓</span>}
            {step.label}
          </li>
        );
      })}
    </ol>
  ),
);
Stepper.displayName = "Stepper";
```

Create `Stepper.stories.tsx` with stories: `Default` (4 steps, current=1), `FirstStep` (current=0), `LastStep` (current=3), `AllDone` (current=4 i.e. beyond array length → all done).

Create `Stepper.test.tsx` per <behavior>.

Create `CommandBox.types.ts`:
```ts
export interface CommandBoxProps {
  text: string;
  copyButton?: boolean;
  className?: string;
}
```

Create `CommandBox.tsx`. The copy flow:
```tsx
const [justCopied, setJustCopied] = useState(false);
async function handleCopy() {
  try {
    await navigator.clipboard.writeText(text);
    setJustCopied(true);
    setTimeout(() => setJustCopied(false), 1500);
  } catch {
    // swallow — tests assert the writeText call, not error UX
  }
}
```
Render `<div class="cmd-box">` containing a `<pre>` for the text and (if copyButton) a `<button type="button" aria-label={justCopied ? "Copied" : "Copy to clipboard"} class="cmd-box-copy" onClick={handleCopy}>` with inline clipboard SVG.

Create `CommandBox.stories.tsx`: `Default` (no copy), `WithCopyButton`, `LongCommand` (text spanning multiple lines using `white-space: pre-wrap` rule in composites.css).

Create `CommandBox.test.tsx` per <behavior>. Mock the clipboard: `Object.assign(navigator, { clipboard: { writeText: vi.fn().mockResolvedValue(undefined) } });`. Use `vi.useFakeTimers()` for the 1500ms revert assertion.

Create `ThemeToggle.tsx`:
```tsx
import React, { useEffect, useState } from "react";
import {
  applyLivTheme,
  readLivTheme,
  LIV_THEME_STORAGE_KEY,
  LIV_THEMES,
  type LivTheme,
} from "../lib/theme-classes";
import { cn } from "../lib/cn";
import "../styles/composites.css";

const NEXT: Record<LivTheme, LivTheme> = {
  light: "dark",
  dark: "iridescent",
  iridescent: "light",
};

const LABEL: Record<LivTheme, string> = {
  light: "Switch to dark theme",
  dark: "Switch to iridescent theme",
  iridescent: "Switch to light theme",
};

export interface ThemeToggleProps {
  className?: string;
}

export const ThemeToggle: React.FC<ThemeToggleProps> = ({ className }) => {
  const [theme, setTheme] = useState<LivTheme>("light");

  useEffect(() => {
    const initial = readLivTheme();
    setTheme(initial);
    applyLivTheme(initial);
  }, []);

  function cycle() {
    const next = NEXT[theme];
    setTheme(next);
    applyLivTheme(next);
    try {
      window.localStorage.setItem(LIV_THEME_STORAGE_KEY, next);
    } catch {
      /* private mode — fail silent */
    }
  }

  return (
    <button
      type="button"
      onClick={cycle}
      aria-label={LABEL[theme]}
      className={cn("theme-toggle", className)}
    >
      {/* inline sun/moon/sparkle SVG keyed off `theme` — see composites.css for sizing */}
      <span aria-hidden="true">{theme === "light" ? "☀" : theme === "dark" ? "☾" : "✦"}</span>
    </button>
  );
};
ThemeToggle.displayName = "ThemeToggle";
```
(Unicode glyphs are acceptable for v0.1.0 — DESIGN-SYSTEM.md does not specify icon assets. A future v1.x can swap to SVG.)

Create `ThemeToggle.stories.tsx`: a single `Default` story.

Create `ThemeToggle.test.tsx` per <behavior>. Use `beforeEach(() => window.localStorage.clear());`.

RED first for each, then GREEN.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit test -- src/components/Stepper.test.tsx src/components/CommandBox.test.tsx src/components/ThemeToggle.test.tsx &amp;&amp; node -e "const fs=require('fs'); const css=fs.readFileSync('packages/ui-kit/src/styles/composites.css','utf8'); ['var(--accent-blue)','var(--accent-green)','.stepper','.step.active','.step.done','.cmd-box','.theme-toggle','#0b0b0b'].forEach(t=&gt;{if(!css.includes(t))throw new Error('composites.css missing '+t);}); console.log('composites.css ok');"</automated>
  </verify>
  <done>
- composites.css contains `.stepper`, `.step.active` (var(--accent-blue)), `.step.done` (var(--accent-green)), `.cmd-box` (with the documented #0b0b0b exception comment), `.theme-toggle`
- 5 + 5 + 4 = 14 new test assertions pass
- Stepper.stories renders 4 named stories; CommandBox.stories renders 3; ThemeToggle.stories renders 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modal with focus trap + Toast with global provider</name>
  <files>
    livos/packages/ui-kit/src/components/Modal.types.ts,
    livos/packages/ui-kit/src/components/Modal.tsx,
    livos/packages/ui-kit/src/components/Modal.stories.tsx,
    livos/packages/ui-kit/src/components/Modal.test.tsx,
    livos/packages/ui-kit/src/components/Toast.types.ts,
    livos/packages/ui-kit/src/components/Toast.tsx,
    livos/packages/ui-kit/src/components/Toast.stories.tsx,
    livos/packages/ui-kit/src/components/Toast.test.tsx
  </files>
  <behavior>
- Modal.test.tsx:
  - Test 1: when open=false, no modal markup rendered (no role='dialog' in DOM)
  - Test 2: when open=true, role='dialog' + aria-modal='true' + aria-labelledby pointing at the title
  - Test 3: pressing Escape calls onClose
  - Test 4: clicking the backdrop calls onClose; clicking the panel does NOT
  - Test 5: clicking the close X button calls onClose
  - Test 6: on open, focus moves into the modal (first focusable element); on close, focus returns to the element that was focused before open
  - Test 7: tab cycles focus within the modal (focus trap — assert that tabbing past the last focusable wraps to the first)
- Toast.test.tsx:
  - Test 1: <ToastProvider> wraps children and renders a region with role='region' aria-label='Notifications'
  - Test 2: useToast() inside the provider returns an object with success, warn, error, info methods
  - Test 3: calling toast.success('hi') renders a node with role='status' containing 'hi'
  - Test 4: calling toast.error('oops') renders a node with role='alert'
  - Test 5: a toast auto-dismisses after 4000ms (vi.useFakeTimers + vi.advanceTimersByTime)
  - Test 6: useToast() OUTSIDE a ToastProvider throws a clear error
  </behavior>
  <action>
**Read first:** `livos/packages/ui-kit/src/styles/composites.css` (the file shipped in Task 1 — Task 2 APPENDS modal + toast rules). `livos/packages/ui-kit/src/lib/cn.ts`.

Append to `composites.css`: `.modal-backdrop`, `.modal-panel`, `.modal-close`, `.toast-region`, `.toast`, `.toast.success/.warn/.error/.info`. Token-driven (var(--card-bg), var(--card-shadow), var(--dash-radius), var(--dash-pad), accent vars for tone tints).

Create `Modal.types.ts`:
```ts
import type { ReactNode } from "react";
export interface ModalProps {
  open: boolean;
  onClose: () => void;
  title: string;
  children?: ReactNode;
  className?: string;
}
```

Create `Modal.tsx`. Implementation requirements:
- When `open` flips false→true: snapshot `document.activeElement` into a ref, then on mount of the dialog DOM, focus the first focusable descendant (use a query: `'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])'`).
- When `open` flips true→false: restore focus to the previously snapshotted element if still in the DOM.
- Keydown listener on the dialog: `Escape` → `onClose()`; `Tab` → if shift+tab on first focusable → focus last; if tab on last → focus first.
- Render via `ReactDOM.createPortal` to `document.body` when open=true. (Import `createPortal` from "react-dom".)
- Backdrop has `onClick={onClose}`; panel has `onClick={(e) => e.stopPropagation()}`.
- aria-labelledby points to a generated id (React.useId) on the heading element rendered from `title`.

Skeleton:
```tsx
import React, { useEffect, useId, useRef } from "react";
import { createPortal } from "react-dom";
import type { ModalProps } from "./Modal.types";
import { cn } from "../lib/cn";
import "../styles/composites.css";

const FOCUSABLE =
  'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';

export const Modal: React.FC<ModalProps> = ({ open, onClose, title, children, className }) => {
  const titleId = useId();
  const panelRef = useRef<HTMLDivElement>(null);
  const previousActiveElement = useRef<Element | null>(null);

  useEffect(() => {
    if (!open) return;
    previousActiveElement.current = document.activeElement;
    const root = panelRef.current;
    const focusables = root?.querySelectorAll<HTMLElement>(FOCUSABLE);
    focusables?.[0]?.focus();

    function onKey(e: KeyboardEvent) {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose();
        return;
      }
      if (e.key !== "Tab" || !root) return;
      const items = Array.from(root.querySelectorAll<HTMLElement>(FOCUSABLE));
      if (items.length === 0) return;
      const first = items[0];
      const last = items[items.length - 1];
      if (e.shiftKey && document.activeElement === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && document.activeElement === last) {
        e.preventDefault();
        first.focus();
      }
    }
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("keydown", onKey);
      const prev = previousActiveElement.current as HTMLElement | null;
      if (prev && document.contains(prev)) prev.focus();
    };
  }, [open, onClose]);

  if (!open) return null;
  return createPortal(
    <div className="modal-backdrop" onClick={onClose}>
      <div
        ref={panelRef}
        role="dialog"
        aria-modal="true"
        aria-labelledby={titleId}
        className={cn("modal-panel", className)}
        onClick={(e) => e.stopPropagation()}
      >
        <header className="modal-header">
          <h2 id={titleId} className="modal-title">{title}</h2>
          <button type="button" aria-label="Close" className="modal-close" onClick={onClose}>×</button>
        </header>
        <div className="modal-body">{children}</div>
      </div>
    </div>,
    document.body,
  );
};
Modal.displayName = "Modal";
```

Create `Modal.stories.tsx` with stories that wrap the Modal in a `useState`-driven trigger (Storybook control toggles `open`): `Default`, `WithLongContent`, `WithFormInside`.

Create `Modal.test.tsx` per <behavior>. The focus-trap test requires the modal to contain at least 2 focusable elements (the close X + a body button). Use `userEvent.tab()` and `userEvent.keyboard("{Escape}")`.

Create `Toast.types.ts`:
```ts
import type { ReactNode } from "react";
export type ToastTone = "success" | "warn" | "error" | "info";
export interface ToastItem {
  id: string;
  tone: ToastTone;
  message: ReactNode;
  duration?: number;
}
export interface ToastApi {
  success: (message: ReactNode, options?: { duration?: number }) => string;
  warn: (message: ReactNode, options?: { duration?: number }) => string;
  error: (message: ReactNode, options?: { duration?: number }) => string;
  info: (message: ReactNode, options?: { duration?: number }) => string;
  dismiss: (id: string) => void;
}
export interface ToastProviderProps {
  children: ReactNode;
  defaultDuration?: number;
}
```

Create `Toast.tsx` with a React Context implementation:
```tsx
import React, { createContext, useCallback, useContext, useEffect, useMemo, useRef, useState } from "react";
import { cn } from "../lib/cn";
import type { ToastApi, ToastItem, ToastProviderProps, ToastTone } from "./Toast.types";
import "../styles/composites.css";

const ToastContext = createContext<ToastApi | null>(null);

let counter = 0;
function makeId() { return `liv-toast-${++counter}-${Date.now()}`; }

export const ToastProvider: React.FC<ToastProviderProps> = ({ children, defaultDuration = 4000 }) => {
  const [items, setItems] = useState<ToastItem[]>([]);
  const timers = useRef<Map<string, ReturnType<typeof setTimeout>>>(new Map());

  const dismiss = useCallback((id: string) => {
    setItems((prev) => prev.filter((t) => t.id !== id));
    const timer = timers.current.get(id);
    if (timer) clearTimeout(timer);
    timers.current.delete(id);
  }, []);

  const push = useCallback(
    (tone: ToastTone, message: React.ReactNode, options?: { duration?: number }) => {
      const id = makeId();
      const duration = options?.duration ?? defaultDuration;
      setItems((prev) => [...prev, { id, tone, message, duration }]);
      if (duration > 0) {
        timers.current.set(id, setTimeout(() => dismiss(id), duration));
      }
      return id;
    },
    [defaultDuration, dismiss],
  );

  const api = useMemo<ToastApi>(
    () => ({
      success: (m, o) => push("success", m, o),
      warn: (m, o) => push("warn", m, o),
      error: (m, o) => push("error", m, o),
      info: (m, o) => push("info", m, o),
      dismiss,
    }),
    [push, dismiss],
  );

  useEffect(() => () => {
    timers.current.forEach((t) => clearTimeout(t));
    timers.current.clear();
  }, []);

  return (
    <ToastContext.Provider value={api}>
      {children}
      <div className="toast-region" role="region" aria-label="Notifications">
        {items.map((t) => (
          <div
            key={t.id}
            role={t.tone === "error" ? "alert" : "status"}
            className={cn("toast", t.tone)}
          >
            {t.message}
          </div>
        ))}
      </div>
    </ToastContext.Provider>
  );
};

export function useToast(): ToastApi {
  const ctx = useContext(ToastContext);
  if (!ctx) {
    throw new Error("useToast must be used within <ToastProvider>");
  }
  return ctx;
}
```

Create `Toast.stories.tsx` — single story that wraps `<ToastProvider>` + a row of buttons triggering each tone. Hook `useToast` inside an inner component.

Create `Toast.test.tsx` per <behavior>. Use `vi.useFakeTimers()` for the auto-dismiss assertion. For the "outside provider throws" test, wrap a hook-test in a `try { render(<HookConsumer />) } catch (e) { ... }` — or use `renderHook` with `expect(...).toThrow(...)`.

RED → GREEN for each.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit test -- src/components/Modal.test.tsx src/components/Toast.test.tsx</automated>
  </verify>
  <done>
- Modal: 7 assertions pass; focus trap verified; ESC + backdrop + close X all trigger onClose
- Toast: 6 assertions pass; provider/consumer wiring works; auto-dismiss honored
- composites.css contains `.modal-backdrop`, `.modal-panel`, `.toast-region`, `.toast`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NavBar + final index.ts merge + library-wide sweep</name>
  <files>
    livos/packages/ui-kit/src/components/NavBar.types.ts,
    livos/packages/ui-kit/src/components/NavBar.tsx,
    livos/packages/ui-kit/src/components/NavBar.stories.tsx,
    livos/packages/ui-kit/src/components/NavBar.test.tsx,
    livos/packages/ui-kit/src/index.ts
  </files>
  <behavior>
- NavBar.test.tsx:
  - Test 1: renders a brand element (text or node passed via `brand` prop)
  - Test 2: renders an actions slot when `actions` is provided
  - Test 3: renders semantic <header> (or <nav>) landmark
  - Test 4: has the `.navbar` class for styling
  - Test 5: passes through `className` (spread/cn)
  </behavior>
  <action>
**Read first:** `livos/packages/ui-kit/src/components/ThemeToggle.tsx` (NavBar's actions slot will typically include `<ThemeToggle />`), `livos/packages/ui-kit/src/styles/composites.css` (append `.navbar`, `.navbar-brand`, `.navbar-actions` styles).

Append to `composites.css`:
```css
.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px var(--dash-pad);
  background: var(--card-bg);
  border-bottom: 1px solid var(--dash-line);
  transition: 0.18s ease;
}
.navbar-brand {
  font-weight: 600;
  font-size: 15px;
  letter-spacing: -0.01em;
  color: inherit;
}
.navbar-actions {
  display: flex;
  align-items: center;
  gap: 12px;
}
```

Create `NavBar.types.ts`:
```ts
import type { ReactNode } from "react";
export interface NavBarProps {
  brand: ReactNode;
  actions?: ReactNode;
  className?: string;
  as?: "header" | "nav";
}
```

Create `NavBar.tsx`:
```tsx
import React, { forwardRef } from "react";
import { cn } from "../lib/cn";
import type { NavBarProps } from "./NavBar.types";
import "../styles/composites.css";

export const NavBar = forwardRef<HTMLElement, NavBarProps>(
  ({ brand, actions, className, as = "header" }, ref) => {
    const Tag = as as React.ElementType;
    return (
      <Tag ref={ref} className={cn("navbar", className)}>
        <div className="navbar-brand">{brand}</div>
        {actions && <div className="navbar-actions">{actions}</div>}
      </Tag>
    );
  },
);
NavBar.displayName = "NavBar";
```

Create `NavBar.stories.tsx` with 3 stories: `BrandOnly`, `BrandWithThemeToggle` (uses `<ThemeToggle />` in actions slot), `BrandWithCustomActions`.

Create `NavBar.test.tsx` per <behavior>. RED → GREEN.

**Final index.ts merge** — `src/index.ts` becomes the complete library export (composes outputs from 119-02 AND 119-03). If 119-02 has already shipped, edit the file additively. If 119-02 ships in parallel, write the index.ts assuming all components exist; coordinate via the file-ownership rule (this plan owns index.ts in Wave 2 just like 119-02 does — that's a conflict).

**Conflict resolution decision (LOCKED): The orchestrator scheduled 119-02 and 119-03 on the same wave with both editing `src/index.ts`. To preserve the wave-2 parallelism advertised in 119-CONTEXT.md, this plan WILL touch index.ts BUT ONLY APPEND its 6 composite exports + ToastProvider + useToast hook. The executor MUST:**
1. Read the current `src/index.ts` at task entry
2. Detect whether 119-02's atom exports are present (look for `export { Button }`)
3. If atoms exports present → append composite exports
4. If atoms exports NOT present → write the full union (atoms + composites) so 119-02 can re-merge later
5. Either way, the final file contains ALL 11 component exports

Final shape of `src/index.ts` after both 119-02 + 119-03 complete:
```ts
export const __ui_kit_version__ = "0.1.0";

// Atoms (Phase 119-02)
export { Button } from "./components/Button";
export type { ButtonProps, ButtonVariant, ButtonSize } from "./components/Button.types";
export { Card } from "./components/Card";
export type { CardProps } from "./components/Card.types";
export { Pill } from "./components/Pill";
export type { PillProps, PillTone } from "./components/Pill.types";
export { Input } from "./components/Input";
export type { InputProps } from "./components/Input.types";
export { PasswordInput } from "./components/PasswordInput";

// Composites (Phase 119-03)
export { Stepper } from "./components/Stepper";
export type { StepperProps, StepperStep } from "./components/Stepper.types";
export { CommandBox } from "./components/CommandBox";
export type { CommandBoxProps } from "./components/CommandBox.types";
export { Modal } from "./components/Modal";
export type { ModalProps } from "./components/Modal.types";
export { ToastProvider, useToast } from "./components/Toast";
export type { ToastApi, ToastItem, ToastTone, ToastProviderProps } from "./components/Toast.types";
export { NavBar } from "./components/NavBar";
export type { NavBarProps } from "./components/NavBar.types";
export { ThemeToggle } from "./components/ThemeToggle";
export type { ThemeToggleProps } from "./components/ThemeToggle";

// Utilities
export { cn } from "./lib/cn";
export {
  LIV_THEME_STORAGE_KEY,
  LIV_THEMES,
  applyLivTheme,
  readLivTheme,
} from "./lib/theme-classes";
export type { LivTheme } from "./lib/theme-classes";
```

(Note on `ThemeToggleProps`: it's declared inline in ThemeToggle.tsx — re-export the type from that file.)

Run the full sweep: typecheck → build:lib → test (all suites). Verify `dist/index.d.ts` contains all 11 component names + Toast hooks/provider + theme utilities.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit test -- src/components/NavBar.test.tsx &amp;&amp; pnpm --filter @livinity/ui-kit typecheck &amp;&amp; pnpm --filter @livinity/ui-kit build:lib &amp;&amp; pnpm --filter @livinity/ui-kit test &amp;&amp; node -e "const dts=require('fs').readFileSync('packages/ui-kit/dist/index.d.ts','utf8'); ['Stepper','CommandBox','Modal','ToastProvider','useToast','NavBar','ThemeToggle'].forEach(s=&gt;{if(!dts.includes(s))throw new Error('d.ts missing '+s);}); console.log('composite exports ok');"</automated>
  </verify>
  <done>
- NavBar shipped + tested (5 assertions pass)
- `src/index.ts` exports all 11 components (5 atoms + 6 composites) + useToast/ToastProvider + cn + theme utilities + all `*Props` types
- `dist/index.d.ts` contains every component + hook name
- Full test run shows ≥30 component assertions + 5 scaffold (cumulative ≥55 with 119-02's contribution counted)
- `pnpm --filter @livinity/ui-kit typecheck` exits 0
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @livinity/ui-kit test` → all suites pass (cumulative with 119-02: scaffold 5 + Button 6 + Card 5 + Pill 3 + Input 7 + PasswordInput 4 + Stepper 5 + CommandBox 5 + ThemeToggle 4 + Modal 7 + Toast 6 + NavBar 5 = 62 assertions)
- `pnpm --filter @livinity/ui-kit build:lib` exits 0; `dist/index.d.ts` exports all 11 components + ToastProvider + useToast + 4 utility helpers
- `pnpm --filter @livinity/ui-kit storybook:build` exits 0; storybook-static contains ≥12 story groups (Scaffold + 5 atoms + 6 composites)
- Manual storybook smoke: cycle the `livTheme` toolbar across all stories — no story errors out in dark or iridescent
- ARIA: Modal has role/aria-modal/aria-labelledby; Toast region has role='region' aria-label; Stepper has role='list'/'listitem' + aria-current='step'; ThemeToggle has dynamic aria-label
</verification>

<success_criteria>
- 6 composite components shipped: Stepper, CommandBox, Modal, Toast (Provider + hook), NavBar, ThemeToggle
- Each has stories + tests; ≥30 new component assertions pass in this plan
- composites.css covers every visual class using design-tokens vars (with one documented exception for `.cmd-box` dark surface)
- `src/index.ts` is the final library API surface for v0.1.0
- D-119-DASHBOARD-HTML-IS-SOURCE: Stepper uses canonical `.step.active`/`.step.done` accents; CommandBox matches dashboard-install.html visual; ThemeToggle cycles per dashboard.html convention
- D-119-NO-CONSUMER-CHANGES: `git diff --name-only` shows ONLY `livos/packages/ui-kit/**` paths
- D-119-LIGHT-DARK-IRIDESCENT-PARITY: every story works across all 3 themes
- D-119-A11Y-FOCUS-RINGS: Modal close button + ThemeToggle + NavBar children all inherit `:focus-visible` rings from atoms.css (or composites.css adds matching rules — verify)
</success_criteria>

<output>
After completion, create `.planning/phases/119-reusable-component-library/119-03-SUMMARY.md` with:
- Composite component test counts (Stepper, CommandBox, ThemeToggle, Modal, Toast, NavBar)
- Final `dist/index.mjs` bundle size + `dist/index.d.ts` size
- ToastProvider usage example (3 lines: wrap app + useToast() + toast.success("hi"))
- Modal usage example (3 lines: useState + <Modal open onClose title>)
- A note about index.ts merge resolution if 119-02 + 119-03 ran in parallel and a final reconciliation commit was needed
- Confirmation: `git diff --name-only` lists only ui-kit paths
</output>
