---
phase: 40-per-user-claude-oauth-home-isolation
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - nexus/packages/core/src/agent.ts
  - nexus/packages/core/src/sdk-agent-runner.ts
  - nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts
autonomous: true
requirements:
  - FR-AUTH-03

must_haves:
  truths:
    - "AgentConfig.homeOverride field exists in agent.ts (NON-sacred file) — typed as `string | undefined`, JSDoc explains per-user OAuth isolation purpose."
    - "sdk-agent-runner.ts line 266 reads `this.config.homeOverride || process.env.HOME || '/root'` (was: `process.env.HOME || '/root'`). When homeOverride is undefined, behavior is byte-identical to pre-Phase-40."
    - "git diff nexus/packages/core/src/sdk-agent-runner.ts shows EXACTLY one line changed (line 266). Zero other modifications."
    - "Phase 39's sdk-agent-runner-integrity.test.ts BASELINE_SHA constant is updated to the new post-Phase-40 SHA. Test still PASSES (against the new baseline). Comment block added per D-40-11."
    - "TypeScript build (`npm run build --workspace=packages/core`) exits 0 with zero errors."
    - "Phase 39's no-authtoken-regression test STILL passes — claude.ts is untouched by this plan."
  artifacts:
    - path: "nexus/packages/core/src/agent.ts"
      provides: "AgentConfig.homeOverride optional field + JSDoc"
      contains: "homeOverride"
    - path: "nexus/packages/core/src/sdk-agent-runner.ts"
      provides: "Line 266 reads from this.config.homeOverride before falling back"
      contains: "this.config.homeOverride"
    - path: "nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts"
      provides: "BASELINE_SHA updated to new post-Phase-40 SHA + comment pointing to 40-CONTEXT.md D-40-02"
      contains: "Phase 40"
  key_links:
    - from: "AgentConfig.homeOverride (agent.ts)"
      to: "safeEnv.HOME (sdk-agent-runner.ts:266)"
      via: "this.config.homeOverride read inside run()"
      pattern: "this\\.config\\.homeOverride"
    - from: "sdk-agent-runner-integrity.test.ts BASELINE_SHA"
      to: "new sdk-agent-runner.ts SHA after edit"
      via: "git hash-object verification"
      pattern: "BASELINE_SHA"
---

<objective>
Surgical, behavior-preserving edit to the sacred file. Adds `homeOverride?: string` to `AgentConfig` (in NON-sacred `agent.ts`) and modifies ONE line in `sdk-agent-runner.ts` (line 266) so the spawned `claude` CLI subprocess can have its HOME env var redirected per-user. Zero behavior change for any caller that does not set `homeOverride` — every existing call site (api.ts:2502, daemon.ts:1149, daemon.ts:3700) continues exactly as before.

Per D-40-01: "Sacred = behavior-preserving, NOT byte-identical." Phase 39's byte-identical guard is EXPLICITLY relaxed for this surgical change. The integrity test is RE-PINNED (kept alive, just with a new baseline SHA) so future phases (41+) protect from drift starting from this new baseline.

Purpose: Enables Phase 40 Plan 03's per-user `claude login` invocation to land OAuth credentials in `/opt/livos/data/users/&lt;user_id&gt;/.claude/.credentials.json` instead of `/root/.claude/.credentials.json`. Without this 1-line change, per-user OAuth credentials would be impossible to isolate.

Output: Sacred file with single-line surgical edit, AgentConfig with new optional field, integrity test re-pinned and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-per-user-claude-oauth-home-isolation/40-CONTEXT.md
@.planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md
@.planning/phases/40-per-user-claude-oauth-home-isolation/40-01-SUMMARY.md
@.planning/phases/39-risk-fix-close-oauth-fallback/39-SUMMARY.md
@nexus/packages/core/src/sdk-agent-runner.ts
@nexus/packages/core/src/agent.ts
@nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts

<interfaces>
<!-- Pre-extracted contracts to eliminate any ambiguity. -->

The single sacred-file change (line 266):
```typescript
// BEFORE:
const safeEnv: Record<string, string | undefined> = {
  HOME: process.env.HOME || '/root',
  PATH: process.env.PATH || '/usr/local/bin:/usr/bin:/bin',
  // ...
};

// AFTER:
const safeEnv: Record<string, string | undefined> = {
  HOME: this.config.homeOverride || process.env.HOME || '/root',
  PATH: process.env.PATH || '/usr/local/bin:/usr/bin:/bin',
  // ...
};
```

The agent.ts addition (NON-sacred — add inside `interface AgentConfig`, position after `nexusConfig?` block, before `maxTurns?` for cleanest grouping):
```typescript
/**
 * Override HOME env for the spawned claude CLI subprocess.
 * Used for per-user OAuth credential isolation in multi-user mode
 * (each user's `.claude/.credentials.json` lives in their own synthetic dir
 * under /opt/livos/data/users/&lt;user_id&gt;/.claude/).
 *
 * When undefined: falls back to process.env.HOME || '/root' (pre-Phase-40 behavior).
 *
 * Introduced in v29.3 Phase 40 (FR-AUTH-03). See .planning/phases/40-.../40-CONTEXT.md D-40-02.
 */
homeOverride?: string;
```

The integrity test re-pin (D-40-11):
```typescript
// BEFORE:
const BASELINE_SHA = '2b3b005bf1594821be6353268ffbbdddea5f9a3a';

// AFTER (NEW_SHA captured AFTER Task 2's edits):
// BASELINE updated 2026-04-30 by v29.3 Phase 40 (homeOverride addition).
// See .planning/phases/40-per-user-claude-oauth-home-isolation/40-CONTEXT.md D-40-02.
const BASELINE_SHA = '<NEW_SHA_FROM_TASK_3>';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-edit sacred file SHA verification</name>
  <files>(read-only verification)</files>
  <action>
1. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts`. MUST equal `2b3b005bf1594821be6353268ffbbdddea5f9a3a` (Phase 39 baseline). If it does NOT match → STOP. Some other plan has touched the sacred file out-of-band; halt Phase 40 and ask for replanning.

2. Run `grep -n "HOME: process.env.HOME" nexus/packages/core/src/sdk-agent-runner.ts`. Confirm exactly ONE match at line 266 (per AUDIT.md Section 1). If not at line 266, use the actual line number from grep — but the line content MUST match `  HOME: process.env.HOME || '/root',` exactly (3 spaces, comma at end).

3. Run `git status nexus/packages/core/src/sdk-agent-runner.ts`. MUST be clean (no uncommitted changes).

If any of (1)/(2)/(3) fails: STOP and report. Do not proceed to Task 2.
  </action>
  <verify>
    <automated>cd nexus/packages/core &amp;&amp; test "$(git hash-object src/sdk-agent-runner.ts)" = "2b3b005bf1594821be6353268ffbbdddea5f9a3a" &amp;&amp; grep -c "HOME: process.env.HOME" src/sdk-agent-runner.ts | grep -q "^1$"</automated>
  </verify>
  <done>Sacred file confirmed at Phase 39 baseline SHA, exactly one HOME assignment line found, working tree clean for this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add homeOverride to AgentConfig + edit line 266 in sacred file</name>
  <files>nexus/packages/core/src/agent.ts, nexus/packages/core/src/sdk-agent-runner.ts</files>
  <behavior>
    - When `agentConfig.homeOverride === '/opt/livos/data/users/abc/.claude'` is passed to SdkAgentRunner constructor: `safeEnv.HOME` becomes `/opt/livos/data/users/abc/.claude`.
    - When `agentConfig.homeOverride` is undefined: `safeEnv.HOME` falls back to `process.env.HOME || '/root'` — byte-identical pre-Phase-40 behavior.
    - When `agentConfig.homeOverride === ''` (empty string): falls through to `process.env.HOME` (because `'' || x` short-circuits to `x`). This is intentional and matches the pre-Phase-40 fallback semantics.
    - TypeScript type check: `agentConfig.homeOverride` is `string | undefined`. Required to be omitted in all existing call sites without a TypeScript error (because optional).
  </behavior>
  <action>
**Edit 1 — agent.ts (NON-sacred, add the field):**

Use the Edit tool to add the `homeOverride` field to `interface AgentConfig`. Position: after the `nexusConfig?: NexusConfig;` line and before `maxTurns?: number;`. Add the JSDoc comment block verbatim from the `<interfaces>` section above, then the `homeOverride?: string;` line.

**Edit 2 — sacred file (line 266 ONLY):**

Use the Edit tool with this exact `old_string` and `new_string`:

```
old_string:    const safeEnv: Record<string, string | undefined> = {
      HOME: process.env.HOME || '/root',

new_string:    const safeEnv: Record<string, string | undefined> = {
      HOME: this.config.homeOverride || process.env.HOME || '/root',
```

(Preserve the exact 4-space indent and trailing comma. The `\n      HOME` boundary ensures the Edit tool matches uniquely.)

**Critical: do not touch any other line in sdk-agent-runner.ts.** Not the JSDoc at the top, not the comment above the safeEnv block, not the comment at line 270, not whitespace anywhere. Edit ONLY the HOME assignment line.

After both edits:
1. `cd nexus/packages/core &amp;&amp; npm run build --workspace=packages/core` (run from nexus/ root via cwd) OR `cd nexus &amp;&amp; npm run build --workspace=packages/core` — must exit 0 with zero TypeScript errors.
2. `git diff nexus/packages/core/src/sdk-agent-runner.ts | grep "^[+-]" | grep -v "^+++" | grep -v "^---"` — count the +/- lines. MUST be exactly 2 (1 deletion of old HOME line, 1 addition of new HOME line). If more than 2 → ABORT and run `git checkout -- nexus/packages/core/src/sdk-agent-runner.ts` then re-edit more carefully.
  </action>
  <verify>
    <automated>cd nexus &amp;&amp; npm run build --workspace=packages/core 2>&amp;1 | tail -5 &amp;&amp; cd .. &amp;&amp; test "$(git diff nexus/packages/core/src/sdk-agent-runner.ts | grep -c '^[+-][^+-]')" = "2" &amp;&amp; grep -q "this.config.homeOverride" nexus/packages/core/src/sdk-agent-runner.ts &amp;&amp; grep -q "homeOverride" nexus/packages/core/src/agent.ts</automated>
  </verify>
  <done>agent.ts has new homeOverride field with JSDoc; sdk-agent-runner.ts line 266 reads from this.config.homeOverride; git diff on sacred file shows exactly 2 +/- lines (1 line changed); TypeScript build exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Capture new sacred file SHA + re-pin integrity test</name>
  <files>nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts</files>
  <action>
1. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and capture the output (the NEW_SHA — should be different from `2b3b005bf...`).

2. Use the Edit tool on `sdk-agent-runner-integrity.test.ts`:

```
old_string:// Baseline SHA recorded in .planning/phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md Section 5.
// Computed via: git hash-object nexus/packages/core/src/sdk-agent-runner.ts
const BASELINE_SHA = '2b3b005bf1594821be6353268ffbbdddea5f9a3a';

new_string:// Baseline SHA originally recorded in .planning/phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md Section 5.
// BASELINE updated 2026-04-30 by v29.3 Phase 40 (homeOverride addition for per-user OAuth isolation).
// See .planning/phases/40-per-user-claude-oauth-home-isolation/40-CONTEXT.md D-40-02 / D-40-11.
// Computed via: git hash-object nexus/packages/core/src/sdk-agent-runner.ts
const BASELINE_SHA = '<NEW_SHA_FROM_STEP_1>';
```

(Substitute `<NEW_SHA_FROM_STEP_1>` with the actual hash captured in step 1.)

3. Run `cd nexus/packages/core && npx tsx src/providers/sdk-agent-runner-integrity.test.ts`. MUST print `PASS: sdk-agent-runner.ts integrity verified (SHA: <NEW_SHA>)` and exit 0.

4. Run `cd nexus/packages/core && npm run test:phase39`. MUST pass all 5 assertions including the integrity test (now against the new baseline).

5. Verify no other source file changes: `git diff --stat nexus/packages/core/` should show exactly 3 files modified: `agent.ts`, `sdk-agent-runner.ts`, `providers/sdk-agent-runner-integrity.test.ts`.
  </action>
  <verify>
    <automated>cd nexus/packages/core &amp;&amp; npx tsx src/providers/sdk-agent-runner-integrity.test.ts 2>&amp;1 | grep -q "PASS" &amp;&amp; npm run test:phase39 2>&amp;1 | tail -10 | grep -q "passed"</automated>
  </verify>
  <done>BASELINE_SHA constant updated to new post-Phase-40 SHA with documenting comment; integrity test passes against new baseline; full Phase 39 test suite still passes (5/5).</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit</name>
  <files>(git only)</files>
  <action>
Stage the 3 modified files explicitly and commit:

```
git add nexus/packages/core/src/agent.ts \
        nexus/packages/core/src/sdk-agent-runner.ts \
        nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts

git commit -m "$(cat <&lt;'EOF'
feat(40-02): add homeOverride to SdkAgentRunner for per-user OAuth isolation (FR-AUTH-03)

Surgical 1-line edit to sacred sdk-agent-runner.ts:
- agent.ts: add AgentConfig.homeOverride?: string optional field + JSDoc
- sdk-agent-runner.ts: line 266 reads this.config.homeOverride before HOME fallback
- providers/sdk-agent-runner-integrity.test.ts: re-pin BASELINE_SHA to post-Phase-40 SHA

Per D-40-01: sacred = behavior-preserving, not byte-identical (explicitly relaxed
for this surgical change). All existing call sites (api.ts:2502, daemon.ts:1149,
daemon.ts:3700) continue identically — homeOverride is opt-in.

Phase 39 sacred baseline (2b3b005bf...) re-pinned to new SHA in integrity test
per D-40-11. Future phases (41+) protect from drift starting from this new baseline.

git diff sdk-agent-runner.ts: exactly 2 +/- lines (1 line changed). Build clean.
EOF
)"
```

Verify the commit landed: `git log -1 --name-only` should show exactly the 3 files above.
  </action>
  <verify>
    <automated>git log -1 --name-only --format="%s" | head -5 | grep -q "40-02" &amp;&amp; git log -1 --name-only --format="" | grep -q "sdk-agent-runner.ts" &amp;&amp; git log -1 --name-only --format="" | grep -q "agent.ts" &amp;&amp; git log -1 --name-only --format="" | grep -q "sdk-agent-runner-integrity.test.ts"</automated>
  </verify>
  <done>Single commit on master containing exactly 3 files: agent.ts, sdk-agent-runner.ts, sdk-agent-runner-integrity.test.ts. Commit message references FR-AUTH-03 + 40-02.</done>
</task>

</tasks>

<verification>
- `git diff HEAD~1 HEAD nexus/packages/core/src/sdk-agent-runner.ts | grep "^[+-]" | grep -v "^+++" | grep -v "^---" | wc -l` = 2 (one minus, one plus — exactly one line changed).
- `grep "this.config.homeOverride" nexus/packages/core/src/sdk-agent-runner.ts` returns the line at the new position of (formerly) line 266.
- `grep "homeOverride" nexus/packages/core/src/agent.ts` shows the new field declaration.
- `npm run test:phase39` from nexus/packages/core/ exits 0 (all 5 assertions pass — integrity test passes against new baseline).
- `npm run build --workspace=packages/core` from nexus/ exits 0 with zero TypeScript errors.
- All callers of `new SdkAgentRunner(agentConfig)` (api.ts:2502, daemon.ts:1149, daemon.ts:3700) compile unchanged — homeOverride is optional.
</verification>

<success_criteria>
- ROADMAP success criterion 3 (HOME env var per-user) is structurally enabled. Plan 03 will wire actual user_id resolution; this plan provides the parameter mechanism.
- D-40-02 fully satisfied: AgentConfig has homeOverride, line 266 reads from it.
- D-40-11 / D-40-15 fully satisfied: integrity test BASELINE_SHA re-pinned with documenting comment.
- Phase 39 regression preserved: claude.ts untouched, no-authtoken-regression test still passes.
- Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/40-per-user-claude-oauth-home-isolation/40-02-SUMMARY.md` summarizing:
- Pre-edit SHA: `2b3b005bf1594821be6353268ffbbdddea5f9a3a` (Phase 39 baseline)
- Post-edit SHA: `<actual_new_sha>` (record the value)
- Files modified: 3 (list them)
- Lines changed in sacred file: exactly 1 (line 266)
- Tests run: `npm run test:phase39` — 5/5 passed (integrity now against new baseline)
- Build: clean
- Commit SHA: `<git rev-parse HEAD>`
- Plan 03 unblocked: AgentConfig.homeOverride is now available; needs user_id resolution + per-user dir creation to actually pass the override.
</output>
