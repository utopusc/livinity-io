---
phase: 66-liv-design-system-v1
plan: 03
type: execute
wave: 2
depends_on: [66-01, 66-02]
files_modified:
  - livos/packages/ui/src/shadcn-components/ui/button.tsx
  - livos/packages/ui/src/shadcn-components/ui/badge.tsx
  - livos/packages/ui/src/components/ui/card.tsx
  - livos/packages/ui/src/shadcn-components/ui/slider.tsx
autonomous: true
requirements: [DESIGN-07]
tags: [design-system, shadcn, cva, variants]
user_setup: []

must_haves:
  truths:
    - "`livos/packages/ui/src/shadcn-components/ui/button.tsx` `buttonVariants.variants.variant` map contains a `liv-primary` entry with cyan-accent + glow-on-hover behavior referencing `var(--liv-accent-cyan)` AND `.liv-glow-cyan` (from Plan 66-01)"
    - "`livos/packages/ui/src/shadcn-components/ui/badge.tsx` `badgeVariants.variants.variant` map contains a `liv-status-running` entry with pulsing-dot + cyan accent"
    - "`livos/packages/ui/src/components/ui/card.tsx` adds a `liv-elevated` variant (via cva extension or className branch) that applies `.liv-glass` + `var(--liv-border-subtle)` border per D-15"
    - "A new file `livos/packages/ui/src/shadcn-components/ui/slider.tsx` exists with a Radix-based shadcn Slider primitive AND a `liv-slider` variant/className providing cyan track per D-15"
    - "All four variant additions reference Liv tokens (CSS variables `--liv-accent-cyan`, `--liv-accent-amber`, `--liv-accent-violet`, OR utility classes `.liv-glass`, `.liv-glow-cyan`) from Plan 66-01 — confirms cross-plan token consumption works"
    - "ZERO existing variants (default, primary, secondary, destructive, ghost, outline) are renamed, removed, or behaviorally changed (D-14: extend, do not fork; preserve all existing surfaces)"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b`"
    - "Build is green: `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0; `pnpm --filter ui typecheck` exits 0"
    - "No file under `livos/packages/ui/src/components/motion-primitives/` is modified (D-09)"
  artifacts:
    - path: "livos/packages/ui/src/shadcn-components/ui/button.tsx"
      provides: "Button component with new liv-primary variant"
      contains: "liv-primary"
    - path: "livos/packages/ui/src/shadcn-components/ui/badge.tsx"
      provides: "Badge component with new liv-status-running variant"
      contains: "liv-status-running"
    - path: "livos/packages/ui/src/components/ui/card.tsx"
      provides: "Card component with new liv-elevated variant"
      contains: "liv-elevated"
    - path: "livos/packages/ui/src/shadcn-components/ui/slider.tsx"
      provides: "Slider primitive (NEW file) with liv-slider variant"
      contains: "liv-slider"
      min_lines: 30
  key_links:
    - from: "livos/packages/ui/src/shadcn-components/ui/button.tsx liv-primary variant"
      to: "Plan 66-01 tokens (--liv-accent-cyan, .liv-glow-cyan)"
      via: "className strings using arbitrary-value Tailwind syntax (`bg-[var(--liv-accent-cyan)]`) or direct utility class reference (`liv-glow-cyan`)"
      pattern: "liv-accent-cyan|liv-glow-cyan"
    - from: "livos/packages/ui/src/components/ui/card.tsx liv-elevated variant"
      to: "Plan 66-01 .liv-glass utility"
      via: "className adds `liv-glass` directly"
      pattern: "liv-glass"
    - from: "livos/packages/ui/src/shadcn-components/ui/slider.tsx"
      to: "@radix-ui/react-slider package"
      via: "package install + named imports (Radix slider is standard shadcn primitive)"
      pattern: "@radix-ui/react-slider"
---

<objective>
Add four `liv-*` variants to the existing shadcn primitives so downstream phases (P68 side panel, P69 tool views, P70 composer, P75 reasoning cards) can render Liv-branded buttons, badges, cards, and sliders without ad-hoc styling. Per CONTEXT.md D-14, extend the existing `cva` variant maps; do NOT fork into new component files (one exception: `slider.tsx` does not yet exist in this codebase, so it is created fresh as a standard shadcn-pattern Radix slider with the `liv-slider` variant).

Purpose: Plan 66-01 created the tokens + utility classes; this plan wires them into the four shadcn surfaces v31-DRAFT line 250-253 names. Without this plan, P68-P75 must hand-write `style={{...}}` blocks every time they want a cyan-accent button. Variants land here once, consumers compose freely.

Critical path note: `card.tsx` lives at `livos/packages/ui/src/components/ui/card.tsx` (NOT under `shadcn-components/ui/` like button/badge — verified via codebase scan). The CONTEXT.md D-14 mention of `shadcn-components/ui/{button,card,badge,slider}.tsx` was approximate; this plan uses the actual paths. `slider.tsx` does NOT exist yet — this plan creates it as a standard shadcn Radix primitive.

Output:
- MODIFY `livos/packages/ui/src/shadcn-components/ui/button.tsx` — add `liv-primary` variant
- MODIFY `livos/packages/ui/src/shadcn-components/ui/badge.tsx` — add `liv-status-running` variant
- MODIFY `livos/packages/ui/src/components/ui/card.tsx` — add `liv-elevated` variant (or className branch)
- NEW `livos/packages/ui/src/shadcn-components/ui/slider.tsx` — Radix slider primitive + `liv-slider` variant
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-liv-design-system-v1/66-CONTEXT.md
@.planning/v31-DRAFT.md
@.planning/phases/66-liv-design-system-v1/66-01-SUMMARY.md

# Existing files this plan modifies
@livos/packages/ui/src/shadcn-components/ui/button.tsx
@livos/packages/ui/src/shadcn-components/ui/badge.tsx
@livos/packages/ui/src/components/ui/card.tsx
@livos/packages/ui/package.json

<scope_guard>
**HARD RULES:**
- D-14: Extend existing variants via cva pattern. Do NOT rename existing variants. Do NOT remove existing variants. Do NOT change existing variant behavior.
- D-15: Variant names exactly match v31-DRAFT 250-253: `liv-primary`, `liv-elevated`, `liv-status-running`, `liv-slider`. NO additional `liv-*` variants in this phase.
- D-09: Do NOT touch `livos/packages/ui/src/components/motion-primitives/`.
- D-NO-BYOK: Do NOT touch any broker code (`livos/packages/livinityd/source/modules/livinity-broker/`).
- Do NOT touch `nexus/packages/core/src/sdk-agent-runner.ts` (sacred file SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`).
- Do NOT migrate any consumer of Button/Badge/Card/Slider to the new variants — that's P68-P70.
- Do NOT add a Storybook entry (D-16).
- Do NOT add light-theme variants (D-03).
</scope_guard>
</context>

<interfaces>
<!-- Plan 66-01 deliverables this plan consumes -->
From `livos/packages/ui/src/styles/liv-tokens.css`:
- CSS variables on `:root`: `--liv-accent-cyan` (#4dd0e1), `--liv-accent-amber` (#ffbd38), `--liv-accent-violet` (#a78bfa), `--liv-bg-glass`, `--liv-border-subtle`, `--liv-dur-fast` (200ms), `--liv-ease-out` (cubic-bezier(0.16,1,0.3,1))
- Utility classes: `.liv-glass`, `.liv-grain`, `.liv-glow-amber`, `.liv-glow-cyan`, `.liv-glow-violet`

<!-- Existing button.tsx structure (read first; preserve) -->
```tsx
const buttonVariants = cva(
  '...base classes...',
  {
    variants: {
      variant: {
        default: '...',
        primary: '...',  // existing — DO NOT CHANGE
        secondary: '...',
        destructive: '...',
        ghost: '...',
        // NEW: liv-primary added here
      },
      size: { ... },
      text: { ... },
    },
    compoundVariants: [...],
    defaultVariants: { variant: 'default', size: 'default' },
  },
)
```

<!-- Existing badge.tsx structure -->
```tsx
const badgeVariants = cva(
  'inline-flex items-center rounded-full border ...',
  {
    variants: {
      variant: {
        default: '...',
        primary: '...',
        destructive: '...',
        outline: '...',
        // NEW: liv-status-running added here
      },
    },
  },
)
```

<!-- card.tsx pattern: read the existing file first; if it uses cva, extend variants. If it does NOT use cva (just function components with className passthrough), add a `liv-elevated` className branch via a `data-liv-variant` attribute or a className composition helper. Either approach is acceptable; pick whichever produces the smallest diff. -->

<!-- slider.tsx — NEW FILE following shadcn standard pattern -->
```tsx
'use client'
import * as React from 'react'
import * as SliderPrimitive from '@radix-ui/react-slider'
import {cva, type VariantProps} from 'class-variance-authority'
import {cn} from '@/shadcn-lib/utils'

const sliderVariants = cva('relative flex w-full touch-none select-none items-center', {
  variants: {
    variant: {
      default: '[&_[data-slot=track]]:bg-surface-2 [&_[data-slot=range]]:bg-brand',
      'liv-slider': '[&_[data-slot=track]]:bg-[color:var(--liv-bg-elevated)] [&_[data-slot=range]]:bg-[color:var(--liv-accent-cyan)] [&_[data-slot=thumb]]:bg-[color:var(--liv-accent-cyan)] [&_[data-slot=thumb]]:shadow-[0_0_8px_rgba(77,208,225,0.4)]',
    },
  },
  defaultVariants: {variant: 'default'},
})

const Slider = React.forwardRef<...>((...) => (
  <SliderPrimitive.Root ref={ref} className={cn(sliderVariants({variant}), className)} {...}>
    <SliderPrimitive.Track data-slot='track' className='...' />
    <SliderPrimitive.Range data-slot='range' className='...' />
    <SliderPrimitive.Thumb data-slot='thumb' className='...' />
  </SliderPrimitive.Root>
))
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add liv-primary (Button) and liv-status-running (Badge) variants</name>
  <files>livos/packages/ui/src/shadcn-components/ui/button.tsx, livos/packages/ui/src/shadcn-components/ui/badge.tsx</files>
  <read_first>
    - livos/packages/ui/src/shadcn-components/ui/button.tsx (entire file — locate `buttonVariants` cva block)
    - livos/packages/ui/src/shadcn-components/ui/badge.tsx (entire file — locate `badgeVariants` cva block)
    - livos/packages/ui/src/styles/liv-tokens.css (Plan 66-01 output — confirm token names exist)
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-14, D-15)
    - .planning/v31-DRAFT.md lines 250-253 (variant names + behaviors)
  </read_first>
  <behavior>
    - `<Button variant="liv-primary">Continue</Button>` renders with cyan background (`var(--liv-accent-cyan)`), white text, and on hover gains the `.liv-glow-cyan` box-shadow (24px outer cyan glow). Transition uses `--liv-dur-fast` (200ms) and `--liv-ease-out` curve.
    - `<Badge variant="liv-status-running">Running</Badge>` renders with cyan border + cyan text + a leading pulsing dot (8px) that animates opacity 1.0 → 0.4 → 1.0 over ~1.5s loop.
    - Existing variants (default, primary, secondary, destructive, ghost on Button; default, primary, destructive, outline on Badge) are unchanged — same className strings, same visual output.
  </behavior>
  <action>
1. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
2. Confirm Plan 66-01's tokens are in place: `grep -q '\-\-liv-accent-cyan' livos/packages/ui/src/styles/liv-tokens.css` and `grep -q '.liv-glow-cyan' livos/packages/ui/src/styles/liv-tokens.css` — both must succeed. If not, stop and fail (Plan 66-01 incomplete).
3. Edit `livos/packages/ui/src/shadcn-components/ui/button.tsx`:
   - Locate the `buttonVariants` cva call and the `variants.variant` object.
   - ADD a new key `'liv-primary'` after the existing `ghost` entry. Value:
     ```ts
     'liv-primary': 'bg-[color:var(--liv-accent-cyan)] text-[#050b14] hover:liv-glow-cyan ring-[color:var(--liv-accent-cyan)]/40 active:bg-[color:#3bbac9] data-[state=open]:bg-[color:#3bbac9] hover:scale-[1.02] active:scale-[0.98] transition-all duration-[var(--liv-dur-fast)]',
     ```
   - DO NOT modify any existing variant value, the `size` map, the `text` map, the `compoundVariants` array, or `defaultVariants`.
   - Tailwind 3.4 supports arbitrary `[color:var(--liv-accent-cyan)]` syntax — confirm by reading the existing file for similar arbitrary-value patterns.
   - For the `hover:liv-glow-cyan` part — Tailwind cannot prefix a non-Tailwind utility class via `hover:`. Replace with: `hover:shadow-[0_0_24px_rgba(77,208,225,0.2),inset_0_1px_0_rgba(77,208,225,0.1)]` (the literal expansion of `.liv-glow-cyan`). This avoids needing a Tailwind plugin to register `liv-glow-cyan` as a utility.
4. Edit `livos/packages/ui/src/shadcn-components/ui/badge.tsx`:
   - Locate the `badgeVariants` cva call and the `variants.variant` object.
   - ADD a new key `'liv-status-running'` after `outline`. Value:
     ```ts
     'liv-status-running': 'border-[color:var(--liv-accent-cyan)]/40 text-[color:var(--liv-accent-cyan)] bg-[color:var(--liv-accent-cyan)]/10 before:content-[""] before:inline-block before:w-2 before:h-2 before:rounded-full before:bg-[color:var(--liv-accent-cyan)] before:mr-1.5 before:animate-pulse',
     ```
   - The `before:` pseudo-element produces the leading pulsing dot via Tailwind's `animate-pulse` (already in tailwindcss-animate plugin per package.json).
   - DO NOT modify any other variants or props.
5. Run `pnpm --filter ui typecheck` — must exit 0.
6. Run `pnpm --filter @livos/config build && pnpm --filter ui build` — must exit 0.
7. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
8. Verify motion-primitives untouched: `git status livos/packages/ui/src/components/motion-primitives/` shows no changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const btn=fs.readFileSync('livos/packages/ui/src/shadcn-components/ui/button.tsx','utf8'); const bdg=fs.readFileSync('livos/packages/ui/src/shadcn-components/ui/badge.tsx','utf8'); if(!btn.includes(\"'liv-primary'\")){console.error('Button missing liv-primary'); process.exit(1)} if(!btn.includes('--liv-accent-cyan')){console.error('Button liv-primary missing token reference'); process.exit(1)} const existingBtnVariants=['default:','primary:','secondary:','destructive:','ghost:']; for(const v of existingBtnVariants){if(!btn.includes(v)){console.error('Button missing existing variant:',v); process.exit(1)}} if(!bdg.includes(\"'liv-status-running'\")){console.error('Badge missing liv-status-running'); process.exit(1)} if(!bdg.includes('animate-pulse')){console.error('Badge liv-status-running missing pulse animation'); process.exit(1)} const existingBdgVariants=['default:','primary:','destructive:','outline:']; for(const v of existingBdgVariants){if(!bdg.includes(v)){console.error('Badge missing existing variant:',v); process.exit(1)}} console.log('Variants OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter ui typecheck</automated>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `button.tsx` `buttonVariants.variants.variant` contains `'liv-primary'` key (greppable).
    - `button.tsx` liv-primary variant string references `--liv-accent-cyan` token.
    - `button.tsx` STILL contains all 5 original variants: default, primary, secondary, destructive, ghost (none renamed/removed).
    - `badge.tsx` `badgeVariants.variants.variant` contains `'liv-status-running'` key.
    - `badge.tsx` liv-status-running variant uses `before:` pseudo-element + `animate-pulse` for the dot.
    - `badge.tsx` STILL contains all 4 original variants: default, primary, destructive, outline.
    - `pnpm --filter ui typecheck` exits 0.
    - `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes.
  </acceptance_criteria>
  <done>liv-primary added to Button; liv-status-running added to Badge; existing variants preserved; build+typecheck green; sacred SHA unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add liv-elevated (Card) variant + create slider.tsx with liv-slider variant</name>
  <files>livos/packages/ui/src/components/ui/card.tsx, livos/packages/ui/src/shadcn-components/ui/slider.tsx, livos/packages/ui/package.json</files>
  <read_first>
    - livos/packages/ui/src/components/ui/card.tsx (entire file — determine if it uses cva or plain function-component pattern)
    - livos/packages/ui/src/shadcn-components/ui/button.tsx (just-modified; reference the cva pattern for slider)
    - livos/packages/ui/package.json (check whether `@radix-ui/react-slider` is already a dep; if not, this task adds it)
    - livos/packages/ui/src/styles/liv-tokens.css (Plan 66-01 — confirm `--liv-accent-cyan`, `--liv-bg-glass`, `--liv-border-subtle`, `.liv-glass` exist)
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-14, D-15)
  </read_first>
  <behavior>
    - `<Card variant="liv-elevated">...</Card>` (or the equivalent prop API the existing Card supports) renders with `.liv-glass` (backdrop blur + glass background) AND a 1px `var(--liv-border-subtle)` border, producing a glassmorphic elevated card. Existing default Card rendering is unchanged.
    - `<Slider variant="liv-slider" value={[50]} max={100} />` (NEW component) renders a Radix slider with cyan range + cyan thumb + soft cyan glow on the thumb. Default variant renders with the existing brand color (no change for callers using `variant="default"`).
  </behavior>
  <action>
1. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
2. Read `livos/packages/ui/src/components/ui/card.tsx` to determine its pattern. Two cases:
   - **Case A — Card uses cva:** Add a `liv-elevated` variant to the existing cva `variants.variant` map. Value: `'liv-glass border border-[color:var(--liv-border-subtle)] backdrop-blur-[12px] saturate-[1.2]'` — this composes the `.liv-glass` utility (from Plan 66-01) PLUS the explicit border. Add `defaultVariants: {variant: 'default'}` if missing.
   - **Case B — Card is plain components (CardHeader, CardContent, etc. with className passthrough):** Add a `cva` import + a small variant helper, OR simpler: accept a `variant` prop on `Card` that conditionally appends `'liv-glass border border-[color:var(--liv-border-subtle)]'` when `variant === 'liv-elevated'`. Keep changes minimal — the goal is a `liv-elevated` switch, not a refactor.
   - Pick whichever case the file reveals. If unclear, prefer Case B (smaller diff).
3. Confirm `@radix-ui/react-slider` dependency status:
   - `node -e "const p=require('./livos/packages/ui/package.json'); console.log(p.dependencies['@radix-ui/react-slider'] || p.devDependencies['@radix-ui/react-slider'] || 'MISSING')"`.
   - If `MISSING`: run `pnpm --filter ui add @radix-ui/react-slider@^1.2.0` from the workspace root. This is the standard shadcn slider primitive; adding a Radix sub-package is in-policy (the package.json already has 15+ `@radix-ui/*` packages — D-NO-NEW-DEPS does NOT block sibling Radix primitives, only entirely new vendors).
4. Write `livos/packages/ui/src/shadcn-components/ui/slider.tsx`:
   - Standard shadcn pattern with cva variants (see `<interfaces>` block above for exact shape).
   - Two variants: `default` (existing brand color via `bg-brand` etc. — match the codebase's existing surface tokens) and `liv-slider` (cyan track + cyan thumb + cyan glow per D-15 + v31-DRAFT line 253).
   - Component uses `React.forwardRef`, accepts all `SliderPrimitive.Root` props plus `VariantProps<typeof sliderVariants>`.
   - Exports both `Slider` (the component) and `sliderVariants` (the cva map).
5. Run `pnpm --filter ui typecheck` — must exit 0.
6. Run `pnpm --filter @livos/config build && pnpm --filter ui build` — must exit 0.
7. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
8. Verify motion-primitives untouched: `git status livos/packages/ui/src/components/motion-primitives/` shows no changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const card=fs.readFileSync('livos/packages/ui/src/components/ui/card.tsx','utf8'); if(!card.includes('liv-elevated')){console.error('Card missing liv-elevated variant'); process.exit(1)} if(!/liv-glass|--liv-bg-glass/.test(card)){console.error('Card liv-elevated does not reference glass token'); process.exit(1)} const sl=fs.readFileSync('livos/packages/ui/src/shadcn-components/ui/slider.tsx','utf8'); if(!sl.includes('@radix-ui/react-slider')){console.error('slider.tsx missing Radix import'); process.exit(1)} if(!sl.includes(\"'liv-slider'\")){console.error('slider.tsx missing liv-slider variant'); process.exit(1)} if(!sl.includes('--liv-accent-cyan')){console.error('liv-slider missing cyan token'); process.exit(1)} console.log('Card+Slider OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter ui typecheck</automated>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `livos/packages/ui/src/components/ui/card.tsx` contains the string `liv-elevated` (greppable).
    - `card.tsx` references either `liv-glass` or `--liv-bg-glass` (token consumption from Plan 66-01).
    - `card.tsx`: existing CardHeader/CardContent/CardFooter/CardTitle/CardDescription exports (or whatever the existing surface is) are preserved — no exports removed.
    - `livos/packages/ui/src/shadcn-components/ui/slider.tsx` exists, imports from `@radix-ui/react-slider`.
    - `slider.tsx` defines a `liv-slider` variant referencing `--liv-accent-cyan`.
    - `slider.tsx` exports a `Slider` component using `React.forwardRef`.
    - `livos/packages/ui/package.json` lists `@radix-ui/react-slider` as a dependency.
    - `pnpm --filter ui typecheck` exits 0.
    - `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes.
  </acceptance_criteria>
  <done>liv-elevated added to Card; slider.tsx created with Radix primitive + liv-slider variant; @radix-ui/react-slider installed if missing; build+typecheck green; sacred SHA unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New variants ← Plan 66-01 tokens | Variants reference `--liv-*` CSS variables and `.liv-*` utility classes from Plan 66-01; depends on Wave-1 completion. |
| Slider primitive ← @radix-ui/react-slider | New Radix sub-package; Radix is the established UI primitive vendor across LivOS already. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03-01 | Tampering | Existing variants accidentally renamed/removed | mitigate | Verify-step greps for all original variant names; build fails if any disappear. |
| T-66-03-02 | Tampering | New variant string typo silently breaks rendering | mitigate | Tailwind 3.4 in JIT mode fails build with unknown utility classes; cva at runtime returns the literal class string so a typo'd `bg-[color:var(--lv-accent-cyan)]` (note typo) would render as missing color, but the build still passes — visual verification happens in Plan 66-05 playground. Acceptance criteria require greppable token references. |
| T-66-03-03 | Spoofing | `before:` pseudo-element CSP block | accept | Tailwind already uses pseudo-elements throughout LivOS; no CSP issue (pseudo-elements are CSS, not inline script). |
| T-66-03-04 | DoS | Adding @radix-ui/react-slider bloats bundle | accept | ~12 KB gzipped; LivOS already ships 15+ Radix sub-packages. Tree-shaking via Vite mitigates. |
</threat_model>

<verification>
- `button.tsx` contains `liv-primary` variant referencing `--liv-accent-cyan`.
- `badge.tsx` contains `liv-status-running` variant with pulsing-dot + cyan accent.
- `card.tsx` contains `liv-elevated` variant referencing the glass token.
- `slider.tsx` (new) exists with Radix primitive + `liv-slider` variant.
- All 5 original Button variants + all 4 original Badge variants preserved.
- `pnpm --filter ui typecheck` exits 0.
- `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes (D-09).
</verification>

<success_criteria>
- ROADMAP Phase 66 success criterion #3 met: All shadcn liv-* variants render with proper accent + glow (validated visually in Plan 66-05 playground).
- DESIGN-07 (shadcn liv-* variants) implemented per v31-DRAFT lines 250-253.
- Downstream phases can use `<Button variant="liv-primary">`, `<Badge variant="liv-status-running">`, `<Card variant="liv-elevated">`, `<Slider variant="liv-slider">` directly.
- Plan 66-05 playground will render every variant for visual A/B verification.
</success_criteria>

<output>
After completion, create `.planning/phases/66-liv-design-system-v1/66-03-SUMMARY.md` summarizing: 4 variants added (3 modifications + 1 new file), token references confirmed, existing variants preserved, build+typecheck status, sacred SHA verification, and any deviations from CONTEXT.md path assumptions (e.g. card.tsx actual location).
</output>
