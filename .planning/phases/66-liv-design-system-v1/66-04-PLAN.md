---
phase: 66-liv-design-system-v1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/icons/liv-icons.ts
autonomous: true
requirements: [DESIGN-06]
tags: [design-system, icons, tabler]
user_setup: []

must_haves:
  truths:
    - "File `livos/packages/ui/src/icons/liv-icons.ts` exists and exports a typed const map `LivIcons` covering all tool categories P69 will need: browser, terminal, file, fileEdit, webSearch, webCrawl, webScrape, mcp, generic, plus aliases per v31-DRAFT line 247 (`MonitorPlay → IconScreenShare`, `Terminal → IconTerminal2`, `Globe → IconWorldSearch`)"
    - "Every icon value is imported from `@tabler/icons-react` (NOT `lucide-react` — Tabler is primary per CONTEXT.md code_context section)"
    - "`LivIcons` is exported as a typed map: `const LivIcons = { browser: IconWorld, ... } as const` with TypeScript narrowing so `LivIcons.browser` resolves to `Icon` (Tabler's component type)"
    - "File also exports a TypeScript type `LivIconKey = keyof typeof LivIcons` for downstream phases (P69) to constrain props"
    - "ZERO changes to existing ad-hoc icon imports in `livos/packages/ui/src/routes/ai-chat/`, `livos/packages/ui/src/modules/`, etc. (D-18 — consumers adopt incrementally in P68-P69)"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b`"
    - "Build is green: `pnpm --filter ui typecheck` exits 0"
  artifacts:
    - path: "livos/packages/ui/src/icons/liv-icons.ts"
      provides: "Typed Tabler-icon map for all tool categories"
      exports: ["LivIcons", "LivIconKey"]
      contains: "from '@tabler/icons-react'"
      min_lines: 40
  key_links:
    - from: "livos/packages/ui/src/icons/liv-icons.ts"
      to: "@tabler/icons-react"
      via: "named imports of IconWorld, IconTerminal2, IconFile, IconEdit, IconWorldSearch, IconSpider, IconCode, IconBolt, IconTool, IconScreenShare"
      pattern: "from\\s+['\"]@tabler/icons-react['\"]"
---

<objective>
Add a single typed icon-mapping file at `livos/packages/ui/src/icons/liv-icons.ts` that exports a `LivIcons` map covering every tool category P69 will need. Replaces ad-hoc Tabler imports scattered across consumer files. Sets up the inventory P68-P69 will consume; this phase does NOT migrate existing consumers (D-18).

Purpose: Per CONTEXT.md D-17 + v31-DRAFT line 245-247, the Liv tool views need a single source of truth mapping tool category → icon. Without this map, P69 has 9 separate ad-hoc icon imports. This is a tiny, low-risk plan that unblocks P69 and reduces icon-import churn.

Output:
- `livos/packages/ui/src/icons/liv-icons.ts` (NEW — `icons/` subdirectory does not yet exist; create it)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-liv-design-system-v1/66-CONTEXT.md
@.planning/v31-DRAFT.md

@livos/packages/ui/package.json

<scope_guard>
**HARD RULES:**
- D-18: Do NOT migrate existing ad-hoc icon imports in `routes/ai-chat/`, `modules/`, etc. The map exists; consumers adopt incrementally in P68-P69.
- Do NOT touch `nexus/packages/core/src/sdk-agent-runner.ts` (sacred file).
- Do NOT touch `livos/packages/ui/src/components/motion-primitives/` (D-09).
- Do NOT add `lucide-react` icons to the map. Tabler is primary; `lucide-react` is stale (D-17).
- Do NOT add icons for P67+ scope (computer use, browser tabs, etc. that aren't tool-category icons). Keep tight.
- Do NOT touch any broker/livinityd code.
</scope_guard>
</context>

<interfaces>
<!-- Required tool categories per v31-DRAFT line 247 + CONTEXT D-17 + P69 anticipated needs -->
<!-- Tabler icons are imported via named exports from @tabler/icons-react@3.36.1 -->

Required mappings (Suna→Tabler per v31-DRAFT line 247 + general tool categories):
- `browser` → `IconWorld` (general browser)
- `screenShare` → `IconScreenShare` (Suna `MonitorPlay` equiv per v31-DRAFT)
- `terminal` → `IconTerminal2` (Suna `Terminal` equiv per v31-DRAFT)
- `file` → `IconFile`
- `fileEdit` → `IconEdit` (or `IconFileText` — pick `IconEdit` for clarity per Claude's discretion)
- `webSearch` → `IconWorldSearch` (Suna `Globe` equiv per v31-DRAFT)
- `webCrawl` → `IconSpider`
- `webScrape` → `IconCode`
- `mcp` → `IconBolt` (MCP/extensions accent — per v31-DRAFT --liv-accent-violet)
- `generic` → `IconTool` (fallback for unknown tools)

Expected file shape:
```ts
import {
  IconWorld,
  IconScreenShare,
  IconTerminal2,
  IconFile,
  IconEdit,
  IconWorldSearch,
  IconSpider,
  IconCode,
  IconBolt,
  IconTool,
  type Icon,
} from '@tabler/icons-react'

export const LivIcons = {
  browser: IconWorld,
  screenShare: IconScreenShare,
  terminal: IconTerminal2,
  file: IconFile,
  fileEdit: IconEdit,
  webSearch: IconWorldSearch,
  webCrawl: IconSpider,
  webScrape: IconCode,
  mcp: IconBolt,
  generic: IconTool,
} as const satisfies Record<string, Icon>

export type LivIconKey = keyof typeof LivIcons
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the LivIcons typed map</name>
  <files>livos/packages/ui/src/icons/liv-icons.ts</files>
  <read_first>
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-17, D-18)
    - .planning/v31-DRAFT.md lines 245-247 (icon system migration spec)
    - livos/packages/ui/package.json (confirm @tabler/icons-react ^3.36.1 is installed)
  </read_first>
  <action>
1. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
2. Confirm `@tabler/icons-react` is in `livos/packages/ui/package.json` dependencies — version `^3.36.1` per CONTEXT.md.
3. Verify the listed Tabler icon names exist in v3.36.1 by checking `livos/packages/ui/node_modules/@tabler/icons-react/dist/esm/icons/` (or via the package's TS exports). The icons listed in `<interfaces>` are all standard Tabler exports as of v3.x — if any is missing, document the substitution in a code comment and pick the closest semantic alternative.
4. Write `livos/packages/ui/src/icons/liv-icons.ts` per the `<interfaces>` block above. Format:
   - Header comment: "Liv tool-category → Tabler icon map (Phase 66, D-17). Consumers in P68-P69 adopt incrementally per D-18."
   - Named imports of all 10 icons + the `Icon` type.
   - `export const LivIcons = {...} as const satisfies Record<string, Icon>`.
   - `export type LivIconKey = keyof typeof LivIcons;`.
5. Run `pnpm --filter ui typecheck` — must exit 0. If any icon name is wrong (e.g. Tabler renamed an icon), the typecheck will fail with a clear "has no exported member" error; substitute the correct Tabler name.
6. Re-verify sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
7. Verify NO existing icon-import files were modified: `git diff --stat livos/packages/ui/src/routes/ livos/packages/ui/src/modules/` should show zero changes (D-18).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('livos/packages/ui/src/icons/liv-icons.ts','utf8'); const required=['IconWorld','IconScreenShare','IconTerminal2','IconFile','IconEdit','IconWorldSearch','IconSpider','IconCode','IconBolt','IconTool','LivIcons','LivIconKey','@tabler/icons-react']; const missing=required.filter(r=>!s.includes(r)); if(missing.length){console.error('MISSING:',missing); process.exit(1)} const keys=['browser:','screenShare:','terminal:','file:','fileEdit:','webSearch:','webCrawl:','webScrape:','mcp:','generic:']; const missingKeys=keys.filter(k=>!s.includes(k)); if(missingKeys.length){console.error('MISSING KEYS:',missingKeys); process.exit(1)} console.log('LivIcons OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter ui typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/ui/src/icons/liv-icons.ts` exists.
    - File imports from `@tabler/icons-react` (greppable).
    - File exports `LivIcons` const with at least 10 keys: `browser`, `screenShare`, `terminal`, `file`, `fileEdit`, `webSearch`, `webCrawl`, `webScrape`, `mcp`, `generic`.
    - File exports `LivIconKey` type alias.
    - File contains zero `lucide-react` imports.
    - `pnpm --filter ui typecheck` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git diff --stat livos/packages/ui/src/routes/ livos/packages/ui/src/modules/` shows zero changes (D-18).
  </acceptance_criteria>
  <done>LivIcons map written; all 10 categories mapped; typecheck green; sacred SHA unchanged; no consumer migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LivIcons map ← @tabler/icons-react package | Trusted package already in dependency tree; only risk is icon-name drift between Tabler versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04-01 | Tampering | Tabler v3.36.1 → future version renames an icon | accept | TypeScript catches missing exports at typecheck time. Frozen as `^3.36.1` in package.json minimizes drift. |
| T-66-04-02 | DoS | Importing 10 icons bloats bundle | accept | Tree-shaking via Vite + `@tabler/icons-react`'s ESM-per-icon shape keeps bundle impact ~10 KB total. |
</threat_model>

<verification>
- File `livos/packages/ui/src/icons/liv-icons.ts` exists with all 10 mappings.
- All imports resolve to `@tabler/icons-react`.
- `pnpm --filter ui typecheck` exits 0.
- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- No edits to `livos/packages/ui/src/routes/` or `livos/packages/ui/src/modules/`.
</verification>

<success_criteria>
- DESIGN-06 (icon system) implemented per v31-DRAFT lines 245-247.
- P69 can import `import { LivIcons, LivIconKey } from '@/icons/liv-icons'` and constrain tool-category props.
- Suna→Tabler mappings (`MonitorPlay`, `Terminal`, `Globe`) addressed via `screenShare`, `terminal`, `webSearch` keys.
</success_criteria>

<output>
After completion, create `.planning/phases/66-liv-design-system-v1/66-04-SUMMARY.md` summarizing: 10 tool-category mappings, Tabler version used, typecheck status, sacred SHA verification, and a note that consumers (P68-P69) adopt incrementally per D-18.
</output>
