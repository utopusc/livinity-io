---
phase: 66-liv-design-system-v1
plan: 05
type: execute
wave: 3
depends_on: [66-01, 66-02, 66-03, 66-04]
files_modified:
  - livos/packages/ui/src/routes/playground/liv-design-system.tsx
  - livos/packages/ui/src/router.tsx
autonomous: false
requirements: [DESIGN-07]
tags: [design-system, playground, verification, visual-reference]
user_setup: []

must_haves:
  truths:
    - "Route `/playground/liv-design-system` is registered in `livos/packages/ui/src/router.tsx` and gated behind `EnsureLoggedIn` (NOT admin-only per D-21)"
    - "Component file `livos/packages/ui/src/routes/playground/liv-design-system.tsx` exists and renders 6 named sections: Color Tokens, Typography Scale, Motion Primitives (with replay buttons per D-20), Glass/Grain/Glow Utilities, shadcn liv-* Variants, Icon Map"
    - "Color Tokens section renders a swatch for every CSS variable from Plan 66-01 (18 variables) — each swatch shows the variable name AND a color/easing/duration preview"
    - "Typography Scale section renders sample text for all 6 type-scale entries: text-display-1 (48px), text-display-2 (36px), text-h1 (24px), text-body (15px), text-caption (12px), text-mono-sm (13px)"
    - "Motion Primitives section imports `FadeIn`, `GlowPulse`, `SlideInPanel`, `TypewriterCaret`, `StaggerList` from `@/components/motion` (Plan 66-02) and demos each with a replay button"
    - "Glass/Grain/Glow section renders 5 demo panels: `.liv-glass`, `.liv-grain`, `.liv-glow-amber`, `.liv-glow-cyan`, `.liv-glow-violet` (Plan 66-01 utilities)"
    - "shadcn liv-* Variants section renders `<Button variant=\"liv-primary\">`, `<Badge variant=\"liv-status-running\">`, `<Card variant=\"liv-elevated\">`, `<Slider variant=\"liv-slider\">` (Plan 66-03 outputs)"
    - "Icon Map section iterates over `LivIcons` (from Plan 66-04) and renders each icon with its key label in a grid"
    - "Playground is hidden from main navigation (no Dock entry, no menu link) — accessible only via direct URL per D-21"
    - "Build is green: `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b`"
    - "Visiting `/playground/liv-design-system` (logged in as any user) renders the page without console errors (verified in checkpoint task by Chrome DevTools MCP screenshot OR human walk per D-22)"
  artifacts:
    - path: "livos/packages/ui/src/routes/playground/liv-design-system.tsx"
      provides: "Single-page visual reference for every Liv design-system primitive"
      min_lines: 150
      contains: "Color Tokens"
    - path: "livos/packages/ui/src/router.tsx"
      provides: "Route registration for /playground/liv-design-system gated by EnsureLoggedIn"
      contains: "/playground/liv-design-system"
  key_links:
    - from: "playground component"
      to: "Plan 66-01 tokens (liv-tokens.css)"
      via: "CSS variable references in style attribute or className"
      pattern: "--liv-(bg|text|accent|dur|ease|border)"
    - from: "playground component"
      to: "Plan 66-02 motion primitives"
      via: "imports from @/components/motion barrel"
      pattern: "from\\s+['\"]@/components/motion['\"]"
    - from: "playground component"
      to: "Plan 66-03 shadcn liv-* variants"
      via: "JSX with variant='liv-primary' etc."
      pattern: "variant=\\{?['\"]liv-(primary|status-running|elevated|slider)['\"]"
    - from: "playground component"
      to: "Plan 66-04 LivIcons map"
      via: "import { LivIcons } from '@/icons/liv-icons'"
      pattern: "from\\s+['\"]@/icons/liv-icons['\"]"
    - from: "router.tsx"
      to: "playground route component"
      via: "lazy import + RouteObject registration under EnsureLoggedIn parent"
      pattern: "playground/liv-design-system"
---

<objective>
Build a single-page visual reference at `/playground/liv-design-system` that renders every token, motion primitive, utility class, shadcn variant, and icon mapping from Plans 66-01..66-04 in one scrollable view. This is the verification surface for Phase 66 (no Storybook per D-16) and the side-by-side comparison source for the v31-DRAFT line 257-258 "WOW differential A/B" judgment.

Purpose: Per CONTEXT.md D-19/D-20/D-22, the playground IS the verification mechanism. Without it, there is no surface for the user to walk and judge whether the WOW differential is achieved. Per D-21, gated behind login but accessible to any user (not admin-only); hidden from main navigation.

Output:
- NEW `livos/packages/ui/src/routes/playground/liv-design-system.tsx` (the single-page reference; minimum ~150 LOC for 6 sections)
- MODIFY `livos/packages/ui/src/router.tsx` (register the route under `EnsureLoggedIn`)

This plan has `autonomous: false` because Task 3 is `checkpoint:human-verify` per D-22 — Claude renders the playground via Chrome DevTools MCP and the user walks it to confirm WOW differential.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-liv-design-system-v1/66-CONTEXT.md
@.planning/v31-DRAFT.md
@.planning/phases/66-liv-design-system-v1/66-01-SUMMARY.md
@.planning/phases/66-liv-design-system-v1/66-02-SUMMARY.md
@.planning/phases/66-liv-design-system-v1/66-03-SUMMARY.md
@.planning/phases/66-liv-design-system-v1/66-04-SUMMARY.md

# Existing files this plan reads or modifies
@livos/packages/ui/src/router.tsx
@livos/packages/ui/src/modules/auth/ensure-logged-in.tsx
@livos/packages/ui/src/styles/liv-tokens.css
@livos/packages/ui/src/components/motion/index.ts
@livos/packages/ui/src/icons/liv-icons.ts

<scope_guard>
**HARD RULES:**
- D-19: Single route `/playground/liv-design-system` only. Do NOT create additional sub-routes.
- D-20: Visual reference only — no editor controls beyond replay buttons. Do NOT add color pickers, font-size sliders, or live token-editing UI.
- D-21: Gate via `EnsureLoggedIn` (existing middleware), NOT admin-only. Hidden from main navigation.
- D-22: Side-by-side WOW comparison vs current `ai-chat/index.tsx` is `needs-human-walk` — Claude takes screenshots, human judges. Never fake-pass this.
- D-09: Do NOT touch `livos/packages/ui/src/components/motion-primitives/`.
- Do NOT touch `nexus/packages/core/src/sdk-agent-runner.ts` (sacred file).
- Do NOT touch any broker/livinityd code.
- Do NOT migrate `ai-chat/index.tsx` or any consumer to use the new tokens — that's P68-P70.
- Do NOT add a Storybook config (D-16).
</scope_guard>
</context>

<interfaces>
<!-- Imports the playground component will need -->

```tsx
// Plan 66-01 tokens — referenced via CSS variable names in style props or arbitrary-value Tailwind classes
const COLOR_TOKENS = [
  '--liv-bg-deep', '--liv-bg-elevated', '--liv-bg-glass', '--liv-border-subtle',
  '--liv-text-primary', '--liv-text-secondary', '--liv-text-tertiary',
  '--liv-accent-cyan', '--liv-accent-amber', '--liv-accent-violet', '--liv-accent-emerald', '--liv-accent-rose',
] as const

const DURATION_TOKENS = ['--liv-dur-instant', '--liv-dur-fast', '--liv-dur-normal', '--liv-dur-slow'] as const
const EASING_TOKENS = ['--liv-ease-out', '--liv-ease-spring'] as const

// Plan 66-01 utility classes
const UTILITY_CLASSES = ['liv-glass', 'liv-grain', 'liv-glow-amber', 'liv-glow-cyan', 'liv-glow-violet'] as const

// Plan 66-02 motion primitives
import {FadeIn, GlowPulse, SlideInPanel, TypewriterCaret, StaggerList} from '@/components/motion'

// Plan 66-03 shadcn liv-* variants
import {Button} from '@/shadcn-components/ui/button'
import {Badge} from '@/shadcn-components/ui/badge'
import {Card} from '@/components/ui/card'  // Note: card.tsx is in components/ui/, not shadcn-components/
import {Slider} from '@/shadcn-components/ui/slider'

// Plan 66-04 LivIcons
import {LivIcons, type LivIconKey} from '@/icons/liv-icons'

// Plan 66-01 typography (consumed via Tailwind utilities)
// text-display-1, text-display-2, text-h1, text-body, text-caption, text-mono-sm
```

<!-- Existing router pattern (read first) -->
```tsx
// livos/packages/ui/src/router.tsx wraps logged-in routes in <EnsureLoggedIn>...</EnsureLoggedIn>
// New route slots into the children array. Use React.lazy + Suspense for code-split.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the liv-design-system.tsx playground component (6 sections)</name>
  <files>livos/packages/ui/src/routes/playground/liv-design-system.tsx</files>
  <read_first>
    - livos/packages/ui/src/styles/liv-tokens.css (Plan 66-01 — list every variable + utility class)
    - livos/packages/ui/src/components/motion/index.ts (Plan 66-02 barrel exports)
    - livos/packages/ui/src/components/motion/TypewriterCaret.tsx (props for the demo)
    - livos/packages/ui/src/shadcn-components/ui/button.tsx (Plan 66-03 — confirm liv-primary variant)
    - livos/packages/ui/src/shadcn-components/ui/badge.tsx (Plan 66-03 — confirm liv-status-running variant)
    - livos/packages/ui/src/components/ui/card.tsx (Plan 66-03 — confirm liv-elevated variant)
    - livos/packages/ui/src/shadcn-components/ui/slider.tsx (Plan 66-03 — confirm liv-slider variant)
    - livos/packages/ui/src/icons/liv-icons.ts (Plan 66-04 — LivIcons map)
    - livos/packages/ui/src/routes/notifications.tsx (existing route pattern reference; do NOT modify)
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
1. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
2. Verify all upstream plans completed: read each Plan 66-0X SUMMARY.md and confirm artifacts listed exist (token CSS, motion barrel, shadcn variants, LivIcons map). If any artifact missing, STOP and report — playground depends on all 4 upstream plans.
3. Verify the `playground/` subdirectory does not exist yet under `livos/packages/ui/src/routes/`. Create it implicitly via the file write.
4. Write `livos/packages/ui/src/routes/playground/liv-design-system.tsx` with the following structure (~150-300 LOC):

   **File header:**
   - Default-export a React component named `LivDesignSystemPlayground`.
   - Top-level wrapper uses `bg-[color:var(--liv-bg-deep)]` and `text-[color:var(--liv-text-primary)]` to apply the dark Liv background.
   - Page is a single scrolling column with `max-w-5xl mx-auto px-8 py-12 space-y-16`.

   **Section 1 — Color Tokens (lines ~20-60):**
   - Heading: `<h1 className="text-display-2">Liv Design System v1 — Playground</h1>` followed by a `text-body text-[color:var(--liv-text-secondary)]` subtitle.
   - Subsection "Color Tokens": iterate `COLOR_TOKENS` array, render each as a row: a 64×64 swatch (background-color: `var(--<token>)`) + the variable name in `text-mono-sm` + the resolved value (use `getComputedStyle` in a `useEffect` to read each CSS variable's value at mount, store in state, render).
   - Subsection "Duration & Easing": render duration tokens as a small animation demo (e.g. a div that translates `100%` over `var(--<duration>)` on click) + easing tokens as a `<div>` with a CSS transition using each easing curve.

   **Section 2 — Typography Scale (lines ~60-90):**
   - For each of the 6 type-scale keys, render `<p className="text-display-1">text-display-1 — The quick brown fox</p>`, `<p className="text-display-2">...</p>`, etc. Use the `font-sans` family (Inter from Plan 66-01) for non-mono entries; `font-mono` (JetBrains Mono) for `text-mono-sm`.

   **Section 3 — Motion Primitives (lines ~90-150):**
   - State: `const [replayKeys, setReplayKeys] = useState({fadeIn: 0, glowPulse: 0, slideInPanel: 0, typewriterCaret: 0, staggerList: 0})`.
   - For each motion primitive, render a small demo card with a "Replay" button that increments the key (forces re-mount = re-trigger animation):
     - `<FadeIn key={replayKeys.fadeIn} delay={0} y={8}><div>FadeIn demo content</div></FadeIn>`
     - `<GlowPulse key={replayKeys.glowPulse} color="amber"><div>GlowPulse amber</div></GlowPulse>` (also render cyan and violet variants)
     - `<SlideInPanel key={replayKeys.slideInPanel} from="right"><div>SlideInPanel from right</div></SlideInPanel>`
     - `<TypewriterCaret>` demo: a state-driven streaming-text simulator (use `useEffect` + `setTimeout` to append chars to a string; render `<span ref={anchorRef}>{streamedText}</span><TypewriterCaret anchorRef={anchorRef} />`).
     - `<StaggerList key={replayKeys.staggerList}>` with 5 child `<div>`s.

   **Section 4 — Glass / Grain / Glow Utilities (lines ~150-180):**
   - 5 demo panels (`flex flex-wrap gap-6`):
     - `<div className="liv-glass w-64 h-32">.liv-glass</div>` (over a colorful gradient background to make the blur visible)
     - `<div className="liv-grain w-64 h-32">.liv-grain</div>`
     - `<div className="liv-glow-amber w-64 h-32 bg-[color:var(--liv-bg-elevated)]">.liv-glow-amber</div>`
     - `<div className="liv-glow-cyan w-64 h-32 bg-[color:var(--liv-bg-elevated)]">.liv-glow-cyan</div>`
     - `<div className="liv-glow-violet w-64 h-32 bg-[color:var(--liv-bg-elevated)]">.liv-glow-violet</div>`

   **Section 5 — shadcn liv-* Variants (lines ~180-220):**
   - Render each variant inline:
     - `<Button variant="liv-primary">Continue</Button>`
     - `<Button variant="liv-primary" disabled>Disabled</Button>`
     - `<Badge variant="liv-status-running">Running</Badge>`
     - `<Card variant="liv-elevated"><CardHeader><CardTitle>Elevated Card</CardTitle></CardHeader><CardContent>Glass + subtle border.</CardContent></Card>` (use whatever Card sub-components the existing card.tsx exports; if Card is a single component, use that)
     - `<Slider variant="liv-slider" defaultValue={[50]} max={100} step={1} className="w-64" />`

   **Section 6 — Icon Map (lines ~220-260):**
   - `Object.entries(LivIcons).map(([key, IconComponent]) => <div key={key} className="flex flex-col items-center gap-2"><IconComponent size={32} stroke={1.5} /><span className="text-mono-sm">{key}</span></div>)` arranged in a `grid grid-cols-5 gap-6`.

5. Use TypeScript strictly: type `replayKeys` state, type `IconComponent` correctly via `LivIconKey`, no `any`. The component should be self-contained — no props.

6. Run `pnpm --filter ui typecheck` — must exit 0. Fix any type errors by aligning prop signatures with the actual exports of motion/shadcn/icons modules (DO NOT modify the upstream files; this playground adapts to them).

7. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.

8. Verify motion-primitives untouched: `git status livos/packages/ui/src/components/motion-primitives/` shows no changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('livos/packages/ui/src/routes/playground/liv-design-system.tsx','utf8'); const sections=['Color Tokens','Typography','Motion','Glass','Variants','Icon']; const missing=sections.filter(x=>!s.includes(x)); if(missing.length){console.error('Missing sections:',missing); process.exit(1)} const required=['FadeIn','GlowPulse','SlideInPanel','TypewriterCaret','StaggerList','liv-primary','liv-status-running','liv-elevated','liv-slider','LivIcons','liv-glass','liv-glow-amber']; const m2=required.filter(x=>!s.includes(x)); if(m2.length){console.error('Missing references:',m2); process.exit(1)} if(s.split(/\n/).length < 100){console.error('Component too small'); process.exit(1)} console.log('Playground OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter ui typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/ui/src/routes/playground/liv-design-system.tsx` exists, ≥100 lines.
    - File default-exports a React component.
    - All 6 sections present (greppable: "Color Tokens", "Typography", "Motion", "Glass", "Variants", "Icon").
    - All 5 motion primitives imported and demoed (`FadeIn`, `GlowPulse`, `SlideInPanel`, `TypewriterCaret`, `StaggerList`).
    - All 4 shadcn liv-* variants rendered (`liv-primary`, `liv-status-running`, `liv-elevated`, `liv-slider`).
    - `LivIcons` imported and iterated.
    - All 5 utility classes referenced (`liv-glass`, `liv-grain`, `liv-glow-amber`, `liv-glow-cyan`, `liv-glow-violet`).
    - Replay buttons present for motion primitives (greppable: `setReplay` or equivalent state setter for animation re-mount).
    - `pnpm --filter ui typecheck` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes.
  </acceptance_criteria>
  <done>Playground component written with 6 sections; all upstream artifacts referenced; types clean; sacred SHA unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Register /playground/liv-design-system route in router.tsx (gated by EnsureLoggedIn, NOT admin)</name>
  <files>livos/packages/ui/src/router.tsx</files>
  <read_first>
    - livos/packages/ui/src/router.tsx (entire file — locate the existing `<EnsureLoggedIn>` block and the children array structure)
    - livos/packages/ui/src/modules/auth/ensure-logged-in.tsx (confirm export name + behavior)
    - livos/packages/ui/src/routes/playground/liv-design-system.tsx (just-written; confirm default export name)
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-21)
  </read_first>
  <action>
1. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
2. Read `router.tsx` and identify the route-array structure inside `<EnsureLoggedIn>`. Routes appear to use `createBrowserRouter` with nested children — find the children array of the logged-in branch.
3. Add a new route entry inside `<EnsureLoggedIn>` (NOT admin-gated; D-21 says any logged-in user). Use lazy import per the existing pattern:
   ```tsx
   {
     path: 'playground/liv-design-system',
     lazy: async () => {
       const {default: LivDesignSystemPlayground} = await import('@/routes/playground/liv-design-system')
       return {Component: LivDesignSystemPlayground}
     },
   }
   ```
   - If the existing router uses a different lazy-loading idiom (e.g. `React.lazy` + `<Suspense>`), match that idiom instead.
   - DO NOT add a navigation link to the Dock or main menu (D-21: hidden from main nav).
   - DO NOT add an admin-only guard.
4. Run `pnpm --filter ui typecheck` — must exit 0.
5. Run `pnpm --filter @livos/config build && pnpm --filter ui build` — must exit 0.
6. Sacred file SHA gate: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` must return `4f868d318abff71f8c8bfbcf443b2393a553018b`.
7. Verify motion-primitives untouched.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('livos/packages/ui/src/router.tsx','utf8'); if(!s.includes('playground/liv-design-system')){console.error('Route not registered'); process.exit(1)} if(/admin/i.test(s.split('playground/liv-design-system')[0].slice(-300))){console.error('WARN: route may be inside admin guard — check D-21'); /* warning, not failure */} console.log('Router OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter ui typecheck</automated>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `livos/packages/ui/src/router.tsx` contains the path string `playground/liv-design-system` (greppable).
    - The new route is INSIDE the `<EnsureLoggedIn>` branch (verified by reading 50 lines of context around the route entry — `EnsureLoggedIn` should appear earlier in the parent chain).
    - The new route is NOT inside any admin-only guard (no `<EnsureAdmin>`-style wrapper visible in the parent chain).
    - No new entry added to Dock, main menu, mobile tab bar, or any navigation surface (D-21).
    - `pnpm --filter ui typecheck` exits 0.
    - `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes.
  </acceptance_criteria>
  <done>Route registered under EnsureLoggedIn (not admin); hidden from nav; build+typecheck green; sacred SHA unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-walk WOW differential A/B verification (per D-22, never fake-pass)</name>
  <what-built>
    - `/playground/liv-design-system` route renders all 6 sections with the new Liv tokens, motion primitives, utility classes, shadcn liv-* variants, and icon map (Tasks 1+2).
    - Build passes; typecheck passes; sacred file untouched.
    - Claude has attempted to capture screenshots via Chrome DevTools MCP (browserUrl http://127.0.0.1:9223) for both the playground AND the existing `routes/ai-chat/index.tsx` for side-by-side comparison.
  </what-built>
  <how-to-verify>
1. **Run the UI dev server locally** (or use the deployed Mini PC instance):
   ```bash
   cd livos/packages/ui
   pnpm dev   # serves on http://localhost:3000
   ```
   Visit `http://localhost:3000/playground/liv-design-system` (you must be logged in to LivOS first; if not logged in, the route redirects to `/login`).

2. **Walk every section in order:**
   - **Color Tokens** — confirm 12 color swatches render with their CSS variable name + resolved hex value. Confirm 4 duration tokens animate when clicked. Confirm 2 easing curves visibly differ (`--liv-ease-spring` should have visible overshoot).
   - **Typography Scale** — confirm 6 sample lines render at distinct sizes. Inter Variable should be visible (not the default browser sans-serif). `text-mono-sm` should render in JetBrains Mono.
   - **Motion Primitives** — click each "Replay" button:
     - FadeIn fades in over ~350ms with slight upward translation.
     - GlowPulse breathes (amber/cyan/violet) with visible 24px glow.
     - SlideInPanel slides in from the right (and other directions if rendered).
     - TypewriterCaret blinks at 530ms intervals AND repositions when the streaming-text demo updates.
     - StaggerList children appear with 50ms stagger between each.
   - **Glass/Grain/Glow** — confirm `.liv-glass` has visible backdrop blur (use a colorful background behind it). `.liv-grain` has subtle texture overlay. `.liv-glow-*` classes show 24px outer glow in their respective colors.
   - **shadcn liv-* Variants** — confirm:
     - `liv-primary` Button is cyan with white text; on hover, gains visible cyan glow.
     - `liv-status-running` Badge has a leading pulsing cyan dot (animate-pulse, not just static).
     - `liv-elevated` Card has glass background + subtle border.
     - `liv-slider` has cyan range/thumb, default value renders correctly.
   - **Icon Map** — confirm all 10 icons render at 32px stroke=1.5 with their key labels.

3. **WOW differential A/B (D-22):**
   - Open a second tab to `http://localhost:3000/` (or your existing AI chat route, e.g. `/ai-chat` if it exists).
   - Take a screenshot of the playground.
   - Take a screenshot of the existing AI chat.
   - Side-by-side comparison: does the playground "say brand new product" vs the existing UI? This is a JUDGMENT call per v31-DRAFT line 258. If you (the user) say YES → playground passes. If NO → defer to a Phase 66 follow-up plan to refine token values (e.g. accent saturation, glow intensity).
   - Per D-22, this verification is `needs-human-walk` — Claude cannot judge "WOW" autonomously.

4. **Console errors check:**
   - Open browser DevTools → Console. Refresh the playground page. Confirm zero red errors. Yellow warnings are acceptable if they're framer-motion or React-strict-mode warnings; document any in the resume signal.

5. **Mobile / responsive check (lightweight):**
   - Resize browser to ~375px wide. Confirm layout doesn't break catastrophically (sections may stack — that's fine; horizontal scroll on a single section is NOT fine).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - **"approved"** — playground renders correctly, WOW differential present, no console errors, ready to ship Phase 66.
    - **"approved with notes: \<notes\>"** — passes but log specific issues to BACKLOG (e.g. "amber glow too dim", "TypewriterCaret jitter on long streams").
    - **"failed: \<reason\>"** — describe what's broken; Claude will create a follow-up gap-closure plan via `/gsd-plan-phase 66 --gaps`.

    Do NOT auto-approve this task without walking the page (per `feedback_milestone_uat_gate.md`: never declare a milestone passed without UAT).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playground ← all upstream Plan 66 outputs | Aggregates artifacts from 66-01..66-04; if any upstream artifact is missing/wrong, playground fails to compile or render. |
| Browser ← Chrome DevTools MCP (screenshot capture) | If used by Claude in Task 3 to capture screenshots; trusted local-loopback connection on `127.0.0.1:9223`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05-01 | Spoofing | Unauthenticated user accesses playground | mitigate | Route nested inside `EnsureLoggedIn`; existing middleware blocks unauthenticated requests with redirect to `/login`. |
| T-66-05-02 | Information Disclosure | Playground exposes internal token values | accept | Token names + hex values are public-facing visual identity, not secrets. No PII, no API keys. |
| T-66-05-03 | DoS | Playground renders 5 motion primitives + many DOM nodes | accept | Single-page reference loads ~50 DOM nodes; framer-motion animations pause on tab-blur. Negligible perf cost. |
| T-66-05-04 | Tampering | User skips Task 3 verification | mitigate | Task is `checkpoint:human-verify` with `gate="blocking"` — execute-phase pauses. Per `feedback_milestone_uat_gate.md`, never fake-pass. |
</threat_model>

<verification>
- File `livos/packages/ui/src/routes/playground/liv-design-system.tsx` exists with all 6 sections.
- File `livos/packages/ui/src/router.tsx` registers the route under `EnsureLoggedIn` (NOT admin).
- `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
- `pnpm --filter ui typecheck` exits 0.
- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- `git status livos/packages/ui/src/components/motion-primitives/` shows zero changes.
- Task 3 human-walk completed with explicit "approved" / "approved with notes" / "failed" signal (per D-22 + `feedback_milestone_uat_gate.md`).
</verification>

<success_criteria>
- ROADMAP Phase 66 success criterion #1 met: playground/Storybook shows every token, motion, variant in one place (achieved via playground; no Storybook per D-16).
- ROADMAP Phase 66 success criterion #2 met (subject to human judgment in Task 3): side-by-side screenshot vs current ai-chat shows visible WOW differential.
- ROADMAP Phase 66 success criterion #3 met: All shadcn liv-* variants render with proper accent + glow (visible in Section 5).
- ROADMAP Phase 66 success criterion #4 met: Motion primitives reusable across subsequent phases (demonstrated by playground rendering them).
- All 4 upstream plans (66-01..66-04) integration-tested via single rendering surface.
</success_criteria>

<output>
After completion (i.e. after Task 3 returns "approved" or "approved with notes"), create `.planning/phases/66-liv-design-system-v1/66-05-SUMMARY.md` summarizing:
- Playground sections rendered + screenshot paths (if Chrome DevTools MCP captured them).
- Human-walk verdict (approved / approved-with-notes / failed) — quote the user's resume signal verbatim.
- Any BACKLOG items logged from "approved with notes".
- Build/typecheck status.
- Sacred file SHA verification.
- Any deviations from CONTEXT.md path assumptions encountered (e.g. card.tsx actual location).
</output>
