---
phase: 66-liv-design-system-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/styles/liv-tokens.css
  - livos/packages/ui/src/index.css
  - livos/packages/ui/tailwind.config.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04]
tags: [design-system, tokens, css, typography, tailwind]
user_setup: []

must_haves:
  truths:
    - "File `livos/packages/ui/src/styles/liv-tokens.css` exists and contains EXACT verbatim values from v31-DRAFT.md lines 198-224 — every CSS variable name and value matches character-for-character (regex grepable: --liv-bg-deep, --liv-bg-elevated, --liv-bg-glass, --liv-border-subtle, --liv-text-primary, --liv-text-secondary, --liv-text-tertiary, --liv-accent-cyan, --liv-accent-amber, --liv-accent-violet, --liv-accent-emerald, --liv-accent-rose, --liv-dur-instant, --liv-dur-fast, --liv-dur-normal, --liv-dur-slow, --liv-ease-out, --liv-ease-spring)"
    - "File `liv-tokens.css` defines `.liv-glass`, `.liv-grain`, `.liv-glow-amber`, `.liv-glow-cyan`, `.liv-glow-violet` utility classes per D-13 (5 classes, all greppable as `\\.liv-(glass|grain|glow-(amber|cyan|violet))\\s*\\{`)"
    - "File `livos/packages/ui/src/index.css` contains BOTH `@import url(...)` lines for Inter Variable AND JetBrains Mono fonts per D-10, plus the existing Space Grotesk import is preserved (D-12 stipulates DO NOT remove)"
    - "File `livos/packages/ui/src/index.css` contains `@import './styles/liv-tokens.css';` (or equivalent relative path) placed after `@tailwind base` per D-04"
    - "File `livos/packages/ui/tailwind.config.ts` extends `theme.extend.fontSize` with the 6 type-scale entries per D-11: text-display-1 (48px), text-display-2 (36px), text-h1 (24px), text-body (15px), text-caption (12px), text-mono-sm (13px)"
    - "File `livos/packages/ui/tailwind.config.ts` `theme.fontFamily` references Inter and JetBrains Mono (existing JetBrains Mono ref in `mono` array preserved; Inter added to `sans` chain)"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b` at end of plan execution (read-only — this plan never reads or writes it; SHA is verified as a sanity gate)"
    - "Build is green: `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0 with no TypeScript errors and no Tailwind warnings"
  artifacts:
    - path: "livos/packages/ui/src/styles/liv-tokens.css"
      provides: "All Liv design tokens as CSS variables on :root + glass/grain/glow utility classes"
      min_lines: 60
      contains: ":root"
    - path: "livos/packages/ui/src/index.css"
      provides: "Root style entry with Inter + JetBrains Mono CDN imports + liv-tokens import"
      contains: "@import './styles/liv-tokens.css'"
    - path: "livos/packages/ui/tailwind.config.ts"
      provides: "Tailwind theme extension with Liv typography scale"
      contains: "text-display-1"
  key_links:
    - from: "livos/packages/ui/src/index.css"
      to: "livos/packages/ui/src/styles/liv-tokens.css"
      via: "@import directive after @tailwind base"
      pattern: "@import\\s+['\"]\\./styles/liv-tokens\\.css['\"]"
    - from: "livos/packages/ui/tailwind.config.ts theme.extend.fontSize"
      to: "Inter Variable + JetBrains Mono CDN-loaded fonts"
      via: "fontFamily references map to Google-loaded font names"
      pattern: "Inter|JetBrains Mono"
---

<objective>
Add the Liv color/motion/typography token foundation that every subsequent UI phase (P67-P75) consumes. Establishes `--liv-*` CSS variables on `:root`, glass/grain/glow utility classes, Inter + JetBrains Mono Google Fonts CDN imports, and the 6-step type scale as Tailwind utilities.

Purpose: Without this token layer, P68-P70 cannot reference `var(--liv-accent-cyan)`, `text-h1`, or `.liv-glass`. Per CONTEXT.md D-01, values are EXACT verbatim from v31-DRAFT.md — no bikeshedding.

Output:
- `livos/packages/ui/src/styles/liv-tokens.css` (NEW; styles/ subdirectory does not yet exist — create it)
- `livos/packages/ui/src/index.css` (MODIFY — add 2 font CDN imports + 1 token-file import)
- `livos/packages/ui/tailwind.config.ts` (MODIFY — extend fontSize + fontFamily)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-liv-design-system-v1/66-CONTEXT.md
@.planning/v31-DRAFT.md

# Existing files this plan modifies
@livos/packages/ui/src/index.css
@livos/packages/ui/tailwind.config.ts
@livos/packages/ui/package.json

<scope_guard>
**HARD RULES (D-NO-BYOK, D-NO-SERVER4, D-09):**
- Do NOT touch any file under `livos/packages/ui/src/components/motion-primitives/` (D-09).
- Do NOT touch `nexus/packages/core/src/sdk-agent-runner.ts` (sacred file SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`). Verify SHA at start AND end of plan execution.
- Do NOT add any light-theme variant or `[data-theme="light"]` selector — Liv is dark-only at v31 entry (D-03).
- Do NOT remove the existing Space Grotesk Google Fonts import from `index.css` (D-12).
- Do NOT chunk `liv-tokens.css` into multiple files — single file per D-02.
- Do NOT touch any broker code (`livos/packages/livinityd/source/modules/livinity-broker/` — out of scope for P66 entirely).
- This phase ONLY adds tokens; consumers (chat UI, side panel, etc.) are P68-P70.
</scope_guard>

<reference_values>
**EXACT verbatim values from v31-DRAFT.md lines 198-224 (DO NOT bikeshed):**

```css
/* Surface system */
--liv-bg-deep:      #050b14;
--liv-bg-elevated:  #0a1525;
--liv-bg-glass:     rgba(20, 30, 50, 0.6);
--liv-border-subtle: rgba(120, 180, 255, 0.08);

/* Text */
--liv-text-primary:   #e8f0ff;
--liv-text-secondary: #a8b8cc;
--liv-text-tertiary:  #6b7a8f;

/* Accent */
--liv-accent-cyan:   #4dd0e1;
--liv-accent-amber:  #ffbd38;
--liv-accent-violet: #a78bfa;
--liv-accent-emerald: #4ade80;
--liv-accent-rose:   #fb7185;

/* Motion durations */
--liv-dur-instant: 100ms;
--liv-dur-fast:    200ms;
--liv-dur-normal:  350ms;
--liv-dur-slow:    600ms;

/* Easing */
--liv-ease-out:    cubic-bezier(0.16, 1, 0.3, 1);
--liv-ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
```

**Type scale per v31-DRAFT line 238:**
- `text-display-1`: 48px
- `text-display-2`: 36px
- `text-h1`: 24px
- `text-body`: 15px
- `text-caption`: 12px
- `text-mono-sm`: 13px

**Glass/grain/glow shapes per v31-DRAFT 240-243 + CONTEXT D-13:**
- `.liv-glass`: `backdrop-filter: blur(12px) saturate(1.2);` + `background: var(--liv-bg-glass);` + `border: 1px solid var(--liv-border-subtle);`
- `.liv-grain`: `repeating-conic-gradient(from 0deg, rgba(255,255,255,0.012) 0deg 0.5deg, transparent 0.5deg 1deg)` overlay
- `.liv-glow-amber`: `box-shadow: 0 0 24px rgba(255,189,56,0.2), inset 0 1px 0 rgba(255,189,56,0.1);`
- `.liv-glow-cyan`: same shape, `rgba(77,208,225,0.2)` outer + `rgba(77,208,225,0.1)` inset
- `.liv-glow-violet`: same shape, `rgba(167,139,250,0.2)` outer + `rgba(167,139,250,0.1)` inset

**Font CDN URLs per D-10:**
- `https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300..700&display=swap`
- `https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&display=swap`
</reference_values>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create liv-tokens.css with all design tokens + utility classes</name>
  <files>livos/packages/ui/src/styles/liv-tokens.css</files>
  <read_first>
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-01 through D-13)
    - .planning/v31-DRAFT.md lines 184-262 (exact spec values)
    - livos/packages/ui/src/index.css (existing pattern reference — Space Grotesk import shape)
  </read_first>
  <action>
1. Verify the styles/ subdirectory doesn't yet exist (per D-02 it does not — `livos/packages/ui/src/styles/` is new). Create it implicitly via the file write.
2. Sanity-gate sacred file: run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and confirm output equals `4f868d318abff71f8c8bfbcf443b2393a553018b`. If mismatch, ABORT and report — drift detected.
3. Write `livos/packages/ui/src/styles/liv-tokens.css` with this exact content structure:
   - Header comment block: "Liv Design Tokens v1 (Phase 66) — values per v31-DRAFT.md lines 198-224. Dark-only (D-03). DO NOT add light-theme variants in this phase."
   - `:root { ... }` block with EXACTLY the variables listed in `<reference_values>` above. Variable names, hex values, easing curves, durations — character-for-character match.
   - `.liv-glass { ... }` per D-13 spec.
   - `.liv-grain { ... }` per D-13 spec — apply as `position: relative;` + `&::after { content: ''; position: absolute; inset: 0; pointer-events: none; background: <pattern>; }`. The grain is an overlay pseudo-element so the host element's content is unaffected.
   - `.liv-glow-amber { ... }`, `.liv-glow-cyan { ... }`, `.liv-glow-violet { ... }` per D-13 spec — all share the same `box-shadow: 0 0 24px <color-20%>, inset 0 1px 0 <color-10%>` shape.
4. Validate the file by visual diff against `<reference_values>` — no typos, no rounded values, no light-theme block, no extra variables not in spec.
5. Re-run sacred file SHA check after write — must still equal `4f868d318abff71f8c8bfbcf443b2393a553018b` (this task does not touch nexus, but the gate is the proof).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('livos/packages/ui/src/styles/liv-tokens.css','utf8'); const required=['--liv-bg-deep:      #050b14','--liv-bg-elevated:  #0a1525','--liv-bg-glass:     rgba(20, 30, 50, 0.6)','--liv-border-subtle: rgba(120, 180, 255, 0.08)','--liv-text-primary:   #e8f0ff','--liv-text-secondary: #a8b8cc','--liv-text-tertiary:  #6b7a8f','--liv-accent-cyan:   #4dd0e1','--liv-accent-amber:  #ffbd38','--liv-accent-violet: #a78bfa','--liv-accent-emerald: #4ade80','--liv-accent-rose:   #fb7185','--liv-dur-instant: 100ms','--liv-dur-fast:    200ms','--liv-dur-normal:  350ms','--liv-dur-slow:    600ms','cubic-bezier(0.16, 1, 0.3, 1)','cubic-bezier(0.34, 1.56, 0.64, 1)','.liv-glass','.liv-grain','.liv-glow-amber','.liv-glow-cyan','.liv-glow-violet']; const missing=required.filter(r=>!s.includes(r)); if(missing.length){console.error('MISSING:',missing); process.exit(1)} else {console.log('Tokens OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - File `livos/packages/ui/src/styles/liv-tokens.css` exists.
    - All 18 CSS variable names from `<reference_values>` are present (greppable strings).
    - All 5 utility classes (`.liv-glass`, `.liv-grain`, `.liv-glow-amber`, `.liv-glow-cyan`, `.liv-glow-violet`) are defined.
    - File contains NO `[data-theme="light"]` selector or light-theme variant block.
    - File contains NO CSS variables outside the 18 listed in `<reference_values>` (no extra invented tokens).
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b` after task completion.
  </acceptance_criteria>
  <done>liv-tokens.css written with all spec values verbatim; 5 utility classes present; sacred SHA unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Inter + JetBrains Mono fonts and import liv-tokens.css from index.css</name>
  <files>livos/packages/ui/src/index.css, livos/packages/ui/tailwind.config.ts</files>
  <read_first>
    - livos/packages/ui/src/index.css (existing Space Grotesk import on line 1)
    - livos/packages/ui/tailwind.config.ts (existing theme.fontFamily and theme.extend block)
    - .planning/phases/66-liv-design-system-v1/66-CONTEXT.md (D-04, D-10, D-11, D-12)
  </read_first>
  <action>
1. Edit `livos/packages/ui/src/index.css`:
   - Below the existing `@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk...')` line (line 1), add two new imports per D-10:
     - `@import url('https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300..700&display=swap');`
     - `@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&display=swap');`
   - Per D-04, immediately after `@tailwind base;` and before `@tailwind components;`, add `@import './styles/liv-tokens.css';`
   - DO NOT remove the Space Grotesk import (D-12 — used elsewhere in existing UI).
   - DO NOT touch any other content in index.css (the html/body/view-transition rules etc. remain unchanged).

2. Edit `livos/packages/ui/tailwind.config.ts`:
   - In `theme.fontFamily.sans` (currently `['var(--font-jakarta)', 'var(--font-inter)', ...defaultTheme.fontFamily.sans]`), the existing `var(--font-inter)` already references Inter; add `'Inter Variable'` and `'Inter'` literals to ensure the CDN-loaded font resolves: change to `['var(--font-jakarta)', 'var(--font-inter)', 'Inter Variable', 'Inter', ...defaultTheme.fontFamily.sans]`.
   - `theme.fontFamily.mono` already includes `'JetBrains Mono'` — leave as-is.
   - In the `theme.extend` block, locate the existing `fontSize: { ... }` block (around line 156 per the codebase scan) and ADD these 6 entries per D-11 type scale (preserve existing fontSize entries, only ADD):
     ```ts
     'display-1': ['48px', { lineHeight: '1.1', fontWeight: '600', letterSpacing: '-0.02em' }],
     'display-2': ['36px', { lineHeight: '1.15', fontWeight: '600', letterSpacing: '-0.02em' }],
     'h1': ['24px', { lineHeight: '1.25', fontWeight: '600', letterSpacing: '-0.01em' }],
     'body': ['15px', { lineHeight: '1.5', fontWeight: '400' }],
     'caption': ['12px', { lineHeight: '1.4', fontWeight: '400' }],
     'mono-sm': ['13px', { lineHeight: '1.5', fontWeight: '400', fontFamily: 'JetBrains Mono, monospace' }],
     ```
   - These keys produce Tailwind utilities `text-display-1`, `text-display-2`, `text-h1`, `text-body`, `text-caption`, `text-mono-sm` per D-11.
   - DO NOT modify `darkMode`, `content`, `screens`, or any other root config keys.

3. Run the UI build verification: `pnpm --filter @livos/config build && pnpm --filter ui build` from the `livos/` workspace root. Build must exit 0 with no TS errors and no Tailwind warnings about unknown classes.

4. Sacred file SHA gate: run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` after both edits — must equal `4f868d318abff71f8c8bfbcf443b2393a553018b`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('livos/packages/ui/src/index.css','utf8'); const tw=fs.readFileSync('livos/packages/ui/tailwind.config.ts','utf8'); const cssOk=css.includes('Inter:opsz,wght@14..32,300..700') && css.includes('JetBrains+Mono:wght@400;500;700') && css.includes(\"@import './styles/liv-tokens.css'\") && css.includes('Space+Grotesk'); const twOk=tw.includes(\"'display-1'\") && tw.includes(\"'display-2'\") && tw.includes(\"'h1'\") && tw.includes(\"'body'\") && tw.includes(\"'caption'\") && tw.includes(\"'mono-sm'\") && tw.includes('Inter'); if(!cssOk){console.error('index.css missing required imports'); process.exit(1)} if(!twOk){console.error('tailwind.config.ts missing fontSize entries'); process.exit(1)} console.log('Wiring OK')"</automated>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/config build &amp;&amp; pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `livos/packages/ui/src/index.css` contains the Inter Variable Google Fonts import.
    - `livos/packages/ui/src/index.css` contains the JetBrains Mono Google Fonts import.
    - `livos/packages/ui/src/index.css` contains `@import './styles/liv-tokens.css';` placed AFTER `@tailwind base;`.
    - `livos/packages/ui/src/index.css` STILL contains the original Space Grotesk import (D-12 preservation).
    - `livos/packages/ui/tailwind.config.ts` `theme.extend.fontSize` contains all 6 keys: `display-1`, `display-2`, `h1`, `body`, `caption`, `mono-sm`.
    - `livos/packages/ui/tailwind.config.ts` `theme.fontFamily.sans` includes a literal `'Inter Variable'` or `'Inter'` reference.
    - `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
    - Sacred file SHA equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No edits to `livos/packages/ui/src/components/motion-primitives/` (D-09).
  </acceptance_criteria>
  <done>Two font CDN imports added; liv-tokens.css imported from index.css; Tailwind type-scale extended; build green; sacred SHA unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← Google Fonts CDN | Font CSS + WOFF2 files loaded from `fonts.googleapis.com` and `fonts.gstatic.com`; trusted publisher but external dependency. |
| Authored CSS ← Token spec | Token values copy-pasted from v31-DRAFT.md; risk of typo silently breaking accent palette. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01-01 | Tampering | liv-tokens.css value drift from spec | mitigate | Verify task uses grep with exact verbatim strings from `<reference_values>`; CI-style automated check rejects drift. |
| T-66-01-02 | Information Disclosure | Google Fonts referrer leakage | accept | LivOS already loads Space Grotesk from same CDN; adding 2 more fonts has zero incremental privacy cost. Self-host is BACKLOG (D-10). |
| T-66-01-03 | Denial of Service | CDN outage breaks UI | accept | Existing Space Grotesk has same exposure; default fallback chain (`...defaultTheme.fontFamily.sans`) keeps system fonts as backup. |
</threat_model>

<verification>
- File `livos/packages/ui/src/styles/liv-tokens.css` exists with all 18 spec variables + 5 utility classes.
- File `livos/packages/ui/src/index.css` imports both fonts + tokens; preserves Space Grotesk.
- File `livos/packages/ui/tailwind.config.ts` extends fontSize with 6 type-scale keys.
- `pnpm --filter @livos/config build && pnpm --filter ui build` exits 0.
- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- No edits to motion-primitives/ library files (D-09).
</verification>

<success_criteria>
- ROADMAP Phase 66 success criterion #3 prerequisite met: tokens exist for shadcn liv-* variants to reference (Plan 66-03 consumes these via `var(--liv-accent-cyan)`).
- ROADMAP Phase 66 success criterion #1 prerequisite met: tokens visible in playground (Plan 66-05 will swatch every variable).
- DESIGN-01 (color tokens) implemented per v31-DRAFT line 198-224.
- DESIGN-02 (typography) implemented per D-10 + D-11.
- DESIGN-04 (glass/grain/glow utilities) implemented per D-13.
- Downstream phases (P67+) can now reference `var(--liv-*)` and `text-display-1` etc. as first-class utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/66-liv-design-system-v1/66-01-SUMMARY.md` summarizing: tokens added, file paths, build status, sacred SHA verification, and a snippet of the rendered fontSize entries from tailwind.config.ts.
</output>
