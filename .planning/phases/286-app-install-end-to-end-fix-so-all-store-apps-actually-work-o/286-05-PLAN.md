---
phase: 286-app-install-end-to-end-fix
plan: 05
type: execute
wave: 4
depends_on: [286-01, 286-02, 286-03, 286-04]
files_modified:
  - livos/packages/livinityd/source/modules/apps/reconcile-volume-ownership.test.ts
  - livos/packages/livinityd/source/modules/apps/health-poll.test.ts
  - livos/packages/livinityd/source/modules/apps/builtin-precedence.test.ts
  - .planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-INSTALL-MATRIX.md
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, SC7]
must_haves:
  truths:
    - "The full livinityd vitest suite passes; tsc error count is <= 305 (baseline preserved)."
    - "Unit tests cover the load-bearing pure functions: uid resolution (user: parse + image-inspect fallback + root-skip + non-1000 uids), volume classification (named/bind/skip), named-volume runtime name, health-poll verdict classifier, builtin-precedence allowlist."
    - "A representative install matrix exists (n8n / activepieces-postgres / campfire-redis / a PUID linuxserver app / a non-1000 uid app e.g. user:927) describing the exact verification each must pass: container Up+healthy, Caddy block present, HTTP 200 reachable, data persists across restart."
    - "Ship notes describe the release-tag -> update.sh path and confirm the boot backfill self-heals existing boxes on Update WITHOUT touching the operator's live box."
  artifacts:
    - path: ".planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-INSTALL-MATRIX.md"
      provides: "the representative install matrix + per-app verification steps + ship notes"
      min_lines: 40
  key_links:
    - from: "vitest suite"
      to: "the 3 new test files"
      via: "npm test passes including reconcile/health/precedence tests"
      pattern: "reconcile-volume-ownership.test|health-poll.test|builtin-precedence.test"
---

<objective>
Final verification + ship readiness. Run the full test suite + tsc gate, ensure the new unit tests cover the load-bearing logic, document the representative install matrix the operator (or a clean test box) will walk, and capture ship notes confirming the source-only / self-heal-on-Update / never-touch-the-operator-box constraints.

Purpose: deliver the CONTEXT-locked phase E (verification + ship). No production code is invented here; this plan proves the prior 4 plans hold together and produces the ship checklist.

Output: augmented test coverage (if any gaps from plans 01-03 remain), the install-matrix doc, and a verified tsc<=305 + green-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-CONTEXT.md
@.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-RESEARCH.md
@.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-01-PLAN.md
@.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-02-PLAN.md
@.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the gate + close any unit-coverage gaps</name>
  <read_first>
    - livos/packages/livinityd/package.json (the `test:run` and `typecheck` scripts)
    - livos/packages/livinityd/source/modules/apps/reconcile-volume-ownership.test.ts (from 286-01)
    - livos/packages/livinityd/source/modules/apps/health-poll.test.ts (from 286-02)
    - livos/packages/livinityd/source/modules/apps/builtin-precedence.test.ts (from 286-03)
    - 286-CONTEXT.md phase E (required unit coverage: uid resolution, named-volume name, health-poll)
  </read_first>
  <action>
1. Run the tsc gate and record the exact error count:
   `cd livos/packages/livinityd && npx tsc --noEmit 2>&1 | grep -c "error TS"`
   It MUST be <= 305. If it is > 305, the new code introduced errors — fix them (do NOT raise the baseline).

2. Run the three new test files:
   `cd livos/packages/livinityd && npx vitest run source/modules/apps/reconcile-volume-ownership.test.ts source/modules/apps/health-poll.test.ts source/modules/apps/builtin-precedence.test.ts`
   All must pass.

3. Audit unit-coverage against the CONTEXT phase-E required set and ADD any missing case to the relevant test file:
   - uid resolution: `user:` numeric parse, `user:` name->1000, image-inspect numeric fallback, image-inspect name->1000, empty Config.User->skip, `user:0`->skip, a NON-1000 uid (e.g. 927, 5001) -> exact value. (286-01 Task 1 covers these — confirm; add the missing one if any.)
   - named-volume runtime name: assert `${projectName}_${key}` formatting. If not directly tested, extract a tiny `namedVolumeRuntimeName(projectName, key)` helper into reconcile-volume-ownership.ts and add a pure test for it (`namedVolumeRuntimeName('n8n','n8n_data') === 'n8n_n8n_data'`, `namedVolumeRuntimeName('n8n-user-bruce','data') === 'n8n-user-bruce_data'`).
   - health-poll verdict classifier: the 7 cases (286-02 Task 1) — confirm present.

4. Run the FULL livinityd suite to ensure no regression:
   `cd livos/packages/livinityd && npm run test:run`
   Record the pass/fail totals in the SUMMARY. Pre-existing unrelated failures (if any) are noted but must not be NEWLY introduced by this phase.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep -c "error TS"` prints a number <= 305.
    - The three new test files all pass (0 failures) under `npx vitest run`.
    - `npm run test:run` completes with no NEW failures vs the pre-286 baseline (totals recorded in SUMMARY).
    - uid resolution coverage includes at least one NON-1000 uid case (927 or 5001) returning the exact value.
    - named-volume runtime name (`${projectName}_${key}`) has an explicit assertion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the representative install matrix + ship notes doc</name>
  <read_first>
    - 286-CONTEXT.md phase E (install matrix: n8n / activepieces / campfire / linuxserver-PUID / non-1000 app) + ship section
    - 286-RESEARCH.md sections 1, 6 (crash-loopers + uid distribution: the 5 non-1000 uids 5001/927/1001/101/0; 34 PUID/linuxserver apps)
    - 286-01-PLAN.md must_haves (the reconcile guarantees the matrix must prove)
  </read_first>
  <action>
Write `.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-INSTALL-MATRIX.md` containing:

**Section 1 — Representative install matrix** (a table). For EACH app below, list source (builtin/catalog), the uid class it exercises, and the 4 checks that must pass:
   - `n8n` — catalog (named volume `n8n_data`, no explicit `user:` -> image `node`/1000); exercises image-inspect->1000 + named-volume chown.
   - `activepieces` — catalog (postgres service `user:1000` + named volume); exercises `user:1000` directive + root-owned named-volume class (the crash class).
   - `campfire` — catalog (redis `user:1000`); exercises `user:1000` + named volume.
   - a PUID/linuxserver app (e.g. `calibre-web` or a catalog app with `PUID/PGID`); exercises image-starts-root-reads-PUID-self-heals -> pre-chown to 1000 must be harmless/aligned (no breakage).
   - a NON-1000 uid app (catalog app with `user:927` or `user:5001`); exercises chown to the DECLARED uid, NOT 1000.

   The 4 per-app checks (columns), with exact commands:
   1. Container Up+healthy: `docker ps`; `docker inspect -f '{{.State.Status}}' <container>` == running AND (`docker inspect -f '{{.State.Health.Status}}' <container>` == healthy OR no healthcheck); `docker logs <container>` has no EACCES.
   2. Caddy block present: `grep '<app-subdomain>' /etc/caddy/Caddyfile` (or the Caddyfile path on the box) shows a `reverse_proxy 127.0.0.1:<port>` block.
   3. HTTP 200 reachable: `curl -sI http://127.0.0.1:<port>/` returns 2xx/3xx (not connection-refused).
   4. Data persists: `docker restart <container>` (or app stop+start), then confirm prior data still present (n8n workflow / postgres table).

**Section 2 — Backfill self-heal check**: after a livinityd restart, `docker ps -a --filter status=restarting` is EMPTY. The 4 known crash-loopers (`n8n_server_1`, `activepieces_db_1`, `campfire_redis_1`, `syncthing_server_1`) are Up.

**Section 3 — uid-agnostic proof**: the matrix should be walked on a box whose desktop user is uid 1001 (the live-box class) to prove uid-agnosticism — but per the operator constraint it is walked on a CLEAN/test box or when the operator opts in, NEVER by modifying the operator's live box.

**Section 4 — Ship notes**:
   - Source-only change; no operator-box modification by any plan.
   - Ship = version bump + release tag -> `bash /opt/livos/update.sh` (rsync source, restart livinityd via tsx; no compile step for livinityd).
   - On the next livinityd start after Update, the boot backfill (286-01 Task 3) reconciles ALL installed apps -> existing broken boxes self-heal automatically.
   - tsc baseline 305 preserved; livinityd runs via tsx.
   - Rollback: the change is additive (new modules + reordered/retried calls); reverting the commit restores prior behavior.

Keep it concrete and command-driven (exact docker/curl/grep commands), not prose.
  </action>
  <verify>
    <automated>test -f ".planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-INSTALL-MATRIX.md" && grep -c "docker inspect" ".planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-INSTALL-MATRIX.md"</automated>
  </verify>
  <acceptance_criteria>
    - `286-INSTALL-MATRIX.md` exists with >= 40 lines.
    - It names all 5 representative apps (n8n, activepieces, campfire, a PUID/linuxserver app, a non-1000 uid app).
    - It contains the exact verification commands (`docker inspect`, `curl`, `grep ... reverse_proxy`).
    - It includes the backfill self-heal check (`docker ps -a --filter status=restarting`).
    - It includes ship notes: source-only, update.sh self-heal, never-touch-operator-box, tsc<=305.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (CHECKPOINT): Representative install-matrix walk on a clean/test box (NOT the operator's live box)</name>
  <what-built>
The full 286 fix is code-complete and unit-tested: volume-ownership reconciliation (root alpine helper container, uid-agnostic), health/readiness verification, catalog>builtin precedence, Caddy-reachability hardening. The 286-INSTALL-MATRIX.md describes the exact end-to-end walk.
  </what-built>
  <action>Operator-action gate: this task has no automated step. The operator (or a clean/test box) executes the 286-INSTALL-MATRIX.md walk described in how-to-verify and reports per-app pass/fail. No code or box change is performed by the agent.</action>
  <how-to-verify>
This phase's end-to-end proof (real container start + HTTP reachability + data persistence) requires installing real store apps, which CANNOT be done from this dev environment and MUST NOT be done on the operator's live box (locked constraint). The operator (or a clean/test box) walks `286-INSTALL-MATRIX.md`:
  1. Ship the release tag and run `bash /opt/livos/update.sh` on a CLEAN/test box (or wait for the operator to opt in on a non-production box).
  2. Install each of the 5 representative apps via the store UI.
  3. For each: run the 4 checks (container Up+healthy, Caddy block present, HTTP 200, data persists).
  4. Restart livinityd and confirm `docker ps -a --filter status=restarting` is empty.
  5. Confirm the operator's live box was NOT modified by us — it self-heals only on the operator's own next Update.
  </how-to-verify>
  <resume-signal>Operator replies with the matrix results (per-app pass/fail) or "defer live-walk" to mark the phase code-complete pending an opt-in box walk.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> code under test | unit tests exercise pure functions only (no Docker, no network); the install-matrix walk is operator-driven on a non-production box |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-286-16 | Tampering | install-matrix walk could be run against the operator's live box | mitigate | the plan + matrix doc + checkpoint EXPLICITLY constrain the walk to a clean/test box; the operator's box self-heals only via their own Update. No automated step in this phase touches any box. |
| T-286-17 | Repudiation | no record of which apps were verified | mitigate | the checkpoint requires per-app pass/fail results recorded in the SUMMARY before the phase is marked complete. |
</threat_model>

<verification>
- `npx tsc --noEmit` error count <= 305 (baseline).
- The 3 new test files pass; `npm run test:run` has no new failures.
- `286-INSTALL-MATRIX.md` exists and is command-driven for all 5 representative apps + backfill check + ship notes.
- The end-to-end walk is documented and gated behind a non-production-box checkpoint.
</verification>

<success_criteria>
- SC1-SC7 — all covered by the prior plans; this plan proves the unit gate (tsc<=305 + green suite) and produces the operator-walkable install matrix that demonstrates each crash class (named-volume root-owned, user:1000, PUID, non-1000 uid) installs Up+healthy+reachable+persistent, uid-agnostically, without touching the operator's live box.
</success_criteria>

<output>
After completion, create `.planning/phases/286-app-install-end-to-end-fix-so-all-store-apps-actually-work-o/286-05-SUMMARY.md`.
</output>
