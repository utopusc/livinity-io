---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 05
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - livos/packages/ui/public/figma-exports/dock-home.svg
  - livos/packages/ui/public/figma-exports/dock-live-usage.svg
  - livos/packages/ui/public/figma-exports/dock-app-store.svg
  - livos/packages/ui/src/providers/apps.tsx
  - livos/packages/ui/src/modules/desktop/dock.test.tsx
  - livos/packages/ui/public/figma-exports/dock-home.png
  - livos/packages/ui/public/figma-exports/dock-live-usage.png
  - livos/packages/ui/public/figma-exports/dock-app-store.png
  - livos/packages/ui/public/figma-exports/dock-settings.png
  - livos/packages/ui/public/figma-exports/dock-files.png
autonomous: false
requirements: [Item-2]
must_haves:
  truths:
    - "Home, Live Usage, and App Store system apps show new LivOS gradient SVG tiles (no Umbrel PNGs)"
    - "The 3 new tiles match the dock-files-new.svg design system (120x120, rx=26, 2-stop diagonal gradient, centered white glyph)"
    - "The dock.test.tsx mock strings for Live Usage and App Store match the new apps.tsx icon literals exactly"
    - "The 5 now-orphan Umbrel PNGs (dock-home/live-usage/app-store/settings/files) are deleted"
    - "Operator has seen and approved the 3 rendered tiles"
  artifacts:
    - path: "livos/packages/ui/public/figma-exports/dock-home.svg"
      provides: "New LivOS Home tile (blue gradient, house glyph)"
      contains: "linearGradient"
    - path: "livos/packages/ui/public/figma-exports/dock-live-usage.svg"
      provides: "New LivOS Live Usage tile (green gradient, gauge/activity glyph)"
      contains: "linearGradient"
    - path: "livos/packages/ui/public/figma-exports/dock-app-store.svg"
      provides: "New LivOS App Store tile (indigo/purple gradient, bag/grid glyph)"
      contains: "linearGradient"
  key_links:
    - from: "apps.tsx Home/LiveUsage/AppStore icon literals"
      to: "the 3 new SVG files + dock.test.tsx mock literals"
      via: "icon string '/figma-exports/<tile>.svg?v=285' references the new files; mock mirrors them"
      pattern: "dock-(home|live-usage|app-store)\\.svg\\?v=285"
---

<objective>
Author the 3 new LivOS dock tiles (Home / Live Usage / App Store) that have no existing LivOS SVG, repoint their registry entries + the dock.test mock, and delete the 5 now-orphan Umbrel PNGs (Item 2b — the new-art half of Item 2).

Purpose: There is NO existing LivOS SVG for Home / Live Usage / App Store (confirmed in RESEARCH against the full figma-exports inventory). The operator wants all Umbrel art gone, so author 3 minimal tiles in the LivOS design system (mimicking `dock-files-new.svg`), repoint the registry, and delete the orphaned PNGs. Operator must SEE the rendered tiles before finalizing (locked decision: "show the operator the result").

Output: 3 new SVG files; repointed `apps.tsx:42/63/72` + `dock.test.tsx:60/61` (with `?v=285`); 5 deleted PNGs (`dock-home/live-usage/app-store/settings/files.png` — all now zero-importer after Plan 02 + this plan's repoints); an operator visual-approval checkpoint. ONE atomic commit. Gated by `pnpm --filter ui build` + `pnpm --filter ui test`.

DEPENDS ON Plan 02 (Item 2a): Plan 02 repointed Devices/Schedules/mobile off `dock-settings.png` and the mobile Files tab off `dock-files.png`. ONLY after Plan 02 lands AND this plan repoints Home/LiveUsage/AppStore are `dock-settings.png` + `dock-files.png` fully orphaned and deletable here. Both plans edit `apps.tsx` + `dock.test.tsx` → sequential (this is Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md

<interfaces>
<!-- EXACT skeleton to copy — dock-files-new.svg (the LivOS tile design system): -->
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" fill="none">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="120" y2="120" gradientUnits="userSpaceOnUse">
      <stop offset="0%" stop-color="#f59e0b"/>
      <stop offset="100%" stop-color="#d97706"/>
    </linearGradient>
  </defs>
  <rect width="120" height="120" rx="26" fill="url(#bg)"/>
  <path d="M34 44c0-3.3 2.7-6 6-6h14l8 8h18c3.3 0 6 2.7 6 6v28c0 3.3-2.7 6-6 6H40c-3.3 0-6-2.7-6-6V44z" fill="white" fill-opacity="0.95"/>
  <path d="M34 52h52v28c0 3.3-2.7 6-6 6H40c-3.3 0-6-2.7-6-6V52z" fill="white" fill-opacity="0.85"/>
</svg>
```
<!-- LOCKED palettes (CONTEXT §RESOLVED Open-Q2): -->
<!--   dock-home.svg       : #3b82f6 -> #2563eb (blue),   house glyph -->
<!--   dock-live-usage.svg : #22c55e -> #16a34a (green),  gauge/activity-line glyph -->
<!--   dock-app-store.svg  : #6366f1 -> #4f46e5 (indigo),  shopping-bag or grid glyph -->
<!-- apps.tsx cache-bust self-analog at :143 : icon: '/figma-exports/dock-ai-chat.svg?v=chat_2026_06_02' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 3 new LivOS gradient tiles (Home / Live Usage / App Store)</name>
  <files>livos/packages/ui/public/figma-exports/dock-home.svg, livos/packages/ui/public/figma-exports/dock-live-usage.svg, livos/packages/ui/public/figma-exports/dock-app-store.svg</files>
  <read_first>
    - livos/packages/ui/public/figma-exports/dock-files-new.svg — read the full file (the exact skeleton + glyph structure to mimic).
    - livos/packages/ui/public/figma-exports/dock-server.svg — read it for the simpler "plain geometric shapes" glyph alternative (rect/circle in solid accent colors), which is easier than a single complex path.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §1 (THREE NEW SVG TILES — design-system rules + LOCKED palettes/glyph table).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 2 "New-tile asset analysis".
  </read_first>
  <action>
    Create 3 new SVG files in `livos/packages/ui/public/figma-exports/`, each following the design system EXACTLY: root `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" fill="none">`, ONE `<linearGradient id="bg" x1="0" y1="0" x2="120" y2="120" gradientUnits="userSpaceOnUse">` with exactly 2 stops, a `<rect width="120" height="120" rx="26" fill="url(#bg)"/>` tile, and a centered white glyph in the ~30–90 coordinate band (use `fill="white"` with `fill-opacity` layering, OR plain geometric shapes per `dock-server.svg`). No `width`/`height` attrs, no editor metadata, lowercase hex. Target ~0.5–1.5 KB.

    LOCKED palettes + glyphs:
    1. `dock-home.svg` — gradient `#3b82f6` → `#2563eb` (blue). Glyph: a simple HOUSE (roof triangle + body rect, optionally a door), centered, white. Example glyph approach: a triangle path for the roof + a rounded `<rect>` for the body.
    2. `dock-live-usage.svg` — gradient `#22c55e` → `#16a34a` (green). Glyph: a GAUGE or an ACTIVITY LINE (e.g. a polyline/path zig-zag like a pulse, or an arc gauge with a needle), centered, white.
    3. `dock-app-store.svg` — gradient `#6366f1` → `#4f46e5` (indigo/purple). Glyph: a SHOPPING BAG (rounded rect with handle arcs) OR a 2×2 GRID of rounded squares, centered, white.

    Keep each glyph simple and centered; the exact path coordinates are at your discretion as long as the glyph reads clearly at dock-icon size and stays within the tile. Mimic the proportions of `dock-files-new.svg`'s glyph (drawn roughly between x/y 30 and 90).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && for f in dock-home dock-live-usage dock-app-store; do test -f "livos/packages/ui/public/figma-exports/$f.svg" && echo "OK $f" || echo "MISSING $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `test -f livos/packages/ui/public/figma-exports/dock-home.svg` succeeds; `test -f .../dock-live-usage.svg` succeeds; `test -f .../dock-app-store.svg` succeeds.
    - Each file contains `viewBox="0 0 120 120"`: `grep -c 'viewBox="0 0 120 120"' livos/packages/ui/public/figma-exports/dock-home.svg` returns 1 (same for the other 2).
    - Each file contains a `linearGradient` and `rx="26"`: `grep -c "linearGradient" .../dock-home.svg` ≥1 and `grep -c 'rx="26"' .../dock-home.svg` =1 (same for the other 2).
    - Home has the locked blue stops: `grep -c "#3b82f6" .../dock-home.svg` =1 and `grep -c "#2563eb" .../dock-home.svg` =1. Live Usage: `#22c55e` + `#16a34a`. App Store: `#6366f1` + `#4f46e5`.
    - Each SVG is well-formed (parses): `cd livos && pnpm --filter ui build` exits 0 in a later task confirms the public/ copy ships.
  </acceptance_criteria>
  <done>3 new design-system-conformant SVG tiles exist with the locked palettes.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint Home/LiveUsage/AppStore icons in apps.tsx + the dock.test.tsx mock (lock-step)</name>
  <files>livos/packages/ui/src/providers/apps.tsx, livos/packages/ui/src/modules/desktop/dock.test.tsx</files>
  <read_first>
    - livos/packages/ui/src/providers/apps.tsx — read :38-110. CONFIRM Home icon at :42 = `'/figma-exports/dock-home.png'`, Live Usage at :63 = `'/figma-exports/dock-live-usage.png'`, App Store at :72 = `'/figma-exports/dock-app-store.png'`. (Plan 02 already changed Devices :99 + Schedules :107 — leave those.)
    - livos/packages/ui/src/modules/desktop/dock.test.tsx — read :57-70. CONFIRM Live Usage mock at :60 = `dock-live-usage.png`, App Store at :61 = `dock-app-store.png`. Home is NOT in this mock (no edit for Home). (Plan 02 already changed Devices :63.)
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §4 (apps.tsx edits :42/:63/:72 + dock.test.tsx mock :60/:61, cache-bust `?v=285`).
  </read_first>
  <action>
    In `livos/packages/ui/src/providers/apps.tsx`:
    - Home (:42): `icon: '/figma-exports/dock-home.png',` → `icon: '/figma-exports/dock-home.svg?v=285',`
    - Live Usage (:63): `icon: '/figma-exports/dock-live-usage.png',` → `icon: '/figma-exports/dock-live-usage.svg?v=285',`
    - App Store (:72): `icon: '/figma-exports/dock-app-store.png',` → `icon: '/figma-exports/dock-app-store.svg?v=285',`
    Do NOT touch :51 `systemAppTo: '/files/Home'`, and do NOT touch the Devices/Schedules entries Plan 02 already changed.

    In `livos/packages/ui/src/modules/desktop/dock.test.tsx` (lock-step — mock must match apps.tsx literals character-for-character):
    - Live Usage (:60): change `icon: '/figma-exports/dock-live-usage.png'` → `icon: '/figma-exports/dock-live-usage.svg?v=285'`
    - App Store (:61): change `icon: '/figma-exports/dock-app-store.png'` → `icon: '/figma-exports/dock-app-store.svg?v=285'`
    Home is NOT in the mock → no edit for Home. Do NOT change the Devices entry (:63) — Plan 02 already set it to `dock-settings-new.svg?v=285`.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui test -- dock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dock-home.svg?v=285" livos/packages/ui/src/providers/apps.tsx` returns 1.
    - `grep -c "dock-live-usage.svg?v=285" livos/packages/ui/src/providers/apps.tsx` returns 1.
    - `grep -c "dock-app-store.svg?v=285" livos/packages/ui/src/providers/apps.tsx` returns 1.
    - No Umbrel PNG icon refs remain in apps.tsx registry: `grep -c "dock-home.png\|dock-live-usage.png\|dock-app-store.png\|dock-settings.png\|dock-files.png" livos/packages/ui/src/providers/apps.tsx` returns 0 (only comment lines, if any, may mention old names — confirm no `icon:` line references a `.png`).
    - dock.test.tsx mock matches: `grep -c "dock-live-usage.svg?v=285" livos/packages/ui/src/modules/desktop/dock.test.tsx` returns 1; `grep -c "dock-app-store.svg?v=285" .../dock.test.tsx` returns 1.
    - `cd livos && pnpm --filter ui test -- dock` passes.
  </acceptance_criteria>
  <done>Home/LiveUsage/AppStore use the new SVG tiles with ?v=285; the dock.test mock mirrors apps.tsx; dock.test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the 5 now-orphan Umbrel PNGs</name>
  <files>livos/packages/ui/public/figma-exports/dock-home.png, livos/packages/ui/public/figma-exports/dock-live-usage.png, livos/packages/ui/public/figma-exports/dock-app-store.png, livos/packages/ui/public/figma-exports/dock-settings.png, livos/packages/ui/public/figma-exports/dock-files.png</files>
  <read_first>
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 2 "Orphan-PNG delete list" (these 5 become 0-importer after Item 2a + Item 2b repoints).
    - Before deleting, RE-VERIFY zero importers across `ui/src` (Plan 02 + Task 2 above must both have landed first).
  </read_first>
  <action>
    Re-verify zero importers (these depend on BOTH Plan 02 and Task 2 above having repointed every consumer):
    ```bash
    cd "$(git rev-parse --show-toplevel)"
    for f in dock-home dock-live-usage dock-app-store dock-settings dock-files; do
      echo "=== $f.png importers (src) ==="
      grep -rn "$f.png" livos/packages/ui/src || echo "0 importers"
    done
    ```
    Each MUST report 0 importers. If ANY still has an importer (e.g. a path Plan 02 missed), STOP and report — do NOT delete it. Then:
    ```bash
    cd "$(git rev-parse --show-toplevel)"
    git rm livos/packages/ui/public/figma-exports/dock-home.png \
           livos/packages/ui/public/figma-exports/dock-live-usage.png \
           livos/packages/ui/public/figma-exports/dock-app-store.png \
           livos/packages/ui/public/figma-exports/dock-settings.png \
           livos/packages/ui/public/figma-exports/dock-files.png
    ```
    Do NOT delete the new `.svg` tiles, the existing `dock-files-new.svg`/`dock-settings-new.svg`/`dock-server.svg`, or any LivOS PNG.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 names, `grep -rc "<name>.png" livos/packages/ui/src` returns 0.
    - For each of the 5 names, `test -f livos/packages/ui/public/figma-exports/<name>.png` returns non-zero (deleted).
    - The new tiles survive: `test -f livos/packages/ui/public/figma-exports/dock-home.svg` succeeds (same for live-usage, app-store).
    - `dock-files-new.svg` still present: `test -f livos/packages/ui/public/figma-exports/dock-files-new.svg` succeeds.
    - `cd livos && pnpm --filter ui build` exits 0 (no dangling import).
  </acceptance_criteria>
  <done>The 5 now-orphan Umbrel PNGs are deleted; the new tiles + existing LivOS SVGs remain; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator reviews + approves the 3 new tiles</name>
  <what-built>3 new LivOS dock tiles authored in the existing design system (120×120, rounded, 2-stop diagonal gradient, centered white glyph): Home (blue, house), Live Usage (green, gauge/activity), App Store (indigo/purple, bag/grid). The Home/Live Usage/App Store registry entries now point at them; the old Umbrel PNGs are deleted.</what-built>
  <how-to-verify>
    1. With the UI built, open the LivOS desktop and look at the dock / launchpad: confirm the Home, Live Usage, and App Store tiles render as the new LivOS gradient tiles (NOT broken/empty, NOT the old Umbrel art).
    2. (If a quick render is easier than a full box deploy) the executor should describe each tile and/or open the 3 SVG files in a browser/preview and share what they look like, so the operator can judge the glyph + color.
    3. Confirm the 3 tiles look consistent with the existing `dock-files-new.svg` / `dock-server.svg` style.
  </how-to-verify>
  <files>(none — checkpoint)</files>
  <action>This is a human-verify checkpoint — no automated implementation. After Tasks 1-3 land and the build passes, the executor presents the 3 rendered tiles (describe them and/or open the SVGs in a preview) and pauses for operator approval per the how-to-verify steps.</action>
  <verify>Operator performs the how-to-verify steps; no automated command.</verify>
  <acceptance_criteria>
    - Operator confirms the 3 tiles render correctly and look acceptable (consistent LivOS style, correct glyph + color, no Umbrel art).
    - If the operator wants a tweak (different glyph/shade), the executor adjusts the SVG(s) and re-presents before finalizing.
  </acceptance_criteria>
  <done>Operator has approved the 3 rendered tiles (or requested tweaks that the executor applied and re-presented).</done>
  <resume-signal>Type "approved" if the 3 tiles look good, or describe the change you want (e.g. "App Store should be a grid not a bag", "Home gradient too dark").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | New static SVG assets + icon-path string repoints + static asset deletions. No code path, input parsing, or data flow is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-06 | — | 3 new SVG tiles + apps.tsx/dock.test.tsx repoints + PNG deletes | accept | No new attack surface — the SVGs are static, hand-authored, contain no script/`<foreignObject>`/external refs (verify the authored markup is gradient + path only). Served as static `<img src>` icons. Build gate confirms no dangling asset import. |
</threat_model>

<verification>
- `cd livos && pnpm --filter ui build` exits 0 (catches a deleted PNG with a remaining importer or a malformed SVG path).
- `cd livos && pnpm --filter ui test -- dock` passes (mock lock-step).
- grep confirms the 3 SVGs exist with locked palettes; the 5 PNGs are gone with 0 importers.
- Operator visual approval of the 3 tiles (Task 4 checkpoint).
</verification>

<success_criteria>
- 3 new LivOS tiles authored with locked palettes/glyphs; Home/LiveUsage/AppStore repointed with `?v=285`.
- dock.test.tsx mock mirrors apps.tsx; dock.test passes.
- 5 now-orphan Umbrel PNGs deleted; new tiles + existing LivOS SVGs retained.
- Operator has approved the rendered tiles.
- `pnpm --filter ui build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-05-SUMMARY.md`
</output>
