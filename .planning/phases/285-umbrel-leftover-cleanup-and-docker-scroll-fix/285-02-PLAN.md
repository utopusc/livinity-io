---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/providers/apps.tsx
  - livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx
  - livos/packages/ui/src/modules/desktop/dock.test.tsx
  - livos/packages/ui/public/figma-exports/dock-chrome.png
  - livos/packages/ui/public/figma-exports/dock-preview.png
  - livos/packages/ui/public/figma-exports/dock-widgets.png
  - livos/packages/ui/public/figma-exports/app-facebook.png
  - livos/packages/ui/public/figma-exports/app-gmail.png
  - livos/packages/ui/public/figma-exports/app-whatsapp.png
  - livos/packages/ui/public/figma-exports/app-youtube.png
  - livos/packages/ui/public/figma-exports/dock-remote-desktop.png
autonomous: true
requirements: [Item-2]
must_haves:
  truths:
    - "The Devices and Schedules system apps show the LivOS dock-settings-new.svg icon (no Umbrel dock-settings.png)"
    - "The mobile Files / Settings / Server tabs show LivOS SVG icons (no Umbrel PNGs)"
    - "The 8 always-orphan Umbrel PNGs no longer exist in figma-exports"
    - "The dock.test.tsx mock string for Devices matches the new apps.tsx icon literal exactly"
  artifacts:
    - path: "livos/packages/ui/src/providers/apps.tsx"
      provides: "Devices + Schedules registry entries pointing at dock-settings-new.svg?v=285"
      contains: "dock-settings-new.svg?v=285"
    - path: "livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx"
      provides: "Mobile tab icons repointed to LivOS SVGs"
      contains: "dock-files-new.svg"
  key_links:
    - from: "apps.tsx Devices/Schedules icon literal"
      to: "dock.test.tsx:63 mock icon literal"
      via: "character-for-character match (mock mirrors production)"
      pattern: "dock-settings-new\\.svg\\?v=285"
---

<objective>
Repoint the live Umbrel PNG icons that have an EXISTING LivOS SVG replacement, and delete the always-orphan Umbrel PNGs (Item 2a). This is the safe half of Item 2 — no new art needed.

Purpose: Operator wants ALL Umbrel art gone. The Devices/Schedules system apps and the mobile Files/Settings/Server tabs still reference Umbrel `dock-settings.png` / `dock-files.png`; repoint them to the existing LivOS SVGs (`dock-settings-new.svg`, `dock-files-new.svg`, `dock-server.svg`). Separately, 8 Umbrel PNGs have ZERO importers (grep-verified in RESEARCH) and can be deleted now.

Output: Repointed icon strings (with `?v=285` cache-bust) in `apps.tsx`, `mobile-tab-bar.tsx`, the matching `dock.test.tsx` mock string, and 8 deleted orphan PNGs — all in ONE atomic commit. Gated by `pnpm --filter ui build` (exit 0) + `pnpm --filter ui test` (dock.test).

NOTE: Home / Live Usage / App Store icons are NOT touched here — they need NEW tiles (Plan 05 Item 2b). Their PNGs (`dock-home.png`, `dock-live-usage.png`, `dock-app-store.png`) and `dock-settings.png` / `dock-files.png` are deleted in Plan 05 AFTER all their importers are repointed (lock-step rule). Do NOT delete those 5 PNGs in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md

<interfaces>
<!-- apps.tsx repoint pattern (self-analog with ?v= cache-bust) — apps.tsx:143 already does this: -->
```ts
icon: '/figma-exports/dock-ai-chat.svg?v=chat_2026_06_02',
```
<!-- Existing LivOS SVGs available as repoint targets (all confirmed present): -->
<!--   dock-files-new.svg, dock-settings-new.svg, dock-server.svg -->
<!-- dock.test.tsx mock self-analog at :69 already mirrors a ?v= suffix: -->
```ts
'LIVINITY_liv-assistant': {name: 'Liv AI', icon: '/figma-exports/dock-ai-chat.svg?v=238_7', systemAppTo: '/liv-assistant'},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint Devices + Schedules icons in apps.tsx and mobile tab icons in mobile-tab-bar.tsx</name>
  <files>livos/packages/ui/src/providers/apps.tsx, livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx</files>
  <read_first>
    - livos/packages/ui/src/providers/apps.tsx — read :38-147 (the system-app registry). CONFIRM Devices (`LIVINITY_my-devices`) icon at :99 and Schedules (`LIVINITY_schedules`) icon at :107 are both `'/figma-exports/dock-settings.png'`. CONFIRM the cache-bust convention at :143 (`dock-ai-chat.svg?v=chat_2026_06_02`). Re-locate by `LIVINITY_my-devices` / `LIVINITY_schedules` if lines drift.
    - livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx — read the `TABS` array (:5-12). CONFIRM Files `appIcon` :7 = `dock-files.png`, Settings `appIcon` :8 = `dock-settings.png`, Server `appIcon` :11 = `dock-settings.png`.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §4 (ICON REPOINTS — exact target strings + cache-bust convention).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 2 (repoint map table).
  </read_first>
  <action>
    In `livos/packages/ui/src/providers/apps.tsx`:
    - Devices entry (`LIVINITY_my-devices`, icon currently at :99): change `icon: '/figma-exports/dock-settings.png',` → `icon: '/figma-exports/dock-settings-new.svg?v=285',`
    - Schedules entry (`LIVINITY_schedules`, icon currently at :107): change `icon: '/figma-exports/dock-settings.png',` → `icon: '/figma-exports/dock-settings-new.svg?v=285',`
    Do NOT touch Home (:42), Live Usage (:63), App Store (:72) — those stay on their PNGs until Plan 05 (new tiles). Do NOT touch :51 `systemAppTo: '/files/Home'`.

    In `livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx` (`TABS` array `appIcon` fields):
    - Files (:7): `appIcon: '/figma-exports/dock-files.png'` → `appIcon: '/figma-exports/dock-files-new.svg?v=285'`
    - Settings (:8): `appIcon: '/figma-exports/dock-settings.png'` → `appIcon: '/figma-exports/dock-settings-new.svg?v=285'`
    - Server (:11): `appIcon: '/figma-exports/dock-settings.png'` → `appIcon: '/figma-exports/dock-server.svg?v=285'`
    Leave the Docker tab (:10, already `dock-server.svg`) and any IconFolder/IconSettings/IconServer Tabler icon imports unchanged — only the `appIcon` path strings change.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dock-settings-new.svg?v=285" livos/packages/ui/src/providers/apps.tsx` returns 2 (Devices + Schedules).
    - `grep -c "dock-settings.png" livos/packages/ui/src/providers/apps.tsx` returns 0 (no live Umbrel settings PNG ref left in the registry; the only remaining occurrence is the comment at ~:86 which is `dock-settings.png` inside a comment — confirm by reading that the 2 ICON LINES :99/:107 no longer use it; if the grep returns 1 it must be the comment line, NOT an `icon:` line).
    - `grep -c "dock-files-new.svg?v=285" livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx` returns 1.
    - `grep -c "dock-settings-new.svg?v=285" livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx` returns 1.
    - `grep -c "dock-server.svg?v=285" livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx` returns 1.
    - `grep -c "dock-settings.png\|dock-files.png" livos/packages/ui/src/modules/mobile/mobile-tab-bar.tsx` returns 0.
    - `cd livos && pnpm --filter ui build` exits 0.
  </acceptance_criteria>
  <done>Devices/Schedules use dock-settings-new.svg?v=285; mobile Files/Settings/Server tabs use the LivOS SVGs with ?v=285; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update the dock.test.tsx Devices mock string in lock-step</name>
  <files>livos/packages/ui/src/modules/desktop/dock.test.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/desktop/dock.test.tsx — read the mock block :57-70. CONFIRM the Devices entry at :63 is `'LIVINITY_my-devices': {name: 'Devices', icon: '/figma-exports/dock-settings.png', systemAppTo: '/my-devices'},`. The comment at the top of the mock says it "mirrors production exactly" — keep it true.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §4 (dock.test.tsx mock lock-step — the `:63` Devices target). Note: Live Usage (:60) and App Store (:61) mock strings stay on their PNGs in THIS plan; they change in Plan 05 when the new tiles land. Home is NOT in this mock.
  </read_first>
  <action>
    In `livos/packages/ui/src/modules/desktop/dock.test.tsx`, change the Devices mock entry (currently at :63):
    ```ts
    'LIVINITY_my-devices': {name: 'Devices', icon: '/figma-exports/dock-settings.png', systemAppTo: '/my-devices'},
    ```
    to:
    ```ts
    'LIVINITY_my-devices': {name: 'Devices', icon: '/figma-exports/dock-settings-new.svg?v=285', systemAppTo: '/my-devices'},
    ```
    This matches the new `apps.tsx` Devices icon literal CHARACTER-FOR-CHARACTER. Do NOT change Live Usage (:60), App Store (:61), Files (:58, already dock-files-new.svg), Settings (:59), or any other mock entry in this plan.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui test -- dock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dock-settings-new.svg?v=285" livos/packages/ui/src/modules/desktop/dock.test.tsx` returns 1.
    - The dock.test.tsx Devices mock no longer references `dock-settings.png`: `grep -c "'LIVINITY_my-devices'.*dock-settings.png" livos/packages/ui/src/modules/desktop/dock.test.tsx` returns 0.
    - `cd livos && pnpm --filter ui test -- dock` passes (dock.test.tsx green).
  </acceptance_criteria>
  <done>The dock.test.tsx Devices mock string mirrors the new apps.tsx icon literal; dock.test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the 8 always-orphan Umbrel PNGs</name>
  <files>livos/packages/ui/public/figma-exports/dock-chrome.png, livos/packages/ui/public/figma-exports/dock-preview.png, livos/packages/ui/public/figma-exports/dock-widgets.png, livos/packages/ui/public/figma-exports/app-facebook.png, livos/packages/ui/public/figma-exports/app-gmail.png, livos/packages/ui/public/figma-exports/app-whatsapp.png, livos/packages/ui/public/figma-exports/app-youtube.png, livos/packages/ui/public/figma-exports/dock-remote-desktop.png</files>
  <read_first>
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 2 "Orphan-PNG delete list" — all 8 confirmed ZERO importers via grep across `ui/src`.
    - Before deleting, RE-VERIFY zero importers (line numbers/usage drift): run the grep in the action below and confirm 0 hits for each.
  </read_first>
  <action>
    FIRST re-verify zero importers (defense against drift), then delete. From repo root:
    ```bash
    cd "$(git rev-parse --show-toplevel)"
    for f in dock-chrome dock-preview dock-widgets app-facebook app-gmail app-whatsapp app-youtube dock-remote-desktop; do
      echo "=== $f.png importers ==="
      grep -rn "$f.png" livos/packages/ui/src || echo "0 importers"
    done
    ```
    Each MUST report "0 importers". If ANY reports an importer, STOP and report it — do NOT delete that file (it is not actually orphaned).
    Then delete the 8 PNGs:
    ```bash
    cd "$(git rev-parse --show-toplevel)"
    git rm livos/packages/ui/public/figma-exports/dock-chrome.png \
           livos/packages/ui/public/figma-exports/dock-preview.png \
           livos/packages/ui/public/figma-exports/dock-widgets.png \
           livos/packages/ui/public/figma-exports/app-facebook.png \
           livos/packages/ui/public/figma-exports/app-gmail.png \
           livos/packages/ui/public/figma-exports/app-whatsapp.png \
           livos/packages/ui/public/figma-exports/app-youtube.png \
           livos/packages/ui/public/figma-exports/dock-remote-desktop.png
    ```
    Do NOT delete `dock-home.png`, `dock-live-usage.png`, `dock-app-store.png`, `dock-settings.png`, `dock-files.png` — those still have importers (or are repointed only in Plan 05). Do NOT delete any `migrate-*`, `system-livinity-home.png`, `livinity-*`, or `docker-app-icon.png` — those are LivOS, KEEP.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 names, `grep -rc "<name>.png" livos/packages/ui/src` returns 0 (no remaining importer).
    - For each of the 8 names, `test -f livos/packages/ui/public/figma-exports/<name>.png` returns non-zero (file gone).
    - `test -f livos/packages/ui/public/figma-exports/dock-home.png` succeeds (NOT deleted here).
    - `test -f livos/packages/ui/public/figma-exports/system-livinity-home.png` succeeds (LivOS asset preserved).
    - `cd livos && pnpm --filter ui build` exits 0 (no dangling import to a deleted PNG).
  </acceptance_criteria>
  <done>The 8 orphan Umbrel PNGs are removed; the 5 still-referenced PNGs and all LivOS assets remain; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Icon-path string repoints + static asset deletions. No new code path, no input parsing, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-02 | — | apps.tsx / mobile-tab-bar.tsx / dock.test.tsx / orphan PNGs | accept | No new attack surface — removal/repoint of static icon references only. Build gate confirms no dangling asset import. |
</threat_model>

<verification>
- `cd livos && pnpm --filter ui build` exits 0 (catches a deleted PNG that still has an importer, or a dangling SVG ref).
- `cd livos && pnpm --filter ui test -- dock` passes (dock.test mock string lock-step).
- Manual grep checks per acceptance_criteria confirm zero remaining Umbrel `dock-settings.png` / `dock-files.png` refs in the touched files and zero importers for the 8 deleted PNGs.
</verification>

<success_criteria>
- Devices + Schedules + mobile Files/Settings/Server tabs use LivOS SVGs with `?v=285`.
- dock.test.tsx Devices mock mirrors the new apps.tsx literal; dock.test passes.
- 8 orphan Umbrel PNGs deleted; 5 still-referenced PNGs + all LivOS assets retained.
- `pnpm --filter ui build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-02-SUMMARY.md`
</output>
