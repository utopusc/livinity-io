---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/docker/resources/container-section.tsx
autonomous: false
requirements: [Item-5]
must_haves:
  truths:
    - "Operator can scroll the full list of Docker containers on the desktop containers table (rows below the fold are reachable)"
    - "The containers table width and rounded-corner styling are not visibly regressed"
    - "The mobile container card stack still scrolls (unchanged path)"
  artifacts:
    - path: "livos/packages/ui/src/routes/docker/resources/container-section.tsx"
      provides: "Desktop containers table-container that no longer hard-clips overflow"
      contains: "rounded-xl border border-border-default"
  key_links:
    - from: "container-section.tsx:401 table-container div"
      to: "container-section.tsx:234 wrapper (flex h-full flex-col overflow-y-auto)"
      via: "removing overflow-hidden so the wrapper's overflow-y-auto detects overflow and scrolls"
      pattern: "overflow"
---

<objective>
Fix the Docker containers desktop table so it scrolls instead of hard-clipping rows below the fold (Item 5).

Purpose: The desktop containers table-container div at `container-section.tsx:401` has `overflow-hidden` and no `min-h-0`/`flex-1`. As a flex child of the `flex-col overflow-y-auto` wrapper at `:234`, `overflow-hidden` hard-clips rows that exceed the available flex height; because the content is clipped (not overflowing), the wrapper's `overflow-y-auto` never detects overflow → rows below the fold are unreachable. This is the classic flexbox "min-height:auto + overflow-hidden clips instead of scrolls" trap.

Output: A one-line CSS class change at `container-section.tsx:401`, verified by `pnpm --filter ui build` (exit 0) and an operator visual scroll check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md

<interfaces>
<!-- The flex chain (outer → inner), confirmed in RESEARCH §Item 5 and PATTERNS §5. -->
<!-- Working analog scroll container — routes/docker/docker-app.tsx:51 (parent that mounts SectionView): -->
```tsx
<main className='flex min-w-0 flex-1 flex-col overflow-hidden'>
  ...
  <div className='min-h-0 flex-1 overflow-auto'>   {/* working scroll container */}
    <SectionView section={section} />
  </div>
```
<!-- container-section.tsx:234 wrapper (a flex column whose overflow-y-auto should scroll): -->
```tsx
<div className='flex h-full flex-col overflow-y-auto p-4 sm:p-6'>
```
<!-- container-section.tsx:401 BROKEN desktop table-container (direct flex child of the :234 wrapper): -->
```tsx
<div className='rounded-xl border border-border-default bg-surface-base overflow-hidden'>
  <Table> … </Table>
</div>
```
<!-- container-section.tsx:318 mobile path (space-y-2 card stack) — scrolls at parent, MUST stay unaffected. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove overflow-clipping on the desktop containers table-container so the wrapper scrolls</name>
  <files>livos/packages/ui/src/routes/docker/resources/container-section.tsx</files>
  <read_first>
    - livos/packages/ui/src/routes/docker/resources/container-section.tsx — read the full flex chain: the wrapper at :234 (`flex h-full flex-col overflow-y-auto p-4 sm:p-6`), the mobile path at ~:318 (`space-y-2`), and the desktop table-container at :401. CONFIRM line 401 still reads `<div className='rounded-xl border border-border-default bg-surface-base overflow-hidden'>` (line numbers drift — re-locate by the literal class string if needed).
    - livos/packages/ui/src/routes/docker/docker-app.tsx — read :46-53 to confirm the parent scroll chain (`min-h-0 flex-1 overflow-auto`) the analog relies on.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 5 (flexbox diagnosis + recommended fix A).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §5 (DOCKER SCROLL FIX analog).
  </read_first>
  <action>
    At `container-section.tsx:401`, the desktop table-container div currently is:
    ```tsx
    <div className='rounded-xl border border-border-default bg-surface-base overflow-hidden'>
    ```
    RESEARCH-preferred fix (least likely to disturb table width): replace `overflow-hidden` with `overflow-x-hidden overflow-y-auto` so the rounded-corner horizontal clipping is preserved but vertical content can scroll. New line:
    ```tsx
    <div className='rounded-xl border border-border-default bg-surface-base overflow-x-hidden overflow-y-auto'>
    ```
    Rationale: keeping `overflow-x-hidden` preserves the `rounded-xl` corner clip on the horizontal axis (cosmetic) while `overflow-y-auto` makes the container the vertical scroll region. This is the hybrid RESEARCH recommended.
    Do NOT touch the wrapper at :234, the mobile path at ~:318, or the `<Table>` markup. CSS-only, no logic change. Change EXACTLY this one className string — nothing else in the file.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `cd livos && pnpm --filter ui build` exits 0.
    - `grep -c "bg-surface-base overflow-hidden" livos/packages/ui/src/routes/docker/resources/container-section.tsx` returns 0 (the desktop-table `overflow-hidden` string is gone).
    - `grep -c "bg-surface-base overflow-x-hidden overflow-y-auto" livos/packages/ui/src/routes/docker/resources/container-section.tsx` returns 1.
    - The wrapper at :234 still reads `flex h-full flex-col overflow-y-auto` (unchanged): `grep -c "flex h-full flex-col overflow-y-auto" livos/packages/ui/src/routes/docker/resources/container-section.tsx` returns 1.
  </acceptance_criteria>
  <done>The desktop table-container at :401 uses `overflow-x-hidden overflow-y-auto`; the UI build is clean; the wrapper and mobile path are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visually verifies Docker containers list scrolls</name>
  <files>(none — checkpoint)</files>
  <what-built>The desktop Docker containers table-container no longer hard-clips overflow; it now uses `overflow-x-hidden overflow-y-auto` so the long container list scrolls.</what-built>
  <how-to-verify>
    1. With the UI built (`pnpm --filter ui build` already ran), open the Docker app (the LivOS desktop Docker window) on a box / dev environment that has MORE containers than fit on screen.
    2. Confirm you can scroll DOWN through the entire desktop containers table and reach the rows that were previously below the fold.
    3. Confirm the table WIDTH and the rounded-corner border look the same as before (no horizontal scrollbar appearing, corners still rounded).
    4. Switch to a narrow/mobile viewport: confirm the mobile container card stack still scrolls normally (this path was untouched).
  </how-to-verify>
  <action>This is a human-verify checkpoint — no automated implementation. The executor pauses and asks the operator to perform the "how-to-verify" steps after Task 1's build passes.</action>
  <verify>Operator performs the how-to-verify steps; no automated command.</verify>
  <acceptance_criteria>
    - Operator confirms the full desktop containers list is scrollable (all rows reachable).
    - Operator confirms no visible width/border regression on desktop and mobile still scrolls.
  </acceptance_criteria>
  <done>Operator has confirmed the containers list scrolls with no visual regression.</done>
  <resume-signal>Type "approved" if scroll works and no visual regression, or describe the issue (e.g. width changed, corners broke, mobile regressed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This change is a CSS-only Tailwind class edit on an existing rendered component. No data, input, or trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-01 | — | container-section.tsx (CSS only) | accept | No new attack surface — CSS-only class change, no input parsing, no new code path, no data flow. |
</threat_model>

<verification>
- `cd livos && pnpm --filter ui build` exits 0 (catches any accidental syntax error).
- Operator visual scroll verification (Task 2 checkpoint).
- No tsc gate (UI-only); no test edit (no test asserts this className).
</verification>

<success_criteria>
- Desktop containers table scrolls fully; rows below the fold are reachable.
- Table width and rounded corners visually unchanged; mobile card stack still scrolls.
- `pnpm --filter ui build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-01-SUMMARY.md`
</output>
