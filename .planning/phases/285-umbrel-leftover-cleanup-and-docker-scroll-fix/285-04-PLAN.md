---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/startup-migrations/index.ts
  - livos/packages/livinityd/source/modules/startup-migrations/startup-migrations.integration.test.ts
  - livos/packages/ui/src/routes/notifications.tsx
autonomous: true
requirements: [Item-3]
must_haves:
  truths:
    - "On boot, livinityd no longer runs migrateBackThatMacUpPort() and never adds the migrated-back-that-mac-up notification"
    - "The UI no longer has copy for the Back That Mac Up / Time Machine notice (the dispatch branch + content function are gone)"
    - "The generic AlertDialog notification render loop still renders backups-failing and livos-updated notices"
    - "The integration test no longer references the deleted migrateBackThatMacUpPort method"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/startup-migrations/index.ts"
      provides: "start() without the back-that-mac-up migration block; all other migrations intact"
      contains: "migrateDownloadsDirectory"
    - path: "livos/packages/ui/src/routes/notifications.tsx"
      provides: "Notifications route with the generic render loop + backups-failing + livos-updated intact"
      contains: "getBackupFailingContent"
  key_links:
    - from: "startup-migrations/index.ts start()"
      to: "removed migrateBackThatMacUpPort() method + call block"
      via: "deleting method :117-137 and call block :179-184 while keeping all sibling migrations"
      pattern: "migrateBackThatMacUpPort"
---

<objective>
Remove the "Back That Mac Up" / Time Machine notification (Item 3) — backend trigger, UI copy, and its integration test — without touching the shared notification machinery.

Purpose: The operator sees a global AlertDialog overlay (conflated with the Ctrl+K palette) that says the legacy `back-that-mac-up` Samba port was migrated. It is generated by `migrateBackThatMacUpPort()` on boot and rendered by `getMigratedBackThatMacUpContent()`. The operator does NOT run the legacy app → safe to remove (RESEARCH confirmed the 445→1445 remap only matters if the app is installed).

Output: Backend method + call block deleted (`startup-migrations/index.ts`); UI content function + dispatch branch deleted (`notifications.tsx`); the entire integration test block deleted (`startup-migrations.integration.test.ts:43-88`). All in ONE commit (lock-step). Gated by `pnpm --filter ui build` (UI) + livinityd `tsc --noEmit` (≤305 total, `startup-migrations/index.ts` stays at 0 errors).

⚠️ MUST NOT BREAK: Keep `<Notifications/>`, the generic AlertDialog render loop (`notifications.tsx:223-245`), `getBackupFailingContent`, the backups query, the `livos-updated` auto-clear, and ALL other `start()` migration blocks (activateImportedDataDirectory, migrateLegacyData, migrateLegacyLinuxData, migrateDownloadsDirectory, version write, the `livos-updated` add). Do NOT touch the legitimate Time Machine i18n strings (`public/locales/en.json:537-542`) or `macos-instructions.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md

<interfaces>
<!-- startup-migrations/index.ts — the start() body has SEVERAL try/catch migration blocks. -->
<!-- KEEP all of them EXCEPT the back-that-mac-up block (:179-184). The method is at :117-137; -->
<!-- it ends with `await this.livinityd.notifications.add('migrated-back-that-mac-up')` at :136. -->
<!-- notifications.tsx dispatch (:215-217) — the branch to delete: -->
```ts
if (notification === 'migrated-back-that-mac-up') {
  return getMigratedBackThatMacUpContent()
}
```
<!-- notifications.tsx getMigratedBackThatMacUpContent() (:154-160) — a hard-coded English string (NOT i18n) → -->
<!-- deleting the function removes all its copy; no en.json edit needed. -->
<!-- KEEP: getBackupFailingContent (:101-149), backups query (:183-185), livos-updated auto-clear (:191-198), -->
<!--       the generic render loop (:223-245), getDefaultNotificationContent (:165-170). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture livinityd tsc baseline (must stay ≤305; startup-migrations/index.ts at 0)</name>
  <files>(none — measurement only)</files>
  <read_first>
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Verification Strategy "Baselines" — livinityd `tsc --noEmit` = exactly 305 errors; `startup-migrations/index.ts` = 0 baseline errors.
  </read_first>
  <action>
    BEFORE editing anything, capture the baseline so "no new errors" is measurable:
    ```bash
    cd "$(git rev-parse --show-toplevel)/livos"
    pnpm --filter livinityd exec tsc --noEmit 2>&1 | tee /tmp/285-tsc-baseline.txt | grep -c "error TS" || echo "0"
    # And isolate errors in the file we're about to touch:
    grep -c "startup-migrations/index.ts" /tmp/285-tsc-baseline.txt || echo "0"
    ```
    Record both numbers in the SUMMARY. Expected: total ≈ 305 (CONTEXT/RESEARCH baseline), `startup-migrations/index.ts` = 0. If the total is materially different from 305, note it — the gate becomes "≤ the captured baseline, no new errors in touched files" rather than a hard 305.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "baseline captured"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/285-tsc-baseline.txt` exists and the total `error TS` count is recorded (expected ≈305).
    - The count of errors referencing `startup-migrations/index.ts` in that file is recorded (expected 0).
  </acceptance_criteria>
  <done>Baseline tsc error count (total + startup-migrations/index.ts) captured for the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Delete migrateBackThatMacUpPort() + its start() call block + the integration test block</name>
  <files>livos/packages/livinityd/source/modules/startup-migrations/index.ts, livos/packages/livinityd/source/modules/startup-migrations/startup-migrations.integration.test.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/startup-migrations/index.ts — read :110-205. CONFIRM: the method `async migrateBackThatMacUpPort()` is at :117-137 (ends with `notifications.add('migrated-back-that-mac-up')` at :136). The `start()` call block is at :179-184 (a try/catch calling `await this.migrateBackThatMacUpPort()` at :181). Note the SIBLING blocks to KEEP: activateImportedDataDirectory (:159-163), migrateLegacyData (:166-170), migrateLegacyLinuxData (:173-177), migrateDownloadsDirectory (:187-191), version write + `livos-updated` add (:193-201).
    - livos/packages/livinityd/source/modules/startup-migrations/startup-migrations.integration.test.ts — read :40-90. CONFIRM the test block `test('Back That Mac Up app port is migrated from 445 to 1445', async () => { ... })` spans :43-88 (the assertion `expect(notifications.includes('migrated-back-that-mac-up')).toBe(true)` is at :87).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 3 (file:line table + MUST-NOT-BREAK + Pitfall 2) and §PATTERNS "Removals (Items 3 & 4)".
  </read_first>
  <action>
    In `livos/packages/livinityd/source/modules/startup-migrations/index.ts`:
    1. Delete the ENTIRE method `async migrateBackThatMacUpPort() { ... }` (currently :117-137 — from the method signature line through its closing brace, including any leading doc-comment that belongs ONLY to this method).
    2. Delete the `start()` call block (currently :179-184) — the try/catch that contains `await this.migrateBackThatMacUpPort()`. Delete the whole try/catch wrapper for it, not just the call line, so no empty `try {} catch {}` remains.
    Do NOT touch any sibling migration block. Do NOT touch the `livos-updated` add at :200. Re-locate by the method/string names if line numbers drift.

    In `livos/packages/livinityd/source/modules/startup-migrations/startup-migrations.integration.test.ts`:
    3. Delete the ENTIRE test block `test('Back That Mac Up app port is migrated from 445 to 1445', async () => { ... })` (currently :43-88) — the whole `test(...)` call including its closing `})`. Do NOT delete neighboring tests. Verify no other test references `migrateBackThatMacUpPort` or `migrated-back-that-mac-up` after deletion.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "migrateBackThatMacUpPort" livos/packages/livinityd/source` returns 0.
    - `grep -rc "migrated-back-that-mac-up" livos/packages/livinityd/source` returns 0.
    - tsc total error count is ≤ the captured baseline (≤305) AND there are 0 errors referencing `startup-migrations/index.ts`: `cd livos && pnpm --filter livinityd exec tsc --noEmit 2>&1 | grep -c "startup-migrations/index.ts"` returns 0.
    - The sibling migrations survive: `grep -c "migrateDownloadsDirectory" livos/packages/livinityd/source/modules/startup-migrations/index.ts` is unchanged (still present); `grep -c "migrateLegacyLinuxData" livos/packages/livinityd/source/modules/startup-migrations/index.ts` still present.
    - No empty `try {` immediately followed by `} catch` with no body was left behind (read the start() body to confirm clean structure).
  </acceptance_criteria>
  <done>The backend method, its call block, and the integration test block are deleted; sibling migrations intact; tsc clean (≤baseline, startup-migrations at 0).</done>
</task>

<task type="auto">
  <name>Task 3: Delete the UI content function + dispatch branch (keep the generic render loop)</name>
  <files>livos/packages/ui/src/routes/notifications.tsx</files>
  <read_first>
    - livos/packages/ui/src/routes/notifications.tsx — read :95-250. CONFIRM: `getMigratedBackThatMacUpContent()` is at :154-160 (a function returning a NotificationContent with a hard-coded English string, NOT an i18n key). The dispatch branch `if (notification === 'migrated-back-that-mac-up') { return getMigratedBackThatMacUpContent() }` is at :215-217. The GENERIC render loop (renders ALL notifications) is at :223-245 — DO NOT DELETE. `getBackupFailingContent` (:101-149), backups query (:183-185), `livos-updated` auto-clear (:191-198), and `getDefaultNotificationContent` (:165-170) — KEEP.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 3 (Pitfall 1 — do not delete the generic render loop) + PATTERNS "Removals".
  </read_first>
  <action>
    In `livos/packages/ui/src/routes/notifications.tsx`:
    1. Delete the ENTIRE function `getMigratedBackThatMacUpContent(): NotificationContent { ... }` (currently :154-160 — signature through closing brace).
    2. Delete ONLY the dispatch branch (currently :215-217):
    ```ts
    if (notification === 'migrated-back-that-mac-up') {
      return getMigratedBackThatMacUpContent()
    }
    ```
    Do NOT delete the generic AlertDialog render loop (:223-245). Do NOT delete `getBackupFailingContent`, the `backups-failing` dispatch branch, the backups query, the `livos-updated` auto-clear, or `getDefaultNotificationContent`. Re-locate by the function/string names if line numbers drift. No `en.json` edit is needed (the copy was a hard-coded literal in the deleted function).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getMigratedBackThatMacUpContent" livos/packages/ui/src/routes/notifications.tsx` returns 0.
    - `grep -c "migrated-back-that-mac-up" livos/packages/ui/src/routes/notifications.tsx` returns 0.
    - The generic render loop + backups + livos-updated survive: `grep -c "getBackupFailingContent" livos/packages/ui/src/routes/notifications.tsx` is unchanged (still present); `grep -c "livos-updated" livos/packages/ui/src/routes/notifications.tsx` still present; `grep -c "getDefaultNotificationContent" livos/packages/ui/src/routes/notifications.tsx` still present.
    - The legitimate Time Machine i18n strings are UNTOUCHED: `grep -c "time-machine" livos/packages/ui/public/locales/en.json` is unchanged (en.json is NOT in this plan's files_modified — must be byte-identical).
    - `cd livos && pnpm --filter ui build` exits 0.
  </acceptance_criteria>
  <done>The UI content function + dispatch branch are removed; the generic render loop, backups-failing, and livos-updated notices are intact; i18n strings untouched; build clean.</done>
</task>

<task type="auto">
  <name>Task 4: Run the integration test in WSL livos-itest if available; else record tsc+grep fallback</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Verification Strategy (the integration suite needs Linux D-Bus; a `livos-itest` WSL distro exists; Windows cannot run it).
  </read_first>
  <action>
    The `startup-migrations.integration.test.ts` suite needs Linux D-Bus. Attempt to run it in the `livos-itest` WSL distro:
    ```bash
    wsl -d livos-itest -- bash -lc 'cd /opt/livos && pnpm --filter livinityd test startup-migrations.integration' 2>&1 | tail -30 || echo "WSL livos-itest unavailable"
    ```
    IF the WSL distro runs it: confirm the remaining tests pass and there is NO reference to the deleted `'Back That Mac Up...'` test.
    IF the WSL distro is unavailable / fails to launch (Windows-only environment): the acceptance per CONTEXT/RESEARCH is "tsc clean + the test block is deleted (no dangling reference to the removed method)". Record in the SUMMARY which path was taken.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd exec tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - EITHER: WSL `livos-itest` ran the integration suite and remaining tests pass (no `'Back That Mac Up'` test, no failure referencing `migrateBackThatMacUpPort`).
    - OR (Windows-only fallback): tsc total ≤ baseline AND `grep -rc "migrateBackThatMacUpPort\|migrated-back-that-mac-up" livos/packages/livinityd/source` returns 0 (no dangling reference). The chosen path is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The integration test outcome is verified (WSL run) or the tsc+grep fallback is documented; no dangling reference to the removed method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| livinityd boot (start()) | A boot-time migration is removed; no new boundary is introduced. The removed code only rewrote a compose port + added a notification — deleting it removes behavior, adds none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-04 | — | startup-migrations/index.ts, notifications.tsx | accept | No new attack surface — removal of a boot migration + its UI copy. The generic notification render loop (the shared mechanism) is explicitly preserved. tsc + ui build gates confirm no dangling references. |
| T-285-05 | I (Info Disclosure) | already-affected boxes' persisted notification ID | accept | A box that already stored `migrated-back-that-mac-up` falls through to `getDefaultNotificationContent` and shows the raw ID once until OK is clicked. Operator confirmed the legacy app is not used → near-zero impact. No remote/attacker vector. |
</threat_model>

<verification>
- `cd livos && pnpm --filter livinityd exec tsc --noEmit` total ≤ captured baseline (≤305) AND 0 errors in `startup-migrations/index.ts`.
- `cd livos && pnpm --filter ui build` exits 0.
- `grep -rc "migrateBackThatMacUpPort\|migrated-back-that-mac-up"` across livinityd source + notifications.tsx returns 0.
- Shared machinery (generic render loop, backups-failing, livos-updated, sibling migrations, i18n Time Machine strings) confirmed unchanged via grep.
- Integration test run in WSL `livos-itest` if available; else tsc+grep fallback documented.
</verification>

<success_criteria>
- migrateBackThatMacUpPort() + its start() call block + the UI content function + dispatch branch + the integration test block are all removed.
- The shared notification render loop, backups-failing, livos-updated, all sibling migrations, and the legitimate Time Machine i18n strings are intact.
- tsc ≤ baseline (startup-migrations at 0); ui build exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-04-SUMMARY.md`
</output>
