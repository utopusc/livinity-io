---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 06
type: execute
wave: 3
depends_on: ["02", "05"]
files_modified:
  - livos/packages/ui/src/components/apple-spotlight.tsx
  - livos/packages/ui/src/components/cmdk.tsx
  - livos/packages/ui/src/router.tsx
  - livos/packages/ui/src/features/files/routes.tsx
  - livos/packages/ui/src/features/files/index.tsx
  - livos/packages/ui/src/modules/desktop/dock.test.tsx
autonomous: true
requirements: [Item-1]
must_haves:
  truths:
    - "Clicking Files in the apple-spotlight (live Search palette) opens Files as a LivOS window WITHOUT changing the browser URL to /files/Home"
    - "The full-page /files route is no longer mounted; navigating to /files no longer shows the Umbrel full-page 3-col layout"
    - "The windowed Files surface (FilesWindowContent) still opens for dock/launchpad/desktop-folder/mobile (unchanged)"
    - "apps.tsx systemAppTo:'/files/Home' and system-windowed-routes.ts still feed the windowed initialRoute (kept)"
  artifacts:
    - path: "livos/packages/ui/src/components/apple-spotlight.tsx"
      provides: "Search palette Files actions repointed to windowManager.openWindow"
      contains: "useWindowManagerOptional"
    - path: "livos/packages/ui/src/router.tsx"
      provides: "Router without the full-page filesRoutes mount"
      min_lines: 100
  key_links:
    - from: "apple-spotlight.tsx Files/Recents/Apps/Trash actions"
      to: "windowManager.openWindow('LIVINITY_files', route, 'Files', icon)"
      via: "useWindowManagerOptional() added; navigate() replaced for the 4 Files actions"
      pattern: "openWindow\\('LIVINITY_files'"
    - from: "router.tsx"
      to: "removed full-page /files route"
      via: "deleting the filesRoutes import + the ...filesRoutes spread; deleting routes.tsx + index.tsx"
      pattern: "filesRoutes"
---

<objective>
Remove the full-page `/files` URL route and repoint the two command palettes to open Files as a LivOS window (Item 1a — the route-removal + palette half of Item 1).

Purpose (Option A, LOCKED): Clicking Files must NOT change the browser URL to `/files/Home` or show the umbrelOS full-page 3-col layout. The windowed Files (`FilesWindowContent`) already exists and is self-contained (verified: it imports the shared listing components directly, NOT `filesRoutes`/`FilesLayout`). So remove the full-page route + its wrapper, and repoint the only two browser-`navigate()` consumers (the palettes). RESEARCH confirmed OwnCloud deep-linking is NOT a wired consumer (only a stale comment) → the route can be removed cleanly.

Output: `apple-spotlight.tsx` (live Search palette) + `cmdk.tsx` (dead/Docker palette) repointed to `windowManager.openWindow('LIVINITY_files', …)`; `router.tsx` `filesRoutes` import + `...filesRoutes` spread removed; `features/files/routes.tsx` + `features/files/index.tsx` (the full-page wrapper) deleted; `dock.test.tsx` default-pins re-checked. ONE commit. Gated by `pnpm --filter ui build` (CRITICAL — catches the dangling FilesLayout import) + `pnpm --filter ui test`.

DEPENDS ON Plans 02 + 05: those settle the `dock.test.tsx` mock; this plan only re-checks the default-pins assertion there (likely no edit). The 3 backups deep-links (setup-wizard/restore-wizard/backups-mobile-drawer) are repointed SEPARATELY in Plan 07 (Item 1b) because they need the dialog-auto-open bridge — keep the full-page route's deep-link behavior OUT of scope here; the route removal does not 404 them because Plan 07 lands in the same phase/wave-after and repoints them to the window. (Order: this plan removes the route; Plan 07 — Wave 4 — adds the bridge + repoints the 3 deep-links. Between commits the 3 deep-links would hit a removed route, so the phase must ship 06 and 07 together; see depends_on / wave.)

KEEP: `apps.tsx:51` `systemAppTo:'/files/Home'`, `system-windowed-routes.ts:6` (`LIVINITY_files='/files/Home'`), `FilesWindowContent`, the shared `features/files/components/sidebar`, and all `features/files/components/*` listing modules — they power the windowed surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md

<interfaces>
<!-- CANONICAL openWindow analog — desktop-folder.tsx:87-101: -->
```ts
import {useWindowManagerOptional} from '@/providers/window-manager'   // export confirmed at window-manager.tsx:585
import {systemAppsKeyed} from '@/providers/apps'
const windowManager = useWindowManagerOptional()
const handleOpen = () => {
  const route = `/files/Home/${encodeURIComponent(name)}`
  const filesIcon = systemAppsKeyed['LIVINITY_files']?.icon || ''
  if (windowManager) {
    windowManager.openWindow('LIVINITY_files', route, name, filesIcon)
  }
}
```
<!-- openWindow signature: openWindow(appId, route, name, icon). -->
<!-- FilesWindowContent strips '/files' from initialRoute (files-content.tsx:45-48) → deep paths fine. -->
<!-- apple-spotlight.tsx current state: imports useNavigate (:5), systemAppsKeyed (:15); does NOT import any -->
<!--   window-manager hook (must ADD useWindowManagerOptional). navigate is :208. Files actions: -->
<!--   :284 Files (navigate(lastFilesPath || systemAppsKeyed['LIVINITY_files'].systemAppTo)) -->
<!--   :291 Recents (navigate(`/files${FILES_RECENTS_PATH}`)) -->
<!--   :297 Apps    (navigate(`/files${FILES_APPS_PATH}`)) -->
<!--   :303 Trash   (navigate(`/files${FILES_TRASH_PATH}`)) -->
<!--   KEEP navigate for Home (:276) and all Settings actions — NOT Files, out of scope. -->
<!-- cmdk.tsx ALREADY imports useWindowManagerOptional (:23) and has `const windowManager = useWindowManagerOptional()` -->
<!--   (:94). Files navigate :163, Recents :173, Apps :183, Trash :193. -->
<!-- router.tsx: import {filesRoutes} from '@/features/files/routes' (:9); ...filesRoutes spread inside SheetLayout (:147); -->
<!--   OwnCloud stale comment at :167 (remove the comment too since the route is gone). -->
<!-- DELETION SAFETY (verified): filesRoutes is imported ONLY by router.tsx; '@/features/files' (the Files/FilesLayout -->
<!--   full-page wrapper) is imported ONLY by routes.tsx (lazy import). FilesWindowContent does NOT import either. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint apple-spotlight Files/Recents/Apps/Trash actions to windowManager.openWindow</name>
  <files>livos/packages/ui/src/components/apple-spotlight.tsx</files>
  <read_first>
    - livos/packages/ui/src/components/apple-spotlight.tsx — read :1-20 (imports) and :200-310 (the actions array). CONFIRM: imports `useNavigate` (:5) + `systemAppsKeyed` (:15) but NO window-manager hook (grep `windowManager`/`openWindow` → 0). `const navigate = useNavigate()` at :208. Files action :283-285, Recents :291, Apps :297, Trash :303. Home at :276 + all Settings actions stay on `navigate`.
    - livos/packages/ui/src/modules/desktop/desktop-folder.tsx — read :85-101 (the canonical `useWindowManagerOptional()` + `openWindow` analog).
    - livos/packages/ui/src/providers/window-manager.tsx — confirm `useWindowManagerOptional` is exported (:585).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §3 (PALETTE REPOINT — openWindow signature + apple-spotlight current state).
  </read_first>
  <action>
    In `livos/packages/ui/src/components/apple-spotlight.tsx`:
    1. Add the import (alongside the existing imports near :15): `import {useWindowManagerOptional} from '@/providers/window-manager'`
    2. Inside the component, after `const navigate = useNavigate()` (:208), add: `const windowManager = useWindowManagerOptional()`
    3. Repoint the 4 Files actions to open the window (keep `navigate` as the fallback when windowManager is null, mirroring desktop-folder's `if (windowManager)` guard). Use the Files icon from `systemAppsKeyed['LIVINITY_files']?.icon`:
       - Files (:283-285): replace `navigate(lastFilesPath || systemAppsKeyed['LIVINITY_files'].systemAppTo)` with:
         ```ts
         const target = lastFilesPath || systemAppsKeyed['LIVINITY_files'].systemAppTo
         const filesIcon = systemAppsKeyed['LIVINITY_files']?.icon || ''
         if (windowManager) windowManager.openWindow('LIVINITY_files', target, 'Files', filesIcon)
         else navigate(target)
         ```
       - Recents (:291): replace `navigate(\`/files${FILES_RECENTS_PATH}\`)` with:
         ```ts
         windowManager
           ? windowManager.openWindow('LIVINITY_files', `/files${FILES_RECENTS_PATH}`, 'Files', systemAppsKeyed['LIVINITY_files']?.icon || '')
           : navigate(`/files${FILES_RECENTS_PATH}`)
         ```
       - Apps (:297): same pattern with `FILES_APPS_PATH`.
       - Trash (:303): same pattern with `FILES_TRASH_PATH`.
    Do NOT change Home (:276) or any Settings action — they stay `navigate(...)`. `lastFilesPath` is already a `/files/...`-form string → passes straight through `FilesWindowContent`'s `/files` strip.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useWindowManagerOptional" livos/packages/ui/src/components/apple-spotlight.tsx` returns 2 (the import + the call).
    - `grep -c "openWindow('LIVINITY_files'" livos/packages/ui/src/components/apple-spotlight.tsx` returns 4 (Files + Recents + Apps + Trash).
    - Home/Settings still use navigate: `grep -c "systemAppsKeyed\['LIVINITY_home'\].systemAppTo" livos/packages/ui/src/components/apple-spotlight.tsx` is unchanged (the Home action line still calls navigate).
    - `cd livos && pnpm --filter ui build` exits 0.
  </acceptance_criteria>
  <done>apple-spotlight opens Files (+ Recents/Apps/Trash) as a window via windowManager, with navigate fallback; Home/Settings untouched; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint cmdk.tsx Files/Recents/Apps/Trash actions to windowManager.openWindow</name>
  <files>livos/packages/ui/src/components/cmdk.tsx</files>
  <read_first>
    - livos/packages/ui/src/components/cmdk.tsx — read :1-30 (imports) and :88-200. CONFIRM: it ALREADY imports `useWindowManagerOptional` (:23) and has `const windowManager = useWindowManagerOptional()` (:94). Files navigate :163, Recents :173, Apps :183, Trash :193. (cmdk is the dead/Docker-only palette per MEMORY.md — repoint for consistency.)
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md §3 (cmdk.tsx — same pattern, hook already present).
  </read_first>
  <action>
    In `livos/packages/ui/src/components/cmdk.tsx`, repoint the 4 Files actions to use the already-present `windowManager` (with `navigate` fallback). Mirror the apple-spotlight pattern:
    - Files (:163): `navigate(lastFilesPath || systemAppsKeyed['LIVINITY_files'].systemAppTo)` → window-open the resolved target with `navigate` fallback.
    - Recents (:173): `navigate(\`/files${FILES_RECENTS_PATH}\`)` → `windowManager ? windowManager.openWindow('LIVINITY_files', \`/files${FILES_RECENTS_PATH}\`, 'Files', systemAppsKeyed['LIVINITY_files']?.icon || '') : navigate(...)`
    - Apps (:183): same with `FILES_APPS_PATH`.
    - Trash (:193): same with `FILES_TRASH_PATH`.
    No new import needed (the hook is already imported + called). Do NOT touch the non-Files actions.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openWindow('LIVINITY_files'" livos/packages/ui/src/components/cmdk.tsx` returns 4.
    - `cd livos && pnpm --filter ui build` exits 0.
  </acceptance_criteria>
  <done>cmdk Files actions open the window via windowManager with navigate fallback; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the full-page /files route mount + delete the full-page route tree and wrapper</name>
  <files>livos/packages/ui/src/router.tsx, livos/packages/ui/src/features/files/routes.tsx, livos/packages/ui/src/features/files/index.tsx, livos/packages/ui/src/modules/desktop/dock.test.tsx</files>
  <read_first>
    - livos/packages/ui/src/router.tsx — read :1-20 and :140-170. CONFIRM: `import {filesRoutes} from '@/features/files/routes'` at :9; `...filesRoutes` spread inside `SheetLayout` at :147; OwnCloud stale comment at :167. Read :148-168 to see the precedent comment about Settings already being removed from this SheetLayout (mirror that pattern).
    - livos/packages/ui/src/features/files/routes.tsx — read the whole file (it `lazy`-imports `@/features/files` = the full-page `Files`/`FilesLayout` wrapper and defines the index Navigate redirect + child listing routes).
    - livos/packages/ui/src/features/files/index.tsx — read :1-35 (the `Files`/`FilesLayout` full-page 3-col wrapper).
    - livos/packages/ui/src/modules/window/app-contents/files-content.tsx — re-confirm `FilesWindowContent` imports the listing components DIRECTLY (DirectoryListing, Sidebar, RewindOverlay, FormatDriveDialog, etc.) and does NOT import `filesRoutes` or `@/features/files` — so deleting the full-page tree does not break the window.
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 1 (Notes: keep apps.tsx:51 + system-windowed-routes.ts; only the full-page route mount + filesRoutes tree + FilesLayout/Files wrapper are removable) + Planner Guidance step 6 (1a).
    - DELETION SAFETY (verified by planner): `filesRoutes` is imported ONLY by `router.tsx`; `@/features/files` is imported ONLY by `routes.tsx`'s lazy import. No other importer.
  </read_first>
  <action>
    1. In `livos/packages/ui/src/router.tsx`: delete the import line `import {filesRoutes} from '@/features/files/routes'` (:9) AND delete the `...filesRoutes,` spread line inside `SheetLayout` (:147). Also delete the now-stale OwnCloud comment (:167, `// /files PRESERVED — OwnCloud daily driver depends on URL deep-linking.`) and any adjacent comment lines that ONLY explain the now-removed `/files` mount (mirror how the Settings removal was documented per the :148-168 precedent — leave a one-line note like `// /files full-page route removed (Phase 285) — Files opens as a window via LIVINITY_files.` if it aids future readers, optional). Do NOT remove unrelated routes from SheetLayout.
    2. Delete the file `livos/packages/ui/src/features/files/routes.tsx` entirely (`git rm`). It exists only to define the full-page `filesRoutes`.
    3. Delete the file `livos/packages/ui/src/features/files/index.tsx` entirely (`git rm`). It is the full-page `Files`/`FilesLayout` 3-col wrapper, imported only by `routes.tsx` (which is also being deleted). ⚠️ Confirm via grep that NOTHING else imports `@/features/files` (bare, no subpath) before deleting — the windowed surface imports `@/features/files/components/...` and `@/features/files/constants` (subpaths, KEEP those modules, only the bare-index file goes).
    4. In `livos/packages/ui/src/modules/desktop/dock.test.tsx`: re-check the default-pins assertion (~:267, the set `['launchpad','files','settings','app-store','server-control','liv-assistant']`). It asserts presence of the data-test seam, not the route, so it is LIKELY unaffected. If `pnpm --filter ui test` for dock.test goes red because of the route change, update the assertion minimally to match the new windowed behavior; otherwise leave it unchanged.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "filesRoutes" livos/packages/ui/src` returns 0 (import + spread + the defining file all gone).
    - `grep -rc "from '@/features/files'" livos/packages/ui/src` returns 0 (the bare-index wrapper has no remaining importer).
    - `test -f livos/packages/ui/src/features/files/routes.tsx` returns non-zero (deleted).
    - `test -f livos/packages/ui/src/features/files/index.tsx` returns non-zero (deleted).
    - The windowed surface + shared modules survive: `test -f livos/packages/ui/src/modules/window/app-contents/files-content.tsx` succeeds; `test -d livos/packages/ui/src/features/files/components/sidebar` succeeds; `grep -c "systemAppTo: '/files/Home'" livos/packages/ui/src/providers/apps.tsx` returns 1 (KEPT); `grep -c "LIVINITY_files" livos/packages/ui/src/modules/window/system-windowed-routes.ts` returns 1 (KEPT).
    - `cd livos && pnpm --filter ui build` exits 0 (CRITICAL — proves no dangling import of the deleted FilesLayout/filesRoutes).
    - `cd livos && pnpm --filter ui test -- dock` passes.
  </acceptance_criteria>
  <done>The full-page /files route + its tree + wrapper are removed; the windowed Files + shared modules + systemAppTo/system-windowed-routes are kept; build + dock.test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side routing | A full-page route is removed and palette launches are repointed to an in-process window-open call. No server route, no Caddy/livinityd change, no new external input. `openWindow` is called with an internally-constructed appId + route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-07 | — | router.tsx / apple-spotlight.tsx / cmdk.tsx / deleted files | accept | No new attack surface — route REMOVAL + repointing existing launch entries to an existing in-process window-open API. `openWindow('LIVINITY_files', route, …)` is called with a hard-coded appId and an internally-derived route (`lastFilesPath` from sessionStorage or a constant FILES_*_PATH) — no attacker-supplied free text, no eval, no dynamic import. The route string flows into `FilesWindowContent`'s existing `/files`-strip + `decodeURIComponent`, an existing validated path. |
</threat_model>

<verification>
- `cd livos && pnpm --filter ui build` exits 0 (the KEY gate — catches the dangling FilesLayout/filesRoutes import after deletion).
- `cd livos && pnpm --filter ui test -- dock` passes.
- grep confirms: 0 `filesRoutes` refs, 0 bare `@/features/files` importers, the 2 full-page files deleted, and the windowed surface + `systemAppTo`/`system-windowed-routes` KEPT.
</verification>

<success_criteria>
- Clicking Files in the palettes opens a LivOS window (no browser URL change); the full-page Umbrel layout is gone.
- The windowed Files + shared listing/sidebar modules + `apps.tsx:51` systemAppTo + `system-windowed-routes.ts` are retained.
- `pnpm --filter ui build` exits 0; dock.test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-06-SUMMARY.md`
</output>
