---
phase: 285-umbrel-leftover-cleanup-and-docker-scroll-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/install.sh
  - scripts/install/deploy-livinityd.sh
  - scripts/install/__tests__/test-deploy-livinityd.sh
autonomous: true
requirements: [Item-4]
must_haves:
  truths:
    - "No Umbrel-removal documentation comments remain in the 3 install scripts (the 5 comment ranges are gone)"
    - "The orphaned setup_docker_prerequisites() function, its call site, and its flow-comment line are removed"
    - "The LIVE Umbrel-compat code paths (umbrel-app.yml fallback, ${UMBREL_ROOT} injection) are UNCHANGED"
    - "The LIVE top-level $LIVOS_DIR/app-data per-app dir creation/chown is UNCHANGED"
  artifacts:
    - path: "livos/install.sh"
      provides: "Install script with Umbrel comments + orphaned dir-prep helper removed, live app-data path intact"
      contains: "app-data"
  key_links:
    - from: "install.sh start() flow"
      to: "removed setup_docker_prerequisites call"
      via: "deleting the call at :1818 + the flow-comment line at :1754 + the def at :413-429"
      pattern: "setup_docker_prerequisites"
---

<objective>
Remove the Umbrel-removal documentation comments and the orphaned `setup_docker_prerequisites()` dir-prep helper from the install scripts (Item 4).

Purpose: After Phase 276, every surviving "Umbrel" token in the install scripts is a COMMENT documenting the removal — no live pulls/clones. Operator chose to remove them. Separately, `setup_docker_prerequisites()` only `mkdir`s `data/tor/data` + `data/app-data` (under `data/`) for the tor_proxy/auth services that were deleted in Phase 276 — RESEARCH confirmed zero downstream consumers. Operator pre-approved removing it.

Output: 5 comment ranges deleted across 3 files, plus `setup_docker_prerequisites()` (def + call + flow-comment) removed from `install.sh`. Shell-only, no build gate — verified by grep that the constructs are gone AND that the LIVE Umbrel-compat paths + the LIVE top-level `app-data` dir are untouched.

⚠️ CRITICAL: Two distinct "app-data" paths exist. The ORPHANED `$LIVOS_DIR/data/app-data` (under `data/`, created by `setup_docker_prerequisites`) is removed. The LIVE top-level `$LIVOS_DIR/app-data/<appId>` per-app dir (install.sh:1110, 1126-1127, 1887 + builtin-apps volume mounts) MUST STAY. Do not conflate them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md
@.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 5 Umbrel-removal comment ranges across the 3 install scripts</name>
  <files>livos/install.sh, scripts/install/deploy-livinityd.sh, scripts/install/__tests__/test-deploy-livinityd.sh</files>
  <read_first>
    - livos/install.sh — read :405-415 (the first comment block) and :1754-1760 (the flow-comment + second Umbrel comment). CONFIRM :408-411 is the "docker-image pull/retag helper was REMOVED" comment block and :1757-1758 is the "Phase 276: the docker-image pull/retag step was removed" comment.
    - scripts/install/deploy-livinityd.sh — read :768-778. CONFIRM :771-775 is the "LivOS Docker images — REMOVED (Phase 276)" divider + Umbrel comment.
    - scripts/install/__tests__/test-deploy-livinityd.sh — read :940-950 and :983-988. CONFIRM :943-947 and :985-986 are Umbrel comments (TESTS 39/40/41 already deleted in Phase 276 — these are comment-only, no assertion change).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Item 4 (file:line re-verification table + the MUST-NOT-TOUCH live-compat list).
  </read_first>
  <action>
    Delete EXACTLY these comment ranges (all comment-only; deleting changes no behavior). Re-locate by the literal comment text if line numbers drift:
    1. `livos/install.sh:408-411` — the 4-line comment block beginning `# Phase 276 (276-01 + 276-05): the docker-image pull/retag helper was REMOVED.` through `# any upstream Umbrel image at install anymore.`
    2. `livos/install.sh:1757-1758` — the 2-line comment `# Phase 276: the docker-image pull/retag step was removed with the dead Umbrel` / `# tor service — no upstream Umbrel image is pulled at install anymore.`
    3. `scripts/install/deploy-livinityd.sh:771-775` — the divider line `# ── 4c. LivOS Docker images — REMOVED (Phase 276) ──...` plus the 4 following comment lines through `# livinityd's Apps module no longer references them.`
    4. `scripts/install/__tests__/test-deploy-livinityd.sh:943-947` — the Umbrel comment block.
    5. `scripts/install/__tests__/test-deploy-livinityd.sh:985-986` — the Umbrel comment block.

    Do NOT delete the flow-comment line `# 3. setup_docker_prerequisites -> app-data dirs ready` yet (install.sh:1754) — that is removed in Task 2 together with the function. Do NOT touch ANY line containing `umbrel-app.yml`, `UMBREL_ROOT`, or `${UMBREL_ROOT}` (LIVE third-party-app compat). Do NOT touch the `legacy-compat/app-script.ts` comment, `cmdk.tsx:263` comment, or `notifications.tsx:187` comment — those are out of scope.
  </action>
  <verify>
    <automated>bash -n livos/install.sh && bash -n scripts/install/deploy-livinityd.sh && bash -n scripts/install/__tests__/test-deploy-livinityd.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "docker-image pull/retag" livos/install.sh` returns 0.
    - `grep -c "upstream Umbrel image" livos/install.sh` returns 0.
    - `grep -c "LivOS Docker images — REMOVED" scripts/install/deploy-livinityd.sh` returns 0.
    - `grep -ci "umbrel" scripts/install/__tests__/test-deploy-livinityd.sh` returns 0.
    - `bash -n` (syntax check) passes for all 3 files (no broken heredocs/blocks from a mis-aligned delete).
    - LIVE-COMPAT UNCHANGED: `grep -c "umbrel-app.yml" livos/packages/livinityd/source/modules/apps/app.ts` and `grep -c "UMBREL_ROOT" livos/packages/livinityd/source/modules/apps/apps.ts` are both unchanged from baseline (still 2 and 1 respectively — these files are NOT in this plan's files_modified, so they must be byte-identical).
  </acceptance_criteria>
  <done>The 5 Umbrel comment ranges are removed; all 3 scripts pass `bash -n`; live-compat code untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the orphaned setup_docker_prerequisites() helper (def + call + flow-comment)</name>
  <files>livos/install.sh</files>
  <read_first>
    - livos/install.sh — read the function def at :413-429 (`setup_docker_prerequisites() { ... }`). CONFIRM it ONLY `mkdir -p "$data_dir/tor/data"` + `mkdir -p "$data_dir/app-data"` (where `data_dir="$LIVOS_DIR/data"`) and `chown -R 1000:1000` on those two. Read the call site at :1818 (`    setup_docker_prerequisites`) and the flow-comment at :1754 (`# 3. setup_docker_prerequisites -> app-data dirs ready`).
    - livos/install.sh — read :1108-1130 and :1885-1890 (the LIVE top-level `$LIVOS_DIR/app-data/<appId>` per-app dir handling) so you can CONFIRM you are NOT touching it. The orphaned path is `$LIVOS_DIR/data/app-data` (note the `data/` prefix); the LIVE path is `$LIVOS_DIR/app-data` (top-level, no `data/`).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-RESEARCH.md §Open Question 3 (definitive answer: SAFE to remove; the CRITICAL distinction between the two app-data paths).
    - .planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-PATTERNS.md "Removals (Items 3 & 4)" — Item 4 orphaned dir-creation note.
  </read_first>
  <action>
    Re-verify the two orphaned dirs have zero consumers (defense against drift):
    ```bash
    cd "$(git rev-parse --show-toplevel)"
    grep -rn "data/tor/data\|data/app-data" livos/ scripts/ | grep -v "install.sh:41" || echo "no consumers outside setup_docker_prerequisites"
    grep -rn "tor_proxy" livos/packages/livinityd/source/modules/apps/ scripts/ 2>/dev/null || echo "0 tor_proxy refs"
    ```
    If anything OTHER than the `setup_docker_prerequisites` body references `data/tor/data` or `$data_dir/app-data`, STOP and report — removal may not be safe.

    Then in `livos/install.sh`:
    1. Delete the entire function definition `setup_docker_prerequisites() { ... }` (currently :413-429, including the opening line, all `info`/`mkdir`/`chown`/`ok` lines, and the closing `}`).
    2. Delete the call site (currently :1818) — the single line `    setup_docker_prerequisites`.
    3. Delete the flow-comment line (currently :1754) — `    # 3. setup_docker_prerequisites -> app-data dirs ready`.
    Do NOT renumber other flow-comment steps; leave the surrounding comments (steps 1,2,4...) as-is even if step "3" is now gone — minimize churn. Do NOT touch any line that creates/chowns `$LIVOS_DIR/app-data` WITHOUT the `data/` prefix (that is the LIVE per-app dir at :1110, :1126-1127, :1887).
  </action>
  <verify>
    <automated>bash -n livos/install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setup_docker_prerequisites" livos/install.sh` returns 0 (def + call + flow-comment all gone).
    - `grep -c "data/tor/data" livos/install.sh` returns 0 (orphaned tor dir creation gone).
    - `grep -c "data_dir/app-data\|data/app-data" livos/install.sh` returns 0 (orphaned data/app-data creation gone).
    - LIVE top-level app-data UNCHANGED: `grep -c "LIVOS_DIR/app-data\|\\$LIVOS_DIR/app-data\|app-data/" livos/install.sh` is > 0 (the live per-app dir handling at :1110/:1126-1127/:1887 survives) — confirm by reading those lines that they still create/chown `$LIVOS_DIR/app-data/<appId>`.
    - `bash -n livos/install.sh` passes (no orphaned `}` or broken block from the function deletion).
  </acceptance_criteria>
  <done>setup_docker_prerequisites() and its call/flow-comment are removed; the live top-level app-data handling survives; install.sh syntax is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-time shell (root) | install.sh runs as root at install/update; but this plan only DELETES comments and an orphaned dir-prep helper — it adds no new shell, no new input handling, no new privilege use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-285-03 | — | install.sh / deploy-livinityd.sh / test-deploy-livinityd.sh | accept | No new attack surface — comment deletions + removal of an orphaned mkdir/chown helper. Removing the `chown -R 1000:1000` on now-unused dirs slightly REDUCES surface. `bash -n` guards against introducing a broken script. The LIVE app-data + Umbrel-compat paths are explicitly verified unchanged. |
</threat_model>

<verification>
- `bash -n` syntax check passes for install.sh, deploy-livinityd.sh, test-deploy-livinityd.sh.
- grep confirms the 5 comment ranges + `setup_docker_prerequisites` (def/call/flow-comment) + the two orphaned `data/` dirs are gone.
- grep confirms the LIVE Umbrel-compat (`umbrel-app.yml`, `UMBREL_ROOT`) + the LIVE top-level `$LIVOS_DIR/app-data` are UNCHANGED.
- No UI build / tsc gate (shell-only). The `test-deploy-livinityd.sh` comment hits are TESTS-already-deleted-in-P276, so no test assertion changes.
</verification>

<success_criteria>
- 5 Umbrel comment ranges removed; `setup_docker_prerequisites()` def + call + flow-comment removed.
- LIVE Umbrel-compat code paths and the LIVE top-level `app-data` per-app dir untouched.
- All 3 scripts pass `bash -n`.
</success_criteria>

<output>
After completion, create `.planning/phases/285-umbrel-leftover-cleanup-and-docker-scroll-fix/285-03-SUMMARY.md`
</output>
