---
phase: 262-security-hardening-pass-3
plan: 262-01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/domain/caddy.ts
  - livos/packages/livinityd/source/modules/domain/caddy.test.ts
  - livos/packages/livinityd/source/modules/server/index.ts
  - livos/packages/livinityd/source/modules/server/liv-login-handler.ts
  - livos/packages/livinityd/source/modules/server/liv-login-handler.test.ts
  - livos/packages/livinityd/source/index.ts
  - systemd/liv-assistant.service
autonomous: true
requirements: [LIVOS-041, LIVOS-047, LIVOS-049, LIVOS-053, LIVOS-054]
must_haves:
  truths:
    - "GET /liv-login without a valid LIVINITY_SESSION returns 401 and mints NO aionui-session cookie (no qr-token fetch fires)"
    - "/liv, /ws, and the AionUi /api subresource path all carry a Caddy forward_auth gate to 127.0.0.1:8080 /auth/verify — unauthenticated requests are 401/redirected before reaching :3020"
    - "A revoked or deactivated-user session is rejected at /auth/verify and /__livos_sso (jti revocation + active-user re-check, same as the tRPC isAuthenticated middleware)"
    - "/__livos_auth-minted session tokens are recorded via createSession so they are revocable"
    - "The framed AionUi SPA can no longer reach LivOS tRPC via /liv/trpc/* (bridge removed)"
    - "Apex routes that forget a per-route auth check are fail-closed by a global apex session gate (explicit allowlist only)"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/domain/caddy.ts"
      provides: "forward_auth-gated /liv family handles, no @liv_trpc bridge, no Referer matcher"
    - path: "livos/packages/livinityd/source/modules/server/liv-login-handler.ts"
      provides: "verifySession-gated handler (401 without valid LIVINITY_SESSION)"
    - path: "livos/packages/livinityd/source/modules/server/index.ts"
      provides: "verifySessionFull helper + revocation-checked /auth/verify + /__livos_sso + createSession in /__livos_auth + apex fail-closed gate"
  key_links:
    - from: "domain/caddy.ts LIV_ASSISTANT_HANDLE / @liv_ws / @liv_api_subresource / @livos_terminal_ws / @liv_login"
      to: "livinityd /auth/verify"
      via: "forward_auth 127.0.0.1:8080 { uri /auth/verify; copy_headers Cookie }"
      pattern: "forward_auth 127\\.0\\.0\\.1:8080"
    - from: "server/index.ts /auth/verify"
      to: "database/sessions.ts isSessionRevoked"
      via: "verifySessionFull (mirrors server/trpc/is-authenticated.ts:80-100)"
      pattern: "isSessionRevoked"
    - from: "source/index.ts:565 mount"
      to: "liv-login-handler.ts"
      via: "makeLivLoginHandler(redis, verifySession)"
      pattern: "makeLivLoginHandler\\(.*verifySession"
---

<objective>
WS1 — Close the `/liv` (AionUi "Liv AI") unauthenticated front door.

Closes: **LIVOS-041 (Critical)** unauthenticated `/liv-login` mints an AionUi session cookie for
anyone; **LIVOS-047 (High)** Referer-only `/api/*` routing is client-spoofable; **LIVOS-049 (High,
code portion)** pin AionUi `:3020` to loopback fail-closed; **LIVOS-053 (Medium)** apex has no
server-side auth gate (fail-open); **LIVOS-054 (Medium, minimal fix)** remove the `/liv/trpc/*` →
`:8080` Caddy bridge; plus the **cross-surface revocation gap** (jti revocation exists only in the
tRPC middleware — `/auth/verify`, `/__livos_sso` skip it; `/__livos_auth` mints un-revocable tokens).

Purpose: a valid `LIVINITY_SESSION` must be required before ANY AionUi traffic (including QR cookie
minting), and revoked sessions must be rejected at every auth surface, not just tRPC.

Output: hardened caddy.ts emit, gated liv-login handler, revocation-checked auth endpoints, apex
fail-closed gate, systemd loopback-bind assertion. RED→GREEN tests in liv-login-handler.test.ts and
caddy.test.ts.

**HARD CONSTRAINTS: CODE ONLY — NO DEPLOY.** No `update.sh`, no `systemctl`, no SSH mutation, no
`ufw`. Backend runs via tsx — no build step. The live UFW-deny 3020 and the AionUi-origin move are
OPERATOR/deferred items (see CONTEXT `## Deferred`). Only NEW tsc errors block (livinityd baseline
~TS18048 noise is pre-existing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-security-hardening-pass-3/262-CONTEXT.md
@SECURITY-RESEARCH-PASS-3.md  (LIVOS-041 / 047 / 049 / 053 / 054 sections + WS1 success criteria)

<interfaces>
Existing analog — the 256-04 forward_auth block (caddy.ts:675, `gatedHandleBody`):

```
const gatedHandleBody = `\tforward_auth 127.0.0.1:8080 {
\t\turi /auth/verify
\t\tcopy_headers Cookie
\t\t@bad status 401
\t\thandle_response @bad {
\t\t\tredir https://${config.mainDomain}/__livos_sso?return={scheme}://{host}{uri}
\t\t}
\t}
\treverse_proxy 127.0.0.1:${sub.port} { ... }`
```

Current /liv family constants in caddy.ts (module-level, NO config access — use Caddy placeholders):
- `LIV_ASSISTANT_HANDLE` (line 355): `@liv_trpc path /liv/trpc /liv/trpc/*` handle (strip /liv →
  :8080) THEN `@liv path /liv /liv/*` handle (strip /liv → :3020, XFO/CSP strip, frame-ancestors CSP).
- `LIV_ASSISTANT_SUBRESOURCE_HANDLE` (line 465): `@webapp_stream_ws` (→:8080, DO NOT GATE — LivOS's
  own WS, out of scope), `@liv_ws path /ws /ws/*` (→:3020), `@liv_api_subresource
  { header_regexp Referer ^https?://[^/]+/liv(/|$); path /api/* }` (→:3020).
- `LIVOS_TERMINAL_WS_HANDLE` (line 519): `@livos_terminal_ws path /livos/terminal/ws` (→:8080).
- Emit sites for these constants: three apex/mainDomain block builders (around lines 547-548,
  581-582, 630-631 inside `generateFullCaddyfile`). The apex block ends with the bare catch-all
  `handle { reverse_proxy 127.0.0.1:8080 }` (~line 583-588) — leave the catch-all itself unchanged
  (the Express apex gate in Task 3 covers it).

Revocation pattern to replicate (server/trpc/is-authenticated.ts:80-100):
```ts
if (dbUser && dbUser.isActive) {
  if (payload.jti && getPool()) {
    const revoked = await isSessionRevoked(payload.jti)
    if (revoked) throw ... 'Session revoked'
  }
  ...
}
```

createSession signature (database/sessions.ts:43):
```ts
createSession(args: {userId: string; jti: string; expiresAt: Date; deviceName?: string|null; ipAddress?: string|null}): Promise<void>
```
Login call-site analog: user/routes.ts:191-199 (verifyToken to recover jti, then createSession,
non-fatal catch).

liv-login mount (livos/packages/livinityd/source/index.ts:565):
```ts
this.server.app.get('/liv-login', makeLivLoginHandler(this.ai.redis))
```
`this.server` is the Server class in modules/server/index.ts, which has `verifyToken(token)`,
`signUserToken(userId, role)` (jti embedded by jwt.ts:98), `signToken()` (legacy, no jti/userId).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: caddy.ts — forward_auth over the whole /liv family, path-prefix /api matcher, /liv-login handle, delete the /liv/trpc bridge</name>
  <files>livos/packages/livinityd/source/modules/domain/caddy.ts, livos/packages/livinityd/source/modules/domain/caddy.test.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/domain/caddy.ts — full LIV_ASSISTANT_HANDLE (~:355-372), LIV_ASSISTANT_SUBRESOURCE_HANDLE (~:465-491), LIVOS_TERMINAL_WS_HANDLE (~:519-527), the three emit sites in generateFullCaddyfile, and the gatedHandleBody forward_auth analog at ~:675
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-041 Recommendation, LIVOS-047 (Referer matcher), LIVOS-054 (bridge removal)
    - livos/packages/livinityd/source/modules/domain/caddy.test.ts — existing @liv assertions (:406-:631) that must stay green or be updated
  </read_first>
  <action>
    Implements CONTEXT D-WS1 items (a), (c), (f).

    1. Add a module-level constant (above LIV_ASSISTANT_HANDLE) with the EXACT directive body —
       mirrors gatedHandleBody but uses Caddy placeholders since module constants have no `config`:
       ```
       const LIV_GATE_BODY = `\t\tforward_auth 127.0.0.1:8080 {
       \t\t\turi /auth/verify
       \t\t\tcopy_headers Cookie
       \t\t\t@bad status 401
       \t\t\thandle_response @bad {
       \t\t\t\tredir /login?redirect={uri} 302
       \t\t\t}
       \t\t}`
       ```
    2. LIV_ASSISTANT_HANDLE: DELETE the entire `@liv_trpc` matcher + handle (the
       `@liv_trpc path /liv/trpc /liv/trpc/*` block, lines ~355-359) — this is the LIVOS-054
       load-bearing mitigation (the framed same-origin SPA must NOT reach LivOS tRPC). Insert
       `${LIV_GATE_BODY}` as the FIRST directive inside `handle @liv { ... }` (before
       `uri strip_prefix /liv`).
    3. LIV_ASSISTANT_SUBRESOURCE_HANDLE: leave `@webapp_stream_ws` UNCHANGED (LivOS-owned WS, not
       AionUi). Insert `${LIV_GATE_BODY}` as the first directive inside `handle @liv_ws { ... }`.
       REPLACE the Referer matcher block
       `@liv_api_subresource {\n header_regexp Referer ^https?://[^/]+/liv(/|$)\n path /api/*\n}`
       with the single-line path matcher `@liv_api_subresource path /liv/api/*` (LIVOS-047: Referer
       is client-spoofable and is NOT auth; install-liv-assistant.sh already rewrites `/api/` →
       `/liv/api/` in the bundle). Inside its handle, insert `${LIV_GATE_BODY}` first AND add
       `uri strip_prefix /liv` before the reverse_proxy so :3020 still receives `/api/*`.
       The string `header_regexp Referer` must no longer appear anywhere in caddy.ts.
    4. LIVOS_TERMINAL_WS_HANDLE: insert `${LIV_GATE_BODY}` as the first directive inside
       `handle @livos_terminal_ws { ... }`.
    5. Add a new constant `LIV_LOGIN_HANDLE`:
       ```
       const LIV_LOGIN_HANDLE = `\t@liv_login path /liv-login
       \thandle @liv_login {
       ${LIV_GATE_BODY}
       \t\treverse_proxy 127.0.0.1:8080 {
       ${WS_TRANSPORT_BODY}
       \t\t}
       \t}`
       ```
       Emit it immediately after `${LIV_ASSISTANT_HANDLE}` at ALL THREE emit sites in
       generateFullCaddyfile (the no-domain fallback block, the apex/mainDomain block, and the
       multi-user wildcard block — grep for every `${LIV_ASSISTANT_HANDLE}` interpolation).
       This puts forward_auth in front of /liv-login at the Caddy layer (Express gate lands in
       Task 2 — both are required, the qr-mint endpoints are otherwise reachable through `@liv`).
    6. caddy.test.ts: fix any assertion broken by the gate insertion/order, then ADD RED→GREEN
       cases: (a) generated apex output contains `forward_auth 127.0.0.1:8080` inside the
       `handle @liv`, `handle @liv_ws`, `handle @liv_api_subresource`, `handle @livos_terminal_ws`,
       and `handle @liv_login` bodies (index-order assertions like the existing :425 pattern);
       (b) output does NOT contain `@liv_trpc` and does NOT contain `header_regexp Referer`;
       (c) `@liv_api_subresource path /liv/api/*` present.
    Note adjacent code idiom: tab-escaped template literals, `WS_TRANSPORT_BODY` interpolation —
    match exactly. Do not touch gatedHandleBody, the subdomain emit branches, or PUBLIC carve-outs.
  </action>
  <acceptance_criteria>
    - caddy.ts: the LIV_ASSISTANT_HANDLE / @liv block contains a forward_auth directive
    - caddy.ts: `grep -c 'header_regexp Referer' caddy.ts` == 0
    - caddy.ts: `grep -c '@liv_trpc' caddy.ts` == 0
    - caddy.ts: contains `@liv_api_subresource path /liv/api/*` and `@liv_login path /liv-login`
    - caddy.ts: @liv_ws and @livos_terminal_ws handles each contain forward_auth
    - caddy.test.ts green including the new negative assertions
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/domain/caddy.test.ts</automated>
  </verify>
  <done>Every /liv-family Caddy handle is forward_auth-gated; the tRPC bridge and Referer matcher are gone; caddy tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate /liv-login on a verified session + add revocation/active-user re-check to /auth/verify and /__livos_sso + createSession in /__livos_auth</name>
  <files>livos/packages/livinityd/source/modules/server/liv-login-handler.ts, livos/packages/livinityd/source/modules/server/liv-login-handler.test.ts, livos/packages/livinityd/source/modules/server/index.ts, livos/packages/livinityd/source/index.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/server/liv-login-handler.ts (whole file, 70 lines) + liv-login-handler.test.ts (mock-fetch pattern)
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-041 Description/Exploit + the WS-A1 revocation-gap paragraph in Mitigation-Confirmed
    - livos/packages/livinityd/source/modules/server/trpc/is-authenticated.ts:80-100 — the EXACT jti-revocation + active-user pattern to replicate (and its imports: isSessionRevoked from '../../database/sessions.js', the user lookup, getPool)
    - livos/packages/livinityd/source/modules/server/index.ts — /auth/verify (:1216), /__livos_sso (:1242), /__livos_auth (:1285), signUserToken (:140)
    - livos/packages/livinityd/source/modules/user/routes.ts:191-202 — the createSession call-site analog
  </read_first>
  <behavior>
    - liv-login-handler test (RED→GREEN): request WITHOUT a LIVINITY_SESSION cookie → 401 JSON, NO fetch to /api/webui/generate-qr-token, NO Set-Cookie on the response
    - request with a cookie that verifySession rejects (resolves null) → 401, no qr fetch
    - request with a valid session (stub verifySession resolves a payload) → existing behavior intact: 302 + forwarded Set-Cookie (update the 5 existing tests to pass the stub + cookie)
    - flag='false' + valid session → 302 to /liv/ without qr fetch (existing Test 3, now with auth)
  </behavior>
  <action>
    Implements CONTEXT D-WS1 items (b) and (e).

    1. server/index.ts — add a method on the Server class (near verifyToken, ~:133):
       `async verifySessionFull(token: string): Promise<TokenPayload | null>` that:
       (a) `const payload = await this.verifyToken(token).catch(() => null)`; null → null.
       (b) If `payload.userId`: resolve the DB user with the SAME lookup is-authenticated.ts uses;
           reject (return null) when the user is missing or `!isActive`. Skip when no PG pool
           (single-user no-DB path — mirror is-authenticated's skip semantics exactly).
       (c) If `payload.jti && getPool()`: `await isSessionRevoked(payload.jti)` → revoked → null.
           A MISSING session row must NOT reject (false-revoke lockout — see the 257-04.1 comment
           in is-authenticated.ts; isSessionRevoked already implements this).
       Import isSessionRevoked (and the user/pool helpers) with the same module paths
       is-authenticated.ts uses, adjusted for server/index.ts relative depth.
    2. /auth/verify (:1216): replace the bare `this.verifyToken(token)` with
       `this.verifySessionFull(token)` — a revoked/inactive session now 401s, which transitively
       hardens EVERY forward_auth consumer including the Task-1 /liv gates and the apex `/api`
       resolver path (the "/api resolver" item in CONTEXT (e) is satisfied via this single
       choke point since Task 1 routes /liv/api/* through forward_auth → /auth/verify).
    3. /__livos_sso (:1242): replace `this.verifyToken(sessionToken)` with
       `this.verifySessionFull(sessionToken)` (revoked sessions must not get SSO bounce tokens).
    4. /__livos_auth (:1285): after `const sessionToken = claims.userId ? await
       this.signUserToken(...) : await this.signToken()`, when `claims.userId` is set: recover the
       jti via `const minted = await this.verifyToken(sessionToken).catch(() => null)` and call
       `createSession({userId: claims.userId, jti: minted.jti, expiresAt: new Date(Date.now() + 7*24*60*60*1000)})`
       inside a non-fatal try/catch, mirroring user/routes.ts:191-202 verbatim (import
       createSession from '../database/sessions.js'). Legacy `signToken()` path (no userId) skips —
       it has no user row. This closes the "un-revocable 30-day cookie" gap.
    5. liv-login-handler.ts: change the factory to
       `makeLivLoginHandler(redis: Redis, verifySession: (token: string) => Promise<unknown | null>)`.
       At the TOP of the handler (before the feature-flag read), add:
       ```ts
       const token = req.cookies?.LIVINITY_SESSION
       const session = token ? await verifySession(token).catch(() => null) : null
       if (!session) {
         res.status(401).json({error: 'unauthorized'})
         return
       }
       ```
       Rename `_req` → `req`. IMPORTANT: the catch-all error path at the bottom must NOT mint or
       redirect for unauthenticated callers — the 401 return above is before the try body; keep the
       existing failure redirect ONLY for post-auth qr failures.
    6. source/index.ts:565 — update the mount:
       `this.server.app.get('/liv-login', makeLivLoginHandler(this.ai.redis, (t) => this.server.verifySessionFull(t)))`.
    7. liv-login-handler.test.ts — write the RED tests from <behavior> FIRST (they fail against
       the current signature/behavior), then implement 5-6 and re-run GREEN. Keep the existing
       mock-fetch + qr-mint server pattern; existing tests 1-5 get a stub
       `verifySession = async () => ({loggedIn: true})` and `req.cookies = {LIVINITY_SESSION: 'x'}`.
  </action>
  <acceptance_criteria>
    - liv-login-handler.ts (or its mount in source/index.ts) calls a verify function on LIVINITY_SESSION before minting: `grep -n 'LIVINITY_SESSION' liv-login-handler.ts` non-empty and the 401 path precedes any fetch to :3020
    - server/index.ts: /auth/verify and /__livos_sso call verifySessionFull (not bare verifyToken)
    - server/index.ts: verifySessionFull contains an isSessionRevoked call
    - server/index.ts: /__livos_auth contains a createSession call
    - liv-login-handler.test.ts: includes a test asserting 401 + zero qr-token fetches when no cookie present; all tests green
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/server/liv-login-handler.test.ts</automated>
  </verify>
  <done>Unauthenticated /liv-login = 401 with no cookie minted; revocation + active-user checks live at /auth/verify and /__livos_sso; /__livos_auth tokens are revocable.</done>
</task>

<task type="auto">
  <name>Task 3: Apex fail-closed session gate (LIVOS-053) + systemd loopback-bind assertion for aionui-web :3020 (LIVOS-049)</name>
  <files>livos/packages/livinityd/source/modules/server/index.ts, systemd/liv-assistant.service</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-053 (Recommendation + Residual Risk: allowlist construction is the dangerous part) and LIVOS-049 (verifier note: live ss already shows 127.0.0.1:3020)
    - livos/packages/livinityd/source/modules/server/index.ts — the app-gateway middleware (:387-401, the `host === mainDomain → next()` bail), the apiPathPrefixes list (:2063-2090, late-mounted handlers incl. /liv-login), and the SPA catch-all that follows
    - systemd/liv-assistant.service — ExecStart at :46 (`aionui-web start --port 3020`, no --host flag; the binary is an upstream prebuilt tarball, NOT in-repo source)
  </read_first>
  <action>
    Implements CONTEXT D-WS1 "Apex fail-closed gate" + "Bind AionUi :3020 to 127.0.0.1".

    1. server/index.ts — register a NEW middleware immediately AFTER the app-gateway middleware
       (so subdomain/custom-domain routing is untouched): `apexSessionGate`. Behavior:
       - Read `livos:domain:config` from Redis exactly like the gateway middleware does; if no
         active domain OR `request.hostname !== domainConfig.domain` → `next()` (only the apex is
         gated; dev/no-domain boxes unaffected).
       - ALLOWLIST (path-prefix match, explicit constant `APEX_PUBLIC_PREFIXES` with a comment
         citing LIVOS-053): `/login`, `/auth/`, `/__livos_sso`, `/__livos_auth`, `/trpc`
         (per-procedure auth enforced by isAuthenticated), `/api/auth`, `/api/mcp` (own
         LIVINITY_PROXY_TOKEN check), `/assets/`, `/icons/`, `/fonts/`, `/favicon`, `/manifest`,
         `/sw.js`, `/registerSW.js`, `/workbox-`. Also allow exact `/` and any GET whose path
         matches the static-asset extension regex
         `/\.(js|css|map|svg|png|jpg|ico|woff2?|webmanifest)$/` — the SPA shell + login page must
         load pre-auth (the SPA serves /login). DO NOT allowlist `/liv-login` or `/liv`.
       - Everything else: `const session = token ? await this.verifySessionFull(token) : null`
         (token from `request.cookies?.LIVINITY_SESSION` or Bearer header, same as /auth/verify).
         Valid → next(). Invalid: GET with `Accept` containing `text/html` → 302
         `/login?redirect=<encodeURIComponent(request.originalUrl)>` (preserves the logged-out
         deep-link UX); anything else → 401 JSON `{error: 'unauthorized'}`.
       - Wrap the whole middleware body in try/catch → on internal error respond 401 (FAIL-CLOSED,
         never `next()` on error). This is the inverse-of-fail-open posture the report demands.
    2. systemd/liv-assistant.service — the :3020 bind host is compiled into the upstream aionui-web
       prebuilt (live-verified loopback-only today, per LIVOS-049 verifier note). We cannot set it
       in-repo, so PIN it fail-closed: add after ExecStart:
       ```
       # LIVOS-049 (Phase 262 WS1): aionui-web's bind host is upstream-compiled. Live-verified
       # 127.0.0.1:3020 today; this assertion fail-stops the unit if a future AionUi bump ever
       # binds non-loopback (defense-in-depth; UFW-deny 3020 is the operator WS6 step).
       ExecStartPost=/bin/bash -c 'for i in $(seq 1 30); do ss -ltnH "sport = :3020" | grep -q . && break; sleep 1; done; if ss -ltnH "sport = :3020" | grep ":3020" | grep -vq "127.0.0.1:3020"; then echo "FATAL: aionui-web bound non-loopback on :3020" >&2; exit 1; fi'
       ```
    3. Sanity-grep before finishing: `grep -rn "'/liv/trpc'" livos/packages/ui/src scripts/` — if
       any in-repo consumer of the removed bridge exists, list it in the SUMMARY as expected
       breakage per the LOCKED bridge-removal decision (do NOT re-add the bridge).
  </action>
  <acceptance_criteria>
    - server/index.ts: an apex middleware exists that 401/302s non-allowlisted apex paths without a valid session, with an explicit APEX_PUBLIC_PREFIXES allowlist and a catch→401 (fail-closed) error path
    - server/index.ts: `/liv-login` and `/liv` are NOT in the allowlist
    - systemd/liv-assistant.service: contains an ExecStartPost that exits 1 when :3020 is bound non-loopback
    - tsc check on livinityd produces no NEW errors vs baseline (`cd livos/packages/livinityd && npx tsc --noEmit` — pre-existing ~TS18048 noise does not block)
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/server/liv-login-handler.test.ts source/modules/domain/caddy.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Apex is fail-closed behind an explicit allowlist; a non-loopback :3020 bind fail-stops liv-assistant; no in-repo /liv/trpc consumer left silently broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → Caddy apex (`bruce.livinity.io`) | unauthenticated traffic reaches livinityd :8080 and AionUi :3020 via relay |
| LivOS shell origin → framed AionUi SPA | same-origin iframe with allow-same-origin+allow-scripts |
| forward_auth → /auth/verify | Caddy trusts livinityd's 200/401 verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-01 | Spoofing | /liv-login cookie mint | mitigate | verifySession gate in handler + forward_auth at Caddy (Tasks 1+2) |
| T-262-02 | Spoofing | Referer-routed /api/* | mitigate | path-prefix matcher + forward_auth (Task 1) |
| T-262-03 | Elevation | framed SPA → LivOS tRPC | mitigate | delete @liv_trpc bridge (Task 1); full origin split deferred (WS6) |
| T-262-04 | Repudiation/Spoofing | revoked session reuse at /auth/verify, /__livos_sso, /__livos_auth | mitigate | verifySessionFull + createSession (Task 2) |
| T-262-05 | Info Disclosure | future unguarded apex route | mitigate | apex fail-closed gate (Task 3) |
| T-262-06 | Info Disclosure | :3020 direct bind exposure | mitigate (code) / transfer (operator) | ExecStartPost bind assert; UFW-deny 3020 = operator WS6 |
| T-262-07 | Elevation | prompt-injected agent w/ ambient ~/.claude RW | accept (this phase) | inherent residual per LIVOS-049; credential brokering is a follow-up |
</threat_model>

<verification>
- `cd livos/packages/livinityd && npx vitest run source/modules/domain/caddy.test.ts source/modules/server/liv-login-handler.test.ts` — all green.
- `grep -c 'header_regexp Referer' source/modules/domain/caddy.ts` == 0; `grep -c '@liv_trpc' source/modules/domain/caddy.ts` == 0.
- `grep -n 'forward_auth' source/modules/domain/caddy.ts` shows the gate inside @liv, @liv_ws, @liv_api_subresource, @livos_terminal_ws, @liv_login (plus the pre-existing gatedHandleBody).
- `npx tsc --noEmit` — no NEW errors vs baseline.
- Report WS1 success criteria (live items deferred to operator deploy walk): curl /liv-login without a valid LIVINITY_SESSION returns 401/redirect and mints no aionui-session cookie; /liv, /ws, /api/*→:3020 all 401 unauthenticated; a revoked session is rejected at /auth/verify.
</verification>

<success_criteria>
- All five /liv-family Caddy surfaces forward_auth-gated; /liv-login Express handler 401s without a verified session; jti revocation + active-user re-check live at /auth/verify and /__livos_sso; /__livos_auth sessions revocable; /liv/trpc bridge removed; apex fail-closed; :3020 loopback pinned fail-closed. Tests green, no new tsc errors, atomic commits per task.
</success_criteria>

<output>
After completion, create `.planning/phases/262-security-hardening-pass-3/262-01-SUMMARY.md`
</output>
