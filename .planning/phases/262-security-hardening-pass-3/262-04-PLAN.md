---
phase: 262-security-hardening-pass-3
plan: 262-04
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/system/routes.ts
  - livos/packages/livinityd/source/modules/files/routes.ts
  - livos/packages/livinityd/source/modules/files/external-storage.ts
  - livos/packages/livinityd/source/modules/files/network-storage.ts
  - livos/packages/livinityd/source/modules/apps/cred-egress-proxy.ts
  - livos/packages/livinityd/source/modules/apps/cred-egress-proxy.test.ts
  - livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts
autonomous: true
requirements: [LIVOS-046, LIVOS-048, LIVOS-050, LIVOS-051, LIVOS-056]
must_haves:
  truths:
    - "system.restart/system.shutdown and all storage/share mutation+secret routes return FORBIDDEN for non-admin callers (adminProcedure, server-side)"
    - "formatExternalDevice/unmountExternalDevice reject a deviceId not in the USB-only external set (e.g. nvme0n1) before any destructive command"
    - "addNetworkShare rejects host/username/password containing `,`/`=`/newline and blocks loopback/RFC1918/metadata SMB hosts; creds pass via a credentials= file (no inline option-string injection)"
    - "cred-egress proxy CONNECT without the issued per-app token returns 403; the proxy binds to the docker-bridge gateway address (not 0.0.0.0); the source-IP CIDR is the actual bridge CIDR"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/system/routes.ts"
      provides: "shutdown/restart as adminProcedure"
    - path: "livos/packages/livinityd/source/modules/files/routes.ts"
      provides: "format/unmount/network-share/samba/sharePassword as adminProcedure"
    - path: "livos/packages/livinityd/source/modules/files/external-storage.ts"
      provides: "deviceId external-set membership + strict regex guard"
    - path: "livos/packages/livinityd/source/modules/apps/cred-egress-proxy.ts"
      provides: "per-app token CONNECT gate + gateway-interface bind + tightened CIDR"
  key_links:
    - from: "system/routes.ts shutdown/restart"
      to: "adminProcedure (server/trpc/trpc.ts:35)"
      via: "procedure swap"
      pattern: "shutdown: adminProcedure"
    - from: "cred-egress-proxy.ts CONNECT handler"
      to: "per-app token registry"
      via: "Proxy-Authorization check tied to bridge IP"
      pattern: "403"
---

<objective>
WS4 — Re-classify the new Settings backends + cred-egress isolation.

Closes: **LIVOS-048 (High)** non-admin `system.restart`/`system.shutdown` (mgmt-plane DoS +
power-off); **LIVOS-050 (Medium)** any user can `formatExternalDevice` an arbitrary block device
incl. the OS disk (unvalidated `deviceId`); **LIVOS-051 (Medium)** `addNetworkShare` CIFS
mount-option injection + arbitrary-host SMB SSRF; **LIVOS-056 (Low)** `unmount`/`removeNetworkShare`/
samba toggles/`sharePassword`-secret reachable by any user; **LIVOS-046 (High)** cred-egress proxy
authenticates by docker-bridge source IP only → any container borrows the operator's OAuth
subscription.

Purpose: privileged host-control + storage + cred-egress operations become admin-only and
input-validated server-side (these routes are reachable from the Files app too, not just Settings),
and the cred proxy gains a per-app identity instead of trusting the whole bridge.

**HARD CONSTRAINTS: CODE ONLY — NO DEPLOY.** No update.sh, systemctl, SSH, or ufw (the live UFW-deny
13129 is operator WS6). Backend = tsx, no build. Only NEW tsc errors block (~TS18048 baseline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-security-hardening-pass-3/262-CONTEXT.md
@SECURITY-RESEARCH-PASS-3.md  (LIVOS-046 / 048 / 050 / 051 / 056 + WS4 success criteria)

<interfaces>
Procedure helpers: `adminProcedure` (server/trpc/trpc.ts:35 = privateProcedure + requireRole('admin')),
`privateProcedure` (:29), `publicProcedureWhenNoUserExists`. requireRole fails closed for non-admin
and admits legacy single-user only via the explicit ctx.legacySingleUser flag (256-04).

system/routes.ts: `shutdown: privateProcedure.mutation(...)` (:250), `restart: privateProcedure`
(:257). adminProcedure already imported (:28). factoryReset at :288 is the adminProcedure analog.

modules/files/routes.ts (the files module lives at modules/files/, NOT modules/apps/files/):
- `sharePassword: privateProcedure.query(... samba.getSharePassword())` (:180)
- `addShare: privateProcedure` (:186), `removeShare: privateProcedure` (:191)
- `formatExternalDevice: privateProcedure` (:196) → externalStorage.formatExternalDevice(input)
- `unmountExternalDevice: privateProcedure` (:212) → externalStorage.unmountExternalDevice(id, {remove:true})
- `addNetworkShare: publicProcedureWhenNoUserExists` (:241) → networkStorage.addShare(input)
- `removeNetworkShare: privateProcedure` (:253)
- `discoverNetworkSharesOnServer: publicProcedureWhenNoUserExists` (:263)

external-storage.ts formatExternalDevice (:270-331): interpolates deviceId into `sgdisk --zap-all
/dev/${deviceId}`, `wipefs -a`, `parted -s ... mklabel gpt`, `mkfs.ext4 -F -L ${label}
/dev/${deviceId}1`. It calls getMountedExternalDevices() only to decide unmount-first — NEVER
cross-checks deviceId against the external set. unmountExternalDevice already does a find() lookup
(~:233) — mirror it. #getExternalDevices() is the USB-transport-only list.

network-storage.ts #mountShare (:128-152): `mount -t cifs //${host}/${share} ${mnt} -o
username=${share.username},password=${share.password},uid=...` — no whitespace ⇒ one argv token ⇒
username/password are inside the option string. addShare (:187-213) persists + the 60s
#watchAndMountShares re-mounts.

cred-egress-proxy.ts: CREDPROXY_PORT=13129 (:51), CREDPROXY_PLACEHOLDER_KEY='__livinity_credproxy__'
(:55), DEFAULT_BRIDGE_SUBNET='172.16.0.0/12' (:58), isFromBridge (:187), CONNECT handler (:399-434,
gate is `isFromBridge(remote, subnet)` + `isInjectableHost(target)` only), server.listen(
CREDPROXY_PORT) with NO host arg (:546, binds 0.0.0.0). inject-local-ai-clis.ts:266-278 sets
`HTTPS_PROXY=http://livinity-credproxy:13129` + `ANTHROPIC_API_KEY=__livinity_credproxy__`
placeholder — this is the per-app delivery slot. cred-egress-proxy.test.ts is the existing test.

SSRF guard to reuse for SMB host: webapps/url-validator.ts (blocks loopback/RFC1918/link-local/
metadata) — or replicate its IP-range checks directly on the resolved host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: adminProcedure for power + storage/share mutations & secret; deviceId external-set validation</name>
  <files>livos/packages/livinityd/source/modules/system/routes.ts, livos/packages/livinityd/source/modules/files/routes.ts, livos/packages/livinityd/source/modules/files/external-storage.ts</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-048 (Recommendation: mirror factoryReset), LIVOS-050 (resolve deviceId against #getExternalDevices, add strict regex), LIVOS-056 (promote the defense-in-depth set; "must not be UI-only — reachable from Files app")
    - livos/packages/livinityd/source/modules/system/routes.ts:250-264, :288 (factoryReset analog)
    - livos/packages/livinityd/source/modules/files/routes.ts:180-263
    - livos/packages/livinityd/source/modules/files/external-storage.ts:270-331 (format) + the unmount find()-lookup (~:233) to mirror
  </read_first>
  <action>
    Implements CONTEXT D-WS4 items (a) and (b).

    1. system/routes.ts — `shutdown: privateProcedure` → `shutdown: adminProcedure`;
       `restart: privateProcedure` → `restart: adminProcedure`. (One word each; matches the
       client-side adminOnly intent + factoryReset precedent.)
    2. files/routes.ts — change these from their current procedure to `adminProcedure`:
       `sharePassword` (:180, leaks the 128-char Samba secret), `addShare` (:186), `removeShare`
       (:191), `formatExternalDevice` (:196), `unmountExternalDevice` (:212), `addNetworkShare`
       (:241 — currently publicProcedureWhenNoUserExists, the unauth pre-onboarding window is the
       worst case), `removeNetworkShare` (:253), and `discoverNetworkSharesOnServer` (:263). Leave
       the read-only `list`/`externalDevices`/`listNetworkShares`/`viewPreferences` queries as-is.
    3. external-storage.ts formatExternalDevice (:270-331) — BEFORE any sgdisk/wipefs/parted/mkfs
       command:
       ```ts
       const DEVICE_ID_RE = /^(sd[a-z]+|nvme\d+n\d+|mmcblk\d+)$/
       if (!DEVICE_ID_RE.test(deviceId)) throw new Error('[invalid-device-id]')
       const external = await this.#getExternalDevices()  // USB-transport-only set
       if (!external.some((d) => d.id === deviceId)) throw new Error('[invalid-device-id]')
       ```
       (Use the actual method name the class exposes for the USB-only external list — read the file;
       the report names it `#getExternalDevices()`. The `unmountExternalDevice` find()-lookup is the
       in-file precedent.) Apply the SAME guard inside unmountExternalDevice's `/sys/block/
       ${deviceId}/device/delete` path if it does not already resolve against the external set.
  </action>
  <acceptance_criteria>
    - system/routes.ts: `grep -n 'shutdown: adminProcedure' system/routes.ts` and `restart: adminProcedure` both match
    - files/routes.ts: sharePassword, addShare, removeShare, formatExternalDevice, unmountExternalDevice, addNetworkShare, removeNetworkShare, discoverNetworkSharesOnServer all declared adminProcedure
    - external-storage.ts: formatExternalDevice validates deviceId against /^(sd[a-z]+|nvme\d+n\d+|mmcblk\d+)$/ AND external-set membership before any sgdisk/mkfs call
    - no NEW tsc errors
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Power/storage/share mutations + the samba secret are admin-only server-side; formatExternalDevice cannot target the OS disk.</done>
</task>

<task type="auto">
  <name>Task 2: CIFS input validation + credentials-file mount + SMB SSRF block (LIVOS-051)</name>
  <files>livos/packages/livinityd/source/modules/files/network-storage.ts</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-051 (Description: comma/equals/newline → mount-option injection; host = SMB/HTTP SSRF primitive; Recommendation: validate charset, pass creds via `mount -o credentials=<file>`, block loopback/RFC1918/metadata)
    - livos/packages/livinityd/source/modules/files/network-storage.ts:128-152 (#mountShare), :187-213 (addShare), the smbclient/ky probe helpers (discoverNetworkSharesOnServer, isServerAnLivinityDevice)
    - livos/packages/livinityd/source/modules/webapps/url-validator.ts — IP-range SSRF checks to reuse/replicate
  </read_first>
  <action>
    Implements CONTEXT D-WS4 item (c).

    1. Add validators at the top of addShare (:187) and any direct #mountShare entry:
       - host: `const SMB_HOST_RE = /^[a-zA-Z0-9._-]+$/` (reject `,`/`=`/`/`/whitespace/newline);
         then resolve + reject loopback/RFC1918/link-local(169.254)/metadata(169.254.169.254) and
         `::1`/fc00::/7 — reuse url-validator's range logic (or replicate the octet checks). Throw
         `[invalid-smb-host]` on violation. Apply the SAME host guard to
         discoverNetworkSharesOnServer and isServerAnLivinityDevice (the SSRF probe primitives).
       - share: `/^[a-zA-Z0-9._$ -]+$/` (typical SMB share names).
       - username/password: STRICTLY reject `,`, `=`, and newline (`/[\n\r,=]/`) → throw
         `[invalid-credentials]`. These are the option-string injection chars.
    2. #mountShare (:140) — STOP interpolating username/password into the `-o` string. Write a
       0600 temp credentials file (`username=<u>\npassword=<p>\n`) under a livinityd-owned scratch
       dir and mount with `mount -t cifs //${host}/${share} ${mnt} -o
       credentials=${credFile},uid=${userId},gid=${groupId},iocharset=utf8`. Delete the cred file
       in a finally after mount returns (success OR failure). This makes option-string injection
       structurally impossible even if a charset check is ever bypassed.
    Keep the existing execa `$` tagged-template idiom; the credFile path is the only new
    interpolation and it is a server-generated path (not attacker-controlled).
  </action>
  <acceptance_criteria>
    - network-storage.ts: addShare rejects host not matching /^[a-zA-Z0-9._-]+$/ and username/password matching /[\n\r,=]/
    - network-storage.ts: loopback/RFC1918/link-local/metadata SMB hosts are rejected (SSRF) in addShare AND the discover/isLivinityDevice probes
    - network-storage.ts: #mountShare passes credentials via `-o credentials=<file>` (no inline `password=${...}`), and the cred file is removed in a finally
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx tsc --noEmit 2>&1 | tail -5 && ! grep -q 'password=\${share.password}' source/modules/files/network-storage.ts</automated>
  </verify>
  <done>CIFS creds can no longer inject mount options; the SMB host param can no longer reach internal/metadata services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cred-egress proxy per-app token CONNECT gate + gateway-interface bind + tightened CIDR</name>
  <files>livos/packages/livinityd/source/modules/apps/cred-egress-proxy.ts, livos/packages/livinityd/source/modules/apps/cred-egress-proxy.test.ts, livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-046 (Recommendation: mint a unique opaque token per requiresLocalAiClis install, deliver via the __livinity_credproxy__ slot, require a matching Proxy-Authorization/X-Livinity-App-Token tied to the bridge IP, reject 403 absent/unknown; bind to the specific docker-bridge gateway, tighten /12 to the actual bridge CIDR) + the WS-C "[Medium] Firewall 13129" rec
    - livos/packages/livinityd/source/modules/apps/cred-egress-proxy.ts — CONNECT handler (:399-434), isFromBridge (:187), server.listen (:546), CREDPROXY_PLACEHOLDER_KEY (:55), DEFAULT_BRIDGE_SUBNET (:58)
    - livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts:266-278 — the HTTPS_PROXY + placeholder injection slot
    - livos/packages/livinityd/source/modules/apps/cred-egress-proxy.test.ts — existing CONNECT-gate test style
  </read_first>
  <behavior>
    - CONNECT with NO Proxy-Authorization (and no X-Livinity-App-Token) → 403 (current source-IP-only pass must now also require a token)
    - CONNECT with an UNKNOWN token → 403
    - bind-on-first-use: a KNOWN but UNBOUND token's first CONNECT claims its source IP (200); a later CONNECT presenting that SAME token from a DIFFERENT source IP → 403
    - CONNECT with a KNOWN token from its bound bridge IP + allowlisted host → 200 Connection Established (MITM path)
    - registerAppToken/revokeAppToken round-trip: a revoked token → 403
    - per-app-network reach: isFromBridge ACCEPTS a per-app compose-network source (e.g. 172.18.0.2) AND a docker0 source (172.17.0.2); a non-bridge / public source (e.g. 10.0.0.5) → 403. The token — NOT a narrowed CIDR — is the primary auth.
  </behavior>
  <action>
    Implements CONTEXT D-WS4 item (d).

    1. cred-egress-proxy.ts — add a per-app token registry with BIND-ON-FIRST-USE (the container's
       compose-network IP is not deterministically known at inject time, so the token claims its
       source IP on first CONNECT and is pinned thereafter — no docker-inspect race):
       ```ts
       const _appTokens = new Map<string, {bridgeIp: string | null}>()  // token -> bound IP (null until first use)
       export function registerAppToken(token: string, bridgeIp: string | null = null): void { _appTokens.set(token, {bridgeIp}) }
       export function revokeAppToken(token: string): void { _appTokens.delete(token) }
       export function mintAppToken(): string { return crypto.randomBytes(24).toString('hex') }
       export function checkAppToken(token: string | null, remoteIp: string): boolean {
         if (!token) return false
         const rec = _appTokens.get(token)
         if (!rec) return false
         const norm = (ip: string) => ip.startsWith('::ffff:') ? ip.slice(7) : ip
         const src = norm(remoteIp)
         if (rec.bridgeIp == null) { rec.bridgeIp = src; return true }  // bind-on-first-use
         return norm(rec.bridgeIp) === src
       }
       ```
    2. CONNECT handler (:399) — AFTER the isFromBridge check, parse the token: prefer
       `Proxy-Authorization: Basic <base64(user:token)>` (Node emits this when HTTPS_PROXY carries
       userinfo), fall back to an `X-Livinity-App-Token` header. If `!checkAppToken(token, remote)`
       → `403 Forbidden` + destroy (same idiom as the existing non-bridge 403). This is the
       per-container identity the report requires; source-IP alone is no longer sufficient.
    3. Bind to the gateway interface, not 0.0.0.0. Change `server.listen(CREDPROXY_PORT)` (:546) to
       `server.listen(CREDPROXY_PORT, bridgeGatewayAddr)` where `bridgeGatewayAddr` is resolved at
       startup (the docker bridge gateway, e.g. `172.17.0.1` — read it from the proxy's existing
       config/creds object or resolve the `docker0` gateway; fall back to `127.0.0.1` only if
       unresolved, logging a warning). **Do NOT narrow `DEFAULT_BRIDGE_SUBNET` to a single `/16`.**
       LivOS runs apps on per-app/per-user compose networks (`br-*`, typically `172.18.0.0/16` and
       up), all within `172.16.0.0/12` — a `/16` would 403 the legitimate `requiresLocalAiClis`
       container (the very feature this protects). Keep `isFromBridge` scoped to the docker-bridge
       range (`172.16.0.0/12`, which covers docker0 + all `br-*`) — OR, better, derive the allowed
       CIDR set from `docker network inspect` of the LivOS bridges at startup. `isFromBridge` is now
       only a coarse "are you on a docker bridge at all" gate (defense-in-depth); **the per-app TOKEN
       is the primary auth** and is what pins identity. The gateway-interface bind (not the CIDR) is
       what keeps the proxy off the public/LAN interface.
    4. inject-local-ai-clis.ts — when wiring a `requiresLocalAiClis` container, mint a token via
       `mintAppToken()`, register it UNBOUND via `registerAppToken(token)` (bridgeIp defaults to
       null → bind-on-first-use; this avoids the compose-network-IP-not-known-at-inject-time race),
       and deliver it by embedding it in the proxy URL userinfo:
       `HTTPS_PROXY=http://app:<token>@livinity-credproxy:13129` (so the CLIs' CONNECT carries
       `Proxy-Authorization: Basic`). The first CONNECT the container makes binds the token to that
       container's source IP; any other source presenting the same token thereafter is 403'd. Keep
       the placeholder `ANTHROPIC_API_KEY` slot as-is (the upstream bearer is still injected by the
       proxy). Revoke the token (`revokeAppToken`) on app stop/uninstall where the inject lifecycle
       has a teardown hook.
    5. cred-egress-proxy.test.ts — write the RED cases from <behavior> FIRST, then GREEN. Reuse the
       existing CONNECT-gate harness; assert 403 on absent/unknown/wrong-IP token and 200 on the
       matched token.
  </action>
  <acceptance_criteria>
    - cred-egress-proxy.ts: a container without the issued proxy token gets 403 from the CONNECT handler (checkAppToken gate present after isFromBridge)
    - cred-egress-proxy.ts: server.listen is called with a bind host argument (not bare port → not 0.0.0.0)
    - cred-egress-proxy.ts: checkAppToken binds the token to its source IP on first use, then 403s a different source presenting the same token (bind-on-first-use)
    - cred-egress-proxy.ts: a CONNECT from a per-app compose-network source (e.g. 172.18.0.2) with a valid token still passes (the bridge check is NOT narrowed to a single /16 that would 403 br-* networks)
    - inject-local-ai-clis.ts: mints + registers an unbound per-app token and delivers it via the HTTPS_PROXY userinfo (Proxy-Authorization)
    - cred-egress-proxy.test.ts green with the absent/unknown/first-use-bind/wrong-IP/valid token cases + a 172.18.x source acceptance case
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/apps/cred-egress-proxy.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>The cred proxy requires a per-app token tied to its bridge IP, binds to the bridge gateway only, and uses the real bridge CIDR — a stray container can no longer borrow the operator subscription.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin authed user → power/storage tRPC | role boundary the UI claims but the backend did not enforce |
| caller-supplied deviceId/host/creds → privileged block/mount commands | input boundary into destructive sinks |
| docker bridge container → cred-egress proxy | only the source-IP was trusted; now a per-app token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-17 | Elevation/Denial | system.restart/shutdown privateProcedure | mitigate | adminProcedure (Task 1) |
| T-262-18 | Tampering | formatExternalDevice arbitrary deviceId | mitigate | regex + external-set membership (Task 1) |
| T-262-19 | Info Disclosure | sharePassword secret to non-admin | mitigate | adminProcedure (Task 1) |
| T-262-20 | Tampering/SSRF | CIFS option injection + arbitrary SMB host | mitigate | charset validation + credentials-file + IP-range block (Task 2) |
| T-262-21 | Spoofing | cred-egress source-IP-only auth → sub abuse | mitigate | per-app token tied to bridge IP + gateway bind + tight CIDR (Task 3) |
| T-262-22 | Spoofing | direct :13129 reach from non-docker hosts | transfer | UFW-deny 13129 = operator WS6; gateway-bind narrows it in code |
</threat_model>

<verification>
- `cd livos/packages/livinityd && npx vitest run source/modules/apps/cred-egress-proxy.test.ts` — green.
- `grep -n 'shutdown: adminProcedure\|restart: adminProcedure' source/modules/system/routes.ts` — both match.
- `! grep -q 'password=\${share.password}' source/modules/files/network-storage.ts`.
- `npx tsc --noEmit` — no NEW errors.
- Report WS4 success criteria (live half = operator): non-admin calls to power/format/share routes return FORBIDDEN; formatExternalDevice on the OS disk is rejected as non-external; a container without the issued proxy token gets 403 from :13129.
</verification>

<success_criteria>
- All five findings closed in code: power/storage/share mutations + samba secret are admin-only; deviceId + CIFS inputs validated server-side; cred-egress proxy gains per-app token identity + gateway bind + real CIDR. Tests green, no new tsc errors, atomic commits per task.
</success_criteria>

<output>
After completion, create `.planning/phases/262-security-hardening-pass-3/262-04-SUMMARY.md`
</output>
