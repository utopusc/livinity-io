---
phase: 262-security-hardening-pass-3
plan: 262-03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install/deploy-livinityd.sh
  - scripts/install/sudoers.d/livinityd
  - scripts/install/__tests__/test-deploy-livinityd.sh
  - scripts/install/__tests__/test-sudoers-livinityd.sh
  - livos/packages/livinityd/source/modules/fail2ban-admin/client.ts
autonomous: true
requirements: [LIVOS-043]
must_haves:
  truths:
    - "deploy-livinityd.sh no longer provisions any `ALL=(ALL) NOPASSWD:ALL` sudoers drop-in; re-provision actively removes a pre-existing 99-<user> drop-in"
    - "The scoped Phase-192 fragment gains a narrow LIVINITYD_FAIL2BAN Cmnd_Alias so the fail2ban panel keeps working via `sudo -n` once the blanket grant is gone"
    - "fail2ban-admin invokes fail2ban-client via `sudo -n` (works under the scoped grant, not the blanket one)"
  artifacts:
    - path: "scripts/install/deploy-livinityd.sh"
      provides: "_dld_create_desktop_user without the blanket sudoers write"
    - path: "scripts/install/sudoers.d/livinityd"
      provides: "LIVINITYD_FAIL2BAN Cmnd_Alias (exact fail2ban-client argv shapes only)"
      contains: "LIVINITYD_FAIL2BAN"
    - path: "livos/packages/livinityd/source/modules/fail2ban-admin/client.ts"
      provides: "sudo -n /usr/bin/fail2ban-client invocation"
  key_links:
    - from: "fail2ban-admin/client.ts execFile"
      to: "scripts/install/sudoers.d/livinityd LIVINITYD_FAIL2BAN"
      via: "sudo -n argv matching the Cmnd_Alias shapes"
      pattern: "sudo.*-n.*fail2ban-client"
---

<objective>
WS3 — Remove the blanket `bruce` sudoers grant.

Closes: **LIVOS-043 (High)** — `_dld_create_desktop_user()` writes `bruce ALL=(ALL) NOPASSWD:ALL`
to `/etc/sudoers.d/99-bruce` in the SAME deploy run that installs the carefully-scoped Phase-192
fragment, silently subsuming it. Every `bruce`-reachable bug (LIVOS-041, LIVOS-042, any future
shell-out injection) becomes a root bug because of this single line.

Purpose: stop provisioning the amplifier. After this change, `sudo -n -l` as `bruce` on a freshly
provisioned box lists ONLY the scoped Cmnd_Aliases. The fail2ban Settings panel must keep working
via a new narrow scoped grant.

Scope note (CONTEXT `## Deferred` / WS6): the LIVE `/etc/sudoers.d/99-bruce` removal on the
Mini PC is an OPERATOR step — this phase only stops provisioning it (and makes re-provision clean
it up). **CODE ONLY — NO DEPLOY**: no SSH, no live sudoers edits, no systemctl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-security-hardening-pass-3/262-CONTEXT.md
@SECURITY-RESEARCH-PASS-3.md  (LIVOS-043 + WS3 success criteria)

<interfaces>
The offending write (scripts/install/deploy-livinityd.sh:437, inside `_dld_create_desktop_user()`,
called at :2326):
```sh
printf '%s\n' "${user} ALL=(ALL) NOPASSWD:ALL" > "$tmp_sudoers"
```
(then visudo-validated and installed to `/etc/sudoers.d/99-${user}`). Related comments referencing
the NOPASSWD drop-in: :78, :355, :366, :429.

Scoped fragment: scripts/install/sudoers.d/livinityd — one binary per Cmnd_Alias, e.g.
`Cmnd_Alias LIVINITYD_TIMEDATECTL = /usr/bin/timedatectl set-timezone *` (:56). Read the whole
file for the user-spec line that joins the aliases — the new alias must be appended to that join.

fail2ban-admin/client.ts: `const BINARY_PATH = '/usr/bin/fail2ban-client'` (:34), invoked at :167
via `execFile(BINARY_PATH, args, {timeout: EXECFILE_TIMEOUT_MS})` — NO sudo today (it has been
riding the blanket grant / root-reachable socket). Argv shapes used by the five procedures (read
the file to enumerate exactly): `status`, `status <jail>`, `set <jail> banip <ip>`,
`set <jail> unbanip <ip>`, plus the events/ignoreip reads.

EXISTING TEST ASSERTIONS THAT MUST BE INVERTED — scripts/install/__tests__/test-deploy-livinityd.sh
:1045-1058 currently PASSES when the helper writes `/etc/sudoers.d/99-` and `NOPASSWD:ALL`
(Phase 106 Bug #10 era). These two assertions must flip to assert ABSENCE.
test-sudoers-livinityd.sh asserts the scoped fragment's shape — extend for the new alias.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy-livinityd.sh — delete the blanket NOPASSWD write + active cleanup of a pre-existing drop-in</name>
  <files>scripts/install/deploy-livinityd.sh</files>
  <read_first>
    - scripts/install/deploy-livinityd.sh — the full `_dld_create_desktop_user()` body (find it via `awk '/^_dld_create_desktop_user\(\)/,/^}/'`), the call site :2326, and comments :78, :355-366, :429
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-043 (Description + Residual Risk: "a code change alone will not remove the already-written file" — hence the active rm on re-provision)
  </read_first>
  <action>
    Implements CONTEXT D-WS3.

    1. In `_dld_create_desktop_user()`: DELETE the entire blanket-sudoers provisioning block — the
       `printf '%s\n' "${user} ALL=(ALL) NOPASSWD:ALL" > "$tmp_sudoers"` line (:437) plus its
       tmp-file/visudo/install plumbing for `/etc/sudoers.d/99-${user}`. Replace with:
       ```sh
       # Phase 262 WS3 (LIVOS-043): the blanket `${user} ALL=(ALL) NOPASSWD:ALL` drop-in is GONE.
       # Privileges come ONLY from the scoped Cmnd_Alias fragment (sudoers.d/livinityd).
       # Re-provision actively removes a legacy drop-in left by older installs.
       rm -f "/etc/sudoers.d/99-${user}"
       ```
       Keep the user-creation / group-membership (sudo+docker groups) parts of the helper intact —
       group membership without a NOPASSWD grant means password-prompted sudo only, which is the
       intended posture.
    2. Update the now-stale comments at :78, :355, :366 (and the step description text near :366,
       e.g. "create desktop user (sudo + docker + NOPASSWD sudoers)" → "create desktop user
       (sudo + docker groups; scoped sudoers fragment only)") and the :429 block comment. After the
       edit the literal string `NOPASSWD:ALL` must appear ZERO times in the file (including
       comments — reword them).
    3. `bash -n scripts/install/deploy-livinityd.sh` must pass.
  </action>
  <acceptance_criteria>
    - `grep -c 'NOPASSWD:ALL' scripts/install/deploy-livinityd.sh` == 0
    - `_dld_create_desktop_user` body contains `rm -f "/etc/sudoers.d/99-${user}"`
    - `bash -n scripts/install/deploy-livinityd.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/install/deploy-livinityd.sh && ! grep -q "NOPASSWD:ALL" scripts/install/deploy-livinityd.sh</automated>
  </verify>
  <done>Fresh provisions never create the blanket grant; re-provisions remove a legacy one.</done>
</task>

<task type="auto">
  <name>Task 2: Scoped LIVINITYD_FAIL2BAN Cmnd_Alias + sudo -n invocation in fail2ban-admin client</name>
  <files>scripts/install/sudoers.d/livinityd, livos/packages/livinityd/source/modules/fail2ban-admin/client.ts</files>
  <read_first>
    - scripts/install/sudoers.d/livinityd — full file (alias style, the user-spec join line, the "No NOPASSWD: ALL" doc header)
    - livos/packages/livinityd/source/modules/fail2ban-admin/client.ts — ALL execFile call sites + the exact argv arrays each procedure builds (status/banip/unbanip/events/ignoreip)
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-043 Recommendation (the suggested alias shape) + the WS-A3 Mitigation-Confirmed entry (client.ts is fixed-argv, zod/regex-validated — preserve that)
    - Any fail2ban-admin test file (`ls source/modules/fail2ban-admin/`) — argv assertions must be updated for the sudo prefix
  </read_first>
  <action>
    Implements CONTEXT D-WS3 fail2ban continuity ("the fail2ban panel must keep working via the
    scoped grant").

    1. sudoers.d/livinityd — append, matching the file's exact comment + alias idiom:
       ```
       # Phase 262 WS3 (LIVOS-043) — fail2ban Settings panel. Exact subcommand shapes only;
       # client.ts validates jail/IP via zod+regex before spawn (see fail2ban-admin/client.ts).
       Cmnd_Alias LIVINITYD_FAIL2BAN = /usr/bin/fail2ban-client status, /usr/bin/fail2ban-client status *, /usr/bin/fail2ban-client set * banip *, /usr/bin/fail2ban-client set * unbanip *, /usr/bin/fail2ban-client get *
       ```
       Trim/extend the list to EXACTLY the argv shapes client.ts actually builds (enumerate them
       from the source — do not grant shapes no procedure uses). Add LIVINITYD_FAIL2BAN to the
       user-spec alias join line.
    2. client.ts — change the spawn at :167 (and any sibling execFile call) from
       `execFile(BINARY_PATH, args, ...)` to
       `execFile('/usr/bin/sudo', ['-n', BINARY_PATH, ...args], ...)`.
       Keep BINARY_PATH hardcoded, keep the timeout, keep ALL existing zod/regex validation
       untouched (the Mitigation-Confirmed entry depends on it). Update any client test asserting
       the spawned binary/argv.
    3. Why this works pre- and post-operator-walk: today the live box still has the blanket grant →
       `sudo -n` succeeds; after the operator installs the updated fragment and removes 99-bruce
       (WS6), `sudo -n` matches LIVINITYD_FAIL2BAN. Record this sequencing note in the SUMMARY for
       the operator.
  </action>
  <acceptance_criteria>
    - scripts/install/sudoers.d/livinityd contains `Cmnd_Alias LIVINITYD_FAIL2BAN` and STILL contains zero `NOPASSWD: ALL`/`NOPASSWD:ALL` occurrences
    - fail2ban-admin/client.ts spawns `/usr/bin/sudo` with `['-n', '/usr/bin/fail2ban-client', ...]`
    - existing fail2ban-admin tests (if present) green after argv update
  </acceptance_criteria>
  <verify>
    <automated>grep -q "LIVINITYD_FAIL2BAN" scripts/install/sudoers.d/livinityd && ! grep -qE "NOPASSWD: ?ALL" scripts/install/sudoers.d/livinityd && grep -q "sudo" livos/packages/livinityd/source/modules/fail2ban-admin/client.ts</automated>
  </verify>
  <done>fail2ban panel functions via the scoped grant alone; no blanket grant anywhere in provisioning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Invert the deploy-script test assertions (RED→GREEN) + extend the sudoers fragment test</name>
  <files>scripts/install/__tests__/test-deploy-livinityd.sh, scripts/install/__tests__/test-sudoers-livinityd.sh</files>
  <read_first>
    - scripts/install/__tests__/test-deploy-livinityd.sh:1040-1060 — the Bug #10 assertions that currently REQUIRE the 99- drop-in + NOPASSWD:ALL write
    - scripts/install/__tests__/test-sudoers-livinityd.sh — fragment shape assertions + pass/fail helper idiom
  </read_first>
  <behavior>
    - test-deploy-livinityd.sh: PASSES when `_dld_create_desktop_user` body does NOT write a `/etc/sudoers.d/99-` file and does NOT contain `NOPASSWD:ALL`; PASSES when the body contains the `rm -f "/etc/sudoers.d/99-` cleanup
    - test-sudoers-livinityd.sh: PASSES when the fragment contains LIVINITYD_FAIL2BAN with the exact subcommand shapes, and still contains no blanket NOPASSWD ALL
  </behavior>
  <action>
    1. Invert the two Bug #10 assertions at test-deploy-livinityd.sh:1045-1058: the awk-extracted
       helper body must NOT match `> .*sudoers` blanket writes nor `NOPASSWD:ALL`; ADD a positive
       assertion for the `rm -f "/etc/sudoers.d/99-` cleanup line. Use the file's existing
       pass/fail helper functions and label the assertions `LIVOS-043`.
    2. Extend test-sudoers-livinityd.sh: assert `LIVINITYD_FAIL2BAN` alias present, assert the
       alias grants ONLY /usr/bin/fail2ban-client shapes, re-assert no `NOPASSWD: ALL`.
    3. Run BOTH test scripts to completion (they are self-contained bash; run with bash, count
       PASS/FAIL in output) — zero FAIL required. Note for the SUMMARY: the suite's total PASS
       count changes vs the historic 190 — that is expected (assertions inverted, not removed).
  </action>
  <acceptance_criteria>
    - bash scripts/install/__tests__/test-deploy-livinityd.sh → 0 FAIL lines
    - bash scripts/install/__tests__/test-sudoers-livinityd.sh → 0 FAIL lines
    - the inverted assertions reference LIVOS-043 in their labels
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/install/__tests__/test-deploy-livinityd.sh 2>&1 | tail -3 && bash scripts/install/__tests__/test-sudoers-livinityd.sh 2>&1 | tail -3</automated>
  </verify>
  <done>The test suite now REGRESSION-LOCKS the absence of the blanket grant instead of requiring it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| livinityd (bruce) → root via sudoers | the scoped fragment is the ONLY intended privilege path |
| deploy script → /etc/sudoers.d | provisioning writes define the steady-state grant surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-14 | Elevation | blanket 99-bruce NOPASSWD:ALL | mitigate | delete write + rm-on-reprovision (Task 1); live removal = operator WS6 |
| T-262-15 | Denial | fail2ban panel breaks post-removal | mitigate | LIVINITYD_FAIL2BAN scoped alias + sudo -n client (Task 2) |
| T-262-16 | Elevation | admin-compromised session unbans attacker IPs via scoped grant | accept | bounded to fail2ban state; admin-gated + regex-validated (verified closed in report) |
</threat_model>

<verification>
- `grep -rn "NOPASSWD:ALL" scripts/install/deploy-livinityd.sh` → empty (`grep -c` == 0).
- Both bash test suites 0 FAIL.
- Report WS3 success criterion (live half = operator): `sudo -n -l` as bruce lists ONLY scoped Cmnd_Aliases; fail2ban panel still functions via the scoped grant.
</verification>

<success_criteria>
- No provisioning path writes a blanket sudoers grant; legacy drop-ins are cleaned on re-provision; fail2ban continuity via LIVINITYD_FAIL2BAN + sudo -n; deploy/sudoers test suites inverted and green. Operator WS6 carries the live `/etc/sudoers.d/99-bruce` removal.
</success_criteria>

<output>
After completion, create `.planning/phases/262-security-hardening-pass-3/262-03-SUMMARY.md`
</output>
