---
phase: 262-security-hardening-pass-3
plan: 262-05
type: execute
wave: 2
depends_on: [262-02]
files_modified:
  - livos/packages/livinityd/source/modules/secrets/dek.ts
  - livos/packages/livinityd/source/modules/secrets/dek.unit.test.ts
  - livos/packages/livinityd/source/modules/docker/git-credentials.ts
  - livos/packages/livinityd/source/modules/docker/stack-secrets.ts
  - livos/packages/livinityd/source/modules/scheduler/backup-secrets.ts
  - livos/packages/livinityd/source/modules/apps/compose-generator.ts
  - livos/packages/livinityd/source/modules/apps/apps.ts
  - livos/packages/livinityd/source/modules/apps/routes.ts
  - livos/packages/livinityd/source/modules/apps/public-forbidden.test.ts
  - liv/packages/core/src/sandbox.ts
  - liv/packages/core/src/shell.ts
  - liv/packages/core/src/sandbox.test.ts
autonomous: true
requirements: [LIVOS-052, LIVOS-052b, LIVOS-057, LIVOS-058]
must_haves:
  truths:
    - "git/stack/backup credential stores no longer derive their at-rest key from the JWT signing secret — they read the shared credential-dek DEK"
    - "A JWT-secret rotation does not brick existing git/stack/backup blobs (getLegacyKey lazy-re-key fallback ported to all three)"
    - "buildPublicForbiddenSignals OR-sources requiresLocalAiClis from getBuiltinApp(appId) so an install-path-written flag-absent manifest still forbids a credentialed app"
    - "compose-generator + native builtin manifest writers thread requiresLocalAiClis + neverPublic (and the platform path threads neverPublic)"
    - "setPublicAccess (routes.ts:517) no longer infers isAdmin=true from an absent currentUser without the explicit ctx.legacySingleUser guard"
    - "bwrap usability is gated by a real runtime userns probe; on failure the shell tool returns a stable refusal and never silently falls back to unsandboxed exec; the sandboxed: log flag reflects actual engagement"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/secrets/dek.ts"
      provides: "shared getKey/getLegacyKey/encrypt/decrypt DEK module"
      contains: "credential-dek"
    - path: "liv/packages/core/src/sandbox.ts"
      provides: "runtime userns probe replacing bwrap --version"
  key_links:
    - from: "docker/git-credentials.ts, docker/stack-secrets.ts, scheduler/backup-secrets.ts"
      to: "modules/secrets/dek.ts"
      via: "import getKey/getLegacyKey (no local sha256(JWT))"
      pattern: "from '.*secrets/dek"
    - from: "apps/apps.ts buildPublicForbiddenSignals"
      to: "getBuiltinApp(appId).requiresLocalAiClis"
      via: "OR with on-disk manifest"
      pattern: "getBuiltinApp\\(appId\\)"
    - from: "liv/packages/core/src/shell.ts"
      to: "sandbox.ts runtime probe result"
      via: "usable gate → stable refusal on failure"
      pattern: "usable"
---

<objective>
WS5 — Finish the DEK migration + fast-follow hygiene.

Closes: **LIVOS-052 (Medium)** git/stack/backup credential stores still derive their AES key from
`sha256(JWT)` (LIVOS-033 fixed only registry); **LIVOS-052b (Info)** those siblings have no legacy
fallback so a JWT rotation bricks stored creds; **LIVOS-057 (Low)** builtin/native manifest writers
drop `requiresLocalAiClis`/`neverPublic` → latent public-forbidden fail-open + the routes.ts:517
legacySingleUser parity gap; **LIVOS-058 (Low)** bwrap `usable` gate is PATH-only (`bwrap --version`)
with no runtime userns probe.

Purpose: one secret no longer unlocks both auth and three credential domains; JWT rotation is
non-destructive; the public-forbidden guard cannot be silently disabled by a manifest-write
regression; the agent shell fails safe (never silently unsandboxed) when userns is unavailable.

**Wave 2 / depends_on 262-02:** this plan edits `apps/apps.ts` + `apps/routes.ts`, which WS2 (262-02)
also edits — they MUST NOT run concurrently. Start from the 262-02 commits.

**HARD CONSTRAINTS: CODE ONLY — NO DEPLOY.** No update.sh/systemctl/SSH. livinityd = tsx (no build).
**`liv/packages/core` is COMPILED** — after editing sandbox.ts/shell.ts run
`npm run build --workspace=packages/core` from `liv/` (Task 3). Only NEW tsc errors block.

Carry-forward (CONTEXT WS5 Low/optional, NOT in this plan's tasks per budget): moving the Kopia
backup repository password off cleartext in `backups/backups.ts:246,286,432` into the vault pattern,
and a dedicated `/opt/livos/data/secrets/backup-vault-key` distinct from the shared DEK. Record as a
follow-up in the SUMMARY — the DEK migration below already decouples backup-secrets.ts from
sha256(JWT), which is the load-bearing half.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-security-hardening-pass-3/262-CONTEXT.md
@SECURITY-RESEARCH-PASS-3.md  (LIVOS-052 / 052b / 057 / 058 + WS5 success criteria)

<interfaces>
FIXED reference — docker/registry-credentials.ts:
- CREDENTIAL_DEK_PATH = '/opt/livos/data/secrets/credential-dek' (:34), JWT_SECRET_PATH (:33)
- getKey() (:66) reads the DEK file (crypto.randomBytes(32) created 0600 if absent — read the impl);
  getLegacyKey() (:96) = sha256(trim(jwt)); encrypt/decrypt = iv12||tag16||ct base64.
- lazy re-key on decrypt (:255-278): try DEK; on throw, getLegacyKey, decrypt with legacy, then
  re-encrypt with DEK and persist (non-fatal on persist failure). MIRROR THIS EXACTLY per store.

The three UNFIXED siblings ALL currently have:
```ts
const JWT_SECRET_PATH = '/opt/livos/data/secrets/jwt'
let _key: Buffer | null = null
async function getKey(): Promise<Buffer> {
  if (_key) return _key
  const jwt = await readFile(JWT_SECRET_PATH, 'utf-8')
  _key = crypto.createHash('sha256').update(jwt.trim()).digest()
  return _key
}
```
plus identical encrypt()/decrypt(). Locations: git-credentials.ts:23-28 (PG column
`git_credentials.encrypted_data`), stack-secrets.ts:28-34 (Redis hash `liv:stack:secrets:{stack}`),
backup-secrets.ts:25-31 (Redis hash `liv:scheduler:backup-creds:{jobId}`).

Manifest writers:
- compose-generator.ts:111-148 — builtin manifest builder; threads requiresAiProvider (:134) +
  installOptions (:142); add requiresLocalAiClis + neverPublic the same way.
- apps.ts:461-474 — native builtin install branch writes a minimal manifest (no AI flags); add both.
- apps.ts platform path (~:1067-1093) threads requiresLocalAiClis but DROPS neverPublic — add
  `neverPublic: data.neverPublic ?? data.manifest?.neverPublic ?? undefined`.
- apps.ts:1722-1742 buildPublicForbiddenSignals reads ONLY the on-disk manifest
  (`manifest?.requiresLocalAiClis === true`). The mount path at :828-829 already ORs with
  getBuiltinApp — mirror that OR here.

routes.ts:517 setPublicAccess: `const isAdmin = ctx.currentUser ? ctx.currentUser.role === 'admin' : true`
— the `: true` is the unguarded legacy inference (the WS-A5 Info parity gap).

bwrap — liv/packages/core/src/sandbox.ts:
- probeBwrap() (:69-78) runs `execFileSync('bwrap', ['--version'])`; BWRAP_AVAILABLE (:78);
  wrapWithBwrap returns `usable: BWRAP_AVAILABLE` (:131).
- shell.ts:51 logs `sandboxed: BWRAP_AVAILABLE`; shell.ts:71-79 branches on `usable` — true →
  `execFile('bwrap', ...)`, false → unsandboxed `exec(command, {cwd: LIV_AGENT_WORKSPACE, env})`
  (the env-scrubbed dev fallback — fires ONLY when bwrap is off PATH).
- sandbox.test.ts runs via `npx tsx src/sandbox.test.ts` (node:assert/strict). liv build:
  `npm run build --workspace=packages/core` from liv/ (packages/core "build": "tsc").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared credential-dek module + migrate git/stack/backup stores off sha256(JWT) with legacy lazy-re-key</name>
  <files>livos/packages/livinityd/source/modules/secrets/dek.ts, livos/packages/livinityd/source/modules/secrets/dek.unit.test.ts, livos/packages/livinityd/source/modules/docker/git-credentials.ts, livos/packages/livinityd/source/modules/docker/stack-secrets.ts, livos/packages/livinityd/source/modules/scheduler/backup-secrets.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/docker/registry-credentials.ts:33-105, 255-278 — the FIXED reference (DEK file read, getLegacyKey, lazy re-key)
    - livos/packages/livinityd/source/modules/docker/registry-credentials.unit.test.ts — the unit-test style for the leaked-jwt + rotation properties to replicate
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-052 (Recommendation: shared dek.ts for all stores, keep sha256(JWT) as legacy decrypt only) + LIVOS-052b (no legacy fallback today = rotation brick)
    - the three siblings: docker/git-credentials.ts:15-45, docker/stack-secrets.ts:20-45, scheduler/backup-secrets.ts:15-45
  </read_first>
  <behavior>
    - dek.ts round-trip: `decrypt(encrypt(s, key), key) === s`
    - leaked-jwt property: a blob encrypted with the DEK does NOT decrypt under the legacy sha256(jwt) key
    - legacy-migration property: a blob encrypted with the legacy sha256(jwt) key DOES decrypt via the legacy path (getLegacyKey), proving JWT rotation is non-destructive
  </behavior>
  <action>
    Implements CONTEXT D-WS5 item (a).

    1. Create `modules/secrets/dek.ts` — extract the registry pattern into one shared module:
       export `getKey(): Promise<Buffer>` (reads CREDENTIAL_DEK_PATH = '/opt/livos/data/secrets/
       credential-dek', creating it with crypto.randomBytes(32) mode 0600 if absent — copy the
       registry impl verbatim incl. the fs-deps seam if present so it stays unit-testable),
       `getLegacyKey(): Promise<Buffer | null>` (sha256(trim(jwt)) from JWT_SECRET_PATH, null on
       miss), and `encrypt(plaintext, key)` / `decrypt(blob, key)` (the byte-identical iv12||tag16||
       ct base64 codec). Add a `_resetCachesForTests()` like registry has, if it has one.
    2. git-credentials.ts — DELETE the local JWT_SECRET_PATH/getKey/encrypt/decrypt; import them from
       '../secrets/dek.js'. On the READ path (the decrypt of `git_credentials.encrypted_data`), port
       the registry lazy-re-key: try `decrypt(blob, await getKey())`; on throw, `getLegacyKey()` →
       decrypt with legacy → re-encrypt with DEK → `UPDATE git_credentials SET encrypted_data=$1 ...`
       (non-fatal on persist failure). Mirror registry-credentials.ts:255-278 exactly, adjusted for
       the PG column.
    3. stack-secrets.ts — same: import from dek.ts; lazy-re-key on read, re-encrypting the Redis hash
       field via `hset liv:stack:secrets:{stack} {field} {reEncrypted}` (non-fatal on failure).
    4. backup-secrets.ts — same: import from dek.ts; lazy-re-key on read, re-encrypting the Redis hash
       `liv:scheduler:backup-creds:{jobId}` field (non-fatal on failure). (This also satisfies
       "derive the backup key off something other than sha256(JWT)" — the shared DEK is that
       something; the dedicated backup-vault-key variant is the deferred carry-forward.)
    5. After this task `grep -rn 'sha256.*jwt\|createHash(.sha256.).update(jwt' modules/docker/git-credentials.ts modules/docker/stack-secrets.ts modules/scheduler/backup-secrets.ts` returns ZERO
       direct-getKey hits (the only sha256(jwt) usage is inside dek.ts getLegacyKey).
    6. Write dek.unit.test.ts (vitest) covering the <behavior> properties using the fs-deps seam to
       inject a fixed DEK + JWT (no real disk). RED first, then GREEN.
  </action>
  <acceptance_criteria>
    - modules/secrets/dek.ts exists, exports getKey/getLegacyKey/encrypt/decrypt, reads '/opt/livos/data/secrets/credential-dek'
    - git-credentials.ts / stacks.ts(stack-secrets) / backup-secrets.ts: key derivation no longer reads the jwt secret directly (uses the shared credential-dek); each imports from '../secrets/dek.js' (or '../../secrets/dek.js')
    - each of the three siblings has a getLegacyKey lazy-re-key fallback on its decrypt/read path
    - dek.unit.test.ts green proving the leaked-jwt + legacy-migration properties
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/secrets/dek.unit.test.ts source/modules/docker/registry-credentials.unit.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>A leaked jwt no longer decrypts git/stack/backup blobs; JWT rotation is non-destructive (legacy fallback re-keys on read).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread requiresLocalAiClis/neverPublic into manifest writers + OR buildPublicForbiddenSignals with the builtin + routes.ts:517 legacySingleUser guard</name>
  <files>livos/packages/livinityd/source/modules/apps/compose-generator.ts, livos/packages/livinityd/source/modules/apps/apps.ts, livos/packages/livinityd/source/modules/apps/routes.ts, livos/packages/livinityd/source/modules/apps/public-forbidden.test.ts</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-057 (Recommendation parts 1/2/3: thread both flags, OR buildPublicForbiddenSignals with getBuiltinApp, add the install-path test) + the WS-A5 routes.ts:517 parity note in Mitigation-Confirmed
    - apps/compose-generator.ts:111-148 (manifest builder, requiresAiProvider thread at :134)
    - apps/apps.ts:461-474 (native builtin manifest), :828-829 (the mount-path OR to mirror), :1067-1093 (platform path that drops neverPublic), :1722-1742 (buildPublicForbiddenSignals)
    - apps/routes.ts:517 (setPublicAccess isAdmin inference) — note WS2 (262-02) already edited this file; rebase onto its commits
    - apps/public-forbidden.test.ts (pure isPublicForbidden tests — add the new buildPublicForbiddenSignals OR case alongside, or in a sibling apps test if the class needs construction)
  </read_first>
  <behavior>
    - buildPublicForbiddenSignals with an on-disk manifest where requiresLocalAiClis is ABSENT but getBuiltinApp(appId).requiresLocalAiClis === true → signals.requiresLocalAiClis === true → isPublicForbidden(signals).forbidden === true (reason local-ai-clis)
    - setPublicAccess: an absent currentUser that is NOT legacySingleUser → not treated as admin (the owner/forbidden gates still apply)
  </behavior>
  <action>
    Implements CONTEXT D-WS5 items (b) and (d).

    1. compose-generator.ts:111-148 — after the `requiresAiProvider` thread (:134), add:
       `if ((app as any).requiresLocalAiClis === true) manifest.requiresLocalAiClis = true`
       `if ((app as any).neverPublic === true) manifest.neverPublic = true`
    2. apps.ts:461-474 (native builtin manifest) — add `requiresLocalAiClis: (builtinApp as any).requiresLocalAiClis ?? undefined` and `neverPublic: (builtinApp as any).neverPublic ?? undefined` to the written manifest object (drop undefined keys if the writer is strict).
    3. apps.ts platform path (~:1067-1093) — add `neverPublic: data.neverPublic ?? data.manifest?.neverPublic ?? undefined` alongside the existing requiresLocalAiClis thread.
    4. apps.ts buildPublicForbiddenSignals (:1735-1738) — change the requiresLocalAiClis signal to OR
       with the builtin definition (mirroring :828-829):
       `requiresLocalAiClis: manifest?.requiresLocalAiClis === true || (getBuiltinApp(appId) as any)?.requiresLocalAiClis === true`
       (ensure getBuiltinApp is imported in apps.ts — it already is for the mount path). Optionally
       OR neverPublic the same way if a builtin can declare it.
    5. routes.ts:517 — change `: true` → `: ctx.legacySingleUser === true` so an absent currentUser
       is admin ONLY in genuine single-user mode (parity with the 256-04 requireRole guard). Update
       the inline comment that claims "Legacy single-user (no currentUser) is admin-equivalent" to
       reference the explicit flag.
    6. public-forbidden.test.ts — add the install-path OR test from <behavior>: construct/stub the
       apps object so readManifest returns a flag-absent manifest and getBuiltinApp returns
       `{requiresLocalAiClis: true}`, call buildPublicForbiddenSignals, assert isPublicForbidden
       forbids it. RED first (fails before step 4), then GREEN.
  </action>
  <acceptance_criteria>
    - compose-generator.ts and apps.ts native branch thread requiresLocalAiClis + neverPublic into the written manifest
    - apps.ts platform path threads neverPublic
    - apps.ts buildPublicForbiddenSignals ORs requiresLocalAiClis with getBuiltinApp(appId)
    - routes.ts:517 setPublicAccess uses `ctx.legacySingleUser === true` (not bare `: true`) for the absent-currentUser admin inference
    - public-forbidden.test.ts has a green test exercising the real buildPublicForbiddenSignals OR against an install-path-written (flag-absent) manifest
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/apps/public-forbidden.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>A credentialed app written via any install path is correctly forbidden from public exposure; legacy WS tokens no longer infer admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bwrap runtime userns probe + stable shell refusal (no silent unsandboxed fallback) + fix sandboxed log flag, then rebuild liv/core</name>
  <files>liv/packages/core/src/sandbox.ts, liv/packages/core/src/shell.ts, liv/packages/core/src/sandbox.test.ts</files>
  <read_first>
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-058 (Recommendation: real runtime probe, single operator health log, stable refusal, NO silent unsandboxed fallback — "that would convert this Low into a High", fix the sandboxed: log flag) + the WS-B2 Mitigation-Confirmed entry (the no-silent-fallback property MUST be preserved)
    - liv/packages/core/src/sandbox.ts:60-135 (probeBwrap, BWRAP_AVAILABLE, wrapWithBwrap, buildScrubbedEnv)
    - liv/packages/core/src/shell.ts:45-82 (the usable branch + the unsandboxed else)
    - liv/packages/core/src/sandbox.test.ts (tsx + node:assert/strict; runs via `npx tsx src/sandbox.test.ts`)
  </read_first>
  <behavior>
    - probeBwrap returns true ONLY when a real userns command succeeds, not merely when bwrap is on PATH
    - when the probe fails, the shell tool returns a stable refusal object (non-zero code + a clear stderr message) and does NOT execute the command unsandboxed
    - the dev fallback (bwrap entirely off PATH) is unchanged (still env-scrubbed exec) — only the "present-but-userns-unavailable" case becomes a refusal
  </behavior>
  <action>
    Implements CONTEXT D-WS5 item (c).

    1. sandbox.ts — replace probeBwrap's `execFileSync('bwrap', ['--version'])` with a real runtime
       probe: `execFileSync('bwrap', ['--unshare-all', '--share-net', '--ro-bind', '/', '/', 'true'],
       {stdio: 'ignore'})` (or `--unshare-user-try` per the report). Keep the result cached in
       BWRAP_AVAILABLE. Add a THIRD distinct state so callers can tell "off PATH" from
       "present-but-unusable": export `export const BWRAP_ON_PATH: boolean` (a cheap `--version`
       check) alongside `BWRAP_AVAILABLE` (the runtime probe). On `BWRAP_ON_PATH && !BWRAP_AVAILABLE`,
       emit ONE operator-facing health log at module load: "bwrap present but userns unavailable;
       agent shell disabled (LIVOS-058)".
    2. shell.ts — restructure the branch:
       - `BWRAP_AVAILABLE` (runtime-usable) → run `execFile('bwrap', argv.slice(1), ...)` as today.
       - `!BWRAP_ON_PATH` (genuinely absent, dev box) → the existing env-scrubbed unsandboxed `exec`
         fallback (UNCHANGED — preserve the verified no-regression dev path).
       - `BWRAP_ON_PATH && !BWRAP_AVAILABLE` (present but userns broken) → DO NOT exec; resolve a
         stable refusal: `{stdout: '', stderr: 'Shell sandbox unavailable: bwrap present but user
         namespaces are not permitted on this host. Refusing to run unsandboxed (LIVOS-058).', code: 126}`.
       - Fix shell.ts:51: log `sandboxed: BWRAP_AVAILABLE` → reflect ACTUAL engagement (true only when
         the bwrap branch is taken; e.g. compute a `const sandboxed = BWRAP_AVAILABLE` and log the
         real per-call branch outcome, not the stale flag).
    3. sandbox.test.ts — add (tsx/node:assert) cases: the probe argv is the runtime form (assert it
       is NOT `['--version']`); a stubbed probe-failure yields the refusal (code 126, no exec). RED
       first, then GREEN. Run `npx tsx src/sandbox.test.ts`.
    4. **REBUILD (liv/core is compiled):** from `liv/` run `npm run build --workspace=packages/core`
       (= `tsc` in packages/core). This is MANDATORY — livinityd/liv-core consume `dist/`, not src.
       Confirm `liv/packages/core/dist/sandbox.js` reflects the runtime probe.
  </action>
  <acceptance_criteria>
    - sandbox.ts: probe runs an actual bwrap namespace command (`--unshare-all`/`--unshare-user-try` + `--ro-bind / /`), not just `--version`
    - sandbox.ts: a single health log fires on present-but-unusable; BWRAP_ON_PATH and BWRAP_AVAILABLE are distinct
    - shell.ts: present-but-userns-unavailable returns a stable refusal (code 126) and never reaches the unsandboxed exec; the genuinely-off-PATH dev fallback is unchanged
    - shell.ts:51 sandboxed log reflects actual engagement
    - `npx tsx src/sandbox.test.ts` green; `liv/packages/core/dist/sandbox.js` rebuilt
  </acceptance_criteria>
  <verify>
    <automated>cd liv && npx tsx packages/core/src/sandbox.test.ts && npm run build --workspace=packages/core 2>&1 | tail -5</automated>
  </verify>
  <done>bwrap usability is a real runtime fact; the shell fails safe (stable refusal, never silently unsandboxed); compiled dist updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jwt secret file → credential at-rest keys | key-domain separation (auth vs credential stores) |
| install-path manifest writer → isPublicForbidden guard | a write regression must not silently disable the guard |
| host kernel userns availability → agent shell sandbox | the contained-autonomy control must fail safe, not open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-23 | Info Disclosure | leaked jwt = universal decryptor for git/stack/backup | mitigate | shared credential-dek + legacy decrypt-only path (Task 1) |
| T-262-24 | Denial | JWT rotation bricks stored creds | mitigate | getLegacyKey lazy-re-key ported to all three (Task 1) |
| T-262-25 | Elevation/Bypass | flag-absent manifest → public-forbidden fail-open | mitigate | thread flags + OR with getBuiltinApp (Task 2) |
| T-262-26 | Elevation | legacy no-userId WS token infers isAdmin | mitigate | ctx.legacySingleUser guard at routes.ts:517 (Task 2) |
| T-262-27 | Tampering (control-disable) | bwrap PATH-only gate → silent unsandboxed risk / hard-DoS | mitigate | runtime userns probe + stable refusal, no silent fallback (Task 3) |
| T-262-28 | Info Disclosure | Kopia backup password cleartext in store JSON | accept (this phase) | carry-forward to follow-up (CONTEXT Low/optional) |
</threat_model>

<verification>
- `cd livos/packages/livinityd && npx vitest run source/modules/secrets/dek.unit.test.ts source/modules/apps/public-forbidden.test.ts source/modules/docker/registry-credentials.unit.test.ts` — green.
- `grep -rn "secrets/dek" source/modules/docker/git-credentials.ts source/modules/docker/stack-secrets.ts source/modules/scheduler/backup-secrets.ts` — three matches.
- `cd liv && npx tsx packages/core/src/sandbox.test.ts` — green; `npm run build --workspace=packages/core` succeeds and dist updated.
- `npx tsc --noEmit` (livinityd) — no NEW errors.
- Report WS5 success criteria: a leaked jwt no longer decrypts git/stack/backup blobs (registry-pattern test passes for all four); a JWT rotation does not brick stored creds; buildPublicForbiddenSignals forbids a requiresLocalAiClis app written via the install path; bwrap runtime-probe failure surfaces a health log and the shell tool returns a stable refusal (no silent unsandboxed fallback).
</verification>

<success_criteria>
- DEK migration complete for all three siblings with non-destructive legacy fallback; manifest flags threaded + buildPublicForbiddenSignals hardened; legacySingleUser parity guard added; bwrap runtime probe + safe refusal shipped and liv/core rebuilt. Tests green, no new tsc errors, atomic commits per task. Kopia-cleartext + dedicated-backup-vault-key recorded as carry-forward.
</success_criteria>

<output>
After completion, create `.planning/phases/262-security-hardening-pass-3/262-05-SUMMARY.md`
</output>
