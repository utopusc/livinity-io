---
phase: 262-security-hardening-pass-3
plan: 262-02
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/apps/routes.ts
  - livos/packages/livinityd/source/modules/apps/apps.ts
  - livos/packages/livinityd/source/modules/apps/native-installer.ts
  - livos/packages/livinityd/source/modules/apps/native-installer.unit.test.ts
  - scripts/install/livos-add-apt-repo.sh
autonomous: true
requirements: [LIVOS-042, LIVOS-044, LIVOS-045, LIVOS-055]
must_haves:
  truths:
    - "A member/guest installV37 call returns FORBIDDEN (adminProcedure)"
    - "A manifest with an injected `-o DPkg::Pre-Invoke` aptPackage is rejected pre-spawn (never reaches the sudo apt-get argv)"
    - "An artifact with no/short sha256 or a non-allowlisted/http:// host is refused fail-closed"
    - "A downloadToFile redirect to a private/loopback/metadata or http:// target is blocked; redirect count is capped"
    - "For native installs the client-supplied manifest is DISCARDED and re-fetched server-side by appId from the trusted catalog"
    - "apt-repo installs require a pinned aptKeyFingerprint matching the downloaded key; the Pin-Priority 1000 shadowing is dropped to 500"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/apps/routes.ts"
      provides: "installV37/uninstallV37 declared adminProcedure + server-side manifest re-fetch + assertInstallAllowed pre-dispatch"
    - path: "livos/packages/livinityd/source/modules/apps/native-installer.ts"
      provides: "APT_PACKAGE_RE validation, mandatory sha256s, https-only host allowlist, fingerprint pin, hardened downloadToFile"
      contains: "APT_PACKAGE_RE"
    - path: "livos/packages/livinityd/source/modules/apps/native-installer.unit.test.ts"
      provides: "RED→GREEN unit coverage of the new validators"
  key_links:
    - from: "apps/routes.ts installV37"
      to: "apps/install-admin-gate.ts assertInstallAllowed"
      via: "pre-dispatch call with ctx-resolved isAdmin"
      pattern: "assertInstallAllowed"
    - from: "apps/native-installer.ts downloadToFile"
      to: "webapps/url-validator.ts validateUrl"
      via: "per-redirect-hop re-validation"
      pattern: "validateUrl"
    - from: "apps/routes.ts installV37 (section==='native')"
      to: "apps/apps.ts platform catalog fetch (fetchPlatformTemplate plumbing, :1046)"
      via: "server-side manifest re-fetch by appId"
      pattern: "fetchPlatform"
---

<objective>
WS2 — Lock down the native installer pipeline.

Closes: **LIVOS-042 (Critical)** `installV37` is `privateProcedure` with a fully client-supplied
`manifest: z.unknown()` → any authenticated user gets RCE as `bruce`; **LIVOS-044 (High)**
unvalidated `aptPackages` reach the `apt-get install -y *` NOPASSWD grant → `-o DPkg::Pre-Invoke`
root RCE; **LIVOS-045 (High)** optional checksums, no host allowlist, attacker apt key + Pin 1000;
**LIVOS-055 (Medium)** `downloadToFile` follows unbounded redirects to `http://`/private targets.

Purpose: privileged native installs become admin-only, trust comes from a server-resolved catalog
manifest, and every artifact/URL/package is validated fail-closed before any privileged sink.

Output: hardened routes.ts + native-installer.ts, a new unit-test file for the pure validators,
apt-repo pin de-escalated.

**HARD CONSTRAINTS: CODE ONLY — NO DEPLOY.** No `update.sh`, no `systemctl`, no SSH, no ufw.
Backend runs via tsx — no build. Only NEW tsc errors block (livinityd ~TS18048 baseline pre-exists).
Keep edits minimal + idiomatic; do not refactor unrelated installer code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-security-hardening-pass-3/262-CONTEXT.md
@SECURITY-RESEARCH-PASS-3.md  (LIVOS-042 / 044 / 045 / 055 + WS2 success criteria)

<interfaces>
installV37 today (apps/routes.ts:734): `privateProcedure.input(z.object({appId, section, name,
category, manifest: z.unknown(), iconUrl?}))` → builds installCtx → `d.install({id, name, section,
category, manifest: input.manifest, iconUrl}, installCtx, emit)`. uninstallV37 at :810, also
privateProcedure. `adminProcedure` is already imported at routes.ts:4.

assertInstallAllowed (apps/install-admin-gate.ts:39):
```ts
assertInstallAllowed({isAdmin: boolean, isGeneratedTemplate: boolean,
  manifest: {requiresLocalAiClis?: boolean; requiresAiProvider?: boolean} | null | undefined): void
```
Currently called ONLY from apps.ts:546 (legacy compose path) — never from routes.ts.

ctx-resolved admin (the 256-04 pattern, see setPublicAccess routes.ts:517 + requireRole in
server/trpc/is-authenticated.ts:154-168):
`const isAdmin = ctx.currentUser ? ctx.currentUser.role === 'admin' : ctx.legacySingleUser === true`

Trusted-catalog fetch plumbing: apps.ts:1046 `private async fetchPlatformTemplate(appId)` — reuse
its endpoint/headers to add a sibling public method that returns the catalog ROW (manifest jsonb),
not just the compose template. The platform install path (apps.ts:1067-1093) shows the row shape
(`data.manifest`, `data.requiresLocalAiClis`, ...).

native-installer.ts hot spots: install union types :61-84 (`aptPackages: string[]`,
`debSha256?: string`, `scriptSha256?: string`, `appimageSha256: string`, apt-repo fields);
`downloadToFile` :201-238 (recursive redirect follow at :222, `^https?` accepted at :203);
apt branch :378-411; deb :412-445; apt-repo :446-513 (sudo apt-get argv at :499); appimage
:514-542; script :543-599. SSRF guard to reuse:
`webapps/url-validator.ts:116 export function validateUrl(input: string, opts: {isAdmin: boolean}): ValidateResult`.

Test framework: vitest (livinityd convention; see apps/install-admin-gate.test.ts as a style
analog for pure-function security tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routes.ts — adminProcedure for installV37/uninstallV37 + server-side manifest re-fetch + assertInstallAllowed pre-dispatch</name>
  <files>livos/packages/livinityd/source/modules/apps/routes.ts, livos/packages/livinityd/source/modules/apps/apps.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/apps/routes.ts:734-860 (installV37 + uninstallV37 bodies)
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-042 (Description, Residual Risk: adminProcedure alone is insufficient — the re-fetch is the durable fix)
    - livos/packages/livinityd/source/modules/apps/install-admin-gate.ts (assertInstallAllowed + InstallForbidden)
    - livos/packages/livinityd/source/modules/apps/apps.ts:500-560 (legacy server-side manifest sourcing + the apps.ts:546 gate call analog) and :1046-1100 (fetchPlatformTemplate plumbing + platform row shape)
  </read_first>
  <action>
    Implements CONTEXT D-WS2 item (a).

    1. Change `installV37: privateProcedure` (:734) → `installV37: adminProcedure` and
       `uninstallV37: privateProcedure` (:810) → `uninstallV37: adminProcedure`. (Per the locked
       decision: native/script/apt-repo installs are inherently privileged; LIV_API_KEY and
       legacy single-user map to admin via requireRole's ctx.legacySingleUser admit, so the
       operator flow is unaffected.)
    2. apps.ts — add a public method next to fetchPlatformTemplate (:1046), reusing its exact
       fetch/endpoint/header plumbing:
       `async fetchPlatformAppManifest(appId: string): Promise<Record<string, unknown> | null>`
       returning the catalog row's manifest (the same `data.manifest` jsonb the platform install
       path at :1067-1093 consumes; return null on fetch failure/no row).
    3. routes.ts installV37 — BEFORE `d.install(...)`:
       ```ts
       const isAdmin = ctx.currentUser ? ctx.currentUser.role === 'admin' : ctx.legacySingleUser === true
       // LIVOS-042: privileged sections NEVER trust the client manifest — re-fetch by appId
       // from the trusted catalog; fail closed when unresolvable.
       let manifest = input.manifest
       if (input.section === 'native') {
         const trusted = await ctx.apps.fetchPlatformAppManifest(input.appId)
         if (!trusted) {
           return {ok: false as const, code: 'manifest_unresolved' as const,
             message: `native install refused: no trusted catalog manifest for ${input.appId}`}
         }
         manifest = trusted
       }
       assertInstallAllowed({isAdmin, isGeneratedTemplate: false,
         manifest: manifest as {requiresLocalAiClis?: boolean; requiresAiProvider?: boolean}})
       ```
       and pass `manifest` (not `input.manifest`) into `d.install`. Wrap assertInstallAllowed in
       the same try/catch→TRPCError FORBIDDEN conversion apps.ts:546 uses (read it; InstallForbidden
       must surface as a FORBIDDEN tRPC error, not a 500). The gate is defense-in-depth under
       adminProcedure — it keeps the path safe if the procedure is ever downgraded.
       Import `assertInstallAllowed` from './install-admin-gate.js'.
    4. Run the apps routes tests + tsc.
  </action>
  <acceptance_criteria>
    - apps/routes.ts: installV37 is declared with adminProcedure (not privateProcedure); same for uninstallV37
    - apps/routes.ts: installV37 calls assertInstallAllowed before d.install
    - apps/routes.ts: for section==='native' the dispatched manifest comes from ctx.apps.fetchPlatformAppManifest, and a null result returns ok:false 'manifest_unresolved' (input.manifest never reaches the dispatcher for native)
    - apps.ts: fetchPlatformAppManifest exists and reuses the fetchPlatformTemplate endpoint plumbing
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/apps/routes.test.ts source/modules/apps/install-admin-gate.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Member/guest installV37 → FORBIDDEN; native installs run only on trusted server-fetched manifests.</done>
</task>

<task type="auto">
  <name>Task 2: native-installer.ts — aptPackages charset validation, fixed apt helper, mandatory sha256s, https-only host allowlist, aptKeyFingerprint pin, hardened downloadToFile</name>
  <files>livos/packages/livinityd/source/modules/apps/native-installer.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/apps/native-installer.ts — install union :61-84, downloadToFile :201-238, apt :378-411, deb :412-445, apt-repo :446-513, appimage :514-542, script :543-599
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-044 (the `-o DPkg::Pre-Invoke` exploit + "wrap apt in a fixed helper"), LIVOS-045 (per-method gaps), LIVOS-055 (redirect recursion at :222 is UNBOUNDED)
    - livos/packages/livinityd/source/modules/webapps/url-validator.ts:116 — validateUrl(input, {isAdmin}) SSRF guard to reuse
  </read_first>
  <action>
    Implements CONTEXT D-WS2 items (b), (c), (e) + the fingerprint half of (d).

    1. **aptPackages validation (LIVOS-044).** Add module-level exports:
       ```ts
       export const APT_PACKAGE_RE = /^[a-z0-9][a-z0-9+._-]*$/
       export function validateAptPackages(pkgs: string[]): string | null {
         if (!pkgs.length) return 'aptPackages empty'
         for (const p of pkgs) {
           if (!APT_PACKAGE_RE.test(p)) return `invalid apt package name: ${JSON.stringify(p)}`
         }
         return null
       }
       ```
       (The regex structurally rejects leading `-`, `/`, `=`, whitespace, and `::` option syntax.)
       Call it in BOTH the apt branch (:378-411, right after the `pkgs` empty-check at :381) and
       the apt-repo branch (:446-513, after :450), returning
       `fail(app.id, 'native', 'manifest_invalid', <msg>)` on violation. Then route both branches'
       apt execution through ONE private helper with PINNED flags (no caller-supplied flags can
       enter the argv):
       `#aptInstall(pkgs)` → argv exactly `['-n', '/usr/bin/apt-get', 'install', '-y', ...APT_CONFOLD, '--', ...validatedPkgs]`
       via the existing sudo spawn pattern (the `--` end-of-options marker is belt-and-suspenders
       under the sudoers `install -y *` wildcard).
    2. **Mandatory checksums (LIVOS-045).** Add
       `export const SHA256_RE = /^[0-9a-f]{64}$/i`. In script (:568), deb (:427), appimage (:529):
       replace the `if (sha && sha.length === 64)` optional-check idiom with FAIL-CLOSED:
       `if (!SHA256_RE.test(sha ?? '')) return fail(app.id, 'native', 'signature_invalid', 'sha256 checksum required (64 hex chars)')`
       then ALWAYS verify the digest. Update the install union types (:61-84): `debSha256: string`
       and `scriptSha256: string` become required (appimageSha256 already typed required — the
       runtime check was the gap).
    3. **Host allowlist + https-only (LIVOS-045).** Add:
       ```ts
       export const NATIVE_DOWNLOAD_HOST_ALLOWLIST: ReadonlySet<string> = new Set([...])
       export function assertAllowedDownloadUrl(raw: string): string | null  // error msg or null
       ```
       assertAllowedDownloadUrl: `new URL(raw)`; protocol MUST be `https:`; hostname (lowercased)
       MUST be in the allowlist; else return an error string. Seed the allowlist from the hosts the
       curated catalog actually uses: grep the repo for `scriptUrl|debUrl|appimageUrl|aptKeyUrl|aptRepoLine`
       literals (builtin catalog, scripts/, .planning excluded) and include at minimum
       `github.com`, `objects.githubusercontent.com`, `release-assets.githubusercontent.com`,
       `raw.githubusercontent.com`, plus every vendor host found in the catalog (e.g. Brave/Signal/
       Spotify repo hosts). Apply it to `scriptUrl`, `debUrl`, `appimageUrl`, `aptKeyUrl`, AND the
       URL token inside `aptRepoLine` (parse the first `http(s)://` token out of the repo line;
       https-only — this closes the `aptRepoLine permits http://` MITM hole).
    4. **aptKeyFingerprint pin (LIVOS-045).** Add `aptKeyFingerprint: string` (required, 40-hex) to
       the apt-repo install union member. In the apt-repo branch after the key download: run
       `gpg --show-keys --with-colons <tmpkey>` via execa, parse the `fpr:::::::::<40HEX>:` lines,
       and require one fingerprint to equal `aptKeyFingerprint` (case-insensitive) — mismatch/parse
       failure → `fail(..., 'signature_invalid', ...)` BEFORE livos-add-apt-repo.sh is invoked.
    5. **downloadToFile redirect re-validation (LIVOS-055).** Change the signature to
       `downloadToFile(url: string, dest: string, redirectsLeft = 5)`. At function entry AND on
       every redirect hop (:215-225): reject non-`https:` schemes; run
       `validateUrl(<target>, {isAdmin: false})` (import from '../webapps/url-validator.js') and
       fail on its rejection (blocks loopback/RFC1918/link-local/metadata); re-run
       `assertAllowedDownloadUrl(<target>)` (no cross-host escape via redirect);
       `redirectsLeft <= 0` → reject 'too many redirects'. Recurse with `redirectsLeft - 1`. Fix
       the wrong "Follow one redirect" comment.
  </action>
  <acceptance_criteria>
    - native-installer.ts: aptPackages validated against /^[a-z0-9][a-z0-9+._-]*$/ in BOTH apt and apt-repo branches, pre-spawn
    - native-installer.ts: a single #aptInstall helper owns the apt argv; the `-o` flags are pinned constants, `--` precedes package names
    - native-installer.ts: `grep -n 'sha && ' native-installer.ts` shows no remaining optional-checksum idiom; SHA256_RE fail-closed checks present in script/deb/appimage branches
    - native-installer.ts: assertAllowedDownloadUrl applied to scriptUrl, debUrl, appimageUrl, aptKeyUrl, aptRepoLine; NATIVE_DOWNLOAD_HOST_ALLOWLIST exported
    - native-installer.ts: downloadToFile re-validates scheme+host+validateUrl on every hop and caps redirects at 5
    - native-installer.ts: apt-repo requires aptKeyFingerprint and verifies via gpg --show-keys --with-colons before invoking livos-add-apt-repo.sh
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Every privileged installer input is validated fail-closed before any download/spawn; option-injection into the sudo apt wildcard is structurally impossible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN unit tests for the validators + de-escalate the apt-repo Pin-Priority</name>
  <files>livos/packages/livinityd/source/modules/apps/native-installer.unit.test.ts, scripts/install/livos-add-apt-repo.sh</files>
  <read_first>
    - livos/packages/livinityd/source/modules/apps/install-admin-gate.test.ts — vitest style analog for pure security validators
    - scripts/install/livos-add-apt-repo.sh:44-60 — the sources entry + "Pin-Priority 1000" block ("repo wins over Ubuntu's package")
    - SECURITY-RESEARCH-PASS-3.md — LIVOS-045 apt-repo bullet (Pin 1000 = persistent shadowing of Ubuntu packages)
  </read_first>
  <behavior>
    - validateAptPackages: rejects ['-o'], ['DPkg::Pre-Invoke::=cp /bin/bash /tmp/.r'], ['-o','DPkg::Pre-Invoke::=x','coreutils'], ['a b'], ['pkg=1.2'], ['../evil.deb'], ['/tmp/evil.deb'], ['']; accepts ['brave-browser'], ['libxss1','g++','signal-desktop'], ['7zip']
    - assertAllowedDownloadUrl: rejects 'http://github.com/x' (scheme), 'https://attacker.tld/p.sh' (host), 'ftp://github.com/x'; accepts 'https://github.com/org/repo/releases/download/v1/x.deb' and one catalog vendor host
    - SHA256_RE: rejects '', 'abc', 63-char, 65-char, non-hex; accepts a 64-hex digest (both cases)
  </behavior>
  <action>
    1. Write `native-installer.unit.test.ts` (vitest, import the exported validators from
       './native-installer.js') with the cases in <behavior>. Write them FIRST and confirm they
       fail against pre-Task-2 exports if Task 2 is incomplete (RED), then GREEN.
    2. scripts/install/livos-add-apt-repo.sh — change `Pin-Priority: 1000` → `Pin-Priority: 500`
       in the preferences block (~:48-58) and update the comment: 500 = same as the Ubuntu default,
       so the vendor repo provides packages absent from Ubuntu but can no longer SHADOW Ubuntu's
       own versions across upgrades (LIVOS-045). Keep the keyring/sources logic byte-identical.
    3. Run the full new test file + the existing apps test files touched this plan.
  </action>
  <acceptance_criteria>
    - native-installer.unit.test.ts exists and is green with ≥ 15 assertions covering injection, host-allowlist, and checksum shapes
    - `grep -c 'Pin-Priority: 1000' scripts/install/livos-add-apt-repo.sh` == 0; `grep -c 'Pin-Priority: 500' scripts/install/livos-add-apt-repo.sh` == 1
    - `bash -n scripts/install/livos-add-apt-repo.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/apps/native-installer.unit.test.ts source/modules/apps/routes.test.ts && bash -n ../../../scripts/install/livos-add-apt-repo.sh</automated>
  </verify>
  <done>Validators are regression-locked by unit tests; attacker repos can no longer outrank Ubuntu packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → installV37 (httpOnlyPaths) | client-supplied manifest crossed into privileged installers |
| native-installer → sudo apt-get (NOPASSWD wildcard) | validated argv is the only barrier to root |
| internet artifact hosts → downloadToFile → root apt sink | download provenance/integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-08 | Elevation | installV37 privateProcedure + client manifest | mitigate | adminProcedure + server-side re-fetch + assertInstallAllowed (Task 1) |
| T-262-09 | Elevation | aptPackages `-o` hook injection → root | mitigate | APT_PACKAGE_RE pre-spawn + fixed #aptInstall argv (Task 2) |
| T-262-10 | Tampering | optional sha256 on script/deb/appimage | mitigate | SHA256_RE fail-closed mandatory (Task 2) |
| T-262-11 | Tampering/Spoofing | attacker apt key + Pin 1000 persistence | mitigate | aptKeyFingerprint pin + Pin 500 (Tasks 2+3) |
| T-262-12 | Info Disclosure/SSRF | unbounded redirects to http/private targets | mitigate | per-hop validateUrl + allowlist + cap (Task 2) |
| T-262-13 | Elevation | maintainer scripts of LEGIT pinned packages run as root | accept | inherent to apt; bounded by catalog curation + checksum + admin gate |
</threat_model>

<verification>
- `cd livos/packages/livinityd && npx vitest run source/modules/apps/native-installer.unit.test.ts source/modules/apps/routes.test.ts source/modules/apps/install-admin-gate.test.ts` — green.
- `grep -n 'installV37: adminProcedure' source/modules/apps/routes.ts` — match.
- `npx tsc --noEmit` — no NEW errors vs baseline.
- Report WS2 success criteria (live items = operator deploy walk): member/guest installV37 returns FORBIDDEN; an injected `-o DPkg::Pre-Invoke` aptPackage is rejected pre-spawn; a no/short-sha256 or non-allowlisted/http host artifact is refused; a redirect to a private/http host is blocked.
</verification>

<success_criteria>
- installV37/uninstallV37 admin-only with server-resolved manifests; all four native install methods validate packages, checksums, hosts, fingerprints fail-closed; downloadToFile SSRF-hardened; validators unit-locked; Pin-Priority de-escalated. No new tsc errors; atomic commits per task.
</success_criteria>

<output>
After completion, create `.planning/phases/262-security-hardening-pass-3/262-02-SUMMARY.md`
</output>
