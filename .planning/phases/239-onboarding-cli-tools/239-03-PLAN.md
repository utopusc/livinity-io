---
phase: 239-onboarding-cli-tools
plan: 03
type: execute
wave: 2
depends_on: [239-01, 239-02]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md
autonomous: true
requirements: []
tags: [deploy, mini-pc, uat, sacred-sha]
must_haves:
  truths:
    - "Mini PC livinityd serves cliInstaller.install + cliInstaller.detect tRPC procedures"
    - "Onboarding wizard at https://bruce.livinity.io/onboarding renders the new CliToolsStep when feature flag is on"
    - "Onboarding wizard renders the flag-disabled notice when feature flag is off"
    - "cliInstaller.detect tRPC probe returns valid response for each of the 5 SUPPORTED_CLIS names"
    - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f unchanged PRE/POST deploy"
    - "Mini PC sha256 62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe byte-identical PRE/POST (per CONTEXT canonical_refs)"
    - "LICENSE + NOTICE files byte-identical PRE/POST (D-V43-APACHE-NOTICE)"
    - "All 6 systemd services active POST deploy (livos, liv-core, liv-worker, liv-memory, liv-assistant, caddy)"
    - "STATE.md + ROADMAP.md updated with Phase 239 SHIPPED status"
  artifacts:
    - path: .planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md
      provides: "Deploy + UAT evidence"
      contains: "Phase 239 SHIPPED"
  key_links:
    - from: "Mini PC bruce@10.69.31.68 /opt/livos/"
      to: "GitHub utopusc/livinity-io master HEAD"
      via: "bash /opt/livos/update.sh"
      pattern: "update.sh"
    - from: "Mini PC Redis"
      to: "livos:v43:onboarding_cli_section flag"
      via: "redis-cli SET (operator-controlled feature flag flip)"
      pattern: "livos:v43:onboarding_cli_section"
---

<objective>
Deploy Phase 239 to Mini PC (`bruce@10.69.31.68`), flip the feature flag ON, and walk 3 UAT probes (new step renders / detect endpoint responds / feature flag off reverts to disabled notice).

Purpose: Verify Plans 239-01 + 239-02 work end-to-end on the only LivOS deployment that matters (Mini PC ONLY per CLAUDE.md hard rule — Server4/Server5 OFF-LIMITS for LivOS work).

Output: Mini PC running new bundle, feature flag default decided + documented, STATE.md + ROADMAP.md final updates, 239-03-SUMMARY.md with UAT evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/239-onboarding-cli-tools/239-CONTEXT.md
@.planning/phases/239-onboarding-cli-tools/239-01-PLAN.md
@.planning/phases/239-onboarding-cli-tools/239-02-PLAN.md

<interfaces>
<!-- Deploy interface — Mini PC SSH + update.sh + redis-cli + curl tRPC probes. -->

Mini PC SSH:
```
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68
```
(Fall back to `contabo_master` key path if `minipc` not found per MEMORY entries.)

Deploy command (NEVER PM2 — MEMORY.md hard rule):
```
ssh ... bash /opt/livos/update.sh
```

Sacred SHA verify:
```
git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts
# MUST equal f3538e1d811992b782a9bb057d1b7f0a0189f95f
```

Mini PC sha256 verify (CONTEXT canonical_refs):
```
ssh ... sha256sum /etc/liv-assistant/aionui-frontend.tar.gz  # or equivalent canonical blob
# MUST equal 62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe
```
Confirm the exact path via Mini PC PRE-deploy snapshot; the canonical blob's path may have evolved.

LICENSE + NOTICE byte-identical:
```
ssh ... sha256sum /opt/livos/LICENSE /opt/livos/NOTICE
# Compare against repo HEAD shasums
```

tRPC probe shape (via HTTP, per Plan 239-01 httpOnlyPaths entry):
```
curl -sS -X POST https://bruce.livinity.io/api/trpc/cliInstaller.detect \
  -H "Content-Type: application/json" \
  -H "Cookie: <admin JWT cookie from operator session>" \
  -d '{"json":{"name":"claude-code"}}'
# Expected: {"result":{"data":{"json":{"detected":true|false, ...}}}}
```

Feature flag flip:
```
ssh ... 'REDIS_URL=$(grep "^REDIS_URL=" /opt/livos/.env | cut -d= -f2-); redis-cli -u "$REDIS_URL" SET livos:v43:onboarding_cli_section true'
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: PRE-deploy verification — sacred SHA + Mini PC snapshot + service health</name>
  <files>(read-only probes — no file modifications)</files>
  <read_first>
    - .planning/STATE.md (current Mini PC SHA marker + sacred SHA reference)
    - CLAUDE.md / MEMORY.md "Mini PC" + "Sacred SHA" entries
  </read_first>
  <action>
    Batch ALL Mini PC SSH probes into ONE invocation (fail2ban discipline per MEMORY entry):

    1. Local (no SSH) — verify sacred SHA on current branch:
    ```
    git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts
    ```
    Expected: `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If different → ABORT deploy + investigate.

    2. Local — verify Plans 239-01 + 239-02 are committed and pushed:
    ```
    git log --oneline -10
    git status
    git diff --stat HEAD~5..HEAD
    ```
    Confirm 239-01 + 239-02 artifacts present in recent commits.

    3. Mini PC PRE-deploy batched probe (single SSH session — outputs to stdout for record):
    ```
    ssh -i <key> bruce@10.69.31.68 'set -e; echo === SERVICES PRE ===; systemctl is-active livos liv-core liv-worker liv-memory liv-assistant caddy 2>&1; echo === LICENSE+NOTICE SHA PRE ===; sha256sum /opt/livos/LICENSE /opt/livos/NOTICE 2>/dev/null || echo MISSING; echo === CANONICAL BLOB PRE ===; ls -la /etc/liv-assistant/ 2>/dev/null; sha256sum /etc/liv-assistant/aionui-frontend.tar.gz 2>/dev/null || echo MISSING; echo === CLI INSTALLER MODULE PRE ===; ls -la /opt/livos/packages/livinityd/dist/modules/cli-installer/ 2>/dev/null || echo NOT-YET-DEPLOYED; echo === INSTALL SCRIPTS PRE ===; ls -la /opt/livos/scripts/install/cli/ 2>/dev/null || echo NOT-YET-DEPLOYED; echo === REDIS FLAG PRE ===; REDIS_URL=$(grep "^REDIS_URL=" /opt/livos/.env | cut -d= -f2-); redis-cli -u "$REDIS_URL" GET livos:v43:onboarding_cli_section 2>&1 || echo MISSING'
    ```

    4. Record outputs in `.planning/phases/239-onboarding-cli-tools/239-03-PRE-SNAPSHOT.txt` (gitignored; for evidence in SUMMARY only):
    ```
    ssh ... '...' > .planning/phases/239-onboarding-cli-tools/239-03-PRE-SNAPSHOT.txt
    ```
  </action>
  <verify>
    <automated>git rev-parse HEAD:liv/packages/core/src/sdk-agent-runner.ts | grep -q "^f3538e1d811992b782a9bb057d1b7f0a0189f95f$"</automated>
  </verify>
  <acceptance_criteria>
    - Sacred SHA equals exactly `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
    - PRE-snapshot file created with all 6 systemd services showing `active`
    - LICENSE + NOTICE sha256s recorded for POST comparison
    - Canonical blob path + sha256 recorded for POST comparison (if path differs from MEMORY's documented path, note the actual path discovered)
    - Redis flag PRE state recorded (expected: empty / nil / "false")
  </acceptance_criteria>
  <done>
    PRE-deploy state captured, sacred SHA confirmed, no Server4/Server5 SSH attempted (Mini PC only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy via `bash /opt/livos/update.sh` + POST-deploy snapshot</name>
  <files>(deployment only — no local file modifications)</files>
  <read_first>
    - .planning/phases/239-onboarding-cli-tools/239-03-PRE-SNAPSHOT.txt (from Task 1)
    - MEMORY.md "update.sh pnpm-store quirk" entry — may need manual cli-installer dist copy if multiple `@liv+core*` dirs
  </read_first>
  <action>
    1. Trigger deploy (single SSH; 5-10 min runtime — use long bash timeout):
    ```
    ssh -i <key> bruce@10.69.31.68 'bash /opt/livos/update.sh 2>&1 | tail -200'
    ```
    Expected: exit 0; tail shows builds for `@livos/config`, ui (vite), liv core/worker/mcp-server (tsc), then `systemctl restart livos liv-core liv-worker liv-memory liv-assistant` succeeded.

    2. If `update.sh` fails on `pnpm install` due to new cli-installer module not being picked up, debug:
    ```
    ssh ... 'cd /tmp/livinity-update-*/livos && pnpm install 2>&1 | tail -50'
    ```
    Fix in source + re-deploy. Common cause: missing index.ts barrel export → re-run pnpm install after fixing.

    3. POST-deploy batched probe (single SSH session):
    ```
    ssh -i <key> bruce@10.69.31.68 'set -e; echo === SERVICES POST ===; systemctl is-active livos liv-core liv-worker liv-memory liv-assistant caddy; echo === LICENSE+NOTICE SHA POST ===; sha256sum /opt/livos/LICENSE /opt/livos/NOTICE; echo === CANONICAL BLOB POST ===; sha256sum /etc/liv-assistant/aionui-frontend.tar.gz 2>/dev/null || echo MISSING; echo === CLI INSTALLER MODULE POST ===; ls -la /opt/livos/packages/livinityd/dist/modules/cli-installer/; echo === INSTALL SCRIPTS POST ===; ls -la /opt/livos/scripts/install/cli/; echo === JOURNAL TAIL ===; journalctl -u livos --since "5 min ago" --no-pager | tail -50'
    ```

    4. Save POST output to `.planning/phases/239-onboarding-cli-tools/239-03-POST-SNAPSHOT.txt`.

    5. Compare PRE vs POST:
    ```
    diff .planning/phases/239-onboarding-cli-tools/239-03-PRE-SNAPSHOT.txt .planning/phases/239-onboarding-cli-tools/239-03-POST-SNAPSHOT.txt | head -100
    ```
    Required NEW lines POST:
    - `cli-installer/` directory contains compiled .js files (index.js, installer.js, detector.js, install-scripts.js, types.js)
    - `scripts/install/cli/` contains 5 .sh files (claude-code, opencode, gemini, openclaw, aion-cli) all executable
    Required UNCHANGED lines:
    - LICENSE + NOTICE sha256s identical PRE/POST
    - Canonical blob sha256 identical PRE/POST (if present at the documented path)
    - All 6 services still `active`
  </action>
  <verify>
    <automated>ssh -i <key> bruce@10.69.31.68 'systemctl is-active livos liv-core liv-worker liv-memory liv-assistant caddy' | grep -c "^active$" | grep -q "^6$"</automated>
  </verify>
  <acceptance_criteria>
    - `update.sh` exits 0
    - All 6 services `active` POST
    - LICENSE sha256 byte-identical PRE/POST
    - NOTICE sha256 byte-identical PRE/POST
    - Canonical blob sha256 byte-identical PRE/POST (or documented absence if MEMORY's path is stale)
    - `/opt/livos/packages/livinityd/dist/modules/cli-installer/` exists with at least 5 .js files
    - `/opt/livos/scripts/install/cli/` exists with 5 executable .sh files
    - `journalctl -u livos` shows no errors related to cli-installer or trpc router wire-up
    - Sacred SHA still `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (re-check from local repo)
  </acceptance_criteria>
  <done>
    Mini PC running new bundle; module + scripts + router wired; all invariants preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: UAT walk 1 — invoke cliInstaller.detect for all 5 CLIs (curl tRPC probes)</name>
  <files>(probes only)</files>
  <read_first>
    - Plan 239-01 tRPC response shape (`cliInstaller.detect → {detected, version?, path?}`)
    - Mini PC admin JWT — operator session cookie or use livinityd direct port (3200/8080) with `X-Api-Key` header if exposed
  </read_first>
  <action>
    Probe each of the 5 SUPPORTED_CLIS names via tRPC HTTP (cliInstaller.detect was added to httpOnlyPaths in Plan 239-01):

    For an authenticated admin probe, first obtain a fresh JWT:
    ```
    # Option A — use existing operator browser session, extract JWT from devtools:
    # Option B — call user.login via curl with admin password (from /opt/livos/data/secrets/jwt context; password in CONTEXT memory)
    ```

    Probe each CLI in sequence (single batched SSH if curl is run on Mini PC, or 5 sequential curl from local):
    ```
    for CLI in claude-code opencode gemini openclaw aion-cli; do
      echo "=== detect $CLI ==="
      curl -sS -X POST https://bruce.livinity.io/api/trpc/cliInstaller.detect \
        -H "Content-Type: application/json" \
        -H "Cookie: jwt=<admin-jwt>" \
        -d "{\"json\":{\"name\":\"$CLI\"}}" | head -c 500
      echo
    done
    ```

    Also probe ONE invalid name to verify whitelist rejection (RCE boundary smoke test):
    ```
    curl -sS -X POST https://bruce.livinity.io/api/trpc/cliInstaller.detect \
      -H "Content-Type: application/json" \
      -H "Cookie: jwt=<admin-jwt>" \
      -d '{"json":{"name":"foo-bar-baz"}}'
    # Expected: HTTP 400 with TRPC error code BAD_REQUEST + cause CLI_NOT_SUPPORTED
    ```

    Record all 6 probe outputs in `.planning/phases/239-onboarding-cli-tools/239-03-UAT-DETECT.txt`.
  </action>
  <verify>
    <automated>test -f .planning/phases/239-onboarding-cli-tools/239-03-UAT-DETECT.txt && grep -c "result\\|error" .planning/phases/239-onboarding-cli-tools/239-03-UAT-DETECT.txt | awk '{if ($1 >= 6) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 5 valid-name probes return HTTP 200 with `{"result":{"data":{"json":{"detected":...}}}}` shape
    - At least one of the 5 returns `detected:true` (likely `claude-code` or `openclaw` if pre-installed; if NONE return true, document Mini PC's pre-existing CLI state in SUMMARY)
    - Invalid-name probe returns HTTP 400 with TRPC error containing `BAD_REQUEST` AND `CLI_NOT_SUPPORTED`
    - No probe returns HTTP 500 (router crash)
    - No probe returns HTTP 401/403 (auth misconfigured — re-fetch JWT if seen)
  </acceptance_criteria>
  <done>
    cliInstaller.detect endpoint live + whitelist guard provably enforced over the wire.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT walk 2+3 — operator browser walk of onboarding wizard with feature flag ON then OFF</name>
  <what-built>
    Mini PC livinityd serving Phase 239 cli-installer router; UI bundle includes CliToolsStep; feature flag controls render.
  </what-built>
  <how-to-verify>
    **Operator-pace UAT (not auto-runnable; requires browser + visual judgment).**

    Pre-step: Reset onboarding to step 5 (or use ?step=4 dev helper if NODE_ENV permits).

    **Walk 2 — Feature flag ON (CliToolsStep should render):**
    1. SSH to Mini PC and flip the flag ON:
       ```
       ssh -i <key> bruce@10.69.31.68 'REDIS_URL=$(grep "^REDIS_URL=" /opt/livos/.env | cut -d= -f2-); redis-cli -u "$REDIS_URL" SET livos:v43:onboarding_cli_section true'
       ```
       If the wizard reads the flag from localStorage only (Plan 239-02 Task 3 fallback path), instead open browser devtools console and run:
       ```
       window.localStorage.setItem('livos.v43.onboarding_cli_section', 'true')
       ```
    2. Hard-reload `https://bruce.livinity.io/onboarding` (Ctrl+Shift+R / Cmd+Shift+R).
    3. Click Continue / Skip through steps 0-4 until reaching step 5 (Personalize → CLI Tools).
    4. Verify: step header reads "05 · CLI Tools" and "Pick your CLI agents".
    5. Verify: 5 cards visible in a single row (or 2 columns on narrow viewport, 1 on mobile), in this exact order: Claude Code, OpenCode, Gemini, OpenClaw, Aion CLI.
    6. Verify: Continue button is ENABLED without clicking any Install button.
    7. Verify: any pre-installed CLI shows "Installed ✓" pill instead of Install button.
    8. (Optional, slow) Click Install on one card (e.g. Gemini if not present, or claude-code) → verify spinner appears → wait up to 5 min → verify state transitions to Installed ✓ or Failed (with Retry button).
    9. Click Continue → wizard advances to step 6 (Location).

    **Walk 3 — Feature flag OFF (flag-disabled notice should render):**
    1. SSH to Mini PC and flip the flag OFF:
       ```
       ssh -i <key> bruce@10.69.31.68 'REDIS_URL=$(grep "^REDIS_URL=" /opt/livos/.env | cut -d= -f2-); redis-cli -u "$REDIS_URL" SET livos:v43:onboarding_cli_section false'
       ```
       Or in devtools console: `window.localStorage.setItem('livos.v43.onboarding_cli_section', 'false')`.
    2. Hard-reload `https://bruce.livinity.io/onboarding`.
    3. Navigate to step 5.
    4. Verify: render shows "This step is disabled" notice (NOT legacy ProviderStep — that file was deleted per Plan 239-02 Task 4).
    5. Verify: notice text mentions the flag key `livos:v43:onboarding_cli_section`.
    6. Verify: Skip button advances to step 6.

    Capture 1 screenshot for each walk (CliTools grid view + flag-disabled notice). Save to `.planning/phases/239-onboarding-cli-tools/screenshots/`.

    Final state: leave flag in the position the operator wants for Phase 239 SHIPPED status (recommendation: leave OFF until operator-driven smoke testing rounds complete; flip ON when v44 work begins).
  </how-to-verify>
  <resume-signal>
    Type "approved" + state the chosen final flag state (on/off). If issues, describe them; planner will route to gap-closure plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md + ROADMAP.md + write 239-03-SUMMARY.md</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md
  </files>
  <read_first>
    - .planning/STATE.md (current Phase 239 status row + sacred SHA banner)
    - .planning/ROADMAP.md:3681-3700 (Phase 239 section to mark SHIPPED)
    - All three PRE/POST/UAT snapshot files from Tasks 1-4
    - .planning/templates/summary.md if exists
  </read_first>
  <action>
    1. Write `.planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md` covering:
       - Deploy outcome (update.sh exit code, service states POST, journal excerpts)
       - PRE/POST snapshot diffs (LICENSE + NOTICE + canonical blob sha256s — confirm byte-identical)
       - UAT walk 1 (detect endpoint) — paste tRPC response samples for all 5 CLIs + the rejection probe
       - UAT walk 2 (feature flag ON visual confirmation + operator screenshot link)
       - UAT walk 3 (feature flag OFF disabled-notice + operator screenshot link)
       - Final flag state chosen by operator
       - Sacred SHA PRE = POST = `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
       - Any gotchas encountered (e.g. update.sh pnpm-store quirk, Aion CLI install verification, etc.)
       - Phase 240 hand-off notes (SUPPORTED_CLIS contract preserved; cliInstaller.install + .detect contracts stable)

    2. Update `.planning/STATE.md`:
       - Phase 239 row → `SHIPPED 2026-05-27` (or current date)
       - Mini PC sha marker updated to POST-deploy git HEAD sha
       - Sacred SHA confirmation line refreshed

    3. Update `.planning/ROADMAP.md` Phase 239 section (line 3681 area):
       - Header → `✅ SHIPPED <date> (3/3 plans)`
       - Add `**Plans:** 3/3 plans complete` summary block
       - Add bullet rows for each of 239-01, 239-02, 239-03 with commit SHAs + SUMMARY.md links + ONE-line outcome each
       - Add `**UAT evidence (Mini PC <date>):**` block with the 5 detect probe outcomes + flag-on / flag-off render confirmation
       - Move the section out of the 🟡 PLANNED group if there's a separate SHIPPED group

    4. Commit per usual Phase pattern:
       ```
       git add .planning/STATE.md .planning/ROADMAP.md .planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md
       git commit -m "docs(239-03): Phase 239 SHIPPED — onboarding CLI Tools section live on Mini PC"
       ```

    5. Push:
       ```
       git push origin master
       ```
  </action>
  <verify>
    <automated>grep -q "Phase 239.*SHIPPED" .planning/STATE.md && grep -q "Phase 239.*SHIPPED" .planning/ROADMAP.md && test -f .planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md Phase 239 row shows SHIPPED
    - ROADMAP.md Phase 239 section shows ✅ SHIPPED + 3/3 plans + UAT evidence
    - 239-03-SUMMARY.md exists with all required sections
    - git commit + push succeed
  </acceptance_criteria>
  <done>
    Phase 239 closed; v43 milestone progress incremented; next milestone work can start.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → Mini PC SSH | bruce@10.69.31.68 via shared SSH key (contabo_master / minipc) — TRUSTED |
| update.sh → GitHub fetch | https clone of utopusc/livinity-io — accept (existing posture) |
| operator browser → Caddy → Mini PC | HTTPS via livinity.io DNS → Server5 relay → Mini PC private tunnel (CONTEXT routing topology) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-03-01 | Tampering | Sacred SHA `f3538e1d...` drift during deploy | mitigate | PRE-deploy verify + POST-deploy verify. Pre-commit hook in repo blocks any change to `liv/packages/core/src/sdk-agent-runner.ts`. Task 2 acceptance criteria explicitly re-checks after deploy. |
| T-239-03-02 | Tampering | Mini PC canonical blob sha256 drift | mitigate | PRE/POST sha256 capture + diff. Any mismatch = ABORT + rollback investigation. |
| T-239-03-03 | Information Disclosure | Snapshot files committed by accident | mitigate | PRE/POST/UAT snapshot files written under `.planning/phases/239-onboarding-cli-tools/` and explicitly NOT git-added in Task 5 commit (only STATE/ROADMAP/SUMMARY committed). |
| T-239-03-04 | Denial of Service | Mini PC SSH rate-limit (fail2ban) | mitigate | All probes batched into single SSH invocations per CLAUDE.md hard rule. No more than 4 separate SSH sessions across the entire plan (Tasks 1 / 2 / 3 / 4-flag-flip). |
| T-239-03-05 | Spoofing | Deploy to wrong server (Server4/Server5 instead of Mini PC) | mitigate | All ssh commands target `bruce@10.69.31.68` (Mini PC IP). NO command in this plan references `45.137.194.103` (Server4) or `45.137.194.102` (Server5). Hard rule per MEMORY 2026-04-27 user-confirmed prohibition. |
| T-239-03-06 | Elevation | feature flag flip bypasses admin check | accept | Flag is UX-only — UI render gate. tRPC procedures behind it (cliInstaller.install) are independently adminProcedure-gated per Plan 239-01. Worst case = unauth user sees the install grid but can't actually install. |
</threat_model>

<verification>
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` confirmed PRE deploy (Task 1) AND POST deploy (Task 2 + Task 5)
- Mini PC canonical blob sha256 byte-identical PRE/POST
- LICENSE + NOTICE sha256 byte-identical PRE/POST
- All 6 systemd services active POST deploy
- 5 valid `cliInstaller.detect` probes return HTTP 200 with correct shape
- 1 invalid-name probe returns HTTP 400 + CLI_NOT_SUPPORTED (RCE boundary live)
- Operator confirms (Task 4 checkpoint) feature-flag-ON renders CliToolsStep with 5 cards in correct order, Continue enabled
- Operator confirms feature-flag-OFF renders disabled-notice (NOT legacy ProviderStep, which is deleted)
- STATE.md + ROADMAP.md updated, SUMMARY.md committed + pushed
- Only Mini PC touched; no Server4/Server5 SSH attempts
</verification>

<success_criteria>
- Phase 239 SHIPPED on Mini PC
- 3/3 plans (239-01, 239-02, 239-03) marked complete in ROADMAP
- v43 milestone progress incremented from open phase to closed phase
- Phase 240 unblocked — cliInstaller namespace + SUPPORTED_CLIS contract stable for extension
- Operator has chosen + documented the final feature flag state
- No sacred invariants violated
</success_criteria>

<output>
After completion, ensure `.planning/phases/239-onboarding-cli-tools/239-03-SUMMARY.md` is present and committed. STATE.md + ROADMAP.md reflect SHIPPED status. Push to origin/master.
</output>
