---
phase: 24-app-store-expansion
plan: 04
type: execute
wave: 3
depends_on: [24-02, 24-03]
files_modified: []
autonomous: false
requirements: [R-APPS-DB, R-APPS-TEST]

must_haves:
  truths:
    - "All 10 new apps appear in the apps.livinity.io store catalog"
    - "SQL migration executes without errors on Server5 (platform DB)"
    - "livinityd on Server4 can generate compose templates for all 28 builtin apps"
    - "At least 2 representative apps install and load their web UI on Server4"
  artifacts: []
  key_links:
    - from: "builtin-apps.ts"
      to: "Server4 livinityd"
      via: "git pull + PM2 restart"
      pattern: "pm2 restart"
    - from: "0002_seed_expansion_apps.sql"
      to: "Server5 PostgreSQL"
      via: "psql -f migration"
      pattern: "psql"
---

<objective>
Deploy the 10 new apps to production: run SQL migration on Server5 platform DB, deploy updated builtin-apps.ts to Server4 livinityd, and test that representative apps install and show their web UI.

Purpose: Complete the end-to-end app store expansion by deploying code + data and verifying real installs work.
Output: 10 new apps live in the store catalog, at least 2 confirmed working via install test on Server4.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-app-store-expansion/24-CONTEXT.md
@.planning/phases/24-app-store-expansion/24-01-SUMMARY.md
@.planning/phases/24-app-store-expansion/24-02-SUMMARY.md
@.planning/phases/24-app-store-expansion/24-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy to Server4 (livinityd) and Server5 (platform DB)</name>
  <action>
**Step 1: Git push from local**
```bash
git add -A && git push origin master
```

**Step 2: Deploy livinityd on Server4 (45.137.194.103)**
SSH into Server4 using: `/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@45.137.194.103`

Run:
```bash
cd /opt/livos && git pull origin master
source /root/.profile
pm2 restart livos
```

**Step 3: Run SQL migration on Server5 (45.137.194.102) platform DB**
SSH into Server5 using: `/c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@45.137.194.102`

Run:
```bash
cd /opt/platform/web && git pull origin master
psql -U apps_livinity -d apps_livinity -f src/db/migrations/0002_seed_expansion_apps.sql
```

**Step 4: Verify DB seed**
```bash
psql -U apps_livinity -d apps_livinity -c "SELECT name, category, version FROM apps ORDER BY name;"
```
Should show all 18 apps (8 original + 10 new).

NOTE: The SSH command prefix is specific to this Windows development machine. Use the exact SSH path shown above.
  </action>
  <resume-signal>Confirm deployment completed: "Server4 deployed, Server5 migration run" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deployed 10 new apps to both Server4 (livinityd builtin definitions) and Server5 (platform DB store catalog). The apps should now appear in the App Store at apps.livinity.io/store and be installable from LivOS.</what-built>
  <how-to-verify>
    1. Open the LivOS App Store (or visit apps.livinity.io/store directly)
    2. Verify new apps appear in the catalog: look for AdGuard Home, Navidrome, Stirling PDF, Element Web, Wiki.js
    3. Try installing one lightweight app (e.g., Stirling PDF or Element Web) to verify the install flow works
    4. After install, verify the app's web UI loads at its subdomain
  </how-to-verify>
  <resume-signal>Type "approved" if new apps appear in store and at least one installs successfully, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Server5 DB query returns 18 total apps including all 10 new ones
- Server4 livinityd is running with updated builtin-apps.ts
- At least one new app installs and loads its web UI
</verification>

<success_criteria>
- 10 new apps visible in apps.livinity.io/store
- SQL migration executed without errors
- At least 1 new app successfully installs and loads web UI on Server4
- Human verification confirms store catalog is correct
</success_criteria>

<output>
After completion, create `.planning/phases/24-app-store-expansion/24-04-SUMMARY.md`
</output>
