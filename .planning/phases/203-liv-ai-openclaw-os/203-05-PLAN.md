---
phase: 203-liv-ai-openclaw-os
plan: 05
type: execute
wave: 2
depends_on: [203-03]
files_modified:
  - livos/packages/livinityd/source/modules/openclawos/handshake-route.ts (NEW)
  - livos/packages/livinityd/source/modules/openclawos/device-token.ts (NEW)
  - livos/packages/livinityd/source/index.ts (EXTEND — mount /openclawos/handshake)
  - livos/scripts/caddy/bruce.caddy (EXTEND or PATCH — handle /liv-ai-app/* → :18789)
  - livos/scripts/update.sh (EXTEND — Caddy fragment install)
autonomous: true
requirements: [REQ-203-03, REQ-203-07]
tags: [auth, caddy, websocket, wave-2]
must_haves:
  truths:
    - "POST /openclawos/handshake verifies LIVINITY_SESSION JWT and returns a 5-min Ed25519 device token"
    - "Token format matches openclaw gateway's expected handshake (per 203-01 SPIKE answer #4)"
    - "Caddy handle for /liv-ai-app/* points at :18789 (NOT :3010)"
    - "All other Caddy handles UNCHANGED (INV-203-08)"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/openclawos/handshake-route.ts"
      provides: "Express RequestHandler — JWT verify + token mint + 200 with {token, expiresAt}"
      min_lines: 60
    - path: "livos/packages/livinityd/source/modules/openclawos/device-token.ts"
      provides: "mintToken(userId) / verifyToken(token) — Ed25519 sign + 5-min TTL"
---

<objective>
Bridge the outer LIVINITY_SESSION JWT auth (parent UI → iframe) to the inner openclaw gateway Ed25519 device handshake. Ship `POST /openclawos/handshake` in livinityd: verifies the JWT cookie, mints a 5-min Ed25519-signed device token, returns it to the iframe which forwards to the openclaw gateway. Also flip the Caddy `/liv-ai-app/*` handle from `:3010` to `:18789` so the gateway serves the rebranded UI.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-05, D-203-12, T-203-02, INV-203-08, INV-203-10)
@.planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md (answer #4 — gateway handshake shape)
@livos/packages/livinityd/source/modules/jwt.ts — existing JWT verify helper; REUSE, do NOT re-implement
@livos/packages/livinityd/source/modules/server/index.ts — Express mount pattern (mirror /chat/livAi + /agents/status/stream from Phases 197–202)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ed25519 device token mint/verify helper</name>
  <action>
    Create `livos/packages/livinityd/source/modules/openclawos/device-token.ts`:
    - Load gateway keypair from `/opt/livos/data/secrets/openclaw-ed25519` (generated on first boot via `tweetnacl` if missing; deterministic per Mini PC). If running in dev/Windows, fallback to `livos/data/secrets/openclaw-ed25519` under repo root.
    - `mintToken(userId: string): { token: string, expiresAt: number }` — payload `{sub:userId, iat, exp:iat+300}`, signed with Ed25519, base64url-encoded.
    - `verifyToken(token: string): { userId: string } | null` — for the inbound RPC path from `liv-claw-plugin`.
    - Token format must match what 203-01 SPIKE answer #4 documented (if openclaw expects JWT instead of bare Ed25519, swap to `jose` SignJWT with `EdDSA` algorithm).

    Add `tweetnacl` to livinityd deps if not already present.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- device-token</automated>
  </verify>
  <done>4+ unit tests PASS (mint → verify roundtrip, expired token rejected, tampered signature rejected, wrong key rejected)</done>
</task>

<task type="auto">
  <name>Task 2: POST /openclawos/handshake route</name>
  <action>
    Create `livos/packages/livinityd/source/modules/openclawos/handshake-route.ts`. Express RequestHandler:
    1. Read `LIVINITY_SESSION` cookie (re-use existing JWT verify helper from `livos/packages/livinityd/source/modules/jwt.ts`).
    2. If invalid/missing → 401.
    3. `mintToken(currentUser.userId)` → `{ token, expiresAt }`.
    4. Return 200 JSON `{ token, expiresAt }`.
    5. Log the handshake for audit: `[openclawos-handshake] userId=X expiresAt=Y`.

    Mount in `livos/packages/livinityd/source/index.ts` next to the existing `/agents/status/stream` mount.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- handshake-route</automated>
  </verify>
  <done>5+ tests PASS (no cookie → 401, invalid cookie → 401, valid cookie → 200 + token, token verifies, token has 5-min TTL)</done>
</task>

<task type="auto">
  <name>Task 3: Caddy handle rewrite</name>
  <action>
    Locate the existing `bruce.caddy` (or equivalent user-subdomain Caddyfile fragment) under `livos/scripts/caddy/` (create the dir if it doesn't exist — Plan 113 may have left a template). Find the line:
    ```caddyfile
    handle /liv-ai-app/* {
      reverse_proxy :3010
    }
    ```
    Replace with:
    ```caddyfile
    handle /liv-ai-app/* {
      reverse_proxy :18789
    }
    ```
    ZERO other handle modifications (INV-203-08).

    Patch `update.sh` to `install -m 0644` the fragment to `/etc/caddy/Caddyfile.d/bruce.caddy` (idempotent, `cmp -s` guard), then `systemctl reload caddy` (NOT restart — preserves TLS sessions).
  </action>
  <verify>
    <automated>grep -c "reverse_proxy :18789" livos/scripts/caddy/bruce.caddy</automated>
  </verify>
  <done>Single handle changed; Caddy fragment validates locally via `caddy validate`</done>
</task>

<task type="auto">
  <name>Task 4: Wire the handshake into liv-claw-os iframe boot</name>
  <action>
    In `livos/packages/liv-claw-os/` (the rebranded UI clone), find the upstream client init code that performs the Ed25519 handshake against the gateway. Patch it to FIRST `fetch('/openclawos/handshake', { credentials: 'include' })`, then pass the returned `token` to the existing gateway handshake call. Keep upstream contract; only inject the token-fetch step.

    Document the patched file paths in the commit message (so a future upstream re-clone can be re-patched cleanly).
  </action>
  <verify>
    <automated>grep -rn "openclawos/handshake" livos/packages/liv-claw-os/ | wc -l</automated>
  </verify>
  <done>Handshake fetch present in liv-claw-os client init (≥ 1 reference)</done>
</task>

<task type="auto">
  <name>Task 5: Commit</name>
  <action>
    Commit: `feat(203-05): /openclawos/handshake JWT→Ed25519 bridge + Caddy /liv-ai-app/* → :18789`
    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-05"</automated>
  </verify>
  <done>Commit lands; sacred SHA hook passes</done>
</task>

</tasks>

<verification>
- /openclawos/handshake mints + returns Ed25519 token after JWT verify
- Token verifies cleanly; 5-min TTL; replay-rejected after expiry (T-203-02)
- Caddy handle for /liv-ai-app/* points at :18789; no other handle changed
- liv-claw-os client fetches handshake before gateway handshake
- 9+ unit tests across device-token + handshake-route
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-05-SUMMARY.md`
</output>
