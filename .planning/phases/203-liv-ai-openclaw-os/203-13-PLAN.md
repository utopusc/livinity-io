---
phase: 203-liv-ai-openclaw-os
plan: 13
type: execute
wave: 4
depends_on: [203-12]
files_modified:
  - .planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md (NEW)
  - .planning/STATE.md (EXTEND — flip last_shipped_phase, next_phase)
  - .planning/ROADMAP.md (EXTEND — flip Phase 203 heading 🟡 CODE-COMPLETE + DEPLOYED + checkbox row)
autonomous: true
requirements: [REQ-203-08]
tags: [verification, state, roadmap, uat, wave-4]
must_haves:
  truths:
    - "203-VERIFICATION.md filed with `status: human_needed` + 13-step operator UAT walk template"
    - "STATE.md `last_shipped_phase` flipped to Phase 203"
    - "ROADMAP.md gets a `### Phase 203 — 🟡 CODE-COMPLETE + DEPLOYED` heading + 13-row plan checklist"
    - "Phase 203 carry-overs documented for Phase 204+"
    - "Sacred SHA preserved on the docs commit"
---

<objective>
File the operator-walkable VERIFICATION.md (`status: human_needed`) with the 13-step UAT script from CONTEXT.md Acceptance Envelope baked as a checkbox table. Flip STATE.md + ROADMAP.md to reflect Phase 203 CODE-COMPLETE + DEPLOYED. Document carry-overs for Phase 204+. This is the closing docs commit.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (Acceptance Envelope §)
@.planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md (deploy state)
@.planning/phases/202-agents-platform/202-VERIFICATION.md — pattern to mirror (frontmatter shape, § layout)
@.planning/STATE.md — `last_shipped_phase` + `next_phase` fields to update
@.planning/ROADMAP.md — Phase 203 heading insertion right after Phase 202 line 2854
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 203-VERIFICATION.md</name>
  <action>
    Create `.planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md` with:
    - Frontmatter: `phase: 203-liv-ai-openclaw-os`, `status: human_needed`, `deployed_sha: <from 203-12 DEPLOY-LOG>`, `services_active: 6`, `smoke_tests_passed: <count from 203-12>`, `created: <date>`.
    - § A — Deploy state summary (services up, ports listening, sacred SHA preserved verbatim).
    - § B — Mini PC service inventory (`systemctl is-active` output for the 6 units).
    - § C — Executor smoke results from `203-12-DEPLOY-LOG.md`.
    - § D — INV-203-01..10 verification (each invariant — PASS / PARTIAL / FAIL).
    - § E — T-203-01..07 mitigation status.
    - § I — **Operator 13-step UAT walk** (verbatim from CONTEXT.md Acceptance Envelope, each row formatted as `- [ ] PENDING — <step>`).
    - § J — Carry-overs to Phase 204+.

    Operator threshold: ≥ 11/13 PASS to flip Phase 203 → 🟢 SHIPPED in the next session.
  </action>
  <verify>
    <automated>test -f .planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md && grep -c "PENDING" .planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md exists with ≥ 13 PENDING rows + § A–J populated</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md</name>
  <action>
    Edit `.planning/STATE.md`:
    - Frontmatter `last_updated` → now()
    - Body `**Last shipped phase:**` → flip to Phase 203 (mirror Phase 202 sentence shape from line 23)
    - Body `**Next action:**` → "Operator runs 13-step Phase 203 UAT walk per `.planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md` § I — ≥ 11/13 PASS to flip Phase 203 → 🟢 SHIPPED."
    - `## Current Position` block → "Phase: 203 (liv-ai-openclaw-os) — CODE-COMPLETE + DEPLOYED, awaiting operator UAT. Plan: 13/13."

    Keep `## Previously` chain — append the new Phase 203 plan summaries.
  </action>
  <verify>
    <automated>grep -c "Phase 203" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md mentions Phase 203 in last_shipped_phase + next_action + position</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md</name>
  <action>
    Edit `.planning/ROADMAP.md`, after the existing Phase 202 block (ending around line 2865), insert:
    ```markdown
    ### Phase 203: Liv AI on openclaw-os + Desktop App Integration — 🟡 CODE-COMPLETE + DEPLOYED <date>T<time>Z (operator UAT pending)

    **Goal:** Replace Mastra + assistant-ui with a fork-and-rebrand of openclaw-os ("Liv AI") backed by the MIT-licensed `openclaw` gateway, AND wire desktop integration so OpenUI apps generated by `app_create` appear as clickable LivOS dock icons opening windows mounting the live OpenUI markup.

    **Depends on:** Phase 202 (Agents Platform — preserved via INV-203-09). All Phase 202 backend contracts (agents.*, agents.tasks.*, mcp.config.*) kept identical signatures; only inner Mastra calls swapped for the new runtime.

    **Wave plan:** Wave 1 spike + clone + rebrand → Wave 2 bridge to livinityd → Wave 3 Mastra + assistant-ui purge → Wave 4 desktop integration + deploy + UAT.

    **Plans (13):**
    - [x] 203-01-PLAN.md — L0 spike: clone openclaw-os, boot gateway, write decision matrix
    - [x] 203-02-PLAN.md — Clone openclaw-os into livos/packages/liv-claw-os + rebrand pass
    - [x] 203-03-PLAN.md — liv-claw-gateway workspace package + systemd unit + update.sh patch
    - [x] 203-04-PLAN.md — liv-claw-plugin tools call livinityd + livos_openui_apps table + openclawos.apps.* tRPC
    - [x] 203-05-PLAN.md — /openclawos/handshake JWT→Ed25519 bridge + Caddy /liv-ai-app/* → :18789
    - [x] 203-06-PLAN.md — Luse + 11 built-in tools as openclaw gateway tools via liv-claw-plugin
    - [x] 203-07-PLAN.md — LivOSAgent + agent-runtime factory + memory + boot toggle (Branch <X>)
    - [x] 203-08-PLAN.md — Purge @mastra/* + LivOSMastra; move runtime files to agent-runtime/
    - [x] 203-09-PLAN.md — Purge @assistant-ui/* + chat surface; keep Phase 202 /agents + /settings
    - [x] 203-10-PLAN.md — Desktop integration: OpenUI apps register as dock icons + OpenUiAppContent window
    - [x] 203-11-PLAN.md — /liv-ai-app/apps/[slug] standalone OpenUI app renderer
    - [x] 203-12-PLAN.md — Mini PC deploy + smoke log + update.sh Phase 203 patches
    - [x] 203-13-PLAN.md — 203-VERIFICATION.md + STATE/ROADMAP flip + final commit

    **Phase-level deploy state (<date>):**
    - Push range: <from 203-12>
    - Deployed SHA: <from 203-12>
    - Sacred SHA canonical: f3538e1d811992b782a9bb057d1b7f0a0189f95f — PRESERVED across every Phase 203 commit
    - Services active: 6/6 — livos + liv-core + liv-worker + liv-memory + liv-claw-gateway + liv-claw-plugin (livos-app-liv-ai retired)
    - 13-step UAT pending operator at 203-VERIFICATION.md § I

    **Carry-overs to Phase 204+:** custom OpenUI app icons (agent-emitted iconUrl) + per-user OpenUI apps + OpenUI app marketplace + multi-user iframe isolation + external telemetry backend + LangGraph subgraph editor UI (if Branch B) + hot-reload of MCP servers + sub-agent depth > 2 + distributed gateway + voice mode + PDF attachment adapter.
    ```

    Fill `<date>` + `<from 203-12>` placeholders from the actual deploy log.
  </action>
  <verify>
    <automated>grep -c "Phase 203: Liv AI on openclaw-os" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md has Phase 203 heading + 13 checkbox rows + deploy state block + carry-overs</done>
</task>

<task type="auto">
  <name>Task 4: Commit + sacred SHA</name>
  <action>
    `git add -f .planning/phases/203-liv-ai-openclaw-os/203-VERIFICATION.md .planning/STATE.md .planning/ROADMAP.md` (forced because `.planning/` is gitignored per MEMORY pattern).

    Commit: `docs(203-13): 203-VERIFICATION.md + STATE/ROADMAP flip — Phase 203 CODE-COMPLETE + DEPLOYED`

    Push to origin. Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-13"</automated>
  </verify>
  <done>Commit lands; pushed; sacred SHA preserved</done>
</task>

</tasks>

<verification>
- 203-VERIFICATION.md filed with status:human_needed + 13 PENDING rows
- STATE.md flipped to Phase 203 last-shipped
- ROADMAP.md gets Phase 203 heading + deploy state + carry-overs
- Sacred SHA preserved
- Phase 203 CODE-COMPLETE + DEPLOYED; operator UAT is the only thing standing between 🟡 and 🟢 SHIPPED
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-13-SUMMARY.md`
</output>
