---
phase: 203-liv-ai-openclaw-os
plan: 12
type: execute
wave: 4
depends_on: [203-08, 203-09, 203-10, 203-11]
files_modified:
  - livos/scripts/update.sh (EXTEND — full Phase 203 patch)
  - .planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md (NEW)
autonomous: true
requirements: [REQ-203-07]
tags: [deploy, mini-pc, update-sh, systemd, wave-4]
must_haves:
  truths:
    - "`bash /opt/livos/update.sh` on Mini PC converges idempotently (re-run = no-op)"
    - "7 systemd units active post-deploy: livos + liv-core + liv-worker + liv-memory + liv-claw-gateway + liv-claw-plugin + (livos-app-liv-ai retired = inactive by design)"
    - "Postgres migrations 0003 + 0004 applied (livos_openui_apps + drop mastra tables)"
    - "Caddy `/liv-ai-app/*` handle points at :18789 (NOT :3010)"
    - "LIV_AGENT_RUNTIME=openclaw set in /opt/livos/.env"
    - "Sacred SHA on Mini PC verified — `/opt/liv/packages/core/src/sdk-agent-runner.ts` git-blob hash = f3538e1d811992b782a9bb057d1b7f0a0189f95f"
  artifacts:
    - path: ".planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md"
      provides: "Verbatim Mini PC deploy log with timestamps, services status, smoke results"
      min_lines: 80
---

<objective>
Ship Phase 203 to Mini PC via `bash /opt/livos/update.sh`. Install the 2 new systemd units, run the migrations, flip Caddy, restart the unit chain, and verify all the smoke surfaces. Per the recurring update.sh drift pattern (P198/P199/P200/P201/P202 carry-overs), update both repo-tracked `livos/scripts/update.sh` AND the Mini PC `/opt/livos/update.sh` shadow.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-04, D-203-05, INV-203-06, INV-203-07)
@livos/scripts/update.sh — canonical deploy script; Phase 203 patches stack on top of P201/P202 fixes
@MEMORY [update.sh source/production drift] — diff Mini PC shadow vs repo before patching
@MEMORY [Mini PC SSH] — `ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68` (sudo for /opt/)
@MEMORY [ZeroTier link to Mini PC unstable] — detach long ops (nohup + log + background poll); never hold foreground SSH for >30s operations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final update.sh patch — assemble Phase 203 hooks</name>
  <action>
    Consolidate Phase 203 deploy patches into `livos/scripts/update.sh`:
    1. rsync source list: add `packages/liv-claw-os/` + `packages/liv-claw-gateway/` + `packages/liv-claw-plugin/` + `packages/openui-validator/` (Plan 203-04).
    2. Migrations: ensure `0003_livos_openui_apps.sql` + `0004_drop_mastra_tables.sql` are run by `runLivOSMigrations`.
    3. Build chain: `pnpm --filter @livos/openui-validator build` → `pnpm --filter @livos/liv-claw-os build` → `pnpm --filter @livos/liv-claw-gateway install`.
    4. systemd unit install (Phase 203 introduces 2 new): `liv-claw-gateway.service` (Plan 203-03) + `liv-claw-plugin.service` (Plan 203-06 worker).
    5. Caddy fragment install: `/etc/caddy/Caddyfile.d/bruce.caddy` rewritten per Plan 203-05; reload caddy.
    6. /opt/livos/.env additions: `LIV_AGENT_RUNTIME=openclaw`, `LIV_PLUGIN_TOKEN=<random>` (generate if missing).
    7. Retire `livos-app-liv-ai.service`: `systemctl disable --now livos-app-liv-ai.service` (won't be needed after openclaw takes over). Idempotent — skip if already disabled.
    8. Restart chain: `systemctl restart livos liv-core liv-worker liv-memory liv-claw-gateway liv-claw-plugin`.
    9. Existing recurring chown hook: `chown -R bruce:bruce /opt/livos/packages/liv-claw-os /opt/livos/packages/liv-claw-gateway /opt/livos/packages/liv-claw-plugin /opt/livos/packages/openui-validator`.
  </action>
  <verify>
    <automated>grep -cE "liv-claw-gateway|liv-claw-plugin|liv-claw-os|openui-validator" livos/scripts/update.sh</automated>
  </verify>
  <done>update.sh references all 4 new packages in ≥ 6 places (rsync, build, install, restart, chown, env)</done>
</task>

<task type="auto">
  <name>Task 2: Push to GitHub</name>
  <action>
    `git push origin master`. update.sh on Mini PC clones from this remote per MEMORY pattern.
  </action>
  <verify>
    <automated>git push origin master 2>&1 | findstr /R "master\|up-to-date"</automated>
  </verify>
  <done>Push lands on origin</done>
</task>

<task type="auto" gate="non-blocking">
  <name>Task 3: Run update.sh on Mini PC (detached, monitor via background poll)</name>
  <action>
    Per ZeroTier instability MEMORY: detach.

    SSH command (single session):
    ```
    nohup sudo bash /opt/livos/update.sh > /tmp/livos-203-deploy.log 2>&1 &
    echo $! > /tmp/livos-203-deploy.pid
    ```

    Poll with separate SSH every 30s for ≤ 10 min: `tail -20 /tmp/livos-203-deploy.log && systemctl is-active livos liv-core liv-worker liv-memory liv-claw-gateway liv-claw-plugin`.

    If the deploy fails: capture log, fix the deviation in `livos/scripts/update.sh`, commit (Rule-1 / Rule-2 / Rule-3 deviation per MEMORY P202 pattern), push, re-run update.sh. Each hotfix is an atomic commit before the next attempt.
  </action>
  <verify>
    <automated>ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 "sudo systemctl is-active liv-claw-gateway liv-claw-plugin" 2>NUL</automated>
  </verify>
  <done>Both new units report `active`</done>
</task>

<task type="auto">
  <name>Task 4: Smoke tests on Mini PC (executor-run)</name>
  <action>
    Run via SSH (batch into one session per MEMORY [SSH rate limit]):
    ```
    set -e
    # Health
    curl -sf http://127.0.0.1:18789/health
    # Caddy routing
    curl -sf https://bruce.livinity.io/liv-ai-app/ | grep -c "Liv AI"
    # Phase 202 preserved
    curl -sf https://bruce.livinity.io/liv-ai-app/agents
    curl -sf https://bruce.livinity.io/liv-ai-app/settings
    # New openclawos.apps surface
    curl -sf -X POST -H "Content-Type: application/json" \
      -d '{"0":{"json":{"slug":"deploy-smoke","name":"Deploy Smoke","content":"<heading>OK</heading>"}}}' \
      https://bruce.livinity.io/trpc/openclawos.apps.create?batch=1
    curl -sf https://bruce.livinity.io/liv-ai-app/apps/deploy-smoke | grep -c "OK"
    # Sacred SHA
    sudo bash /opt/livos/scripts/verify-sacred-sha.sh
    # Mastra purge
    journalctl -u livos -n 200 | grep -i mastra | wc -l   # MUST be 0
    ```

    Capture all output verbatim into `203-12-DEPLOY-LOG.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md && grep -c "PASS\|active\|HTTP/2 200" .planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md</automated>
  </verify>
  <done>≥ 6/7 smokes PASS; deploy log filed</done>
</task>

<task type="auto">
  <name>Task 5: Commit + record deployed SHA</name>
  <action>
    `git add livos/scripts/update.sh .planning/phases/203-liv-ai-openclaw-os/203-12-DEPLOY-LOG.md` → commit: `chore(203-12): Mini PC deploy + smoke log + update.sh Phase 203 patches`.

    On Mini PC, record deployed SHA: `git rev-parse HEAD > /opt/livos/.deployed-sha`.

    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-12"</automated>
  </verify>
  <done>Commit lands; deployed-sha recorded; sacred SHA preserved</done>
</task>

</tasks>

<verification>
- update.sh patched + idempotent
- 6 systemd units active (livos + liv-core + liv-worker + liv-memory + liv-claw-gateway + liv-claw-plugin); livos-app-liv-ai retired
- 6+ HTTP smokes PASS
- `journalctl -u livos | grep mastra = 0` (UAT step 10 pre-verified)
- Sacred SHA verified on Mini PC
- Deploy log filed
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-12-SUMMARY.md`
</output>
