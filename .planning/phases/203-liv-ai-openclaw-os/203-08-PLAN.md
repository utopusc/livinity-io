---
phase: 203-liv-ai-openclaw-os
plan: 08
type: execute
wave: 3
depends_on: [203-07]
files_modified:
  - livos/packages/livinityd/package.json (REMOVE @mastra/* deps)
  - livos/packages/livinityd/source/modules/mastra/** (DELETE — LivOSMastra, mastra-instance, etc.)
  - livos/packages/livinityd/source/index.ts (EXTEND — flip default LIV_AGENT_RUNTIME=openclaw, remove mastra branch)
  - livos/packages/livinityd/source/modules/mastra/chat-route.ts (DELETE; replaced by agent-runtime/chat-route.ts via Plan 203-07)
  - livos/packages/livinityd/source/modules/mastra/scheduler.ts (MOVE → agent-runtime/scheduler.ts)
  - livos/packages/livinityd/source/modules/mastra/agents/*.ts (MOVE → agent-runtime/agents/)
  - livos/packages/livinityd/source/modules/mastra/approval-manager.ts (MOVE → agent-runtime/approval-manager.ts)
  - livos/packages/livinityd/source/modules/server/trpc/agent-router.ts (PATCH imports)
  - livos/packages/livinityd/source/modules/server/trpc/agent-task-router.ts (PATCH imports)
  - livos/packages/livinityd/source/modules/server/trpc/mcp-config-router.ts (PATCH imports if any mastra ref)
autonomous: true
requirements: [REQ-203-01]
tags: [purge, mastra-removal, wave-3]
must_haves:
  truths:
    - "Zero @mastra/* packages in livinityd/package.json (`pnpm list | grep mastra` = 0)"
    - "Zero references to LivOSMastra in the codebase (`grep -rn LivOSMastra livos/packages/livinityd/source` = 0)"
    - "AgentRegistry + AgentScheduler + agent-factory + ApprovalManager moved under agent-runtime/ with imports updated"
    - "Phase 202 tRPC contracts preserved (INV-203-09) — agents.* + agents.tasks.* + mcp.config.* identical signatures"
    - "Scheduler drain executed cleanly on swap (T-203-04)"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/agent-runtime/scheduler.ts"
      provides: "AgentScheduler moved from mastra/ — same class, same contract"
---

<objective>
Purge Mastra entirely. Delete `LivOSMastra`, all `@mastra/*` deps, and the `mastra/` subdirectory. Move scheduler + agent registry + approval-manager + agent files into `agent-runtime/`. Update tRPC router imports. Flip the boot default to `LIV_AGENT_RUNTIME=openclaw`. Phase 202 contracts (`agents.*`, `agents.tasks.*`, `mcp.config.*`) MUST keep identical signatures — only their internals point at the new runtime.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-07, INV-203-09, T-203-04)
@livos/packages/livinityd/source/modules/mastra/ — directory to be drained then deleted
@livos/packages/livinityd/source/modules/agent-runtime/ — destination from Plan 203-07
@livos/packages/livinityd/source/modules/server/trpc/agent-router.ts — Phase 202-03 router; imports must repoint, contract MUST NOT change
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drain phase — pause scheduler, capture state</name>
  <action>
    Add a `scheduler.drainForRuntimeSwap()` helper in `agent-runtime/scheduler.ts` that:
    1. Calls `pauseAll()` — stops all node-cron timers without unscheduling.
    2. Awaits any in-flight `runOnce` drains via the existing `runningTasks` Set.
    3. Returns `{ paused: agentIds[], wasRunning: agentIds[] }`.

    Call this from `livinityd/source/index.ts` BEFORE re-instantiating the registry with the new runtime on a hot-swap. On cold-boot, simply skip.

    Add `resumeAll()` complement.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- scheduler.drain</automated>
  </verify>
  <done>4+ tests PASS (pause stops timers, drain awaits running tasks, resume re-arms, idempotent)</done>
</task>

<task type="auto">
  <name>Task 2: Move agent-related files mastra/ → agent-runtime/</name>
  <action>
    `git mv` (preserves history):
    - `source/modules/mastra/scheduler.ts` → `source/modules/agent-runtime/scheduler.ts`
    - `source/modules/mastra/agents/agent-registry.ts` → `source/modules/agent-runtime/agents/agent-registry.ts`
    - `source/modules/mastra/agents/agent-repository.ts` → `source/modules/agent-runtime/agents/agent-repository.ts`
    - `source/modules/mastra/agents/destructive-tools.ts` → `source/modules/agent-runtime/agents/destructive-tools.ts`
    - `source/modules/mastra/approval-manager.ts` → `source/modules/agent-runtime/approval-manager.ts`
    - `source/modules/mastra/memory.ts` → if Branch B, DELETE (replaced by agent-runtime/memory.ts); if Branch A, keep but rename to memory-legacy.ts and DELETE next plan

    Update all imports across `livinityd/source/**` via search-and-replace. Run `pnpm --filter livinityd typecheck` — must stay at or below the pre-existing 382 error count (per Phase 202 baseline).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd typecheck 2>&1 | findstr /C:"error TS" | find /c "error TS"</automated>
  </verify>
  <done>Files moved with history preserved; typecheck error count ≤ 382 (Phase 202 baseline)</done>
</task>

<task type="auto">
  <name>Task 3: Delete LivOSMastra + remaining mastra/ files</name>
  <action>
    Delete:
    - `source/modules/mastra/index.ts` (LivOSMastra class)
    - `source/modules/mastra/mastra-instance.ts` (Plan 202-09 wrapper)
    - `source/modules/mastra/chat-route.ts` (Phase 197 SSE route — replaced by openclaw gateway's own chat surface)
    - `source/modules/mastra/mcp-bridge.ts` — KEEP but move to `agent-runtime/mcp-bridge.ts` (still needed for Luse + external MCP)
    - `source/modules/mastra/provider-router.ts` — KEEP but move to `agent-runtime/provider-router.ts`
    - `source/modules/mastra/redact-error.ts`, `errors.ts`, `mcp-errors.ts` — MOVE to `agent-runtime/`
    - `source/modules/mastra/agents/liv-ai.ts` (createLivAiAgent shim) — replaced by agent-runtime factory; DELETE
    - Any remaining files in mastra/ → MOVE
    - Final: `rmdir source/modules/mastra/` (must be empty)

    Update boot wire-up to remove the mastra branch of `LIV_AGENT_RUNTIME` (now only the openclaw branch exists; flag stays for forward compatibility but no longer dispatches).
  </action>
  <verify>
    <automated>! test -d livos/packages/livinityd/source/modules/mastra && grep -rn "LivOSMastra" livos/packages/livinityd/source 2>NUL | wc -l</automated>
  </verify>
  <done>mastra/ directory gone; zero LivOSMastra refs in source</done>
</task>

<task type="auto">
  <name>Task 4: Remove @mastra/* deps</name>
  <action>
    Edit `livos/packages/livinityd/package.json`, remove:
    - `@mastra/core`
    - `@mastra/ai-sdk`
    - `@mastra/memory`
    - `@mastra/mcp`
    - `@mastra/pg`
    - `@mastra/observability`

    Run `pnpm install` from workspace root to update lockfile. Verify zero `@mastra` matches in `pnpm-lock.yaml` for the livinityd package.

    Drop `mastra_*` Postgres tables IF Branch B (LangGraph has its own tables); if Branch A, openclaw's persistence may overlap — verify per 203-01 SPIKE. Either way, write the DROP statements into a migration `0004_drop_mastra_tables.sql` guarded by `IF EXISTS`.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd list 2>&1 | findstr "@mastra"</automated>
  </verify>
  <done>0 @mastra/* packages installed for livinityd</done>
</task>

<task type="auto">
  <name>Task 5: Update tRPC router imports — preserve INV-203-09 contracts</name>
  <action>
    In `agent-router.ts`, `agent-task-router.ts`, `mcp-config-router.ts`: replace any import from `'../../mastra/...'` with `'../../agent-runtime/...'`. Procedure signatures (input zod, output shape, error codes) MUST stay identical — Phase 202 frontend has live callers (`/agents`, `/agents/[id]`, `/agents/new`, `/settings`).

    Run the Phase 202 vitest suites (`agent-repository.test.ts`, `agent-registry.test.ts`, `scheduler.test.ts`) — all 8+8+8 tests MUST still PASS.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- agent-repository agent-registry scheduler</automated>
  </verify>
  <done>All Phase 202 vitest suites still PASS; tRPC contracts identical (eyeball diff)</done>
</task>

<task type="auto">
  <name>Task 6: Commit (atomic — single squash)</name>
  <action>
    Commit: `refactor(203-08): purge @mastra/* + LivOSMastra; move agent runtime files to agent-runtime/`
    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-08"</automated>
  </verify>
  <done>Commit lands; sacred SHA preserved; build still green</done>
</task>

</tasks>

<verification>
- 0 @mastra/* packages in livinityd
- 0 LivOSMastra refs in source
- mastra/ dir deleted; contents moved to agent-runtime/
- Phase 202 vitest suites still PASS (regression-locked tRPC contracts)
- Scheduler drain helpers in place (T-203-04)
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-08-SUMMARY.md`
</output>
