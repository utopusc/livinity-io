---
phase: 203-liv-ai-openclaw-os
plan: 07
type: execute
wave: 3
depends_on: [203-04, 203-05, 203-06]
files_modified:
  - livos/packages/livinityd/source/modules/agent-runtime/index.ts (NEW — LivOSAgent class)
  - livos/packages/livinityd/source/modules/agent-runtime/agent-factory.ts (NEW)
  - livos/packages/livinityd/source/modules/agent-runtime/memory.ts (NEW)
  - livos/packages/livinityd/source/index.ts (EXTEND — LIV_AGENT_RUNTIME toggle)
  - livos/packages/livinityd/source/modules/mastra/chat-route.ts (EXTEND — branch dispatch)
  - livos/packages/livinityd/package.json (EXTEND IF Branch B — add LangGraph deps)
autonomous: true
requirements: [REQ-203-01]
tags: [agent-runtime, swap, wave-3]
must_haves:
  truths:
    - "LivOSAgent class exists with the same attach* surface LivOSMastra had"
    - "agent-factory contract preserved — AgentRegistry from Phase 202-02 calls it unchanged"
    - "Chat surface dispatches an LLM call end-to-end via the new runtime"
    - "Boot toggle `LIV_AGENT_RUNTIME=mastra|openclaw` selects the runtime; default mastra to keep rollback"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/agent-runtime/index.ts"
      provides: "LivOSAgent class — successor to LivOSMastra; same attach* surface"
      min_lines: 100
    - path: "livos/packages/livinityd/source/modules/agent-runtime/agent-factory.ts"
      provides: "createAgentFromRow(row) — contract-equivalent to mastra/agents/agent-factory.ts"
      min_lines: 60
---

<objective>
Implement the agent runtime swap per the branch chosen in Plan 203-01. The new `LivOSAgent` class lives in `livos/packages/livinityd/source/modules/agent-runtime/` (parallel to `mastra/`). It exposes the EXACT same `attach*` surface as `LivOSMastra` so the boot wire-up can swap with minimal changes. AgentRegistry + AgentScheduler call `agent-factory.createAgentFromRow(row)` unchanged. Mastra deps stay on disk for this plan (purged in 203-08) so we can A/B test by toggling a boot flag.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-06, D-203-07, D-203-15, T-203-04)
@.planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md (CHOSEN BRANCH drives implementation)
@livos/packages/livinityd/source/modules/mastra/index.ts — LivOSMastra surface to replicate
@livos/packages/livinityd/source/modules/mastra/agents/agent-factory.ts — current Mastra-bound factory contract
@livos/packages/livinityd/source/modules/mastra/agents/agent-registry.ts — caller; do NOT modify, match the contract
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch resolution from 203-01 SPIKE</name>
  <action>
    Read `.planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md` and extract `CHOSEN BRANCH`.

    **Branch A (built-in dispatch):** `LivOSAgent` is a thin wrapper around the openclaw client. No LangGraph install.

    **Branch B (delegated dispatch):** Install `@langchain/langgraph@^0.4.x` + `@langchain/langgraph-checkpoint-postgres@^0.1.x` + provider adapter matching existing ProviderRouter. Build a StateGraph: `chatNode → toolNode → conditional edge`. PostgresSaver for checkpointing against the existing `livos` DB.

    Document the choice in the commit message.
  </action>
  <verify>
    <automated>grep -E "CHOSEN BRANCH:\s*[AB]" .planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md</automated>
  </verify>
  <done>Branch identified and locked in the implementation</done>
</task>

<task type="auto">
  <name>Task 2: LivOSAgent class</name>
  <action>
    Create `livos/packages/livinityd/source/modules/agent-runtime/index.ts`:
    ```ts
    export class LivOSAgent {
      memory: Memory | null = null
      mcpBridge: McpBridge | null = null
      registry: AgentRegistry | null = null
      scheduler: AgentScheduler | null = null
      agentInstance: AgentInstance | null = null   // openclaw Client OR LangGraph compiled graph
      agents: { livAi?: RuntimeAgent } = {}

      attachMemory(m: Memory) { this.memory = m }
      attachMcpBridge(b: McpBridge) { this.mcpBridge = b }
      attachRegistry(r: AgentRegistry) { this.registry = r }
      attachScheduler(s: AgentScheduler) { this.scheduler = s }
      attachAgentInstance(a: AgentInstance) { this.agentInstance = a }
    }
    ```
    The 5 attach methods + 6 slots are a strict superset of `LivOSMastra` so boot wire-up does not need restructuring.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd typecheck 2>&1 | findstr "agent-runtime"</automated>
  </verify>
  <done>LivOSAgent compiles; 5 attach methods + 6 slots present</done>
</task>

<task type="auto">
  <name>Task 3: agent-factory.ts</name>
  <action>
    Create `livos/packages/livinityd/source/modules/agent-runtime/agent-factory.ts` with `createAgentFromRow(row, deps): RuntimeAgent`. Signature MUST match the existing Mastra factory so `AgentRegistry.refresh()` (Phase 202-02) swaps implementations via a single boot-flag check.

    Branch-specific body:
    - **Branch A:** `return deps.openclawClient.agent({ id: row.id, instructions: row.instructions, model: row.modelName, tools: filterTools(row.toolIds, deps.tools) })`. Apply same `wrapDestructive` (W-02) + `filterMcp` ordering as Phase 202-02.
    - **Branch B:** Build a LangGraph StateGraph with `chatNode(model=row.modelName, systemPrompt=row.instructions)` + `toolNode(tools=filterTools(row.toolIds, deps.tools))` + conditional routing. Compile and return.

    Sub-agent wiring (D-202-03 Supervisor pattern): Branch A inherits openclaw's nested-agent support; Branch B uses LangGraph's `Send` pattern. Either way, expose the same `.stream(prompt)` surface so chat-route stays small.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- agent-factory</automated>
  </verify>
  <done>5+ tests PASS (factory builds agent, tool filter applied, destructive wrap applied, sub-agent map built when parentAgentId set)</done>
</task>

<task type="auto">
  <name>Task 4: Memory backend</name>
  <action>
    Create `livos/packages/livinityd/source/modules/agent-runtime/memory.ts`:
    - **Branch A:** wrapper around openclaw's built-in memory API; persists to openclaw-managed table OR to `livos_agent_memory` if openclaw exposes a custom-backend hook.
    - **Branch B:** wrapper around `PostgresSaver` from `@langchain/langgraph-checkpoint-postgres`. Re-use existing `livos` DB. Tables prefixed `livos_lg_*` so they don't collide with Mastra's `mastra_*` (still present until Plan 203-08 purge).

    Either way, Memory class exposes `saveThread / listThreads / updateThread` so Plan 202-03 scheduler + 202-05 RecentTasksList keep working without changes.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- agent-runtime/memory</automated>
  </verify>
  <done>4+ tests PASS (save / list / update / delete thread)</done>
</task>

<task type="auto">
  <name>Task 5: Boot toggle + chat-route branch dispatch</name>
  <action>
    In `livos/packages/livinityd/source/index.ts`, add `process.env.LIV_AGENT_RUNTIME` toggle:
    - `mastra` (default) → constructs LivOSMastra (current path)
    - `openclaw` → constructs LivOSAgent + agent-runtime factory

    Both paths wire the same `attachRegistry`, `attachScheduler`, `attachMemory`. Plan 203-12 deploys with the flag set; rollback = env-var only.

    `chat-route.ts` inspects `livOSContainer.kind === 'openclaw' | 'mastra'` and routes accordingly. Until Plan 203-08 deletes the Mastra branch, both code paths remain.
  </action>
  <verify>
    <automated>grep -c "LIV_AGENT_RUNTIME" livos/packages/livinityd/source/index.ts</automated>
  </verify>
  <done>Toggle in place; both branches wire the same surface; chat-route dispatches both</done>
</task>

<task type="auto">
  <name>Task 6: End-to-end smoke + commit</name>
  <action>
    With `LIV_AGENT_RUNTIME=openclaw` and `liv-claw-gateway` running locally, fire a test prompt through the gateway and verify a response streams back end-to-end (curl against the gateway, assert non-empty SSE stream).

    Commit: `feat(203-07): LivOSAgent (Branch <X>) + agent-runtime factory + memory + boot toggle`
    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-07"</automated>
  </verify>
  <done>End-to-end smoke prompt returns non-empty stream; commit lands; sacred SHA preserved</done>
</task>

</tasks>

<verification>
- LivOSAgent class implemented with full attach* surface
- agent-factory.ts contract-equivalent to Mastra factory (AgentRegistry untouched)
- Memory backend operational for both branches
- LIV_AGENT_RUNTIME toggle wired; rollback available via env var
- E2E smoke: prompt → openclaw gateway → LivOSAgent → LLM → streamed response
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-07-SUMMARY.md`
</output>
