---
phase: 203-liv-ai-openclaw-os
plan: 02
type: execute
wave: 1
depends_on: [203-01]
files_modified:
  - livos/packages/liv-claw-os/** (NEW — full clone of thesysdev/openclaw-os at SPIKE HEAD SHA)
  - livos/pnpm-workspace.yaml (EXTEND — add packages/liv-claw-os if needed)
autonomous: true
requirements: [REQ-203-02]
tags: [clone, rebrand, wave-1]
must_haves:
  truths:
    - "`livos/packages/liv-claw-os/` exists with the upstream tree at the SPIKE HEAD SHA"
    - "Every user-visible string says `Liv AI` (NEVER `OpenClaw` / `Open Claw` / `openclaw`)"
    - "Build passes (`pnpm --filter liv-claw-os build` exits 0)"
    - "Sacred SHA preserved across each rebrand commit"
  artifacts:
    - path: "livos/packages/liv-claw-os/package.json"
      provides: "Workspace package definition with name `@livos/liv-claw-os` and same start scripts as upstream"
---

<objective>
Clone `thesysdev/openclaw-os` at the SPIKE HEAD SHA into `livos/packages/liv-claw-os/` and perform the user-visible-string rebrand pass: page title, breadcrumb, prompts/, SKILL.md, notifications, command palette, error messages all read "Liv AI". NO functional code changes. The folder name stays `liv-claw-os` so we can grep against upstream when debugging.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-02, D-203-03)
@.planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md (HEAD SHA to clone at)
@livos/pnpm-workspace.yaml — workspace pattern (add packages/* if not already wildcarded)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone at the SPIKE SHA into the workspace</name>
  <action>
    Inside `livos/packages/`, run:
    ```
    git clone --no-checkout https://github.com/thesysdev/openclaw-os liv-claw-os
    cd liv-claw-os
    git checkout <SPIKE HEAD SHA>
    rm -rf .git    # detach from upstream history; the tree merges into our monorepo
    ```
    Rename `package.json` `name` field from upstream value to `@livos/liv-claw-os`.
  </action>
  <verify>
    <automated>test -d livos/packages/liv-claw-os && test -f livos/packages/liv-claw-os/package.json && ! test -d livos/packages/liv-claw-os/.git</automated>
  </verify>
  <done>Tree present; package.json name = `@livos/liv-claw-os`; no nested `.git`</done>
</task>

<task type="auto">
  <name>Task 2: Install + build pre-rebrand baseline</name>
  <action>
    From workspace root: `pnpm install` then `pnpm --filter @livos/liv-claw-os build`. Record any upstream build errors verbatim in the commit message and fix only if they're install-deterministic (missing dep, version pin). NO source edits beyond `package.json` name in this task.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/liv-claw-os build</automated>
  </verify>
  <done>Build exits 0 on the pre-rebrand baseline</done>
</task>

<task type="auto">
  <name>Task 3: User-visible string rebrand sweep</name>
  <action>
    Run a deterministic find-and-replace pass across `livos/packages/liv-claw-os/`:

    | Match (case-sensitive) | Replace with |
    |------------------------|--------------|
    | `OpenClaw OS` | `Liv AI` |
    | `OpenClaw` | `Liv AI` |
    | `Open Claw` | `Liv AI` |
    | `openclaw-os` (in user-visible strings only — leave package names + import paths alone) | `liv-ai` |

    Targets (in order):
    1. `app/**/*.tsx` + `app/**/*.ts` — page titles, layout titles, metadata
    2. `components/**/*.tsx` — JSX text content
    3. `prompts/**/*.md` — system prompts
    4. `SKILL.md` (if exists at repo root)
    5. `public/**/*.json` — manifest, robots, etc.
    6. `package.json` `description` field
    7. Any toast / error message string literals

    EXCLUDE from rebrand: `node_modules/`, import paths (`from 'openclaw'`), `package.json` `dependencies` keys, any code comment that documents upstream cross-reference.
  </action>
  <verify>
    <automated>cd livos/packages/liv-claw-os && (grep -rE "OpenClaw|Open Claw" --include="*.tsx" --include="*.ts" --include="*.md" . | grep -v "node_modules" | grep -v "// upstream:" | wc -l) -eq 0</automated>
  </verify>
  <done>0 user-visible occurrences of OpenClaw / Open Claw remain (allowlist: upstream cross-ref comments only)</done>
</task>

<task type="auto">
  <name>Task 4: Re-build post-rebrand + INV-203-05 English-only check</name>
  <action>
    Re-run `pnpm --filter @livos/liv-claw-os build` — must still exit 0 (we only changed string literals). Then grep for Turkish chars per INV-203-05.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter @livos/liv-claw-os build && (grep -rE "[ışğüöçİŞĞÜÖÇ]" packages/liv-claw-os --include="*.tsx" --include="*.ts" | wc -l) -eq 0</automated>
  </verify>
  <done>Build PASS post-rebrand; 0 Turkish chars in JSX/TS files</done>
</task>

<task type="auto">
  <name>Task 5: Commit in two atomic steps</name>
  <action>
    Commit 1: `chore(203-02): clone openclaw-os at <SHA> into packages/liv-claw-os` (Tasks 1–2)
    Commit 2: `chore(203-02): rebrand user-visible strings OpenClaw → Liv AI` (Tasks 3–4)
    Sacred SHA hook MUST PASS on both commits.
  </action>
  <verify>
    <automated>git log -2 --format="%s" | findstr /C:"203-02"</automated>
  </verify>
  <done>Two commits land cleanly; sacred SHA hook passes on both</done>
</task>

</tasks>

<verification>
- liv-claw-os workspace package exists with upstream tree
- Build PASS pre- and post-rebrand
- 0 user-visible OpenClaw strings; 0 Turkish chars
- Two atomic commits; sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-02-SUMMARY.md`
</output>
