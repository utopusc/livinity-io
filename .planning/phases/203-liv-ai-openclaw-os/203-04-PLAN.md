---
phase: 203-liv-ai-openclaw-os
plan: 04
type: execute
wave: 2
depends_on: [203-03]
files_modified:
  - livos/packages/liv-claw-plugin/** (NEW — cloned from @openuidev/openclaw-os-plugin source + rebranded)
  - livos/packages/livinityd/source/db/migrations/0003_livos_openui_apps.sql (NEW)
  - livos/packages/livinityd/source/db/schema.ts (EXTEND)
  - livos/packages/livinityd/source/modules/openclawos/openui-apps-repository.ts (NEW)
  - livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts (NEW)
  - livos/packages/livinityd/source/modules/server/trpc/index.ts (EXTEND — mount openclawos.*)
  - livos/packages/livinityd/source/modules/server/trpc/common.ts (EXTEND — httpOnlyPaths)
autonomous: true
requirements: [REQ-203-06]
tags: [plugin, postgres, trpc, openui, wave-2]
must_haves:
  truths:
    - "Postgres table `livos_openui_apps` exists with the D-203-09 columns"
    - "tRPC namespace `openclawos.apps.*` exposes create/list/get/update/delete/version"
    - "Plugin's `app_create` / `artifact` / `db_query` tools call livinityd HTTP — NOT filesystem writes"
    - "OpenUI markup validated against the 14-component whitelist BEFORE persistence (T-203-03)"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/liv-claw-plugin/src/tools/app-create.ts"
      provides: "app_create tool — POSTs to livinityd /trpc/openclawos.apps.create"
    - path: "livos/packages/livinityd/source/modules/openclawos/openui-apps-repository.ts"
      provides: "OpenUIAppsRepository with listAll/getBySlug/upsert/delete/incrementVersion"
      min_lines: 80
---

<objective>
Clone + rebrand the `@openuidev/openclaw-os-plugin` package into `livos/packages/liv-claw-plugin/`. Reshape the three IO-heavy tools (`app_create`, `artifact`, `db_query`) so they call livinityd HTTP instead of writing JSON files. Create the `livos_openui_apps` Postgres table + `openclawos.apps.*` tRPC namespace + OpenUIAppsRepository. Validate OpenUI markup against the existing 14-component whitelist (Phase 202-08) BEFORE persistence to mitigate T-203-03.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-09, D-203-13, T-203-03, T-203-07)
@livos/packages/liv-ai-app/src/lib/openui/openui-components.tsx — existing 14-component whitelist (Phase 202-08); REUSE the validator helper, do not re-derive
@livos/packages/livinityd/source/db/schema.ts — Drizzle pattern (livos_agents reference)
@livos/packages/livinityd/source/modules/server/trpc/index.ts — tRPC composition pattern; mount openclawos.* alongside mcp.* + agents.*
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle migration + schema binding for livos_openui_apps</name>
  <action>
    Create `livos/packages/livinityd/source/db/migrations/0003_livos_openui_apps.sql`:
    ```sql
    CREATE TABLE IF NOT EXISTS livos_openui_apps (
      slug TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      content TEXT NOT NULL,
      version INT NOT NULL DEFAULT 1,
      user_id TEXT,
      created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
      updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );
    CREATE INDEX IF NOT EXISTS livos_openui_apps_user_idx ON livos_openui_apps(user_id);
    ```
    Extend `livos/packages/livinityd/source/db/schema.ts` with the corresponding `pgTable('livos_openui_apps', {...})` definition + `LivosOpenuiApp` + `LivosOpenuiAppInsert` types (mirror the `livosAgents` pattern from Plan 202-01).

    Wire migration into the existing `runLivOSMigrations` runner (Plan 202-01 helper).
  </action>
  <verify>
    <automated>test -f livos/packages/livinityd/source/db/migrations/0003_livos_openui_apps.sql && grep -c "livos_openui_apps" livos/packages/livinityd/source/db/schema.ts</automated>
  </verify>
  <done>Migration + schema binding present; types exported</done>
</task>

<task type="auto">
  <name>Task 2: OpenUIAppsRepository</name>
  <action>
    Create `livos/packages/livinityd/source/modules/openclawos/openui-apps-repository.ts` with `listAll() / getBySlug(slug) / upsert(input) / delete(slug) / incrementVersion(slug)`. Mirror the `AgentRepository` pattern from Plan 202-01 — same `NodePgDatabase` injection, same `eq` import. On `upsert`, INCREMENT `version` when slug exists, set `updated_at = now()`.
  </action>
  <verify>
    <automated>test -f livos/packages/livinityd/source/modules/openclawos/openui-apps-repository.ts && grep -c "export class OpenUIAppsRepository" livos/packages/livinityd/source/modules/openclawos/openui-apps-repository.ts</automated>
  </verify>
  <done>Repository class compiles; 5 methods exported</done>
</task>

<task type="auto">
  <name>Task 3: tRPC openclawos.apps.* router (with whitelist validation)</name>
  <action>
    Create `livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts` with 6 `adminProcedure` routes:
    - `apps.list` → `repo.listAll()`
    - `apps.get` → `repo.getBySlug(slug)` (NOT_FOUND if missing)
    - `apps.create` → zod-validate input → **validate `content` against the 14-component whitelist** via a shared helper imported from `liv-ai-app` (re-exported as `@livos/openui-validator`) → `repo.upsert()` → return the row. Reject with BAD_REQUEST + `OPENUI_DISALLOWED_COMPONENT` if validation fails (T-203-03).
    - `apps.update` → same validation → upsert + increment version
    - `apps.delete` → `repo.delete(slug)` (also fires desktop-unregister hook — wired in Plan 203-10)
    - `apps.version` → return current version int

    Mount under `openclawos` namespace in `trpc/index.ts` (factory-DI pattern; mirror `agents?` slot from Plan 202-03). Add 6 paths to `httpOnlyPaths` in `trpc/common.ts`.

    The shared 14-component whitelist validator lives at `livos/packages/liv-ai-app/src/lib/openui/openui-components.tsx` (Phase 202-08). To avoid a frontend→backend import, extract `WHITELISTED_COMPONENTS` + `isSafeUrl` + `validateTree(tree)` into a new package `livos/packages/openui-validator/` that BOTH the subapp and livinityd can import. Subapp swaps its inline import for the shared package.
  </action>
  <verify>
    <automated>test -f livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts && grep -c "OPENUI_DISALLOWED_COMPONENT" livos/packages/livinityd/source/modules/server/trpc/openclawos-router.ts</automated>
  </verify>
  <done>Router exposes 6 procedures + whitelist validation on create/update; httpOnlyPaths extended</done>
</task>

<task type="auto">
  <name>Task 4: Clone + rebrand @openuidev/openclaw-os-plugin into liv-claw-plugin</name>
  <action>
    Clone the source of `@openuidev/openclaw-os-plugin@0.1.5` (either via `npm pack` then unpack, or fork the GitHub source if available) into `livos/packages/liv-claw-plugin/`. Same rebrand pattern as Plan 203-02: strings only, no code changes yet. `package.json` name → `@livos/liv-claw-plugin`.
  </action>
  <verify>
    <automated>test -d livos/packages/liv-claw-plugin && test -f livos/packages/liv-claw-plugin/package.json && grep -c "@livos/liv-claw-plugin" livos/packages/liv-claw-plugin/package.json</automated>
  </verify>
  <done>Plugin tree exists, renamed; build smoke clean</done>
</task>

<task type="auto">
  <name>Task 5: Reshape app_create / artifact / db_query tools to call livinityd HTTP</name>
  <action>
    In `livos/packages/liv-claw-plugin/src/tools/`:
    - `app-create.ts` → replace filesystem write with `fetch('http://127.0.0.1:8080/trpc/openclawos.apps.create?batch=1', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Internal-Plugin-Token': process.env.LIV_PLUGIN_TOKEN }, body: JSON.stringify({0:{json:{slug,name,content}}}) })`. Parse the v10/v11 batch envelope.
    - `artifact.ts` → same pattern, calls `openclawos.apps.update`
    - `db_query.ts` → call livinityd `db.queryReadOnly` (existing read-only PG pool guard per T-203-07; if missing, livinityd patch in same plan adds the route)

    Add `LIV_PLUGIN_TOKEN` to livinityd `.env` schema (random 32-byte hex; validated on every plugin RPC).
  </action>
  <verify>
    <automated>grep -l "127.0.0.1:8080/trpc/openclawos" livos/packages/liv-claw-plugin/src/tools/app-create.ts</automated>
  </verify>
  <done>All 3 tools call livinityd HTTP, not filesystem</done>
</task>

<task type="auto">
  <name>Task 6: livinityd boot wire-up + unit + commit</name>
  <action>
    In `livos/packages/livinityd/source/index.ts`, instantiate `OpenUIAppsRepository` after `agentRepoForRouter`, pass into `createAppRouter({ openclawosApps: ... })`. Add 5+ unit tests for the repository under `openui-apps-repository.test.ts` (upsert idempotency, version increment, whitelist rejection via the shared validator, delete cascade-safe, listAll ordering by `updated_at desc`).

    Commit: `feat(203-04): livos_openui_apps table + openclawos.apps.* tRPC + liv-claw-plugin tools call livinityd`
    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter livinityd test -- openui-apps-repository</automated>
  </verify>
  <done>5+ tests PASS; boot wire-up in place; commit lands; sacred SHA preserved</done>
</task>

</tasks>

<verification>
- `livos_openui_apps` table + repo + tRPC namespace present
- Plugin tools call livinityd HTTP, not filesystem
- 14-component whitelist validation enforced at the boundary (T-203-03)
- Shared `openui-validator` package consumed by both subapp + livinityd (no duplicate truth)
- 5+ unit tests PASS
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-04-SUMMARY.md`
</output>
