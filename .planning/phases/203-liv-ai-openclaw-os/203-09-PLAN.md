---
phase: 203-liv-ai-openclaw-os
plan: 09
type: execute
wave: 3
depends_on: [203-08]
files_modified:
  - livos/packages/liv-ai-app/package.json (REMOVE @assistant-ui/* deps)
  - livos/packages/liv-ai-app/app/assistant.tsx (DELETE)
  - livos/packages/liv-ai-app/app/page.tsx (REWRITE — minimal redirect or removal)
  - livos/packages/liv-ai-app/components/assistant-ui/** (DELETE)
  - livos/packages/liv-ai-app/components/thread-list.tsx (DELETE)
  - livos/packages/liv-ai-app/components/tool-fallback.tsx (DELETE)
  - livos/packages/liv-ai-app/components/tool-group.tsx (DELETE)
  - livos/packages/liv-ai-app/components/markdown-text.tsx (DELETE)
  - livos/packages/liv-ai-app/components/reasoning.tsx (DELETE)
  - livos/packages/liv-ai-app/components/attachment.tsx (DELETE)
  - livos/packages/liv-ai-app/src/lib/tool-ui/** (DELETE)
  - livos/packages/liv-ai-app/src/lib/openui/openui-renderer.tsx (DELETE — superseded by shared openui-validator from Plan 203-04)
  - livos/packages/liv-ai-app/src/lib/openui/openui-components.tsx (DELETE — moved to shared openui-validator package in Plan 203-04)
  - livos/packages/liv-ai-app/components/threadlist-sidebar.tsx (DELETE — replaced by AgentsSidebar from Phase 202-04)
  - livos/packages/liv-ai-app/components/livinity-logo.tsx (KEEP — reused by AgentsSidebar)
autonomous: true
requirements: [REQ-203-03]
tags: [purge, assistant-ui-removal, wave-3]
must_haves:
  truths:
    - "Zero @assistant-ui/* packages in liv-ai-app/package.json"
    - "Zero references to @assistant-ui in any .ts/.tsx in liv-ai-app"
    - "Phase 202 routes /agents, /agents/[id], /agents/new, /settings still build + render"
    - "Subapp Next.js build PASS (`pnpm --filter liv-ai-app build` exits 0)"
    - "Sacred SHA preserved"
  artifacts:
    - path: "livos/packages/liv-ai-app/package.json"
      provides: "Subapp manifest with @assistant-ui/* removed; Phase 202 deps preserved (zod, radix-ui, etc.)"
---

<objective>
Purge assistant-ui from the Next.js subapp. Delete the chat surface (`app/assistant.tsx`, thread/composer/tool-renderers, openui-renderer). The chat experience now lives entirely inside the openclaw gateway at `/liv-ai-app/*`. Phase 202 routes (`/agents`, `/agents/[id]`, `/agents/new`, `/settings`) KEEP working — they import shadcn primitives, native fetch, and tRPC only, no assistant-ui.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-08, INV-203-09)
@livos/packages/liv-ai-app/app/ — current route tree; only /agents, /agents/[id], /agents/new, /settings survive
@livos/packages/liv-ai-app/components/agents/ — Phase 202-04..09 components — KEEP all
@livos/packages/liv-ai-app/components/settings/ — Phase 202-07 components — KEEP all
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory which files import assistant-ui</name>
  <action>
    Grep `livos/packages/liv-ai-app` for `@assistant-ui` imports + the deleted file list above. Confirm:
    - 0 imports from Phase 202 surfaces (`components/agents/**`, `components/settings/**`, `app/agents/**`, `app/settings/**`)
    - All imports come from the doomed file list

    If a Phase 202 component imports something we're deleting, STOP and report — that's a contract violation we need to fix first.
  </action>
  <verify>
    <automated>cd livos/packages/liv-ai-app && (grep -rn "@assistant-ui" app/agents app/settings components/agents components/settings 2>NUL | wc -l) -eq 0</automated>
  </verify>
  <done>Zero assistant-ui imports from Phase 202 surfaces; safe to proceed</done>
</task>

<task type="auto">
  <name>Task 2: Delete chat surface files</name>
  <action>
    `git rm` (preserves history):
    - `app/assistant.tsx`
    - `components/assistant-ui/**` (full subtree)
    - `components/thread-list.tsx`
    - `components/tool-fallback.tsx`
    - `components/tool-group.tsx`
    - `components/markdown-text.tsx`
    - `components/reasoning.tsx`
    - `components/attachment.tsx`
    - `components/threadlist-sidebar.tsx` (replaced by AgentsSidebar everywhere per Phase 202-04 + 202-07)
    - `src/lib/tool-ui/**` (full subtree)
    - `src/lib/openui/openui-renderer.tsx` (in-repo OpenUI renderer; superseded by shared `openui-validator` from Plan 203-04 + the route Plan 203-11 builds)
    - `src/lib/openui/openui-components.tsx` (moved to shared `openui-validator` in Plan 203-04)

    KEEP:
    - `components/livinity-logo.tsx` (used by AgentsSidebar header)
    - `components/agents/**` (all Phase 202 surfaces)
    - `components/settings/**` (all Phase 202 surfaces)
    - `components/ui/**` (shadcn primitives shared by Phase 202 surfaces)
  </action>
  <verify>
    <automated>! test -f livos/packages/liv-ai-app/app/assistant.tsx && ! test -d livos/packages/liv-ai-app/components/assistant-ui && ! test -d livos/packages/liv-ai-app/src/lib/tool-ui</automated>
  </verify>
  <done>All chat-surface files deleted; Phase 202 components intact</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite app/page.tsx as a stub redirect</name>
  <action>
    The root route `/liv-ai-app/` previously hosted the chat surface. Now that the openclaw gateway owns chat at the same path, we have a routing conflict — the openclaw gateway serves `/liv-ai-app/` directly per Caddy handle from Plan 203-05.

    Therefore the subapp's `app/page.tsx` should either:
    - (a) Be deleted entirely, OR
    - (b) Become an internal-only redirect to `/agents` for the fallback case when someone hits the subapp directly (e.g. `:3010` port-direct in dev).

    Choose (b): rewrite `app/page.tsx` as `import { redirect } from 'next/navigation'; export default function Home() { redirect('/agents') }`. Layout.tsx loses the AssistantRuntimeProvider wrap.
  </action>
  <verify>
    <automated>grep -c "redirect" livos/packages/liv-ai-app/app/page.tsx</automated>
  </verify>
  <done>page.tsx is a stub redirect; layout.tsx no longer wraps AssistantRuntimeProvider</done>
</task>

<task type="auto">
  <name>Task 4: Remove @assistant-ui/* deps + reinstall</name>
  <action>
    Edit `livos/packages/liv-ai-app/package.json`, remove:
    - `@assistant-ui/react`
    - `@assistant-ui/react-ai-sdk`
    - `@assistant-ui/react-markdown`
    - `assistant-stream` (transitive but listed)
    - `@ai-sdk/openai`, `@ai-sdk/anthropic` if listed (subapp no longer dispatches LLM calls)

    `pnpm install` from workspace root. Confirm 0 `@assistant-ui` in subapp lockfile entries.
  </action>
  <verify>
    <automated>cd livos && pnpm --filter liv-ai-app list 2>&1 | findstr "@assistant-ui"</automated>
  </verify>
  <done>0 @assistant-ui/* packages installed</done>
</task>

<task type="auto">
  <name>Task 5: Build PASS + Phase 202 routes still render</name>
  <action>
    `cd livos && pnpm --filter liv-ai-app build` MUST exit 0. Route manifest MUST contain `/agents`, `/agents/[id]`, `/agents/new`, `/settings` (no `/`'s assistant surface entry, no /agents/_chat etc).

    Inspect generated Next.js route table in stdout — count must be exactly 5 (`/`, `/_not-found`, `/agents`, `/agents/[id]`, `/agents/new`, `/settings`).
  </action>
  <verify>
    <automated>cd livos && pnpm --filter liv-ai-app build 2>&1 | findstr /R "○ /agents\|○ /settings\|ƒ /agents/\[id\]"</automated>
  </verify>
  <done>Build exits 0; route manifest matches expected 5 routes</done>
</task>

<task type="auto">
  <name>Task 6: Commit</name>
  <action>
    Commit: `refactor(203-09): purge @assistant-ui/* + chat surface; keep Phase 202 /agents + /settings`
    Sacred SHA hook MUST PASS.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"203-09"</automated>
  </verify>
  <done>Commit lands; sacred SHA preserved</done>
</task>

</tasks>

<verification>
- 0 @assistant-ui/* packages in liv-ai-app
- 12+ files deleted, 0 broken imports
- Phase 202 routes still build + render
- Subapp Next.js build PASS
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-09-SUMMARY.md`
</output>
