---
phase: 203-liv-ai-openclaw-os
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md (NEW)
autonomous: true
requirements: [REQ-203-01]
tags: [spike, research, decision-matrix, wave-1]
must_haves:
  truths:
    - "openclaw-os repo cloned locally and built clean on Windows"
    - "`openclaw` gateway boots on a random local port and serves the claw-client"
    - "LLM dispatch behaviour verified — openclaw confirmed self-dispatching per D-203-06 Branch A lock"
    - "Provider config shape + persistence semantics documented for Plan 203-07 consumption"
  artifacts:
    - path: ".planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md"
      provides: "L0 spike findings + Branch A confirmation + Plan 203-07 setup notes"
      min_lines: 60
---

<objective>
L0 SPIKE — clone `thesysdev/openclaw-os` locally, install `openclaw@^2026.5.20` from npm, boot the gateway, and verify openclaw's built-in LLM dispatch behaviour (operator pre-committed D-203-06 to Branch A on 2026-05-23). Document the provider config shape, env vars, persistence semantics, and tool-registration API so Plan 203-07 has zero discovery left. ESCALATE TO OPERATOR if the spike discovers openclaw does NOT self-dispatch — do not silently flip to Branch B.
</objective>

<context>
@.planning/phases/203-liv-ai-openclaw-os/203-CONTEXT.md (D-203-01, D-203-06)
@livos/packages/livinityd/source/modules/mastra/index.ts — current LivOSMastra class shape; the new LivOSAgent must replicate `attach*` surface
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone openclaw-os to a scratch dir</name>
  <action>
    Clone outside the repo:
    ```
    git clone https://github.com/thesysdev/openclaw-os C:/tmp/openclaw-os-spike
    cd C:/tmp/openclaw-os-spike
    git rev-parse HEAD > C:/tmp/openclaw-spike-head.txt
    ```
    Record the cloned HEAD SHA — Plan 203-02 will re-clone at the same SHA for the in-tree copy.
  </action>
  <verify>
    <automated>test -f C:/tmp/openclaw-spike-head.txt && wc -l C:/tmp/openclaw-spike-head.txt</automated>
  </verify>
  <done>HEAD SHA recorded; openclaw-os tree present at C:/tmp/openclaw-os-spike</done>
</task>

<task type="auto">
  <name>Task 2: Install openclaw gateway + dependencies</name>
  <action>
    Inside C:/tmp/openclaw-os-spike, run the install pattern documented in upstream README (likely `pnpm install` or `npm install`). Then `npm i openclaw@^2026.5.20` (the gateway package) in a sibling scratch dir if not already a workspace dep. Capture exact install commands + exit codes in the spike notes.
  </action>
  <verify>
    <automated>cd C:/tmp/openclaw-os-spike && (test -d node_modules || test -d packages/*/node_modules)</automated>
  </verify>
  <done>Install completes without error; `openclaw` binary or library importable</done>
</task>

<task type="auto">
  <name>Task 3: Boot the gateway + observe LLM dispatch</name>
  <action>
    Boot the gateway on a random local port (`PORT=18790 openclaw start` or upstream equivalent). Hit the chat surface from a browser. Send a trivial prompt. Capture in the spike notes:

    1. Does `openclaw` accept an `OPENAI_API_KEY` (or anthropic equiv) env var and dispatch LLM calls itself? **→ Branch A**
    2. Does `openclaw` POST tool calls / chat completion requests to a backend URL that the operator configures? **→ Branch B**
    3. What is the exact env var / config surface for LLM dispatch (model name, base URL, key)?
    4. What is the WebSocket handshake shape between gateway and claw-client (Ed25519 key location, token format)?
    5. Where does `app_create` write OpenUI app content by default (filesystem path, HTTP endpoint)?

    These five answers go in the decision matrix.
  </action>
  <verify>
    <automated>curl -s http://localhost:18790/health 2>NUL | findstr /R "ok\|ready\|healthy"</automated>
  </verify>
  <done>Gateway booted; all 5 questions answered with primary-source quotes (commit SHAs, file paths, code snippets)</done>
</task>

<task type="auto">
  <name>Task 4: Write the decision matrix to 203-01-SPIKE.md</name>
  <action>
    Create `.planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md` with the following structure:

    ```markdown
    # Phase 203 — L0 Spike Findings (D-203-06 decision)

    ## Cloned HEAD SHA
    <SHA from Task 1>

    ## Install commands used
    <verbatim shell history>

    ## Gateway boot
    Port: 18790, command: <verbatim>, exit code: 0

    ## LLM Dispatch Observation
    Branch chosen: **A** (built-in dispatch) | **B** (delegated dispatch)

    ## Answers to 5 questions
    1. ...
    2. ...
    3. ...
    4. ...
    5. ...

    ## Decision Matrix for Plan 203-07

    | Subsystem | Branch A wiring | Branch B wiring |
    |-----------|-----------------|-----------------|
    | LLM call surface | openclaw gateway env vars | livinityd LangGraph.js node |
    | Memory persistence | openclaw built-in | LangGraphCheckpointPostgres |
    | Tool registry | openclaw plugin tools (Plan 203-06) | openclaw plugin tools + LangGraph ToolNode |
    | LivOSAgent shape | thin wrapper around openclaw client | LangGraph StateGraph compile() |
    | Mastra removal blast radius | small (replace inner Agent({...})) | larger (LangGraph node graph rewrite) |
    | Days estimate | 3 | 4 |

    ## CHOSEN BRANCH: <A or B>

    ## Implications for downstream plans
    - 203-04: tools must call livinityd HTTP regardless of branch (unchanged)
    - 203-07: implement branch <X> exactly as written above
    - 203-08: purge @mastra/* (both branches)
    - 203-13: UAT step 10 (`journalctl | grep mastra = 0`) unchanged
    ```

    Commit message: `spike(203-01): clone openclaw-os, boot gateway, decide branch <X>`
  </action>
  <verify>
    <automated>test -f .planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md && wc -l .planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md</automated>
  </verify>
  <done>Spike file exists with all 6 sections filled; chosen branch is unambiguous</done>
</task>

<task type="auto">
  <name>Task 5: Commit + sacred SHA verify</name>
  <action>
    `git add .planning/phases/203-liv-ai-openclaw-os/203-01-SPIKE.md` + commit. The sacred SHA hook MUST PASS (`[sacred-sha] PASS: 20 files verified`).
  </action>
  <verify>
    <automated>git log -1 --format="%s" | findstr /C:"spike(203-01)"</automated>
  </verify>
  <done>Commit lands cleanly; sacred SHA hook passes</done>
</task>

</tasks>

<verification>
- openclaw-os cloned + built locally
- Gateway booted, LLM dispatch behaviour observed
- 203-01-SPIKE.md filled with decision matrix
- Chosen branch (A or B) locked for Plan 203-07
- Sacred SHA preserved
</verification>

<output>
Path: `.planning/phases/203-liv-ai-openclaw-os/203-01-SUMMARY.md` (filed after spike file is committed)
</output>
