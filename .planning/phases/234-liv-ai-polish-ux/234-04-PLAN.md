---
phase: 234-liv-ai-polish-ux
plan: 04
type: execute
wave: 4
depends_on: [234-01, 234-02, 234-03]
files_modified:
  # ============================================================
  # STRATEGY TBD -- this list is BEST-EFFORT pending 234-01-INVESTIGATION.md
  # Section H selection. Orchestrator MUST rewrite this list to the option-
  # specific shape BEFORE Plan 04 executes. Best-effort placeholders below
  # cover the three plausible option shapes (A / B / C) -- a real run will
  # only need a subset.
  # ============================================================
  - scripts/install-liv-assistant.sh                                  # Option A: config flag write step
  - livos/packages/livinityd/source/modules/server/trpc/config-router.ts  # Option B: admin-only tRPC procedure
  - livos/packages/livinityd/source/modules/server/trpc/common.ts     # Option B: httpOnlyPaths entry for the new procedure
  - livos/packages/ui/src/hooks/use-liv-assistant-session.ts          # Option B: frontend hook
  - livos/packages/ui/src/modules/window/app-contents/liv-assistant-window.tsx  # Option B: useEffect cookie/localStorage injection
  - livos/packages/ui/src/modules/window/app-contents/liv-assistant-window.unit.test.tsx  # Option B: hook + injection assertions
  - livos/packages/livinityd/source/modules/domain/caddy.ts           # Option C: header_up Cookie directive
  - livos/packages/livinityd/source/modules/domain/caddy.test.ts      # Option C: header_up assertion
  - scripts/install/systemd/liv-ai-session-refresh.timer              # Option C: refresh cron
  - .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SC-06, SC-07, SC-08]
tags: [v42, polish, auth-bypass, mini-pc, feature-flag, redis, STRATEGY-TBD, checkpoint]

must_haves:
  truths:
    - "STRATEGY TBD -- the truth list below is the OPTION-AGNOSTIC envelope. Orchestrator rewrites these per 234-01-INVESTIGATION.md Section H selected option before execution."
    - "Operator opening the Liv AI window from the LivOS dock lands DIRECTLY on the chat surface -- no AionUi login form is visible (SC-06)"
    - "Feature flag `liv:config:liv_ai_autologin_enabled` exists in Redis with default value TRUE; flipping to FALSE restores upstream AionUi login UX within ~30s (D-LIVAI-AUTOLOGIN-ROLLBACK pattern matching D-V42-ROLLBACK from Phase 224)"
    - "Auto-login mechanism is admin-scoped where applicable (Option B): only ctx.currentUser.role === 'admin' can mint Liv AI sessions; non-admin LivOS users still see the AionUi login form"
    - "Auto-login does NOT log AionUi credentials to disk or transmit them over the network outside the Mini PC localhost loop (security invariant)"
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED across all commits + matches Mini PC sha256 `62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe`"
    - "Phase 233 UAT subset still GREEN post-Plan-04 deploy (Plan 04 does NOT regress iframe routing, auth status endpoints, or basic Liv AI functionality)"
    - "STATE.md + ROADMAP.md flipped to Phase 234 COMPLETE 4/4 plans"
    - "Operator UAT auto-approved per `workflow._auto_chain_active=true` chain protocol (matches all prior v42 deploy precedent)"
  artifacts:
    - path: ".planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md"
      provides: "Full deploy log with auth-bypass evidence (Option-specific) + feature-flag toggle proof + Phase 233 UAT subset re-run"
      contains: "liv:config:liv_ai_autologin_enabled"
    - path: ".planning/STATE.md"
      provides: "Current Position flipped to Phase 234 COMPLETE 4/4 plans"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 234 status READY -> SHIPPED with all 8 SCs GREEN"
  key_links:
    - from: "234-01-INVESTIGATION.md Section H 'Selected option'"
      to: "Plan 234-04 <action> + <verify> blocks below"
      via: "ORCHESTRATOR REWRITE -- Section I 'Plan 234-04 spec' text becomes Task 1's action verbatim"
      pattern: "Marker `STRATEGY TBD -- will be replaced after 234-01 ships` rewritten to the option-specific implementation"
    - from: "Redis key `liv:config:liv_ai_autologin_enabled`"
      to: "Plan 04's auth-bypass code path"
      via: "Pattern matching D-V42-ROLLBACK (Phase 224 `useV42MigrationActive` hook precedent)"
      pattern: "Setting key to 'false' disables the bypass; deleting OR setting to any other value enables it"
---

<objective>
Phase 234 Plan 04 -- **AUTH BYPASS** (STRATEGY locked by 234-01-INVESTIGATION.md Section H).

Eliminate the AionUi login form for LivOS operators opening the Liv AI window. The exact implementation is one of three options (A: config flag, B: tRPC mint + iframe injection, C: Caddy header_up Cookie) -- selected by 234-01-INVESTIGATION.md Section H based on evidence gathered from the Mini PC AionUi binary. All three options share the same observable outcome (no login form visible, feature-flag-gated rollback via Redis `liv:config:liv_ai_autologin_enabled`).

Purpose: Operator directive 2026-05-27 night requires the Liv AI window to feel native. The login form is the last visible AionUi-side UX seam. This plan closes it.

**Output (STRATEGY TBD -- orchestrator rewrites Task 1 below per Section H selection):**
- 1-2 source edits matching the selected option's file set (see frontmatter `files_modified` for option-by-option enumeration)
- Redis feature flag set on Mini PC (default TRUE)
- 1 deploy checkpoint (auto-approved per chain)
- 1 docs commit flipping STATE.md + ROADMAP.md
- Sacred SHA UNCHANGED

----

# IMPORTANT -- STRATEGY TBD NOTICE

The `<action>` block below is a **PLACEHOLDER** authored before 234-01-INVESTIGATION.md ships. Its content covers the 3 candidate options at SKETCH level so the orchestrator knows what to expect.

**BEFORE THIS PLAN EXECUTES:** the orchestrator MUST:

1. Read `.planning/phases/234-liv-ai-polish-ux/234-01-INVESTIGATION.md` Section H "Selected option" + Section I "Plan 234-04 spec".
2. Replace the entire `<action>` block in Task 1 below with the option-specific implementation text from Section I.
3. Replace the `<verify>` automated check with the option-specific verification command.
4. Trim the `files_modified:` frontmatter list to only the files the selected option actually touches.

The current placeholder marker is `STRATEGY TBD -- will be replaced after 234-01 ships`. If this marker is still present when Plan 04 is dispatched to an executor, the executor MUST refuse to run and instead surface a planning-rewrite request to the orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/234-liv-ai-polish-ux/234-01-INVESTIGATION.md
@.planning/phases/234-liv-ai-polish-ux/234-02-PLAN.md
@.planning/phases/234-liv-ai-polish-ux/234-03-PLAN.md
@.planning/phases/224-v42-feature-flag/224-01-SUMMARY.md
@.planning/phases/223-vendor-aionui-install/223-03-PLAN.md
@.planning/phases/228-claude-auth-bridge/228-02-DEPLOY-LOG.md

@livos/packages/ui/src/modules/window/app-contents/liv-assistant-window.tsx
@livos/packages/ui/src/hooks/use-v42-migration-active.ts
@scripts/install-liv-assistant.sh

<interfaces>
<!--
STRATEGY TBD -- orchestrator rewrites this <interfaces> block per Section H selection BEFORE executor reads it.

Below is the OPTION-AGNOSTIC envelope -- shared infrastructure references all three options will need:

Mini PC SSH (HARD RULE 2026-04-27):
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68

Redis password (URL-decoded from /opt/livos/.env REDIS_URL):
sudo grep REDIS_URL /opt/livos/.env  # extract + URL-decode %21 -> !
redis-cli -a "$PASS" ...

Feature flag pattern (Phase 224 D-V42-ROLLBACK precedent):
- Redis key: liv:config:liv_ai_autologin_enabled
- Default: missing OR any non-'false' value -> ENABLED
- Disabled: SET liv:config:liv_ai_autologin_enabled false
- Pattern: useV42MigrationActive hook in livos/packages/ui/src/hooks/use-v42-migration-active.ts -- mirror this for Option B if frontend gate needed

Phase 224-01 useV42MigrationActive ref:
- 30s staleTime
- ENABLED-by-default semantics (missing key = TRUE)
- tRPC `config.getFeatureFlag` query backing it

Liv AI credentials location (Phase 223-03 D-V42-CRED-LOCATION):
- /etc/liv-assistant/credentials -- root-readable plaintext password file
- One line, no trailing whitespace
- Created on first install by install-liv-assistant.sh

AionUi login endpoint (Phase 228-02 discovery):
- POST http://127.0.0.1:3020/api/login (Content-Type: application/json, body: {"password": "..."})
- Response shape and Set-Cookie format TBD pending 234-01 Section A.6 probe results

Sandbox tokens (liv-assistant-window.tsx LIV_ASSISTANT_SANDBOX const):
- 'allow-same-origin allow-scripts allow-forms allow-popups allow-downloads'
- allow-same-origin + same-origin URL = parent can write iframe.contentWindow.document.cookie / localStorage (Section C verification in 234-01)

Sacred SHA:
- Repo: f3538e1d811992b782a9bb057d1b7f0a0189f95f
- Mini PC sha256: 62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement selected auth-bypass option -- STRATEGY TBD until 234-01 ships</name>
  <files>
    STRATEGY TBD -- orchestrator rewrites this file list per 234-01-INVESTIGATION.md Section I Plan 234-04 spec.
    Best-effort placeholder set above in frontmatter (Option A / B / C combined).
  </files>
  <action>
    ## STRATEGY TBD -- will be replaced after 234-01 ships

    Orchestrator: load `.planning/phases/234-liv-ai-polish-ux/234-01-INVESTIGATION.md`, find the `## Section I` -> `### Plan 234-04 spec` block, and replace this entire `<action>` body with that block's text verbatim. Then trim `<files>` above and `files_modified:` in frontmatter to only the files the selected option touches.

    ### Sketch -- Option A (config flag flip)

    If 234-01 Section H selected Option A:

    1. Extend `scripts/install-liv-assistant.sh` with a config-write step (placed AFTER the Plan 03 rebrand block, BEFORE bun-install). Step:
       - Read selected config-file path from `${INSTALL_ROOT}/data/<config_path_from_section_B>` (path determined by 234-01 Section B).
       - Write the auth-disable flag (key/value determined by 234-01 Section A.9 evidence -- e.g. `require_auth: false` or `disable_auth: true`).
       - Idempotency: read the file, only write if the flag is missing OR has a different value.
       - Feature flag gate: only apply if Redis `liv:config:liv_ai_autologin_enabled` != 'false'.
    2. Set Redis flag on Mini PC: `redis-cli -a "$PASS" SET liv:config:liv_ai_autologin_enabled true` (default ON).
    3. Mini PC deploy: `bash /opt/livos/update.sh` -- re-runs install-liv-assistant.sh, applies config write.
    4. Verify: external `curl https://bruce.livinity.io/liv/` -> HTML body does NOT contain the AionUi login form markup.
    5. Reversibility test: `redis-cli -a "$PASS" SET liv:config:liv_ai_autologin_enabled false`; next `bash /opt/livos/update.sh` run skips the config write; manually revert config file or wait for next install run to restore login form.

    ### Sketch -- Option B (tRPC mint + iframe injection)

    If 234-01 Section H selected Option B:

    1. **Backend tRPC procedure** -- new file `livos/packages/livinityd/source/modules/server/trpc/config-router.ts` (or extend existing config-router):
       ```typescript
       getLivAssistantSession: adminProcedure  // gated on ctx.currentUser.role === 'admin'
         .query(async ({ctx}) => {
           // Honor feature flag
           const enabled = await ctx.redis.get('liv:config:liv_ai_autologin_enabled') !== 'false'
           if (!enabled) return { enabled: false, cookie: null }

           // Cache-aside in Redis (1h TTL)
           const cacheKey = `liv:session:liv_ai_${ctx.currentUser.id}`
           const cached = await ctx.redis.get(cacheKey)
           if (cached) return { enabled: true, cookie: cached }

           // Read AionUi password
           const pw = (await fs.readFile('/etc/liv-assistant/credentials', 'utf8')).trim()

           // POST to AionUi /api/login
           const res = await fetch('http://127.0.0.1:3020/api/login', {
             method: 'POST',
             headers: {'Content-Type': 'application/json'},
             body: JSON.stringify({password: pw}),
           })
           const setCookie = res.headers.get('set-cookie')
           if (!setCookie) throw new TRPCError({code: 'INTERNAL_SERVER_ERROR', message: 'no Set-Cookie from AionUi login'})

           await ctx.redis.setex(cacheKey, 3600, setCookie)
           return { enabled: true, cookie: setCookie }
         })
       ```
    2. **httpOnlyPaths entry** in common.ts: `'config.getLivAssistantSession'`.
    3. **Frontend hook** `livos/packages/ui/src/hooks/use-liv-assistant-session.ts`:
       ```typescript
       export function useLivAssistantSession() {
         return trpc.config.getLivAssistantSession.useQuery(undefined, {
           refetchInterval: 3600_000,  // re-mint hourly
           retry: false,
         })
       }
       ```
    4. **Iframe wrapper** `liv-assistant-window.tsx` extends with:
       ```typescript
       const {data} = useLivAssistantSession()
       const iframeRef = useRef<HTMLIFrameElement>(null)
       useEffect(() => {
         if (!data?.enabled || !data.cookie || !iframeRef.current) return
         const iframe = iframeRef.current
         const onLoad = () => {
           // Same-origin write (allow-same-origin sandbox)
           try {
             iframe.contentWindow?.document.cookie = data.cookie ?? ''
             iframe.contentWindow?.location.reload()
           } catch (e) { console.warn('liv-ai auto-login inject failed', e) }
         }
         iframe.addEventListener('load', onLoad)
         return () => iframe.removeEventListener('load', onLoad)
       }, [data])
       ```
       Add `ref={iframeRef}` to the existing `<iframe ... />`.
    5. **Vitest** update for liv-assistant-window.unit.test.tsx: mock useLivAssistantSession; assert iframe.contentWindow.document.cookie write fires after load event with the mocked cookie value.
    6. **Mini PC deploy**: push + `bash /opt/livos/update.sh` rebuilds UI + livinityd; `redis-cli SET liv:config:liv_ai_autologin_enabled true`; verify external `curl /liv/` then operator-browser-walks the dock -> Liv AI window: no login form visible.

    ### Sketch -- Option C (Caddy header_up Cookie)

    If 234-01 Section H selected Option C:

    1. `livos/packages/livinityd/source/modules/domain/caddy.ts` -- extend LIV_ASSISTANT_HANDLE to include `header_up Cookie "session={CACHED_COOKIE}"` directive. CACHED_COOKIE comes from a file at `/etc/liv-assistant/session-cookie` written by a refresh cron.
    2. `scripts/install/systemd/liv-ai-session-refresh.{service,timer}` -- hourly cron that POSTs to /api/login, captures Set-Cookie, writes to `/etc/liv-assistant/session-cookie`.
    3. `livos/packages/livinityd/source/modules/domain/caddy.test.ts` -- assert the LIV_ASSISTANT_HANDLE includes `header_up Cookie` directive.
    4. Mini PC deploy + cron-enable + external verify.

    ### Feature-flag default

    All three options share the Redis key `liv:config:liv_ai_autologin_enabled`. Default state (unset OR any value != 'false') = ENABLED (the bypass active). Operator override:
    ```bash
    # Disable auto-login (operator sees AionUi login form on next page load):
    PASS=$(sudo grep REDIS_URL /opt/livos/.env | sed -E 's|.*://:?([^@]+)@.*|\1|' | sed 's/%21/!/g')
    redis-cli -a "$PASS" SET liv:config:liv_ai_autologin_enabled false

    # Re-enable:
    redis-cli -a "$PASS" DEL liv:config:liv_ai_autologin_enabled
    ```

    ### Verification (option-agnostic envelope)

    After deploy:
    1. External `curl -sS https://bruce.livinity.io/liv/ -L --max-time 15 -o /tmp/livhtml.html`.
    2. Login-form fingerprint check: `grep -c 'password\|<input.*type=.*password' /tmp/livhtml.html` -- expected count depends on Option (A: HTML literally no longer includes login form; B/C: HTML may still include login form markup but operator never sees it because the cookie injection on load bypasses it).
    3. Operator walks LivOS dock -> Liv AI tile -> window opens directly on chat surface, NO password prompt.

    ### Atomic commit shape

    1 feat commit (source edits, option-specific message) + 1 docs commit (DEPLOY-LOG + STATE/ROADMAP, identical to Plan 03's Task 4 pattern). Sacred SHA gated on each via pre-commit hook.
  </action>
  <verify>
    <!-- STRATEGY TBD -- orchestrator rewrites this verify with the option-specific check.
         Placeholder asserts the option-agnostic outcomes: feature flag set, Phase 233 UAT subset green, sacred SHA pinned. -->
    <automated>STRATEGY_TBD_GATE=$(grep -c 'STRATEGY TBD -- will be replaced after 234-01 ships' .planning/phases/234-liv-ai-polish-ux/234-04-PLAN.md); test "${STRATEGY_TBD_GATE}" = "0" &amp;&amp; test -f .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md &amp;&amp; grep -q 'liv:config:liv_ai_autologin_enabled' .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    STRATEGY TBD marker removed from PLAN.md (orchestrator rewrote per Section H). Option-specific source edits + deploy + verification complete. DEPLOY-LOG.md documents the chosen-option evidence. Feature flag set on Mini PC. Phase 233 UAT subset GREEN. Sacred SHA UNCHANGED. Atomic commits on master pushed to origin.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT verdict on auth bypass (auto-approved per chain protocol)</name>
  <files>.planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md (operator verdict block appended)</files>
  <action>
    Present the auth-bypass outcome to the operator.

    What was built (automated work to verify):
    - 234-01 Section H selected Option {A | B | C}; implementation deployed to Mini PC.
    - Redis flag `liv:config:liv_ai_autologin_enabled` set TRUE; bypass active.
    - External `https://bruce.livinity.io/liv/` -- operator opening the Liv AI window from the LivOS dock lands directly on the chat surface; no AionUi login form is visible.
    - Phase 233 UAT subset 5/5 GREEN (no regression).
    - Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged.

    How to verify (OPTIONAL -- chain may auto-approve):
    1. Open https://bruce.livinity.io/ in a clean browser (or incognito).
    2. Click the Liv AI dock tile (new chat icon, 1280x800 window from Plan 02).
    3. Confirm the iframe contents are the chat UI directly -- no password input field, no "Sign in" button.
    4. (Optional) Send a chat message to confirm Claude Code agent (Phase 228) responds.
    5. (Reversibility test, optional) `ssh` to Mini PC and `redis-cli -a "$PASS" SET liv:config:liv_ai_autologin_enabled false`; refresh the Liv AI window in browser; confirm the AionUi login form NOW appears (proves the flag works); revert with `DEL` of the key.

    Resume signal: Type "approved" or describe issues. Auto-approve per chain protocol if `workflow._auto_chain_active=true`.

    After approval (or auto-approval), append to DEPLOY-LOG.md:
    > ## Operator verdict (Task 2 checkpoint)
    >
    > [approved | auto-approved per chain protocol] at <ISO timestamp>. Rationale: <one-line>.
  </action>
  <verify>
    <automated>grep -q '^## Operator verdict' .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md &amp;&amp; grep -qE 'approved|auto-approved' .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md</automated>
  </verify>
  <done>Operator approval logged (or auto-approval per chain protocol). DEPLOY-LOG.md records the verdict + rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize DEPLOY-LOG SC verdict + flip STATE.md + ROADMAP.md to Phase 234 COMPLETE + atomic docs commit</name>
  <files>
    .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    1. **Append SC verdict table to DEPLOY-LOG.md:**
       ```markdown
       ## SC verdict table (Plan 234-04 closure + Phase 234 COMPLETE)

       | SC | Description | Verdict | Evidence |
       |----|-------------|---------|----------|
       | SC-06 | Opening Liv AI window auto-pastes operator to chat screen (NO login form visible) | PASS | External /liv/ HTML probe + operator UAT walk |
       | SC-07 | Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged | PASS | 3-snapshot agreement |
       | SC-08 | Feature-flag `liv:config:liv_ai_autologin_enabled` default TRUE; flip to false restores upstream AionUi UX | PASS | Reversibility test logged in DEPLOY-LOG (or NICE-TO-HAVE deferred to operator) |

       ## Phase 234 overall verdict

       | SC | Description | Verdict | Plan |
       |----|-------------|---------|------|
       | SC-01 | LivAssistantWindow default render size >= 1280x800 | PASS | 234-02 |
       | SC-02 | Dock icon swapped to chat-style icon | PASS | 234-02 |
       | SC-03 | UI-side AionUi strings replaced with Liv AI | PASS | 234-02 |
       | SC-04 | Mini PC /opt/liv-assistant/current/ HTML/JS: AionUi grep = 0 | PASS | 234-03 |
       | SC-05 | External /liv/ HTML body contains Liv AI + lacks AionUi | PASS | 234-03 |
       | SC-06 | Liv AI window opens directly on chat screen (no login form) | PASS | 234-04 |
       | SC-07 | Sacred SHA unchanged | PASS | 234-01..04 all commits |
       | SC-08 | Feature-flag auth bypass via Redis | PASS | 234-04 |
       ```

    2. **STATE.md** -- update Current Position to:
       ```
       ## Current Position (v42 -- Phase 234 ✅ SHIPPED 4/4 plans -- Liv AI polish COMPLETE, sacred SHA UNCHANGED, all 8 SCs GREEN)

       **Plan 234-04 (auth bypass + STATE/ROADMAP commit)** -- 234-01 Section H selected Option {A|B|C}. Implementation deployed to Mini PC via `bash /opt/livos/update.sh`. Redis flag `liv:config:liv_ai_autologin_enabled=true` (default ON; flip to false restores AionUi login). External `https://bruce.livinity.io/liv/` -- operator opening Liv AI window lands directly on chat (no login form). Phase 233 UAT subset 5/5 GREEN (no regression). Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED across 4 plans / N commits. Phase 234 closes 4/4 plans + 8/8 SCs GREEN. **Liv AI polish complete: bigger window (1280x800), chat icon, 'Liv AI' brand everywhere, auto-login.** Next phase: TBD per operator priority.
       ```
       Preserve Previous Position history.

    3. **ROADMAP.md** -- Phase 234 section update:
       - Header: `### Phase 234: Liv AI polish ... -- ✅ SHIPPED <date> (4/4 plans, 8/8 SCs GREEN)`
       - Plan list: mark all 4 plans `[x]` with commit hashes
       - Add "Outcome" paragraph summarizing the Liv AI polish: window enlarged 900x600 -> 1280x800; chat-style icon; 'Liv AI' brand string throughout (wrapper + vendored binary); auto-login removes the AionUi login form; all gated by Redis feature flag for reversibility.

    4. **Atomic combined docs commit:**
       ```bash
       git add .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md .planning/STATE.md .planning/ROADMAP.md
       git commit -m "docs(234-04): DEPLOY-LOG + STATE/ROADMAP -- Phase 234 ✅ SHIPPED (4/4 plans, 8/8 SCs GREEN)"
       git push origin master
       ```

    5. **Final sacred SHA gate:** `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST print `f3538e1d811992b782a9bb057d1b7f0a0189f95f`.
  </action>
  <verify>
    <automated>grep -q '## SC verdict table' .planning/phases/234-liv-ai-polish-ux/234-04-DEPLOY-LOG.md &amp;&amp; grep -q 'Phase 234.*SHIPPED\|Phase 234.*COMPLETE' .planning/STATE.md &amp;&amp; grep -qE 'Phase 234.*SHIPPED.*4/4|234-04.*SHIPPED|\[x\] 234-04' .planning/ROADMAP.md &amp;&amp; git log -1 --format='%s' | grep -q '234-04' &amp;&amp; git hash-object liv/packages/core/src/sdk-agent-runner.ts | grep -q '^f3538e1d811992b782a9bb057d1b7f0a0189f95f$'</automated>
  </verify>
  <done>
    DEPLOY-LOG.md finalized with SC verdict table + Phase 234 overall verdict (8/8 GREEN). STATE.md current position = Phase 234 SHIPPED 4/4 plans. ROADMAP.md Phase 234 flipped to ✅ SHIPPED. Atomic docs commit pushed to origin. Sacred SHA UNCHANGED. Phase 234 COMPLETE.
  </done>
</task>

</tasks>

<verification>
After all tasks complete (assumes orchestrator has rewritten Task 1 per 234-01 Section H):

```bash
# 1. STRATEGY TBD marker absent (proves orchestrator rewrote)
grep -c 'STRATEGY TBD -- will be replaced after 234-01 ships' .planning/phases/234-liv-ai-polish-ux/234-04-PLAN.md
# Expected: 0

# 2. Sacred SHA hard-gate
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# Expected: f3538e1d811992b782a9bb057d1b7f0a0189f95f

# 3. Feature flag set on Mini PC
ssh -i "$SSH_KEY" -T bruce@10.69.31.68 'PASS=$(sudo grep REDIS_URL /opt/livos/.env | sed -E "s|.*://:?([^@]+)@.*|\1|" | sed "s/%21/!/g"); redis-cli -a "$PASS" GET liv:config:liv_ai_autologin_enabled'
# Expected: "true" (or empty = default ON)

# 4. External Liv AI window opens on chat surface (login form absent)
curl -sS https://bruce.livinity.io/liv/ --max-time 15 -o /tmp/livhtml.html
# Manual inspection of /tmp/livhtml.html OR option-specific automated check

# 5. STATE.md + ROADMAP.md flipped
grep -E 'Phase 234.*(SHIPPED|COMPLETE)' .planning/STATE.md .planning/ROADMAP.md
```
</verification>

<success_criteria>
- [ ] **GATE:** `STRATEGY TBD -- will be replaced after 234-01 ships` marker REMOVED from this PLAN.md (proves orchestrator rewrote Task 1 per 234-01 Section H selection)
- [ ] Selected option from 234-01 Section H (A / B / C) implemented per Section I "Plan 234-04 spec"
- [ ] Redis feature flag `liv:config:liv_ai_autologin_enabled` set on Mini PC with default TRUE
- [ ] Mini PC `bash /opt/livos/update.sh` EXIT 0 with auth-bypass code path live
- [ ] External `https://bruce.livinity.io/liv/` -- operator opening Liv AI window from LivOS dock sees chat surface directly (no AionUi login form)
- [ ] Phase 233 UAT subset (SC-01..SC-05) GREEN post-Plan-04 deploy (no regression)
- [ ] Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED on every commit
- [ ] DEPLOY-LOG.md has SC verdict table + Phase 234 overall verdict (8/8 SCs GREEN)
- [ ] STATE.md + ROADMAP.md flipped to Phase 234 ✅ SHIPPED 4/4 plans
- [ ] Operator UAT auto-approved per chain protocol
- [ ] Atomic commits on master pushed to origin
</success_criteria>

<output>
DEPLOY-LOG.md serves as the Plan 04 SUMMARY (matching prior v42 deploy-phase convention).

Phase 234 closure: Liv AI polish complete. The AionUi-backed Liv AI window now feels native to LivOS:
- Bigger window (1280x800, Plan 02)
- Chat-style dock icon (Plan 02)
- 'Liv AI' brand string throughout wrapper (Plan 02) AND vendored binary (Plan 03)
- Auto-login eliminates the AionUi login form (Plan 04)
- All gated by Redis feature flags for reversibility

Sacred SHA invariant `f3538e1d811992b782a9bb057d1b7f0a0189f95f` honored across all 4 plans. Mini PC remains the only deployment target per HARD RULE 2026-04-27. Apache-2.0 LICENSE + NOTICE attribution preserved per D-V42-APACHE-NOTICE.

Resume after /clear: TBD per operator priority. v42 milestone closes with Phase 234 if no further phases are queued.
</output>
