---
phase: 228-claude-auth-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - systemd/liv-assistant.service
  - docs/liv-assistant-install.md
autonomous: true
requirements: [SC-01, SC-02, SC-05, SC-06]
tags: [v42, liv-assistant, systemd, claude-auth, docs, audit, repo-only, mini-pc]
must_haves:
  truths:
    - "`systemd/liv-assistant.service` (Phase 223-02 / committed to repo) is read end-to-end and confirmed to already contain a literal `Environment=\"HOME=/home/bruce\"` line in its `[Service]` section. If present (it IS present per repo audit 2026-05-27), the file is a NO-OP for this plan — touched only if a doc-comment annotation about Phase 228 is added; otherwise left byte-identical."
    - "If by some drift the `HOME=/home/bruce` Environment line is missing, the patch adds it BETWEEN the existing `Environment=\"PATH=...\"` line and the `ExecStart=` line, preserving tab/space style and quoting of the surrounding directives — no other edits."
    - "`docs/liv-assistant-install.md` (Phase 223-04 / 147 lines current) gets a new `## Claude subscription credentials (Phase 228)` section appended BEFORE `## Locked invariants` — documenting (a) credential file path `/home/bruce/.claude/.credentials.json` (Phase 221 capture path), (b) ownership/mode requirements (`bruce:bruce 0600`), (c) recovery steps if creds are lost (re-run Phase 221 login flow via `claude` CLI or LivOS Settings → Claude Auth), (d) why `HOME=/home/bruce` in the systemd unit matters (AionUi's vendored Claude Code agent reads `$HOME/.claude/.credentials.json` at agent-spawn time)."
    - "Troubleshooting table existing row for `Claude Code agent shows available: false` (docs line 138 — already references `ls -la /home/bruce/.claude/.credentials.json`) is left intact. The new Phase 228 section cross-links to it via `(see Troubleshooting row 5)`."
    - "The new section explicitly enumerates the bruce-readable file check used by Plan 228-02 SC-01 smoke (`sudo -u bruce test -r /home/bruce/.claude/.credentials.json && echo OK`) so the runbook matches the deploy verification command byte-for-byte."
    - "Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED — both files (`systemd/liv-assistant.service`, `docs/liv-assistant-install.md`) live OUTSIDE `liv/packages/core/`. Pre-commit hook PASSES. `git diff --cached --name-only | grep -E '^liv/packages/core/'` returns empty."
    - "Single atomic commit with message starting `feat(228-01):` (audit + docs append). NO behavioral code changes — repo-only edits."
    - "Idempotency guarantee — re-running this plan's diff (or pulling the commit fresh) produces zero further diff: docs section header text is unique (`## Claude subscription credentials (Phase 228)`) so a re-run plan would see the section already present and no-op."
  artifacts:
    - path: "systemd/liv-assistant.service"
      provides: "systemd unit (Phase 223-02) — audited; `HOME=/home/bruce` Environment line confirmed present (or added if drifted)"
      contains: "HOME=/home/bruce"
    - path: "docs/liv-assistant-install.md"
      provides: "Operator runbook (Phase 223-04) — new Phase 228 section documenting Claude credential file path + recovery"
      contains: "Claude subscription credentials (Phase 228)"
    - path: "docs/liv-assistant-install.md"
      provides: "Same file — references the canonical credential path"
      contains: "/home/bruce/.claude/.credentials.json"
    - path: "docs/liv-assistant-install.md"
      provides: "Same file — references recovery via Phase 221 login flow"
      contains: "Phase 221"
  key_links:
    - from: "systemd/liv-assistant.service Environment=HOME=/home/bruce"
      to: "AionUi vendored Claude Code agent at runtime"
      via: "process env inherited from systemd → AionUi spawns claude CLI subprocess → claude reads $HOME/.claude/.credentials.json"
      pattern: "HOME=/home/bruce"
    - from: "docs/liv-assistant-install.md Phase 228 section"
      to: "Operator recovery workflow"
      via: "section text → Phase 221 LivOS Settings login (or `claude` CLI fallback) → rewrites /home/bruce/.claude/.credentials.json"
      pattern: "Phase 221"
    - from: "Plan 228-02 SC-01 smoke command"
      to: "docs runbook line that documents the SAME command"
      via: "identical literal `sudo -u bruce test -r /home/bruce/.claude/.credentials.json`"
      pattern: "sudo -u bruce test -r"
---

<objective>
Audit `systemd/liv-assistant.service` (Phase 223-02) for `Environment=HOME=/home/bruce` — confirmed already present per 2026-05-27 repo read, so this plan is a docs-append-only change in the happy path. Augment `docs/liv-assistant-install.md` (Phase 223-04) with a new Phase 228 section documenting the Claude subscription credential file path, ownership/mode requirements, and recovery steps. Pure repo edits — no Mini PC contact, no behavioral change.

Purpose: Phase 228's SC-02 (`liv-assistant.service env contains HOME=/home/bruce`) and SC-06 (`docs updated with credential file path + recovery steps`) are pure repo-state assertions. Splitting them into Wave 1 lets Plan 228-02 (Wave 2 deploy) consume the committed docs as runbook reference. Operator-onboarding ergonomic — when a future operator restores Mini PC from scratch, the docs that ship in `utopusc/livinity-io` master tell them exactly where the Claude creds live + how to recover them, without spelunking the systemd unit.

Output: `systemd/liv-assistant.service` audited (no-op in happy path; one Environment line added if drifted), `docs/liv-assistant-install.md` extended with a new Phase 228 section (~30 lines), single atomic commit, sacred SHA unchanged, pre-commit hook PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/223-vendor-aionui-install/223-02-PLAN.md
@.planning/phases/223-vendor-aionui-install/223-02-SUMMARY.md
@.planning/phases/223-vendor-aionui-install/223-03-PLAN.md
@.planning/phases/223-vendor-aionui-install/223-04-SUMMARY.md
@.planning/phases/221-claude-auth-ui/221-PLAN.md

<interfaces>
<!-- ============================================================
     systemd/liv-assistant.service — CURRENT CONTENT (Phase 223-02 ship state)
     ============================================================
     The file's [Service] section as committed (2026-05-27 repo read):

         [Service]
         Type=simple
         User=bruce
         Group=bruce
         WorkingDirectory=/opt/liv-assistant/current
         Environment="PATH=/home/bruce/.bun/bin:/usr/local/bin:/usr/bin:/bin"
         Environment="HOME=/home/bruce"          <-- ALREADY PRESENT (Phase 223-02)
         ExecStart=/opt/liv-assistant/current/aionui-web start --port 3020 ...
         Restart=on-failure
         RestartSec=5
         StandardOutput=journal
         StandardError=journal
         SyslogIdentifier=liv-assistant
         NoNewPrivileges=false
         ProtectSystem=full
         ProtectHome=read-only
         ReadWritePaths=/opt/liv-assistant/data /home/bruce/.claude /home/bruce/.bun /home/bruce/.cache

     Phase 223-02 ALREADY shipped the `Environment="HOME=/home/bruce"` line. This
     plan's Task 1 is an AUDIT — read the file, confirm the line is present,
     emit no diff. If (and only if) a drift between memory and current file is
     observed (e.g. someone edited the unit out-of-band), Task 1 adds the line back.

     The `ReadWritePaths` already grants the service write access to
     `/home/bruce/.claude` (so the unit's `ProtectHome=read-only` doesn't block
     `claude` CLI from writing credentials back during refresh flows). This is
     Phase 223-02's lineage — DO NOT modify ReadWritePaths in this plan.

     ============================================================
     docs/liv-assistant-install.md — CURRENT STRUCTURE (147 lines, Phase 223-04)
     ============================================================
     Section anchors (left col line numbers from repo 2026-05-27):

         Line   1   # Liv Assistant — Install Runbook
         Line   3   > What this is: ...
         Line  10   ## Upstream provenance
         Line  21   ## File layout (Mini PC)
         Line  47   ## Install (fresh Mini PC)
         Line  69   ## Re-install / idempotency
         Line  81   ## Upgrade (future versions)
         Line  91   ## Password rotation
         Line 116   ## Locked invariants (don't break these)   <-- insert NEW section JUST BEFORE this
         Line 123   ## Known limitations (v42, see PROJECT.md)
         Line 130   ## Troubleshooting
         Line 132   | Symptom | Likely cause | Fix |
         Line 138   | Claude Code agent shows `available: false` | ... `~/.claude/.credentials.json` missing | ...
         Line 140   ## Related phases
         Line 145   - **228** (auth bridge): wires Phase 221 Claude OAuth → Liv Assistant credentials surface.

     The new `## Claude subscription credentials (Phase 228)` section goes
     IMMEDIATELY BEFORE `## Locked invariants` (so it appears as the natural
     follow-up to the Password rotation section + before the invariants
     summary). Section order matters for operator-reading flow: install →
     re-install → upgrade → password → credentials → invariants → limits →
     troubleshooting → related phases.

     ============================================================
     Phase 221 — Claude OAuth login flow (the recovery path)
     ============================================================
     Phase 221 (`.planning/phases/221-claude-auth-ui/221-PLAN.md` line 11)
     shipped a LivOS Settings UI that exchanges the OAuth `code` for a
     credentials.json write at `~/.claude/.credentials.json`. The docs section
     references Phase 221 by ID so a future operator knows where in the
     LivOS UI to go.

     CLI fallback: the `claude` binary itself supports interactive login. The
     operator can `sudo -u bruce HOME=/home/bruce claude` and walk the OAuth
     prompt if the LivOS Settings UI is unavailable.

     ============================================================
     Sacred SHA invariant
     ============================================================
     Neither `systemd/liv-assistant.service` nor `docs/liv-assistant-install.md`
     lives under `liv/packages/core/`. Sacred SHA
     `f3538e1d811992b782a9bb057d1b7f0a0189f95f` (blob SHA of
     `liv/packages/core/src/sdk-agent-runner.ts`) is UNCHANGED. Pre-commit hook
     PASSES. Verify with:

         git ls-files -s liv/packages/core/src/sdk-agent-runner.ts
         # → 100644 f3538e1d811992b782a9bb057d1b7f0a0189f95f 0 liv/packages/core/src/sdk-agent-runner.ts

     ============================================================
     No Mini PC contact in this plan
     ============================================================
     Plan 228-01 is REPO ONLY. No SSH, no curl against bruce.livinity.io, no
     `update.sh`. Mini PC verification lives entirely in Plan 228-02 (Wave 2).
     This split mirrors the 226-04 / 227-03 (repo-edit then deploy) cadence —
     known-good, fail2ban-friendly, one-SSH-per-major-step rhythm.

     ============================================================
     Server4 / Server5 — DO NOT touch (HARD RULE 2026-04-27)
     ============================================================
     Only Mini PC bruce@10.69.31.68 is in scope FOR v42 — and even Mini PC is
     not contacted by this plan. Server4 + Server5 references in docs must NOT
     appear in the new section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit systemd/liv-assistant.service for HOME=/home/bruce + append Claude credentials section to docs/liv-assistant-install.md</name>
  <files>systemd/liv-assistant.service, docs/liv-assistant-install.md</files>
  <read_first>
    - systemd/liv-assistant.service (FULL — 31 lines; verify the `Environment="HOME=/home/bruce"` line is present at line 14 of the unit; verify `ReadWritePaths` line 27 includes `/home/bruce/.claude`)
    - docs/liv-assistant-install.md (FULL — 147 lines; locate the `## Locked invariants` header at line 116 to know exact insertion point)
    - .planning/phases/223-vendor-aionui-install/223-02-PLAN.md (the systemd unit's ship spec)
    - .planning/phases/223-vendor-aionui-install/223-04-SUMMARY.md (the install docs ship spec)
    - .planning/phases/221-claude-auth-ui/221-PLAN.md (the OAuth login flow that writes `~/.claude/.credentials.json`)
  </read_first>
  <action>
**CRITICAL constraints:**
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` MUST remain UNCHANGED. `systemd/liv-assistant.service` and `docs/liv-assistant-install.md` live OUTSIDE `liv/packages/core/`. Pre-commit hook PASSES. Confirm before commit: `git diff --cached --name-only | grep -E '^liv/packages/core/' || echo 'sacred safe'`.
- Mini PC ONLY for v42 — and this plan does NOT contact Mini PC at all. Pure repo edits.
- The systemd unit is `Environment="HOME=/home/bruce"` (with double-quotes around the entire `KEY=VALUE`) — match existing style. Phase 223-02's `Environment="PATH=..."` line is the precedent.
- NO emojis in any file content.

**Step A — Audit `systemd/liv-assistant.service`.**

1. Read the full file.
2. Confirm line 14 (or thereabouts in the `[Service]` block) is exactly:
   ```
   Environment="HOME=/home/bruce"
   ```
3. Confirm `ReadWritePaths` includes `/home/bruce/.claude` (line 27 in current shape).

**Happy path (HOME line ALREADY present):** No edit to `systemd/liv-assistant.service`. Move to Step B.

**Drift path (HOME line MISSING):** Use Edit tool to insert the line IMMEDIATELY AFTER the existing `Environment="PATH=..."` line. Style note: tab indentation is NOT used (systemd unit files are key=value with no indentation); the quote style matches the PATH line above.

After Step A, no other changes to the unit file.

**Step B — Append `## Claude subscription credentials (Phase 228)` section to `docs/liv-assistant-install.md`.**

Locate the `## Locked invariants (don't break these)` header (currently line 116). Insert the following new section IMMEDIATELY BEFORE that header (so the existing blank line above `## Locked invariants` is preserved between the new section's last line and `## Locked invariants`):

```markdown
## Claude subscription credentials (Phase 228)

Liv Assistant ships AionUi's built-in Claude Code agent. That agent uses the
host's `claude` CLI to talk to Anthropic, which reads its session token from
`$HOME/.claude/.credentials.json`. The systemd unit sets `HOME=/home/bruce`
explicitly so the agent (running as `bruce`) finds the file at the expected
path.

| Field | Value |
|---|---|
| Credentials file | `/home/bruce/.claude/.credentials.json` |
| Owner / mode | `bruce:bruce 0600` |
| Written by | Phase 221 LivOS Settings → Claude Auth (OAuth code exchange), or interactive `claude` CLI login |
| Read by | AionUi's vendored Claude Code agent on agent spawn (per-chat-turn) |
| systemd grant | `liv-assistant.service` `ReadWritePaths=/home/bruce/.claude` (Phase 223-02) |
| Required env | `liv-assistant.service` `Environment="HOME=/home/bruce"` (Phase 223-02) |

### Verify creds are usable by the service

```bash
sudo -u bruce test -r /home/bruce/.claude/.credentials.json && echo OK
# expect: OK
ls -la /home/bruce/.claude/.credentials.json
# expect: -rw------- 1 bruce bruce ... .credentials.json
systemctl show liv-assistant -p Environment | grep -o 'HOME=/home/bruce'
# expect: HOME=/home/bruce
```

If any line fails, the agent will show `available: false` in `/api/agents`
(see Troubleshooting row "Claude Code agent shows `available: false`" below).

### Recovery — credentials missing or expired

1. **Preferred — LivOS Settings (Phase 221 UI):** Open
   `https://bruce.livinity.io` → Settings → Claude Auth → "Sign in to Claude".
   Walk the OAuth device-code prompt. Phase 221's `submitLoginCode(code)` writes
   `/home/bruce/.claude/.credentials.json` with the correct ownership + mode.
2. **CLI fallback:** SSH to the Mini PC as `bruce` (or `sudo -u bruce`) and run
   `HOME=/home/bruce claude` — the binary's interactive login flow writes the
   credentials file directly. Then `sudo systemctl restart liv-assistant` to
   re-pick up the file.
3. **Post-recovery smoke:** Re-run the three verify commands in the table above.

### Why this is split out from the install runbook

Install (Phase 223) only stands up AionUi's process; the Claude credentials are
a separate concern that depends on the operator having a Claude subscription
linked via Phase 221. Phase 228 is the audit + docs that wire these together
so a future operator onboarding from a fresh Mini PC knows the exact file path
and recovery flow without spelunking the systemd unit.
```

Verify after edit:
- The new section header `## Claude subscription credentials (Phase 228)` appears exactly once in `docs/liv-assistant-install.md`.
- The line `/home/bruce/.claude/.credentials.json` appears at least 3 times in the new section (table + verify block + recovery block).
- The literal command `sudo -u bruce test -r /home/bruce/.claude/.credentials.json` appears at least once (Plan 228-02 SC-01 smoke uses the same literal).
- `## Locked invariants (don't break these)` STILL appears at the original anchor (just moved further down the file by ~40 lines).

**Step C — Local verification (before commit):**

```bash
cd /c/Users/hello/Desktop/Projects/contabo/livinity-io
# Verify the systemd unit still parses (offline check — systemd-analyze verify needs systemd at runtime,
# but we at least confirm no UTF-8 BOM / no Windows CRLF introduced and the HOME line is intact):
grep -c '^Environment="HOME=/home/bruce"$' systemd/liv-assistant.service
# expect: 1

# Verify docs section header was inserted exactly once:
grep -c '^## Claude subscription credentials (Phase 228)$' docs/liv-assistant-install.md
# expect: 1

# Verify the credentials path is present in the new section:
grep -c '/home/bruce/.claude/.credentials.json' docs/liv-assistant-install.md
# expect: >= 3

# Verify Phase 221 cross-reference is present:
grep -c 'Phase 221' docs/liv-assistant-install.md
# expect: >= 2  (the new section + the existing line 145 "Related phases" entry)

# Sacred file guard:
git diff --name-only | grep -E '^liv/packages/core/' || echo 'sacred safe'
# expect: sacred safe

# Stage and inspect:
git add systemd/liv-assistant.service docs/liv-assistant-install.md
git diff --cached --stat
# expect: 2 files changed (or 1 if happy path with no systemd edit needed — then add only docs file)
```

If happy path (no drift in systemd unit), only `docs/liv-assistant-install.md` is staged.

**Step D — Commit (single atomic commit):**

Happy path message:

```
git commit -m "feat(228-01): audit liv-assistant systemd unit + document Claude creds path

Phase 228 Plan 01 (Wave 1) — repo-only audit + docs append.

systemd/liv-assistant.service was audited end-to-end: the
Environment=\"HOME=/home/bruce\" line that AionUi's vendored Claude Code agent
needs to find /home/bruce/.claude/.credentials.json is ALREADY present (shipped
in Phase 223-02). No edit to the unit file needed in the happy path; this
commit captures the audit verdict via the docs append.

docs/liv-assistant-install.md gets a new section ## Claude subscription
credentials (Phase 228) inserted BEFORE ## Locked invariants. The section
documents:
  - credential file path (/home/bruce/.claude/.credentials.json)
  - ownership / mode (bruce:bruce 0600)
  - the systemd guards that make it readable to the service
    (ReadWritePaths + HOME env)
  - three verify commands (matching Plan 228-02 SC-01..SC-02 smoke literals)
  - two recovery paths: Phase 221 LivOS Settings UI (preferred) and
    interactive 'claude' CLI fallback
  - rationale for why this is split out of the Phase 223 install runbook

Plan 228-02 (Wave 2) will deploy nothing new — just verify on Mini PC that
the file exists with correct perms, systemd-show reports HOME=/home/bruce,
and external curl https://bruce.livinity.io/liv/api/auth/status reflects
auth state.

Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f UNCHANGED. Neither file
touched lives under liv/packages/core/. Pre-commit hook PASSES.

Closes (repo side): SC-02 (Environment HOME=/home/bruce confirmed in repo
systemd unit), SC-06 (docs updated with credential file path + recovery
steps). SC-01 (file readable by service user) + SC-03 (AionUi internal
auth endpoint) + SC-04 (external relay endpoint) + SC-05 (Mini PC sacred
SHA unchanged) captured in Plan 228-02 deploy task."
```

Drift path message (if the systemd HOME line had to be added back):

```
git commit -m "fix(228-01): restore HOME=/home/bruce in liv-assistant.service + document Claude creds path

Out-of-band drift fix — the Environment=\"HOME=/home/bruce\" line that Phase
223-02 shipped was missing from systemd/liv-assistant.service. Re-added.
Plus the docs append described above.

[rest of message identical to happy path, minus the 'No edit to the unit
file needed' line]"
```

Pre-commit hook must PASS (no `liv/packages/core/` paths touched).
  </action>
  <verify>
    <automated>grep -c '^Environment="HOME=/home/bruce"$' systemd/liv-assistant.service && grep -c '^## Claude subscription credentials (Phase 228)$' docs/liv-assistant-install.md && [ "$(grep -c '/home/bruce/.claude/.credentials.json' docs/liv-assistant-install.md)" -ge 3 ] && [ "$(grep -c 'Phase 221' docs/liv-assistant-install.md)" -ge 2 ] && grep -q 'sudo -u bruce test -r /home/bruce/.claude/.credentials.json' docs/liv-assistant-install.md && grep -q '^## Locked invariants' docs/liv-assistant-install.md && git log -1 --name-only | grep -qE '^docs/liv-assistant-install\.md$' && ! git log -1 --name-only | grep -qE '^liv/packages/core/' && [ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `systemd/liv-assistant.service` contains the literal line `Environment="HOME=/home/bruce"` exactly once
    - `docs/liv-assistant-install.md` contains the literal header `## Claude subscription credentials (Phase 228)` exactly once
    - The new docs section appears BEFORE the existing `## Locked invariants (don't break these)` header (operator-reading order preserved)
    - `/home/bruce/.claude/.credentials.json` appears at least 3 times in `docs/liv-assistant-install.md` (table + verify + recovery)
    - The literal command `sudo -u bruce test -r /home/bruce/.claude/.credentials.json` appears at least once (matches Plan 228-02 SC-01 smoke)
    - `Phase 221` appears at least twice in `docs/liv-assistant-install.md` (new section + existing line 145 "Related phases" entry)
    - The existing `## Locked invariants` / `## Known limitations` / `## Troubleshooting` / `## Related phases` sections remain present (no accidental deletion)
    - Single atomic commit landed; `git log -1 --name-only` shows `docs/liv-assistant-install.md` (and `systemd/liv-assistant.service` ONLY if drift path)
    - `git log -1 --name-only` does NOT include any path under `liv/packages/core/`
    - Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` UNCHANGED (`git hash-object liv/packages/core/src/sdk-agent-runner.ts` returns the canonical SHA)
    - Pre-commit hook PASSED for the commit
  </acceptance_criteria>
  <done>systemd unit audited (HOME=/home/bruce present), docs section appended (credential path + recovery steps documented), sacred SHA unchanged, one atomic commit landed. Plan 228-02 (Wave 2) is unblocked.</done>
</task>

</tasks>

<verification>
- All `<verify>` blocks of all tasks above must PASS.
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` unchanged at end of plan.
- Git working tree clean post-commit.
- No Mini PC contact in this plan (deferred to Plan 228-02).
</verification>

<success_criteria>
- SC-02 (`liv-assistant.service env contains HOME=/home/bruce`) closed at the repo level — Plan 228-02 will verify on the live Mini PC unit via `systemctl show`.
- SC-06 (`docs/liv-assistant-install.md updated with credential file path + recovery steps`) closed.
- SC-05 (Sacred SHA unchanged) holds at the repo level after this plan's commit.
- Plan 228-02 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/228-claude-auth-bridge/228-01-SUMMARY.md` capturing: audit verdict (HOME line present pre-edit, no systemd diff needed in happy path), docs section line count + key anchors, commit SHA, sacred SHA verify.
</output>
