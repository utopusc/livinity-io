---
phase: 10-unified-identity-memory-management-ui
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  - livos/packages/ui/src/routes/settings/memory.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Settings > Memory section appears in the menu and renders the MemorySection component"
    - "Memories tab lists stored memories with search bar and delete button per item"
    - "Conversations tab lists conversation turns grouped by channel with channel icon and search"
    - "Deleting a memory or conversation turn removes it from the list without page refresh"
  artifacts:
    - path: "livos/packages/ui/src/routes/settings/memory.tsx"
      provides: "MemorySection component with Memories and Conversations tabs"
      min_lines: 100
    - path: "livos/packages/ui/src/routes/settings/_components/settings-content.tsx"
      provides: "Memory menu item and SectionContent case"
      contains: "memory"
  key_links:
    - from: "livos/packages/ui/src/routes/settings/memory.tsx"
      to: "livos/packages/livinityd/source/modules/ai/routes.ts"
      via: "trpcReact.ai.memoryList.useQuery, trpcReact.ai.conversationTurnsList.useQuery"
      pattern: "trpcReact\\.ai\\.memory"
    - from: "livos/packages/ui/src/routes/settings/_components/settings-content.tsx"
      to: "livos/packages/ui/src/routes/settings/memory.tsx"
      via: "React.lazy import and SectionContent switch case"
      pattern: "MemorySection"
---

<objective>
Create the Settings > Memory UI page with two tabs (Memories and Conversations) for browsing, searching, and deleting stored AI memories and conversation history across all channels.

Purpose: Users need to see what the AI remembers about them, search through conversation history from all channels (Web, Telegram, WhatsApp, Discord), and delete individual entries. This completes the user-facing requirements UI-01, UI-02, UI-03.

Output: New memory.tsx component with full Memory Management UI, integrated into Settings via lazy loading.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-identity-memory-management-ui/10-01-SUMMARY.md

<interfaces>
<!-- tRPC routes created by Plan 01 that this UI consumes -->

From livos/packages/livinityd/source/modules/ai/routes.ts (created in Plan 01):
- ai.memoryList: query, no input -> { memories: [{id, content, metadata, createdAt}] }
- ai.memoryDelete: mutation, input {id: string} -> { success: true }
- ai.conversationTurnsList: query, input {limit?, offset?, channel?} -> { turns: [{id, userId, channel, chatId, role, content, metadata, createdAt}], total: number }
- ai.conversationTurnsDelete: mutation, input {id: number} -> { success: true }
- ai.conversationTurnsSearch: mutation, input {query, channel?, limit?} -> { results: [{id, user_id, channel, chat_id, role, content, metadata, created_at}] }

From settings-content.tsx (existing patterns):
- SettingsSection type union: add | 'memory'
- MENU_ITEMS array: add {id: 'memory', icon: TbBrain, label: 'Memory', description: 'AI memory & conversations'}
- SectionContent switch: add case 'memory' with Suspense + lazy load
- Lazy import pattern: React.lazy(() => import('@/routes/settings/memory').then(m => ({default: m.MemorySection})))

From UI library (already imported in settings-content.tsx):
- Tabs, TabsContent, TabsList, TabsTrigger from '@/shadcn-components/ui/tabs'
- Input from '@/shadcn-components/ui/input'
- Button from '@/shadcn-components/ui/button'
- Card from '@/components/ui/card'
- trpcReact from '@/trpc/trpc'
- useIsMobile from '@/hooks/use-is-mobile'
- cn from '@/shadcn-lib/utils'

Channel icons available in react-icons/tb:
- TbBrandTelegram, TbBrandWhatsapp, TbBrandDiscord, TbWorld (for web), TbBrandSlack
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Memory section component with Memories and Conversations tabs</name>
  <files>
    livos/packages/ui/src/routes/settings/memory.tsx
  </files>
  <read_first>
    - livos/packages/ui/src/routes/settings/dm-pairing.tsx (first 80 lines — example of a settings section with lazy-loaded content, exports pattern)
    - livos/packages/ui/src/routes/settings/usage-dashboard.tsx (first 80 lines — example of a data-fetching settings section with tabs pattern)
    - livos/packages/ui/src/routes/settings/_components/settings-content.tsx (lines 86-104 — lazy import patterns)
  </read_first>
  <action>
    Create `livos/packages/ui/src/routes/settings/memory.tsx` exporting `MemorySection` component.

    **Structure:**
    ```
    MemorySection
      -> Tabs (defaultValue="memories")
        -> TabsList
          -> TabsTrigger "Memories" (TbBrain icon)
          -> TabsTrigger "Conversations" (TbHistory icon)
        -> TabsContent "memories" -> MemoriesTab
        -> TabsContent "conversations" -> ConversationsTab
    ```

    **MemoriesTab component:**
    - Uses `trpcReact.ai.memoryList.useQuery()` to fetch memories
    - Search input (controlled state) that filters memories client-side by content (case-insensitive includes)
    - List of memory cards, each showing:
      - Content text (truncated to 200 chars with "..." if longer)
      - Created date formatted with `new Date(createdAt).toLocaleDateString()` + time
      - Delete button (TbTrash icon) that calls `trpcReact.ai.memoryDelete.useMutation()` with optimistic update: `utils.ai.memoryList.invalidate()` on success
    - Empty state: "No memories stored yet. Chat with the AI to build memory."
    - Loading state: Loader2 spinner (same pattern as other settings sections)

    **ConversationsTab component:**
    - Uses `trpcReact.ai.conversationTurnsList.useQuery({ limit: 50 })` for initial load
    - Search input that when submitted (Enter key or search button), calls `trpcReact.ai.conversationTurnsSearch.useMutation()` with the query. When search is cleared, show the default list again.
    - Optional channel filter: small button row at top with "All", "Web", "Telegram", "WhatsApp", "Discord" — sets channel filter param that re-fetches conversationTurnsList with the channel value
    - Each conversation turn shows:
      - Channel icon: use a `channelIcon(channel: string)` helper function mapping channel names to react-icons: 'web' -> TbWorld, 'telegram' -> TbBrandTelegram, 'whatsapp' -> TbBrandWhatsapp, 'discord' -> TbBrandDiscord, 'slack' -> TbMessage (or TbBrandSlack), default -> TbMessage
      - Channel label text (capitalized)
      - Role badge: "User" or "AI" with different colors (user = blue, assistant = green)
      - Content preview (first 150 chars)
      - Timestamp formatted as relative or date
      - Delete button (TbTrash) calling `trpcReact.ai.conversationTurnsDelete.useMutation()` with `{ id: turn.id }`, invalidate conversationTurnsList on success
    - "Load more" button at bottom if `total > turns.length` (pass offset = current turns length)
    - Empty state: "No conversation history yet."

    **Styling (Tailwind, follow existing patterns):**
    - Use `cn()` for conditional classes
    - Mobile responsive: single column on mobile (useIsMobile()), comfortable touch targets (min-h-[44px] for interactive elements)
    - Dark theme compatible: use existing color tokens (text-white/70, text-white/40, bg-white/5, hover:bg-white/10 etc.)
    - Memory/turn items: `bg-white/5 rounded-lg p-3 hover:bg-white/8` card style
    - Delete button: `text-white/40 hover:text-red-400` ghost style with TbTrash icon
    - Search input: standard Input component from shadcn
    - Channel filter buttons: small pill buttons `px-3 py-1 rounded-full text-xs` with active state `bg-white/20`

    **Imports:**
    ```typescript
    import React, {useState} from 'react'
    import {TbBrain, TbHistory, TbTrash, TbSearch, TbWorld, TbBrandTelegram, TbBrandWhatsapp, TbBrandDiscord, TbMessage, TbLoader2} from 'react-icons/tb'
    import {Tabs, TabsContent, TabsList, TabsTrigger} from '@/shadcn-components/ui/tabs'
    import {Input} from '@/shadcn-components/ui/input'
    import {Button} from '@/shadcn-components/ui/button'
    import {trpcReact} from '@/trpc/trpc'
    import {useIsMobile} from '@/hooks/use-is-mobile'
    import {cn} from '@/shadcn-lib/utils'
    ```
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && test -f livos/packages/ui/src/routes/settings/memory.tsx && grep -c "MemorySection\|MemoriesTab\|ConversationsTab\|memoryList\|conversationTurnsList\|memoryDelete\|conversationTurnsDelete\|TbBrandTelegram\|TbBrandWhatsapp" livos/packages/ui/src/routes/settings/memory.tsx</automated>
  </verify>
  <acceptance_criteria>
    - memory.tsx exists and exports MemorySection
    - MemoriesTab uses trpcReact.ai.memoryList.useQuery()
    - MemoriesTab has search input filtering memories client-side
    - MemoriesTab has delete button calling trpcReact.ai.memoryDelete.useMutation()
    - ConversationsTab uses trpcReact.ai.conversationTurnsList.useQuery()
    - ConversationsTab has channel filter buttons (All, Web, Telegram, WhatsApp, Discord)
    - ConversationsTab shows channel icons (TbBrandTelegram, TbBrandWhatsapp, TbBrandDiscord, TbWorld)
    - ConversationsTab has delete button calling trpcReact.ai.conversationTurnsDelete.useMutation()
    - ConversationsTab has search that calls trpcReact.ai.conversationTurnsSearch.useMutation()
    - Mobile responsive using useIsMobile()
  </acceptance_criteria>
  <done>MemorySection component renders two tabs with search, channel filtering, delete functionality, and channel icons for all conversation turns</done>
</task>

<task type="auto">
  <name>Task 2: Register Memory section in Settings and wire lazy loading</name>
  <files>
    livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  </files>
  <read_first>
    - livos/packages/ui/src/routes/settings/_components/settings-content.tsx (lines 86-106 — lazy imports, lines 111-161 — SettingsSection type + MENU_ITEMS, lines 413-454 — SectionContent switch)
  </read_first>
  <action>
    **1. Add TbBrain import:** In the react-icons/tb import block (around line 10-45), add `TbBrain` to the import list if not already present.

    **2. Add lazy import:** After the existing lazy imports (around line 104), add:
    ```typescript
    const MemorySectionLazy = React.lazy(() =>
      import('@/routes/settings/memory').then((m) => ({default: m.MemorySection})),
    )
    ```

    **3. Add to SettingsSection type:** Add `| 'memory'` to the type union (around line 130).

    **4. Add menu item:** In the MENU_ITEMS array, add the memory item in the per-user settings section, after 'usage' and before the admin-only section (around line 151):
    ```typescript
    {id: 'memory', icon: TbBrain, label: 'Memory', description: 'AI memory & conversations'},
    ```

    **5. Add SectionContent case:** In the `SectionContent` function switch statement, add before the `default` case:
    ```typescript
    case 'memory':
      return <Suspense fallback={<div className='flex items-center justify-center py-8'><Loader2 className='size-5 animate-spin text-text-tertiary' /></div>}><MemorySectionLazy /></Suspense>
    ```
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && grep -c "memory" livos/packages/ui/src/routes/settings/_components/settings-content.tsx && grep "TbBrain" livos/packages/ui/src/routes/settings/_components/settings-content.tsx && grep "MemorySectionLazy" livos/packages/ui/src/routes/settings/_components/settings-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsSection type includes 'memory'
    - MENU_ITEMS contains {id: 'memory', icon: TbBrain, label: 'Memory', description: 'AI memory & conversations'}
    - SectionContent switch has case 'memory' returning MemorySectionLazy wrapped in Suspense
    - MemorySectionLazy lazy import exists pointing to '@/routes/settings/memory'
    - TbBrain is in the react-icons/tb import
  </acceptance_criteria>
  <done>Memory section registered in Settings menu, lazy loaded, and routable via SectionContent switch</done>
</task>

</tasks>

<verification>
- `grep "MemorySection" livos/packages/ui/src/routes/settings/memory.tsx` confirms component export
- `grep "memory" livos/packages/ui/src/routes/settings/_components/settings-content.tsx` confirms section registration
- `grep "memoryList\|conversationTurnsList" livos/packages/ui/src/routes/settings/memory.tsx` confirms tRPC hook usage
- `grep "TbBrandTelegram\|TbBrandWhatsapp\|TbWorld" livos/packages/ui/src/routes/settings/memory.tsx` confirms channel icons
- `grep "memoryDelete\|conversationTurnsDelete" livos/packages/ui/src/routes/settings/memory.tsx` confirms delete functionality
</verification>

<success_criteria>
1. Settings > Memory menu item visible with TbBrain icon (UI-01)
2. Memories tab shows stored memories with search and per-item delete button (UI-01, UI-02)
3. Conversations tab shows conversation turns with channel icons (Telegram, WhatsApp, Discord, Web) and search (UI-03)
4. User can delete individual memory entries and conversation turns (UI-02)
5. Channel filter buttons allow filtering conversations by source channel (UI-03)
6. Mobile responsive layout following existing patterns
</success_criteria>

<output>
After completion, create `.planning/phases/10-unified-identity-memory-management-ui/10-02-SUMMARY.md`
</output>
