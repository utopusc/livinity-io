---
phase: 260-window-dock-to-displays-stream-recall-ux
plan: 04
type: execute
wave: 2
depends_on: [260-03]
files_modified:
  - livos/packages/ui/src/modules/desktop/top-bar.tsx
  - livos/packages/ui/src/modules/desktop/displays-popover.tsx
  - livos/packages/ui/src/modules/desktop/windows-manager-panel.tsx
  - livos/packages/ui/src/modules/desktop/top-bar.test.tsx
autonomous: true
requirements: [SC3, SC5]
must_haves:
  truths:
    - "The Displays button shows a badge = the number of docked (pinned) windows, hidden when 0, updating live on dock/undock"
    - "A docked window can be recalled by clicking it in the Displays popover (unpinWindowFromTopBar)"
    - "The badge count is documented as docked-count (or live-display count per the resolved Open Question) and matches the popover contents"
  artifacts:
    - path: "livos/packages/ui/src/modules/desktop/top-bar.tsx"
      provides: "{n} count badge on the Displays button"
      contains: "pinnedWindows.length"
    - path: "livos/packages/ui/src/modules/desktop/displays-popover.tsx"
      provides: "docked windows recall surface (Docked section / wired rows)"
      contains: "unpinWindowFromTopBar"
  key_links:
    - from: "displays-popover.tsx docked row"
      to: "windowManager.unpinWindowFromTopBar(w.id)"
      via: "click-to-recall"
      pattern: "unpinWindowFromTopBar"
    - from: "top-bar.tsx Displays button"
      to: "badge span"
      via: "pinnedWindows.length"
      pattern: "pinnedWindows.length"
---

<objective>
Add SC5 (the {n} badge on the Displays button) and the SC3 recall-from-Displays-button surface.
The count is already derived (top-bar.tsx:105: `pinnedWindows = windows.filter(isPinnedToTopBar)`),
and the popover already folds in `WindowsManagerPanel` whose rows already call
`unpinWindowFromTopBar` (windows-manager-panel.tsx:72-103). This plan renders the badge and makes
the docked windows recallable from the popover (a dedicated "Docked" section or wired rows).

OPEN QUESTION to resolve here (RESEARCH Open Q1 / Assumption A3): does the badge count DOCKED
windows or LIVE displays? CONTEXT SC5 says "currently open/docked windows (displays)" but verify
step 1 says the badge reflects a native app the moment you OPEN it (before docking) — implying
live-display count. The plan makes this an explicit investigation step and a documented choice.

Purpose: visible {n} badge + recall docked windows from the Displays popover.
Output: badge on Displays button, docked-recall wired into the popover, badge + recall tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/CONTEXT.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-RESEARCH.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-PATTERNS.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-03-SUMMARY.md

<interfaces>
```ts
// Derived count for the badge — top-bar.tsx:105 (already computed)
const pinnedWindows = (windowManager?.windows ?? []).filter((w) => w.isPinnedToTopBar)
```

```tsx
// Recall row buttons ALREADY wired — windows-manager-panel.tsx:72-103
<button onClick={() => (w.isPinnedToTopBar ? wm.unpinWindowFromTopBar(w.id) : wm.pinWindowToTopBar(w.id))}>{w.isPinnedToTopBar ? 'Unpin' : 'Pin'}</button>
<button onClick={() => wm.focusWindow(w.id)}>Focus</button>
```

```tsx
// Popover already renders displays.list + folds in WindowsManagerPanel — displays-popover.tsx:39-71
const displaysQuery = trpcReact.displays.list.useQuery(undefined, { enabled: open, refetchInterval: 4000 })
{displays.map((d) => <DisplayCard key={d.display} d={d} open={open} />)}
<WindowsManagerPanel />
```

NOTE: AnimatePresence + motion already imported in top-bar.tsx:2 — reuse the PinnedWindowChip spring
(top-bar.tsx:423-426 — the chip itself was removed in 260-03, but the spring config is a good
reference) for the badge pop on increment/decrement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve badge semantics + render the {n} badge on the Displays button</name>
  <files>livos/packages/ui/src/modules/desktop/top-bar.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/desktop/top-bar.tsx (READ current state after 260-03: the Monitor/Displays button ~366-380, the derived `pinnedWindows` at :105, the AnimatePresence/motion import at :2)
    - .planning/phases/260-window-dock-to-displays-stream-recall-ux/CONTEXT.md (SC5 text lines 59-61 AND verify step 1 line 114 — the two readings that conflict)
    - .planning/phases/260-window-dock-to-displays-stream-recall-ux/260-RESEARCH.md (SC5 section + Open Question 1 + Assumption A3)
  </read_first>
  <action>
    1. RESOLVE the badge-semantics open question: CONTEXT verify step 1 ("Open a native app → it shows
       in the Displays popover; badge {n} reflects it") implies the badge should reflect what's IN the
       Displays popover (= displays.list count, which after 260-02 includes open native displays) NOT
       only docked windows. Implement the badge as **the count shown in the Displays popover**:
       prefer `displays.list` count (live open displays) UNION docked windows, deduped — practically,
       the count of distinct entries the popover would render. If `displays.list` is not already
       queried in top-bar (it is queried in displays-popover, gated on `open`), add a lightweight
       always-on `trpcReact.displays.list` query in top-bar (refetchInterval ~4000) to drive the count,
       OR derive the count from a shared source. Document the final choice in the SUMMARY.
       Fallback minimum if the displays.list wiring is heavy: badge = `pinnedWindows.length` and note
       the deviation explicitly in the SUMMARY for operator confirmation at 260-06 live-verify.
    2. Render a small absolutely-positioned badge pill on the Monitor button (top-bar.tsx:366-380):
       a `<motion.span>` showing the count, `hidden when count === 0`, wrapped in `AnimatePresence`
       with a spring pop on change. Reuse the spring values referenced from the former PinnedWindowChip
       (top-bar.tsx:423-426 spring config) for the pop.
    3. Ensure the badge updates live: because the count derives from window-manager state and/or the
       displays.list query, it re-renders on dock/undock/open/close automatically.
  </action>
  <verify>
    <automated>pnpm --filter ui test -- top-bar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - top-bar.tsx renders a badge element on the Displays/Monitor button that is conditionally hidden when count is 0 (grep: a `count === 0` / `count > 0` guard near the Monitor button + a motion.span badge)
    - The count source is documented in code comment AND in the SUMMARY (displays.list-based vs pinnedWindows.length)
    - The badge uses AnimatePresence/motion for the pop (grep `AnimatePresence` wrapping the badge)
    - `pnpm --filter ui test -- top-bar.test.tsx` passes
  </acceptance_criteria>
  <done>{n} badge renders on the Displays button, hidden at 0, pops on change, semantics documented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire docked-window recall into the Displays popover</name>
  <files>livos/packages/ui/src/modules/desktop/displays-popover.tsx, livos/packages/ui/src/modules/desktop/windows-manager-panel.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/desktop/displays-popover.tsx (READ 39-71 the display-card map + WindowsManagerPanel fold-in, and the click-to-open DISPLAY_:N at 93-98)
    - livos/packages/ui/src/modules/desktop/windows-manager-panel.tsx (READ the full rows 60-104 — Focus/Min/Pin-Unpin/Close already wired to wm)
    - .planning/phases/260-window-dock-to-displays-stream-recall-ux/260-PATTERNS.md (the displays-popover.tsx + windows-manager-panel.tsx assignment sections)
  </read_first>
  <action>
    1. In displays-popover.tsx, surface DOCKED windows for recall. Add a "Docked" section (above or
       below WindowsManagerPanel) that lists windows where `isPinnedToTopBar`, each row clickable to
       recall via `windowManager.unpinWindowFromTopBar(w.id)`. Reuse the existing
       WindowsManagerPanel pattern — the simplest correct path is to ensure WindowsManagerPanel's
       docked (pinned) rows are clearly labeled "Recall" and call `unpinWindowFromTopBar` (they
       already do via the Pin/Unpin button at windows-manager-panel.tsx:88-95). If a dedicated
       Docked section is clearer for the operator, add one that filters `windows.filter(isPinnedToTopBar)`
       and renders a "Recall" button calling `unpinWindowFromTopBar(w.id)` plus the window title/icon.
    2. In windows-manager-panel.tsx, rename the Unpin action label for pinned rows to "Recall" (or add
       a dedicated Recall button) so the recall affordance is obvious. Keep the underlying call
       `wm.unpinWindowFromTopBar(w.id)` unchanged.
    3. Do NOT call closeWindow for recall — recall is unpin only (stream stays alive, window re-expands).
  </action>
  <verify>
    <automated>pnpm --filter ui test -- displays-popover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - displays-popover.tsx (or windows-manager-panel.tsx) renders docked windows with a recall action calling `unpinWindowFromTopBar` (grep `unpinWindowFromTopBar`)
    - The recall affordance is labeled "Recall" for pinned rows (grep `Recall`)
    - No recall path calls `closeWindow` (grep: recall handlers reference unpinWindowFromTopBar, not closeWindow)
    - `pnpm --filter ui test -- displays-popover.test.tsx` passes (extend the existing displays-popover test if present; if a docked-recall assertion is missing, add it)
  </acceptance_criteria>
  <done>Docked windows are recallable from the Displays popover via unpin; never via close.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test — badge count reflects docked/open count; popover recall calls unpin</name>
  <files>livos/packages/ui/src/modules/desktop/top-bar.test.tsx</files>
  <read_first>
    - livos/packages/ui/src/modules/desktop/top-bar.test.tsx (existing harness + the 260-03 collapse tests)
    - livos/packages/ui/src/modules/desktop/displays-popover.tsx (to assert recall wiring if testing here)
    - .planning/phases/260-window-dock-to-displays-stream-recall-ux/260-RESEARCH.md (Test Map: SC5 badge count = pinned/open windows)
  </read_first>
  <behavior>
    - Test 1: with 0 docked/open displays, the badge is NOT rendered (hidden at 0)
    - Test 2: with 2 docked windows (and/or 2 displays in displays.list per the resolved semantics), the badge text is "2"
    - Test 3: clicking a docked row's recall in the popover calls windowManager.unpinWindowFromTopBar with that window id (NOT closeWindow)
  </behavior>
  <action>
    Extend top-bar.test.tsx (and/or displays-popover.test.tsx for Test 3) with the three cases. Mock
    windowManager.windows with pinned windows and/or mock the displays.list query (trpcReact) per the
    semantics chosen in Task 1. Assert the badge text and the hidden-at-0 behavior. For Test 3, render
    the popover docked row and assert the recall handler calls `unpinWindowFromTopBar` with the right id.
  </action>
  <verify>
    <automated>pnpm --filter ui test -- top-bar.test.tsx displays-popover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - top-bar.test.tsx asserts badge hidden at 0 and shows "2" with two docked/open entries
    - A test asserts popover recall calls `unpinWindowFromTopBar` (and never `closeWindow`)
    - `pnpm --filter ui test -- top-bar.test.tsx displays-popover.test.tsx` passes
  </acceptance_criteria>
  <done>Badge count + recall behavior are regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC client → displays.list | Already gated by privateProcedure + canAccessDisplay; badge/recall are read-only client-side derivations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-08 | (n/a) | badge count + popover recall | accept | Pure client-side derived UI state; recall only unpins a window the user owns this session; no new input or network surface |
| T-260-09 | Information disclosure (I) | displays.list driving the badge count | accept | displays.list already session-scoped + canAccessDisplay-gated; badge merely counts entries the operator can already see in the popover |
</threat_model>

<verification>
- `pnpm --filter ui test -- top-bar.test.tsx displays-popover.test.tsx` green
- grep: badge on Displays button hidden at 0; recall via unpinWindowFromTopBar; never closeWindow
- Manual (deferred to 260-06): badge increments on open/dock, decrements on close/undock; clicking a docked entry recalls it
</verification>

<success_criteria>
SC5 satisfied (live {n} badge on the Displays button, hidden at 0) and SC3 recall-from-Displays-button
satisfied (docked windows recall via unpin). Badge semantics documented for operator confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-04-SUMMARY.md`
</output>
