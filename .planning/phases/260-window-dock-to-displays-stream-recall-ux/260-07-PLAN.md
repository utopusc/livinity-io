---
phase: 260-window-dock-to-displays-stream-recall-ux
plan: 07
type: execute
wave: 3
depends_on: [260-01, 260-02, 260-03, 260-04, 260-05, 260-06]
files_modified: []
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, SC7, SC8]
must_haves:
  truths:
    - "The phase is deployed to the Mini PC via update.sh and the PWA service worker is cleared so the new bundle serves"
    - "All six CONTEXT verify steps pass on the live Mini PC"
    - "No Phase 259 regression: native apps still launch (no ENOENT crash) and VNC errors don't crash livinityd"
  artifacts: []
  key_links:
    - from: "bash /opt/livos/update.sh"
      to: "live Mini PC bundle + livinityd restart"
      via: "deploy + SW clear"
      pattern: "update.sh"
---

<objective>
Deploy Phase 260 to the Mini PC and run the live verification from CONTEXT.md "Verify" steps 1-6.
This is a manual (checkpoint) plan because the verification is visual/interactive and the deploy
sequence has a non-automatable SW-clear step. NO code changes — deploy + verify only.

Purpose: prove SC1-SC8 on the real box and confirm no Phase 259 regression.
Output: live verification result; phase marked done or gaps surfaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/CONTEXT.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-01-SUMMARY.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-02-SUMMARY.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-03-SUMMARY.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-04-SUMMARY.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-05-SUMMARY.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-06-SUMMARY.md

<interfaces>
Deploy reality (from CONTEXT.md + MEMORY):
- UI changes ship via `bash /opt/livos/update.sh` (builds @livos/config + ui) on the Mini PC.
- update.sh needs `sudo`.
- After deploy, the PWA service worker (`sw.js`) aggressively caches the bundle — MUST clear it
  (DevTools → Application → Clear site data / Unregister service worker) or the stale bundle keeps
  serving (the #1 "it didn't change" cause).
- Backend livinityd = tsx (no compile); `systemctl restart livos` picks up source.
- SSH: `ssh -i .../pem/minipc bruce@100.112.68.1` (Tailscale).
- Phase 259 stability invariants to NOT regress: native-app-spawner ENOENT handler + vnc-bridge
  `?.()` logger guards.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites before deploy (gate)</name>
  <files></files>
  <read_first>
    - .planning/phases/260-window-dock-to-displays-stream-recall-ux/260-RESEARCH.md (Sampling Rate: "Per wave merge: full ui + livinityd vitest suites")
  </read_first>
  <action>
    Run the full UI and livinityd test suites locally (or on the box) to confirm all SC1-SC8 unit
    tests are green before deploying:
    `pnpm --filter ui test` and `pnpm --filter @livos/livinityd test`.
    If anything fails, STOP and report — do NOT deploy a red build.
  </action>
  <verify>
    <automated>pnpm --filter ui test; pnpm --filter @livos/livinityd test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter ui test` exits 0
    - `pnpm --filter @livos/livinityd test` exits 0
  </acceptance_criteria>
  <done>Both suites green; safe to deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>SC1-SC8 implemented + unit-tested across plans 260-01..06. This step deploys to the Mini PC.</what-built>
  <how-to-verify>
    The deploy + SW-clear cannot be fully automated (the SW clear is a browser action). Perform:
    1. SSH to the Mini PC: `ssh -i .../pem/minipc bruce@100.112.68.1`
    2. Run the deploy: `sudo bash /opt/livos/update.sh` — wait for it to build @livos/config + ui and
       restart livos. If a new apt package was somehow introduced (none expected this phase), re-run once.
    3. Confirm services: `systemctl status livos liv-core` — both `active (running)`.
    4. In the browser (bruce.livinity.io or the Mini PC UI), open DevTools → Application → Clear site
       data / Unregister service worker, then hard-reload so the NEW bundle serves.
    5. Confirm the new bundle is live (network tab shows the new index-*.js hash, not a cached one).
  </how-to-verify>
  <resume-signal>Type "deployed" once update.sh succeeded, both services are active, and the SW is cleared with the new bundle serving. Report any deploy error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full dock/recall/persist/instance UX (SC1-SC8) on the live Mini PC.</what-built>
  <how-to-verify>
    Run CONTEXT.md "Verify" steps 1-6 on the live Mini PC (bruce@100.112.68.1):
    1. **SC2/SC5:** Open a native app → it shows in the Displays popover; the badge {n} reflects it.
    2. **SC3/SC4/SC5:** Drag a stream window up → it slides INTO the Displays button (animation);
       the navbar returns to normal (collapsed, NO wedged pill); the badge increments; the stream
       stays alive (recall shows live content, not a reconnecting/black canvas).
    3. **SC3:** Click the app icon OR the Displays button → the docked stream is recalled (re-opens
       with live content).
    4. **SC6:** Refresh the page → docked state persists, badge still shows {n}, streams recallable.
       (Dead pins from a livinityd restart should NOT dangle.)
    5. **SC7/SC8:** Click a WebApp (e.g. Facebook) twice → TWO separate streams. Click a native app
       twice → ONE window (the existing one re-focused/recalled).
    6. **SC1:** Confirm no app is stuck in "uninstalling" / "restarting" (trigger a real
       restart/uninstall → spinner shows then CLEARS; no permanently-wedged tile).
    7. **Phase 259 no-regression:** native apps still launch without crashing livinityd; check
       `journalctl -u livos --since "10 min ago"` for ENOENT or vnc-bridge logger crashes (should be none).
  </how-to-verify>
  <resume-signal>Type "approved" if all six verify steps pass and no Phase 259 regression. Otherwise list the specific failing step(s) so a --gaps plan can be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator SSH → Mini PC deploy | Existing trusted admin path (Tailscale + key); no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260-15 | (n/a) | deploy + live verify | accept | No code change; deploy uses the existing hardened update.sh; verification is observation-only on a single-user Mini PC |
</threat_model>

<verification>
- Both test suites green (Task 1)
- update.sh succeeded, both services active, SW cleared, new bundle serving
- All six CONTEXT verify steps pass; no Phase 259 regression in journalctl
</verification>

<success_criteria>
SC1-SC8 verified live on the Mini PC; phase shippable. Any failing verify step is captured for a
`/gsd-execute-phase 260 --gaps` follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-07-SUMMARY.md`
</output>
