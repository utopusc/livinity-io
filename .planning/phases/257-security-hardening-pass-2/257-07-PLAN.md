---
phase: 257-security-hardening-pass-2
plan: 07
type: execute
wave: 3
depends_on: ['257-01', '257-02', '257-03', '257-04', '257-05', '257-06']
files_modified:
  - .planning/phases/257-security-hardening-pass-2/257-DEPLOY-LOG.md
requirements: [LIVOS-005, LIVOS-006, LIVOS-010, LIVOS-011, LIVOS-012, LIVOS-015, LIVOS-020, LIVOS-021, LIVOS-023, LIVOS-024, LIVOS-026, LIVOS-027, LIVOS-028, LIVOS-030, LIVOS-031, LIVOS-032, LIVOS-033, LIVOS-034, LIVOS-035, LIVOS-036, LIVOS-038, LIVOS-039, LIVOS-040]
autonomous: false

# WAVE/DEPENDENCY NOTE:
# wave 3 (FINAL), depends_on ALL of 257-01..06 — this is the integration/deploy gate.
# It deploys the merged WS-A..F code to the Mini PC via update.sh and runs the per-WS
# synthetic probe walk. It is the ONLY 257 plan that touches the live box; all prior
# plans are local code + tests (matching the 256-01..04 'local-only, 256-05 deploys'
# pattern). Has a checkpoint:human-verify for the operator's per-WS probe walk ->
# autonomous: false. Touches only the deploy log (no source).

must_haves:
  truths:
    - "The merged 257 (WS-A..F) code is deployed to the Mini PC via update.sh with all 7 services active (no regression)."
    - "Each High/Medium WS has a passing synthetic probe on the live box (or a documented operator-verify checkpoint result)."
    - "Phase 256 gains, agent autonomy, and curated apps are intact post-deploy (regression guard)."
    - "The SC-E grep-clean invariant holds on the deployed tree."
  artifacts:
    - path: ".planning/phases/257-security-hardening-pass-2/257-DEPLOY-LOG.md"
      provides: "deploy transcript + per-WS probe results + any deploy-time hot-fixes"
      contains: "SC-"
  key_links:
    - from: "merged 257 code"
      to: "Mini PC (bruce@10.69.31.68)"
      via: "update.sh deploy + systemctl restart"
      pattern: "update.sh"
---

<objective>
WS-INTEGRATION — Deploy gate + per-WS probe walk. Push the merged WS-A..F changes, deploy them to the Mini PC via
`update.sh`, run the pre-deploy local gate (build + all 257 unit suites + bash -n on the scripts), then run the
per-WS synthetic probe walk on the live box and capture results + any deploy-time hot-fixes in a DEPLOY-LOG.

Purpose: prove the remaining-audit-findings sweep is live and regression-free on the only production deployment,
mirroring the 256-05 deploy-gate discipline.
Output: a 257-DEPLOY-LOG.md with the deploy transcript, per-WS probe results, and the regression checklist.

DEPLOY-TARGET RULE: Mini PC (bruce@10.69.31.68) ONLY. Server4/Server5 are off-limits. The platform/web +
platform/relay edits (WS-E Task 2) are SOURCE-ONLY and are NOT deployed here — note the operator should rotate the
Server5 platform DB password out of band if it was ever live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-security-hardening-pass-2/257-CONTEXT.md
@.planning/phases/256-security-hardening-contained-autonomy/256-05-SUMMARY.md
@.planning/phases/256-security-hardening-contained-autonomy/256-DEPLOY-LOG.md

<interfaces>
<!-- Deploy mechanics (from MEMORY + 256-05). -->
- Deploy: SSH to the Mini PC, run `bash /opt/livos/update.sh` (clones GitHub utopusc/livinity-io HEAD, rsyncs
  /opt/livos + /opt/liv, installs, builds @livos/config + UI + liv core/worker/mcp-server, restarts livos /
  liv-core / liv-worker / liv-memory / liv-assistant).
- SSH: `ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@10.69.31.68` (sudo for /opt).
- 256-05 caught 2 deploy-time bugs via the live walk (tinyproxy config quoting, bwrap AppArmor userns) — expect
  to catch + hot-fix similar live-only issues here and log them.
- Services that must be active post-deploy: livos, liv-core, liv-worker, liv-memory, liv-assistant, livos-egress,
  tinyproxy (+ liv-claw-gateway).
- update.sh now has the WS-B verify-before-deploy guard (257-01) — on the current unpinned box it WARNS and
  proceeds (opt-in-strict); confirm it does not block the deploy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-deploy local gate — build + all 257 unit suites + script lint</name>
  <files>.planning/phases/257-security-hardening-pass-2/257-DEPLOY-LOG.md</files>
  <read_first>
    - the 6 plan SUMMARYs (257-01..06) for the exact test commands each used
  </read_first>
  <action>
Before any deploy, run the full local gate and record PASS/FAIL in the DEPLOY-LOG:
1. Builds: `cd liv && npx tsc --noEmit -p packages/core`; `cd livos && npx tsc --noEmit -p packages/livinityd`
   (expect the pre-existing deferred tsc errors from 256-02's deferred-items.md to remain — do NOT treat those as
   257 regressions; only NEW errors fail the gate).
2. Unit suites (all 257): liv tsx suites (skill-signature, mcp-ssrf-guard); livos vitest suites (server bind,
   app-repository, tools.sandbox, sessions, is-authenticated, files, jwt, registry-credentials, approvals-routes,
   caddy). Run each per its plan's verify command.
3. Script lint: `bash -n update.sh install.sh livos/install.sh scripts/install/deploy-livinityd.sh livos/setup.sh`
   (+ setup-server4.sh if not deleted).
4. SC-E invariant: `git grep -nE 'LivRedis2024|LivPostgres2024|LivPlatform2024|NexusRedis2024|NexusDB2024|liv:liv@'
   -- ':!.planning/' ':!SECURITY-AUDIT.md'` -> MUST be empty.
Record the full pass matrix. If anything fails, STOP — the merged tree is not deploy-ready (fix or escalate).
  </action>
  <verify>
    <automated>git grep -nE 'LivRedis2024|LivPostgres2024|LivPlatform2024|NexusRedis2024|NexusDB2024|liv:liv@' -- ':!.planning/' ':!SECURITY-AUDIT.md'; echo "exit=$? (1/empty = clean)"</automated>
  </verify>
  <done>All 257 builds + unit suites + script lints PASS (no NEW tsc errors vs the 256 deferred baseline); the SC-E grep-clean invariant is empty; the pass matrix is recorded in the DEPLOY-LOG.</done>
</task>

<task type="auto">
  <name>Task 2: push + deploy to the Mini PC + confirm services active</name>
  <files>.planning/phases/257-security-hardening-pass-2/257-DEPLOY-LOG.md</files>
  <read_first>
    - 256-DEPLOY-LOG.md (the update.sh-x2 + service-check pattern + the 2 hot-fix shape)
  </read_first>
  <action>
1. Push all 257 commits to origin/master (the merged WS-A..F work).
2. SSH to the Mini PC and run `bash /opt/livos/update.sh`. Capture the transcript. Confirm the WS-B verify guard
   warns-and-proceeds (does NOT block on the unpinned box). If update.sh fails on a live-only issue (apt delay,
   build, a 257 change that mis-deploys), diagnose + hot-fix in-repo (commit + re-push + re-run update.sh), exactly
   as 256-05 did — log each hot-fix with its commit SHA.
3. Confirm services active: `systemctl is-active livos liv-core liv-worker liv-memory liv-assistant livos-egress
   tinyproxy`. All must be active. Record the deployed SHA (`git -C /opt/... rev-parse HEAD` or the update.sh log).
  </action>
  <verify>
    <automated>echo "Deploy is operator-run on the Mini PC; this task records the transcript + service-active matrix + deployed SHA in 257-DEPLOY-LOG.md. Verified by Task 3 probes."</automated>
  </verify>
  <done>257 pushed; update.sh deployed to the Mini PC (verify guard warned-not-blocked); all 7+ services active; deployed SHA + any deploy-time hot-fixes recorded in the DEPLOY-LOG.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: operator per-WS synthetic probe walk + regression guard</name>
  <what-built>
The merged 257 security sweep is deployed to the Mini PC. This checkpoint walks the per-WS probes so the operator
confirms each finding is closed live and that Phase 256 gains / agent autonomy / curated apps did not regress.
  </what-built>
  <how-to-verify>
On the Mini PC (or via the LivOS UI), run these probes and confirm the expected result:

WS-A (session/token):
1. Log in a guest, deactivate it (or change its password), replay the SAME JWT against an admin route ->
   expect UNAUTHORIZED (LIVOS-005).
2. In multi-user mode, GET /api/files with the proxy cookie but NO LIVINITY_SESSION -> expect no admin-tree
   access (LIVOS-006).
3. Inspect the login Set-Cookie -> LIVINITY_SESSION has NO Domain=.livinity.io (host-only) (LIVOS-023).
4. Decode a session JWT -> has aud/iss=livinityd; a proxy token is not interchangeable (LIVOS-028).

WS-B (supply-chain):
5. `LIVOS_EXPECTED_SHA=deadbeef sudo bash /opt/livos/update.sh` -> expect "Refusing to deploy" (LIVOS-011);
   unset -> warns + proceeds. (Use a scratch checkout — do NOT actually redeploy a wrong SHA to prod.)
6. A skill manifest pointing at a tampered index.js (checksum mismatch) -> refused, no import (LIVOS-012).

WS-C (network/SSRF):
7. `ss -tlnp | grep 8080` -> 127.0.0.1:8080 (not *:8080); `curl http://<LAN-IP>:8080/` refused; the public Caddy
   host still loads (LIVOS-015).
8. apps.addRepository('http://169.254.169.254/...') -> rejected; a public github repo -> accepted (LIVOS-024).
9. An MCP streamableHttp config with a rebinding/[::ffff:127.0.0.1] URL -> connect refused (LIVOS-038).

WS-D (luse):
10. Drive luse computer_read_file on /home/bruce/.gemini/oauth_creds.json (and ~/.claude/.credentials.json) ->
    DENIED; a Downloads file -> succeeds (LIVOS-010).

WS-E (secrets):
11. stat /opt/livos/data/secrets/share-password -> 600 (LIVOS-039 lands here too); stat the luse scratch HOME ->
    0700 (LIVOS-034); the credential-dek file exists 0600 + an existing registry cred still decrypts (LIVOS-033).
12. `git grep` for the default passwords on the deployed tree -> empty (SC-E).

WS-F (Low/Info):
13. A guest token to /openclawos/approvals/stream -> 401; admin -> 200 (LIVOS-027).
14. Install an app whose compose sets a hostile OD_API_TOKEN -> the Caddyfile has no injected block + `caddy
    validate` passes (LIVOS-035).
15. unset LIV_API_KEY on a scratch memory service -> /api 503 (LIVOS-029 already closed by 256-04).

REGRESSION GUARD (must all still work):
16. Liv AI chat round-trips (agent autonomy intact — the irreversible-classifier gate from 256 does not block
    ordinary ops); a curated app (OpenDesign) still loads with a valid session + reaches the model via the
    cred-proxy; the public Caddy host serves the LivOS UI; the openclaw/loopback service path still reaches admin
    routes (256-04 fix E).

Reply "approved" if all probes pass, or describe any failures (which become a 257 gap-closure pass).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing probe(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged source -> live Mini PC | the deploy is the moment all WS changes take effect on production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257I-01 | Availability (regression) | a 257 fail-closed change breaks a live path | mitigate | pre-deploy local gate + per-WS regression guard probes (Tasks 1/3); hot-fix loop like 256-05 (Task 2) |
| T-257I-02 | Tampering | deploy itself uses unpinned update.sh | accept (this run) | WS-B guard is opt-in-strict; the deploy box is operator-driven; full pin is a follow-up once EXPECTED_RELEASE ships |
</threat_model>

<verification>
- Pre-deploy local gate green (builds + all 257 unit suites + bash -n + SC-E grep-clean).
- update.sh deploy succeeds; 7+ services active; deployed SHA recorded.
- Operator per-WS probe walk: all probes (1-15) pass; regression guard (16) clean.
- Any failure -> a 257 gap-closure pass (re-plan with --gaps), not a silent close.
</verification>

<success_criteria>
257 is live on the Mini PC with every High/Medium/Low finding's probe passing (or an explicit operator-accepted
note), Phase 256 gains + agent autonomy + curated apps intact, and the SC-E grep-clean invariant holding on the
deployed tree. DEPLOY-LOG captures the transcript, per-WS results, and any hot-fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/257-security-hardening-pass-2/257-07-SUMMARY.md`
(and maintain `.planning/phases/257-security-hardening-pass-2/257-DEPLOY-LOG.md` during the deploy).
</output>
