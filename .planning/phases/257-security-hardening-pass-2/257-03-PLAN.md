---
phase: 257-security-hardening-pass-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts
  - livos/packages/livinityd/source/modules/computer-use/mcp/tools.sandbox.test.ts
requirements: [LIVOS-010]
autonomous: true

# WAVE/DEPENDENCY NOTE (one-writer-per-file-per-wave):
# wave 1, depends_on [] — WS-D touches ONLY computer-use/mcp/tools.ts. No other
# 257 plan writes this file (zero overlap). Runs in parallel with WS-B (257-01),
# WS-C (257-02), and WS-A (257-04). Small/focused single-finding plan.

must_haves:
  truths:
    - "luse computer_read_file CANNOT read the operator's AI-provider credentials under /home/<slug>/ — ~/.claude, ~/.gemini, ~/.kimi, ~/.ssh, ~/.config are denied even though they sit inside the home prefix (LIVOS-010)."
    - "Legitimate luse reads (Downloads, livos-files, the uploads dir, the runtime tmp prefix) still succeed — no functional regression to the computer-use agent's file access."
  artifacts:
    - path: "livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts"
      provides: "isPathAllowed enforces a credential/dotfile DENYLIST (post-realpath) within the allowed home prefix"
      contains: "SENSITIVE"
  key_links:
    - from: "computer_read_file"
      to: "isPathAllowed denylist"
      via: "post-realpath credential-dir rejection"
      pattern: "\\.claude|\\.gemini|\\.ssh|SENSITIVE"
---

<objective>
WS-D — luse file exposure. Close LIVOS-010: the luse `computer_read_file` sandbox allows the whole
`/home/${userSlug}/` prefix (userSlug defaults to `bruce`), so a computer-use agent driven by injected on-screen
or web content can read the operator's live AI-provider OAuth credentials — `~/.claude/.credentials.json`,
`~/.gemini/oauth_creds.json`, `~/.kimi/credentials/*` — and exfiltrate the tokens. The realpath step closes
symlink-escape but does NOT exclude sensitive dotfiles WITHIN the allowed home. Per the operator decision: mirror
the 256 files-allowlist shape and add an explicit credential-dir denylist enforced after realpath.

Purpose: an injected instruction can no longer turn the computer-use agent into a token-exfiltration primitive,
while ordinary luse file reads (Downloads / livos-files / uploads) keep working.
Output: an `isPathAllowed` that denies credential/secret dotfiles after the realpath + allowlist check.

STILL-EXISTS VERIFICATION (read at plan time, current code):
- LIVOS-010: computer-use/mcp/tools.ts:512-528 `isPathAllowed` allowlist = `[ \`/home/${userSlug}/\`,
  LUSE_TMP_PREFIX, \`${LIVOS_ROOT}/data/uploads/${userId}/\` ]` then `allowlist.some(p => resolved.startsWith(p))`.
  The whole-home prefix has NO dotfile/credential exclusion. CONFIRMED OPEN (256 did not touch this file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-security-hardening-pass-2/257-CONTEXT.md
@SECURITY-AUDIT.md
@.planning/phases/256-security-hardening-contained-autonomy/256-01-SUMMARY.md

<interfaces>
<!-- The exact luse sandbox to tighten. Mirror 256-01's files-sandbox denylist shape. -->

From computer-use/mcp/tools.ts (LIVOS-010):
```typescript
:512 export function isPathAllowed(resolved: string, userSlug: string, userId: string): boolean {
:517   const allowlist = [
         `/home/${userSlug}/`,           // <- whole home, includes ~/.claude ~/.gemini ~/.kimi ~/.ssh
         LUSE_TMP_PREFIX,                 // ${XDG_RUNTIME_DIR}/luse-
         `${LIVOS_ROOT}/data/uploads/${userId}/`,
       ];
:527   return allowlist.some((prefix) => resolved.startsWith(prefix));   // allow-only, NO denylist
     }
// computer_read_file (:944-1010) realpaths the path then calls isPathAllowed; returns base64 to the model.
```

256-01 precedent (files-sandbox.ts isFilePathAllowed): "deny wins" — an explicit denylist for `.env`/secrets/
`~/.ssh|.claude|.gemini|.kimi` evaluated AFTER the allowlist, deny taking precedence. Mirror that here.

livinityd tests use vitest (livos/ has vitest@2.1.9 — `npx vitest run` from packages/livinityd).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deny credential/secret dotfiles within the luse home allowlist (LIVOS-010 / SC-D)</name>
  <files>livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts, livos/packages/livinityd/source/modules/computer-use/mcp/tools.sandbox.test.ts</files>
  <read_first>
    - computer-use/mcp/tools.ts:505-528 (isPathAllowed) + :944-1010 (computer_read_file realpath → isPathAllowed → base64)
    - SECURITY-AUDIT.md:253-265 (LIVOS-010 — narrow the home prefix OR add a credential-dir denylist after realpath)
    - (reference) the 256-01 files-sandbox.ts "deny wins" denylist shape (already merged in liv/)
  </read_first>
  <behavior>
    - Test 1 (creds denied): isPathAllowed('/home/bruce/.claude/.credentials.json','bruce',uid) → false.
    - Test 2 (gemini denied): '/home/bruce/.gemini/oauth_creds.json' → false.
    - Test 3 (kimi/ssh/config denied): '/home/bruce/.kimi/credentials/x.json', '/home/bruce/.ssh/id_ed25519',
      '/home/bruce/.config/anything' → false.
    - Test 4 (legit home read allowed): '/home/bruce/Downloads/report.pdf' and '/home/bruce/livos-files/a.txt' → true.
    - Test 5 (uploads + tmp still allowed): a path under `${LIVOS_ROOT}/data/uploads/<uid>/` → true; a LUSE_TMP_PREFIX
      path → true.
    - Test 6 (traversal/realpath guard intact): '/home/bruce/Downloads/../.claude/.credentials.json' (the
      pre-realpath form) — assert the denylist matches the realpathed `/home/bruce/.claude/...` target → false
      (the production caller realpaths first; the unit passes the realpathed string to mirror that).
  </behavior>
  <action>
1. In `isPathAllowed`, AFTER the existing allowlist `.some(...)` admit check, add a credential/secret DENYLIST that
   takes precedence (deny wins). Define `const SENSITIVE_HOME_DIRS = ['.claude','.gemini','.kimi','.ssh','.config']`
   and reject when `resolved` is, or is nested under, `/home/${userSlug}/<dir>/` for any dir in that set (also deny
   a bare file match like `/home/${userSlug}/.claude.json` — the Claude Code project config Audit LIVOS-034 flags —
   so a poisoned MCP/settings config can't be read either). Use path-boundary semantics (the POSIX-normalized
   compare 256-01 used) so `.claudeX` is NOT falsely denied. Return false on any denylist hit; otherwise return the
   allowlist result. Keep the function pure (no fs/realpath inside — the caller still realpaths before calling).
2. Do NOT change `computer_read_file` realpath/base64 flow beyond it already calling isPathAllowed — the realpath
   step (which closes symlink-escape) stays; the new denylist is what closes the in-home credential read.
  </action>
  <verify>
    <automated>cd livos && npx vitest run packages/livinityd/source/modules/computer-use/mcp/tools.sandbox.test.ts</automated>
  </verify>
  <done>isPathAllowed denies ~/.claude/.gemini/.kimi/.ssh/.config (and ~/.claude.json) within the home prefix while still allowing Downloads/livos-files/uploads/tmp; deny takes precedence; path-boundary-safe (no .claudeX false-deny); tests pass (6 cases).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected on-screen/web content → luse agent | the computer-use agent acts on attacker-influenced instructions |
| luse computer_read_file → host filesystem | the tool returns file bytes (base64) to the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257D-01 | Information Disclosure | computer_read_file reads ~/.claude/.gemini/.kimi creds | mitigate | post-realpath credential-dir denylist in isPathAllowed (Task 1) |
| T-257D-02 | Tampering | poisoned ~/.claude.json read back to steer the CLI | mitigate | bare-file denylist entry for .claude.json (Task 1) |
| T-257D-03 | Availability (false-deny) | legit Downloads/uploads reads blocked | mitigate | path-boundary compare + explicit allow tests (Task 1, Tests 4/5) |
</threat_model>

<verification>
- SC-D probe: on the Mini PC, drive luse `computer_read_file{path:'/home/bruce/.gemini/oauth_creds.json'}`
  (and `~/.claude/.credentials.json`) → DENIED; `computer_read_file{path:'/home/bruce/Downloads/<file>'}` → succeeds.
- Unit suite green: tools.sandbox.test.ts (6 cases).
</verification>

<success_criteria>
SC-D met: a luse read of `~/.claude/.credentials.json` (and `.gemini`/`.kimi`/`.ssh`/`.config`/`.claude.json`) is
denied; legitimate home/uploads/tmp reads still work; deny precedence + path-boundary safety unit-proven.
</success_criteria>

<output>
After completion, create `.planning/phases/257-security-hardening-pass-2/257-03-SUMMARY.md`
</output>
