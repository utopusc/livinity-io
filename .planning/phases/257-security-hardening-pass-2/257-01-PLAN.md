---
phase: 257-security-hardening-pass-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - liv/packages/core/src/skill-loader.ts
  - liv/packages/core/src/skill-signature.ts
  - liv/packages/core/src/skill-signature.test.ts
  - update.sh
  - install.sh
  - livos/install.sh
  - README.md
requirements: [LIVOS-011, LIVOS-012, LIVOS-026, LIVOS-040]
autonomous: true

# WAVE/DEPENDENCY NOTE (one-writer-per-file-per-wave):
# wave 1, depends_on [] — WS-B is the supply-chain workstream. It is the SOLE
# writer of update.sh (LIVOS-011 commit-pin + LIVOS-040 apt-pin folded together
# so update.sh is single-writer in ONE plan), install.sh / livos/install.sh
# (LIVOS-026 checksum), and skill-loader.ts (LIVOS-012). No other 257 plan
# touches these files. WS-C also edits deploy-livinityd.sh + server/index.ts but
# NOT update.sh/install.sh, so WS-B and WS-C run in the same wave with zero file
# overlap. LIVOS-040 (apt-pin) was moved here from WS-F so update.sh stays
# single-writer (WS-F's update.sh concern is fully absorbed by Task 4).

must_haves:
  truths:
    - "update.sh REFUSES to rsync/build/restart when the fetched HEAD is not the expected pinned/signed ref (LIVOS-011)."
    - "A DOWNLOADED MARKETPLACE skill bundle's index.js is NOT import()-ed unless it is trusted-by-origin (the pinned official registry) OR its checksum verifies; BUILTIN (path-bundled, first-party) skills are trusted-by-origin and STILL LOAD (LIVOS-012)."
    - "The curl|bash install path verifies a checksum/signature of the cloned tree before it runs anything destructive (LIVOS-026)."
    - "update.sh no longer runs a blanket unpinned `apt-get install` on every run — the heavy package set installs only when a binary is actually missing (LIVOS-040)."
  artifacts:
    - path: "update.sh"
      provides: "commit-pin/allowlist verification of the fetched ref before rsync/build/restart + guarded apt install"
      contains: "LIVOS_UPDATE_TO_SHA"
    - path: "liv/packages/core/src/skill-signature.ts"
      provides: "verifySkillBundle() — origin/checksum gate called before the MARKETPLACE loadSkillLazy import() ONLY (builtin path is exempt)"
      contains: "verify"
    - path: "install.sh"
      provides: "checksum/signature verification of the cloned tree before re-exec"
      contains: "sha256"
  key_links:
    - from: "skill-loader.ts loadSkillLazy (marketplace path ONLY)"
      to: "skill-signature.ts verifySkillBundle"
      via: "pre-import gate (marketplace), builtin loadSkill/loadAll NOT gated"
      pattern: "verifySkillBundle|verifySkill"
    - from: "update.sh fetched ref"
      to: "expected pinned/signed SHA"
      via: "verify-before-deploy guard"
      pattern: "LIVOS_UPDATE_TO_SHA|verify"
---

<objective>
WS-B — Supply-chain integrity. Close LIVOS-011 (update.sh, run as root, clones + builds + restarts whatever
GitHub HEAD serves with NO commit pin / signature — repo compromise or TLS MITM = root RCE; the captured
`LIVOS_UPDATE_TO_SHA` is used only for a log filename), LIVOS-012 (the skill MARKETPLACE `import()`s an arbitrary
DOWNLOADED `index.js` in-process with cosmetic permissions), LIVOS-026 (the documented `curl|bash` installer
runs off the network with no checksum/signature), and LIVOS-040 (update.sh runs a blanket unpinned
`apt-get install` on every run).

Purpose: a compromised/forged remote can no longer push code that the operator's next Update silently runs as
root, and a benign-looking DOWNLOADED community skill can no longer execute attacker code on import — while the
first-party BUILTIN skills that ship inside the repo tree keep loading unchanged (trusted by origin).
Output: a verify-before-deploy guard in update.sh, a signature/checksum gate in front of the MARKETPLACE skill
import (builtin path exempt), an integrity check in the install path, and a guarded (not-every-run) apt install.

STILL-EXISTS VERIFICATION (read at plan time, current code):
- LIVOS-011: update.sh:342 `git clone --depth 1 "$REPO_URL"` then :344 `LIVOS_UPDATE_TO_SHA=$(git ... rev-parse
  HEAD)` — used ONLY at :131-132/:157 for the log filename + JSON field, never compared. CONFIRMED OPEN.
- LIVOS-012: skill-loader.ts:80 (BUILTIN loadSkill — `this.skillsDir`, path-based, source:'builtin') + :461
  (MARKETPLACE loadSkillLazy — downloaded bundle, source:'marketplace') both `const mod = await import(moduleUrl)`
  with no verification. ONLY the :461 marketplace path imports DOWNLOADED code; :80 imports first-party bundled
  skills. CONFIRMED OPEN — gate the :461 marketplace path ONLY (see Task 3 scoping note).
- LIVOS-040: update.sh:359/:459/:465 `DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \ …` unpinned set
  on every run. CONFIRMED OPEN.
- LIVOS-026: README.md documents `curl -fsSL …| bash` / `| sudo bash`; install.sh (root) + livos/install.sh
  self-clone + re-exec with no verify. CONFIRMED OPEN (both files exist).
- LIVOS-012 SCOPE NOTE (revised after re-read): SkillManifest / scanSkillDirectory carry NO checksum / SKILL.lock /
  registryUrl field today, and there is NO exported pinned official-registry constant (the default is
  `process.env.SKILL_REGISTRY_URL || 'https://github.com/utopusc/livinity-skills'`, set at index.ts:455). A
  fail-closed-by-default gate on the BUILTIN :80 path would refuse EVERY first-party bundled skill at import = a
  live regression. The fix MUST scope the gate to the MARKETPLACE path only and treat the builtin skillsDir as
  trusted-by-origin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-security-hardening-pass-2/257-CONTEXT.md
@SECURITY-AUDIT.md
@.planning/phases/256-security-hardening-contained-autonomy/256-01-SUMMARY.md

<interfaces>
<!-- The exact supply-chain gaps WS-B fixes. Use these directly; do not re-explore. -->

From update.sh (LIVOS-011 + LIVOS-040):
```bash
:61   LIVOS_UPDATE_TO_SHA=""                                   # captured but never compared
:342  git clone --depth 1 "$REPO_URL" "$TEMP_DIR" || fail "Failed to clone repository"
:344  LIVOS_UPDATE_TO_SHA=$(git -C "$TEMP_DIR" rev-parse HEAD 2>/dev/null || echo "")   # log only
:359  DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \  # blanket, every run (also :459, :465)
```

From skill-loader.ts (LIVOS-012 — TWO distinct import sites; gate ONLY the marketplace one):
```typescript
// BUILTIN path — loadAll()/loadSkill() over this.skillsDir (first-party, ships in the repo tree):
:67   private async loadSkill(filePath: string)                 // called from loadAll over this.skillsDir
:78   const jsPath = filePath.endsWith('.ts') ? filePath.replace(/\.ts$/, '.js') : filePath;
:79   const moduleUrl = pathToFileURL(jsPath).href + `?t=${Date.now()}`;
:80   const mod = await import(moduleUrl);        // <- TRUSTED BY ORIGIN (path-bundled). DO NOT gate.

// MARKETPLACE path — loadSkillLazy() over a DOWNLOADED bundle dir (source:'marketplace'):
:448  async loadSkillLazy(skillName: string, dirPath: string)
:450  const scanResult = await scanSkillDirectory(dirPath);     // { manifest, entryPoint }
:459  const jsPath = entryPoint.endsWith('.ts') ? entryPoint.replace(/\.ts$/, '.js') : entryPoint;
:461  const mod = await import(moduleUrl);        // <- executes DOWNLOADED code. GATE THIS ONE.
```

From skill-manifest.ts / skill-registry-client.ts (the verifiable-signal reality):
```typescript
// SkillManifest has NO checksum / SKILL.lock / registryUrl field. scanSkillDirectory returns { manifest, entryPoint } only.
// No exported OFFICIAL-registry constant. The default registry is set in index.ts:455:
//   const defaultSkillRegistry = process.env.SKILL_REGISTRY_URL || 'https://github.com/utopusc/livinity-skills';
// SkillInstaller (skill-installer.ts:122) downloads via registryClient.downloadSkill(entry, installDir) and records
// NO checksum. So today the ONLY trustworthy signal available for a marketplace bundle is its REGISTRY OF ORIGIN.
```

From install.sh / livos/install.sh (LIVOS-026): self-clone of utopusc/livinity-io then re-exec; README.md
documents `curl -fsSL https://get.livinity.io | bash` and `curl -fsSL https://livinity.io/install.sh | sudo
bash -s <API_KEY>`.

Pattern to reuse: 256-01 installed scripts emit byte-identical config via heredocs guarded by `|| warn`
(non-fatal). Tests in liv/ use tsx + node:assert/strict (vitest is NOT installed in liv/ — match neighbours
like sandbox.test.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: update.sh — verify the fetched ref against a pinned/signed expected SHA before any destructive step (LIVOS-011 / SC-B)</name>
  <files>update.sh</files>
  <read_first>
    - update.sh:55-75 (the LIVOS_UPDATE_* var region), :330-350 (clone + rev-parse), the first rsync/build/restart
      site AFTER the clone (search for the first `rsync` to `/opt/livos` or `/opt/liv`)
    - SECURITY-AUDIT.md:269-281 (LIVOS-011 — pin/verify-tag recommendation; `LIVOS_UPDATE_TO_SHA` only used for
      log filename today)
  </read_first>
  <action>
After the `git clone` + `rev-parse HEAD` (:342-344), ADD a verify-before-deploy guard, BEFORE the first rsync
into /opt/livos or /opt/liv:
1. Resolve an EXPECTED ref from, in priority order: (a) the env var `LIVOS_EXPECTED_SHA` if set; (b) a
   repo-shipped pin file `scripts/install/EXPECTED_RELEASE` (a single SHA or `refs/tags/<tag>` line) cloned in
   the tree; (c) if a maintainer GPG key is present at `scripts/install/maintainer.gpg` AND the cloned HEAD is a
   tag, run `git -C "$TEMP_DIR" verify-tag` (or `verify-commit`) and accept on success.
2. If an expected SHA is resolved and `LIVOS_UPDATE_TO_SHA` != it (string compare), `fail "Refusing to deploy:
   fetched HEAD <to_sha> does not match the expected pinned ref <expected>"` — exit non-zero BEFORE rsync.
3. If NEITHER a pin file NOR a maintainer key NOR `LIVOS_EXPECTED_SHA` is present, do NOT hard-fail (the Mini PC
   today has no pin shipped) — instead `warn "update.sh: no commit pin / signature available — deploying
   unverified HEAD <to_sha> (set LIVOS_EXPECTED_SHA or ship scripts/install/EXPECTED_RELEASE to enforce)"`. This
   makes the guard OPT-IN-strict but always loud, so a pinned operator gets fail-closed protection while the
   current unpinned box still updates (no deploy regression). The win: the moment an EXPECTED_RELEASE is shipped
   (a follow-up commit), a forged HEAD is refused.
Keep `LIVOS_UPDATE_TO_SHA` populated as before for the log filename. Script must pass `bash -n`.
  </action>
  <verify>
    <automated>bash -n update.sh && grep -c 'LIVOS_EXPECTED_SHA\|EXPECTED_RELEASE\|verify-tag\|Refusing to deploy' update.sh</automated>
  </verify>
  <done>update.sh resolves an expected ref (env/pin-file/signed-tag) and refuses (exit non-zero, before rsync) when the fetched HEAD mismatches; warns loudly + proceeds only when no pin material exists; `bash -n` clean.</done>
</task>

<task type="auto">
  <name>Task 2: update.sh — guard the apt install so the unpinned heavy set runs only when a binary is missing (LIVOS-040 / SC-B)</name>
  <files>update.sh</files>
  <read_first>
    - update.sh:353-385 (the streaming apt block + its `verify loop` that already detects installed binaries),
      :455-470 (the other two apt-get install sites)
    - SECURITY-AUDIT.md:726-739 (LIVOS-040 — move to one-time / skip when binaries present / pin)
  </read_first>
  <action>
For each `apt-get install -y -qq \ …` block (:359, :459, :465), wrap it so it runs ONLY when at least one of the
named binaries is absent: before the install, `command -v` each key binary (x11vnc/ffmpeg/websockify/xvfb/fluxbox
/tint2 etc — the ones the existing verify loop already checks) and SKIP the apt-get entirely when all are present
(`log "update.sh: streaming deps already present — skipping apt install"`). When some are missing, run the install
as today (still `|| warn`, non-fatal). Do NOT change the package list or add a new apt source (no behavior change
beyond conditionality — version pinning of these OS packages is out of scope, noted in the SUMMARY). The 256
streaming-deps + bubblewrap/tinyproxy adds from 256-01 stay inside their existing block; only the run-condition
changes. Script must pass `bash -n`.
  </action>
  <verify>
    <automated>bash -n update.sh && grep -c 'already present — skipping apt\|command -v' update.sh</automated>
  </verify>
  <done>The 3 apt-get install blocks are gated on a missing-binary check (skip when all present); package lists unchanged; `bash -n` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MARKETPLACE skill origin/checksum gate before in-process import — builtin path EXEMPT (LIVOS-012 / SC-B)</name>
  <files>liv/packages/core/src/skill-signature.ts, liv/packages/core/src/skill-signature.test.ts, liv/packages/core/src/skill-loader.ts</files>
  <read_first>
    - liv/packages/core/src/skill-loader.ts:66-95 (BUILTIN loadSkill import() site over this.skillsDir — DO NOT
      gate this one) + :448-466 (MARKETPLACE loadSkillLazy import() site + scanSkillDirectory entryPoint — GATE
      this one)
    - liv/packages/core/src/skill-manifest.ts (confirm: SkillManifest has NO checksum/registryUrl field;
      scanSkillDirectory returns { manifest, entryPoint })
    - liv/packages/core/src/skill-registry-client.ts + skill-installer.ts (confirm: no checksum is recorded at
      download; the registry-of-origin is the only trust signal available)
    - liv/packages/core/src/index.ts:455 (the official-registry default `SKILL_REGISTRY_URL ||
      'https://github.com/utopusc/livinity-skills'`)
    - SECURITY-AUDIT.md:285-300 (LIVOS-012 — do not import() untrusted DOWNLOADED code; verify before load)
  </read_first>
  <behavior>
    <!-- IMPORTANT: this gate is for the MARKETPLACE (downloaded) path ONLY. The BUILTIN loadSkill/loadAll path is
         trusted-by-origin (first-party bundled in the repo tree) and is NOT gated — see the builtin test below. -->
    - Test 1 (builtin NOT gated): verifySkillBundle({ entryPath, origin: 'builtin' }) → ok=true WITHOUT any
      checksum/registry — a path-bundled first-party skill always loads. (Guards the live-regression: bundled
      skills must keep loading.)
    - Test 2 (official-registry trusted-by-origin): verifySkillBundle({ entryPath, origin: 'marketplace',
      registryUrl: OFFICIAL }) → ok=true without requiring a checksum (the pinned official registry is trusted by
      origin). OFFICIAL = the same default the loader uses (`SKILL_REGISTRY_URL` ||
      'https://github.com/utopusc/livinity-skills'); import/derive it, do NOT re-hardcode a second copy.
    - Test 3 (marketplace checksum match): a marketplace bundle WITH a `manifestChecksum` that matches the SHA-256
      of the entry file → ok=true (forward-compat for when the installer starts recording a checksum).
    - Test 4 (marketplace checksum mismatch): tampered index.js whose SHA-256 ≠ the provided checksum → ok=false
      with a clear reason (caller must refuse the import).
    - Test 5 (marketplace, non-official registry, no checksum): community registry + no checksum → ok=false
      (fail-closed: an unverifiable DOWNLOADED bundle is NOT imported).
  </behavior>
  <action>
1. Create `skill-signature.ts` exporting `verifySkillBundle(opts: { entryPath: string; origin: 'builtin' |
   'marketplace'; registryUrl?: string; manifestChecksum?: string }): Promise<{ ok: boolean; reason?: string }>`:
   - If `origin === 'builtin'` → `{ ok: true }` IMMEDIATELY (path-bundled first-party = trusted by origin; never
     gated). This is the regression guard — builtin skills must always load.
   - Else (origin 'marketplace'):
     - If `registryUrl` matches the pinned OFFICIAL registry (derive the official URL from the SAME source the
       loader/index uses — `process.env.SKILL_REGISTRY_URL || 'https://github.com/utopusc/livinity-skills'`,
       normalized like SkillRegistryClient does: strip trailing `/` and `.git`) → `{ ok: true }` (trusted by
       origin).
     - Else if `manifestChecksum` is present → SHA-256 the entry file bytes and constant-time compare → ok on
       match, `{ ok:false, reason:'checksum mismatch' }` otherwise.
     - Else → `{ ok:false, reason:'unverifiable marketplace skill bundle (non-official registry, no checksum)' }`.
   Pure + async file read only (no network, no exec) so it is unit-testable offline.
2. In `skill-loader.ts`, gate ONLY the MARKETPLACE `loadSkillLazy` import (:461). BEFORE `await import(moduleUrl)`,
   call `verifySkillBundle({ entryPath: jsPath, origin: 'marketplace', registryUrl: <the registry this bundle came
   from, if resolvable from the install metadata>, manifestChecksum: <from the manifest if present> })` and, on
   `ok===false`, log an error + `return false` WITHOUT importing. If the registry-of-origin is not threaded into
   loadSkillLazy today, resolve it from the same install metadata the installer persists (Redis
   `liv:skills:installed:{name}` / the registry default) — if it is genuinely unavailable, pass
   `registryUrl: undefined` so an unverifiable bundle fails closed (the correct safe default for a DOWNLOADED
   bundle). Do NOT touch the BUILTIN `loadSkill` import at :80 — leave it exactly as-is (no verifySkillBundle call)
   so loadAll over `this.skillsDir` keeps loading first-party skills. (Optionally call `verifySkillBundle({
   origin:'builtin' })` at :80 for symmetry, but it MUST short-circuit ok=true and never block — only add it if it
   provably cannot regress.)
  </action>
  <verify>
    <automated>cd liv && npx tsx packages/core/src/skill-signature.test.ts</automated>
  </verify>
  <done>verifySkillBundle returns ok for ANY builtin-origin bundle, for the official marketplace registry, and for a matching marketplace checksum; it fails closed for a non-official marketplace bundle with no/with-mismatched checksum; skill-loader gates ONLY the marketplace loadSkillLazy import (no `await import` on ok===false) and leaves the BUILTIN loadSkill import untouched so first-party bundled skills still load; tests pass (5 cases incl. the builtin-loads regression case).</done>
</task>

<task type="auto">
  <name>Task 4: installer integrity — verify the cloned tree before re-exec + document verify-then-run (LIVOS-026 / SC-B)</name>
  <files>install.sh, livos/install.sh, README.md</files>
  <read_first>
    - install.sh (the self-clone + re-exec region — search `git clone` then the exec/`bash` of the cloned entry)
    - livos/install.sh (same self-clone shape)
    - README.md (the `curl … | bash` documented install entrypoints)
    - SECURITY-AUDIT.md:510-522 (LIVOS-026 — detached GPG sig + SHA256 + verify-then-run)
  </read_first>
  <action>
1. In `install.sh` + `livos/install.sh`, after the self-clone and BEFORE re-exec of the cloned entry script,
   add an OPT-IN integrity check: if `LIVOS_INSTALL_EXPECTED_SHA` is set in the env, `git -C <clone> rev-parse
   HEAD` and `fail` on mismatch; if a `scripts/install/EXPECTED_RELEASE` pin file is present in the clone,
   compare against it; otherwise `warn "install.sh: running unverified HEAD — set LIVOS_INSTALL_EXPECTED_SHA to
   pin"` and proceed (same opt-in-strict shape as Task 1 — no install regression, but a loud, enforceable hook).
   Also compute + log the `sha256sum` of the cloned entry script so the operator can record/compare it.
2. In `README.md`, under the install section, ADD a "Verify before running" subsection documenting the
   verify-then-run flow: download the script, print/compare its `sha256sum` against the published checksum, then
   run — alongside the existing one-liner (keep the one-liner; add the verified alternative + the
   `LIVOS_INSTALL_EXPECTED_SHA` env knob).
Both scripts must pass `bash -n`.
  </action>
  <verify>
    <automated>bash -n install.sh && bash -n livos/install.sh && grep -c 'LIVOS_INSTALL_EXPECTED_SHA\|sha256sum' install.sh && grep -ci 'verify before' README.md</automated>
  </verify>
  <done>install.sh + livos/install.sh verify an opt-in pinned SHA before re-exec (warn+proceed when unset) and log the entry-script sha256; README documents verify-then-run + the env knob; both scripts `bash -n` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub remote → root update.sh | whatever HEAD the remote serves is rsync'd + built + restarted as root |
| community skill repo → liv-core process | a DOWNLOADED marketplace index.js is import()-ed in-process with full host privileges |
| network → curl\|bash installer | the piped script runs (as root in the sudo form) off the network |
| apt source → root update.sh | a blanket apt install on every run trusts the host's apt source set as root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257B-01 | Tampering / Elevation | update.sh deploys unverified HEAD as root | mitigate | verify fetched ref vs pinned/signed expected SHA; refuse on mismatch (Task 1) |
| T-257B-02 | Elevation of Privilege | DOWNLOADED marketplace skill index.js import() = in-process RCE | mitigate | verifySkillBundle origin/checksum gate before the MARKETPLACE import; fail-closed for unverifiable downloads; builtin exempt (Task 3) |
| T-257B-03 | Tampering | curl\|bash installer runs unverified | mitigate | opt-in SHA pin before re-exec + documented verify-then-run + logged sha256 (Task 4) |
| T-257B-04 | Tampering | blanket unpinned apt every run | mitigate | gate apt install on a missing-binary check; skip when present (Task 2) |
| T-257B-05 | Repudiation | full signed-tag enforcement requires shipped maintainer key | accept (follow-up) | guard is opt-in-strict now; shipping EXPECTED_RELEASE / maintainer.gpg flips it to fail-closed (noted, out of this plan's repo-state) |
| T-257B-06 | Availability (regression) | gating the builtin import path would refuse first-party bundled skills | mitigate | gate scoped to the MARKETPLACE path only; builtin (path-bundled) is trusted-by-origin and never gated (Task 3) |
</threat_model>

<verification>
- SC-B probe (011): set `LIVOS_EXPECTED_SHA` to a wrong value on a scratch checkout → update.sh exits non-zero
  before rsync ("Refusing to deploy"); unset → warns + proceeds.
- SC-B probe (012): install a MARKETPLACE skill from a non-official registry with no checksum → loadSkillLazy
  refuses (no import, returns false); a tampered checksum → refused; an OFFICIAL-registry skill loads; and a
  BUILTIN bundled skill (loadAll over the skillsDir) STILL loads (no regression).
- SC-B probe (026): `LIVOS_INSTALL_EXPECTED_SHA=wrong bash install.sh` → fails before re-exec.
- SC-B probe (040): re-run update.sh on a box with all streaming binaries present → apt install is skipped.
- `bash -n` green on update.sh / install.sh / livos/install.sh; skill-signature.test.ts green.
</verification>

<success_criteria>
SC-B met: update.sh refuses an unpinned/unsigned HEAD when pin material is present (and loudly warns otherwise);
the MARKETPLACE skill import is gated by an origin/checksum check that fails closed for unverifiable downloads
while BUILTIN bundled skills keep loading; the installer verifies an opt-in pinned SHA + documents verify-then-run;
the blanket apt install is gated on missing binaries. No deploy regression on the current (unpinned) Mini PC and
no skill-loading regression for first-party bundled skills.
</success_criteria>

<output>
After completion, create `.planning/phases/257-security-hardening-pass-2/257-01-SUMMARY.md`
</output>
