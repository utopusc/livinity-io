---
phase: 257-security-hardening-pass-2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - liv/packages/core/src/heartbeat-runner.ts
  - platform/web/src/lib/db.ts
  - platform/web/drizzle.config.ts
  - platform/web/ecosystem.config.cjs
  - platform/web/src/lib/username-validator.test.ts
  - platform/relay/src/config.ts
  - platform/relay/ecosystem.config.cjs
  - livos/packages/livinityd/source/modules/database/index.ts
  - docker/docker-compose.postgres.yml
  - livos/packages/livinityd/source/modules/docker/registry-credentials.ts
  - livos/packages/livinityd/source/modules/docker/registry-credentials.test.ts
  - livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts
  - livos/setup.sh
  - liv/deploy/setup-server4.sh
requirements: [LIVOS-020, LIVOS-021, LIVOS-030, LIVOS-031, LIVOS-032, LIVOS-033, LIVOS-034]
autonomous: true

# WAVE/DEPENDENCY NOTE (one-writer-per-file-per-wave):
# wave 1, depends_on [] — WS-E is the SOLE 257 writer of every file it lists.
# inject-local-ai-clis.ts was edited by 256-02 but NO other 257 plan touches it
# (WS-E owns the 0o777 fix). NONE of WS-E's files overlap WS-A/B/C/D. WS-E does
# NOT touch update.sh / install.sh / deploy-livinityd.sh (WS-B owns the first two,
# WS-C owns the third) — so no installer-script collision. Runs in parallel with
# 257-01/02/03/04. The grep-clean invariant (Task 6) is the goal-backward SC for
# the whole plan and is verified LAST.
# DEPLOY-TARGET NOTE: platform/web + platform/relay run on Server5 (off-limits for
# DEPLOY per the operator hard-rule). These edits are SOURCE-ONLY (remove committed
# secrets from tracked repo) — NOT a Server5 deploy. The deploy gate (257-07) does
# NOT deploy platform/* and the operator should rotate the Server5 platform DB
# password OUT OF BAND if LivPlatform2024 was ever live (flagged to operator).

must_haves:
  truths:
    - "git grep for the known default passwords (LivRedis2024, LivPostgres2024, LivPlatform2024, NexusRedis2024, liv:liv) returns ZERO tracked-source hits (SC-E grep-clean invariant)."
    - "Each former hardcoded-secret call site reads the secret from env / a secret-file and FAILS CLOSED (throws / no connection) when it is unset — no silent fallback to a default."
    - "The at-rest credential encryption key is derived INDEPENDENTLY of the JWT signing secret (no key reuse) (LIVOS-033)."
    - "The luse scratch HOME that nests the operator-credential mount is least-privilege (not 0o777) (LIVOS-034)."
  artifacts:
    - path: "liv/packages/core/src/heartbeat-runner.ts"
      provides: "Redis password parsed from REDIS_URL, not hardcoded"
      contains: "REDIS_URL"
    - path: "livos/packages/livinityd/source/modules/docker/registry-credentials.ts"
      provides: "at-rest key from a dedicated data-encryption-key file, independent of the JWT secret"
      contains: "secrets"
    - path: "livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts"
      provides: "scratch HOME chmod 0o700 (owned by the container uid), not 0o777"
      contains: "0o700"
  key_links:
    - from: "heartbeat-runner / database / platform db"
      to: "process.env (REDIS_URL / DATABASE_URL)"
      via: "env read, fail-closed on unset"
      pattern: "process\\.env"
    - from: "registry-credentials getKey"
      to: "data/secrets dedicated DEK"
      via: "independent at-rest key file"
      pattern: "createHash|randomBytes"
---

<objective>
WS-E — Secret hygiene. Remove every committed/fallback default password from tracked source and replace it with
an env/secret-file read that FAILS CLOSED if unset: LIVOS-020 (`LivRedis2024!` in heartbeat-runner), LIVOS-021
(`LivPlatform2024` in the platform web + relay tier), LIVOS-030 (`LivPostgres2024!` in the livinityd db module +
the orphan compose), LIVOS-031 (`liv:liv` in livos/setup.sh), LIVOS-032 (`NexusRedis2024!`/`NexusDB2024!` in the
legacy setup-server4.sh). Plus LIVOS-033 (derive the at-rest credential-encryption key independently of the JWT
signing secret — no key reuse) and LIVOS-034 (the world-writable 0o777 scratch HOME nesting the operator-cred
mount → least-privilege).

Purpose: a leak of the repo no longer leaks a working credential; a leak of the JWT secret no longer also
decrypts every stored registry/git/stack credential; and a local process can no longer plant a poisoned config
in a world-writable agent HOME. The headline win is the grep-clean invariant.
Output: env/secret-file reads (fail-closed) at every former hardcoded site, an independent at-rest DEK, a 0o700
scratch HOME, and ZERO tracked default-password hits.

STILL-EXISTS VERIFICATION (read at plan time, full git-grep inventory):
- 020: heartbeat-runner.ts:223 `redis-cli -a "LivRedis2024!" ping`. OPEN.
- 021: platform/web/src/lib/db.ts:4, drizzle.config.ts:8, ecosystem.config.cjs:11, username-validator.test.ts:29,
  platform/relay/src/config.ts:28 (`LivPlatform2024!`), platform/relay/ecosystem.config.cjs:13. OPEN.
- 030: database/index.ts:11 DEFAULT_DATABASE_URL `LivPostgres2024!`; docker/docker-compose.postgres.yml:10
  `POSTGRES_PASSWORD: LivPostgres2024!`. OPEN.
- 031: livos/setup.sh:190 `liv:liv@localhost`. OPEN.
- 032: liv/deploy/setup-server4.sh:23,24,86 `NexusRedis2024!` (+ NexusDB2024! per audit). OPEN.
- 033: docker/registry-credentials.ts:27-29 key = sha256(JWT secret file). OPEN.
- 034: apps/inject-local-ai-clis.ts:300-301 `chmod(homeDir, 0o777)`. OPEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-security-hardening-pass-2/257-CONTEXT.md
@SECURITY-AUDIT.md
@.planning/phases/256-security-hardening-contained-autonomy/256-02-SUMMARY.md

<interfaces>
<!-- The exact secret sites. Use directly. -->

```text
liv/packages/core/src/heartbeat-runner.ts:223  run('redis-cli -a "LivRedis2024!" ping 2>/dev/null')
platform/web/src/lib/db.ts:4                    process.env.DATABASE_URL || 'postgresql://platform:LivPlatform2024@127.0.0.1:5432/platform'
platform/web/drizzle.config.ts:8               url: process.env.DATABASE_URL || 'postgresql://platform:LivPlatform2024@...'
platform/web/ecosystem.config.cjs:11           DATABASE_URL: 'postgresql://platform:LivPlatform2024@...'
platform/web/src/lib/username-validator.test.ts:29  (doc-comment example string)
platform/relay/src/config.ts:28                envStr('DATABASE_URL', 'postgresql://platform:LivPlatform2024!@localhost:5432/platform')
platform/relay/ecosystem.config.cjs:13         DATABASE_URL: 'postgresql://platform:LivPlatform2024@...'
livos/.../database/index.ts:11                 const DEFAULT_DATABASE_URL = 'postgresql://livos:LivPostgres2024!@localhost:5432/livos'  (used :54)
docker/docker-compose.postgres.yml:10          POSTGRES_PASSWORD: LivPostgres2024!
livos/setup.sh:190                             DATABASE_URL=postgresql://liv:liv@localhost:5432/livos
liv/deploy/setup-server4.sh:23,24,86           requirepass NexusRedis2024!  /  redis-cli -a NexusRedis2024!  (+ NexusDB2024!)
livos/.../docker/registry-credentials.ts:21-29 JWT_SECRET_PATH = '/opt/livos/data/secrets/jwt'; key = sha256(jwt)
                                               (siblings: git-credentials.ts, stack-secrets.ts, scheduler/backup-secrets.ts)
livos/.../apps/inject-local-ai-clis.ts:300-301 // World-writable…  await fse.chmod(homeDir, 0o777).catch(()=>{})
```

Reference: drizzle.ts (sibling of db.ts) ALREADY does the correct thing — requires DATABASE_URL, throws if unset.
Mirror it. livinityd tests use vitest; liv/ uses tsx + node:assert/strict. Keep edits minimal/atomic (ideally
one commit per finding). Use the Bash tool (not the Grep tool) for the `git grep` invariant in Task 6 so the
pipeline/`wc -l` works.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: heartbeat Redis pw + livinityd PG fallback + orphan compose — env/fail-closed (LIVOS-020 / LIVOS-030)</name>
  <files>liv/packages/core/src/heartbeat-runner.ts, livos/packages/livinityd/source/modules/database/index.ts, docker/docker-compose.postgres.yml</files>
  <read_first>
    - heartbeat-runner.ts:200-230 (the Promise.all run() block — other call sites already extract from REDIS_URL)
    - database/index.ts:1-60 (DEFAULT_DATABASE_URL + its use at :54)
    - docker/docker-compose.postgres.yml:1-20
    - SECURITY-AUDIT.md:419-429 (020) + :570-582 (030)
  </read_first>
  <action>
1. heartbeat-runner.ts: replace `-a "LivRedis2024!"` with the password parsed from `process.env.REDIS_URL`
   (`new URL(REDIS_URL).password`, URL-decoded). If REDIS_URL is unset or has no password, run `redis-cli ping`
   WITHOUT `-a` (or skip the metric) — never embed a literal.
2. database/index.ts: remove `LivPostgres2024!` from `DEFAULT_DATABASE_URL`. Prefer deleting the constant and
   `throw new Error('DATABASE_URL is required')` when `process.env.DATABASE_URL` is unset (fail-closed, mirror
   platform drizzle.ts). No literal password.
3. docker/docker-compose.postgres.yml: change `POSTGRES_PASSWORD: LivPostgres2024!` to
   `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}` (require-from-env).
  </action>
  <verify>
    <automated>cd liv && npx tsc --noEmit -p packages/core 2>&1 | head -5</automated>
  </verify>
  <done>heartbeat-runner derives the Redis pw from REDIS_URL (no literal); database/index.ts has no hardcoded PG password (fail-closed on unset DATABASE_URL); the orphan compose requires POSTGRES_PASSWORD from env; liv core tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: platform web + relay tier — require DATABASE_URL, drop LivPlatform2024 fallbacks (LIVOS-021)</name>
  <files>platform/web/src/lib/db.ts, platform/web/drizzle.config.ts, platform/web/ecosystem.config.cjs, platform/web/src/lib/username-validator.test.ts, platform/relay/src/config.ts, platform/relay/ecosystem.config.cjs</files>
  <read_first>
    - platform/web/src/lib/db.ts:1-8 + the sibling platform/web/src/lib/drizzle.ts (the CORRECT require-or-throw pattern)
    - platform/web/drizzle.config.ts:1-12, platform/web/ecosystem.config.cjs (the DATABASE_URL env block)
    - platform/relay/src/config.ts:20-35, platform/relay/ecosystem.config.cjs
    - platform/web/src/lib/username-validator.test.ts:25-32 (the doc-comment example with the password)
    - SECURITY-AUDIT.md:433-444 (021 — remove fallback; require DATABASE_URL; mirror drizzle.ts; rotate if live)
  </read_first>
  <action>
1. db.ts + drizzle.config.ts: replace `process.env.DATABASE_URL || 'postgresql://platform:LivPlatform2024@...'`
   with a require-or-throw — `const url = process.env.DATABASE_URL; if (!url) throw new Error('DATABASE_URL is
   required')` (mirror drizzle.ts:11). No literal connection string.
2. ecosystem.config.cjs (web + relay): the committed `DATABASE_URL: 'postgresql://platform:LivPlatform2024@...'`
   is a PRODUCTION credential — replace the literal with `process.env.DATABASE_URL` (PM2 reads the value from the
   environment / an env file, not from the committed unit). Do NOT commit a real password.
3. relay/src/config.ts: change `envStr('DATABASE_URL', 'postgresql://platform:LivPlatform2024!@...')` so the
   default is removed — require the env (throw on unset) or use an empty/sentinel default that fails to connect,
   never the literal.
4. username-validator.test.ts: replace the example password in the doc-comment with a `<DATABASE_PASSWORD>`
   placeholder so the literal is gone (keep the comment's instructional intent).
NOTE: SOURCE-ONLY edits — do NOT deploy to Server5. Flag to the operator (SUMMARY): rotate the live Server5
platform DB password if `LivPlatform2024` was ever used.
  </action>
  <verify>
    <automated>git grep -n 'LivPlatform2024' -- platform/ | wc -l</automated>
  </verify>
  <done>All LivPlatform2024 literals removed from platform/web + platform/relay (db, drizzle, both ecosystem units, relay config, the test doc-comment); each now requires DATABASE_URL from env; `git grep LivPlatform2024 -- platform/` returns 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: at-rest credential encryption key independent of the JWT secret (LIVOS-033)</name>
  <files>livos/packages/livinityd/source/modules/docker/registry-credentials.ts, livos/packages/livinityd/source/modules/docker/registry-credentials.test.ts</files>
  <read_first>
    - docker/registry-credentials.ts:21-45 (JWT_SECRET_PATH + getKey + encrypt/decrypt)
    - SECURITY-AUDIT.md:616-628 (033 — dedicated DEK file via crypto.randomBytes(32), distinct from JWT secret)
  </read_first>
  <behavior>
    - Test 1 (round-trip): encrypt(plaintext) then decrypt → plaintext (AES-256-GCM unchanged).
    - Test 2 (key independence): the derived key is NOT sha256(JWT secret) — given a known JWT secret + a distinct
      DEK file, getKey() returns the DEK-derived value, not the JWT-derived one. [Inject the two file paths/contents.]
    - Test 3 (DEK generated if absent): first call with no DEK file creates a 32-byte key file (0600) and reuses it.
  </behavior>
  <action>
In `registry-credentials.ts`, derive `_key` from a DEDICATED data-encryption key file
`/opt/livos/data/secrets/credential-dek` instead of the JWT secret:
- `getKey()`: read the DEK file; if absent, `crypto.randomBytes(32)`, write it `{mode:0o600}` (mkdirp the
  secrets dir if needed), and use it. Use the 32 raw bytes directly as the AES-256 key (or sha256 them — but the
  point is the SOURCE is independent of the JWT secret). Make the DEK path + an injectable reader so the unit
  test runs offline.
- Keep encrypt/decrypt (IV12 + tag16 + ciphertext) byte-identical so EXISTING encrypted blobs that were written
  with the OLD (JWT-derived) key still decrypt: ADD a migration-safe fallback — on a decrypt failure with the new
  DEK, retry once with the legacy sha256(JWT) key, then RE-ENCRYPT with the DEK and persist (lazy re-key). This
  prevents breaking already-stored registry/git/stack credentials on the live box. Document the lazy re-key.
NOTE: the sibling stores (git-credentials.ts, stack-secrets.ts, scheduler/backup-secrets.ts) share the pattern;
if a shared key helper exists, fix it once and the siblings inherit it. If they each inline getKey, this plan
fixes registry-credentials.ts as the lead + notes the siblings as a fast-follow in the SUMMARY (do NOT silently
leave them — list them explicitly so 257-06/258 can sweep, or fix them here if trivial and within budget).
  </action>
  <verify>
    <automated>cd livos && npx vitest run packages/livinityd/source/modules/docker/registry-credentials.test.ts</automated>
  </verify>
  <done>getKey derives the at-rest key from a dedicated 0600 DEK file (generated if absent), independent of the JWT secret; existing blobs still decrypt via a legacy-key fallback + lazy re-key; round-trip + independence + DEK-gen unit-proven; sibling stores listed for sweep.</done>
</task>

<task type="auto">
  <name>Task 4: least-privilege scratch HOME (LIVOS-034)</name>
  <files>livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts</files>
  <read_first>
    - inject-local-ai-clis.ts:295-320 (the writeLocalAiCliWrappers mkdirp + chmod 0o777 + the uid the ACL/setfacl
      step already discovers for the container)
    - SECURITY-AUDIT.md:632-645 (034 — chown to the container uid + chmod 0o700, not 0o777)
  </read_first>
  <action>
Replace `await fse.chmod(homeDir, 0o777)` with least-privilege: chown the scratch HOME to the specific container
uid (the uid the cred-ACL step already discovers — reuse it; the post-256-02 file still computes/has it) and
`chmod(homeDir, 0o700)`. If the uid is unavailable in this code path, fall back to `0o700` owned by the livinityd
process user (still removes world-write). The container that needs to write CLI self-config runs AS that uid, so
0o700-owned-by-uid preserves function while removing the world-writable plant-a-poisoned-config vector. Remove the
"World-writable so any container uid can write" comment; add a LIVOS-034 note. Keep all other mounts/wrappers
unchanged (256-02 regression-locked). The file is not sacred-frozen (256-02 edited it) — confirm before commit.
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit -p packages/livinityd 2>&1 | grep -i inject-local-ai-clis | head -3; git grep -n '0o777' -- livos/packages/livinityd/source/modules/apps/inject-local-ai-clis.ts | wc -l</automated>
  </verify>
  <done>The scratch HOME is chmod 0o700 (chowned to the container uid where available), not 0o777; the world-writable comment is gone; other inject mounts unchanged; no new tsc error in the file; `git grep 0o777` in the file returns 0.</done>
</task>

<task type="auto">
  <name>Task 5: scrub legacy installer secrets — setup.sh (liv:liv) + setup-server4.sh (Nexus*) (LIVOS-031 / LIVOS-032)</name>
  <files>livos/setup.sh, liv/deploy/setup-server4.sh</files>
  <read_first>
    - livos/setup.sh:175-195 (the secret-gen region — REDIS_PASS/JWT use `openssl rand`; PG line is the literal)
    - liv/deploy/setup-server4.sh:20-30, :40-45, :80-90 (NexusRedis2024! + NexusDB2024! sites)
    - SECURITY-AUDIT.md:586-597 (031) + :601-612 (032)
  </read_first>
  <action>
1. livos/setup.sh: replace `DATABASE_URL=postgresql://liv:liv@localhost:5432/livos` so the PG password is
   generated with `openssl rand -hex 24` like the script's REDIS_PASS/JWT (and substituted into the URL). No
   static `liv:liv`. (setup.sh is legacy/alternate; the canonical install.sh already randomizes — this just
   removes the committed weak literal.)
2. liv/deploy/setup-server4.sh: this is the deprecated Server4 script (Server4 is out of scope). Per the audit's
   recommendation, DELETE the committed literals: replace `NexusRedis2024!` (lines 23/24/86) and `NexusDB2024!`
   with `openssl rand`-generated values OR — simplest, since Server4 is dead — DELETE setup-server4.sh entirely if
   nothing references it (grep the repo for `setup-server4`); if referenced, scrub the literals to env reads.
   Confirm no tracked reference before deleting.
Both scripts must pass `bash -n` (if setup-server4.sh is deleted, skip its check).
  </action>
  <verify>
    <automated>bash -n livos/setup.sh && git grep -n 'liv:liv@\|NexusRedis2024\|NexusDB2024' -- livos/setup.sh liv/deploy/ | wc -l</automated>
  </verify>
  <done>setup.sh generates the PG password via openssl rand (no liv:liv); setup-server4.sh literals scrubbed-or-file-deleted (no tracked reference left dangling); the targeted git grep returns 0; setup.sh `bash -n` clean.</done>
</task>

<task type="auto">
  <name>Task 6: grep-clean invariant — assert ZERO tracked default-password hits (SC-E gate)</name>
  <files>livos/packages/livinityd/source/modules/database/index.ts</files>
  <read_first>
    - (no file read needed) — this is the whole-WS verification gate; the listed file is a placeholder so the
      task has a files entry. Do not edit it unless Task 1 left a residual hit.
  </read_first>
  <action>
Run the SC-E invariant across the WHOLE tracked tree and ensure it is ZERO:
`git grep -nE 'LivRedis2024|LivPostgres2024|LivPlatform2024|NexusRedis2024|NexusDB2024|liv:liv@'` over tracked
files MUST return no hits (exit 1 / empty). If ANY hit remains (a site missed by Tasks 1-5, e.g. a sibling
credential store, another ecosystem unit, a yaml, or a doc/example), FIX it in-place (env read / placeholder /
deletion) until the grep is clean. Exclude only `.planning/` (this plan + the audit legitimately NAME the
passwords as findings) and `SECURITY-AUDIT.md` from the assertion — those are documentation OF the secrets, not
live credentials. The invariant is over CODE/CONFIG/SCRIPT source. Record the final grep output (must be empty)
in the SUMMARY.
  </action>
  <verify>
    <automated>! git grep -nE 'LivRedis2024|LivPostgres2024|LivPlatform2024|NexusRedis2024|NexusDB2024|liv:liv@' -- ':!.planning/' ':!SECURITY-AUDIT.md'</automated>
  </verify>
  <done>`git grep` for every known default password over tracked source (excluding .planning/ + SECURITY-AUDIT.md) returns ZERO hits — the SC-E grep-clean invariant holds; any straggler found is fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public repo → credential | a committed default password is a working credential to anyone who reads the repo |
| JWT secret leak → at-rest creds | a single key reused across auth + at-rest encryption widens blast radius |
| local host process → 0o777 agent HOME | a world-writable HOME nesting the cred mount permits config poisoning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257E-01 | Information Disclosure | committed default DB/Redis passwords | mitigate | env/secret-file reads, fail-closed, grep-clean invariant (Tasks 1/2/5/6) |
| T-257E-02 | Information Disclosure | JWT-secret leak also decrypts at-rest creds | mitigate | dedicated DEK independent of JWT secret + lazy re-key (Task 3) |
| T-257E-03 | Tampering | poisoned config in 0o777 scratch HOME | mitigate | 0o700 chown-to-uid scratch HOME (Task 4) |
| T-257E-04 | Availability (regression) | fail-closed throws break a healthy box | accept | every prod path already sets the env (.env DATABASE_URL/REDIS_URL); fail-closed only bites a misconfig |
| T-257E-05 | Repudiation | sibling cred stores still JWT-keyed | mitigate (tracked) | siblings listed in SUMMARY for a fast-follow sweep if not fixed in Task 3 |
</threat_model>

<verification>
- SC-E probe: `git grep -nE 'LivRedis2024|LivPostgres2024|LivPlatform2024|NexusRedis2024|NexusDB2024|liv:liv@'`
  over tracked code/config/scripts → empty.
- SC-E probe (033): the at-rest key file `data/secrets/credential-dek` exists (0600) and differs from the JWT
  secret; existing encrypted registry creds still decrypt (lazy re-key).
- SC-E probe (034): `stat` the scratch HOME → 0700, not 0777.
- Unit/compile: liv core tsc clean; registry-credentials.test.ts green; setup.sh `bash -n` clean.
</verification>

<success_criteria>
SC-E met: zero tracked default-password hits (grep-clean); every former hardcoded site reads env/secret-file and
fails closed on unset; the at-rest DEK is independent of the JWT secret (with a non-breaking lazy re-key); the
scratch HOME is 0o700. Operator flagged to rotate the live Server5 platform DB password (source edit is not a
Server5 deploy).
</success_criteria>

<output>
After completion, create `.planning/phases/257-security-hardening-pass-2/257-05-SUMMARY.md`
</output>
