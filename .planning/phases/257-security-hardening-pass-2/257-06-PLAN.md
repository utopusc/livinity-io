---
phase: 257-security-hardening-pass-2
plan: 06
type: execute
wave: 2
depends_on: ['257-02']
files_modified:
  - livos/packages/livinityd/source/modules/openclawos/approvals-routes.ts
  - livos/packages/livinityd/source/modules/openclawos/approvals-routes.test.ts
  - livos/packages/livinityd/source/modules/domain/caddy.ts
  - livos/packages/livinityd/source/modules/domain/caddy.test.ts
  - livos/packages/livinityd/source/modules/server/index.ts
  - livos/packages/livinityd/source/modules/files/samba.ts
requirements: [LIVOS-027, LIVOS-035, LIVOS-036, LIVOS-039]
autonomous: true

# WAVE/DEPENDENCY NOTE (one-writer-per-file-per-wave):
# wave 2, depends_on 257-02 — WS-F edits server/index.ts (LIVOS-036 custom-domain
# exact-match) which WS-C (257-02) ALSO edits (LIVOS-015 loopback bind). To keep
# server/index.ts single-writer-per-wave, WS-F runs in wave 2 AFTER WS-C. caddy.ts,
# approvals-routes.ts, samba.ts are touched ONLY here (no overlap). NOTE: caddy.ts
# was edited by 256-04 (forward_auth) — the LIVOS-035 escape edit is the reverse_proxy
# upstreamBearer emit, a DISTINCT region from the 256-04 forward_auth blocks; preserve
# those.
# SCOPE-DEFERRABILITY: WS-F is the Low/Info wave. Per CONTEXT.md it MAY split to a
# Phase 258 if the High/Medium waves fill the budget. It is small (4 code tasks, all
# Low) and is planned now, NOT deferred — but it is the natural cut line if the operator
# wants to ship High/Medium first. Two CONTEXT WS-F items are ALREADY HANDLED:
#   - LIVOS-029 (memory fail-open) -> CLOSED by 256-04 (memory/src/auth.ts now 503s on
#     unset key; verified at plan time). Task 5 is verify-and-document, no code.
#   - LIVOS-040 (apt) -> folded into WS-B (257-01 Task 2) so update.sh is single-writer.

must_haves:
  truths:
    - "openclawos approvals stream/resolve routes require the ADMIN role, not merely any valid token — a guest/member token is rejected (LIVOS-027)."
    - "upstreamBearer is charset-validated before it is interpolated into the Caddyfile — a token with quotes/newlines/braces cannot inject Caddy config (LIVOS-035)."
    - "The custom-domain gateway resolves the upstream by EXACT container-name match (no substring fallback) so traffic cannot be proxied to an unintended container (LIVOS-036)."
    - "The share-password secret file is created mode 0600 (not 644) (LIVOS-039)."
    - "Memory-service fail-open (LIVOS-029) is confirmed CLOSED by 256-04 (verify + document, no redundant code)."
  artifacts:
    - path: "livos/packages/livinityd/source/modules/openclawos/approvals-routes.ts"
      provides: "admin-role check on the approvals stream/resolve handlers"
      contains: "role"
    - path: "livos/packages/livinityd/source/modules/domain/caddy.ts"
      provides: "upstreamBearer charset-validated before Caddyfile emit"
      contains: "upstreamBearer"
    - path: "livos/packages/livinityd/source/modules/files/samba.ts"
      provides: "share-password file written mode 0600"
      contains: "0o600"
  key_links:
    - from: "approvals-routes authenticate"
      to: "admin role enforcement"
      via: "resolve role from token, reject non-admin"
      pattern: "role|admin"
    - from: "caddy.ts reverse_proxy emit"
      to: "validated upstreamBearer"
      via: "charset gate before interpolation"
      pattern: "upstreamBearer"
---

<objective>
WS-F — Remaining Low/Info hygiene. Close LIVOS-027 (the openclawos approvals SSE stream + resolve endpoints —
which approve/deny agent tool-call executions — gate on authenticate() which only checks token validity, NO role,
so a guest/member can subscribe to + resolve approvals), LIVOS-035 (the app daemon upstreamBearer token is read
verbatim from the app compose and interpolated UNESCAPED into the generated Caddyfile, so a token with a
quote+newline injects arbitrary Caddy config), LIVOS-036 (the custom-domain gateway resolves the upstream by a
SUBSTRING container-name match, so traffic can hit an unintended container's port), and LIVOS-039 (the
share-password secret file is created mode 644 while sibling secrets are 600). PLUS a verify-and-document close of
LIVOS-029 (memory fail-open — already fixed by 256-04).

Purpose: agent-approval control cannot be hijacked by a non-admin, a hostile app compose cannot inject Caddy
config, custom-domain routing cannot cross to the wrong container, and the share secret matches the 600 policy.
Output: an admin gate on approvals, a charset-validated bearer, an exact container-name match, a 0600 share file,
and a documented LIVOS-029 closure.

STILL-EXISTS VERIFICATION (read at plan time, current code):
- 027: openclawos/approvals-routes.ts:51-66 authenticate() = `await verifyToken(token); return true` — no role.
  :83/:148 stream/resolve gate on it. CONFIRMED OPEN.
- 035: caddy.ts:596 interpolates `${sub.upstreamBearer}` unescaped into the reverse_proxy header_up. CONFIRMED OPEN.
- 036: server/index.ts:203-206 (+ :712-715) has an exact-match clause FIRST but a `|| ...includes(appSlug)`
  SUBSTRING fallback REMAINS. CONFIRMED PARTIALLY OPEN (remove the substring fallback).
- 039: files/samba.ts:125 `fse.writeFile(sharePasswordFile, sharePassword)` — no mode. CONFIRMED OPEN.
- 029: liv/packages/memory/src/auth.ts now 503s on unset LIV_API_KEY (256-04). CONFIRMED CLOSED -> Task 5 verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/257-security-hardening-pass-2/257-CONTEXT.md
@SECURITY-AUDIT.md
@.planning/phases/256-security-hardening-contained-autonomy/256-04-SUMMARY.md

<interfaces>
<!-- The exact WS-F sites. Use directly. -->

From openclawos/approvals-routes.ts (LIVOS-027): authenticate() (around :51-66) reads the token from the
Authorization header or the LIVINITY_SESSION cookie, calls `await verifyToken(token)` and `return true` on success
— it captures NO role. The stream handler (:83) and resolve handler (:148) both gate solely on its boolean.
verifyToken returns the decoded payload (multi-user tokens carry `role`; a legacy `{loggedIn:true}` token has none).

From caddy.ts (LIVOS-035) — DISTINCT region from the 256-04 forward_auth blocks: the subdomain config type has
`upstreamBearer?: string` (around :162); the reverse_proxy emit (around :596) does
`${sub.upstreamBearer ? header_up Authorization "Bearer ${sub.upstreamBearer}" ... : ''}`. readAppDaemonToken
(apps.ts ~:1451-1495) returns the token raw (only trim + VAR-default unwrap + a `${`-reject).

From server/index.ts (LIVOS-036) — the SAME file WS-C edits (loopback bind), but a DISTINCT region: the
routeCustomDomain container resolution (around :203-206 and again :712-715) is
`c.Names.some(n => n === '/' + appSlug || n === appSlug) || c.Names.some(n => n.replace('/','').includes(appSlug))`
— the second `.includes(appSlug)` clause is the substring fallback to remove.

From files/samba.ts (LIVOS-039): getSharePassword (around :118-130) does
`await fse.writeFile(sharePasswordFile, sharePassword)` with no mode option (defaults 0644 under umask 022).

livinityd tests use vitest (run via `npx vitest run` from packages/livinityd). caddy.test.ts already exists
(256-04 extended it). The approvals-routes test should inject a fake verifyToken returning a role-bearing or
role-less payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin-role gate on openclawos approvals stream/resolve (LIVOS-027 / SC-F)</name>
  <files>livos/packages/livinityd/source/modules/openclawos/approvals-routes.ts, livos/packages/livinityd/source/modules/openclawos/approvals-routes.test.ts</files>
  <read_first>
    - openclawos/approvals-routes.ts:40-90 (options type + authenticate + stream handler) + :140-160 (resolve handler)
    - SECURITY-AUDIT.md:526-537 (027 — require admin role; resolve currentUser, enforce role admin)
  </read_first>
  <behavior>
    - Test 1 (admin allowed): authenticate with a payload role 'admin' -> true.
    - Test 2 (guest/member rejected): a payload role 'guest' (or 'member') -> false (401).
    - Test 3 (legacy single-user admin allowed): a legacy loggedIn:true token with no role/userId -> treated as the
      single-user admin -> true (do NOT break the single-operator Mini PC's own approvals access).
    - Test 4 (invalid token rejected): bad token -> false (unchanged).
  </behavior>
  <action>
Change authenticate() to enforce admin: after `const payload = await verifyToken(token)` (capture the payload, not
just truthiness), return true ONLY when the caller is admin-equivalent: `payload.role === 'admin'` OR a genuine
legacy single-user token (no role AND no userId AND loggedIn true). A multi-user token whose role is 'member' or
'guest' returns false. Keep the bad-token to false path. This mirrors the 256-04 principle (legacy single-user is
admin-equivalent, but a resolved non-admin user is NOT). Both the stream (:83) and resolve (:148) handlers already
call authenticate(), so the gate applies to both. Add a brief LIVOS-027 comment.
  </action>
  <verify>
    <automated>cd livos && npx vitest run packages/livinityd/source/modules/openclawos/approvals-routes.test.ts</automated>
  </verify>
  <done>approvals authenticate() admits admin + genuine-legacy-single-user, rejects member/guest (401) and invalid tokens; both stream + resolve handlers enforce it; tests pass (4 cases).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: charset-validate upstreamBearer before Caddyfile emit (LIVOS-035 / SC-F)</name>
  <files>livos/packages/livinityd/source/modules/domain/caddy.ts, livos/packages/livinityd/source/modules/domain/caddy.test.ts</files>
  <read_first>
    - caddy.ts:155-165 (upstreamBearer on the type) + :590-600 (the reverse_proxy emit interpolating it)
    - SECURITY-AUDIT.md:649-660 (035 — strict charset, refuse quotes/newlines/braces)
  </read_first>
  <behavior>
    - Test 1 (clean token emitted): a normal token (e.g. abc.DEF-123_xyz) -> the reverse_proxy block contains the
      Authorization Bearer header with that token.
    - Test 2 (injection token NOT emitted): an upstreamBearer containing a double-quote, newline, brace, or dollar-brace
      -> the bearer header is OMITTED (or that block is skipped) — the malicious string never reaches the Caddyfile.
    - Test 3 (256-04 blocks preserved): the generated config still contains the forward_auth directive for a
      single-user installed-app block (no regression to 256-04).
  </behavior>
  <action>
Before interpolating sub.upstreamBearer at the emit site, validate it against the strict charset
`^[A-Za-z0-9._-]+$`. Compute a safeBearer that equals sub.upstreamBearer only when it matches that regex, else
undefined; emit the `header_up Authorization "Bearer <safeBearer>"` line (and its accompanying Host/Origin rewrite)
ONLY when safeBearer is set, otherwise omit the bearer line entirely (a malformed/hostile token must NOT break out
into Caddy config). Do NOT change the forward_auth blocks 256-04 added (distinct region). If a `caddy validate`
pre-reload hook is trivially available, note it as optional defense-in-depth; not required.
  </action>
  <verify>
    <automated>cd livos && npx vitest run packages/livinityd/source/modules/domain/caddy.test.ts</automated>
  </verify>
  <done>upstreamBearer is charset-validated; a clean token emits the bearer header, a quote/newline/brace token omits it (no Caddyfile injection); 256-04 forward_auth blocks preserved; tests pass (3 cases).</done>
</task>

<task type="auto">
  <name>Task 3: exact container-name match in the custom-domain gateway (LIVOS-036 / SC-F)</name>
  <files>livos/packages/livinityd/source/modules/server/index.ts</files>
  <read_first>
    - server/index.ts:160-210 (routeCustomDomain + the container resolution :203-206) + :705-720 (the second match site)
    - SECURITY-AUDIT.md:664-675 (036 — exact match only, drop the includes() fallback)
    - NOTE: WS-C (257-02 Task 1) already edited the listen() bind in THIS file in wave 1 (merged before this wave).
      Your edit is the container-match region — a distinct region from the listen() call.
  </read_first>
  <action>
At both routeCustomDomain container-resolution sites (:203-206 and :712-715), REMOVE the substring fallback clause
`|| c.Names.some((n) => n.replace('/', '').includes(appSlug))` so only the exact-match clause
(`n === '/' + appSlug || n === appSlug`) remains. An unknown/colliding slug now resolves to no container and the
existing not-found path (503) fires instead of cross-routing to a substring-matching container. Add a LIVOS-036
comment. Do NOT touch the listen()/bind region (WS-C owns it; this edit is in a separate region of the same file,
applied in the next wave so the file stays single-writer-per-wave).
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit -p packages/livinityd 2>&1 | grep -i 'server/index' | head -3</automated>
  </verify>
  <done>Both custom-domain container resolutions use exact-name match only (no includes(appSlug) fallback); unknown/colliding slug -> 503; no new tsc error in server/index.ts; the WS-C listen()/bind edit untouched.</done>
</task>

<task type="auto">
  <name>Task 4: share-password secret file mode 0600 (LIVOS-039 / SC-F)</name>
  <files>livos/packages/livinityd/source/modules/files/samba.ts</files>
  <read_first>
    - files/samba.ts:115-138 (getSharePassword writer + applySharePassword)
    - SECURITY-AUDIT.md:709-722 (039 — write mode 0600 like the other secrets)
  </read_first>
  <action>
At samba.ts:125, change the write to pass a mode: `await fse.writeFile(sharePasswordFile, sharePassword, {mode:
0o600})`. Also add `await fse.chmod(sharePasswordFile, 0o600).catch(() => {})` after the read-or-create so an
EXISTING 644 file (the live Mini PC's current state) is corrected on next access. Add a LIVOS-039 comment. No other
change. (No new test file required — a tsc/build check suffices for a one-line mode fix; if a samba test exists,
extend it to assert the mode arg.)
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit -p packages/livinityd 2>&1 | grep -i 'samba' | head -3</automated>
  </verify>
  <done>getSharePassword writes the share-password file mode 0600 and chmods an existing file to 0600; no new tsc error; LIVOS-039 closed.</done>
</task>

<task type="auto">
  <name>Task 5: verify-and-document the already-closed LIVOS-029 (memory fail-open) — NO code</name>
  <files>livos/packages/livinityd/source/modules/files/samba.ts</files>
  <read_first>
    - liv/packages/memory/src/auth.ts (the requireApiKey — confirm it 503s on unset LIV_API_KEY, 256-04)
    - .planning/phases/256-security-hardening-contained-autonomy/256-04-SUMMARY.md (Task 3 — memory fail-closed)
  </read_first>
  <action>
This is a SKIP-WITH-EVIDENCE task (NO code change — the files entry is a placeholder; do not edit it). Read
`liv/packages/memory/src/auth.ts` and confirm `requireApiKey` reads `process.env.LIV_API_KEY` at call time and
responds 503 (refuses) when it is unset — i.e. memory fails CLOSED. This was implemented by 256-04 Task 3. Record
in the SUMMARY: "LIVOS-029 confirmed CLOSED by Phase 256-04 — memory requireApiKey fails closed (503 on unset key);
no 257 code needed." Do NOT re-implement. If (and only if) the verification shows it is somehow still fail-open
(it is not, per the plan-time read), escalate via Rule 3 rather than silently fixing here.
  </action>
  <verify>
    <automated>git grep -n 'fail-closed\|FAIL CLOSED\|503' -- liv/packages/memory/src/auth.ts</automated>
  </verify>
  <done>LIVOS-029 verified CLOSED by 256-04 (memory requireApiKey 503s on unset key); documented as a skip in the SUMMARY; no redundant code added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any logged-in user -> approvals stream/resolve | a valid token (any role) controls agent tool-call approvals |
| app-author compose -> generated Caddyfile | a daemon token from app compose is written into Caddy config |
| public custom-domain request -> container port | a slug indexes a container by substring name match |
| host filesystem -> share-password file | a 644 secret is readable beyond owner if a parent dir loosens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-257F-01 | Elevation of Privilege | non-admin resolves agent approvals | mitigate | admin-role gate on approvals authenticate() (Task 1) |
| T-257F-02 | Tampering / Injection | upstreamBearer injects Caddy config | mitigate | strict charset validation before emit; omit on mismatch (Task 2) |
| T-257F-03 | Spoofing / Information Disclosure | custom-domain cross-routes to wrong container | mitigate | exact container-name match only (Task 3) |
| T-257F-04 | Information Disclosure | 644 share-password secret | mitigate | write/chmod 0600 (Task 4) |
| T-257F-05 | Spoofing | memory API unauth when key dropped | mitigate (done) | verified closed by 256-04 — fail-closed 503 (Task 5, no code) |
</threat_model>

<verification>
- SC-F probe (027): in multi-user mode, a guest token to GET /openclawos/approvals/stream -> 401; admin -> 200.
- SC-F probe (035): install an app whose compose sets a hostile OD_API_TOKEN (quote+newline) -> the generated
  Caddyfile contains no injected block (bearer omitted) and `caddy validate` passes.
- SC-F probe (036): two containers sharing a name fragment + a custom domain mapped to the exact slug -> only the
  exact container is reached (the substring container is never selected).
- SC-F probe (039): stat /opt/livos/data/secrets/share-password -> 600.
- SC-F probe (029): unset LIV_API_KEY on a scratch memory service -> /api returns 503 (already closed by 256-04).
- Unit suites green: approvals-routes.test.ts, caddy.test.ts; tsc clean on server/index.ts + samba.ts.
</verification>

<success_criteria>
SC-F met: approvals require admin (legacy single-user preserved); upstreamBearer is charset-validated (no Caddy
injection); custom-domain uses exact container-name match; share-password is 0600; LIVOS-029 confirmed closed by
256-04. WS-F is the deferrable cut line (to Phase 258) if the operator wants to ship High/Medium first.
</success_criteria>

<output>
After completion, create `.planning/phases/257-security-hardening-pass-2/257-06-SUMMARY.md`
</output>
