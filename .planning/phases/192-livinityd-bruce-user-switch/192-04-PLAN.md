---
phase: 192-livinityd-bruce-user-switch
plan: 04
type: execute
wave: 3
depends_on:
  - 192-02
  - 192-03
files_modified:
  - scripts/sacred-shas-v38.json
autonomous: false
requirements:
  - V38-3-P192-SACRED-REPIN
  - V38-3-P192-MINIPC-VERIFY

must_haves:
  truths:
    - "scripts/sacred-shas-v38.json contains the post-192-03 git hash-object SHA for any newly-pinned file from this milestone (manager.ts is NOT currently pinned, so the action is to ASSESS whether new pins are needed, not to mass-update)"
    - "`bash scripts/check-sacred.sh` exits 0 after the registry update"
    - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f for sdk-agent-runner.ts is UNCHANGED in the registry (the load-bearing invariant of v38.3)"
    - "Mini PC verification confirms: livos.service runs as bruce, claude flag works, livinityd connects to redis + postgres, no regression in /opt/livos/data accessibility"
  artifacts:
    - path: "scripts/sacred-shas-v38.json"
      provides: "Updated sacred SHA registry post-192-03 source edits"
      contains: "f3538e1d811992b782a9bb057d1b7f0a0189f95f"
  key_links:
    - from: "scripts/sacred-shas-v38.json"
      to: "scripts/check-sacred.sh + .husky/pre-commit"
      via: "registry consumed by check script; pre-commit gates every commit"
      pattern: "expected_sha"
---

<objective>
Close Phase 192 by:

1. Recompute SHAs for any file in `scripts/sacred-shas-v38.json` that was touched by 192-02 or 192-03 (likely NONE, since the existing registry does not pin manager.ts, auth.ts, or deploy-livinityd.sh — but we MUST verify by running `scripts/check-sacred.sh` and acting on its output).
2. If new pins should be added for v38.3 (e.g., the new migration script becomes load-bearing for 193's vault migration), add them with frozen_in_phase="192-bruce-user-switch".
3. Run `bash scripts/check-sacred.sh` — MUST PASS.
4. Operator-gated Mini PC verification: confirm live deployment runs livos as bruce, claude flag works, no regression.

Purpose: Lock in the v38.3 foundation. Phase 193 (vault → ~/livinity refactor) and Phase 194 (sidebar UI) both depend on a stable bruce-user runtime.

Output: 1 file edited (sacred-shas-v38.json) + Mini PC operator confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-livinityd-bruce-user-switch/192-CONTEXT.md
@.planning/phases/192-livinityd-bruce-user-switch/192-01-PLAN.md
@.planning/phases/192-livinityd-bruce-user-switch/192-02-PLAN.md
@.planning/phases/192-livinityd-bruce-user-switch/192-03-PLAN.md
@scripts/sacred-shas-v38.json
@scripts/check-sacred.sh

<interfaces>
<!-- Current sacred SHA registry: 25 entries pinned (as of pre-Phase-192). -->
<!-- Key invariant: entry #1 is sdk-agent-runner.ts SHA f3538e1d... — MUST NOT change. -->

<!-- Files touched by Phase 192 source edits (Plans 02 + 03): -->
- scripts/install/deploy-livinityd.sh             (NOT pinned — deploy scripts are not sacred)
- scripts/migrate-to-bruce-user.sh                (NEW — consider pinning if 193 will consume it)
- scripts/install/sudoers.d/livinityd             (NEW — consider pinning)
- livos/packages/livinityd/source/modules/cc-pty/manager.ts          (NOT pinned currently)
- livos/packages/livinityd/source/modules/livinity-broker/auth.ts    (NOT pinned currently)
- livos/packages/livinityd/source/data/vault-templates/CLAUDE.md     (NOT pinned — template, expected to evolve)

<!-- Conclusion: zero EXISTING registry entries change SHA. -->
<!-- Question: should ANY new entries be added in this phase? -->
<!-- Answer (per CONTEXT.md): the sudoers + migration script become load-bearing  -->
<!-- for 193 (which will adapt the migration script for vault rename) and 194    -->
<!-- (sidebar reads from bruce-owned filesystem). Pinning them prevents          -->
<!-- accidental drift during 193/194 execution. RECOMMEND: pin sudoers fragment  -->
<!-- (immutable security boundary) but NOT migration script (193 explicitly      -->
<!-- extends it).                                                                -->

<!-- check-sacred.sh exit codes: -->
<!-- 0 = all pins valid -->
<!-- 1 = at least one SHA mismatch or missing file -->
<!-- 2 = setup error (registry malformed, node missing) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run check-sacred.sh + assess new pin candidates + update registry</name>
  <files>scripts/sacred-shas-v38.json</files>
  <behavior>
    - `bash scripts/check-sacred.sh` exits 0 after the registry update
    - sdk-agent-runner.ts entry remains with expected_sha=f3538e1d811992b782a9bb057d1b7f0a0189f95f (UNCHANGED)
    - No existing pinned file's SHA changes (Phase 192 did not edit any currently-pinned file)
    - Decision documented in 192-04-SUMMARY: whether new pins were added (and which)
  </behavior>
  <action>
    1. Run baseline:
       ```
       bash scripts/check-sacred.sh
       ```

       Expected: PASS with "25 files verified" (or current registry size). If any
       pin shows mismatch, INVESTIGATE — Phase 192 source edits should NOT have
       touched any pinned file. If a mismatch is found, that's a bug in Plan 192-02
       or 192-03 — STOP and route back to the relevant plan for revision.

    2. For each file touched by 192-02 and 192-03, compute git hash-object:
       ```
       git hash-object livos/packages/livinityd/source/modules/cc-pty/manager.ts
       git hash-object livos/packages/livinityd/source/modules/livinity-broker/auth.ts
       git hash-object scripts/install/deploy-livinityd.sh
       git hash-object scripts/migrate-to-bruce-user.sh
       git hash-object scripts/install/sudoers.d/livinityd
       git hash-object livos/packages/livinityd/source/data/vault-templates/CLAUDE.md
       ```

    3. Decision matrix — for each file, decide pin/no-pin:

       | File | Pin? | Rationale |
       |------|------|-----------|
       | manager.ts | NO | Active code under iteration; 193+ may touch cc-pty for cwd path adaptation |
       | auth.ts | NO | Active broker code; future BROKER_FORCE_ROOT_HOME changes likely |
       | deploy-livinityd.sh | NO | Deploy scripts are operator-tunable; not sacred |
       | scripts/migrate-to-bruce-user.sh | NO | 193 will EXTEND this script for vault rename — would force-bust pin |
       | scripts/install/sudoers.d/livinityd | **YES** | Security boundary; if this changes silently, livinityd loses required permissions OR gains unintended ones |
       | vault-templates/CLAUDE.md | NO | Template, expected to evolve with milestones |

    4. Add the sudoers fragment pin to `scripts/sacred-shas-v38.json`:
       ```json
       {
         "path": "scripts/install/sudoers.d/livinityd",
         "expected_sha": "<output of git hash-object scripts/install/sudoers.d/livinityd>",
         "frozen_in_phase": "192-bruce-user-switch",
         "rationale": "Phase 192-01 sudoers security boundary; widening this file (e.g., adding NOPASSWD: ALL) must be a deliberate, reviewed change."
       }
       ```

       Insert as the LAST entry in the JSON array (preserves existing entry order, which check-sacred.sh iterates).

    5. Verify post-update:
       ```
       bash scripts/check-sacred.sh
       ```
       Expected: PASS with "26 files verified" (was 25 + 1 new pin).

    6. Verify the sacred SHA f3538e1d... entry is byte-identical to pre-edit:
       ```
       node -e "const r=JSON.parse(require('fs').readFileSync('scripts/sacred-shas-v38.json','utf8')); const e=r.find(x=>x.path==='liv/packages/core/src/sdk-agent-runner.ts'); if(!e||e.expected_sha!=='f3538e1d811992b782a9bb057d1b7f0a0189f95f'){console.error('SACRED SHA TAMPERED:',e); process.exit(1)} console.log('sacred SHA intact')"
       ```
  </action>
  <verify>
    <automated>bash scripts/check-sacred.sh && node -e "const r=JSON.parse(require('fs').readFileSync('scripts/sacred-shas-v38.json','utf8')); const e=r.find(x=>x.path==='liv/packages/core/src/sdk-agent-runner.ts'); if(!e||e.expected_sha!=='f3538e1d811992b782a9bb057d1b7f0a0189f95f'){console.error('SACRED TAMPERED'); process.exit(1)} const s=r.find(x=>x.path==='scripts/install/sudoers.d/livinityd'); if(!s){console.error('sudoers pin missing'); process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>check-sacred.sh PASSES post-update. Registry has 26 entries. sdk-agent-runner.ts pin unchanged. sudoers.d/livinityd newly pinned with frozen_in_phase=192-bruce-user-switch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    - Phase 192-02 service unit cutover (User=bruce + Group=bruce in 4 systemd heredocs)
    - Phase 192-02 migration script (idempotent ownership flip + sudoers install)
    - Phase 192-03 code path cleanup (isRoot dropped, HOME=os.homedir() interpolated, /root/.claude → ~/.claude)
    - Phase 192-04 sacred SHA registry pin for sudoers fragment
  </what-built>
  <how-to-verify>
    Mini PC live cutover + verification (operator work — cannot be automated from Windows dev box):

    Pre-flight (one-time, only if not already done by Phase 192-02 Task 3):
    ```
    ssh -i ~/.ssh/minipc bruce@10.69.31.68
    sudo bash /opt/livos/update.sh   # pull latest scripts/ + source
    sudo bash /opt/livos/scripts/migrate-to-bruce-user.sh   # idempotent — safe to re-run
    sudo systemctl daemon-reload
    sudo systemctl restart livos.service liv-core.service liv-worker.service liv-memory.service
    sleep 10
    ```

    Then verify (paste output of each command into the resume signal):

    1. **systemd User= directive:**
       ```
       sudo systemctl show livos.service --property User --property Group
       ```
       Expected: `User=bruce` `Group=bruce`

    2. **Live process UID:**
       ```
       ps -o user,uid,pid,cmd -p $(sudo systemctl show livos.service --property MainPID --value)
       ```
       Expected: USER=bruce UID=1000 (or whatever Mini PC's bruce uid is) CMD includes `npx tsx`

    3. **Service health:**
       ```
       sudo systemctl is-active livos.service liv-core.service liv-worker.service liv-memory.service
       ```
       Expected: 4× `active`

    4. **livinityd HTTP responding:**
       ```
       curl -sf http://localhost:8080/api/health || curl -sf http://localhost:8080/
       ```
       Expected: HTTP 200/302/401 (any non-connection-refused response proves port bound)

    5. **Sudoers installed correctly:**
       ```
       sudo visudo -cf /etc/sudoers.d/livinityd
       ls -l /etc/sudoers.d/livinityd
       ```
       Expected: `parsed OK`, `-r--r----- 1 root root`

    6. **Data dir ownership:**
       ```
       ls -ld /opt/livos/data /opt/livos/.env
       ```
       Expected: both owned by `bruce bruce`

    7. **Bug #17 cleared — Claude flag works:**
       Open AI Chat in browser (https://bruce.livinity.io or local), click the Claude icon, observe a tmux session spawns and does NOT immediately show `[exited]`.

       Or from terminal:
       ```
       sudo -u bruce bash -c 'HOME=/home/bruce tmux new-session -d -s test-claude "claude --dangerously-skip-permissions --version"'
       sleep 2
       sudo -u bruce tmux capture-pane -p -t test-claude
       sudo -u bruce tmux kill-session -t test-claude
       ```
       Expected: claude version output (NOT "cannot be used with root/sudo privileges")

    8. **Idempotency re-check:**
       ```
       sudo bash /opt/livos/scripts/migrate-to-bruce-user.sh
       ```
       Expected: "marker /opt/livos/data/.bruce-migrated exists — already migrated, exit 0"

    9. **Vault items accessible (smoke):**
       Open AI Chat, click an agent in the sidebar. Tmux session attaches. cwd should be the agent's vault dir; no permission-denied errors in journalctl:
       ```
       sudo journalctl -u livos.service -n 50 --no-pager | grep -iE 'permission denied|eacces|eperm'
       ```
       Expected: empty output (no permission errors)

    10. **livinityd test suite (optional but recommended):**
        ```
        cd /opt/livos && sudo -u bruce pnpm --filter livinityd vitest run --reporter=verbose 2>&1 | tail -10
        ```
        Expected: PASS count ≥ baseline + 3 (from 192-03 new assertions)

    Rollback if any step fails:
    ```
    sudo sed -i 's/^User=bruce/User=root/; /^Group=bruce/d' /etc/systemd/system/livos.service
    sudo systemctl daemon-reload && sudo systemctl restart livos.service
    sudo rm -f /opt/livos/data/.bruce-migrated /etc/sudoers.d/livinityd
    # If livinityd starts as root again, the Bug #17 isRoot suppression from
    # pre-192-03 code would have re-engaged — BUT 192-03 dropped that code,
    # so claude will refuse the flag again. Acceptable rollback cost: revert
    # 192-03 commit too if a full rollback is needed.
    ```
  </how-to-verify>
  <resume-signal>Type "192-04 mini-pc-verified" with the 9 outputs collected above, OR describe which step failed with relevant journalctl tail</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sacred-shas-v38.json → check-sacred.sh → pre-commit hook | Adding the sudoers fragment to the registry makes it immutable without a deliberate registry update |
| operator → Mini PC SSH | All verification commands require root or bruce; ssh key is operator-managed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-04-1 | T (Tampering) | Sacred SHA registry edited carelessly, dropping the sdk-agent-runner pin | mitigate | Task 1 verify block explicitly asserts f3538e1d entry presence + value; check-sacred.sh fail-closed |
| T-192-04-2 | T | Sudoers fragment changed without registry update | mitigate | After 192-04 the file is pinned; any silent edit fails check-sacred.sh + pre-commit hook |
| T-192-04-3 | R (Repudiation) | Operator skips Mini PC verification, claims success without evidence | mitigate | Task 2 resume signal REQUIRES output of 9 commands; orchestrator can verify each before unblocking 193 |
| T-192-04-4 | D | livinityd fails to start as bruce due to undiscovered permission issue | accept | Rollback documented; phase ships only after Task 2 confirms green |
</threat_model>

<verification>
1. `bash scripts/check-sacred.sh` exits 0
2. Registry size grew by 0 or 1 entry (no existing pins changed)
3. sdk-agent-runner.ts pin SHA byte-identical to pre-Phase-192 value
4. Operator returns 9 Mini PC verification outputs proving live cutover succeeded
</verification>

<success_criteria>
- scripts/sacred-shas-v38.json updated (sudoers fragment pinned, sacred SHA preserved)
- check-sacred.sh PASS
- Mini PC: livos.service runs as bruce (User=bruce + UID=1000 in ps)
- Mini PC: Claude flag works (tmux+claude smoke test succeeds)
- Mini PC: migration script idempotent on re-run
- Mini PC: no permission-denied errors in livos.service journal
- Phase 193 unblocked (filesystem refactor can now assume bruce-owned ~/livinity/)
</success_criteria>

<output>
After completion, create `.planning/phases/192-livinityd-bruce-user-switch/192-04-SUMMARY.md` summarizing:
- Sacred SHA registry diff (entries added: list paths + SHAs; entries changed: should be 0)
- Mini PC verification outputs (paste from resume signal)
- Bug #17 status: CLOSED / STILL FAILING (with diagnosis if not closed)
- Hand-off to Phase 193: confirmed bruce-owned data dir, sudoers in place, ready for vault → ~/livinity refactor

ALSO update `.planning/STATE.md` "Current Position" block:
- Last shipped phase: Phase 192 (4 plans, X new vitest assertions, sacred SHA preserved Y/Y, N commits)
- Key decisions for Phase 192 (list the major calls: User=bruce, sudoers narrow, isRoot dropped, sudoers pinned)
- Next action: Phase 193 vault → ~/livinity filesystem refactor
</output>
