---
phase: 192-livinityd-bruce-user-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install/sudoers.d/livinityd
  - .planning/phases/192-livinityd-bruce-user-switch/192-AUDIT.md
  - scripts/install/__tests__/test-sudoers-livinityd.sh
autonomous: true
requirements:
  - V38-3-P192-AUDIT
  - V38-3-P192-SUDOERS
user_setup: []

must_haves:
  truths:
    - "Every root-required operation in livinityd source is enumerated with file + line + rationale"
    - "Sudoers template grants ONLY the narrow set of commands found by the audit (no NOPASSWD ALL, no shell wildcards)"
    - "Sudoers template is syntactically valid (visudo -cf passes)"
  artifacts:
    - path: ".planning/phases/192-livinityd-bruce-user-switch/192-AUDIT.md"
      provides: "Documented audit of every root-required call in livinityd"
      contains: "chownSystemPath"
    - path: "scripts/install/sudoers.d/livinityd"
      provides: "NOPASSWD sudoers fragment for bruce running livinityd"
      contains: "bruce ALL=(root) NOPASSWD:"
    - path: "scripts/install/__tests__/test-sudoers-livinityd.sh"
      provides: "Sudoers shape + syntax test"
      contains: "visudo -cf"
  key_links:
    - from: "scripts/install/sudoers.d/livinityd"
      to: "192-AUDIT.md command list"
      via: "Cmnd_Alias entries match enumerated commands one-to-one"
      pattern: "Cmnd_Alias\\s+LIVINITYD_"
---

<objective>
Audit every operation in `livos/packages/livinityd/source/` that currently requires root (or relies on `User=root` in `livos.service`) and produce two artifacts:

1. `192-AUDIT.md` — exhaustive list of root-required call sites grouped by category (chown, systemctl, useradd, /root/ filesystem reads, isRoot/getuid checks).
2. `scripts/install/sudoers.d/livinityd` — sudoers fragment granting bruce NOPASSWD on ONLY the commands the audit identifies as unavoidable. NOT a broad NOPASSWD: ALL.

Purpose: Phase 192-02 needs this audit to write the migration script. Phase 192-03 needs the call-site list to know which code paths to refactor. Phase 192-04 reads the sudoers file to verify shape on Mini PC.

Output: 192-AUDIT.md (~200-400 lines markdown) + sudoers fragment (~30-60 lines) + bash test that verifies sudoers shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-livinityd-bruce-user-switch/192-CONTEXT.md
@scripts/install/sudoers.d/livos-native
@scripts/install/__tests__/test-systemd-env-liv-vault-root.sh

<interfaces>
<!-- Sudoers shape precedent — the existing v37 native sudoers file uses
     Cmnd_Alias + exact-binary + glob-only-on-args. Phase 192-01 MUST follow
     this same pattern. -->

From scripts/install/sudoers.d/livos-native:
```
Cmnd_Alias LIVOS_NATIVE = /usr/bin/apt-get install -y *
bruce ALL=(root) NOPASSWD: LIVOS_NATIVE
```

Known root-required call sites already found (seed list — extend during audit):

- livos/packages/livinityd/source/modules/files/files.ts:302
  → `chownSystemPath()` calls `fse.chown(path, uid, gid)` — Node fs.chown
    REQUIRES CAP_CHOWN, NOT necessarily root if file is owned by bruce. So
    this MAY drop to no-sudo once bruce owns /opt/livos/data. Audit must
    classify: "drops to no-sudo after migration" vs "still needs sudo".

- livos/packages/livinityd/source/modules/files/files.ts:218,296;
  external-storage.ts:202 — all call chownSystemPath. Same classification.

- livos/packages/livinityd/source/modules/cc-pty/manager.ts:208-210 —
  isRoot check + skip-perms suppression. Becomes dead code after 192-03.

- livos/packages/livinityd/source/modules/livinity-broker/auth.ts:127-129 —
  `/root/.claude` hardcoded when BROKER_FORCE_ROOT_HOME=true. With bruce
  user this becomes /home/bruce/.claude. Classify: "code change, no sudoers".

- livos/packages/livinityd/source/modules/apps/builtin-apps.ts:509 —
  `/root/.ollama` volume mount in a Docker compose template. Audit must
  decide: keep /root/.ollama (operator-installed Ollama runs as root) or
  swap to /home/bruce/.ollama.

- livos/packages/livinityd/source/data/vault-templates/CLAUDE.md:3 —
  documentation string referencing HOME=/root. Cosmetic.

The audit MUST add to this list everything found by grep + manual scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enumerate root-required operations into 192-AUDIT.md</name>
  <files>.planning/phases/192-livinityd-bruce-user-switch/192-AUDIT.md</files>
  <behavior>
    - Audit doc MUST list every match of `chownSystemPath|process.getuid|isRoot|'/root/|HOME=/root|systemctl |useradd |chown -R` under livos/packages/livinityd/source/
    - Each entry: file path, line number, category (FS_OWNERSHIP | UID_CHECK | HARDCODED_ROOT_PATH | SYSTEMCTL | USER_MGMT), disposition (DROPS_TO_NO_SUDO | NEEDS_SUDOERS | CODE_CHANGE | COSMETIC | DEFER)
    - Disposition rationale in plain prose (1-2 sentences per entry)
    - Final "Sudoers Command List" section lists ONLY the entries with disposition=NEEDS_SUDOERS, formatted as `Cmnd_Alias` names + binary paths
    - Final "Code Change List" section lists entries with disposition=CODE_CHANGE — these are the inputs for Phase 192-03
  </behavior>
  <action>
    Run these greps from repo root and consolidate into the audit doc:
    ```
    grep -rn "chownSystemPath\|process\.getuid\|isRoot" livos/packages/livinityd/source/ --include='*.ts' | grep -v '__tests__\|\.test\.'
    grep -rn "'/root/\|HOME=/root\|\"/root/" livos/packages/livinityd/source/ --include='*.ts'
    grep -rn "execSync.*systemctl\|spawn.*systemctl\|exec.*systemctl" livos/packages/livinityd/source/ --include='*.ts'
    grep -rn "execSync.*useradd\|execSync.*chown -R\|execSync.*usermod" livos/packages/livinityd/source/ --include='*.ts'
    ```
    For each hit decide disposition. Use this rubric:
    - `fse.chown(path, uid, gid)` on a path owned by bruce → DROPS_TO_NO_SUDO (bruce can chown files it already owns to itself; if uid/gid differ, NEEDS_SUDOERS).
    - Hardcoded `/root/` path → CODE_CHANGE (rewrite to `os.homedir()` in 192-03).
    - `isRoot`/`process.getuid()===0` → CODE_CHANGE (drop in 192-03 since uid will never be 0).
    - `systemctl reload caddy` / `systemctl restart livos` → NEEDS_SUDOERS.
    - Docstring/comment mentions of /root → COSMETIC (note but do not block).

    Structure of 192-AUDIT.md:
    ```
    # Phase 192 Audit — Root-Required Operations in livinityd

    Generated: 2026-05-21
    Phase: 192-01

    ## Summary
    | Category | Count | NEEDS_SUDOERS | CODE_CHANGE | DROPS_TO_NO_SUDO | COSMETIC |
    | FS_OWNERSHIP | N | ... |
    | UID_CHECK | N | ... |
    | HARDCODED_ROOT_PATH | N | ... |
    | SYSTEMCTL | N | ... |
    | USER_MGMT | N | ... |
    Total: M call sites

    ## FS_OWNERSHIP

    ### 1. files.ts:302 — `chownSystemPath()` calls fse.chown
    Category: FS_OWNERSHIP
    Disposition: DROPS_TO_NO_SUDO
    Rationale: After 192-02 migration, /opt/livos/data is owned by bruce.
    Node's fs.chown succeeds when the caller owns the file AND is setting
    uid/gid to one of its supplementary groups. Since fileOwner.userId
    will equal process.getuid() (both bruce), this is a no-op.

    [continue for every hit...]

    ## Sudoers Command List
    (entries with disposition=NEEDS_SUDOERS, formatted for the sudoers file)
    - Cmnd_Alias LIVINITYD_CADDY_RELOAD = /usr/bin/systemctl reload caddy
    - [extend during audit]

    ## Code Change List
    (entries with disposition=CODE_CHANGE — inputs for 192-03)
    - cc-pty/manager.ts:208-210 — drop isRoot check
    - cc-pty/manager.ts:237,245,253,314,315 — HOME=/root → os.homedir()
    - livinity-broker/auth.ts:128 — '/root/.claude' → path.join(os.homedir(), '.claude')
    - [extend during audit]
    ```

    Do NOT propose code edits in this task — only enumerate.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('.planning/phases/192-livinityd-bruce-user-switch/192-AUDIT.md','utf8'); if(!d.match(/## Summary/) || !d.match(/## Sudoers Command List/) || !d.match(/## Code Change List/) || !d.match(/chownSystemPath/) || !d.match(/isRoot/) || !d.match(/HOME=\/root/)) {console.error('audit missing required sections or seed entries'); process.exit(1)} console.log('audit shape OK')"</automated>
  </verify>
  <done>192-AUDIT.md exists with Summary, Sudoers Command List, Code Change List sections. Contains entries for chownSystemPath, isRoot, HOME=/root, /root/ paths. Each entry has file:line + disposition + rationale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write sudoers.d/livinityd template + shape test</name>
  <files>scripts/install/sudoers.d/livinityd, scripts/install/__tests__/test-sudoers-livinityd.sh</files>
  <behavior>
    - sudoers file: header comment block (deploy instructions + security model), one Cmnd_Alias per command (NO bare `bruce ALL=(root) NOPASSWD: ALL`), one `bruce ALL=(root) NOPASSWD: <alias>` line per alias OR a single combined line listing all aliases
    - sudoers file: NO shell wildcards inside command paths (e.g., no `/usr/bin/* `), only argument-position globs after the binary (matching the precedent in sudoers.d/livos-native)
    - sudoers file: header documents deploy path `/etc/sudoers.d/livinityd`, mode 0440, owner root:root
    - test script: bash -n clean, asserts ≥3 invariants (file exists + has Cmnd_Alias + has `bruce ALL=(root)` line + has NO `NOPASSWD: ALL\b` + visudo -cf clean if visudo available)
  </behavior>
  <action>
    1. Read 192-AUDIT.md's "Sudoers Command List" section.

    2. Write `scripts/install/sudoers.d/livinityd` modeled on the existing
       `scripts/install/sudoers.d/livos-native` precedent. Skeleton:

       ```
       # Phase 192-01 — sudoers entries for livinityd running as bruce.
       #
       # Allows bruce (livinityd uid) to run the narrow set of root commands
       # identified by 192-AUDIT.md. No NOPASSWD: ALL — every command is an
       # exact-binary Cmnd_Alias.
       #
       # Deploy:
       #   sudo install -m 0440 -o root -g root livinityd /etc/sudoers.d/livinityd
       #   sudo visudo -c
       #
       # Security model:
       # - Exact binary paths (not shell paths, not /bin shims).
       # - Argument globs only after the binary, never inside binary path.
       # - No setenv preservation.
       # - To grant a new privilege, extend the Cmnd_Alias — do not broaden.

       Cmnd_Alias LIVINITYD_CADDY_RELOAD = /usr/bin/systemctl reload caddy
       Cmnd_Alias LIVINITYD_CADDY_RESTART = /usr/bin/systemctl restart caddy
       # [add more from 192-AUDIT.md Sudoers Command List]

       bruce ALL=(root) NOPASSWD: LIVINITYD_CADDY_RELOAD, LIVINITYD_CADDY_RESTART
       ```

       IMPORTANT: only include Cmnd_Alias entries the audit classified
       NEEDS_SUDOERS. If the audit found zero NEEDS_SUDOERS entries (because
       everything drops to no-sudo once bruce owns the data dir), the
       sudoers file can be a minimal "no commands needed" stub with a single
       comment explaining why — but keep the file present so 192-02's
       migration script has something to install.

    3. Write `scripts/install/__tests__/test-sudoers-livinityd.sh` modeled on
       `test-systemd-env-liv-vault-root.sh` style (pass/fail counters, color
       output, exit 0/1):

       Assertions (≥4):
       - AC-192-01-1: file exists at scripts/install/sudoers.d/livinityd
       - AC-192-01-2: grep -q "bruce ALL=(root)" passes
       - AC-192-01-3: grep -E -v "^#" passes AND `grep -qE "NOPASSWD:\\s+ALL\\b"` FAILS (no broad ALL grant)
       - AC-192-01-4: if `command -v visudo`, `visudo -cf scripts/install/sudoers.d/livinityd` exits 0
       - AC-192-01-5: every Cmnd_Alias defined is referenced by a `bruce ALL=` line (no orphan aliases)

    4. Make test executable: `chmod +x scripts/install/__tests__/test-sudoers-livinityd.sh`
       (Note: chmod from Windows is a no-op; the rsync deploy preserves git
       file modes from the index. Use `git update-index --chmod=+x` if you
       need the bit to land in the commit. Operator-side: file is sourced
       via `bash <path>` so the +x bit is not strictly required for CI.)
  </action>
  <verify>
    <automated>bash scripts/install/__tests__/test-sudoers-livinityd.sh</automated>
  </verify>
  <done>scripts/install/sudoers.d/livinityd exists with header + Cmnd_Aliases derived from 192-AUDIT.md. test-sudoers-livinityd.sh passes all assertions. No `NOPASSWD: ALL` anywhere in the sudoers file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| livinityd-process → host filesystem | bruce-uid livinityd reads/writes /opt/livos/data + /home/bruce/livinity; ownership flip in 192-02 enforces this boundary |
| bruce → root (via sudoers) | NOPASSWD grants in sudoers fragment — narrow set only |
| Sudoers file → /etc/sudoers.d/ | mode 0440 root:root required by sudo; relaxed perms = full host compromise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-192-01-1 | E (Elevation) | sudoers.d/livinityd grants too-broad NOPASSWD | mitigate | Cmnd_Alias per audit entry only; test asserts no `NOPASSWD: ALL\b`; audit doc enumerates every grant with rationale |
| T-192-01-2 | T (Tampering) | sudoers file deployed with wrong mode | mitigate | install -m 0440 -o root -g root in 192-02 migration script (this plan's test just shape-checks the static file) |
| T-192-01-3 | I (Information disclosure) | Audit doc leaks sensitive paths into git | accept | All paths are already in the public repo; no secrets in 192-AUDIT.md |
| T-192-01-4 | E | Command-injection via Cmnd_Alias args glob | mitigate | Glob only on argument position (after binary path), per sudoers.d/livos-native precedent |
</threat_model>

<verification>
1. `bash scripts/install/__tests__/test-sudoers-livinityd.sh` exits 0
2. `node -e "..."` audit-shape check (in Task 1 verify block) exits 0
3. Manual sniff of 192-AUDIT.md: ≥10 call-site entries, each with category + disposition
4. `grep -c "Cmnd_Alias" scripts/install/sudoers.d/livinityd` returns ≥1 (or =0 with explicit "no sudoers needed" stub comment)
5. `grep -c "NOPASSWD: ALL\b" scripts/install/sudoers.d/livinityd` returns 0
</verification>

<success_criteria>
- 192-AUDIT.md exists with all required sections and ≥10 enumerated call sites
- sudoers.d/livinityd exists, deploys 0440 root:root, contains only narrow Cmnd_Alias grants
- test-sudoers-livinityd.sh passes
- Phase 192-02 has a complete input list (Sudoers Command List + commands to install)
- Phase 192-03 has a complete input list (Code Change List)
- All sacred SHAs unchanged (no production code touched in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/192-livinityd-bruce-user-switch/192-01-SUMMARY.md` summarizing:
- Total call sites audited
- Count per disposition (NEEDS_SUDOERS / CODE_CHANGE / DROPS_TO_NO_SUDO / COSMETIC)
- Sudoers Cmnd_Alias count + final NOPASSWD line
- Inputs handed off to 192-02 (sudoers file path) and 192-03 (code change list)
</output>
