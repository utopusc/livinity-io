---
phase: 271
plan: 01
wave: 1
title: Liv AI agent polish — auth/install command matrix, brand logos, console-noise cleanup
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/cli-installer/auth.ts
  - livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts
  - livos/packages/ui/src/hooks/use-cli-auth-bridge.ts
  - scripts/aionui-patches/local-agents-install-section.js
  - livos/packages/ui/public/agent-logos/  (new SVGs)
  - .planning/phases/271-.../271-AUTH-MATRIX.md  (new deliverable doc)
autonomous: false   # auth-command correctness needs live TTY verification on the Mini PC
requirements: []
---

# Phase 271 Plan 01 — Liv AI agent polish

<objective>
Close the three post-v44.22 follow-ups for the Liv AI "Agents" surface: (A) a VERIFIED per-CLI Terminal install+auth command matrix (drift-locked UI mirror ↔ server source of truth), (B) real brand logos for the 9 monogram agents, (C) cleanup of the 3 remaining console-noise lines. Each task is independent — wave 1, but A is gated `autonomous:false` (needs live TTY verification).
</objective>

<must_haves>
- Every one of the 20 SUPPORTED_CLIS has a documented, source-cited correct install command AND auth command (or "no standalone auth — API key only" / "no auth needed"), captured in 271-AUTH-MATRIX.md.
- `use-cli-auth-bridge.ts` CLI_AUTH_COMMANDS + installCommandFor() are byte-consistent in intent with `cli-installer/auth.ts` CLI_AUTH_COMMANDS (drift-lock holds).
- Binary-name mismatches reconciled: `cursor-agent`→`cursor` and `kimi-cli`→`kimi-cli` (or multi-candidate probing) so detection + the Terminal command agree with the actually-installed binary.
- ≥7 of the 9 monogram CLIs render a real brand SVG (the rest documented as monogram-by-choice, never a wrong/placeholder logo).
- The 3 console-noise items are each either fixed or have a documented won't-fix-here rationale; googleAuth.status no longer logs a stub error on a normal Liv AI load.
- `pnpm --filter ui` typecheck clean; `node --check scripts/aionui-patches/local-agents-install-section.js` passes.
</must_haves>

<task id="271-01-A" autonomous="false">
<title>Verify + correct the per-CLI install/auth command matrix</title>
<read_first>
- livos/packages/livinityd/source/modules/cli-installer/auth.ts  (server CLI_AUTH_COMMANDS — source of truth)
- livos/packages/livinityd/source/modules/cli-installer/install-scripts.ts  (SUPPORTED_CLIS, CLI_BIN_NAMES, install-script resolution)
- livos/packages/ui/src/hooks/use-cli-auth-bridge.ts  (UI mirror: CLI_AUTH_COMMANDS + installCommandFor)
- scripts/install/cli/*.sh  (the actual install scripts run by installCommandFor)
</read_first>
<action>
1. Build a matrix for all 20 SUPPORTED_CLIS: columns = CLI name | binary name | install command (scripts/install/cli/<name>.sh contents summary) | auth command currently in the map | CORRECT auth command + auth type (login / `auth login` / api-key env / device-code / browser / none) | upstream-doc source URL.
2. For EACH CLI, verify the auth command against the tool's UPSTREAM docs (WebSearch/WebFetch the official CLI docs). Scrutinize first: codex, qwen-code, qoder-cli, codebuddy, github-copilot, nanobot, snow-cli, mistral-vibe (suspected wrong or missing).
3. LIVE-verify the high-value ones on the Mini PC (`ssh bruce@10.69.31.68`): for an installed CLI, run the candidate auth command in a TTY and confirm it starts the right flow (don't complete auth — just confirm the invocation is correct). At minimum verify claude, codex, gemini, opencode, cursor (the installed ones).
4. Fix any wrong/missing entries in BOTH `cli-installer/auth.ts` CLI_AUTH_COMMANDS AND the `use-cli-auth-bridge.ts` mirror — keep them identical (the file already carries a drift-lock comment).
5. Reconcile binary-name mismatches in install-scripts.ts CLI_BIN_NAMES: cursor-agent's installed binary is `cursor` (not `cursor-agent`); kimi-cli's is `kimi-cli` (not `kimi`). Either correct the single name or make the detector probe multiple candidates (`cursor`/`cursor-agent`). Cross-check against AionUi's own binary_name set from `:3020/api/agents`.
6. Write the verified matrix to `.planning/phases/271-.../271-AUTH-MATRIX.md` (the deliverable).
</action>
<acceptance_criteria>
- `271-AUTH-MATRIX.md` exists and has a row for all 20 CLIs with a source URL per auth command.
- `grep -c "CLI_AUTH_COMMANDS" livos/packages/livinityd/source/modules/cli-installer/auth.ts` ≥ 1 and the UI mirror's commands match it (diff the two command maps → identical keys+values).
- `install-scripts.ts` no longer maps `cursor-agent`→`cursor-agent` if the box's binary is `cursor` (matrix documents the chosen reconciliation).
- For the 5 installed CLIs (claude/codex/gemini/opencode/cursor), the matrix notes a LIVE-verified ✓ that the auth command starts the correct flow.
- `pnpm --filter ui` typecheck clean after the mirror edits.
</acceptance_criteria>
</task>

<task id="271-01-B" autonomous="true">
<title>Ship real brand logos for the 9 monogram agents</title>
<read_first>
- scripts/aionui-patches/local-agents-install-section.js  (CLI_META — the `logo:` field + logoCandidates)
- livos/packages/ui/public/agent-logos/  (existing 10 SVGs — match style/size)
</read_first>
<action>
1. Source accurate brand SVGs (simple/monochrome acceptable) for: openclaw, augment→auggie, codebuddy, qoder-cli→qodercli, factory-droid→droid, hermes-agent→hermes, nanobot, snow-cli→snow, kiro. Save each as `livos/packages/ui/public/agent-logos/<name>.svg` (name = the value you'll set in CLI_META).
2. For each shipped logo, set `logo:'<name>'` in that CLI's CLI_META entry in `scripts/aionui-patches/local-agents-install-section.js` (e.g. openclaw → `logo:'openclaw'`). logoCandidates() already prefers the static SVG.
3. If a clean SVG can't be sourced for one, LEAVE it monogram (do NOT ship a wrong/placeholder) and note it in the SUMMARY.
4. Keep SVGs small + viewBox-normalized like the existing ones (32×32 render).
</action>
<acceptance_criteria>
- `ls livos/packages/ui/public/agent-logos/ | wc -l` increased by the number of logos shipped (≥7).
- For each shipped logo, `grep "logo: '<name>'" scripts/aionui-patches/local-agents-install-section.js` matches AND `livos/packages/ui/public/agent-logos/<name>.svg` exists.
- `node --check scripts/aionui-patches/local-agents-install-section.js` passes.
- No CLI_META entry references a logo file that does not exist (every `logo:'X'` has a corresponding `<X>.svg`).
</acceptance_criteria>
</task>

<task id="271-01-C" autonomous="true">
<title>Quiet the 3 console-noise lines</title>
<read_first>
- the httpBridge stub that logs `googleAuth.status not yet implemented` (grep the ui + liv-ai-app for `googleAuth.status`)
- the caller of `displays.getVncUrl` (grep ui for `getVncUrl`) + the livinityd displays/computer-use router
- the favicon fetch site (grep for `favicon.ico` / remote favicon usage in the panel / AionUi-reachable patch)
</read_first>
<action>
1. googleAuth.status: locate the httpBridge stub. Implement a minimal real handler returning a stable shape (e.g. `{authenticated:false}`) so the SPA stops hitting the stub branch, OR downgrade the stub `console.log` to no output. Decide by which is lower-risk; prefer the real minimal handler.
2. displays.getVncUrl: find the caller. If the route was renamed/removed, either restore/add the route in the displays/computer-use tRPC router or guard the client call so it doesn't fire (and 404) on a normal Liv AI load. Document the chosen fix.
3. favicon CORS: find where remote `favicon.ico` is fetched for agent links (google.com, antigravity.google). Lowest-risk: stop fetching the remote favicon (use a bundled icon or none) for those links. If the fetch is inside the vendored AionUi bundle and not reachable via scripts/aionui-patches/*, document it as won't-fix-here in the SUMMARY.
</action>
<acceptance_criteria>
- A normal Liv AI page load no longer logs `googleAuth.status not yet implemented` (verified by reading the console after a fix, or the stub log line is removed from source: `grep -rn "googleAuth.status not yet implemented" livos/packages/ | wc -l` reflects the fix).
- `/trpc/displays.getVncUrl` either resolves (route exists) or is no longer called on load (the caller is guarded/removed) — documented in SUMMARY.
- favicon CORS item: fixed (no remote favicon fetch for those links) OR a one-line won't-fix-here rationale is in SUMMARY.
- `pnpm --filter ui` typecheck clean.
</acceptance_criteria>
</task>

<verification>
- Build gate: `pnpm --filter @livos/config build && pnpm --filter ui build` succeeds (the deploy gate); `node --check scripts/aionui-patches/local-agents-install-section.js` passes.
- Drift-lock: the UI mirror CLI_AUTH_COMMANDS == server auth.ts CLI_AUTH_COMMANDS.
- Deploy + UAT: cut the release tag, deploy to Mini PC, SW-cache-clear, then in the Agents panel confirm the 9 logos render (no monograms for the shipped ones) and Install/Auth open the Terminal with the corrected commands; confirm the console is quiet of the 3 lines.
</verification>

<notes>
- Ships via update.sh/release like all LivOS changes (Mini PC only). AionUi vendored — patch only via scripts/aionui-patches/*.
- Task A is `autonomous:false` because command correctness must be confirmed against upstream docs + a live TTY; do not blindly trust the Phase-253 guesses.
</notes>
