---
phase: 173-vault-rename-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-sacred.sh
files_created:
  - scripts/sacred-shas-v38.json
  - scripts/__tests__/test-check-sacred-v38.sh
autonomous: true
requirements:
  - D-V38-A
  - D-100-SACRED
tags:
  - sacred-sha
  - byte-freeze
  - pre-commit
  - v38

must_haves:
  truths:
    - "scripts/sacred-shas-v38.json exists, is valid JSON, parses to an array of `{path, expected_sha, frozen_in_phase}` records"
    - "JSON registry includes the existing sdk-agent-runner.ts entry with SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f"
    - "JSON registry includes all Phase 171 vault-items module files (types.ts, vault-root-resolver.ts, item-store.ts, tree-resolver.ts, pubsub.ts, index.ts) with their current git-hash-object SHAs"
    - "JSON registry includes all Phase 172 @livos/cli src files (cli.ts, auth.ts, version.ts, vault-bootstrap.ts, filesystem-mode.ts, query-client.ts, commands/*.ts non-test, query/*.ts non-test) with their current SHAs"
    - "JSON registry does NOT include the new migration-v35-to-v38.ts file (Phase 173-02 ships it; only LATER plans freeze it)"
    - "scripts/check-sacred.sh reads sacred-shas-v38.json, iterates every entry, calls `git hash-object` and compares to expected_sha"
    - "When ALL entries match → check-sacred.sh exits 0 with `[sacred-sha] PASS: N files verified`"
    - "When ANY entry mismatches → check-sacred.sh exits non-zero with the offending path, expected SHA, actual SHA, and frozen_in_phase identifier"
    - "When a registered file is MISSING → check-sacred.sh exits non-zero with `[sacred-sha] ERROR: file not found: <path>`"
    - "Running `git commit` on HEAD with no changes passes the hook (regression test: pre-commit doesn't break clean commits)"
  artifacts:
    - path: "scripts/sacred-shas-v38.json"
      provides: "v38 sacred file SHA pin registry"
      contains: "sdk-agent-runner.ts"
      min_lines: 80
    - path: "scripts/check-sacred.sh"
      provides: "JSON-driven sacred SHA check (replaces hardcoded single-file check)"
      contains: "sacred-shas-v38.json"
      min_lines: 40
    - path: "scripts/__tests__/test-check-sacred-v38.sh"
      provides: "CI test verifying the hook PASSes on current HEAD and FAILs when a sacred file is mutated"
      min_lines: 60
  key_links:
    - from: "scripts/check-sacred.sh"
      to: "scripts/sacred-shas-v38.json"
      via: "jq parse loop"
      pattern: "sacred-shas-v38\\.json"
    - from: ".husky/pre-commit"
      to: "scripts/check-sacred.sh"
      via: "exec sh"
      pattern: "check-sacred\\.sh"
---

<objective>
Replace the hardcoded single-file sacred SHA check in `scripts/check-sacred.sh` with a JSON-driven registry that locks all Phase 171 vault-items files and all Phase 172 @livos/cli src files (in addition to the existing Phase 97 sdk-agent-runner.ts pin). Ship a CI test that proves the hook PASSes on current HEAD and FAILs when any sacred file is mutated.

Purpose: Phase 171 + 172 finalized the v38 backend + CLI surface. Without a byte-freeze, future phases can accidentally drift these files (the exact failure mode that D-100-SACRED prevents for sdk-agent-runner.ts). This plan extends the existing sacred-SHA mechanism — same hook, same exit-code contract — but data-driven so future v38 freezes (e.g. Phase 175 backend) can ADD entries via JSON edits without touching the hook script.

Output: 1 NEW JSON registry (`scripts/sacred-shas-v38.json`) with the 19 Phase 171+172 SHAs computed at planning time + the existing Phase 97 entry, 1 MOD to `scripts/check-sacred.sh` (rewrites to iterate the JSON), 1 NEW bash test (`scripts/__tests__/test-check-sacred-v38.sh`) exercising both PASS and FAIL paths.
</objective>

<execution_context>
@C:\Users\hello\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hello\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/173-vault-rename-migration/173-CONTEXT.md
@scripts/check-sacred.sh
@scripts/verify-sacred-sha.sh
@scripts/verify-sacred-sha.cjs
@.husky/pre-commit

<interfaces>
<!-- Current pre-commit hook chain (verified) -->
- `.husky/pre-commit` execs `sh "$(git rev-parse --show-toplevel)/scripts/check-sacred.sh"`
- `scripts/check-sacred.sh` currently hardcodes ONE file (`liv/packages/core/src/sdk-agent-runner.ts`) and one SHA (`f3538e1d811992b782a9bb057d1b7f0a0189f95f`)
- `scripts/verify-sacred-sha.sh` + `.cjs` are dev-side audit harnesses (separate concern; do NOT modify)

<!-- SHAs computed during planning (git hash-object on current HEAD, 2026-05-20) -->

```json
{
  "phase_97_sdk_runner": {
    "liv/packages/core/src/sdk-agent-runner.ts": "f3538e1d811992b782a9bb057d1b7f0a0189f95f"
  },
  "phase_171_vault_items": {
    "livos/packages/livinityd/source/modules/vault-items/types.ts":               "b95ec8c5c1ec98d9aebc33582eadadefe4fc2cdd",
    "livos/packages/livinityd/source/modules/vault-items/vault-root-resolver.ts": "b1e22923e5ad0fe23bfdac84de8c982c5ddd0030",
    "livos/packages/livinityd/source/modules/vault-items/item-store.ts":          "8bafbdceb34826a02950cc5242fc0357dc5288cc",
    "livos/packages/livinityd/source/modules/vault-items/tree-resolver.ts":       "ce4b8320f30de2db51c9f247dfb8d2faef1d3a6d",
    "livos/packages/livinityd/source/modules/vault-items/pubsub.ts":              "35e62155dc42a5205085392607882054b8966449",
    "livos/packages/livinityd/source/modules/vault-items/index.ts":               "5045b76ce9b71f9817a0f69108ddb8aa3bc495fb"
  },
  "phase_172_cli_root": {
    "livos/packages/cli/src/cli.ts":              "28496c16d321bf12ef44850ae84e5ab99b32cb28",
    "livos/packages/cli/src/auth.ts":             "23fa2a0738b7183825df595ee341c37f17bd842c",
    "livos/packages/cli/src/version.ts":          "ff67758efe2c289c572f093babcec7e94cda9f86",
    "livos/packages/cli/src/vault-bootstrap.ts":  "dfac729b5c26cbdc47b88135d417a930cd7f5203",
    "livos/packages/cli/src/filesystem-mode.ts":  "602ed2b2b738f76578b07f62196fb8c689b800b7",
    "livos/packages/cli/src/query-client.ts":     "4dea59a7d640f60102a6bd69049eeea5c6b18683"
  },
  "phase_172_cli_commands": {
    "livos/packages/cli/src/commands/agent.ts":   "ffc7fe916875f29df89fa09a7453c90096c8ca0e",
    "livos/packages/cli/src/commands/attach.ts":  "3a9999e0788fd865514e3d7fb64189f0a1b0698d",
    "livos/packages/cli/src/commands/chat.ts":    "074d08fb306f868b78980a0d0912f10ed9db2aed",
    "livos/packages/cli/src/commands/config.ts":  "2e65bb6c75fcd788d32c1ede303a240b129e0301",
    "livos/packages/cli/src/commands/doctor.ts":  "8bcbd0079459f71ff333a6d146761bdcca95d367",
    "livos/packages/cli/src/commands/init.ts":    "d6f1ea80ceb5effca9ddfc236580dd4a70b19217",
    "livos/packages/cli/src/commands/list.ts":    "116e6981f1eff7045b9b1351cbaf0acfcd88b5f1",
    "livos/packages/cli/src/commands/migrate.ts": "57503f3109b8e0a8f7cf10e9f1b0cf9f7013d105",
    "livos/packages/cli/src/commands/project.ts": "cf54f3ba3a7febafb89792f0800cccce426066ad",
    "livos/packages/cli/src/commands/query.ts":   "b9c339849623f526ba080154526f4be183d025b7"
  },
  "phase_172_cli_query": {
    "livos/packages/cli/src/query/handlers.ts":   "b6039e0f9774622aa36b8d180021b2271782be73",
    "livos/packages/cli/src/query/registry.ts":   "bed545d223ea9a7527b6818bedffb31e0603bbfe"
  }
}
```

NOTE for executor: BEFORE writing the JSON, RE-COMPUTE each SHA with `git hash-object <path>` to confirm none drifted between planning (now) and execution. If a SHA differs, use the freshly-computed value and note the drift in the SUMMARY.

NOTE on inclusion: the test files (`*.test.ts`) are NOT in the registry — tests are expected to grow as new code lands, freezing them would block legitimate test additions. Only production source files are pinned.

NOTE on exclusion: migration-v35-to-v38.ts (Phase 173-02) is NOT in the registry. Phase 173-03 cannot freeze a file Phase 173-02 just shipped — circular within the same wave. A LATER phase (e.g. Phase 175) will add it.

<!-- Sacred guards (DO NOT MODIFY in this plan beyond check-sacred.sh) -->
- scripts/verify-sacred-sha.sh and .cjs (Phase 97 audit harness — separate concern)
- .husky/pre-commit (already correct — it execs check-sacred.sh)
- All files listed in the registry above are READ-ONLY for this plan; we only compute their SHAs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/sacred-shas-v38.json (JSON registry)</name>
  <read_first>
    - scripts/check-sacred.sh (current hardcoded single-file shape — to understand the contract)
    - .planning/phases/173-vault-rename-migration/173-03-PLAN.md (this plan — the SHA table in interfaces block)
  </read_first>
  <files>scripts/sacred-shas-v38.json</files>
  <action>
Compute current SHAs for every file listed below using `git hash-object <path>`. If a SHA differs from the planning-time value in the interfaces block above, use the FRESH SHA (note drift in SUMMARY).

Then write `scripts/sacred-shas-v38.json` as an array of records:

```json
[
  {
    "path": "liv/packages/core/src/sdk-agent-runner.ts",
    "expected_sha": "f3538e1d811992b782a9bb057d1b7f0a0189f95f",
    "frozen_in_phase": "97-auto-mode",
    "rationale": "D-100-SACRED — SDK agent runner is the load-bearing extension surface; all wrappers go through liv-agent-runner.ts + mcp-client-manager.ts."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/types.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "D-V38-B canonical Item discriminated union; downstream plans MUST narrow on `type` exhaustively."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/vault-root-resolver.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "D-V38-A vault root resolution; Phase 173-04 systemd env var depends on the exact fallback string."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/item-store.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "D-V38-T canonical folder layout writer + atomic CRUD; Phase 173-02 migration consumes this verbatim."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/tree-resolver.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "D-V38-E cycle/depth guard; Phase 174 SidebarTree consumes tree.json directly."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/pubsub.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "Redis liv:tree:updated event contract; UI subscribers depend on payload shape."
  },
  {
    "path": "livos/packages/livinityd/source/modules/vault-items/index.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "171-item-model-storage",
    "rationale": "Barrel export surface — downstream imports stable."
  },
  {
    "path": "livos/packages/cli/src/cli.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "D-V38-G CLI command surface; npm-published bin entrypoint."
  },
  {
    "path": "livos/packages/cli/src/auth.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "D-V38-H auth-mode resolution (filesystem vs daemon)."
  },
  {
    "path": "livos/packages/cli/src/version.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Version banner pinned to @livos/cli package.json."
  },
  {
    "path": "livos/packages/cli/src/vault-bootstrap.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "D-V38-T canonical folder layout writer (CLI-side mirror of livinityd vault-scaffolder)."
  },
  {
    "path": "livos/packages/cli/src/filesystem-mode.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Read-only vault traversal when livinityd is unreachable."
  },
  {
    "path": "livos/packages/cli/src/query-client.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "tRPC client wrapper — wire-format contract."
  },
  {
    "path": "livos/packages/cli/src/commands/agent.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/attach.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/chat.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/config.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/doctor.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/init.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/list.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/migrate.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/project.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/commands/query.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Command handler — D-V38-G surface."
  },
  {
    "path": "livos/packages/cli/src/query/handlers.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Query handler registry — wire-format contract."
  },
  {
    "path": "livos/packages/cli/src/query/registry.ts",
    "expected_sha": "<fresh-git-hash-object>",
    "frozen_in_phase": "172-livos-cli-skeleton",
    "rationale": "Longest-prefix query routing — wire-format contract."
  }
]
```

REPLACE every `<fresh-git-hash-object>` placeholder with the output of `git hash-object <path>` run against the current working tree. Resulting JSON MUST be valid (parses with `python -m json.tool` or `node -e "JSON.parse(require('fs').readFileSync(0,'utf8'))"`).

Total entries: 25 (1 Phase 97 + 6 Phase 171 + 6 Phase 172 root + 10 Phase 172 commands + 2 Phase 172 query).
  </action>
  <verify>
    <automated>node -e "const r=JSON.parse(require('fs').readFileSync('scripts/sacred-shas-v38.json','utf8'));if(!Array.isArray(r))throw new Error('not array');if(r.length!==25)throw new Error('expected 25, got '+r.length);for(const e of r){if(!e.path||!e.expected_sha||!e.frozen_in_phase)throw new Error('bad entry '+JSON.stringify(e));if(!/^[0-9a-f]{40}$/.test(e.expected_sha))throw new Error('bad sha '+e.expected_sha)}console.log('OK '+r.length+' entries')"</automated>
  </verify>
  <done>scripts/sacred-shas-v38.json exists, parses as valid JSON, contains exactly 25 records, every record has {path, expected_sha (40-hex), frozen_in_phase}, sdk-agent-runner.ts entry pins SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite scripts/check-sacred.sh to iterate the JSON registry</name>
  <read_first>
    - scripts/check-sacred.sh (current 19-line hardcoded version)
    - scripts/sacred-shas-v38.json (from Task 1)
    - .husky/pre-commit (caller — confirms `exec sh scripts/check-sacred.sh` contract)
  </read_first>
  <files>scripts/check-sacred.sh</files>
  <action>
REPLACE the entire contents of `scripts/check-sacred.sh` with the data-driven version below. The new script MUST preserve the existing exit-code contract (0 = PASS, non-zero = ABORT) and the `.husky/pre-commit` hook MUST keep working unmodified.

Required new body:

```sh
#!/usr/bin/env sh
# D-100-SACRED gate (Phase 173-03 rewrite):
# Read scripts/sacred-shas-v38.json and verify every (path, expected_sha)
# pair via git hash-object. Replaces the original hardcoded single-file
# check while preserving the exit-code contract for .husky/pre-commit.
#
# Exit codes:
#   0  — PASS, all sacred files unchanged
#   1  — FAIL, at least one sacred SHA mismatch OR missing file
#   2  — Setup error (registry missing, jq unavailable for fallback, etc)

set -e

REGISTRY="scripts/sacred-shas-v38.json"

if [ ! -f "$REGISTRY" ]; then
    echo "[sacred-sha] ERROR: registry not found: $REGISTRY" >&2
    exit 2
fi

# Use node (always present in this repo via package.json engines) to extract
# (path TAB expected_sha TAB frozen_in_phase) lines from the JSON registry.
# This keeps the hook portable across Linux / macOS / Windows-git-bash without
# requiring jq.
LINES=$(node -e '
const r = JSON.parse(require("fs").readFileSync(process.argv[1], "utf8"));
if (!Array.isArray(r)) { console.error("registry is not an array"); process.exit(2); }
for (const e of r) {
    if (!e.path || !e.expected_sha || !e.frozen_in_phase) {
        console.error("[sacred-sha] bad entry: " + JSON.stringify(e));
        process.exit(2);
    }
    process.stdout.write(e.path + "\t" + e.expected_sha + "\t" + e.frozen_in_phase + "\n");
}
' "$REGISTRY") || {
    echo "[sacred-sha] ERROR: failed to parse $REGISTRY" >&2
    exit 2
}

FAIL_COUNT=0
TOTAL=0

# IFS="\n" to handle paths safely; we control the JSON so no spaces in paths.
OLDIFS=$IFS
IFS='
'
for LINE in $LINES; do
    TOTAL=$((TOTAL + 1))
    FILE=$(printf '%s' "$LINE" | cut -f1)
    EXPECTED=$(printf '%s' "$LINE" | cut -f2)
    FROZEN=$(printf '%s' "$LINE" | cut -f3)

    if [ ! -f "$FILE" ]; then
        echo "[sacred-sha] ERROR: file not found: $FILE (frozen_in_phase=$FROZEN)" >&2
        FAIL_COUNT=$((FAIL_COUNT + 1))
        continue
    fi

    ACTUAL=$(git hash-object "$FILE")
    if [ "$ACTUAL" != "$EXPECTED" ]; then
        echo "[sacred-sha] ABORT: D-100-SACRED violated." >&2
        echo "[sacred-sha]   file:           $FILE" >&2
        echo "[sacred-sha]   expected SHA:   $EXPECTED" >&2
        echo "[sacred-sha]   actual SHA:     $ACTUAL" >&2
        echo "[sacred-sha]   frozen_in_phase: $FROZEN" >&2
        FAIL_COUNT=$((FAIL_COUNT + 1))
    fi
done
IFS=$OLDIFS

if [ "$FAIL_COUNT" -gt 0 ]; then
    echo "[sacred-sha] FAIL: $FAIL_COUNT of $TOTAL sacred files violated." >&2
    exit 1
fi

echo "[sacred-sha] PASS: $TOTAL files verified"
exit 0
```

Make sure the file has the executable bit (`chmod +x scripts/check-sacred.sh`).

Verify the husky hook still chains correctly:
- `.husky/pre-commit` execs `sh "$(git rev-parse --show-toplevel)/scripts/check-sacred.sh"` — unchanged, still works.
  </action>
  <verify>
    <automated>sh -n scripts/check-sacred.sh && sh scripts/check-sacred.sh | grep -qE "PASS: 25 files verified"</automated>
  </verify>
  <done>scripts/check-sacred.sh rewritten, `sh -n` clean, running it on current HEAD prints `[sacred-sha] PASS: 25 files verified` and exits 0. .husky/pre-commit unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Write scripts/__tests__/test-check-sacred-v38.sh (CI test for PASS + FAIL paths)</name>
  <read_first>
    - scripts/check-sacred.sh (from Task 2 — script under test)
    - scripts/sacred-shas-v38.json (from Task 1 — registry under test)
    - scripts/install/__tests__/test-migrate-script.sh (shape reference — green/red helpers)
  </read_first>
  <files>scripts/__tests__/test-check-sacred-v38.sh</files>
  <action>
First, ensure the test directory exists: `mkdir -p scripts/__tests__`.

Create NEW executable test:

```bash
#!/usr/bin/env bash
# scripts/__tests__/test-check-sacred-v38.sh
# Phase 173-03 — verify the JSON-driven sacred SHA check passes on HEAD
# AND correctly fails when a registered file is mutated or missing.
# CI-safe — uses a per-test sandbox copy of the registry.

set -uo pipefail

REPO_ROOT=$(cd "$(dirname "$0")/../.." && pwd)
HOOK="$REPO_ROOT/scripts/check-sacred.sh"
REGISTRY="$REPO_ROOT/scripts/sacred-shas-v38.json"

GREEN='\033[0;32m'; RED='\033[0;31m'; YELLOW='\033[1;33m'; NC='\033[0m'
pass_count=0
fail_count=0
pass() { echo -e "${GREEN}PASS${NC}: $*"; pass_count=$((pass_count + 1)); }
fail() { echo -e "${RED}FAIL${NC}: $*"; fail_count=$((fail_count + 1)); }
info() { echo -e "${YELLOW}INFO${NC}: $*"; }

# ── AC-173-03-1: shape — hook + registry exist, sh -n clean ──────────────────
info "AC-173-03-1: shape"
[[ -f "$HOOK" ]] && pass "check-sacred.sh exists" || fail "check-sacred.sh missing"
[[ -f "$REGISTRY" ]] && pass "registry exists" || fail "registry missing"
sh -n "$HOOK" 2>/dev/null && pass "sh -n clean" || fail "sh -n failed"

# ── AC-173-03-2: registry is valid JSON array with required keys ─────────────
info "AC-173-03-2: registry JSON shape"
if node -e "const r=JSON.parse(require('fs').readFileSync('$REGISTRY','utf8'));if(!Array.isArray(r))process.exit(1);for(const e of r){if(!e.path||!e.expected_sha||!e.frozen_in_phase)process.exit(1);if(!/^[0-9a-f]{40}\$/.test(e.expected_sha))process.exit(1)}" 2>/dev/null; then
    pass "registry parses with required keys"
else
    fail "registry shape invalid"
fi

# ── AC-173-03-3: registry contains the sacred sdk-agent-runner entry ─────────
info "AC-173-03-3: sdk-agent-runner pin present"
if grep -qF "f3538e1d811992b782a9bb057d1b7f0a0189f95f" "$REGISTRY"; then
    pass "sdk-agent-runner SHA pinned"
else
    fail "sdk-agent-runner SHA missing from registry"
fi

# ── AC-173-03-4: hook PASSes on current HEAD ─────────────────────────────────
info "AC-173-03-4: PASS path on HEAD"
cd "$REPO_ROOT"
out=$(sh "$HOOK" 2>&1); rc=$?
if [[ $rc -eq 0 ]]; then pass "hook exited 0 on HEAD"; else fail "hook exited $rc on HEAD (expected 0); output=$out"; fi
if echo "$out" | grep -qE "PASS: [0-9]+ files verified"; then pass "hook printed PASS summary"; else fail "hook missing PASS summary"; fi

# ── AC-173-03-5: hook FAILs when a registered file is mutated ────────────────
info "AC-173-03-5: FAIL path on mutated file"
# Pick the first non-sacred file we can safely mutate: the migration file we ship in 173-02 is NOT in the registry,
# so we can't use it. Use a temp file + temp registry override to simulate.
SANDBOX=$(mktemp -d)
cp "$REGISTRY" "$SANDBOX/registry.json"
TARGET=$(node -e "const r=JSON.parse(require('fs').readFileSync('$REGISTRY','utf8'));process.stdout.write(r[0].path)")
ORIG_SHA=$(git hash-object "$TARGET")
# Inject a bogus expected_sha into a sandbox copy of the hook script that points at sandbox registry
SANDBOX_HOOK="$SANDBOX/check-sacred.sh"
sed "s|scripts/sacred-shas-v38\\.json|$SANDBOX/registry.json|g" "$HOOK" > "$SANDBOX_HOOK"
chmod +x "$SANDBOX_HOOK"
# Corrupt the expected_sha for the first entry
node -e "
const fs=require('fs');
const r=JSON.parse(fs.readFileSync('$SANDBOX/registry.json','utf8'));
r[0].expected_sha='0000000000000000000000000000000000000000';
fs.writeFileSync('$SANDBOX/registry.json',JSON.stringify(r,null,2));
"
out2=$(sh "$SANDBOX_HOOK" 2>&1); rc2=$?
if [[ $rc2 -ne 0 ]]; then pass "mutated registry → hook exited $rc2 (non-zero)"; else fail "mutated registry → hook exited 0 (expected non-zero)"; fi
if echo "$out2" | grep -qF "D-100-SACRED violated"; then pass "hook printed D-100-SACRED message"; else fail "hook missing D-100-SACRED message"; fi
if echo "$out2" | grep -qF "$ORIG_SHA"; then pass "hook printed actual SHA"; else fail "hook missing actual SHA"; fi
rm -rf "$SANDBOX"

# ── AC-173-03-6: hook FAILs when a registered file is missing ────────────────
info "AC-173-03-6: FAIL path on missing file"
SANDBOX=$(mktemp -d)
cp "$REGISTRY" "$SANDBOX/registry.json"
SANDBOX_HOOK="$SANDBOX/check-sacred.sh"
sed "s|scripts/sacred-shas-v38\\.json|$SANDBOX/registry.json|g" "$HOOK" > "$SANDBOX_HOOK"
chmod +x "$SANDBOX_HOOK"
node -e "
const fs=require('fs');
const r=JSON.parse(fs.readFileSync('$SANDBOX/registry.json','utf8'));
r[0].path='no/such/file-does-not-exist.ts';
fs.writeFileSync('$SANDBOX/registry.json',JSON.stringify(r,null,2));
"
out3=$(sh "$SANDBOX_HOOK" 2>&1); rc3=$?
if [[ $rc3 -ne 0 ]]; then pass "missing file → hook exited $rc3 (non-zero)"; else fail "missing file → hook exited 0 (expected non-zero)"; fi
if echo "$out3" | grep -qF "file not found"; then pass "hook printed file-not-found message"; else fail "hook missing file-not-found message"; fi
rm -rf "$SANDBOX"

# ── Summary ──────────────────────────────────────────────────────────────────
echo ""
echo "─────────────────────────────────────────"
echo "PASS: $pass_count   FAIL: $fail_count"
echo "─────────────────────────────────────────"
[[ $fail_count -eq 0 ]] && exit 0 || exit 1
```

`chmod +x scripts/__tests__/test-check-sacred-v38.sh` after writing.
  </action>
  <verify>
    <automated>bash scripts/__tests__/test-check-sacred-v38.sh</automated>
  </verify>
  <done>Test exits 0 with all PASS lines (≥12 PASS, 0 FAIL); exercises shape, registry validity, sdk-agent-runner pin presence, HEAD PASS path, mutated-file FAIL path, missing-file FAIL path.</done>
</task>

</tasks>

<verification>
1. `sh scripts/check-sacred.sh` exits 0 and prints `[sacred-sha] PASS: 25 files verified`
2. `bash scripts/__tests__/test-check-sacred-v38.sh` exits 0 with 0 FAILs
3. `.husky/pre-commit` still execs check-sacred.sh — no change required there
4. `node scripts/verify-sacred-sha.cjs` still passes (Phase 97 audit harness unchanged)
5. JSON registry contains exactly 25 entries, every SHA is a valid 40-char hex
</verification>

<success_criteria>
- JSON-driven sacred SHA registry shipped with 25 entries (1 Phase 97 + 24 Phase 171/172)
- check-sacred.sh rewritten to iterate the JSON without breaking the husky pre-commit contract
- CI test proves PASS-on-HEAD, FAIL-on-mutation, FAIL-on-missing-file
- migration-v35-to-v38.ts (Phase 173-02) intentionally NOT included — circular within wave 1
- No sacred file mutated by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/173-vault-rename-migration/173-03-SUMMARY.md` documenting: 25 entries pinned, any SHA drift detected between planning (2026-05-20) and execution noted explicitly, registry extension recipe for future phases (add a JSON record, re-run the test, done — no shell-script edits required).
</output>
