---
plan: 169-04
phase: 169
type: execute
wave: 4
depends_on: ["169-03"]
files_modified:
  - "MOD livos/packages/ui/src/routes/ai-chat/index.tsx (add Terminal | Vault Graph tab nav)"
autonomous: true
requirements: [V35-AI-CHAT-TAB-NAV]
sacred_guards:
  - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f preserved"
  - "D-09 luse-system-prompt.ts unchanged"
  - "Phase 161-02 server files unchanged"
  - "Phase 162-01 server files unchanged"
  - "Phase 162-02 server files unchanged"
  - "Phase 163 server files unchanged"
  - "Phase 164 server files unchanged"
  - "Phase 165-01 server files unchanged"
  - "Phase 166 cc-pty/* server files unchanged"
  - "Phase 167 features/cc-terminal/* unchanged (tab nav ADDS a sibling mount; does NOT remove CcTerminal)"
  - "D-NEW-DEPS-v35: NO new deps in this plan"

must_haves:
  truths:
    - "AI Chat route renders a tab strip with 'Terminal' and 'Vault Graph' buttons"
    - "Default activeTab === 'terminal' on first mount (preserves Phase 167 default behavior)"
    - "Clicking Vault Graph button switches activeTab to 'graph'"
    - "Terminal tab still renders CcTerminal when activeSessionId is set"
    - "Terminal tab still renders EmptyState when activeSessionId is null"
    - "Graph tab renders VaultGraph component"
    - "Tab switch remounts the inactive panel (v1 simplicity — per 169-CONTEXT.md L356)"
    - "SessionSidebar (left 280px column) remains unchanged"
  artifacts:
    - path: "livos/packages/ui/src/routes/ai-chat/index.tsx"
      provides: "AiChatRoute with tab nav (Terminal/Vault Graph)"
      contains: "activeTab"
  key_links:
    - from: "AiChatRoute"
      to: "VaultGraph (169-03)"
      via: "import { VaultGraph } from '@/features/vault-graph'"
      pattern: "from.*features/vault-graph"
    - from: "AiChatRoute"
      to: "CcTerminal (167)"
      via: "existing import (unchanged)"
      pattern: "CcTerminal"
---

<objective>
Add a 2-tab navigation strip ("Terminal" | "Vault Graph") to `livos/packages/ui/src/routes/ai-chat/index.tsx`, conditionally rendering either the existing `CcTerminal` (Phase 167) or the new `VaultGraph` (169-03) without altering the 280px SessionSidebar layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-vault-graph/169-CONTEXT.md
@.planning/phases/169-vault-graph/169-03-PLAN.md
@.planning/phases/167-xterm-frontend/167-04-SUMMARY.md
@.planning/v35-CC-PTY-MASTER.md
@livos/packages/ui/src/routes/ai-chat/index.tsx
@livos/packages/ui/src/features/vault-graph/index.ts

<!-- 169-CONTEXT.md L356: pick REMOUNT on tab switch (simpler memory model). If users complain about lost graph zoom state, switch to CSS display:none persistence in a follow-up. -->

<interfaces>
<!-- Existing AiChatRoute (Phase 167-04) exports default. After this plan, the same default export is retained — only its render tree gains a tab strip. -->

```tsx
// Imports the existing layout uses (verify by reading the route file before editing):
//   useIsMobile, useState, SessionSidebar, CcTerminal, EmptyState, MobileFallback
// New import added by this plan:
import { VaultGraph } from '@/features/vault-graph';
//   (adjust to the actual alias used by the existing imports — likely '@livos/ui/features/...' or relative)

type Tab = 'terminal' | 'graph';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Terminal | Vault Graph tab nav to AiChatRoute (remount-on-switch)</name>
  <files>
    livos/packages/ui/src/routes/ai-chat/index.tsx
    livos/packages/ui/src/routes/ai-chat/index.test.tsx
  </files>
  <read_first>
    livos/packages/ui/src/routes/ai-chat/index.tsx (current Phase 167-04 layout — preserve sidebar + CcTerminal mount path)
    livos/packages/ui/src/features/vault-graph/index.ts (169-03 barrel — import VaultGraph from here)
    .planning/phases/169-vault-graph/169-CONTEXT.md (lines 305-351 — verbatim layout)
    .planning/phases/167-xterm-frontend/167-04-SUMMARY.md (existing imports to preserve)
  </read_first>
  <behavior>
    - Test 1: Renders both `<button>Terminal</button>` and `<button>Vault Graph</button>` (text match)
    - Test 2: On initial mount, activeTab === 'terminal' (CcTerminal/EmptyState branch is rendered)
    - Test 3: Click "Vault Graph" button → VaultGraph component is rendered (assert presence via test id or mock spy)
    - Test 4: Click "Terminal" button after switching back → CcTerminal or EmptyState re-renders
    - Test 5: With activeSessionId === null AND activeTab === 'terminal', EmptyState renders (NOT VaultGraph)
  </behavior>
  <action>
Read the existing `livos/packages/ui/src/routes/ai-chat/index.tsx` first to capture the actual import paths used by Phase 167-04 (the import for VaultGraph must match the same alias style — relative vs `@/` vs `@livos/ui/` — used in the existing file).

Apply the verbatim 169-CONTEXT.md L305-351 layout, adapted to the existing import style:

```tsx
import { useState } from 'react';
import { useIsMobile } from '<existing-path>';
import { SessionSidebar } from '<existing-path>';
import { CcTerminal } from '<existing-path>';
import { EmptyState } from '<existing-path>';
import { MobileFallback } from '<existing-path>';
import { VaultGraph } from '<resolve-to-features/vault-graph>'; // NEW (169-03 barrel)

type Tab = 'terminal' | 'graph';

export default function AiChatRoute() {
  const isMobile = useIsMobile();
  const [activeSessionId, setActiveSessionId] = useState<string | null>(null);
  const [activeTab, setActiveTab] = useState<Tab>('terminal');

  if (isMobile) return <MobileFallback />;

  return (
    <div className="grid h-full" style={{ gridTemplateColumns: '280px 1fr' }}>
      <SessionSidebar
        activeSessionId={activeSessionId}
        onSelect={setActiveSessionId}
      />
      <div className="flex h-full flex-col overflow-hidden">
        <div className="flex border-b border-border bg-bg-secondary">
          <button
            type="button"
            onClick={() => setActiveTab('terminal')}
            className={`px-4 py-2 text-sm ${activeTab === 'terminal' ? 'border-b-2 border-primary text-primary' : 'text-text-secondary'}`}
          >
            Terminal
          </button>
          <button
            type="button"
            onClick={() => setActiveTab('graph')}
            className={`px-4 py-2 text-sm ${activeTab === 'graph' ? 'border-b-2 border-primary text-primary' : 'text-text-secondary'}`}
          >
            Vault Graph
          </button>
        </div>
        <div className="flex-1 overflow-hidden">
          {activeTab === 'terminal' ? (
            activeSessionId ? (
              <CcTerminal key={activeSessionId} sessionId={activeSessionId} />
            ) : (
              <EmptyState />
            )
          ) : (
            <VaultGraph />
          )}
        </div>
      </div>
    </div>
  );
}
```

Notes:
- DO NOT change any prop signatures of `SessionSidebar`, `CcTerminal`, `EmptyState`, `MobileFallback` — they belong to Phase 167.
- DO NOT remove any existing imports — only ADD the `VaultGraph` import.
- Preserve any existing `useEffect`/`useMemo` blocks already in the route (if present).

Create `index.test.tsx` with 5 vitest assertions. Mock `VaultGraph`, `CcTerminal`, `EmptyState`, `SessionSidebar`, `MobileFallback` via `vi.mock` with `data-testid` stubs so tests don't need the full subtree. Mock `useIsMobile` to return `false`.

```ts
vi.mock('@/features/vault-graph', () => ({ VaultGraph: () => <div data-testid="vault-graph" /> }));
// etc.
```
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livos/ui test:run -- routes/ai-chat/index.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `livos/packages/ui/src/routes/ai-chat/index.tsx` contains exactly one new top-level import (`VaultGraph`)
    - `git diff livos/packages/ui/src/routes/ai-chat/index.tsx` shows additions ONLY (no removals to CcTerminal/EmptyState/MobileFallback imports or mounts)
    - 5 vitest assertions pass
    - `grep -n "CcTerminal" livos/packages/ui/src/routes/ai-chat/index.tsx` still finds the previous mount line (sacred guard: Phase 167 unchanged)
  </acceptance_criteria>
  <done>ai-chat/index.tsx gains tab nav; 5 assertions pass; Phase 167 mount of CcTerminal preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route component → child components | both child trees (CcTerminal, VaultGraph) own their own auth-scoped fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-04-01 | Information Disclosure | tab switching leaks state | accept | Remount-on-switch (v1 spec) — VaultGraph and CcTerminal unmount when not active, so no stale data from the other tab leaks into render. Future polish could persist via CSS display:none. |
| T-169-04-02 | Tampering | new tab introduces new attack surface | mitigate | VaultGraph's network calls are all auth-gated by 169-02 backend. No new fetch logic added in this plan. |
| T-169-04-03 | Spoofing | tab labels could be confused | accept | Plain labels, no user-controlled content. |
</threat_model>

<verification>
- `pnpm --filter @livos/ui typecheck` passes
- `pnpm --filter @livos/ui test:run -- routes/ai-chat` passes 5 assertions
- `grep -c "CcTerminal" livos/packages/ui/src/routes/ai-chat/index.tsx` ≥1 (Phase 167 mount preserved)
- `grep -c "VaultGraph" livos/packages/ui/src/routes/ai-chat/index.tsx` ≥1 (new mount present)
- `git diff --stat livos/packages/ui/src/routes/ai-chat/index.tsx` shows additions ≥ removals (additive change)
- Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` still in HEAD ancestry
</verification>

<success_criteria>
- ai-chat/index.tsx contains Terminal/Vault Graph tab nav with default activeTab='terminal'
- 5 vitest assertions pass
- Phase 167 CcTerminal mount path preserved exactly
- No other files in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/169-vault-graph/169-04-SUMMARY.md`
</output>
