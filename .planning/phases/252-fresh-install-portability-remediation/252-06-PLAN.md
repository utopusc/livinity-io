---
phase: 252-fresh-install-portability-remediation
plan: 06
type: execute
wave: 5
depends_on: [01, 05]
files_modified:
  - livos/packages/livinityd/source/modules/computer-use/mcp/server.ts
  - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts
  - livos/packages/livinityd/source/modules/server/index.ts
  - scripts/install/seeds/mcp-servers.json
  - scripts/install/deploy-livinityd.sh
autonomous: true
requirements: [R13, R14, R15]

must_haves:
  truths:
    - "LUSE_USER_ID resolves to ONE default ('bruce') across the whole luse child — no admin-vs-bruce split — and is seeded explicitly."
    - "The luse install root derives from a single $LIVOS_ROOT/LIVOS_BASE_DIR source (default /opt/livos), not re-hardcoded in server.ts + tools.ts."
    - "The active-webapp-wid marker + /tmp/luse- allowlist are namespaced under $XDG_RUNTIME_DIR (per-user, symlink-safe), not world-shared /tmp."
  artifacts:
    - path: "livos/packages/livinityd/source/modules/computer-use/mcp/server.ts"
      provides: "LUSE_USER_ID default 'bruce' + $LIVOS_ROOT-derived env fallback paths"
      contains: "'bruce'"
    - path: "livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts"
      provides: "$XDG_RUNTIME_DIR-namespaced markers + derived uploads root + LUSE_USER_ID default 'bruce'"
      contains: "XDG_RUNTIME_DIR"
    - path: "scripts/install/seeds/mcp-servers.json"
      provides: "LUSE_USER_ID seeded on the luse entry"
      contains: "LUSE_USER_ID"
  key_links:
    - from: "server.ts LUSE_USER_ID default"
      to: "tools.ts LUSE_USER_ID default"
      via: "single shared 'bruce' default const"
      pattern: "LUSE_USER_ID"
    - from: "tools.ts active-wid marker"
      to: "$XDG_RUNTIME_DIR/livos/active-webapp-wid"
      via: "per-uid 0700 tmpfs path"
      pattern: "XDG_RUNTIME_DIR"
---

<objective>
P2 hygiene / multi-user-future hardening. (R13) the same env var `LUSE_USER_ID` defaults to `'admin'`
(server.ts:315) vs `'bruce'` (tools.ts:915-916) in one process and is unset on a fresh box — unify
on `'bruce'` (it drives the allowlist) + seed it. (R14) `/opt/livos` is a leaky half-parameter:
installer pins it (deploy :61 no override) while luse code re-hardcodes it (server.ts:124,
tools.ts:454) — derive from a single `$LIVOS_ROOT`/`LIVOS_BASE_DIR` source. (R15) the active-wid
marker (`/tmp/livos-active-webapp-wid`) + `/tmp/luse-` allowlist are world-shared with a TOCTOU
symlink-race surface — namespace under `$XDG_RUNTIME_DIR` (per-user 0700 tmpfs).

Purpose: remove the last identity/root/tmp inconsistencies so a non-bruce or moved-root box and a
future multi-user re-activation don't break or collide.
Output: one LUSE_USER_ID default, one root source, per-uid runtime-dir markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-fresh-install-portability-remediation/252-CONTEXT.md
@.planning/phases/251-fresh-install-portability-audit/findings/251-04-FINDINGS.md
@.planning/phases/251-fresh-install-portability-audit/findings/251-05-FINDINGS.md

<interfaces>
<!-- @livos/config already exposes an env-overridable install root (livos/packages/config/src/index.ts:10-11): -->
```typescript
export const paths = Object.freeze(PathsConfigSchema.parse({
  base: process.env.LIVOS_BASE_DIR,   // default '/opt/livos' (paths.ts:10)
  ...
}))
```
The luse MCP runs as a standalone `npx tsx .../server.ts` child; if its spawn env carries
`LIVOS_BASE_DIR` it can import `@livos/config` (livinityd already deps it), otherwise resolve
locally: `process.env.LIVOS_ROOT ?? process.env.LIVOS_BASE_DIR ?? '/opt/livos'`.

<!-- The split to unify (server.ts:315 vs tools.ts:915-916) -->
```typescript
userId: process.env.LUSE_USER_ID ?? 'admin'   // server.ts:315  ← change to 'bruce'
const userSlug = process.env.LUSE_USER_ID ?? 'bruce'   // tools.ts:915
const userId = process.env.LUSE_USER_ID ?? 'bruce'     // tools.ts:916
```

<!-- The /tmp surfaces (tools.ts:278 + tools.ts:451-455) -->
```typescript
const ACTIVE_WID_MARKER = '/tmp/livos-active-webapp-wid'   // :278
const allowlist = [`/home/${userSlug}/`, '/tmp/luse-', `/opt/livos/data/uploads/${userId}/`]  // :451-455
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify LUSE_USER_ID default to 'bruce' (code + seed) (R13)</name>
  <files>livos/packages/livinityd/source/modules/computer-use/mcp/server.ts, scripts/install/seeds/mcp-servers.json, scripts/install/deploy-livinityd.sh</files>
  <read_first>
    - livos/packages/livinityd/source/modules/computer-use/mcp/server.ts:308-316 (the registerLuseTools call — `userId: process.env.LUSE_USER_ID ?? 'admin'` at :315)
    - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts:908-917 (the `?? 'bruce'` pair at :915-916 — the allowlist-driving value to keep)
    - scripts/install/seeds/mcp-servers.json:174-186 (the luse env block — add LUSE_USER_ID alongside LUSE_USER_SLUG)
    - scripts/install/deploy-livinityd.sh:1125-1142 (the substitution prelude + sed block — note LUSE_USER_ID can be seeded as a STATIC 'bruce' literal in the JSON or substituted from user_slug)
    - .planning/phases/251-fresh-install-portability-audit/findings/251-04-FINDINGS.md (#9/#10 — the admin-vs-bruce divergence + unset-on-fresh-box)
  </read_first>
  <behavior>
    - Test 1: with `LUSE_USER_ID` unset, the luse server's resolved userId equals `'bruce'` (was 'admin').
    - Test 2: with `LUSE_USER_ID='alice'`, both server.ts and tools.ts resolve to `'alice'` (single source honored).
  </behavior>
  <action>
1. In `server.ts:315`, change `userId: process.env.LUSE_USER_ID ?? 'admin'` to
   `userId: process.env.LUSE_USER_ID ?? 'bruce'`. Optionally extract a shared module-level const
   `const DEFAULT_LUSE_USER_ID = 'bruce'` and use it at server.ts:315 AND export it for tools.ts to
   import (replacing the two `?? 'bruce'` literals at tools.ts:915-916) so there is ONE source. If a
   cross-file import is awkward in the standalone tsx child, keep the literal but make it `'bruce'`
   in all three sites — the acceptance grep enforces no remaining `'admin'` default.
2. In `seeds/mcp-servers.json`, add `"LUSE_USER_ID": "__LIVOS_USER_SLUG__"` to the luse `env` block
   (so the seeded value matches the slug the box was built with — single-tenant default 'bruce').
3. The existing `__LIVOS_USER_SLUG__` substitution (deploy-livinityd.sh:1140) already covers the new
   occurrence — no new sed line needed (the placeholder is global `g`). Confirm the substitution
   still produces valid JSON.
  </action>
  <acceptance_criteria>
    - `grep -c "LUSE_USER_ID ?? 'admin'" livos/packages/livinityd/source/modules/computer-use/mcp/server.ts` returns 0
    - `grep -q "LUSE_USER_ID ?? 'bruce'" livos/packages/livinityd/source/modules/computer-use/mcp/server.ts` exits 0
    - `grep -q '"LUSE_USER_ID"' scripts/install/seeds/mcp-servers.json` exits 0
    - `python3 -c "import json;json.load(open('scripts/install/seeds/mcp-servers.json'))"` exits 0
    - `pnpm --filter livinityd typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd livos && pnpm --filter livinityd typecheck; python3 -c "import json;json.load(open('scripts/install/seeds/mcp-servers.json'))" && ! grep -q "LUSE_USER_ID ?? 'admin'" packages/livinityd/source/modules/computer-use/mcp/server.ts</automated>
  </verify>
  <done>LUSE_USER_ID defaults to 'bruce' everywhere (no 'admin' default remains) and is seeded on the luse MCP entry; JSON valid; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Single $LIVOS_ROOT source of truth for the luse install root (R14)</name>
  <files>livos/packages/livinityd/source/modules/computer-use/mcp/server.ts, livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts, livos/packages/livinityd/source/modules/server/index.ts, scripts/install/deploy-livinityd.sh</files>
  <read_first>
    - livos/packages/livinityd/source/modules/computer-use/mcp/server.ts:113-139 (resolveLuseRedisUrl — the hardcoded `['/opt/livos/.env','/opt/livos/livos/.env']` array at :124)
    - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts:448-456 (the uploads prefix `/opt/livos/data/uploads/${userId}/` at :454)
    - livos/packages/config/src/paths.ts (the `base` default '/opt/livos') AND config/src/index.ts:10-11 (`base: process.env.LIVOS_BASE_DIR`)
    - livos/packages/livinityd/source/modules/server/index.ts:1823 (the dangling `<LIV_DATA_ROOT>` comment to reconcile)
    - scripts/install/deploy-livinityd.sh:61 (`_DLD_LIVOS_DIR="/opt/livos"` — bare literal, no `${VAR:-default}`)
    - .planning/phases/251-fresh-install-portability-audit/findings/251-05-FINDINGS.md (Task 2 recommendations — the leaky half-parameter)
  </read_first>
  <action>
Establish ONE install-root source and consume it everywhere the luse code re-hardcodes `/opt/livos`.

1. Add a tiny resolver at the top of `server.ts` (module scope):
   ```typescript
   // R14: single install-root source. Prefer $LIVOS_ROOT, then $LIVOS_BASE_DIR
   // (the @livos/config env var), default /opt/livos. The luse MCP runs as a
   // standalone tsx child so resolve locally rather than importing config (whose
   // env may not propagate to the child).
   const LIVOS_ROOT = process.env.LIVOS_ROOT ?? process.env.LIVOS_BASE_DIR ?? '/opt/livos'
   ```
   Then in `resolveLuseRedisUrl` (:124), change the hardcoded array to derive from it:
   ```typescript
   const paths = deps.envFilePaths ?? [`${LIVOS_ROOT}/.env`, `${LIVOS_ROOT}/livos/.env`]
   ```
   Export `LIVOS_ROOT` from server.ts so tools.ts can import it (same module-relative import).

2. In `tools.ts`, import `LIVOS_ROOT` from server.ts (or replicate the same one-line resolver if a
   circular import would result — check the existing import direction first). Change the uploads
   prefix (:454) from `/opt/livos/data/uploads/${userId}/` to
   `` `${LIVOS_ROOT}/data/uploads/${userId}/` `` (and update the echoed allowed-prefixes string at
   :926 to match, so the agent self-correction message stays accurate).

3. In `deploy-livinityd.sh:61`, make the root overridable to match its neighbours
   (`_DLD_LIVOS_USER` :79 etc.): change `_DLD_LIVOS_DIR="/opt/livos"` to
   `_DLD_LIVOS_DIR="${_DLD_LIVOS_DIR:-/opt/livos}"`.

4. In `server/index.ts:1823`, reconcile the dangling `<LIV_DATA_ROOT>` comment: either replace the
   placeholder with the concrete derived path it documents, or update the comment to name the
   actual env-rooted convention the project ships (`LIVOS_BASE_DIR`/`LIVOS_ROOT`). One-line comment
   fix — no behavioral change.

Do NOT change the daemon's already-parameterized `dataDirectory` flow — this fix only removes the
RE-hardcoded `/opt/livos` literals in the luse child + the bare installer literal.
  </action>
  <acceptance_criteria>
    - `grep -q 'LIVOS_ROOT' livos/packages/livinityd/source/modules/computer-use/mcp/server.ts` exits 0
    - `grep -c "'/opt/livos/.env', '/opt/livos/livos/.env'" livos/packages/livinityd/source/modules/computer-use/mcp/server.ts` returns 0
    - `grep -c "/opt/livos/data/uploads/" livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` returns 0 (the hardcoded prefix is gone; LIVOS_ROOT-derived)
    - `grep -q '_DLD_LIVOS_DIR="${_DLD_LIVOS_DIR:-/opt/livos}"' scripts/install/deploy-livinityd.sh` exits 0
    - `pnpm --filter livinityd typecheck` exits 0
    - `bash -n scripts/install/deploy-livinityd.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd livos && pnpm --filter livinityd typecheck && cd .. && bash -n scripts/install/deploy-livinityd.sh && grep -q '_DLD_LIVOS_DIR="${_DLD_LIVOS_DIR:-/opt/livos}"' scripts/install/deploy-livinityd.sh</automated>
  </verify>
  <done>The luse install root derives from $LIVOS_ROOT/$LIVOS_BASE_DIR (default /opt/livos) in server.ts + tools.ts; the installer root is overridable; the dangling comment is reconciled; typecheck + bash -n pass.</done>
</task>

<task type="auto">
  <name>Task 3: Namespace /tmp markers + allowlist under $XDG_RUNTIME_DIR (R15)</name>
  <files>livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts</files>
  <read_first>
    - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts:275-281 (`ACTIVE_WID_MARKER = '/tmp/livos-active-webapp-wid'` + the widCache mtime logic — note statSync→readFileSync usage = the TOCTOU surface)
    - livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts:448-457 (the allowlist incl. `/tmp/luse-` at :453)
    - .planning/phases/251-fresh-install-portability-audit/findings/251-05-FINDINGS.md (Task 1b — world-shared /tmp collision + symlink race, no O_NOFOLLOW)
  </read_first>
  <action>
Move the world-shared /tmp markers into a per-user 0700 runtime dir so multi-user re-activation
cannot collide and the symlink-follow surface is closed.

1. Add a module-level runtime-dir resolver near ACTIVE_WID_MARKER (:278):
   ```typescript
   // R15: per-user runtime dir (0700 tmpfs) instead of world-shared /tmp. Closes
   // the multi-user collision + TOCTOU symlink-follow surface on the marker file.
   const XDG_RUNTIME_DIR =
     process.env.XDG_RUNTIME_DIR && process.env.XDG_RUNTIME_DIR.length > 0
       ? process.env.XDG_RUNTIME_DIR
       : `/run/user/${process.getuid?.() ?? 1000}`
   const LIVOS_RUNTIME_DIR = `${XDG_RUNTIME_DIR}/livos`
   const ACTIVE_WID_MARKER = `${LIVOS_RUNTIME_DIR}/active-webapp-wid`
   ```
   (Replace the old `const ACTIVE_WID_MARKER = '/tmp/livos-active-webapp-wid'`.)
   Wherever the marker is WRITTEN, ensure the dir exists with mode 0700 first
   (`fs.mkdirSync(LIVOS_RUNTIME_DIR, {recursive: true, mode: 0o700})` — find the write site and add
   the mkdir guard). When READING the marker, prefer `fs.openSync(path, fs.constants.O_NOFOLLOW |
   fs.constants.O_RDONLY)` then `fs.readFileSync(fd)` (or `lstatSync` + reject symlinks) so a planted
   symlink at the path is not followed — closing the TOCTOU race the audit flagged.

2. In the allowlist (:453), change `'/tmp/luse-'` to a runtime-dir-scoped prefix:
   ```typescript
   `${XDG_RUNTIME_DIR}/luse-`,
   ```
   Update the echoed allowed-prefixes string at :926 to match (replace `/tmp/luse-` with the new
   prefix) so the agent self-correction message is accurate.

Keep all behavior backward-tolerant: if a tool elsewhere still writes a `/tmp/luse-*` temp file,
update that write site too (grep `/tmp/luse-` across tools.ts) so the allowlist and the writer agree.
  </action>
  <acceptance_criteria>
    - `grep -q 'XDG_RUNTIME_DIR' livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` exits 0
    - `grep -c "'/tmp/livos-active-webapp-wid'" livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` returns 0
    - `grep -c "'/tmp/luse-'" livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` returns 0
    - `grep -q 'O_NOFOLLOW' livos/packages/livinityd/source/modules/computer-use/mcp/tools.ts` exits 0 (symlink-follow closed) OR an lstat-reject guard is present
    - `pnpm --filter livinityd typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd livos && pnpm --filter livinityd typecheck && ! grep -q "'/tmp/livos-active-webapp-wid'" packages/livinityd/source/modules/computer-use/mcp/tools.ts && grep -q 'XDG_RUNTIME_DIR' packages/livinityd/source/modules/computer-use/mcp/tools.ts</automated>
  </verify>
  <done>The active-wid marker + luse- allowlist live under $XDG_RUNTIME_DIR/livos (0700, per-uid); the marker read closes the symlink-follow race; no /tmp world-shared paths remain; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| world-shared /tmp → luse marker/allowlist | any local user could plant a symlink or collide on the shared /tmp paths |
| env LUSE_USER_ID / LIVOS_ROOT → luse sandbox allowlist | these values drive which filesystem prefixes the agent may read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-17 | Tampering/EoP | TOCTOU symlink race on /tmp/livos-active-webapp-wid (statSync→readFileSync, no O_NOFOLLOW) | mitigate | move marker to `$XDG_RUNTIME_DIR/livos` (per-uid 0700 tmpfs, not world-writable) + read with O_NOFOLLOW / lstat-reject so a planted symlink is not followed |
| T-252-18 | Information Disclosure | multi-user /tmp collision leaks another user's active-wid / luse temp | mitigate | per-user runtime dir (0700) isolates each user's markers; the allowlist prefix is now per-uid |
| T-252-19 | EoP | LUSE_USER_ID drives the read-sandbox allowlist; a wrong default could widen access | mitigate | unify to 'bruce' (the single-tenant desktop user) + seed it explicitly; the allowlist is still user-scoped (`/home/<slug>/`, uploads/<userId>/) — narrower, not wider |
| T-252-20 | Tampering | $LIVOS_ROOT env override points the sandbox at an attacker dir | accept | the env is set by the installer/operator (host-local trust); same trust domain as the existing dataDirectory flag; default remains /opt/livos |
</threat_model>

<verification>
- `grep -c "?? 'admin'" server.ts` → 0; LUSE_USER_ID seeded in mcp-servers.json; JSON valid.
- No `'/opt/livos/.env'` array literal in server.ts; no `/opt/livos/data/uploads/` literal in tools.ts; `_DLD_LIVOS_DIR` overridable.
- No `/tmp/livos-active-webapp-wid` or `/tmp/luse-` literal in tools.ts; XDG_RUNTIME_DIR + symlink-safe read present.
- `pnpm --filter livinityd typecheck` exits 0; `bash -n deploy-livinityd.sh` clean.
- Sacred SHA hook PASS (no sdk-agent-runner.ts change).
</verification>

<success_criteria>
- One LUSE_USER_ID default ('bruce'), seeded explicitly.
- One $LIVOS_ROOT source for the luse install root; installer root overridable.
- Per-uid $XDG_RUNTIME_DIR markers + symlink-safe read; no world-shared /tmp paths.
- No TypeScript regression; no sacred-blob change.
</success_criteria>

<output>
After completion, create `.planning/phases/252-fresh-install-portability-remediation/252-06-SUMMARY.md`
</output>
