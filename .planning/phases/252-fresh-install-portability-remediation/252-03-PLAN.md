---
phase: 252-fresh-install-portability-remediation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md
autonomous: false
requirements: [R11]

must_haves:
  truths:
    - "The live get.livinity.io → install-script mapping is resolved from Cloudflare DNS + Vercel alias inspection and recorded as an auditable doc."
    - "The recorded entrypoint is confirmed to be Path A (scripts/install.sh → deploy-livinityd.sh) OR the actual path is documented with its consequence for the MCP seed."
  artifacts:
    - path: ".planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md"
      provides: "the resolved get.livinity.io entrypoint mapping + evidence"
      min_lines: 20
  key_links:
    - from: "get.livinity.io DNS/Vercel alias"
      to: "the install script it serves"
      via: "live curl + DNS inspection recorded in the resolution doc"
      pattern: "Path A"
---

<objective>
Resolve the ONE question Phase 251 could not settle from the repo: which of the FOUR install
entrypoints does `get.livinity.io` actually serve? (Path A `scripts/install.sh`→deploy-livinityd.sh
seeds liv:mcp:config; Path B `/install.sh` writes CHANGEME; Path C `livos/install.sh` no MCP seed;
Path D the route.ts HTTP shim.) The mapping is a live DNS/Vercel-alias fact, NOT provable in-repo
or by Mini PC SSH.

Purpose: feed R9 part (1) — once we know the live entrypoint, R9 can pin/document it. This is the
ONLY `autonomous: false` item in Phase 252 (needs live Cloudflare DNS + Vercel access).
Output: `.planning/.../GET-LIVINITY-IO-RESOLUTION.md` recording the resolved entrypoint + evidence.

NOTE (CONTEXT D-Wave3): if this cannot be answered in-session, R9 parts (2)+(3) are STILL
shippable independently — do NOT block the phase on this DNS question. Plan 252-04 (R9) does not
hard-depend on this plan's output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-fresh-install-portability-remediation/252-CONTEXT.md
@.planning/phases/251-fresh-install-portability-audit/findings/251-08-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect the live get.livinity.io mapping (best-effort automated, then checkpoint)</name>
  <files>.planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md</files>
  <read_first>
    - .planning/phases/251-fresh-install-portability-audit/findings/251-08-FINDINGS.md (the FOUR-entrypoint analysis + why get.livinity.io is unprovable from the repo: no Vercel route / next.config rewrite / vercel.json maps it)
    - platform/web/src/app/install.sh/route.ts (Path D — the HTTP shim that proxies GitHub-raw scripts/install.sh, fallback clones livos/install.sh)
    - .planning/phases/252-fresh-install-portability-remediation/252-CONTEXT.md (D-Wave3 — R11 gates R9 part 1 only; the rest of R9 ships regardless)
  </read_first>
  <action>
Resolve the live mapping with whatever access is available, then record findings.

1. Attempt the automated probes (all non-destructive, read-only):
   - `curl -sSL -D - https://get.livinity.io -o /tmp/getliv-body.txt` then inspect the headers
     (`server:`, `x-vercel-*`, `cf-ray`, any `location:` redirect) and the served body. Compare the
     body's first ~40 lines against: `scripts/install.sh` (Path A), repo-root `/install.sh` (Path B),
     `livos/install.sh` (Path C), and the route.ts shim's GitHub-raw proxy / clone-fallback (Path D).
   - `dig +short get.livinity.io` and `dig +short CNAME get.livinity.io` to see whether it resolves
     to a Vercel alias (`*.vercel-dns.com` / `cname.vercel-dns.com`) or elsewhere.
   - If the body matches the route.ts shim output, the effective script is whatever route.ts serves
     (after Plan 04 R9(2), that is Path A) — record that dependency explicitly.

2. Write `.planning/.../GET-LIVINITY-IO-RESOLUTION.md` with:
   - The resolved entrypoint (Path A/B/C/D) and the evidence (curl headers + body signature + dig output).
   - Whether it seeds `liv:mcp:config` (Path A yes; B/C no; D depends on route.ts after R9(2)).
   - A one-line verdict: "get.livinity.io → <path> → seeds MCP: yes/no".
   - If the live access is unavailable in-session, record "UNRESOLVED — requires operator Cloudflare/Vercel
     dashboard access" + the exact dashboard steps (Cloudflare DNS for livinity.io → the `get` record;
     Vercel project domains → the `get.livinity.io` alias target), and state that R9 parts (2)+(3)
     still ship independently.

3. If credentials/network are required and not present, raise the checkpoint below.
  </action>
  <acceptance_criteria>
    - `.planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md` exists and is >= 20 lines
    - The doc contains the literal verdict line `get.livinity.io →` with a Path A/B/C/D designation OR an explicit `UNRESOLVED — requires operator ... access` line
    - The doc records the curl/dig evidence actually obtained (or states why it could not be obtained)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md && [ "$(wc -l < .planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md)" -ge 20 ]</automated>
  </verify>
  <done>The resolution doc exists with the live evidence + a Path A/B/C/D verdict, or an explicit UNRESOLVED note with the operator dashboard steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms the get.livinity.io verdict (or supplies the DNS/Vercel alias)</name>
  <files>.planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md</files>
  <action>
CHECKPOINT — this task pauses for the operator. Claude has already attempted the automated
curl + dig resolution in Task 1 and written `GET-LIVINITY-IO-RESOLUTION.md`. Present the doc's
verdict to the operator and request confirmation or the missing alias target.

what-built: An automated attempt to resolve `get.livinity.io`'s live install-script mapping via
curl + dig, written to `GET-LIVINITY-IO-RESOLUTION.md`.

how-to-verify:
  1. Open `.planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md`.
  2. If it shows a confident Path A verdict with matching evidence → confirm and we proceed.
  3. If it shows UNRESOLVED, check the Cloudflare DNS dashboard for `livinity.io` (the `get` record)
     and the Vercel project domains for the `get.livinity.io` alias target, then paste the resolved
     target here so it can be recorded into the doc.

resume-signal: Type "approved" (verdict confirmed) or paste the Cloudflare/Vercel alias target to record.
  </action>
  <verify>
    <automated>grep -qE 'get\.livinity\.io →|UNRESOLVED' .planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md</automated>
  </verify>
  <done>Operator has confirmed the Path verdict OR supplied the alias target, which is recorded in GET-LIVINITY-IO-RESOLUTION.md.</done>
  <what-built>An automated attempt to resolve `get.livinity.io`'s live install-script mapping via curl + dig, written to `GET-LIVINITY-IO-RESOLUTION.md`.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/252-fresh-install-portability-remediation/GET-LIVINITY-IO-RESOLUTION.md`.
    2. If it shows a confident Path A verdict with matching evidence → confirm and we proceed.
    3. If it shows UNRESOLVED, check the Cloudflare DNS dashboard for `livinity.io` (the `get` record)
       and the Vercel project domains for the `get.livinity.io` alias target, then paste the resolved
       target here so it can be recorded.
  </how-to-verify>
  <resume-signal>Type "approved" (verdict confirmed) or paste the Cloudflare/Vercel alias target to record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → get.livinity.io | a curl-able bootstrap URL whose served script runs with root via `curl | bash` on the operator's box |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-08 | Spoofing/Tampering | get.livinity.io serves an unexpected/unpinned script | mitigate | this plan AUDITS + records the live mapping so the canonical body is known and auditable; R9 (Plan 04) then pins route.ts to Path A so the served body is deterministic |
| T-252-09 | Information Disclosure | recording DNS/Vercel internals in a planning doc | accept | `.planning/` is gitignored (force-tracked); the doc records public DNS + alias target, no secrets |
</threat_model>

<verification>
- `GET-LIVINITY-IO-RESOLUTION.md` exists with evidence + verdict (or UNRESOLVED + operator steps).
- `.planning/` doc committed with `git add -f`.
- No source files touched (this is an ops/docs plan) → sacred SHA hook trivially PASS.
</verification>

<success_criteria>
- The live get.livinity.io → install-script mapping is recorded with evidence, OR explicitly marked UNRESOLVED with the exact operator dashboard steps (so R9 part 1 can be completed later without re-discovery).
- The phase is NOT blocked: Plan 04 (R9 parts 2+3) ships regardless of this outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/252-fresh-install-portability-remediation/252-03-SUMMARY.md`
</output>
