---
phase: 43-marketplace-integration-anchor-mirofish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Plans 43-02..05 know exactly which line in compose-generator.ts and apps.ts to patch — not 'find the right place'"
    - "MiroFish image publish state on GHCR + Docker Hub is documented (PASS / FAIL / fallback path)"
    - "Marketplace manifest store path is pinned (sibling repo `utopusc/livinity-apps` vs local `data/app-stores/`)"
    - "Marketplace UI badge insertion file is pinned (line-precise)"
    - "Sacred file SHA `623a65b9a50a89887d36f770dcd015b691793a7f` is re-verified before any planning work proceeds"
    - "Broker module is feature-frozen — audit only reads, never proposes broker changes"
  artifacts:
    - path: ".planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md"
      provides: "Concrete extension points (file paths + line numbers + reuse decisions) for Plans 43-02..05"
      min_lines: 200
  key_links:
    - from: ".planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/apps/compose-generator.ts"
      via: "documents injectAiProviderConfig() insertion point with verbatim line context"
      pattern: "generateAppTemplate|composeDoc"
    - from: ".planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/apps/apps.ts"
      via: "documents installForUser() patch site near compose YAML write (apps.ts:967)"
      pattern: "installForUser|composeData"
    - from: ".planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md"
      to: "livos/packages/livinityd/source/modules/apps/schema.ts"
      via: "documents AppManifestSchema field-add line for `requiresAiProvider?: boolean`"
      pattern: "AppManifestSchema"
---

<objective>
Phase 43 needs to extend three livinityd files (`schema.ts`, `compose-generator.ts`, `apps.ts`), author a marketplace manifest in an external store, and add one UI pill. Before any of that, this plan produces a read-only `43-AUDIT.md` pinning the EXACT insertion points + the MiroFish image publish state so Plans 43-02..05 don't have to re-discover the codebase or re-fetch external registries.

Purpose: CONTEXT.md leaves four discovery items open: (a) precise compose-generator.ts patch line — generator builds compose for built-in apps but `installForUser()` ALSO patches compose at apps.ts:903-967 for community apps, so injection might need to live in `installForUser()` rather than compose-generator (audit decides which is correct); (b) where Livinity loads marketplace manifests from — sibling repo `https://github.com/utopusc/livinity-apps.git` (already pinned in `livos/packages/livinityd/source/constants.ts:2`) vs a local store, with cloned path `<dataDir>/app-stores/...`; (c) MiroFish image publish state on GHCR (`ghcr.io/666ghj/mirofish:latest`) and Docker Hub (`666ghj/mirofish:latest`) — if neither published, the audit documents the fork+build fallback; (d) marketplace UI card location for the "Uses your Claude subscription" pill — likely `livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx` based on Glob results.

Output: `.planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md` with 8 sections (sacred-file baseline; integration point decision: compose-generator vs installForUser; manifest store path; MiroFish image publish state + fallback; UI badge insertion point; test infrastructure pattern; risks; deferred-to-plan items). One atomic commit (audit doc only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-marketplace-integration-anchor-mirofish/43-CONTEXT.md
@.planning/phases/41-anthropic-messages-broker/41-CONTEXT.md
@.planning/phases/42-openai-compatible-broker/42-SUMMARY.md
@livos/packages/livinityd/source/modules/apps/schema.ts
@livos/packages/livinityd/source/modules/apps/apps.ts
@livos/packages/livinityd/source/modules/apps/compose-generator.ts
@livos/packages/livinityd/source/modules/apps/builtin-apps.ts
@livos/packages/livinityd/source/modules/apps/app-repository.ts
@livos/packages/livinityd/source/modules/apps/app-store.ts
@livos/packages/livinityd/source/constants.ts

<interfaces>
<!-- Established Phase 43 contracts (from CONTEXT.md + Phase 41/42 SUMMARYs). -->

Sacred file (NOT modified, EVER):
  Path: nexus/packages/core/src/sdk-agent-runner.ts
  Baseline SHA: 623a65b9a50a89887d36f770dcd015b691793a7f
  (Verified by `git hash-object` matching above; `git diff HEAD --` produces empty output.)

Broker module (FEATURE-FROZEN, NOT modified in Phase 43):
  Path: livos/packages/livinityd/source/modules/livinity-broker/
  Contract Phase 43 consumes: `http://livinity-broker:8080/u/<user_id>/v1` (Anthropic + OpenAI endpoints both reachable under this prefix per Phase 42 D-42-03).

From livos/packages/livinityd/source/modules/apps/schema.ts (line 26-63):
```typescript
export const AppManifestSchema = z.object({
  manifestVersion: validateSemanticVersion,
  id: z.string(),
  // ... existing optional fields ...
  optimizedForLivinityHome: z.boolean().optional(),
  // <- new field `requiresAiProvider?: boolean` adds here per D-43-01
})
```

From livos/packages/livinityd/source/modules/apps/apps.ts (installForUser, line 852-991):
```typescript
async installForUser(appId: string, userId: string): Promise<boolean> {
  // ... template resolution ...
  // line 906-910: read + replace env vars in compose
  // line 910: const composeData = (await import('js-yaml')).default.load(compose) as any
  // line 933-958: per-service patches (container_name, volumes)
  // line 961-964: set host port mapping
  // line 967: yamlDump = (await import('js-yaml')).default.dump(composeData)
  // line 968: await fse.writeFile(`${userDataDir}/docker-compose.yml`, yamlDump)
  // <- injection MUST happen between line 958 (per-service patches done)
  //    and line 967 (yamlDump). Reads manifest's `requiresAiProvider` flag.
}
```

From livos/packages/livinityd/source/constants.ts:
```typescript
export const LIVINITY_APP_STORE_REPO = 'https://github.com/utopusc/livinity-apps.git'
```
(Confirmed external sibling repo; manifest authoring will require a separate commit/PR there.)

Existing test pattern (Phase 41/42 broker):
  - Bare `tsx <file>.test.ts` — no Vitest
  - `node:assert/strict` for assertions
  - npm script chain: `test:phase42 -> test:phase41 -> test:phase40 -> test:phase39`

Existing apps.ts test pattern (different):
  - vitest (`apps.integration.test.ts:3` imports `expect, beforeAll, afterAll, test, vi` from 'vitest')
  - The audit must document BOTH patterns so Plan 43-04 picks the right one for the new tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sacred file + broker freeze + write 43-AUDIT.md</name>
  <files>.planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md</files>
  <action>
Step A — Sacred file + broker integrity check (mandatory before audit body):

Run: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts`
Expected output: `623a65b9a50a89887d36f770dcd015b691793a7f` (Phase 40 baseline; Phase 41 + 42 preserved). If different — STOP and report drift; Phase 43 cannot proceed.

Run: `git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l`
Expected: `0`. If non-zero — STOP and report; uncommitted sacred-file edits are a hard blocker.

Run: `git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l`
Expected: `0`. If non-zero — STOP and report; broker is feature-frozen for Phase 43.

Record all three results verbatim in Section 1 of the audit.

Step B — MiroFish image publish discovery (network probe):

Run each of these and capture the result (success / 404 / network error):
- `docker manifest inspect ghcr.io/666ghj/mirofish:latest 2>&1 | head -20`
- `docker manifest inspect 666ghj/mirofish:latest 2>&1 | head -20`
- `curl -sI https://ghcr.io/v2/666ghj/mirofish/manifests/latest 2>&1 | head -5`
- `curl -s https://hub.docker.com/v2/repositories/666ghj/mirofish/ 2>&1 | head -20`

If `docker` CLI is not available locally, fall back to the two `curl` commands. Record the raw outputs in Section 4 of the audit. Decide:
- **Path A — Image is published** (manifest fetch succeeded on at least one registry): document the chosen image reference verbatim. Plan 43-03 uses it directly.
- **Path B — Image NOT published** (both manifest fetches 404): document the fork+build fallback. The fallback is: (1) Plan 43-03 forks `666ghj/MiroFish` to `utopusc/MiroFish`; (2) GitHub Actions or Mini PC operator builds the image; (3) pushes to `ghcr.io/utopusc/mirofish:v29.3` (or local registry); (4) Plan 43-03 manifest references THAT image. Record the exact commands the operator would run, but DO NOT execute them in this audit (Plan 43-03 picks them up).

Step C — Write `.planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md` with these 8 sections:

1. **Sacred File + Broker Freeze Re-Verification** — record SHA from Step A, both `git diff` outputs (each `0`), and explicit statement: "Phase 43 baseline = `623a65b9...`; broker module byte-frozen at Phase 42 final commit. Phase 43 makes ZERO modifications to either."

2. **Integration Point Decision: compose-generator.ts vs installForUser()** — read both files. Document:
   - `compose-generator.ts:13-127` (`generateAppTemplate(appId)`) ONLY runs for built-in apps (`getBuiltinApp(appId)` early-returns null otherwise). MiroFish is a marketplace app, NOT a built-in — `generateAppTemplate` returns null for it.
   - `apps.ts:852-991` (`installForUser`) is the actual unified per-user install path. It (a) calls `generateAppTemplate` first (built-in), (b) falls back to platform template, (c) falls back to app-store repo template. ALL THREE paths converge at line 906-967 where the compose is loaded as YAML, patched per-service, and re-dumped.
   - **Decision:** `injectAiProviderConfig(composeData, userId)` is a PURE FUNCTION exported from a NEW module `livos/packages/livinityd/source/modules/apps/inject-ai-provider.ts`. It accepts the parsed `composeData` object (already loaded by js-yaml at apps.ts:910) and the resolved `userId` and mutates the object in place (or returns a new one — Plan 43-02 picks). It is called in `installForUser` between line 958 (per-service patches done) and line 961 (host port mapping) — i.e., AFTER all standard transforms, BEFORE the final dump. The flag check (`manifest.requiresAiProvider === true`) lives INSIDE the new function so the call site is unconditional and the function is a no-op when the flag is absent. This satisfies D-43-06 (pure function) and D-43-07 (single integration point in installForUser).
   - **NOT in compose-generator.ts**: `generateAppTemplate` only constructs the FIRST DRAFT for built-in apps; Plan 43-03's MiroFish manifest comes from the marketplace store path (Step C-3 below), not from `getBuiltinApp`. Putting injection there would miss the marketplace path entirely.
   - Record the exact verbatim 5 lines around apps.ts:958 and apps.ts:967 from `git show HEAD:livos/packages/livinityd/source/modules/apps/apps.ts | sed -n '955,970p'`.

3. **Manifest Store Path Decision** — read `livos/packages/livinityd/source/constants.ts` and `livos/packages/livinityd/source/modules/apps/app-repository.ts`:
   - Default repo: `https://github.com/utopusc/livinity-apps.git` (constants.ts:2)
   - Local clone path: `${livinityd.dataDirectory}/app-stores/<cleanUrl>/` (app-repository.ts:42 — atomically cloned by livinityd at start)
   - Each app lives in `<repoRoot>/<app_id>/` with files: `livinity-app.yml` (manifest), `docker-compose.yml`, optional `gallery/`, optional `README.md` (mirror Sparkles app pattern from existing repo).
   - **Decision:** MiroFish manifest authoring happens in the SIBLING repo `utopusc/livinity-apps`, NOT in the LivOS source tree. Plan 43-03 produces the manifest content in a DRAFT file inside `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/` so it is reviewable in this repo's PR; the operator separately commits + pushes to the `livinity-apps` sibling repo. This avoids modifying LivOS source for marketplace content and respects the existing two-repo architecture. Document this decision explicitly with reasoning.
   - Note for Plan 43-05 UAT: the live test on Mini PC requires the sibling-repo PR to be merged first so the Mini PC's app-store auto-pull (`updateInterval: '5m'`) picks up the new manifest. Document this prerequisite.

4. **MiroFish Image Publish State + Fallback Path** — record raw outputs from Step B (4 commands). Pick Path A or Path B and document the chosen image reference. If Path B (fork+build), enumerate the operator commands (verbatim) for fork, clone, `docker build`, `docker push` to GHCR or local Mini PC registry; do not execute. Record the chosen image string that Plan 43-03 will paste into the manifest's compose stanza.

5. **UI Badge Insertion Point** — read `livos/packages/ui/src/modules/window/app-contents/app-store-routes/app-page-window.tsx` (lines 1-72 visible) AND identify the card/header surface that displays per-app metadata. Likely candidates (verify):
   - `livos/packages/ui/src/modules/app-store/app-page/app-content.tsx` (the AppContent imported at app-page-window.tsx:10)
   - `livos/packages/ui/src/components/install-button-connected.tsx` (next-to-install surface)
   - The `<TopHeaderWindow>` component inline in app-page-window.tsx (line 75)
   Pick ONE — pin the file + line + import statement for `<Badge>` (already imported at app-page-window.tsx:16 from `@/shadcn-components/ui/badge`). Document the conditional render snippet: `{app.requiresAiProvider && <Badge variant="secondary">Uses your Claude subscription</Badge>}`. Document where `requiresAiProvider` enters the `RegistryApp` type — it must propagate from `livos/packages/livinityd/source/modules/apps/schema.ts` through whatever serialization layer feeds `useAvailableApp(appId)`. Plan 43-04 owns the wiring; this audit just pins the FILE + LINE for the Badge insertion.

6. **Test Infrastructure Pattern** — document BOTH patterns and pick one per test type:
   - **Pure unit tests** (schema field, `injectAiProviderConfig` function): use bare `tsx + node:assert/strict` to match Phase 41/42 broker tests. Run as `npx tsx <file>.test.ts`. Faster, no Vitest setup needed. NEW test files in `livos/packages/livinityd/source/modules/apps/`.
   - **Integration tests** (mocked installForUser end-to-end): use `vitest` to match the existing `apps.integration.test.ts:3` pattern. Run via `pnpm --filter @livinity/livinityd test`. The integration test file MUST mock `findUserById`, `allocatePort`, `createUserAppInstance`, the `$` shell calls (rsync, docker compose), and `fse.writeFile` to capture the dumped YAML — then assert the YAML contains the 3 env vars + extra_hosts when manifest flag is true.
   - **npm script:** add `test:phase43` to `nexus/packages/core/package.json` chaining `npm run test:phase42`. (Phase 43 has no nexus changes, but the chain preserves regression coverage on Phase 39+40+41+42.)

7. **Risks & Open Questions** — pin these for Plans 43-02..05:
   - Risk R1: `installForUser` reads `manifest` at apps.ts:881-886 via `readManifestInDirectory(appTemplatePath)`. Plan 43-02 must verify the manifest's `requiresAiProvider` flag survives that read path (the function returns `AppManifest` per `app.ts`'s implementation — confirm `validateManifest` does NOT strip unknown fields).
   - Risk R2: Phase 43 must NOT inject env vars when manifest's flag is absent. The integration test for the negative case is mandatory (D-43-13 case (a)).
   - Risk R3: `composeData` from js-yaml may have `services` keyed by name. The injection must handle the case where the service already has `environment:` (merge, do not overwrite) AND where it already has `extra_hosts:` (append, do not overwrite). Cover both in unit tests (D-43-13 cases (c) + (d)).
   - Risk R4: Plan 43-04's UI test cannot run end-to-end without spinning up the full UI dev server. Acceptance: a unit-test snippet that imports the badge component and asserts conditional render, plus the manual UAT in 43-UAT.md serves as the live verification gate.
   - Risk R5: The `<user_id>` in the env var URLs is the LivOS user UUID (matches `findUserById(userId).id`). Plan 43-02 must use `userId` (not `user.username`) so the URL matches what the broker's `/u/:userId/v1/messages` expects.
   - Risk R6: Existing apps without the flag MUST produce byte-identical compose output. Add a regression test that: (1) installs an app with `requiresAiProvider: false` (or omitted), (2) snapshots the dumped YAML, (3) compares against the pre-Phase-43 expected output. Plan 43-04 owns this.

8. **Deferred to Plans 43-02..05** — explicit checklist:
   - Plan 43-02: schema field + `injectAiProviderConfig` function + installForUser wiring + unit tests for the pure function
   - Plan 43-03: MiroFish manifest draft (in `draft-mirofish-manifest/` dir of this phase folder; sibling-repo PR is operator action, NOT executor)
   - Plan 43-04: schema test + `installForUser` integration test + UI badge wiring + `test:phase43` npm script
   - Plan 43-05: 43-UAT.md operator-facing manual verification on Mini PC

End of audit. Total: ~250-350 lines.

Step D — Commit:
Run: `git add .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md`
Run: `git commit -m "docs(43-01): codebase audit + MiroFish image discovery (Phase 43 prep)"`

Step E — Final regression checks (must pass before exit):
Run: `git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l` → must be `0`
Run: `git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l` → must be `0`
  </action>
  <verify>
    <automated>test -f .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md && wc -l .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md | awk '{exit ($1 < 200)}' && grep -q "623a65b9a50a89887d36f770dcd015b691793a7f" .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md && grep -q "injectAiProviderConfig" .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md && grep -q "livinity-apps" .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md && grep -q "MiroFish" .planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md && [ "$(git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l)" = "0" ] && [ "$(git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l)" = "0" ]</automated>
  </verify>
  <done>43-AUDIT.md created with 8 sections, ≥ 200 lines, sacred-file SHA + broker freeze verified, MiroFish image publish state documented (Path A or Path B), integration point decision pinned (`injectAiProviderConfig` in NEW file, called from `installForUser` between apps.ts:958-961), manifest store path pinned (sibling repo `utopusc/livinity-apps`), UI badge file pinned, and committed atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | Read-only audit; produces a markdown doc only. No new auth surface, no new code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Information disclosure | 43-AUDIT.md | accept | Audit document is project-internal `.planning/`; no secrets included; image registry probes are public-API only. |
| T-43-01-02 | Tampering | sacred file `sdk-agent-runner.ts` | mitigate | Plan re-verifies SHA at start AND end (Step E); aborts before any commit if drift detected. |
| T-43-01-03 | Tampering | broker module `livinity-broker/` | mitigate | Plan re-verifies `git diff` produces zero output at start AND end; aborts if drift detected. |
</threat_model>

<verification>
- 43-AUDIT.md exists in `.planning/phases/43-marketplace-integration-anchor-mirofish/`
- Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA still `623a65b9a50a89887d36f770dcd015b691793a7f`; `git diff HEAD --` produces zero output
- Broker module `livos/packages/livinityd/source/modules/livinity-broker/` `git diff HEAD --` produces zero output
- Audit pins all 8 sections with concrete file paths + line numbers
- Audit explicitly decides: NEW `inject-ai-provider.ts` module (not embedded in compose-generator.ts)
- Audit explicitly decides: sibling repo `utopusc/livinity-apps` for MiroFish manifest (with draft in this phase folder)
- Audit explicitly decides MiroFish image: Path A (published) OR Path B (fork+build) — one chosen, the other documented as ruled out
- One atomic commit (audit doc only)
</verification>

<success_criteria>
- Plans 43-02..05 can begin without re-reading apps.ts, compose-generator.ts, schema.ts, or any UI file
- Every "should we put this here or there?" question is answered with a rationale
- Sacred file + broker freeze re-verified at Phase 43 start (not assumed from Phase 42 SUMMARY)
- MiroFish image fallback path is fully documented so Plan 43-03 doesn't have to make decisions under time pressure
</success_criteria>

<output>
After completion, create `.planning/phases/43-marketplace-integration-anchor-mirofish/43-01-SUMMARY.md` with: commit SHA, audit section count, line count, sacred-file SHA verification result, broker freeze verification result, MiroFish image path decision (A or B), and the 6 risk items (R1-R6) for downstream plans.
</output>
