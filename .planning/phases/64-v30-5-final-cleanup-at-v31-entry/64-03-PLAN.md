---
phase: 64-v30-5-final-cleanup-at-v31-entry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md
autonomous: true
requirements: [CARRY-03]
tags: [r-series, classification, phase-63, broker, walkthrough]

must_haves:
  truths:
    - "64-R-SERIES-MATRIX.md exists in the phase dir as a single self-contained markdown file"
    - "The matrix lists ALL 11 R-series plans: R3.1 through R3.11 (one row per plan, 11 rows beyond header)"
    - "Each row's Status column is exactly one of: `script-verified`, `needs-human-walk`, `failed`, `obsolete` — same vocabulary as 64-UAT-MATRIX"
    - "R-series plans that were code-only changes are eligible for `script-verified` via test:phaseN suite or commit-existence verification per D-07"
    - "R3.11 (`/v1/messages` translation per D-08) is verified via curl-based broker test against Mini PC, with response body cited in evidence"
    - "Broker code path is read-only — no edits to livinityd source — `subscription-only` constraint honored"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` is not touched (no Read needed for this plan; documentation-only)"
  artifacts:
    - path: ".planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md"
      provides: "11-row R-series classification matrix with evidence column"
      contains: "| R3.1 |"
      min_lines: 50
  key_links:
    - from: "64-R-SERIES-MATRIX.md"
      to: "the 11 R-series plan files in .planning/milestones/v30.0-phases/63-mandatory-live-verification/"
      via: "Source file column referencing 63-NN-PLAN.md path per row"
      pattern: "63-mandatory-live-verification/63-[0-9]{2}-PLAN"
    - from: "Row R3.11"
      to: "broker `/v1/messages` endpoint live curl on Mini PC"
      via: "curl command + observed response body in Evidence cell"
      pattern: "curl.*api\\.livinity\\.io/v1/messages|kortix-api|message_start"
---

<objective>
Produce `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md` — a classification matrix for the 11 Phase 63 R-series plans (R3.1 .. R3.11), using the same vocabulary as the UAT matrix. R-series plans were the v30.0 broker professionalization work; most are code-only and verifiable by test/commit existence. R3.11 specifically requires a live broker tool-routing curl against Mini PC per D-08.

Purpose: Satisfies Phase 64 success criterion #3 and CARRY-03. Closes the v30.0 R-series formal walkthrough that was deferred.

Output:
- `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md` (new file, ~70-120 lines: preamble + 11-row table + per-row evidence subsections)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md

# 11 R-series source plans (Phase 63 = v30.0 broker professionalization)
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-01-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-02-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-03-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-04-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-05-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-06-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-07-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-08-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-09-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-10-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-11-PLAN.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-UAT-RESULTS.md
@.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-01-SUMMARY.md

# Broker protocol verification memory (live curl proofs from 2026-05-03)
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_broker_protocols_verified.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_subscription_only.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md
</context>

<sacred_boundary>
**STRICT RULE for this plan:** Documentation-only. Do NOT modify any code. Do NOT touch `nexus/packages/core/src/sdk-agent-runner.ts` (sacred). Do NOT touch any livinity-broker source. R-series plans are read-only references. The only writable file is the matrix output.

**Subscription-only:** Per `feedback_subscription_only.md` D-NO-BYOK, any verification curl that requires broker auth uses the existing subscription path (a key from `/opt/livos/.env` `LIV_API_KEY` on Mini PC if needed). No raw `@anthropic-ai/sdk` calls, no fallback BYOK path.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Read 11 R-series plan files and classify each</name>
  <files>.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md (D-07, D-08)
    - All 11 plan files: .planning/milestones/v30.0-phases/63-mandatory-live-verification/63-{01..11}-PLAN.md
    - .planning/milestones/v30.0-phases/63-mandatory-live-verification/63-UAT-RESULTS.md (existing UAT results — primary evidence source)
    - .planning/milestones/v30.0-phases/63-mandatory-live-verification/63-01-SUMMARY.md (sample summary format)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_broker_protocols_verified.md (R3.11 evidence — 2026-05-03 curl proofs of /v1/messages tool translation)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md (batch SSH rule)
  </read_first>
  <action>
1. Read 64-CONTEXT.md, the 11 plan files, the existing 63-UAT-RESULTS.md, and the broker memory. Note: per D-07 most R-series plans were code-only changes — those are auto-pass via:
   - **test:phaseN suite** if the plan defined one (look for `npm run test:phase63-NN` references in the plan)
   - **commit-existence verification** otherwise: confirm the plan's documented commits land on `master` via `git log --oneline --grep="<phase-tag>" master` from the working directory
   
   Per D-08, R3.11 requires a live broker tool-routing test against Mini PC: a curl POST to `https://api.livinity.io/v1/messages` (or `/v1/chat/completions`, whichever R3.11 covered) that includes a `tools[]` array, observing the response body for a correctly translated tool-routing response.

2. For each of the 11 R-series plans, extract:
   - **R-series ID** (R3.1 through R3.11 — these match plan numbers 63-01 through 63-11)
   - **Plan objective** (one-line summary from the plan's `<objective>` block)
   - **Verification approach declared by the plan** (look for `<verification>` block, `test:phase` calls, or commit refs)

3. Classify each row:
   - If 63-UAT-RESULTS.md ALREADY contains a verified result for this R-series item, copy that evidence into the matrix and mark `script-verified` (cite 63-UAT-RESULTS.md).
   - If the plan was code-only and the verification command runs cleanly from the working directory (e.g., `git log --oneline --grep="R3.X" master` returns the expected commits, or `pnpm --filter <pkg> test` passes), run it ONCE and cite the output. Mark `script-verified`.
   - If the plan requires UI interaction (any browser-based verification), mark `needs-human-walk` per the same D-05 rule from the UAT matrix; document the manual steps.
   - For R3.11 specifically: per D-08, run a curl test. Use the broker key from Mini PC `/opt/livos/.env`. Single SSH session per `feedback_ssh_rate_limit.md`:
     ```
     ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'sudo cat /opt/livos/.env | grep ^LIV_API_KEY'
     ```
     Then from the working directory:
     ```
     curl -sS -X POST https://api.livinity.io/v1/messages \
       -H "Content-Type: application/json" \
       -H "x-api-key: <LIV_API_KEY>" \
       -H "anthropic-version: 2023-06-01" \
       -d '{"model":"claude-sonnet-4-5","max_tokens":50,"tools":[{"name":"echo","description":"echo back","input_schema":{"type":"object","properties":{"text":{"type":"string"}},"required":["text"]}}],"messages":[{"role":"user","content":"Use the echo tool to say hi"}]}'
     ```
     Capture the first 500 chars of the response body. Look for a `tool_use` content block (correct translation = `script-verified`). If broker returns plain text instead of `tool_use`, mark `failed` and cite the actual body.
     
     If broker key is unobtainable (SSH fails, env var absent), fall back to citing `reference_broker_protocols_verified.md` (2026-05-03 curl proofs) and mark `script-verified` with date-noted evidence (NOT a fresh live test).

4. Per D-NO-SERVER4: any R-series step that mentions Server4 → mark `obsolete`. Server5 / Mini PC are fine.

5. Build the Evidence cell content per status:
   - `script-verified` → command + first 200-500 chars of stdout/stderr OR commit hash + `git log` line
   - `needs-human-walk` → numbered manual steps + expected outcome
   - `failed` → response body / error trace + commit ref
   - `obsolete` → one-sentence rationale

6. Write `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md` with this structure:

   ```markdown
   # 64-R-SERIES-MATRIX — Phase 63 R3.1–R3.11 classification
   
   **Phase:** 64-v30-5-final-cleanup-at-v31-entry
   **Generated:** <today's date>
   **Source:** Phase 63 R-series plans (`.planning/milestones/v30.0-phases/63-mandatory-live-verification/`)
   **Discipline rule:** Same as 64-UAT-MATRIX.md — no silent elevation, browser-required = `needs-human-walk`.
   **Subscription-only:** All broker verifications use the subscription path (LIV_API_KEY); no raw API key BYOK fallback (D-NO-BYOK).
   
   ## Vocabulary (aligned with VERIFICATION.md `status:` convention)
   
   | Status            | Means                                                                                       |
   |-------------------|---------------------------------------------------------------------------------------------|
   | `script-verified` | Backend evidence observed (curl/test:phaseN/git log output cited in Evidence cell)          |
   | `needs-human-walk`| Browser-required step (rare for R-series, but possible if a UI flow was part of the plan)   |
   | `failed`          | Observed regression that was never closed                                                   |
   | `obsolete`        | Superseded, scope-removed, or D-NO-SERVER4 forbids                                          |
   
   ## Matrix
   
   | R-series ID | Plan | Objective | Source file | Status | Evidence summary |
   |---|---|---|---|---|---|
   | R3.1 | 63-01 | <obj> | .planning/milestones/v30.0-phases/63-mandatory-live-verification/63-01-PLAN.md | <status> | <one-line> |
   | R3.2 | 63-02 | <obj> | ... | ... | ... |
   ... (11 rows)
   
   ## Per-row evidence
   
   ### Row R3.1 — 63-01 (<objective>)
   
   **Status:** <chosen>
   **Source:** `.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-01-PLAN.md`
   **Evidence:**
   <command + observed output  OR  manual steps  OR  failure trace  OR  obsolescence rationale>
   
   ### Row R3.2 — 63-02 (...)
   ... (one ### block per row, 11 total)
   
   ### Row R3.11 — 63-11 (broker /v1/messages tool routing — D-08 live curl)
   
   **Status:** script-verified
   **Source:** `.planning/milestones/v30.0-phases/63-mandatory-live-verification/63-11-PLAN.md`
   **Evidence:**
   ```
   curl -sS -X POST https://api.livinity.io/v1/messages \
     -H "x-api-key: ***" -H "anthropic-version: 2023-06-01" \
     -d '{"model":"...","tools":[{"name":"echo",...}],"messages":[{"role":"user","content":"Use the echo tool"}]}'
   
   Response (first 500 chars):
   <captured response body — must contain "tool_use" content block>
   ```
   
   Cross-reference: matches `reference_broker_protocols_verified.md` 2026-05-03 finding.
   
   ## Summary counts
   
   - `script-verified`: N
   - `needs-human-walk`: N
   - `failed`: N
   - `obsolete`: N
   - **Total:** 11
   ```

7. Confirm summary counts total exactly 11.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md';if(!fs.existsSync(p)){console.error('matrix file missing');process.exit(1)}const s=fs.readFileSync(p,'utf8');const evidenceBlocks=(s.match(/^### Row R3\.\d+/gm)||[]).length;const r3IdsCited=new Set();for(let i=1;i<=11;i++){if(s.includes('R3.'+i)){r3IdsCited.add(i)}}const statuses=(s.match(/script-verified|needs-human-walk|failed|obsolete/g)||[]);const hasR311Curl=/R3\.11[\s\S]{0,2000}curl[\s\S]{0,500}\/v1\/messages/.test(s)||/R3\.11[\s\S]{0,2000}reference_broker_protocols_verified/.test(s);const hasTotal11=/Total.*11/.test(s);if(evidenceBlocks!==11){console.error('evidence blocks count != 11, got '+evidenceBlocks);process.exit(1)}if(r3IdsCited.size!==11){console.error('not all R3.1-R3.11 cited, found '+r3IdsCited.size);process.exit(1)}if(statuses.length<11){console.error('not enough status tags, got '+statuses.length);process.exit(1)}if(!hasR311Curl){console.error('R3.11 evidence missing /v1/messages curl OR memory cross-ref');process.exit(1)}if(!hasTotal11){console.error('summary count missing or != 11');process.exit(1)}console.log('R-series matrix OK: '+evidenceBlocks+' evidence blocks, '+r3IdsCited.size+' R3.x IDs, R3.11 has live or memory evidence')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-R-SERIES-MATRIX.md` exists.
    - File contains exactly 11 `### Row R3.N` evidence subsections (R3.1 through R3.11).
    - Matrix table contains 11 data rows.
    - Each row's Status is exactly one of: `script-verified`, `needs-human-walk`, `failed`, `obsolete`.
    - For every row classified `script-verified`, the Evidence section contains a verifiable artifact: a command + output, OR a commit hash + grep-able log line, OR a citation of 63-UAT-RESULTS.md.
    - **R3.11 specifically:** Evidence section includes either (a) a curl command targeting `/v1/messages` with a `tools[]` request body AND an observed response body excerpt, OR (b) a citation of `reference_broker_protocols_verified.md` 2026-05-03 with explicit date noted.
    - Summary counts present and total exactly 11.
    - No code files modified (no Edit/Write to broker source, sacred file, or any livinityd / nexus source).
    - Subscription-only constraint (D-NO-BYOK) explicitly mentioned in preamble.
    - No D-NO-SERVER4 violation.
  </acceptance_criteria>
  <done>11 R-series plans classified; R3.11 has live or recent-memory broker tool-routing evidence; matrix audit-ready and subscription-path-clean.</done>
</task>

</tasks>

<verification>
- 64-R-SERIES-MATRIX.md exists and passes automated structural check
- 11 rows total covering R3.1..R3.11
- R3.11 has live curl OR memory citation per D-08
- No code modified; sacred file untouched; D-NO-BYOK respected
- Mini PC SSH (if used) batched into single session
</verification>

<success_criteria>
- ROADMAP Phase 64 success criterion #3 satisfied: 11 R-series plans walked & documented
- CARRY-03 requirement closed
- Audit-milestone tooling can parse the matrix
</success_criteria>

<output>
After completion, create `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-03-SUMMARY.md` summarizing: total R-series plans classified, breakdown by status, the R3.11 broker curl outcome (or memory-citation), and any `failed` rows that need follow-up before v31 closes.
</output>
