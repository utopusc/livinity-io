---
phase: 64-v30-5-final-cleanup-at-v31-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs/external-client-compat.md
autonomous: true
requirements: [CARRY-04]
tags: [docs, compat-matrix, broker, external-clients]

must_haves:
  truths:
    - "external-client-compat.md exists at .planning/docs/ as a single self-contained markdown file"
    - "The doc contains exactly one markdown table with one row per client for: Bolt.diy, Cursor, Cline, Continue.dev, Open WebUI (5 rows)"
    - "Each row populates all 7 columns: Client, Auth mode, Endpoint, Streaming, Tool calls, Verified status, Known quirks, Reference"
    - "The doc names BOTH broker endpoints (`/v1/messages` Anthropic-style AND `/v1/chat/completions` OpenAI-style) and which client uses which"
    - "F2-F5 carryover (token cadence, multi-turn tool_result, Caddy timeout, identity preservation) is explicitly flagged in the doc per D-12, with phase pointer to P74"
    - "Bolt.diy plain-text bug is documented as 'Bolt did not send tools[]' (memory `reference_broker_protocols_verified.md` confirmed via curl 2026-05-03), NOT as a broker bug"
    - "D-NO-BYOK constraint is reiterated in the doc preamble: clients can only use subscription path through livinity broker, never raw API keys"
  artifacts:
    - path: ".planning/docs/external-client-compat.md"
      provides: "External client compat matrix (5 clients × 7 columns) + preamble + carryover footnotes"
      contains: "| Bolt.diy |"
      min_lines: 60
  key_links:
    - from: ".planning/docs/external-client-compat.md"
      to: "memory `reference_broker_protocols_verified.md` and `feedback_subscription_only.md`"
      via: "Reference column citing source for each row"
      pattern: "reference_broker_protocols_verified|feedback_subscription_only"
    - from: ".planning/docs/external-client-compat.md"
      to: "P74 (Phase 74 — F2-F5 broker carryover)"
      via: "Footnote / 'Open carryovers' section linking to roadmap phase"
      pattern: "P74|Phase 74"
---

<objective>
Produce `.planning/docs/external-client-compat.md` — a single hand-written compat matrix covering Bolt.diy, Cursor, Cline, Continue.dev, and Open WebUI against the Livinity Broker (livinity.io). One row per client, 7 columns. Source data is existing memory entries plus v30.0 phase summaries; no new live curl tests needed unless memory entries appear stale.

Purpose: This is the Phase 64 success-criterion #4 deliverable. The matrix is referenced by P74 (broker carryover) and any future external-client integration work. It satisfies CARRY-04.

Output:
- `.planning/docs/external-client-compat.md` (new file, ~60-100 lines: preamble + 5-row table + carryover footnotes + reference list)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md
@.planning/v31-DRAFT.md

# Source-of-truth memory entries (read these for compat data)
# Memory paths under: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/
# - reference_broker_protocols_verified.md (2026-05-03 live curl proofs of /v1/messages AND /v1/chat/completions)
# - feedback_subscription_only.md (D-NO-BYOK constraint)
# - project_v30_5_resume.md (v30.5 close state — F2-F5 deferred to P74)

# Existing-code references (read-only — for endpoint citations)
@livos/packages/livinityd/source/modules/livinity-broker/router.ts
@livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts
</context>

<sacred_boundary>
**STRICT RULE for this plan:** This is a documentation-only plan. Do NOT modify any code. The sacred file `nexus/packages/core/src/sdk-agent-runner.ts` is not even read in this plan. The broker router files in `<context>` are read-only references to extract endpoint paths and confirm the Bolt plain-text root cause; do not Edit/Write them.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Write external-client-compat.md</name>
  <files>.planning/docs/external-client-compat.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md (decisions D-09, D-10, D-11, D-12)
    - .planning/v31-DRAFT.md lines 100-117 (P64 deliverables)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_broker_protocols_verified.md (curl proofs for both endpoints, Bolt root-cause)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_subscription_only.md (D-NO-BYOK)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/project_v30_5_resume.md (F2-F5 carryover state)
    - livos/packages/livinityd/source/modules/livinity-broker/router.ts (lines 1-80 — confirm `/v1/messages` route path)
    - livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts (lines 1-80 — confirm `/v1/chat/completions` route path)
    - .planning/milestones/v30.0-phases/63-mandatory-live-verification/63-UAT-RESULTS.md (any per-client live curl evidence)
  </read_first>
  <action>
1. Read all files in `read_first`. From the broker memory (`reference_broker_protocols_verified.md`), extract:
   - The two endpoint paths verified live on 2026-05-03 (Anthropic-style `/v1/messages` and OpenAI-style `/v1/chat/completions`).
   - The tool-translation result for each protocol (both PASS — broker translates `tools[]` correctly when client sends them).
   - The Bolt.diy plain-text observation: per memory, Bolt does NOT send `tools[]` in the upstream request, so the broker has nothing to translate; the visible "plain text instead of tool calls" symptom is therefore a Bolt bug (or a Bolt config issue), not a broker bug.

2. From `feedback_subscription_only.md`, extract the D-NO-BYOK rule: external clients MUST go through livinity broker over the subscription path, never raw `@anthropic-ai/sdk` or BYOK.

3. From `project_v30_5_resume.md`, list F2-F5 carryover items: F2 token-cadence streaming, F3 multi-turn tool_result, F4 Caddy timeout, F5 identity preservation. These ALL move to Phase 74 (P74 — see ROADMAP.md line 93). They are NOT failures; they are scheduled work.

4. Create `.planning/docs/` directory if it does not exist.

5. Write `.planning/docs/external-client-compat.md` with this structure:

   ```markdown
   # External Client Compatibility Matrix
   
   **Status:** Phase 64 deliverable (v31.0 entry)
   **Last verified:** 2026-05-03 (live curl tests against api.livinity.io — see references)
   **Owner:** Livinity Broker team
   
   ## Constraint reminder (D-NO-BYOK)
   
   All external clients route through the Livinity Broker (livinity.io) over the
   subscription path. No client may use a raw `@anthropic-ai/sdk` API key or
   bring-your-own-key flow. The broker exposes two protocols:
   
   - **Anthropic-style:** `POST https://api.livinity.io/v1/messages`
   - **OpenAI-style:** `POST https://api.livinity.io/v1/chat/completions`
   
   Both protocols are token-translated through the same subscription auth chain.
   See `livos/packages/livinityd/source/modules/livinity-broker/router.ts` (Anthropic
   route) and `openai-router.ts` (OpenAI route).
   
   ## Compat matrix
   
   | Client | Auth mode | Endpoint | Streaming | Tool calls | Verified status | Known quirks | Reference |
   |---|---|---|---|---|---|---|---|
   | Bolt.diy | livinity broker key | `/v1/chat/completions` | SSE | broken at client (Bolt does not send `tools[]`) | partial (chat works, tools fail) | client-side: Bolt sends plain-text completions instead of tool-call requests; broker is innocent | memory `reference_broker_protocols_verified.md` (2026-05-03 curl) |
   | Cursor | livinity broker key | `/v1/messages` (Anthropic-style) | SSE | works (forwarded `tools[]` translates) | verified (live) | requires Anthropic-protocol mode in Cursor settings | memory `reference_broker_protocols_verified.md`; v30.0 P57 SUMMARY |
   | Cline | livinity broker key | `/v1/messages` | SSE | works | verified (live) | Cline reads `anthropic-version` header — broker forwards `2023-06-01` | memory `reference_broker_protocols_verified.md` |
   | Continue.dev | livinity broker key | `/v1/chat/completions` | SSE | works | verified (live) | OpenAI-style; configure `apiBase: https://api.livinity.io/v1` | memory `reference_broker_protocols_verified.md` |
   | Open WebUI | livinity broker key | `/v1/chat/completions` | SSE | works | verified (live) | Open WebUI auto-detects model list via `GET /v1/models` — confirm route exists | memory `reference_broker_protocols_verified.md`; v30.0 phase summaries |
   
   ## Open carryovers (scheduled to P74)
   
   The following broker work items affect cross-client behavior and are tracked
   in **Phase 74 (P74 — F2-F5 Carryover from v30.5)** in `.planning/ROADMAP.md`:
   
   - **F2 — Token cadence streaming:** Some clients (notably Cursor, Continue.dev)
     prefer token-by-token cadence rather than block-aggregated. Broker currently
     ships block-aggregated for both protocols. P74 reconciles.
   - **F3 — Multi-turn `tool_result` protocol:** When a client sends a follow-up
     message including `tool_result` content, broker forwarding semantics differ
     between `/v1/messages` and `/v1/chat/completions`. P74 unifies.
   - **F4 — Caddy timeout:** Long-running streaming responses can hit upstream
     Caddy idle-timeout. Mitigation in P74.
   - **F5 — Identity preservation:** Per-user OAuth identity passthrough; relevant
     when broker is behind multi-user routing. Tracked under P74.
   
   The Bolt.diy "plain-text instead of tool calls" symptom is **NOT** in this list
   — it is a Bolt-side bug (Bolt does not include `tools[]` in upstream requests
   despite the user enabling tools in Bolt's UI). It will close naturally once
   Bolt upstream fixes the issue or once we ship a Bolt-specific shim. No P74
   work needed.
   
   ## References
   
   - Memory: `reference_broker_protocols_verified.md` (2026-05-03 live curl proofs)
   - Memory: `feedback_subscription_only.md` (D-NO-BYOK constraint origin)
   - Memory: `project_v30_5_resume.md` (v30.5 close state, F2-F5 disposition)
   - Code: `livos/packages/livinityd/source/modules/livinity-broker/router.ts` (Anthropic route)
   - Code: `livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts` (OpenAI route)
   - Roadmap: `.planning/ROADMAP.md` Phase 74 (F2-F5 broker carryover)
   ```

6. If any single piece of compat data appears stale (e.g., the `reference_broker_protocols_verified.md` memory date 2026-05-03 is more than 30 days from today and there is doubt about a column), do a single live curl test from the working directory:
   `curl -sS https://api.livinity.io/v1/messages -H 'x-api-key: <broker key from /opt/livos/.env on Mini PC if available, else skip>' -H 'anthropic-version: 2023-06-01' -d '{"model":"claude-sonnet-4-5","max_tokens":10,"messages":[{"role":"user","content":"hi"}]}'`
   AND record the response shape in the doc as verification evidence. If the broker key is not available locally, mark that row's "Verified status" column as `verified (memory, 2026-05-03)` and note the date — do not fabricate a fresh "live" verification.

7. Confirm the doc opens cleanly: count the table rows (must be exactly 5 client rows + 1 header + 1 separator = 7 lines in the table block), confirm all 7 columns are populated for each row.

NOTE: Per D-12, the doc explicitly states which carryovers move to P74. Per D-NO-BYOK, the preamble reiterates that no raw-key BYOK is supported. Per the Bolt root-cause memory, Bolt is documented as a client-side bug, not a broker bug.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/docs/external-client-compat.md';if(!fs.existsSync(p)){console.error('file missing');process.exit(1)}const s=fs.readFileSync(p,'utf8');const lines=s.split('\n');const tableRows=lines.filter(l=>l.startsWith('|')).length;const hasAllClients=['Bolt.diy','Cursor','Cline','Continue.dev','Open WebUI'].every(c=>s.includes(c));const hasBothEndpoints=s.includes('/v1/messages')&&s.includes('/v1/chat/completions');const hasP74=/P74|Phase 74/.test(s);const hasNoBYOK=/D-NO-BYOK|no raw|never raw|subscription path/i.test(s);const hasF2F5=['F2','F3','F4','F5'].every(f=>s.includes(f));if(!hasAllClients){console.error('missing client(s)');process.exit(1)}if(!hasBothEndpoints){console.error('missing endpoint reference(s)');process.exit(1)}if(!hasP74){console.error('missing P74 reference');process.exit(1)}if(!hasNoBYOK){console.error('missing D-NO-BYOK reiteration');process.exit(1)}if(!hasF2F5){console.error('missing F2-F5 carryover list');process.exit(1)}if(tableRows<7){console.error('table too short, got '+tableRows+' rows');process.exit(1)}console.log('compat-matrix OK: '+tableRows+' table rows, all clients present, both endpoints, P74 + D-NO-BYOK + F2-F5 cited')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/docs/external-client-compat.md` exists.
    - File contains a markdown table with exactly 5 client rows (Bolt.diy, Cursor, Cline, Continue.dev, Open WebUI).
    - Every row has all 7 columns populated (no empty cells, no `?` in Verified status — use a verbal status).
    - Both endpoint paths `/v1/messages` and `/v1/chat/completions` are mentioned in the preamble or table.
    - D-NO-BYOK constraint is restated in the preamble (substring "subscription path" or "no raw" or "never raw" or "D-NO-BYOK").
    - F2, F3, F4, F5 carryover items are listed by code AND linked to P74 (substring "P74" or "Phase 74" appears).
    - Bolt.diy row's "Known quirks" cell explicitly attributes the plain-text symptom to Bolt-side (NOT broker-side).
    - Reference column for each row cites at least one source: memory file name, phase summary, or code file path.
    - File is at least 60 lines long.
    - No code files outside `.planning/docs/` were modified by this plan.
  </acceptance_criteria>
  <done>Compat matrix written, all 5 clients covered, both broker endpoints cited, F2-F5 carryover flagged to P74, Bolt root-cause correctly attributed.</done>
</task>

</tasks>

<verification>
- `.planning/docs/external-client-compat.md` exists and passes the automated structural check
- Single source of truth: 5 rows × 7 columns, no missing cells
- Memory entries are cited as evidence; live curl only used if memory was stale
- F2-F5 explicitly carry to P74; Bolt is correctly attributed as a client-side bug
- No code modified
</verification>

<success_criteria>
- ROADMAP Phase 64 success criterion #4 satisfied: external client compat matrix doc exists
- CARRY-04 requirement closed
- P74 plan-phase has a referenceable compat doc to start from
</success_criteria>

<output>
After completion, create `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-01-SUMMARY.md` summarizing: which 5 clients were documented, what compat data came from memory vs live curl, and a one-line status per client.
</output>
