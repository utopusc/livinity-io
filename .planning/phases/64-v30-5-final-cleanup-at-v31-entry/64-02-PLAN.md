---
phase: 64-v30-5-final-cleanup-at-v31-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md
autonomous: true
requirements: [CARRY-02]
tags: [uat, classification, milestone-audit, v29.3, v29.4, v29.5]

must_haves:
  truths:
    - "64-UAT-MATRIX.md exists in the phase dir as a single self-contained markdown file"
    - "The matrix lists ALL 14 carryforward UAT files: 4× v29.5, 4× v29.4, 6× v29.3 (total 14 rows beyond header)"
    - "Each row's Status column is exactly one of: `script-verified`, `needs-human-walk`, `failed`, `obsolete` — no other values, no blank cells"
    - "Per D-05 hard rule: any UAT requiring real browser interaction is classified `needs-human-walk`, NEVER `script-verified`"
    - "Each row's Evidence column has either: (a) a curl/redis-cli/systemctl/psql command + observed output for `script-verified`, or (b) the manual steps + expected outcome for `needs-human-walk`, or (c) a failure trace for `failed`, or (d) an obsolescence rationale for `obsolete`"
    - "The matrix's preamble explicitly cites `feedback_milestone_uat_gate.md` as the canonical UAT-discipline rule"
    - "No UAT is silently elevated from `human_needed` (the original VERIFICATION.md frontmatter status) to `passed` (`script-verified`)"
  artifacts:
    - path: ".planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md"
      provides: "14-row UAT classification matrix with evidence column"
      contains: "| Phase 39 |"
      min_lines: 50
  key_links:
    - from: "64-UAT-MATRIX.md"
      to: "the 14 source UAT/VERIFICATION files in .planning/milestones/v29.{3,4,5}-phases/*"
      via: "File path column referencing the source path for each row"
      pattern: "milestones/v29\\.[345]-phases/"
    - from: "64-UAT-MATRIX.md preamble"
      to: "memory `feedback_milestone_uat_gate.md`"
      via: "Explicit citation of the UAT discipline rule"
      pattern: "feedback_milestone_uat_gate"
---

<objective>
Produce `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md` — a single classification matrix listing all 14 carryforward UATs (6× v29.3 + 4× v29.4 + 4× v29.5) with status, evidence, and source-file reference. Per D-05, this matrix HONORS the UAT-discipline rule from `feedback_milestone_uat_gate.md`: any UAT that needs browser interaction is classified `needs-human-walk`, never silently `script-verified`.

Purpose: Satisfies Phase 64 success criterion #2 and CARRY-02. Surfaces honest UAT debt for v31. Downstream `audit-milestone` tooling can parse this file (vocabulary aligned with VERIFICATION.md frontmatter convention).

Output:
- `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md` (new file, ~80-150 lines: preamble + 14-row table + per-row evidence subsections)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md
@.planning/v29.4-REGRESSIONS.md

# UAT discipline rule (read this FIRST)
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_milestone_uat_gate.md

# v29.3 UATs (5 UAT.md files exist; 6th is in 39-AUDIT.md / 39-SUMMARY.md per dir listing — confirm 6 total during read)
@.planning/milestones/v29.3-phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md
@.planning/milestones/v29.3-phases/40-per-user-claude-oauth-home-isolation/40-UAT.md
@.planning/milestones/v29.3-phases/41-anthropic-messages-broker/41-UAT.md
@.planning/milestones/v29.3-phases/42-openai-compatible-broker/42-UAT.md
@.planning/milestones/v29.3-phases/43-marketplace-integration-anchor-mirofish/43-UAT.md
@.planning/milestones/v29.3-phases/44-per-user-usage-dashboard/44-UAT.md

# v29.4 UATs (4 phases; both UAT.md AND VERIFICATION.md exist — pick UAT.md as primary, VERIFICATION.md as evidence support)
@.planning/milestones/v29.4-phases/45-carry-forward-sweep/45-VERIFICATION.md
@.planning/milestones/v29.4-phases/46-fail2ban-admin-panel/46-UAT.md
@.planning/milestones/v29.4-phases/47-ai-diagnostics/47-UAT.md
@.planning/milestones/v29.4-phases/48-live-ssh-session-viewer/48-UAT.md

# v29.5 UATs (4 phases — VERIFICATION.md only)
@.planning/milestones/v29.5-phases/49-mini-pc-diagnostic/49-VERIFICATION.md
@.planning/milestones/v29.5-phases/50-a1-tool-registry-seed/50-VERIFICATION.md
@.planning/milestones/v29.5-phases/51-a2-streaming-fix/51-VERIFICATION.md
@.planning/milestones/v29.5-phases/52-a3-marketplace-state/52-VERIFICATION.md
</context>

<sacred_boundary>
**STRICT RULE for this plan:** Documentation-only. Do NOT modify any code or any of the 14 source UAT/VERIFICATION files. They are read-only inputs. Output is exactly one new file in the phase dir.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Read all 14 source UAT/VERIFICATION files and classify each</name>
  <files>.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md (D-04, D-05, D-06)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_milestone_uat_gate.md (canonical UAT discipline rule)
    - .planning/v29.4-REGRESSIONS.md (autopsy of the milestone-without-UAT failure mode — calibrate the bar)
    - All 14 source files listed in `<context>` above (read each in turn — single-batch the reads where possible)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md (if any row needs Mini PC SSH evidence, batch into ONE session)
  </read_first>
  <action>
1. Read 64-CONTEXT.md, `feedback_milestone_uat_gate.md`, and `v29.4-REGRESSIONS.md` first to internalize the UAT-discipline rule. Key clauses:
   - VERIFICATION.md frontmatter `status: human_needed` → matrix Status MUST be `needs-human-walk`. Never elevate to `script-verified`.
   - Browser interaction required (UI rendering, click flows, PWA cache, install dialogs) → `needs-human-walk`.
   - Pure backend assertions (curl works, systemctl active, redis key present, psql row exists) → eligible for `script-verified` IF a real check runs and observed output is in the Evidence column.
   - Documented prior failure / regression that was never closed → `failed`.
   - Phase scope removed / superseded by later phase → `obsolete`.

2. Confirm the 14-file inventory by listing the milestone dirs:
   - v29.3: 6 phases (39, 40, 41, 42, 43, 44). For phase 39, the UAT-equivalent artifact is `39-AUDIT.md` (no `39-UAT.md` file exists per dir listing). For phases 40-44 the file is `{N}-UAT.md`.
   - v29.4: 4 phases (45, 46, 47, 48). Phase 45 has `45-VERIFICATION.md` (no UAT.md); phases 46-48 have BOTH `{N}-UAT.md` and `{N}-VERIFICATION.md`. Use UAT.md as primary when present, fall back to VERIFICATION.md.
   - v29.5: 4 phases (49, 50, 51, 52). Each has `{N}-VERIFICATION.md`. (Phases 53 and 54 also have VERIFICATION.md but the CONTEXT.md says only 4× v29.5 UATs; cross-check by reading STATE.md "Deferred Items" if there is ambiguity, otherwise use 49-52 — these are the four "A-series" that match v29.5's published UAT scope.)
   
   If during reading the inventory turns out different (e.g., 39 has a separate UAT file under a different name, or v29.5 actually wants 51-54), preserve the count of 14 by following whichever 14 files STATE.md or 64-CONTEXT.md `<canonical_refs>` actually points at. Do NOT silently pad/cut to 14.

3. For each of the 14 source files, perform this classification procedure:
   a. Read the file (full).
   b. Read its frontmatter `status:` field if present.
   c. Look for tags like "human-walk", "browser", "UI", "click", "render", "smoke test in browser" — these force `needs-human-walk`.
   d. Look for backend-only check items (curl X, systemctl status Y, redis-cli SMEMBERS Z, psql SELECT) — these are eligible for `script-verified` IF you can actually run them and observe output.
   e. If a check is backend-eligible AND requires Mini PC access, BATCH the SSH commands into ONE single ssh session (per `feedback_ssh_rate_limit.md`):
      `ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'systemctl is-active livos liv-core liv-worker liv-memory; redis-cli -a "$(grep ^REDIS_URL /opt/livos/.env | cut -d= -f2 | sed "s/.*://;s/@.*//")" ping; ...'` (build the full command string from ALL backend checks across all 14 UATs, run ONCE, parse output into per-row evidence).
   f. Per D-NO-SERVER4: Server4 is OFF-LIMITS. If any UAT mentions Server4, classify it `obsolete` with rationale "D-NO-SERVER4 hard rule per MEMORY.md — never apply".
   g. Choose status: `script-verified` (only if you observed real evidence in step e), `needs-human-walk` (browser/UI/click), `failed` (regression noted), or `obsolete` (superseded/Server4/scope-removed).
   h. Build the Evidence cell content per chosen status:
      - `script-verified` → command + first 200 chars of stdout/stderr observed
      - `needs-human-walk` → 2-5 numbered manual steps + expected outcome (pulled from the source UAT file's existing "manual steps" if it has them; otherwise summarize from context)
      - `failed` → trace excerpt or commit ref of the failure
      - `obsolete` → one-sentence rationale + supersession pointer if any

4. Write `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md` with this structure:

   ```markdown
   # 64-UAT-MATRIX — v29.3 + v29.4 + v29.5 carryforward UAT classification
   
   **Phase:** 64-v30-5-final-cleanup-at-v31-entry
   **Generated:** <today's date>
   **Discipline rule:** Per memory `feedback_milestone_uat_gate.md`, no UAT
   originally marked `human_needed` is allowed to be silently re-classified as
   `passed` / `script-verified` here. Browser-interaction UATs are
   `needs-human-walk` even if "the backend looks healthy."
   
   ## Vocabulary (aligned with VERIFICATION.md `status:` convention)
   
   | Status            | Means                                                                                       |
   |-------------------|---------------------------------------------------------------------------------------------|
   | `script-verified` | Backend evidence observed (curl/systemctl/redis/psql output cited in Evidence cell)         |
   | `needs-human-walk`| Requires real browser interaction or click-flow; cannot be automated; user walks on Mini PC |
   | `failed`          | Regression / observed failure that was never closed                                         |
   | `obsolete`        | Superseded by later phase, scope removed, or D-NO-SERVER4 forbids                           |
   
   ## Matrix
   
   | # | Milestone | Phase | Source file | Status | Evidence summary |
   |---|---|---|---|---|---|
   | 1 | v29.3 | 39 — risk-fix-close-oauth-fallback | .planning/milestones/v29.3-phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md | <status> | <one-line summary; full evidence below> |
   | 2 | v29.3 | 40 — per-user-claude-oauth-home-isolation | ... | ... | ... |
   ... (14 rows total)
   
   ## Per-row evidence
   
   ### Row 1 — Phase 39 (v29.3) — risk-fix-close-oauth-fallback
   
   **Status:** <chosen>
   **Source:** `.planning/milestones/v29.3-phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md`
   **Evidence:**
   <command + observed output  OR  numbered manual steps + expected outcome  OR  failure trace  OR  obsolescence rationale>
   
   ### Row 2 — Phase 40 (v29.3) — per-user-claude-oauth-home-isolation
   ... (one ### block per row, 14 total)
   
   ## Summary counts
   
   - `script-verified`: N
   - `needs-human-walk`: N
   - `failed`: N
   - `obsolete`: N
   - **Total:** 14
   ```

5. Compute the summary counts and confirm they total exactly 14.

6. If any UAT cannot be cleanly classified into one of the 4 statuses, STOP and document the ambiguity inline as a sub-row note (do not invent a 5th status); fall back to `needs-human-walk` (the conservative honest-debt choice).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md';if(!fs.existsSync(p)){console.error('matrix file missing');process.exit(1)}const s=fs.readFileSync(p,'utf8');const lines=s.split('\n');const tableRows=lines.filter(l=>l.startsWith('|')&&l.includes('|',1)).length;const evidenceBlocks=(s.match(/^### Row /gm)||[]).length;const statuses=(s.match(/script-verified|needs-human-walk|failed|obsolete/g)||[]);const hasGate=/feedback_milestone_uat_gate/.test(s);const hasV293=/v29\.3/.test(s);const hasV294=/v29\.4/.test(s);const hasV295=/v29\.5/.test(s);const hasTotal14=/Total.*14/.test(s)||/14 rows/.test(s);if(!hasGate){console.error('missing feedback_milestone_uat_gate citation');process.exit(1)}if(!(hasV293&&hasV294&&hasV295)){console.error('missing one of v29.3/v29.4/v29.5');process.exit(1)}if(evidenceBlocks!==14){console.error('evidence blocks count != 14, got '+evidenceBlocks);process.exit(1)}if(statuses.length<14){console.error('not enough status tags, got '+statuses.length);process.exit(1)}console.log('UAT matrix OK: '+evidenceBlocks+' evidence blocks, '+statuses.length+' status tags, all 3 milestones cited')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-UAT-MATRIX.md` exists.
    - File contains exactly 14 `### Row N` evidence subsections (one per UAT).
    - File contains a matrix table with 14 data rows + 1 header + 1 separator (16 lines starting with `|`).
    - Each row's Status is exactly one of: `script-verified`, `needs-human-walk`, `failed`, `obsolete`.
    - All 3 milestones `v29.3`, `v29.4`, `v29.5` are referenced.
    - Memory `feedback_milestone_uat_gate.md` is cited by name in the preamble.
    - Summary counts are present and total exactly 14.
    - For every row classified `script-verified`, the Evidence section contains a command (`curl`, `systemctl`, `redis-cli`, `psql`, `git log`, etc.) AND an observed output snippet — never just "checked, looks fine".
    - For every row classified `needs-human-walk`, the Evidence section contains a numbered list of manual steps (>=2 steps) AND an expected outcome.
    - No row originally marked `human_needed` in source frontmatter has been silently elevated to `script-verified` (cross-check: for any source file with `status: human_needed`, the matrix row MUST NOT be `script-verified`).
    - No code files were modified.
  </acceptance_criteria>
  <done>14 UATs classified honestly per D-05 hard rule; matrix is self-contained, parseable, and audit-ready.</done>
</task>

</tasks>

<verification>
- 64-UAT-MATRIX.md exists and passes automated structural check
- 14 rows total, 4 statuses, no silent elevations from `human_needed` to `script-verified`
- Memory `feedback_milestone_uat_gate.md` honored
- Mini PC SSH calls (if any) batched into single sessions per `feedback_ssh_rate_limit.md`
- No D-NO-SERVER4 violation (any Server4 UAT marked `obsolete`)
</verification>

<success_criteria>
- ROADMAP Phase 64 success criterion #2 satisfied: 14 UAT files walked & documented
- CARRY-02 requirement closed
- Audit-milestone tooling can parse the matrix using existing VERIFICATION.md vocabulary
</success_criteria>

<output>
After completion, create `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-02-SUMMARY.md` summarizing: total UATs classified, breakdown by status (counts), notable findings (any `failed` or `obsolete` ones), and which `needs-human-walk` items the user must walk before v31 closes.
</output>
