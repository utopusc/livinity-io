---
phase: 64-v30-5-final-cleanup-at-v31-entry
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md
  - .planning/BACKLOG.md
autonomous: true
requirements: [CARRY-05]
tags: [quick-tasks, v28, triage, backlog, hot-patch]

must_haves:
  truths:
    - "64-QUICK-TASK-TRIAGE.md exists in the phase dir and lists all 3 v28.0 quick-tasks (260425-sfg, 260425-v1s, 260425-x6q)"
    - "Each quick-task has a decision: `resolved`, `backlogged`, or `already-resolved` (if SUMMARY.md indicates it was completed)"
    - "Per D-13/D-14, default lean is `resolved` if file paths are unchanged or fixable in <30min; `backlogged` otherwise"
    - "Any backlogged task has its PLAN.md context preserved into a new entry in `.planning/BACKLOG.md`"
    - "Any resolved task that required a code fix has the fix verified by grep/test (cited in the triage doc)"
    - "Any task already-resolved (existing SUMMARY.md indicates completion) is verified by re-checking the file paths still satisfy the original PLAN.md acceptance criteria"
    - "No silent dropping: every quick-task ends in exactly one of the 3 states with documented rationale"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` is NOT touched"
  artifacts:
    - path: ".planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md"
      provides: "Triage decision for 3 v28.0 quick-tasks with rationale and evidence"
      contains: "260425-sfg"
      min_lines: 30
  key_links:
    - from: "64-QUICK-TASK-TRIAGE.md"
      to: ".planning/quick/260425-{sfg,v1s,x6q}-* PLAN.md and SUMMARY.md"
      via: "Per-task subsection with current code-state check"
      pattern: "260425-(sfg|v1s|x6q)"
    - from: "Backlogged tasks"
      to: ".planning/BACKLOG.md (appended entries)"
      via: "Append per `feedback_subscription_only.md` style — preserve original PLAN.md context"
      pattern: "260425-(sfg|v1s|x6q)"
---

<objective>
Triage the 3 v28.0 quick-tasks in `.planning/quick/`: `260425-sfg-v28-0-hot-patch-bundle-tailwind-sync-bg-`, `260425-v1s-v28-0-hot-patch-round-2-activity-overflo`, `260425-x6q-v28-0-hot-patch-round-3-window-only-nav-`. For each, read its PLAN.md and SUMMARY.md, check the current code state of the file paths it touched, and decide per D-13/D-14: `resolved` (apply trivial fix or confirm already-fixed), `backlogged` (preserve in BACKLOG.md), or `already-resolved` (SUMMARY.md indicates completion + spot-check confirms acceptance criteria still hold).

Note: All three quick-task dirs already have a SUMMARY.md present (per dir listing) — most are likely `already-resolved` and need spot-check verification only. Don't duplicate work.

Purpose: Satisfies Phase 64 success criterion #5 and CARRY-05.

Output:
- `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md` (new file, ~30-60 lines)
- `.planning/BACKLOG.md` (appended only if any task is backlogged)
- Optional: small code fix(es) if any task is trivially resolvable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md
@.planning/BACKLOG.md

# Quick-task source dirs
@.planning/quick/260425-sfg-v28-0-hot-patch-bundle-tailwind-sync-bg-/260425-sfg-PLAN.md
@.planning/quick/260425-sfg-v28-0-hot-patch-bundle-tailwind-sync-bg-/260425-sfg-SUMMARY.md
@.planning/quick/260425-v1s-v28-0-hot-patch-round-2-activity-overflo/260425-v1s-PLAN.md
@.planning/quick/260425-v1s-v28-0-hot-patch-round-2-activity-overflo/260425-v1s-SUMMARY.md
@.planning/quick/260425-x6q-v28-0-hot-patch-round-3-window-only-nav-/260425-x6q-PLAN.md
@.planning/quick/260425-x6q-v28-0-hot-patch-round-3-window-only-nav-/260425-x6q-SUMMARY.md
</context>

<sacred_boundary>
**STRICT RULES for this plan:**
1. Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` is NOT touched. Verify SHA at end of plan.
2. Subscription-only honored: any code fix MUST NOT touch broker auth or introduce BYOK.
3. D-NO-SERVER4: no Server4 SSH or references.
4. If a quick-task's "trivial fix" turns out to require >30min or touches >3 files (per D-14 boundary), REVERT to backlogged — do not let scope creep silently.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Read 3 quick-tasks, check code state, decide resolve/backlog/already-resolved per D-13/D-14</name>
  <files>.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md, .planning/BACKLOG.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md (D-13, D-14)
    - All 6 files in `<context>` (3 PLAN.md + 3 SUMMARY.md)
    - .planning/BACKLOG.md (current structure — to know how to append cleanly)
  </read_first>
  <action>
1. For each of the 3 quick-tasks, read both PLAN.md and SUMMARY.md. Note:
   - The PLAN.md describes the original v28.0 issue (e.g., "tailwind sync bg", "activity overflow", "window-only nav").
   - The SUMMARY.md (if it exists with non-trivial content) likely says the issue was fixed in a v28.x hot-patch round. Read it carefully; the SUMMARY.md may already document the resolution.

2. For each quick-task, run a code-state check:
   a. Identify the primary file path(s) the PLAN.md targeted (look for `<files>` blocks, "modify", "edit" mentions, or specific component/module names).
   b. Use Grep / Read to confirm the current state of those files: does the regression described in PLAN.md still exist?
   c. Compare against SUMMARY.md's claim. If SUMMARY.md says "fixed in commit X" and grep confirms the fix is present in master, classify `already-resolved`.
   d. If SUMMARY.md is empty/stub OR the fix is NOT visible in current code, classify `backlogged` UNLESS the fix is a sub-30min cleanup (D-14 default lean).

3. For each task, build the triage entry:
   - **already-resolved:** Spot-check evidence (grep showing the fix is present, or commit hash from SUMMARY.md verified via `git log --oneline --grep="<short-tag>"`). One paragraph.
   - **resolved:** If you applied a fresh fix in this plan, list the file(s) changed, the diff (>=2 lines, <=15 lines per D-14 sub-30min boundary), and a verify command. STOP and re-classify to `backlogged` if the fix grows beyond that.
   - **backlogged:** Append to `.planning/BACKLOG.md`. Use the existing BACKLOG.md style (look at section pattern). Preserve the original PLAN.md context as a quote block; add date and rationale. Format:
     ```markdown
     ---
     
     ## v28.0 hot-patch carryover — <short-name> (<task-id>)
     
     **Captured:** <today>
     **Source:** `.planning/quick/<full-dir>/`
     **Original PLAN.md context:**
     
     > <quoted PLAN.md objective + first paragraph>
     
     **Why backlogged:** <D-14 rationale: file paths drifted / scope > 30min / requires research / blocked by future phase>
     
     **Suggested next phase:** <best fit phase from ROADMAP, or "TBD post-v31">
     ```

4. Write `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md`:

   ```markdown
   # 64-QUICK-TASK-TRIAGE — 3 v28.0 hot-patch quick-tasks
   
   **Phase:** 64-v30-5-final-cleanup-at-v31-entry
   **Generated:** <today>
   **Decision rule:** Per CONTEXT.md D-13/D-14 — resolve trivially or backlog with PLAN.md context preserved.
   
   ## Triage table
   
   | Task ID | Short name | Original target | Decision | Rationale (one line) |
   |---|---|---|---|---|
   | 260425-sfg | bundle/tailwind/sync/bg | <files> | <already-resolved \| resolved \| backlogged> | <one line> |
   | 260425-v1s | activity overflow | <files> | ... | ... |
   | 260425-x6q | window-only nav | <files> | ... | ... |
   
   ## Per-task evidence
   
   ### 260425-sfg — <short name>
   
   **Source:** `.planning/quick/260425-sfg-v28-0-hot-patch-bundle-tailwind-sync-bg-/`
   **Original PLAN.md objective:** <one paragraph quoted>
   **SUMMARY.md state:** <existed/stub/empty + summary>
   **Code-state check:** <grep/read commands run + observed result>
   **Decision:** <already-resolved | resolved | backlogged>
   **Evidence:**
   - already-resolved → grep output showing fix present in master + commit hash
   - resolved → diff applied (file path + line numbers) + verify command output
   - backlogged → entry appended to BACKLOG.md (cite section heading)
   
   ### 260425-v1s — <short name>
   ...
   
   ### 260425-x6q — <short name>
   ...
   
   ## Summary counts
   
   - already-resolved: N
   - resolved (this plan): N
   - backlogged: N
   - **Total:** 3
   ```

5. If any task was backlogged, open `.planning/BACKLOG.md` with the Edit tool, find a clean append point (after the last `---` separator), and append the new section(s). Do NOT rewrite existing BACKLOG content.

6. If any task was `resolved` and required a code fix, run the appropriate verify command to confirm the fix doesn't break the build:
   - For UI-touching fixes: `pnpm --filter ui build` from the repo root, capture exit code only (build success = sufficient evidence; do not need full test suite).
   - For livinityd-touching fixes: `pnpm --filter @livos/livinityd typecheck` if such a script exists, else `tsc --noEmit` on the affected file.
   - Cite output in the triage doc.

7. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append to triage doc footer ("Sacred file SHA: <value>") to confirm sacred file unchanged.

NOTE: Triage doc + BACKLOG.md append + (optional) tiny code fix are all in this single task because the workload is small. If during reading any one task balloons (e.g., the "resolve" path turns out to require >3 file edits), revert that task to `backlogged` and append to BACKLOG.md instead. Do NOT exceed the 30-min D-14 boundary on any single task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md';if(!fs.existsSync(p)){console.error('triage doc missing');process.exit(1)}const s=fs.readFileSync(p,'utf8');const allThreeTasks=['260425-sfg','260425-v1s','260425-x6q'].every(t=>s.includes(t));const decisionsCount=(s.match(/already-resolved|^.*resolved|backlogged/gm)||[]).filter(d=>d).length;const hasTriageTable=/Triage table[\s\S]*\|/.test(s);const hasPerTaskEvidence=(s.match(/^### 260425-(sfg|v1s|x6q)/gm)||[]).length===3;const hasSummaryTotal3=/Total.*3/.test(s);const hasSacredSHA=/Sacred file SHA/.test(s);if(!allThreeTasks){console.error('not all 3 task IDs cited');process.exit(1)}if(!hasTriageTable){console.error('triage table missing');process.exit(1)}if(!hasPerTaskEvidence){console.error('per-task evidence sections != 3');process.exit(1)}if(!hasSummaryTotal3){console.error('summary count != 3');process.exit(1)}if(!hasSacredSHA){console.error('Sacred file SHA missing');process.exit(1)}console.log('triage doc OK: 3 tasks, table present, per-task evidence x3, summary total 3, sacred SHA recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-QUICK-TASK-TRIAGE.md` exists.
    - File lists all 3 quick-tasks with one decision each (`already-resolved` / `resolved` / `backlogged`).
    - File contains a triage table AND 3 per-task `### 260425-{sfg|v1s|x6q}` evidence subsections.
    - For every `already-resolved` task: evidence cites a grep result OR commit hash showing the fix is in current master.
    - For every `resolved` task: evidence cites file path(s), diff (<=15 lines), AND a build/typecheck verification result.
    - For every `backlogged` task: a corresponding section was appended to `.planning/BACKLOG.md` containing the original PLAN.md objective as a quote block AND a "Why backlogged" rationale.
    - Summary counts present and total exactly 3.
    - Sacred file SHA recorded; value unchanged from project baseline (any baseline; the constraint is "didn't change DURING this plan").
    - No edits to `nexus/packages/core/src/sdk-agent-runner.ts`.
    - No edits to broker source code, no BYOK code paths introduced.
    - No Server4 references in any executed command.
  </acceptance_criteria>
  <done>3 v28.0 quick-tasks triaged; BACKLOG.md updated for any deferred items; any in-plan code fixes built cleanly; sacred file untouched.</done>
</task>

</tasks>

<verification>
- 64-QUICK-TASK-TRIAGE.md exists and passes automated structural check
- 3 tasks accounted for; each has exactly one decision; all decisions are documented
- Backlogged items preserve PLAN.md context in BACKLOG.md
- Resolved items (if any) verified by build/typecheck
- Sacred file unchanged
</verification>

<success_criteria>
- ROADMAP Phase 64 success criterion #5 satisfied: 3 v28.0 quick-tasks resolved or moved to backlog
- CARRY-05 requirement closed
- `.planning/quick/` no longer has unactioned v28.0 items at v31 entry
</success_criteria>

<output>
After completion, create `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-05-SUMMARY.md` summarizing: per-task decisions, any code fixes applied, BACKLOG.md additions count, and confirmation that the `.planning/quick/` v28.0 directory is now fully accounted for.
</output>
