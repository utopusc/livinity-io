---
phase: 64-v30-5-final-cleanup-at-v31-entry
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md
  - scripts/suna-insert.sql
autonomous: false
requirements: [CARRY-01]
tags: [suna, sandbox, docker-network, server5, mini-pc, smoke-test, host-docker-internal]

must_haves:
  truths:
    - "Server5 platform.apps row for `slug='suna'` has its `docker_compose` field updated with KORTIX_SANDBOX_URL=http://host.docker.internal:14000 (env-override fix per D-01)"
    - "Validation order honored per D-02: candidate (1) live network connect on Mini PC FIRST to confirm root cause, THEN candidate (3) shipped as permanent fix to Server5"
    - "Mini PC `kortix-api` container can reach `kortix-sandbox` after fix (scriptable verification: curl from kortix-api container to sandbox port 14000 returns 200/expected response)"
    - "If `host.docker.internal` is unavailable on Mini PC's Linux Docker, fallback to candidate (2) source patch is documented (not necessarily implemented in this plan if (3) works)"
    - "64-SUNA-FIX-EVIDENCE.md records: candidate-1 confirmation output, candidate-3 fix SQL, scriptable verification output, AND the human-walk smoke test ('Navigate to google.com via Suna UI') as a `needs-human-walk` deferral per D-03"
    - "scripts/suna-insert.sql is updated to reflect the new compose with KORTIX_SANDBOX_URL env so the fix is reproducible and source-controlled"
    - "D-NO-SERVER4 honored: only Server5 (apps DB) and Mini PC (deploy target) are touched"
    - "Subscription-only honored: no broker code changes; ANTHROPIC_BASE_URL still points at livinity broker, ANTHROPIC_API_KEY still uses subscription-path key"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA unchanged at end of plan"
  artifacts:
    - path: ".planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md"
      provides: "Diagnosis output, fix SQL, scriptable verification output, human-walk smoke-test instructions"
      contains: "KORTIX_SANDBOX_URL"
      min_lines: 40
    - path: "scripts/suna-insert.sql"
      provides: "Updated upsert SQL containing the env-override compose"
      contains: "KORTIX_SANDBOX_URL"
  key_links:
    - from: "Server5 platform.apps suna row"
      to: "Mini PC Suna deployment (kortix-api container env)"
      via: "App Store install pulls compose from Server5 → user installs → Mini PC compose has env override → kortix-api reaches sandbox"
      pattern: "KORTIX_SANDBOX_URL.*host\\.docker\\.internal"
    - from: "64-SUNA-FIX-EVIDENCE.md"
      to: "smoke-test deferral (needs-human-walk)"
      via: "Documented manual steps for user to walk 'Navigate to google.com' through Suna UI"
      pattern: "needs-human-walk|Navigate to google"
---

<objective>
Resolve the F7 Suna sandbox network blocker per D-01..D-03. The fix is a one-line env override in the Suna `docker_compose` field on Server5's `platform.apps` table: set `KORTIX_SANDBOX_URL=http://host.docker.internal:14000` so kortix-api sidesteps Docker DNS entirely and reaches the sandbox via the host bridge.

This plan has 3 tasks:
1. **Diagnose** (candidate 1 live test on Mini PC): confirm the network-isolation theory via `docker network connect suna_default kortix-sandbox`.
2. **Ship the permanent fix** (candidate 3): update Server5's `apps` row + commit the matching SQL template; redeploy Suna on Mini PC; run scriptable container-network checks.
3. **Document the human-walk smoke test** ("Navigate to google.com" through Suna UI) as a `needs-human-walk` deferral — this plan does NOT auto-pass it.

Purpose: Satisfies Phase 64 success criterion #1 and CARRY-01. Closes the v30.0 Suna blocker before v31 builds on top.

Output:
- Updated row in Server5 `platform.apps` (slug='suna') via SQL UPSERT
- Updated `scripts/suna-insert.sql` (source-controlled template matches DB)
- Mini PC redeploy (uninstall+reinstall Suna, OR live `docker compose up -d` with new env)
- `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md` with all evidence + the deferred human-walk smoke test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md
@.planning/v31-DRAFT.md

# Source-of-truth SQL template (will be updated by this plan)
@scripts/suna-insert.sql

# Server/infra references — memory
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_server5_app_store.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_minipc.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_minipc_ssh.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md
# Memory: C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_milestone_uat_gate.md
</context>

<sacred_boundary>
**STRICT RULES for this plan:**

1. **D-NO-SERVER4 (HARD):** Server4 (45.137.194.103) is NOT touched. The user's deploy targets are Mini PC (10.69.31.68) and Server5 (45.137.194.102) ONLY. Any SSH command MUST verify it is targeting Mini PC or Server5; never Server4.
2. **Sacred file:** `nexus/packages/core/src/sdk-agent-runner.ts` is read-only. This plan should not need to even Read it. At end of plan, run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` to confirm unchanged.
3. **Subscription-only (D-NO-BYOK):** Suna's `ANTHROPIC_BASE_URL` continues to point at livinity broker; `ANTHROPIC_API_KEY` continues to be a broker-issued key. This plan does NOT modify the broker auth path.
4. **No livinityd / nexus source edits:** Fix is config-only — Server5 DB row + Mini PC deploy. No code in `livos/` or `nexus/` is changed.
5. **Mini PC SSH batching:** Per `feedback_ssh_rate_limit.md`, batch multi-step diagnostic and deploy commands into single SSH sessions. fail2ban will ban rapid-probe sessions.
6. **No fake `script-verified` for the smoke test:** Per `feedback_milestone_uat_gate.md` and D-03/D-05, the "Navigate to google.com" smoke test is `needs-human-walk` — this plan documents it as a deferred human-walk item; it does NOT silently mark it as passed.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose — confirm root cause via candidate (1) on Mini PC</name>
  <files>.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md (D-01, D-02)
    - .planning/v31-DRAFT.md lines 100-117 (3 candidates, ranking)
    - scripts/suna-insert.sql (current compose state — confirm KORTIX_SANDBOX_URL is NOT set yet)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_minipc_ssh.md (SSH command + sudo notes)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/feedback_ssh_rate_limit.md (batch rule)
  </read_first>
  <action>
1. Read context files. Confirm the diagnosis hypothesis: kortix-api is on `suna_default` network; kortix-sandbox is on Docker default bridge (NOT joined to `suna_default`); therefore Docker DNS in `suna_default` cannot resolve `kortix-sandbox`.

2. SSH to Mini PC in a SINGLE BATCHED session (per `feedback_ssh_rate_limit.md`). Use this exact command pattern from the working directory:
   ```
   ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'set -e; echo "=== A: docker networks ==="; sudo docker network ls; echo "=== B: containers ==="; sudo docker ps --format "{{.Names}}\t{{.Networks}}" | grep -i kortix; echo "=== C: kortix-api network inspect ==="; sudo docker inspect kortix-api --format "{{json .NetworkSettings.Networks}}" 2>/dev/null || echo "kortix-api not running"; echo "=== D: kortix-sandbox network inspect ==="; sudo docker inspect kortix-sandbox --format "{{json .NetworkSettings.Networks}}" 2>/dev/null || echo "kortix-sandbox not running"; echo "=== E: DNS resolution test from kortix-api ==="; sudo docker exec kortix-api getent hosts kortix-sandbox 2>&1 || echo "DNS resolution failed (expected if hypothesis correct)"; echo "=== F: candidate (1) live test — connect kortix-sandbox to suna_default ==="; sudo docker network connect suna_default kortix-sandbox 2>&1 || echo "already connected or network missing"; echo "=== G: re-test DNS after connect ==="; sleep 2; sudo docker exec kortix-api getent hosts kortix-sandbox 2>&1 || echo "still failing"; echo "=== H: re-test connectivity from kortix-api ==="; sudo docker exec kortix-api curl -sS --max-time 5 http://kortix-sandbox:14000/ 2>&1 | head -c 500; echo; echo "=== I: host.docker.internal availability check ==="; sudo docker exec kortix-api getent hosts host.docker.internal 2>&1 || echo "host.docker.internal NOT resolvable — candidate (3) requires extra_hosts"; echo "=== J: sandbox port 14000 reachable from host ==="; curl -sS --max-time 5 http://localhost:14000/ 2>&1 | head -c 500; echo "=== K: confirm Mini PC identity ==="; hostname; ip addr show | grep "10.69.31.68"'
   ```
   
   Capture full stdout. The "K" block confirms we are on Mini PC (NOT Server4 — D-NO-SERVER4 hard rule). Hostname should match Mini PC; IP should include 10.69.31.68.

3. Interpret output:
   - If block E ("DNS resolution test") fails AND block G (after connect) succeeds AND block H returns 200/sandbox response: hypothesis CONFIRMED. Candidate (1) works at runtime, but is non-persistent across recreations — therefore ship candidate (3) as the permanent fix.
   - If block I ("host.docker.internal") shows the hostname IS resolvable: candidate (3) is safe — proceed.
   - If block I fails (Linux Docker default does not have `host.docker.internal`): candidate (3) requires `extra_hosts: ["host.docker.internal:host-gateway"]` in the compose service, OR fallback to candidate (2) source patch.
   - If block J fails (port 14000 not on host): the sandbox is not exposed to host; candidate (3) needs the sandbox compose to publish 14000 to host first, OR use candidate (1)+make-it-persistent (compose `networks` join in sandbox service).

4. Choose the actual fix path based on observation:
   - **Path A (preferred):** candidate (3) — env override `KORTIX_SANDBOX_URL=http://host.docker.internal:14000` AND, if needed, `extra_hosts: ["host.docker.internal:host-gateway"]` for kortix-api on Linux.
   - **Path B (fallback):** candidate (1) made persistent — add `networks: [suna_default]` to kortix-sandbox in compose so it joins the same network at startup.
   - **Path C (last resort):** candidate (2) — source patch in kortix-api Docker spawn config. Out of scope for config-only fix; if reached, STOP and surface to user.

5. Begin writing `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md`. Structure:
   ```markdown
   # 64-SUNA-FIX-EVIDENCE — F7 Suna sandbox network blocker
   
   **Phase:** 64-v30-5-final-cleanup-at-v31-entry
   **Generated:** <today>
   **Hard rules honored:** D-NO-SERVER4 (only Mini PC + Server5 touched), D-NO-BYOK (broker auth path unchanged), sacred file (sdk-agent-runner.ts SHA verified unchanged).
   
   ## Step 1 — Diagnosis (candidate 1 live test on Mini PC)
   
   **Command (single-batched SSH per feedback_ssh_rate_limit.md):**
   ```
   <the full ssh -c batch command>
   ```
   
   **Observed output:**
   ```
   <full captured stdout including hostname/IP confirmation>
   ```
   
   **Identity check:** hostname=<X>, IP=10.69.31.68 → Mini PC confirmed (NOT Server4).
   
   **Interpretation:**
   - DNS resolution from kortix-api → kortix-sandbox: <FAIL/OK>
   - After `network connect`: <FAIL/OK>
   - host.docker.internal resolvable from kortix-api: <YES/NO>
   - Port 14000 reachable from host: <YES/NO>
   
   **Verdict:** Root cause CONFIRMED — candidate (1) restores connectivity at runtime. Permanent fix = candidate (3) <Path A | Path B | Path C>.
   ```

6. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` from the working directory and append output to evidence file under "Sacred file SHA after Task 1". Sacred file SHA is committed in v31 codebase; just record whatever the current value is — the rule is that THIS plan does not change it. If the value differs from a baseline, note it but do NOT fail the task on that alone.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md';if(!fs.existsSync(p)){console.error('evidence file missing');process.exit(1)}const s=fs.readFileSync(p,'utf8');const hasStep1=/Step 1.*Diagnosis/i.test(s);const hasMiniPCConfirm=/10\.69\.31\.68|Mini PC confirmed/.test(s);const hasInterpretation=/Interpretation/.test(s);const hasVerdict=/Verdict/.test(s);const hasSacredSHA=/Sacred file SHA/.test(s);if(!hasStep1){console.error('Step 1 heading missing');process.exit(1)}if(!hasMiniPCConfirm){console.error('Mini PC identity confirmation missing — D-NO-SERVER4 risk');process.exit(1)}if(!hasInterpretation){console.error('Interpretation block missing');process.exit(1)}if(!hasVerdict){console.error('Verdict missing');process.exit(1)}if(!hasSacredSHA){console.error('Sacred file SHA check missing');process.exit(1)}console.log('Step 1 diagnosis OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md` exists with Step 1 section.
    - Step 1 contains: full SSH batch command, full captured output, Mini PC identity confirmation (10.69.31.68 or hostname), interpretation of each diagnostic block, verdict choosing Path A/B/C.
    - Sacred file SHA recorded.
    - SSH used a SINGLE batched session (one `ssh -c '...; ...; ...'` invocation, not multiple).
    - No Server4 references in any executed command.
  </acceptance_criteria>
  <done>Root cause confirmed; fix path chosen; evidence file initialized with Step 1.</done>
</task>

<task type="auto">
  <name>Task 2: Ship candidate (3) — Server5 apps row update + suna-insert.sql commit + Mini PC redeploy + scriptable verification</name>
  <files>scripts/suna-insert.sql, .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-CONTEXT.md
    - scripts/suna-insert.sql (read fully to know exact structure)
    - .planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md (Step 1 verdict — determines Path A vs B)
    - C:/Users/hello/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/reference_server5_app_store.md (SSH path to Server5, platform.apps schema)
  </read_first>
  <action>
1. Re-read `scripts/suna-insert.sql`. Identify the `kortix-api` service block in the `$compose$ ... $compose$` heredoc. Currently the `environment:` map has these keys:
   ```
   SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, DB_MODE, ANTHROPIC_BASE_URL, ANTHROPIC_API_KEY, OPENCODE_CONFIG_JSON
   ```

2. Update the SQL — apply the chosen Path:
   - **Path A (env override):** Add a single new env line under kortix-api `environment:`:
     ```
     KORTIX_SANDBOX_URL: http://host.docker.internal:14000
     ```
     If Step 1 showed `host.docker.internal` was NOT resolvable on Linux, ALSO add at the kortix-api service level:
     ```
     extra_hosts:
       - "host.docker.internal:host-gateway"
     ```
   - **Path B (fallback):** Find or add a `kortix-sandbox` service block. Add to it:
     ```
     networks:
       - default
       - suna_default
     ```
     and define `suna_default` at top-level `networks:` if missing. (Path A is preferred; only use Path B if Step 1 said host.docker.internal cannot work even with extra_hosts.)

3. Use the Edit tool (NOT Write — preserve the rest of the file verbatim) to apply the change to `scripts/suna-insert.sql`. Verify the diff is small and surgical (1-3 lines added).

4. Apply the SAME change to Server5's `platform.apps` row. Single-batched SSH session to Server5:
   ```
   ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@45.137.194.102 'set -e; echo "=== identity ==="; hostname; ip addr show | grep "45.137.194.102" || true; echo "=== current docker_compose for suna ==="; sudo -u postgres psql platform -c "SELECT slug, version FROM apps WHERE slug='\''suna'\'';"; echo "=== writing patched SQL ==="; cat > /tmp/suna-fix.sql << '\''EOF'\''
   <paste the FULL updated suna-insert.sql contents here>
   EOF
   echo "=== applying ==="; sudo -u postgres psql platform -f /tmp/suna-fix.sql; echo "=== verify ==="; sudo -u postgres psql platform -c "SELECT slug, version, docker_compose ~ '\''KORTIX_SANDBOX_URL'\'' AS has_kortix_url FROM apps WHERE slug='\''suna'\'';"; rm -f /tmp/suna-fix.sql'
   ```
   The `has_kortix_url` column MUST return `t` (true). Capture the full output.

5. Redeploy Suna on Mini PC. Two acceptable approaches — pick whichever the user's existing flow supports:
   - **Approach A (clean reinstall):** uninstall Suna via livinityd UI/CLI, then reinstall from the marketplace (which now pulls the patched compose).
   - **Approach B (in-place compose update):** SSH to Mini PC, locate Suna's installed compose dir under `/opt/livos/data/...`, edit the existing compose to add the env var, `docker compose up -d` to recreate kortix-api with the new env. (Faster, but bypasses marketplace verification.)
   
   Default to Approach B for diagnostic speed; document the choice. Single-batched SSH:
   ```
   ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null bruce@10.69.31.68 'set -e; echo "=== identity ==="; hostname; ip addr show | grep "10.69.31.68"; echo "=== locate suna compose ==="; sudo find /opt/livos/data -name "docker-compose*.yml" -path "*suna*" 2>/dev/null | head -5; echo "=== current kortix-api env ==="; sudo docker inspect kortix-api --format "{{range .Config.Env}}{{println .}}{{end}}" 2>/dev/null | grep -i kortix || echo "kortix-api not running"; echo "=== compose update + restart ==="; <suna_compose_dir=$(sudo find /opt/livos/data -name "docker-compose*.yml" -path "*suna*" | head -1)>; sudo sed -i.bak "/ANTHROPIC_API_KEY:/a\      KORTIX_SANDBOX_URL: http://host.docker.internal:14000" "$suna_compose_dir"; sudo docker compose -f "$suna_compose_dir" up -d kortix-api; sleep 5; echo "=== verify env applied ==="; sudo docker inspect kortix-api --format "{{range .Config.Env}}{{println .}}{{end}}" | grep -i kortix; echo "=== container reachability test ==="; sudo docker exec kortix-api curl -sS --max-time 5 http://host.docker.internal:14000/ 2>&1 | head -c 500'
   ```
   
   The "container reachability test" MUST return a non-error response from the sandbox. If empty/error, the fix did not take — STOP and document in evidence file as `failed`, propose Path B fallback in next iteration.

6. Append to `64-SUNA-FIX-EVIDENCE.md`:
   ```markdown
   ## Step 2 — Ship candidate (3)
   
   **Path chosen:** <A | B>
   
   ### Server5 update
   
   **SSH command (single-batched):**
   ```
   <full ssh -c command>
   ```
   
   **Observed output:**
   ```
   <full output including has_kortix_url = t row>
   ```
   
   **Identity check:** Server5 confirmed (45.137.194.102). NOT Server4.
   
   ### scripts/suna-insert.sql diff
   
   ```diff
   <git diff scripts/suna-insert.sql>
   ```
   
   ### Mini PC redeploy
   
   **Approach:** <A clean reinstall | B in-place compose update>
   
   **SSH command (single-batched):**
   ```
   <full ssh -c command>
   ```
   
   **Observed output:**
   ```
   <full output including container env list and reachability test result>
   ```
   
   **Identity check:** Mini PC confirmed (10.69.31.68).
   
   **Scriptable verification:** kortix-api → http://host.docker.internal:14000/ returns: `<excerpt>` → fix applied successfully.
   ```

7. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` again, append to evidence as "Sacred file SHA after Task 2".

8. Stage `scripts/suna-insert.sql` for the next git commit (do NOT commit yet; the user controls the milestone commit cadence per memory `reference_minipc.md` deploy flow).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md';const sql=fs.readFileSync('scripts/suna-insert.sql','utf8');const ev=fs.readFileSync(p,'utf8');const sqlHasFix=/KORTIX_SANDBOX_URL[^\n]*host\.docker\.internal:14000/.test(sql);const hasStep2=/## Step 2/.test(ev);const hasServer5Identity=/45\.137\.194\.102|Server5 confirmed/.test(ev);const hasMiniPCIdentity=/10\.69\.31\.68|Mini PC confirmed/.test(ev);const hasReachOK=/reachability test|fix applied successfully/.test(ev);const noServer4=!/45\.137\.194\.103/.test(ev);const sacredSHA2=(ev.match(/Sacred file SHA/g)||[]).length>=2;if(!sqlHasFix){console.error('scripts/suna-insert.sql missing KORTIX_SANDBOX_URL=http://host.docker.internal:14000');process.exit(1)}if(!hasStep2){console.error('Step 2 missing in evidence');process.exit(1)}if(!hasServer5Identity){console.error('Server5 identity not confirmed');process.exit(1)}if(!hasMiniPCIdentity){console.error('Mini PC identity not confirmed');process.exit(1)}if(!hasReachOK){console.error('Scriptable reachability verification missing');process.exit(1)}if(!noServer4){console.error('Server4 IP appears in evidence — D-NO-SERVER4 violation');process.exit(1)}if(!sacredSHA2){console.error('Sacred file SHA only recorded once; need start + after Task 2');process.exit(1)}console.log('Step 2 ship+deploy+verify OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/suna-insert.sql` contains `KORTIX_SANDBOX_URL: http://host.docker.internal:14000` (or, if Path B, the equivalent network-join change in kortix-sandbox service).
    - Server5 `platform.apps` suna row's `docker_compose` contains the same env var (verified by `psql ... ~ 'KORTIX_SANDBOX_URL'` returning `t`).
    - Mini PC `kortix-api` container has the env var visible in `docker inspect` Config.Env output.
    - Scriptable container-reachability test (`curl http://host.docker.internal:14000/` from inside kortix-api) returned a non-error response — captured in evidence.
    - 64-SUNA-FIX-EVIDENCE.md contains Step 2 section with Server5 SSH evidence, SQL diff, Mini PC SSH evidence, and reachability verdict.
    - Mini PC and Server5 identity confirmations present (10.69.31.68 / 45.137.194.102); NO Server4 IP (45.137.194.103) anywhere in commands or output.
    - Sacred file SHA recorded at start AND after Task 2; values equal.
    - No edits to broker source code, livinityd, or nexus modules.
  </acceptance_criteria>
  <done>Server5 apps row patched; SQL template committed-ready; Mini PC redeployed; container can reach sandbox via host.docker.internal; sacred file unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Document and run the "Navigate to google.com" smoke test (needs-human-walk)</name>
  <what-built>
    Suna sandbox network fix shipped (Server5 `platform.apps` row updated, Mini PC `kortix-api` redeployed with `KORTIX_SANDBOX_URL=http://host.docker.internal:14000`). Container-level reachability scriptably verified. Now needs the real-browser smoke test through the Suna UI.
    
    Per D-03 and D-05, this is a `needs-human-walk` UAT — it CANNOT be automated. Per `feedback_milestone_uat_gate.md`, we never silently treat human-walk as `passed`.
  </what-built>
  <how-to-verify>
1. Open a browser on the user's local machine (not the Mini PC headless).
2. Visit `https://suna.bruce.livinity.io/` (or whichever subdomain the user's Mini PC has Suna installed at — look at the Mini PC's app gateway config or the user's Suna install URL).
3. Log in as `bruce` (the user's Mini PC primary user) using the existing Suna credentials.
4. Open a new agent / chat thread.
5. Type the prompt: **"Navigate to google.com and tell me what you see."**
6. Submit and watch the agent thread:
   - Expected: agent invokes a browser-tool / sandbox-tool, the kortix-sandbox container loads google.com, returns a screenshot or page summary, agent replies with a description.
   - Failure modes to flag:
     a. "Cannot reach sandbox" / DNS error in agent log → fix did not propagate; redeploy needed
     b. Sandbox loads but agent never gets the result → broker streaming issue (NOT this plan's scope; defer to F2/F3 carryover P74)
     c. Agent gets the result but it is empty / blank → sandbox functional issue (escalate; out of P64 scope)
7. Reply with one of:
   - `passed` + brief description of what the agent returned (screenshot text, page summary)
   - `failed: <symptom>` + which failure mode (a/b/c above) + screenshot of the agent thread
   - `partial: <symptom>` + describe what worked vs didn't

After receiving the result, append a Step 3 section to `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-SUNA-FIX-EVIDENCE.md`:

```markdown
## Step 3 — Smoke test ("Navigate to google.com" via Suna UI)

**Status (per D-05 hard rule):** needs-human-walk — completed by user on <date>

**Manual steps (executed by user):**
1. Visited https://suna.bruce.livinity.io/
2. Logged in as bruce
3. New agent thread
4. Prompt: "Navigate to google.com and tell me what you see."

**User-reported result:** <passed | failed | partial>

**Description:** <what the user saw>

**Disposition:**
- If passed → Phase 64 success criterion #1 satisfied; CARRY-01 closed.
- If failed → CARRY-01 stays open; sub-task created for further investigation; Path B/C re-evaluation triggered.
```
  </how-to-verify>
  <resume-signal>
    Reply with `passed` / `failed: <symptom>` / `partial: <symptom>` AND optionally a screenshot. The agent will then write Step 3 to the evidence file with whatever you reported, and either close CARRY-01 or document the open thread to the next sub-task.
  </resume-signal>
</task>

</tasks>

<verification>
- 64-SUNA-FIX-EVIDENCE.md exists with Step 1 (diagnosis), Step 2 (ship+deploy+scriptable verify), and Step 3 (human-walk smoke test result)
- scripts/suna-insert.sql contains the KORTIX_SANDBOX_URL env override
- Server5 platform.apps suna row has the env override visible in docker_compose
- Mini PC kortix-api container has the env applied + scriptably reaches sandbox via host.docker.internal
- Smoke test result honestly recorded (passed/failed/partial) — not silently elevated
- D-NO-SERVER4 honored: no Server4 IPs anywhere
- Sacred file SHA unchanged
- No code modified outside `scripts/suna-insert.sql` and the evidence MD file
</verification>

<success_criteria>
- ROADMAP Phase 64 success criterion #1 satisfied (or honestly deferred): "Navigate to google.com" smoke test result documented
- CARRY-01 closed (passed) or remains open with concrete next-step (failed/partial)
- Phase 64 verification table can cite this evidence file
</success_criteria>

<output>
After completion, create `.planning/phases/64-v30-5-final-cleanup-at-v31-entry/64-04-SUMMARY.md` summarizing: which fix path was chosen (A/B/C), Server5 SQL update outcome, Mini PC redeploy outcome, scriptable verification result, and the user's smoke test verdict. If smoke test failed, list the next-step sub-task.
</output>
