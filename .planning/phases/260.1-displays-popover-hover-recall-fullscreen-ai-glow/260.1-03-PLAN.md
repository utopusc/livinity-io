---
phase: 260.1-displays-popover-hover-recall-fullscreen-ai-glow
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - livos/packages/ui/src/modules/desktop/top-bar.tsx
  - livos/packages/ui/src/modules/desktop/top-bar.test.tsx
autonomous: true
requirements: [SC-A, SC-C]
must_haves:
  truths:
    - "The Displays popover opens on HOVER (mouse enter button or popover), not click"
    - "The popover stays open while the cursor is over the button OR the popover, and closes on leave after a small grace delay"
    - "On drop over the Displays button the button plays an intake animation (spring scale-pop / ring flash)"
    - "After a drag ends the navbar returns to its collapsed state (no wedged pill — 260-03 behavior preserved)"
  artifacts:
    - path: "livos/packages/ui/src/modules/desktop/top-bar.tsx"
      provides: "hover-driven displaysOpen + grace delay + Displays-button intake animation"
      contains: "onMouseEnter"
  key_links:
    - from: "Displays button + PopoverContent onMouseEnter/onMouseLeave"
      to: "displaysOpen state (with grace-delay timer)"
      via: "hover intent, not onClick"
      pattern: "onMouseEnter"
    - from: "onWindowDragDrop (drop inside button)"
      to: "intake animation trigger"
      via: "a controls.start / state flag that fires the spring scale-pop on the Monitor button"
      pattern: "intake|controls\\.start|setIntake"
---

<objective>
SC-A + SC-C (trigger half): make the Displays popover open on HOVER (with a small grace delay so the
cursor can travel from the button to the popover without it snapping shut), and add an "intake" animation
to the Displays button that fires on drop (spring scale-pop + ring flash) confirming it absorbed the
docked window. Preserve the 260-03 navbar-collapse-on-drag-end behavior (no wedged pill) and the existing
drop→pinWindowToTopBar keep-alive gesture.

Purpose: the hover-reveal is locked decision #3; the intake animation is the SC-A "button confirms it
absorbed the window" beat. The actual side-by-side popover layout + per-card affordances are Plan 05.
Output: a hover-controlled, intake-animated Displays button in top-bar.tsx.

This plan ONLY changes top-bar.tsx (+ its test). It must NOT undo the 260-03 collapse fix or the
260-04 badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/CONTEXT.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-03-SUMMARY.md

<interfaces>
<!-- top-bar.tsx current state to build on (DO NOT regress): -->
<!-- displaysOpen state :87 — <Popover open={displaysOpen} onOpenChange={setDisplaysOpen}> :360 -->
<!-- the Monitor button :370-402 has ref={dropZoneRef}, isDragOverShelf ring :377, the badge AnimatePresence :387-401 -->
<!-- isExpanded = dragState.isDragging || isHoverExpanded :142  (the wedge term is ALREADY removed — keep it removed) -->
<!-- onWindowDragDrop subscriber :182-193 — drop INSIDE dropZoneRef rect → windowManager.pinWindowToTopBar (keep-alive). -->
<!-- isDragOverShelf :143 already tracks hover-during-drag over the button. -->

<!-- Available springs (CONTEXT — match these): badge spring {stiffness:500,damping:28}; -->
<!-- pin morph {stiffness:220,damping:26,mass:0.7}; width tween ease [0.16,1,0.3,1] 0.55s; whileHover translateY -6. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hover-open the Displays popover with a grace delay</name>
  <read_first>
    - livos/packages/ui/src/modules/desktop/top-bar.tsx (displaysOpen :87; the Popover/PopoverTrigger/PopoverContent block :360-407; the Monitor button :370-402)
    - livos/packages/ui/src/modules/desktop/top-bar.test.tsx (the source-text-invariant harness style used by 260-03/260-04)
  </read_first>
  <action>
    In top-bar.tsx, drive `displaysOpen` from HOVER intent instead of Radix's click trigger:
    - Add a grace-delay timer ref: `const displaysHoverTimer = useRef<ReturnType<typeof setTimeout> | null>(null)`.
    - Add `openDisplays()` (clears any pending close timer, `setDisplaysOpen(true)`) and `scheduleCloseDisplays()`
      (sets a ~140ms timeout that calls `setDisplaysOpen(false)`; store it in the ref so a re-enter cancels it).
    - On the Monitor `<button>` add `onMouseEnter={openDisplays}` and `onMouseLeave={scheduleCloseDisplays}`.
      Keep the existing `onClick` behavior harmless (the button may still toggle, but hover is the primary path).
    - On the Radix `<PopoverContent>` add `onMouseEnter={openDisplays}` and `onMouseLeave={scheduleCloseDisplays}`
      so the popover stays open while the cursor is over IT (the grace delay covers the gap between button and content).
    - Keep `<Popover open={displaysOpen} onOpenChange={setDisplaysOpen}>` controlled (so Radix still closes on
      Escape / outside-interaction). Clear the timer on unmount (`useEffect` cleanup).
    Radix PopoverContent renders in a portal; the grace delay (~140ms) is what bridges the button→content
    gap — do NOT rely on DOM adjacency. Use 140ms (snappy but forgiving), matching the navbar's brisk feel.
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/desktop/top-bar.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep `onMouseEnter` AND `onMouseLeave` on BOTH the Monitor button and the PopoverContent in top-bar.tsx.
    - grep a grace-delay timer (`setTimeout` with ~140 + a ref to clear it) driving `setDisplaysOpen(false)`.
    - `setDisplaysOpen(true)` is reachable from a mouse-enter path (grep `openDisplays` or equivalent).
    - `isExpanded = dragState.isDragging || isHoverExpanded` still present WITHOUT a `pinnedWindows.length > 0` term (260-03 not regressed).
  </acceptance_criteria>
  <done>Hovering the Displays button opens the popover; moving onto the popover keeps it open; leaving both closes it after ~140ms; controlled-open + Escape still work.</done>
</task>

<task type="auto">
  <name>Task 2: Displays-button intake animation on drop + regression tests</name>
  <read_first>
    - livos/packages/ui/src/modules/desktop/top-bar.tsx (onWindowDragDrop subscriber :182-193; the Monitor button motion + isDragOverShelf ring :370-402; the existing AnimatePresence badge spring :394 as the spring family to match)
    - livos/packages/ui/src/modules/desktop/top-bar.test.tsx (invariant test style)
    - livos/packages/ui/src/modules/window/window.tsx (the EXISTING pin morph :208-246 — `pinAnimateContent` sets `left: pinTargetX, top: pinTargetY` where `pinTargetX/Y` come from `getDisplaysButtonRect()`; this is the SC-A "window slides RIGHT INTO the Displays button" beat ALREADY implemented in 260-03. This plan does NOT modify window.tsx; it adds the button-side intake pop that confirms the slide. VERIFY the morph still targets the button rect.)
    - livos/packages/ui/src/providers/window-drag-state.ts (getDisplaysButtonRect/setDisplaysButtonRect — the channel feeding pinTargetX/Y; top-bar publishes the button rect here)
  </read_first>
  <action>
    Make the Monitor button a `motion.button` (it is currently a plain `<button>`; wrap/convert preserving
    `ref={dropZoneRef}`, `aria-label`, `title`, the className with `isDragOverShelf` ring, and the children
    badge AnimatePresence). Add an intake "pop" using Framer's `useAnimationControls`:
    - `const intakeControls = useAnimationControls()`.
    - In the `onWindowDragDrop` subscriber, AFTER the `inside` branch calls
      `windowManager?.pinWindowToTopBar(event.windowId)`, also fire the intake:
      `void intakeControls.start({scale: [1, 1.28, 1]}, {type: 'spring', stiffness: 500, damping: 18, mass: 0.6})`
      (the 500/18 spring is a snappier cousin of the badge's 500/28 — a deliberate "pop"). Add a brief
      ring-flash by toggling a `setIntakeFlash(true)` state that adds a `ring-2 ring-[color:var(--fg)]`
      class for ~350ms then clears (reuse the existing `isDragOverShelf` ring class so the flash reads as
      the same accent).
    - Attach `animate={intakeControls}` to the `motion.button`. Keep `transition` defaults otherwise.
    The 260-03 collapse already returns the navbar to normal on drag end (isExpanded drops to false when
    isDragging ends) — the intake plays on the button AS the navbar collapses, so SC-A's "after intake the
    navbar returns to collapsed" is satisfied by the existing collapse + this pop on the button.

    Then extend top-bar.test.tsx in the established source-text-invariant style with assertions:
    (1) the Monitor button uses `onMouseEnter`/`onMouseLeave` (hover-open, not click-to-open);
    (2) `useAnimationControls` is imported and `intakeControls.start` is called in the drop subscriber;
    (3) the drop subscriber still calls `pinWindowToTopBar` and NEVER `closeWindow`;
    (4) the grace-delay close timer exists.
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/desktop/top-bar.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep `useAnimationControls` import + an `intakeControls` (or named equivalent) `.start({scale: [1, 1.28, 1]` with a `stiffness: 500` spring.
    - The intake `.start(...)` is called inside the `onWindowDragDrop` `inside` branch, AFTER `pinWindowToTopBar`.
    - grep confirms NO `.closeWindow(` call in top-bar.tsx (260-03 keep-alive invariant).
    - The Monitor button is a `motion.button` with `animate={intakeControls}` and retains `ref={dropZoneRef`.
    - New top-bar.test.tsx invariants for hover-open + intake are present and the file passes.
    - SC-A slide beat verified (no code change required): confirm window.tsx `pinAnimateContent` still sets `left: pinTargetX` / `top: pinTargetY` sourced from `getDisplaysButtonRect()` (grep window.tsx for `getDisplaysButtonRect` AND `left: pinTargetX`). If this is intact, the window slides RIGHT into the Displays button on drop and the new intake pop confirms it. If it is NOT intact, STOP and flag — SC-A's slide beat would be broken.
  </acceptance_criteria>
  <done>Dropping a window over the Displays button pins it (keep-alive) AND plays a spring scale-pop + ring-flash intake; navbar collapses as before; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side UI animation + hover state. No data crosses a trust boundary; no new network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260.1-07 | (n/a) | hover-open + intake animation | accept | Purely presentational client state; the drop still routes through the existing pinWindowToTopBar gesture (no new server call, keep-alive preserved). Genuinely low-risk. |
</threat_model>

<verification>
- top-bar.test.tsx green; hover-open + grace delay + intake animation grep-verified.
- 260-03 collapse + keep-alive invariants intact (no closeWindow, no wedge term); 260-04 badge untouched.
</verification>

<success_criteria>
The Displays popover opens on hover with a grace delay; the Displays button plays a spring intake pop +
ring flash on drop; the navbar still collapses on drag end; no regression of the 260-03/260-04 work.
</success_criteria>

<output>
After completion, create `.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/260.1-03-SUMMARY.md`
</output>
