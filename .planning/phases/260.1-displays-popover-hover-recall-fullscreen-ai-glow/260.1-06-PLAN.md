---
phase: 260.1-displays-popover-hover-recall-fullscreen-ai-glow
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified: []
autonomous: false
requirements: [SC-A, SC-B, SC-C, SC-D, SC-E, SC-F]
must_haves:
  truths:
    - "All 260.1 code is deployed to the Mini PC and the new bundle is actually served (PWA SW cleared)"
    - "luse MCP servers re-spawn with the new glow-stamp code (liv-assistant restarted + per-session servers pkilled)"
    - "Operator confirms SC-A..SC-F live with no Phase 259/260 regression"
  artifacts: []
  key_links:
    - from: "git master"
      to: "Mini PC /opt/livos"
      via: "sudo bash /opt/livos/update.sh"
      pattern: "update.sh"
    - from: "luse glow-stamp code (Plan 01)"
      to: "running luse MCP servers"
      via: "systemctl restart liv-assistant THEN pkill -f computer-use/mcp/server.ts"
      pattern: "pkill"
---

<objective>
Deploy phases 260.1-01..05 to the Mini PC and run the operator live verification of SC-A..SC-F. This plan
is NOT autonomous — it ends in a human-verify checkpoint because the success criteria are animation/UX +
live AI-glow behaviors that only the operator can confirm in the browser.

Critical deploy gotchas (bake them in, do NOT skip):
1. PWA service worker caches the bundle — clear it or the operator sees the stale UI ("it didn't change").
2. luse MCP servers run PER agent session via tsx and CACHE old code — after the Plan 01 glow-stamp change,
   `systemctl restart liv-assistant` is NOT enough; also `pkill -f "computer-use/mcp/server.ts"` so fresh
   agent tasks respawn with updateLastInputAt (else SC-F never fires).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to master + deploy to the Mini PC + restart luse</name>
  <files>(deploy-only — no source files modified in this plan)</files>
  <read_first>
    - .planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/CONTEXT.md (Deploy section + the luse pkill gotcha)
  </read_first>
  <action>
    1. Verify the working tree builds: `cd livos && pnpm --filter @livos/config build && pnpm --filter ui build`
       (UI changes — Plans 03/04/05) and the livinityd tsx files typecheck for the touched modules
       (Plans 01/02). Fix any real new errors (pre-existing baseline TS2786 motion/lucide errors are NOT
       blockers — they were present before this phase per 260-03/04 deferred-items).
    2. Commit + push to master (the user has authorized commit/push for this deploy plan):
       `git add -A && git commit -m "feat(260.1): displays popover hover/close/recall/fullscreen + AI glow" && git push origin master`.
       End the commit body with the Co-Authored-By trailer.
    3. SSH to the Mini PC (Tailscale): `ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@100.112.68.1`
       (per CONTEXT; MEMORY also lists bruce@10.69.31.68 — use whichever resolves). Run
       `sudo bash /opt/livos/update.sh` (clones master, rsyncs, pnpm/npm install, builds config+ui+liv,
       restarts services). Watch for the update.sh success line.
    4. luse re-spawn (SC-F): `sudo systemctl restart liv-assistant` THEN
       `sudo pkill -f "computer-use/mcp/server.ts"` so per-session luse MCP servers respawn with the new
       updateLastInputAt glow-stamp code (a service restart alone leaves cached sessions).
    5. Confirm services: `systemctl is-active livos liv-core liv-worker liv-memory liv-assistant`.
  </action>
  <verify>
    <automated>ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@100.112.68.1 "systemctl is-active livos liv-assistant" 2>&1 || echo "verify over SSH manually"</automated>
  </verify>
  <done>260.1 is built, pushed, deployed via update.sh, and luse MCP servers have been re-spawned with the glow-stamp code; master has the 260.1 commit; livos + liv-assistant report active; the pkill ran after the restart.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator live verification of SC-A..SC-F</name>
  <action>
    Pause for the operator to verify the deployed 260.1 Displays UX in the browser per the steps in
    how-to-verify below. Do not proceed until the operator types "approved" or reports issues.
  </action>
  <what-built>
    The full 260.1 Displays UX on the Mini PC: hover-reveal popover (SC-C), side-by-side cards, per-card
    × close that actually tears the display down (SC-B/SC-D), drop→slide-into-button intake animation
    (SC-A), recall-by-drag + browser fullscreen (SC-E), and a live AI/luse activity edge glow (SC-F).
  </what-built>
  <how-to-verify>
    Open the LivOS desktop in the browser. If the UI looks unchanged, FIRST clear the PWA service worker
    (DevTools → Application → Clear site data / Unregister service worker) and reload, then:
    1. HOVER the Displays button (do NOT click) → the popover animates open; displays are side-by-side with
       live thumbnails. Move the cursor onto the popover → it stays open; leave both → it closes after a
       brief grace delay. (SC-C, hover decision)
    2. Each display card has a × (hover-revealed) → click it → the display disappears from the popover, the
       {n} badge decrements, and the stream stops (re-hover to confirm it's gone, not re-appearing). (SC-B, SC-D)
    3. Drag a stream window UP toward the navbar → on drop it slides RIGHT into the Displays button, the
       button plays an intake pop + ring flash, and the navbar returns to its normal collapsed state (no
       wedged pill). (SC-A)
    4. From a card: drag it down onto the desktop → the display recalls as a window. Click a card's
       Fullscreen control (and/or the window chrome fullscreen button) → the stream goes browser-fullscreen. (SC-E)
    5. Run an AI/luse task that acts on a display (e.g. a luse click/type on a :N) → while the agent is
       acting, that display's card edges PULSE-GLOW; when the agent stops for >~3s the glow fades. (SC-F)
    6. Regression: native apps still open/stream (Phase 259), docking still keeps the stream alive and the
       badge/recall from 260 still work (Phase 260). No app stuck "uninstalling"/"restarting".
    Report any deviation (especially: glow never appears → luse pkill may not have taken; close doesn't
    remove the display → check displays.close logs; stale UI → SW not cleared).
  </how-to-verify>
  <verify>Operator confirmation (manual — this is a human-verify checkpoint).</verify>
  <done>Operator has typed "approved" confirming SC-A..SC-F live with no Phase 259/260 regression, or has reported specific issues to address.</done>
  <resume-signal>Type "approved" or describe the issues found per SC.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → Mini PC (SSH/Tailscale) | Deploy over an authenticated SSH key on the private Tailscale network. No new app-level boundary; verification only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260.1-13 | (n/a) | deploy + verify | accept | No code change in this plan; it deploys already-reviewed code (Plans 01-05 carry the security controls) over an authenticated SSH channel and verifies behavior. The display-close/glow authorization is enforced by Plan 02's server-side canAccessDisplay (verified in step 2/5). Low-risk. |
</threat_model>

<verification>
- update.sh green; services active; luse re-spawned.
- Operator confirms SC-A..SC-F live and no Phase 259/260 regression.
</verification>

<success_criteria>
260.1 is deployed and the operator has confirmed all six success criteria live on the Mini PC with the PWA
SW cleared and luse MCP servers re-spawned (glow firing). No regression.
</success_criteria>

<output>
After completion, create `.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/260.1-06-SUMMARY.md`
</output>
