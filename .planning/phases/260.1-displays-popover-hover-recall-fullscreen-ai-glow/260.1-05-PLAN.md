---
phase: 260.1-displays-popover-hover-recall-fullscreen-ai-glow
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - livos/packages/ui/src/modules/desktop/displays-popover.tsx
  - livos/packages/ui/src/modules/desktop/displays-popover.test.tsx
  - livos/packages/ui/src/modules/window/window.tsx
autonomous: true
requirements: [SC-C, SC-D, SC-E, SC-F]
must_haves:
  truths:
    - "Displays are laid out side-by-side (horizontal wrapping row), not the 2-col stacked grid"
    - "Each display card has a × close button wired to windowManager.closeDisplay"
    - "Each display card can be recalled to the desktop by drag, and has a fullscreen button"
    - "A card whose display.last_input_at is within ~3s shows an animated edge glow that fades when idle"
    - "window.tsx passes onFullscreen to WindowChrome calling requestFullscreen on the content element"
  artifacts:
    - path: "livos/packages/ui/src/modules/desktop/displays-popover.tsx"
      provides: "side-by-side cards + × close + drag-recall + fullscreen + last_input_at glow"
      contains: "closeDisplay"
    - path: "livos/packages/ui/src/modules/window/window.tsx"
      provides: "onFullscreen wiring to requestFullscreen on the content ref"
      contains: "requestFullscreen"
  key_links:
    - from: "DisplayCard × button"
      to: "windowManager.closeDisplay (DISPLAY_:N window)"
      via: "open-or-resolve the DISPLAY_ window then closeDisplay, or call displays.close directly"
      pattern: "closeDisplay|displays\\.close"
    - from: "DisplayCard glow"
      to: "display.last_input_at recency (<~3s)"
      via: "Date.now() - Date.parse(last_input_at) < 3000 → pulsing edge glow"
      pattern: "last_input_at"
    - from: "window.tsx onFullscreen"
      to: "ref.current.requestFullscreen()"
      via: "WindowChrome onFullscreen prop"
      pattern: "requestFullscreen"
---

<objective>
SC-C (layout) + SC-D (× close) + SC-E (recall-by-drag + fullscreen) + SC-F (glow UI): redesign the
Displays popover cards into a polished side-by-side row, add a per-card × close wired to
`windowManager.closeDisplay` (Plan 04 → `displays.close` Plan 02), add recall-by-drag to the desktop +
a fullscreen button per card, and render an animated edge glow on a card whose `display.last_input_at`
(Plan 01) is within ~3s. Also wire `onFullscreen` (Plan 04's WindowChrome prop) in window.tsx to call the
browser Fullscreen API on the content element.

Purpose: the operator-facing payoff of the whole phase — close / recall / fullscreen / glow all land here.
Output: the finished Displays popover + the window.tsx fullscreen call.

Match the established animation feel (CONTEXT springs/easings). Keep the live ~2s screenshot thumbnails and
the existing card-click-to-open contract intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/CONTEXT.md
@.planning/phases/260-window-dock-to-displays-stream-recall-ux/260-04-SUMMARY.md

<interfaces>
<!-- displays-popover.tsx today: -->
type DisplayRecord = {display: string; width: number; height: number; running_apps: unknown[]}
// → EXTEND with `last_input_at?: string` (Plan 01 exposes it through displays.list).
// displaysQuery = trpcReact.displays.list.useQuery(undefined, {enabled: open, refetchInterval: 4000}) :39
// Section A grid: <div className='grid grid-cols-2 gap-2'> :58  → change to a side-by-side wrapping flex row.
// DisplayCard :132 — a <button> that on click: windowManager.openWindow(`DISPLAY_${d.display}`, '/', `Display ${d.display}`, '🖥️', undefined, {width, height})  :150
// per-card screenshot: trpcReact.displays.screenshot.useQuery({display}, {enabled: open, refetchInterval: 2000}) :138
// DockedWindowsSection :86 — reuses windowManager.unpinWindowFromTopBar (recall pinned windows; keep).

<!-- Plan 04 added to the WindowManager context: -->
//   closeDisplay: (windowId) => void   // tears down DISPLAY_:N via displays.close + removes the window
// WindowChrome (Plan 04) now accepts onFullscreen?: () => void.

<!-- window.tsx: content motion.div ref={ref} :303; <WindowChrome .../> rendered :291-298. -->

<!-- Available springs/easings (match): badge {stiffness:500,damping:28}; pin morph {220,26,0.7}; -->
<!-- width tween ease [0.16,1,0.3,1] 0.55s; whileHover translateY -6; AnimatePresence mode='popLayout'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Side-by-side layout + × close + last_input_at edge glow on each card</name>
  <read_first>
    - livos/packages/ui/src/modules/desktop/displays-popover.tsx (DisplayRecord type 26; Section A grid 50-64; DisplayCard 132-180; the screenshot poll 138-141)
    - livos/packages/ui/src/providers/window-manager.tsx (closeDisplay from Plan 04; openWindow signature)
    - livos/packages/ui/src/modules/desktop/displays-popover.test.tsx (the source-text-invariant harness)
  </read_first>
  <action>
    In displays-popover.tsx:
    - Extend `DisplayRecord` (:26) with `last_input_at?: string` (Plan 01 surfaces it via displays.list).
    - Widen the popover container so a side-by-side row reads well: change the outer container width from
      `w-[360px]` (:49) to `w-[520px]` (enough for ~2-3 cards abreast) — keep the max-h + scroll.
    - Replace Section A's `<div className='grid grid-cols-2 gap-2'>` (:58) with a wrapping horizontal flex row:
      `<div className='flex flex-wrap gap-2'>` and give each card a fixed basis (e.g. `w-[160px]`) so they
      sit side-by-side and wrap. Convert `DisplayCard`'s root from a plain `<button>` to a
      `motion.div` wrapper containing the existing clickable thumbnail area (so the × and fullscreen controls
      can be siblings, not nested buttons). Apply `whileHover={{translateY: -6}}` (the established hover) to
      the card.
    - Per-card × CLOSE (SC-D): add a small × button absolutely positioned top-right of the card,
      `aria-label='Close display'`, hover-revealed (`opacity-0 group-hover:opacity-100 transition-opacity`,
      add `group` to the card root). On click: `e.stopPropagation()` then close the display. Since
      `closeDisplay` operates on a WINDOW id and a card may not have an open window, the simplest correct
      path is to call the backend directly: add `const closeMutation = trpcReact.displays.close.useMutation()`
      in DisplayCard and `closeMutation.mutate({display: d.display})`, then optimistically rely on the ~4s
      displays.list refetch to drop the card (or call `displaysQuery.refetch()` via a passed callback). Wire
      it so the card disappears: pass an `onClosed` callback from DisplaysPopover that invalidates/refetches
      the list. (Using displays.close directly here also covers the case where no DISPLAY_ window is open.)
    - last_input_at GLOW (SC-F): compute recency in DisplayCard with a 1s ticker so the glow fades live:
      ```ts
      const [tick, setTick] = useState(0)
      useEffect(() => { const id = setInterval(() => setTick((t) => t + 1), 1000); return () => clearInterval(id) }, [])
      const active = !!d.last_input_at && (Date.now() - Date.parse(d.last_input_at)) < 3000
      ```
      When `active`, render an animated edge glow: a `motion.div` overlay (absolute inset-0, rounded to match
      the card, `pointer-events-none`) with an accent ring + pulsing box-shadow:
      `animate={{boxShadow: ['0 0 0 0 rgba(122,162,255,0.0)', '0 0 0 3px rgba(122,162,255,0.55)', '0 0 0 0 rgba(122,162,255,0.0)']}}`
      `transition={{duration: 1.4, repeat: Infinity, ease: 'easeInOut'}}`. Use the LivOS dark accent
      (#7aa2ff — the same night accent the navbar uses in top-bar.tsx:539) for on-brand subtlety. Wrap in
      `AnimatePresence` so it fades out when `active` flips false.
    Keep the existing thumbnail (~2s screenshot poll) + the click-to-open `openWindow(DISPLAY_:N ...)`
    contract intact (the thumbnail area stays the click target).
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/desktop/displays-popover.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep `flex flex-wrap` in displays-popover.tsx (side-by-side) and NO `grid-cols-2` remaining in Section A.
    - grep `last_input_at` on DisplayRecord + the `Date.now() - Date.parse(d.last_input_at)) < 3000` recency check.
    - grep `aria-label='Close display'` AND `displays.close` (DisplayCard close path) + a hover-reveal (`group-hover:opacity-100`).
    - grep a pulsing glow `boxShadow` animation gated on the `active` recency flag, using the `#7aa2ff` / `122,162,255` accent.
    - The card retains the thumbnail screenshot poll + `openWindow(\`DISPLAY_${d.display}\`` click-to-open.
    - displays-popover.test.tsx extended with invariants for side-by-side layout, × close (displays.close), and the last_input_at glow; file passes.
  </acceptance_criteria>
  <done>Cards sit side-by-side with hover-lift, each has a hover-revealed × that closes the display via displays.close, and an active display pulses an on-brand edge glow that fades when idle.</done>
</task>

<task type="auto">
  <name>Task 2: Recall-by-drag to desktop + per-card fullscreen button</name>
  <read_first>
    - livos/packages/ui/src/modules/desktop/displays-popover.tsx (DisplayCard after Task 1; the openWindow call 150; DockedWindowsSection 86-125 for the recall analog)
    - livos/packages/ui/src/providers/window-manager.tsx (openWindow + unpinWindowFromTopBar)
  </read_first>
  <action>
    In displays-popover.tsx DisplayCard:
    - FULLSCREEN button (SC-E): add a second hover-revealed control (lucide `Maximize2`) next to the ×,
      `aria-label='Fullscreen display'`. On click `e.stopPropagation()` then OPEN the display window and
      request browser fullscreen on it. Simplest robust path: call the existing
      `windowManager.openWindow(\`DISPLAY_${d.display}\`, '/', \`Display ${d.display}\`, '🖥️', undefined, {width: d.width, height: d.height})`
      then `requestAnimationFrame(() => document.querySelector('...the just-opened content...')?.requestFullscreen?.())`.
      Because resolving the freshly-opened element by query is fragile, PREFER: open the window (recall), then
      let the user hit the chrome fullscreen button (Plan 04) — i.e. the per-card fullscreen button RECALLS
      the window to the desktop AND the window's own chrome fullscreen button (Plan 04 + Task 3) does the
      browser-fullscreen. If a one-click card→fullscreen is wanted, open then call
      `(el?.requestFullscreen ?? (el as any).webkitRequestFullscreen)?.call(el)` on
      `document.documentElement` is NOT acceptable (must be the stream). Document the chosen behavior in a
      comment. Minimum bar: the card fullscreen button recalls/opens the display window (one click to a live,
      fullscreen-able window).
    - RECALL-BY-DRAG (SC-E): make the card draggable so dragging it onto the desktop recalls it as a window.
      Use Framer Motion `drag` with `dragSnapToOrigin` on the card `motion.div`:
      `drag dragSnapToOrigin dragElastic={0.2}` and `onDragEnd={(e, info) => { if (info.point.y > <popover bottom>) recall() }}`.
      Since the popover floats near the top-right, treat a downward drag of meaningful distance as "to the
      desktop": `if (info.offset.y > 80) { windowManager?.openWindow(\`DISPLAY_${d.display}\`, '/', \`Display ${d.display}\`, '🖥️', undefined, {width: d.width, height: d.height}) }`.
      For a docked (pinned) DISPLAY window prefer `unpinWindowFromTopBar` (reuse the still-mounted stream) —
      but a fresh `openWindow(DISPLAY_:N)` re-attaches the live `:N` stream by the verbatim 254-03 contract,
      so opening is a correct recall for a non-pinned display. Keep a click/recall FALLBACK (the existing
      thumbnail click-to-open already provides it — do not remove it).
      Guard so a drag does NOT also fire the thumbnail click (Framer's drag suppresses the click when a real
      drag occurs; verify the click-to-open still works for a non-drag tap).
    Match the established feel: `dragTransition={{bounceStiffness: 500, bounceDamping: 28}}` (the badge spring
    family) so the snap-back reads native.
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/desktop/displays-popover.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep `aria-label='Fullscreen display'` + a hover-revealed Maximize2 control that opens the DISPLAY_ window.
    - grep `drag` + `dragSnapToOrigin` + `onDragEnd` on the card motion.div with an `info.offset.y > 80` (or similar) recall threshold calling `openWindow(\`DISPLAY_${d.display}\``.
    - The existing thumbnail click-to-open (`openWindow(\`DISPLAY_${d.display}\``) is retained as the fallback.
    - `dragTransition`/bounce uses the established spring family (stiffness ~500).
    - A comment documents the card-fullscreen behavior (recall + chrome fullscreen, or one-click) chosen.
  </acceptance_criteria>
  <done>Each card can be dragged to the desktop to recall the display as a window (with click fallback) and has a fullscreen control; snap-back + feel match the established springs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire onFullscreen in window.tsx to the browser Fullscreen API</name>
  <read_first>
    - livos/packages/ui/src/modules/window/window.tsx (the content motion.div ref={ref} :303; the <WindowChrome .../> render 291-298)
    - livos/packages/ui/src/modules/window/window-chrome.tsx (the onFullscreen prop added in Plan 04)
  </read_first>
  <action>
    In window.tsx, pass `onFullscreen` to `<WindowChrome>` (:291-298). The content element is the
    `ref={ref}` motion.div (:303). Add the handler:
    ```ts
    const handleFullscreen = () => {
      const el = ref.current as (HTMLElement & {webkitRequestFullscreen?: () => void}) | null
      if (!el) return
      // Locked decision #2 — BROWSER fullscreen, works for all display types (native/webapp/luse).
      ;(el.requestFullscreen?.bind(el) ?? el.webkitRequestFullscreen?.bind(el))?.()
    }
    ```
    and `<WindowChrome ... onFullscreen={handleFullscreen} />`. Only pass it for STREAM windows if you want
    to hide it on non-stream windows — but since WindowChrome already gates the button on `onFullscreen`
    being present, you MAY pass it always (then every window can go fullscreen) OR gate it to
    `webappId || nativeAppId || appId.startsWith('DISPLAY_')`. Choose to gate it to stream/display windows
    (the locked decision targets the stream window / VNC canvas) and document the gate in a comment.
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/window 2>&1 | tail -15 || echo "no window tests — grep-verify"</automated>
  </verify>
  <acceptance_criteria>
    - grep `requestFullscreen` in window.tsx with a `webkitRequestFullscreen` fallback, called on the content `ref`.
    - `<WindowChrome` receives `onFullscreen={handleFullscreen}` (grep).
    - The fullscreen gate (stream/display windows only) is present + documented in a comment.
  </acceptance_criteria>
  <done>The window chrome fullscreen button (Plan 04) now actually puts the stream content into browser fullscreen, all display types, via the standards API + webkit fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → displays.close | The per-card × calls the auth+owner-scoped displays.close (Plan 02). Authorization is server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260.1-10 | Elevation / Tampering | per-card × → displays.close | mitigate | The card passes only the `:N` it is displaying; displays.close enforces canAccessDisplay server-side (Plan 02 T-260.1-04), so a card cannot tear down a display the caller isn't authorized for. The card only ever shows displays already returned by the authorized displays.list. |
| T-260.1-11 | Spoofing | last_input_at glow | accept | The glow is purely presentational, computed client-side from the authorized displays.list payload's timestamp; it cannot affect any other session and exposes no new data (the operator already sees the display). Low-risk. |
| T-260.1-12 | (n/a) | requestFullscreen | accept | User-gesture-gated browser API on the operator's own content element. Low-risk. |
</threat_model>

<verification>
- displays-popover.test.tsx green; side-by-side + × close + drag-recall + glow grep-verified.
- window.tsx requestFullscreen wired to the content ref; chrome fullscreen button now functional.
- Live ~2s thumbnails + click-to-open + DockedWindowsSection recall (260-04) all retained.
</verification>

<success_criteria>
The Displays popover lays displays side-by-side with hover-lift, per-card × close (real backend teardown),
recall-by-drag to the desktop + fullscreen, and an on-brand pulsing edge glow on displays the AI acted on
within ~3s; the window chrome fullscreen button puts the stream into browser fullscreen.
</success_criteria>

<output>
After completion, create `.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/260.1-05-SUMMARY.md`
</output>
