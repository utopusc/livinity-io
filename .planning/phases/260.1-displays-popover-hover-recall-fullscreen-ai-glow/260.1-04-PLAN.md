---
phase: 260.1-displays-popover-hover-recall-fullscreen-ai-glow
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - livos/packages/ui/src/providers/window-manager.tsx
  - livos/packages/ui/src/modules/window/window-chrome.tsx
autonomous: true
requirements: [SC-B, SC-E]
must_haves:
  truths:
    - "The window manager exposes a closeDisplay action that calls displays.close and removes the window"
    - "The window chrome renders a fullscreen button (when onFullscreen is provided) for browser fullscreen"
    - "closeDisplay routes through the backend displays.close (not just a UI detach) so the :N actually tears down"
  artifacts:
    - path: "livos/packages/ui/src/providers/window-manager.tsx"
      provides: "closeDisplay action wired to trpc displays.close"
      contains: "closeDisplay"
    - path: "livos/packages/ui/src/modules/window/window-chrome.tsx"
      provides: "fullscreen button + onFullscreen prop (handler supplied by window.tsx in Plan 05)"
      contains: "onFullscreen"
  key_links:
    - from: "windowManager.closeDisplay"
      to: "trpcReact.displays.close mutation"
      via: "tear down :N server-side then drop the window from state"
      pattern: "displays\\.close"
    - from: "window-chrome fullscreen button"
      to: "onFullscreen() (window.tsx requestFullscreen, Plan 05)"
      via: "callback prop into the chrome control row"
      pattern: "onFullscreen"
---

<objective>
SC-B (UI half) + SC-E (fullscreen affordance): add the window-manager-level `closeDisplay` action that
calls the Plan 02 `displays.close` tRPC route (so a display window's × actually tears the `:N` down
server-side, not just a UI detach), and add a fullscreen button to the window chrome wired through a new
`onFullscreen` callback prop (locked decision #2 — browser fullscreen, frontend only, all display types;
the actual requestFullscreen call is supplied by window.tsx in Plan 05).

Purpose: gives Plan 05's popover the closeDisplay verb and gives live windows a fullscreen control.
Output: `closeDisplay` on the WindowManager context + a fullscreen button + `onFullscreen` prop on
WindowChrome.

This plan ONLY touches window-manager.tsx + window-chrome.tsx (disjoint from Plan 03's top-bar.tsx and
Plan 05's window.tsx/displays-popover.tsx within the same wave/next wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/CONTEXT.md

<interfaces>
<!-- window-manager.tsx contract to extend: -->
type WindowState = { id; appId; route; position; size; zIndex; isMinimized; isPinnedToTopBar?; title; icon; originRect? }
type WindowManagerContextT = {
  windows; openWindow; closeWindow; focusWindow; minimizeWindow; restoreWindow;
  updateWindowPosition; updateWindowSize; pinWindowToTopBar; unpinWindowFromTopBar;
  registerCloseHandler; unregisterCloseHandler;
  // ADD: closeDisplay
}
// closeWindow :449-464 invokes the registered close handler then dispatches CLOSE_WINDOW.
// pinWindowToTopBar :486-501 shows the windowsRef.current.find(...) pattern to read state without a dep.
// DISPLAY_ windows have appId `DISPLAY_:N` (openWindow `DISPLAY_${d.display}` — displays-popover.tsx:150).

<!-- window-chrome.tsx (verified): -->
// WindowChromeProps :10-20 = {title; icon?; onClose; windowWidth; webappId?; nativeAppId?}
// Close button block: <Magnetic ...><button onClick onMouseDown stopPropagation className='... w-9 h-9 rounded-full bg-white/95 ... hover:bg-destructive ...' aria-label='Close window'><TbX/></button></Magnetic>  (:98-118)
// The chrome is a SEPARATE positioned motion.div from the content motion.div (window.tsx:302 ref={ref}),
// so the chrome cannot reach the content element via the DOM — use an explicit onFullscreen callback prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add closeDisplay to the window manager (wired to displays.close)</name>
  <read_first>
    - livos/packages/ui/src/providers/window-manager.tsx (WindowManagerContextT 57-71; closeWindow 449-464; pinWindowToTopBar 486-501 for the windowsRef pattern; the context value object 508-528; check whether trpcReact is already imported)
    - the Plan 02 displays.close shape — input {display}, returns {ok, kind}
  </read_first>
  <action>
    In window-manager.tsx:
    - Ensure `import {trpcReact} from '@/trpc/trpc'` (grep; add if missing) and inside the provider add
      `const displaysCloseMutation = trpcReact.displays.close.useMutation()`.
    - Add `closeDisplay: (windowId: WindowId) => void` to `WindowManagerContextT` (:57-71).
    - Implement (mirroring pinWindowToTopBar's windowsRef read):
      ```ts
      const closeDisplay = useCallback((windowId: WindowId) => {
        const w = windowsRef.current.find((x) => x.id === windowId)
        // DISPLAY_ windows carry appId `DISPLAY_:N`; derive the :N for the backend teardown.
        const display = w?.appId.startsWith('DISPLAY_') ? w.appId.slice('DISPLAY_'.length) : undefined
        if (display) {
          // SC-B: server-side teardown (Redis record + stream) so the :N does NOT re-appear in
          // displays.list. Fire-and-forget — the UI must not hang on a slow backend.
          displaysCloseMutation.mutate({display})
        }
        // Drop the window from the UI regardless (also runs any registered close handler).
        const handler = closeHandlersRef.current.get(windowId)
        if (handler) {
          const handlerPromise = Promise.resolve().then(() => handler())
          const timeout = new Promise<void>((resolve) => setTimeout(resolve, 2000))
          void Promise.race([handlerPromise, timeout]).catch(() => undefined)
          closeHandlersRef.current.delete(windowId)
        }
        dispatch({type: 'CLOSE_WINDOW', payload: windowId})
      }, [displaysCloseMutation])
      ```
    - Add `closeDisplay` to the context value object (alongside closeWindow at :513).
    A plain non-display window (no `:N`) simply skips the mutate and runs the normal close path.
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/providers 2>&1 | tail -15 || echo "no provider tests — grep-verify"</automated>
  </verify>
  <acceptance_criteria>
    - grep `closeDisplay` in window-manager.tsx: in the context type, the useCallback impl, AND the context value object.
    - grep `trpcReact.displays.close` AND `displaysCloseMutation.mutate({display`.
    - closeDisplay derives `:N` from `appId.startsWith('DISPLAY_')` and still dispatches CLOSE_WINDOW (and runs the close handler like closeWindow does).
  </acceptance_criteria>
  <done>windowManager.closeDisplay tears the backend display down via displays.close AND removes the window; non-display windows handled gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Fullscreen button + onFullscreen prop on the window chrome</name>
  <read_first>
    - livos/packages/ui/src/modules/window/window-chrome.tsx (WindowChromeProps 10-20; the close button Magnetic block 98-118 — the exact classes/handlers to mirror; the component params 55-62)
    - livos/packages/ui/src/modules/window/app-contents/chrome-content.tsx (:59 — the `allow='...; fullscreen'` that permits requestFullscreen on the iframe content)
  </read_first>
  <action>
    Locked decision #2 = BROWSER fullscreen (frontend only, all display types). The chrome is rendered in a
    SEPARATE positioned motion.div from the window CONTENT motion.div (window.tsx:302, `ref={ref}`), so the
    chrome cannot reach the content element via the DOM tree reliably. Use an explicit optional callback prop
    — DETERMINISTIC, no fork:
    - In WindowChromeProps (window-chrome.tsx:10-20) add `onFullscreen?: () => void` (additive, optional so
      non-stream chrome + existing call sites compile unchanged). Destructure it in the component params (:55-62).
    - Add a `Maximize2` (lucide-react) icon button in the chrome control row, placed RIGHT AFTER the close
      button `<Magnetic>...</Magnetic>` block (:118). Mirror the close button's shape classes for visual
      consistency: `flex shrink-0 items-center justify-center w-9 h-9 rounded-full bg-white/95 dark:bg-zinc-800/95 border border-dash-line dark:border-white/10 shadow-[0_2px_8px_rgba(0,0,0,0.08)] hover:bg-[color:var(--bg-2)] transition-colors duration-200`
      — a NEUTRAL hover (NOT the close button's destructive hover). `aria-label='Fullscreen'`,
      `title='Fullscreen'`, `onClick={(e) => {e.stopPropagation(); onFullscreen?.()}}`,
      `onMouseDown={(e) => e.stopPropagation()}` (same drag-suppression the close button uses).
    - Gate the button on `{onFullscreen && (...)}` so plain non-stream windows don't show it.
    Add a code comment noting the requestFullscreen call lives in window.tsx (Plan 05), which passes
    `onFullscreen` and uses the standards Fullscreen API with a webkit fallback. This plan delivers the
    button + prop; Plan 05 delivers the call (keeps files disjoint).
  </action>
  <verify>
    <automated>cd livos/packages/ui && npx vitest run src/modules/window 2>&1 | tail -15 || echo "no chrome tests — grep-verify"</automated>
  </verify>
  <acceptance_criteria>
    - grep `onFullscreen` in window-chrome.tsx: in WindowChromeProps, the destructured params, AND the button onClick.
    - A `Maximize2` fullscreen button with `aria-label='Fullscreen'` is added right after the close button block, gated on `{onFullscreen && `.
    - The button mirrors the close button's size/shape classes (w-9 h-9 rounded-full) with a NEUTRAL hover (not destructive), and stopPropagation on click+mousedown.
    - A code comment notes window.tsx (Plan 05) supplies the requestFullscreen handler with webkit fallback.
  </acceptance_criteria>
  <done>WindowChrome renders an additive fullscreen button (when onFullscreen provided) that calls the supplied handler; Plan 05 wires the actual requestFullscreen. No title-bar layout regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → displays.close | closeDisplay calls the auth+owner-scoped displays.close (Plan 02). Authorization is enforced server-side; this client action cannot bypass it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260.1-08 | Elevation | closeDisplay → displays.close | mitigate | All authorization is server-side in displays.close (canAccessDisplay, Plan 02 T-260.1-04). The client merely passes the `:N`; it cannot close a display it isn't authorized for. |
| T-260.1-09 | (n/a) | fullscreen button/prop | accept | Browser Fullscreen API is user-gesture gated by the browser; purely presentational, no data crosses a boundary. Low-risk. |
</threat_model>

<verification>
- window-manager.tsx exposes closeDisplay wired to displays.close; window-chrome.tsx has a fullscreen button + onFullscreen prop.
- Provider/window tests green (or grep-verified). No top-bar.tsx or window.tsx change in this plan.
</verification>

<success_criteria>
The window manager can close a display (server teardown + UI removal) and the window chrome renders a
fullscreen button driven by an onFullscreen callback. These are the verbs Plan 05 consumes/wires.
</success_criteria>

<output>
After completion, create `.planning/phases/260.1-displays-popover-hover-recall-fullscreen-ai-glow/260.1-04-SUMMARY.md`
</output>
