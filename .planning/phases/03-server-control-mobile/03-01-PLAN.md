---
phase: 03-server-control-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/server-control/index.tsx
autonomous: true
requirements: [SRV-01, SRV-04]

must_haves:
  truths:
    - "Dashboard overview cards stack vertically on mobile instead of side-by-side overflow"
    - "Tab bar scrolls horizontally on mobile so all 10 tabs are accessible without overflow"
    - "Server stats charts resize to fill mobile viewport width without cropping"
    - "Overview health cards (CPU/RAM/Disk/Temp) stack to 1 column on mobile"
    - "Summary cards (Docker/PM2/Network) already 1-col on mobile -- no regression"
    - "Monitoring charts render at full mobile width"
  artifacts:
    - path: "livos/packages/ui/src/routes/server-control/index.tsx"
      provides: "Responsive dashboard cards, scrollable tab bar, mobile-friendly overview"
  key_links:
    - from: "OverviewTab grid-cols"
      to: "mobile viewport"
      via: "Tailwind responsive breakpoints"
      pattern: "grid-cols-1.*sm:grid-cols-2"
    - from: "TabsList"
      to: "mobile viewport"
      via: "overflow-x-auto scrollable tab bar"
      pattern: "overflow-x-auto"
---

<objective>
Make the Server Control dashboard cards stack vertically on mobile and ensure the 10-tab navigation bar scrolls horizontally instead of overflowing. Fix Overview tab health cards grid to use 1 column on mobile. Ensure monitoring charts fill mobile viewport width.

Purpose: SRV-01 (dashboard cards stack) and SRV-04 (stats/charts resize to mobile width) -- the layout foundation for mobile Server Control.
Output: Responsive dashboard cards, scrollable tab bar, mobile-adapted overview tab.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-control-mobile/03-CONTEXT.md

Key constraint from STATE.md: Desktop UI must NOT be modified -- all mobile changes gated on useIsMobile() or CSS breakpoints.
Established patterns: 44px touch targets, useIsMobile() hook from @/hooks/use-is-mobile, Tailwind responsive breakpoints (sm: md: lg:).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive dashboard cards and scrollable tab bar</name>
  <read_first>
    - livos/packages/ui/src/routes/server-control/index.tsx lines 3296-3350 (main component return: header, resource cards, tab bar)
    - livos/packages/ui/src/routes/server-control/index.tsx lines 74-169 (ResourceCard component)
    - livos/packages/ui/src/hooks/use-is-mobile.ts (useIsMobile hook)
  </read_first>
  <files>livos/packages/ui/src/routes/server-control/index.tsx</files>
  <action>
CRITICAL: Desktop layout must remain identical. All changes use Tailwind responsive prefixes or useIsMobile() gating.

1. **Resource Cards grid (line ~3305):**
   Current: `grid grid-cols-1 gap-4 px-6 pb-4 sm:grid-cols-3`
   Change to: `grid grid-cols-1 gap-3 px-4 pb-3 sm:grid-cols-3 sm:gap-4 sm:px-6 sm:pb-4`
   - Already 1-col on mobile, but reduce padding from px-6 to px-4 on mobile to gain 16px width.
   - Reduce gap from gap-4 to gap-3 on mobile to save vertical space.

2. **Header (line ~3299-3301):**
   Current: `shrink-0 px-6 pt-5 pb-4`
   Change to: `shrink-0 px-4 pt-4 pb-3 sm:px-6 sm:pt-5 sm:pb-4`
   - Reduce mobile heading from text-2xl to text-xl on mobile: `text-xl sm:text-2xl`

3. **Tab bar (line ~3334-3346):**
   The TabsList currently shows 10 tabs in a row. On mobile this overflows.
   Wrap the TabsList in a container with horizontal scroll:
   - Change the Tabs className: keep `flex min-h-0 flex-1 flex-col px-4 pb-3 sm:px-6 sm:pb-4` (reduce mobile padding)
   - Wrap TabsList with: `<div className='shrink-0 overflow-x-auto scrollbar-none -mx-4 px-4 sm:-mx-0 sm:px-0'>`
   - Add to TabsList: `className='shrink-0 w-max justify-start gap-1 bg-transparent p-0 sm:w-full'`
   - The `w-max` ensures the tabs don't compress on mobile and instead scroll.
   - `scrollbar-none` hides the scrollbar (Tailwind 3.4 utility). If not available, add inline style `{scrollbarWidth: 'none', msOverflowStyle: 'none'}` and a `[&::-webkit-scrollbar]:hidden` class.

4. **Bulk action bar (line ~3604-3661):**
   Current: All 5 buttons shown inline.
   On mobile the bar can overflow. Wrap the buttons container:
   - Change outer `fixed bottom-6 left-1/2 z-50 -translate-x-1/2` to `fixed bottom-4 left-3 right-3 z-50 sm:left-1/2 sm:right-auto sm:-translate-x-1/2 sm:bottom-6`
   - Change inner flex: `flex flex-wrap items-center gap-2 rounded-xl border border-border-default bg-surface-base px-3 py-2 shadow-lg sm:flex-nowrap sm:gap-3 sm:px-4 sm:py-2.5`
   - The `flex-wrap` allows buttons to wrap on very narrow screens.
   - Each bulk action button: add `text-[11px] sm:text-xs` for slightly smaller text on mobile.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && npx tsc --noEmit --project livos/packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "grid-cols-1 gap-3 px-4" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
    - grep -c "overflow-x-auto" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
    - grep -c "w-max" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
    - grep -c "sm:grid-cols-3" livos/packages/ui/src/routes/server-control/index.tsx returns >= 2 (resource cards + overview)
    - grep -c "text-xl sm:text-2xl" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
  </acceptance_criteria>
  <done>Dashboard header has reduced mobile padding (px-4 pt-4) with desktop unchanged (sm:px-6 sm:pt-5). Resource cards grid uses gap-3 px-4 on mobile. Tab bar scrolls horizontally via overflow-x-auto wrapper with w-max TabsList. Bulk action bar uses full-width on mobile (left-3 right-3) with flex-wrap.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Overview tab health cards and monitoring charts</name>
  <read_first>
    - livos/packages/ui/src/routes/server-control/index.tsx lines 514-702 (OverviewTab: health cards grid, summary cards, engine info)
    - livos/packages/ui/src/routes/server-control/index.tsx lines 706-900 (MonitoringTab: network/disk charts, process table)
    - livos/packages/ui/src/routes/server-control/index.tsx lines 327 (EngineInfoSection grid-cols)
  </read_first>
  <files>livos/packages/ui/src/routes/server-control/index.tsx</files>
  <action>
CRITICAL: Desktop layout must remain identical. All changes use Tailwind responsive prefixes.

1. **OverviewTab health cards (line ~517):**
   Current: `grid grid-cols-2 gap-4 lg:grid-cols-4`
   Change to: `grid grid-cols-1 gap-3 sm:grid-cols-2 sm:gap-4 lg:grid-cols-4`
   - On mobile (<640px): 1 column, stacked vertically (SRV-01).
   - On sm (640-1023px): 2 columns.
   - On lg (1024px+): 4 columns (unchanged desktop).

2. **OverviewTab health card padding (lines ~519, 558, 599, 615):**
   Each card uses `p-4`. Change to `p-3 sm:p-4` to save space on mobile.

3. **OverviewTab summary cards row (line ~630):**
   Current: `grid grid-cols-1 gap-4 sm:grid-cols-3`
   Already responsive but add: `gap-3 sm:gap-4` for tighter mobile spacing.

4. **OverviewTab summary card padding (lines ~632, 653, 674):**
   Each uses `p-4`. Change to `p-3 sm:p-4`.

5. **OverviewTab overall container (line ~515):**
   Current: `space-y-4 p-4`
   Change to: `space-y-3 p-3 sm:space-y-4 sm:p-4`

6. **EngineInfoSection grid (line ~327):**
   Current: `grid grid-cols-2 gap-x-6 gap-y-2 ... sm:grid-cols-3 lg:grid-cols-4`
   Already responsive. Reduce mobile gap: `gap-x-4 sm:gap-x-6` to prevent overflow on narrow screens.

7. **MonitoringTab (line ~721):**
   Current: `space-y-6 p-4`
   Change to: `space-y-4 p-3 sm:space-y-6 sm:p-4`

8. **MonitoringTab charts section padding (lines ~723, 784):**
   Current: `p-4`. Change to `p-3 sm:p-4`.

9. **MonitoringTab process table:**
   Current table has 5 columns (PID, Name, CPU%, Mem%, State).
   This is narrow enough for mobile. Just ensure the container wraps with `overflow-x-auto`:
   - Around the process Table, wrap: `<div className='overflow-x-auto'>`

10. **Events tab (line ~370):**
    Current filter row: `flex items-center gap-3` with two selects and a button -- this overflows on mobile.
    Change to: `flex flex-wrap items-center gap-2 sm:gap-3`
    Also reduce the first Select width: `w-[120px] sm:w-[140px]`
    And second Select: `w-[100px] sm:w-[120px]`
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io && npx tsc --noEmit --project livos/packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "grid-cols-1 gap-3 sm:grid-cols-2 sm:gap-4 lg:grid-cols-4" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
    - grep -c "p-3 sm:p-4" livos/packages/ui/src/routes/server-control/index.tsx returns >= 4
    - grep -c "space-y-3 p-3 sm:space-y-4 sm:p-4" livos/packages/ui/src/routes/server-control/index.tsx returns >= 1
    - grep -c "flex-wrap" livos/packages/ui/src/routes/server-control/index.tsx returns >= 2
  </acceptance_criteria>
  <done>Overview health cards use grid-cols-1 on mobile (stacking vertically), grid-cols-2 on sm, grid-cols-4 on lg. All card padding reduced on mobile (p-3 vs p-4). Monitoring charts have tighter mobile padding. Events filter row wraps on mobile. Process table has overflow-x-auto wrapper.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit --project livos/packages/ui/tsconfig.json`
2. Vite dev server starts: `cd livos && pnpm dev` (smoke test)
3. grep verifications for all responsive classes
4. Desktop layout unchanged: no hardcoded pixel values removed, all changes add mobile-first defaults with sm: desktop overrides
</verification>

<success_criteria>
- Dashboard overview cards stack to 1 column on mobile screens (< 640px)
- Tab bar scrolls horizontally on mobile, all 10 tabs accessible
- Charts resize properly within mobile containers (ResponsiveContainer already handles this)
- Bulk action bar fits mobile width without overflow
- Events filter row wraps on mobile instead of overflowing
- Desktop layout completely unchanged (all changes are mobile-first with sm:/lg: breakpoints)
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-control-mobile/03-01-SUMMARY.md`
</output>
