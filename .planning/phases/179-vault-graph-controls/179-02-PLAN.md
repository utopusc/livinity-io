---
phase: 179-vault-graph-controls
plan: 02
type: tdd
wave: 2
depends_on:
  - 179-01
files_modified:
  - livos/packages/ui/src/features/vault-graph/GraphControls.tsx
  - livos/packages/ui/src/features/vault-graph/GraphControls.test.tsx
  - livos/packages/ui/src/features/vault-graph/sections/FiltersSection.tsx
  - livos/packages/ui/src/features/vault-graph/sections/FiltersSection.test.tsx
autonomous: true
requirements:
  - VAULT-GRAPH-179-02
must_haves:
  truths:
    - "A collapsible floating card appears at the right edge of the vault graph canvas"
    - "When collapsed, a small chip (icon + 'Controls' label) is visible at right-center"
    - "When expanded, the card shows a Filters section with type-toggle checkboxes (7 types)"
    - "Filters state (open types, toggles, excluded paths) persists to localStorage key liv:vault-graph:settings:filters"
    - "localStorage key is namespaced by userId when currentUser is available"
  artifacts:
    - path: "livos/packages/ui/src/features/vault-graph/GraphControls.tsx"
      provides: "Collapsible right-edge floating card shell — renders children sections"
      exports: ["GraphControls", "useControlsOpen"]
    - path: "livos/packages/ui/src/features/vault-graph/sections/FiltersSection.tsx"
      provides: "Type toggles + orphan/recent/ghost toggles + excluded-paths textarea"
      exports: ["FiltersSection", "defaultFilters", "FiltersState"]
  key_links:
    - from: "GraphControls.tsx"
      to: "FiltersSection.tsx"
      via: "renders <FiltersSection> as first child section"
      pattern: "FiltersSection"
    - from: "FiltersSection.tsx"
      to: "localStorage"
      via: "reads/writes liv:vault-graph:settings:filters on change"
      pattern: "localStorage\\.setItem.*liv:vault-graph:settings:filters"
---

<objective>
Build the `<GraphControls>` collapsible shell and the `<FiltersSection>` as the first populated section inside it.

Purpose: The Controls panel is the Phase 179 anchor — all subsequent sections (Groups / Display / Forces) slot into this shell. FiltersSection is the highest-priority section (node visibility filtering is the main user need).

Output:
- `GraphControls.tsx` — right-edge floating card, collapses to a chip
- `sections/FiltersSection.tsx` — 7 type checkboxes + 3 boolean toggles + excluded-paths textarea
- localStorage persistence under `liv:vault-graph:settings:filters`
- 10 vitest assertions (5 GraphControls + 5 FiltersSection)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/179-vault-graph-controls/179-CONTEXT.md
@.planning/phases/179-vault-graph-controls/179-01-SUMMARY.md

<interfaces>
<!-- Livinity DS tokens used by existing vault-graph components: -->
<!-- bg-[color:var(--bg-2)], border-[color:var(--line-strong)], text-[color:var(--fg)], text-[color:var(--fg-mute)] -->
<!-- rounded-[var(--r-lg)], shadow-pop (from Phase 178 DS) -->
<!-- GraphSearchBar uses: absolute positioning, z-30, font-mono text-[13px] -->

<!-- GraphNode types (from 179-01 — must match exactly): -->
```typescript
type VaultNodeType = 'memory' | 'session' | 'inbox' | 'agent' | 'skill' | 'command' | 'root'
```

<!-- localStorage contract (new in this plan): -->
```typescript
const FILTERS_KEY = (userId?: string) =>
  userId ? `liv:vault-graph:settings:filters:${userId}` : 'liv:vault-graph:settings:filters'

export interface FiltersState {
  enabledTypes: VaultNodeType[]       // defaults to all 7
  showOrphans: boolean                // default true
  showRecent: boolean                 // default false (filter to files modified <7d)
  showGhosts: boolean                 // default true (ghost = wikilink target that doesn't exist as file)
  excludedPaths: string               // newline-separated glob patterns, default ''
}

export const defaultFilters: FiltersState = {
  enabledTypes: ['memory','session','inbox','agent','skill','command','root'],
  showOrphans: true,
  showRecent: false,
  showGhosts: true,
  excludedPaths: '',
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for GraphControls + FiltersSection</name>
  <files>
    livos/packages/ui/src/features/vault-graph/GraphControls.test.tsx
    livos/packages/ui/src/features/vault-graph/sections/FiltersSection.test.tsx
  </files>
  <behavior>
    GraphControls.test.tsx — 5 assertions:
    1. Renders collapsed chip by default — data-testid="controls-chip" visible, data-testid="controls-panel" absent
    2. Clicking chip opens panel — data-testid="controls-panel" becomes visible
    3. Clicking close button inside panel collapses back — chip visible again
    4. Panel renders its children prop when open
    5. data-testid="controls-chip" has aria-label="Open graph controls"

    FiltersSection.test.tsx — 5 assertions:
    1. Renders 7 type checkboxes (data-testid="type-toggle-{type}" for each of the 7 VaultNodeType values)
    2. Clicking a type checkbox calls onFiltersChange with that type removed from enabledTypes
    3. Orphans toggle (data-testid="toggle-orphans") changes showOrphans boolean
    4. Excluded-paths textarea (data-testid="excluded-paths") fires onChange with new value
    5. Component initializes state from props.initialFilters (not hardcoded defaultFilters)

    Mock pattern: vi.mock('localStorage') is NOT needed — use real jsdom localStorage.
    Use @testing-library/react render + fireEvent.
    Run tests — MUST FAIL (red) since files don't exist yet.
    Commit: test(179-02): add failing tests for GraphControls + FiltersSection
  </behavior>
  <action>
    Create the two test files only. The sections/ subdirectory may not exist yet — create it.
    Do NOT create GraphControls.tsx or FiltersSection.tsx yet.

    For localStorage assertions in FiltersSection tests, use `window.localStorage.getItem(...)` — jsdom provides a real implementation.

    Note: setReactInputValue helper from 178-04 pattern may be needed for controlled textarea. Add it inline in the test file if needed.
  </action>
  <verify>
    <automated>cd livos && pnpm vitest run packages/ui/src/features/vault-graph/GraphControls.test.tsx packages/ui/src/features/vault-graph/sections/FiltersSection.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, tests fail with "Cannot find module" or similar — RED state confirmed.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement GraphControls + FiltersSection</name>
  <files>
    livos/packages/ui/src/features/vault-graph/GraphControls.tsx
    livos/packages/ui/src/features/vault-graph/sections/FiltersSection.tsx
  </files>
  <behavior>
    GraphControls.tsx:
    - `useState(false)` for isOpen
    - Collapsed state: `<button data-testid="controls-chip" aria-label="Open graph controls" onClick={() => setIsOpen(true)}>`
      Positioned: `absolute right-4 top-1/2 -translate-y-1/2 z-20`
      Styling: `flex items-center gap-1.5 rounded-[var(--r-lg)] border border-[color:var(--line-strong)] bg-[color:var(--bg-2)] px-3 py-1.5 text-sm text-[color:var(--fg)]`
    - Expanded state: `<div data-testid="controls-panel" className="absolute right-4 top-16 z-20 w-72 rounded-[var(--r-lg)] border border-[color:var(--line-strong)] bg-[color:var(--bg-2)] shadow-pop">`
      Include a close button `data-testid="controls-close"` that calls `setIsOpen(false)`
      Render `{children}` inside
    - Export: `export function GraphControls({ children }: { children?: React.ReactNode })`

    FiltersSection.tsx:
    - Props: `{ initialFilters?: Partial<FiltersState>; userId?: string; onFiltersChange?: (f: FiltersState) => void }`
    - State: `useState<FiltersState>({ ...defaultFilters, ...initialFilters })`
    - On any change: call `onFiltersChange?.(newState)` AND write to localStorage:
      `localStorage.setItem(FILTERS_KEY(userId), JSON.stringify(newState))`
    - Render 7 type checkboxes: `<input type="checkbox" data-testid={"type-toggle-" + type} checked={state.enabledTypes.includes(type)} onChange={...}`
    - Toggles: `<input type="checkbox" data-testid="toggle-orphans" ...>`, `data-testid="toggle-recent"`, `data-testid="toggle-ghosts"`
    - Textarea: `<textarea data-testid="excluded-paths" value={state.excludedPaths} onChange={e => update({excludedPaths: e.target.value})} />`
    - Excluded paths update: parse integers from count is not applicable here — textarea value is a string stored as-is (newline-separated glob patterns). No parseInt needed.

    Threat T-179-02-A: Tag names / path patterns displayed via React controlled inputs (value prop) — React default escaping prevents XSS.
    Threat T-179-02-B: localStorage key includes userId suffix — mitigates cross-user settings leak in shared-browser scenarios.

    After implementing, run tests to reach GREEN.
    Commit: feat(179-02): add GraphControls shell + FiltersSection with localStorage persistence
  </behavior>
  <action>
    Create GraphControls.tsx and sections/FiltersSection.tsx with exact data-testid attributes specified. Export `FiltersState`, `defaultFilters`, `FILTERS_KEY` from FiltersSection.tsx so downstream plans can import them.

    Use Livinity DS tokens throughout — no literal hex colors. Class pattern matches existing GraphSearchBar.tsx style (bg-[color:var(--bg-2)] border-[color:var(--line-strong)] etc.).
  </action>
  <verify>
    <automated>cd livos && pnpm vitest run packages/ui/src/features/vault-graph/GraphControls.test.tsx packages/ui/src/features/vault-graph/sections/FiltersSection.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 assertions PASS (5 GraphControls + 5 FiltersSection). Pre-existing vault-graph tests unaffected. Commits tagged 179-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → excludedPaths textarea | Free text; stored in localStorage and later used as glob patterns server-side (Phase 180+) |
| userId → localStorage key suffix | Controls namespace isolation between users on shared browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-02-A | Tampering | FiltersSection excludedPaths | mitigate | Value stored as string, never eval'd; future server-side use must anchor regex to vault root (not in scope here — noted for Phase 180 consumer) |
| T-179-02-B | Info Disclosure | localStorage namespace | mitigate | Key includes userId suffix so settings don't leak between accounts on same browser |
| T-179-02-C | Spoofing | Controls panel z-index | accept | z-20 layer is UI-only; no auth surface |
</threat_model>

<verification>
```bash
cd livos && pnpm vitest run packages/ui/src/features/vault-graph/
```
All vault-graph UI tests PASS including 10 new assertions. Zero regressions.
</verification>

<success_criteria>
- `GraphControls.tsx` exists with collapsible chip + panel behavior
- `sections/FiltersSection.tsx` exists with 7 type checkboxes + 3 bool toggles + excluded-paths textarea
- localStorage key pattern `liv:vault-graph:settings:filters` (+ optional `:userId` suffix)
- All 10 new vitest assertions PASS
- Livinity DS tokens used throughout (no literal hex)
- Sacred guards: sdk-agent-runner.ts + Phase 169 backend files UNTOUCHED
</success_criteria>

<output>
After completion, create `.planning/phases/179-vault-graph-controls/179-02-SUMMARY.md`
</output>
