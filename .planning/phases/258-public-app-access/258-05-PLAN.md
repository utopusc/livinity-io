---
phase: 258-public-app-access
plan: 05
type: execute
wave: 4
depends_on: ['258-01', '258-02', '258-03', '258-04']
files_modified:
  - .planning/phases/258-public-app-access/258-DEPLOY-LOG.md
requirements: [PUB-A, PUB-B, PUB-C, PUB-D]
autonomous: false

# WAVE/DEPENDENCY NOTE:
# wave 4 (FINAL), depends_on ALL of 258-01..04 — the integration + deploy gate.
# It runs the full local gate (build + every 258 vitest suite + tsc), deploys the
# merged WS-A..D code to the Mini PC via update.sh, then runs the SC1-SC6 walk incl.
# the Cal.com end-to-end + the security probes (forbidden app rejected; bearer/
# identity headers ABSENT on public routes). The ONLY 258 plan that touches the live
# box (mirrors the 256-05 / 257-07 deploy-gate discipline). Touches only the deploy
# log. Has a checkpoint:human-verify → autonomous: false.
#
# DEPLOY-TARGET RULE: Mini PC (bruce@10.69.31.68) ONLY. Server4/Server5 off-limits.

must_haves:
  truths:
    - "The merged 258 (WS-A..D) code is deployed to the Mini PC via update.sh with all 7 services active (no regression)."
    - "SC1: a 'paths'-mode app's public paths load with NO LivOS session (200) while other paths still 302 → /login."
    - "SC2 + SC4: a public route reaches the container with X-Daemon-Bearer / Remote-User / Remote-Role ABSENT (verified at the container)."
    - "SC3: a forbidden app's enable-public is rejected server-side (403) AND locked in the UI."
    - "SC5: apps without publicAccess stay fully gated — byte-equivalent 256-04 behavior; only owner/admin can enable."
    - "SC6: Cal.com end-to-end — /[user] + /booking/* public; /settings + /event-types still protected by Cal's own login."
  artifacts:
    - path: ".planning/phases/258-public-app-access/258-DEPLOY-LOG.md"
      provides: "deploy transcript + SC1-SC6 probe results + the bearer-absent container capture + any deploy-time hot-fixes"
      contains: "SC1"
  key_links:
    - from: "merged 258 code"
      to: "Mini PC (bruce@10.69.31.68)"
      via: "update.sh deploy + systemctl restart + caddy reload"
      pattern: "update.sh"
---

<objective>
WS-INTEGRATION — Deploy gate + SC1–SC6 walk. Run the pre-deploy local gate (build + every 258 unit suite + tsc),
deploy the merged WS-A..D changes to the Mini PC via `update.sh`, then run the SC1–SC6 verification walk on the live
box — including the Cal.com end-to-end and the security probes (a forbidden app's enable rejected; the daemon
bearer + identity headers ABSENT on a public route, captured AT the container) — and record everything (transcript,
probe results, the bearer-absent capture, any deploy-time hot-fixes) in a DEPLOY-LOG.

Purpose: prove the inverse-of-hardening feature is live AND that the security spine holds on the only production
deployment, mirroring the 256-05 / 257-07 deploy-gate discipline. The security probes are not optional polish —
they are the phase's reason to exist.
Output: 258-DEPLOY-LOG.md with the deploy transcript, the SC1–SC6 results, the at-container bearer-absent capture,
and the regression checklist.

DEPLOY-TARGET RULE: Mini PC (bruce@10.69.31.68) ONLY. Server4 + Server5 are off-limits (per project memory). The
caddy regen + per-app public toggle are runtime (caddy reload) — no reinstall needed.

REAL-CODE / DEPLOY ANCHORS:
- update.sh = `bash /opt/livos/update.sh` on the Mini PC (clones utopusc/livinity-io, rsyncs, pnpm/npm install,
  builds @livos/config + ui (vite) + liv core/worker/mcp (tsc), restarts livos liv-core liv-worker liv-memory).
- The 258 server changes live in livinityd (tsx — no compile) + the ui bundle (vite — rebuilt by update.sh).
- caddy.ts regen fires via registerAppSubdomain → rebuildCaddyFromState → reloadCaddy (apps.ts:1687) on the
  setPublicAccess mutation; verify `caddy reload` succeeds (Caddyfile valid) after enabling.
- Probe at the container: `docker exec <app-container>` a request echo, OR add a temporary header-echo, to confirm
  Remote-User/Remote-Role/X-Daemon-Bearer are absent on a public-route request (SC2/SC4). For Cal.com, the
  /booking page loading with no session IS the SC1/SC6 positive; a curl to /settings 302→login is the negative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/258-public-app-access/258-CONTEXT.md
@.planning/phases/258-public-app-access/258-01-SUMMARY.md
@.planning/phases/258-public-app-access/258-02-SUMMARY.md
@.planning/phases/258-public-app-access/258-03-SUMMARY.md
@.planning/phases/258-public-app-access/258-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-deploy local gate + deploy to the Mini PC via update.sh (PUB-A..D)</name>
  <files>.planning/phases/258-public-app-access/258-DEPLOY-LOG.md</files>
  <read_first>
    - 258-01..04 SUMMARYs (what merged + any deploy notes/hot-fixes flagged)
    - MEMORY.md update.sh quirks (apt-bootstrap delay; @liv+core* pnpm-store copy; service set)
  </read_first>
  <action>
1. LOCAL GATE (must be green before touching the box): from `livos/`, run the full 258 unit set —
   `npx vitest run packages/livinityd/source/modules/apps/schema.test.ts
   packages/livinityd/source/modules/apps/public-access.test.ts
   packages/livinityd/source/modules/apps/public-forbidden.test.ts
   packages/livinityd/source/modules/domain/caddy.test.ts` — plus `npx tsc -p packages/livinityd --noEmit` and the
   ui `pnpm --filter ui exec tsc --noEmit`. Confirm the 256-04 + 257-06 caddy regression cases are still green
   (they run inside caddy.test.ts). Record the results in the DEPLOY-LOG. If any fail → STOP, do not deploy.
2. Ensure the branch is pushed to GitHub (update.sh clones from utopusc/livinity-io).
3. SSH to the Mini PC (bruce@10.69.31.68 — see reference_minipc_ssh.md) and run `bash /opt/livos/update.sh`.
   Capture the transcript. After it completes: `systemctl is-active livos liv-core liv-worker liv-memory caddy`
   (+ liv-assistant) — all active. Watch for the known update.sh quirks (apt-bootstrap on first run; the
   @liv+core* pnpm-store copy). If a service fails, capture logs + apply the minimal hot-fix and re-record.
4. Confirm `caddy validate --config /etc/caddy/Caddyfile` is clean post-deploy (the new emit must not have broken
   the Caddyfile for the existing gated apps — SC5 live regression).
  </action>
  <verify>
    <automated>cd livos && npx vitest run packages/livinityd/source/modules/apps/schema.test.ts packages/livinityd/source/modules/apps/public-access.test.ts packages/livinityd/source/modules/apps/public-forbidden.test.ts packages/livinityd/source/modules/domain/caddy.test.ts && npx tsc -p packages/livinityd --noEmit</automated>
  </verify>
  <done>Local gate green (all 258 suites + tsc + ui tsc); merged code deployed to the Mini PC via update.sh; all services active; caddy validate clean post-deploy; transcript + any hot-fixes recorded in 258-DEPLOY-LOG.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 258 public-app-access feature is deployed to the Mini PC: per-app/per-path public access with the daemon-bearer/identity-header strip spine, server-side forbidden enforcement, and the Share-dialog UI.</what-built>
  <how-to-verify>
    Run the SC1–SC6 walk on the live box and record each result in 258-DEPLOY-LOG.md:

    SC1 (paths public): On a 'paths'-enabled app (Cal.com booking paths), in a PRIVATE browser window (no LivOS
    login) open the booking path → loads (200, booking page). Open a gated path (e.g. /settings) → 302 → /login
    (LivOS) OR the app's own login.

    SC2 + SC4 (bearer/identity absent — the security spine): for a public route, capture the headers the CONTAINER
    receives (docker exec / temporary echo / app access log) → confirm `X-Daemon-Bearer`, `Remote-User`,
    `Remote-Role` are ABSENT. Also try to INJECT them as a client (`curl -H 'Remote-User: admin' -H
    'X-Daemon-Bearer: x' <public-route>`) → confirm they do NOT reach the backend (stripped by the public handle).

    SC3 (forbidden rejected): in the Share dialog of a forbidden app (OpenHands / Portainer / a docker.sock app)
    the public toggle is LOCKED with a reason; calling setPublicAccess for it (or via the API) returns 403.

    SC5 (regression): an app with NO public access enabled is still fully gated (open its subdomain with no session
    → 302 → /login), behaving exactly as pre-258. Only the owner/admin can enable.

    SC6 (Cal.com end-to-end): `/[user]` + `/booking/*` reachable with no LivOS login; `/settings` + `/event-types`
    still protected by Cal's own login.
  </how-to-verify>
  <resume-signal>Type "approved" with the SC1–SC6 results, or describe failures (which become 258-gap inputs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → live Mini PC public route | the real, deployed unauthenticated surface this phase creates |
| client header injection → live container | a real attacker trying to spoof identity / smuggle the bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258E-01 | Info Disclosure / EoP | the live deploy leaks the daemon bearer or accepts injected identity headers on a public route | mitigate | SC2/SC4 probe captures the headers AT the container + attempts a client injection — must show stripped. A failure blocks acceptance (becomes a 258-gap). |
| T-258E-02 | Tampering | the new emit breaks the Caddyfile for existing gated apps (SC5 live regression) | mitigate | `caddy validate` post-deploy + an SC5 walk on a non-public app (still 302→login). |
| T-258E-03 | EoP | a forbidden app is exposable on the live box despite the unit tests | mitigate | SC3 live probe (UI locked + API 403) on a real docker.sock/daemon-bearer app. |
| T-258E-04 | DoS | live public routes have no rate limit | accept (deferred) | out of scope per CONTEXT; noted for a future WAF phase. |
</threat_model>

<verification>
- Local gate green (4 vitest suites + livinityd tsc + ui tsc) before deploy.
- Mini PC: all services active; `caddy validate` clean.
- SC1–SC6 walk results recorded; the SC2/SC4 at-container bearer-absent + injection-stripped capture is present.
- 256/257 gains intact (a gated app still redirects; the OpenDesign daemon-bearer path still works for its gated subdomain).
</verification>

<success_criteria>
All SC1–SC6 verified on the Mini PC and recorded in 258-DEPLOY-LOG.md: paths/whole-app public access works without
a LivOS session; the daemon bearer + identity headers are provably absent (and un-injectable) on public routes;
forbidden apps are rejected server-side + locked in the UI; non-public apps are byte-equivalent-gated as before;
Cal.com's booking pages are public while its dashboard stays protected by its own login. All 256/257 gains, agent
autonomy, and curated apps intact. Mini PC only.
</success_criteria>

<output>
After completion, create `.planning/phases/258-public-app-access/258-05-SUMMARY.md` (alongside the 258-DEPLOY-LOG.md).
</output>
