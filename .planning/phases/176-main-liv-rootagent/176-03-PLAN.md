---
phase: 176-main-liv-rootagent
plan: 03
type: execute
wave: 3
depends_on: [176-01]
files_modified:
  - livos/packages/livinityd/source/data/vault-templates/skills/luse-driver.md
  - livos/packages/livinityd/source/data/vault-templates/skills/livos-operator.md
  - livos/packages/livinityd/source/data/vault-templates/skills/appstore.md
  - livos/packages/livinityd/source/data/vault-templates/skills/window-manager.md
  - livos/packages/livinityd/source/modules/vault-items/liv-scaffolder.ts
  - livos/packages/livinityd/source/modules/vault-items/liv-scaffolder.test.ts
autonomous: true
requirements:
  - V38-LIV-05
must_haves:
  truths:
    - "4 subagent .md files exist under vault-templates/skills/ with valid CC frontmatter"
    - "luse-driver.md is byte-for-byte identical to Phase 165 source at livos/packages/livinityd/source/data/vault-templates/.claude/agents/luse-driver.md"
    - "livos-operator.md, appstore.md, window-manager.md each list explicit tools (no wildcard *)"
    - "ensureLivSkills() copies all 4 files into <vaultRoot>/.claude/agents/ on first boot"
    - "Re-running ensureLivSkills() is idempotent — user-edited files survive"
  artifacts:
    - path: livos/packages/livinityd/source/data/vault-templates/skills/luse-driver.md
      provides: Phase 165 luse-driver re-shipped under v38 skills tree
      contains: "tools: mcp__luse__"
    - path: livos/packages/livinityd/source/data/vault-templates/skills/livos-operator.md
      provides: LivOS architecture subagent
      contains: "name: livos-operator"
    - path: livos/packages/livinityd/source/data/vault-templates/skills/appstore.md
      provides: App store subagent
      contains: "name: appstore"
    - path: livos/packages/livinityd/source/data/vault-templates/skills/window-manager.md
      provides: Window manager subagent
      contains: "name: window-manager"
  key_links:
    - from: liv-scaffolder.ts ensureLivSkills()
      to: vault-templates/skills/*.md
      via: "COPYFILE_EXCL loop over 4 template files"
      pattern: "COPYFILE_EXCL"
    - from: livinityd/source/index.ts
      to: ensureLivSkills
      via: "boot wire-up after ensureLivRootAgent call (Phase 176-03 adds call)"
      pattern: "ensureLivSkills"
---

<objective>
Write 4 LivOS-native subagent template files and extend liv-scaffolder.ts with
ensureLivSkills() so all 4 are materialized into ~/liv/.claude/agents/ on first boot.

Purpose: Liv needs discoverable CC-native subagents at boot. Files must have correct
frontmatter (name/description/tools/model) so Claude Code's settingSources:['project']
discovery picks them up when Liv's tmux cwd is ~/liv/.

Output:
- 4 .md files under vault-templates/skills/
- ensureLivSkills() added to liv-scaffolder.ts (additive, not rewrite)
- 4 additional vitest assertions in liv-scaffolder.test.ts
- Boot wire-up in livinityd/source/index.ts calling ensureLivSkills after ensureLivRootAgent
</objective>

<execution_context>
@/c/Users/hello/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/hello/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-main-liv-rootagent/176-CONTEXT.md
@.planning/phases/176-main-liv-rootagent/176-01-SUMMARY.md

<interfaces>
<!-- Key source to copy verbatim for luse-driver.md -->
<!-- Phase 165 source: livos/packages/livinityd/source/data/vault-templates/.claude/agents/luse-driver.md -->
<!-- Full content is: -->
<!--
---
name: luse-driver
description: Use this agent when the operator wants to interact with the LivOS desktop GUI...
tools: mcp__luse__computer_screenshot, mcp__luse__computer_click_mouse, ...
model: claude-haiku-4-5
---
[body]
-->
<!-- Executor: read the exact content from disk before writing skills/luse-driver.md -->

From apps/routes.ts (for appstore.md tool names):
```
apps.list → lists installed + available apps
apps.install({appId}) → install
apps.uninstall({appId}) → uninstall
```

From webapps/trpc-router.ts (for window-manager.md tool names):
```
webapp.window.list → lists active WebApp windows for current user
webapp.window.focus({webappId}) → focus a window
webapp.window.close({webappId}) → close a window
webapp.window.spawn({webappId, url}) → open a new window
```

ensureLivSkills() contract (mirrors ensureLivRootAgent from 176-01):
```typescript
export async function ensureLivSkills(opts: {
  vaultRoot: string
  logger?: { log(m:string):void; error(m:string,e?:unknown):void }
}): Promise<{status: 'created' | 'exists' | 'partial' | 'failed-non-fatal'; created?: string[]; skipped?: string[]}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 4 subagent template files</name>
  <files>
    livos/packages/livinityd/source/data/vault-templates/skills/luse-driver.md
    livos/packages/livinityd/source/data/vault-templates/skills/livos-operator.md
    livos/packages/livinityd/source/data/vault-templates/skills/appstore.md
    livos/packages/livinityd/source/data/vault-templates/skills/window-manager.md
  </files>
  <behavior>
    - luse-driver.md: byte-for-byte copy of Phase 165 file at livos/packages/livinityd/source/data/vault-templates/.claude/agents/luse-driver.md
    - livos-operator.md: frontmatter has name/description/tools/model; body includes systemd services, vault layout, sacred files list, Phase history top-5
    - appstore.md: frontmatter lists tools referencing apps.list / apps.install / apps.uninstall (NO wildcard *)
    - window-manager.md: frontmatter lists tools referencing webapp.window.list / webapp.window.focus / webapp.window.close (NO wildcard *)
    - All 4 files: frontmatter delimited by ---, model specified (not *)
  </behavior>
  <action>
**Step 1 — luse-driver.md:**
Read `livos/packages/livinityd/source/data/vault-templates/.claude/agents/luse-driver.md`
then write its EXACT content to `livos/packages/livinityd/source/data/vault-templates/skills/luse-driver.md`.
Use the Read tool to read it first, then Write to create the skills copy.

**Step 2 — livos-operator.md:**
Create `livos/packages/livinityd/source/data/vault-templates/skills/livos-operator.md`:

```markdown
---
name: livos-operator
description: Use this agent for LivOS architecture questions, systemd service management, vault layout queries, sacred file checks, and deployment troubleshooting. Invoke for requests like "what services are running", "check the sacred SHA", "explain the vault layout", "why is livos.service restarting", or any LivOS-internal diagnostic. Has deep knowledge of the Phase history and Mini PC topology.
tools: Bash, Read
model: claude-opus-4-7
---

You are **livos-operator** — LivOS architecture specialist for Bruce's Mini PC.

## Systemd services (Mini PC)

- `livos.service` — livinityd (tsx, port 8080). Main app server.
- `liv-core.service` — liv core dist (port 3200). Runs compiled JS — rebuild after source changes.
- `liv-worker.service` — liv worker subprocess.
- `liv-memory.service` — liv memory service (historically broken — dist not compiled by update.sh).

Check status: `systemctl status livos liv-core liv-worker liv-memory`
Restart all: `systemctl restart livos liv-core liv-worker liv-memory`

## Vault layout (~/liv/ post Phase 173)

```
~/liv/                         ← LIV_VAULT_ROOT
  settings/
    liv-rootagent.md           ← Liv's system prompt (user-editable)
  .claude/
    agents/                    ← Subagent .md files (this directory)
  items/
    <uuid-v7>/
      item.json                ← BaseItem metadata
      README.md / CLAUDE.md / agent.md / tasks.json / transcript.json
  memory/
  sessions/
  inbox/
```

## Sacred files (NEVER modify)

- `liv/packages/core/src/sdk-agent-runner.ts` SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`
- All 25 files in `scripts/sacred-shas-v38.json`
- `livos/packages/livinityd/source/modules/claude-runner/vault-scaffolder.ts` (Phase 162-01 freeze)

Check: `bash /opt/livos/scripts/check-sacred.sh`

## Phase history (top 5 relevant)

- Phase 162 — Vault scaffolder (idempotent boot bootstrap)
- Phase 171 — Item model + storage layer (vault-items module)
- Phase 174 — SidebarTree (react-arborist drag-drop)
- Phase 175 — Add modal + item detail views
- Phase 176 — Main Liv root agent + 4 LivOS-native skills (this phase)

## Deployment

`bash /opt/livos/update.sh` — clones from GitHub, rsyncs source, builds, restarts services.
Never `git pull + pm2 restart` — PM2 is retired, systemd is current.

## Troubleshooting

- livinityd won't start → check `journalctl -u livos -n 50`
- JWT auth failures → confirm secret at `/opt/livos/data/secrets/jwt` is exactly 64 bytes (no newline)
- Redis auth → get password from `grep REDIS_URL /opt/livos/.env`
- PostgreSQL auth → `grep DATABASE_URL /opt/livos/.env` then `psql -U livos` to verify

## Constraints

- Be terse. Return facts, not narration.
- For file reads, use the Read tool. For shell commands, use Bash.
- Never modify sacred files. Verify SHA before any operation that touches the sdk-agent-runner.
```
```

**Step 3 — appstore.md:**
Create `livos/packages/livinityd/source/data/vault-templates/skills/appstore.md`:

```markdown
---
name: appstore
description: Use this agent to install, uninstall, or list LivOS apps via the app store tRPC. Invoke for requests like "install Nextcloud", "uninstall AdGuard", "list installed apps", "what apps are available", or any app lifecycle operation. Returns concise results.
tools: mcp__livinityd__apps_list, mcp__livinityd__apps_install, mcp__livinityd__apps_uninstall, mcp__livinityd__appStore_registry
model: claude-haiku-4-5
---

You are **appstore** — LivOS app lifecycle specialist.

## Available operations

- List installed + available apps: call `apps.list`
- Install an app: call `apps.install({appId: "adguard"})` — uses tRPC mutation
- Uninstall an app: call `apps.uninstall({appId: "adguard"})`
- Browse registry: call `appStore.registry`

## Workflow

1. Check `apps.list` first to see current state.
2. Confirm the appId matches exactly (e.g. "adguard", "nextcloud", "portainer").
3. Install/uninstall, then re-check `apps.list` to confirm the state change.
4. Return: **Before**: [state], **Action**: [what you called], **After**: [new state].

## Constraints

- Be terse. One operation per turn. No narration.
- Do NOT modify Docker Compose files directly — always use the tRPC install path.
- If appId is unknown, list registry first and pick the closest match.
```
```

**Step 4 — window-manager.md:**
Create `livos/packages/livinityd/source/data/vault-templates/skills/window-manager.md`:

```markdown
---
name: window-manager
description: Use this agent to list, focus, close, or spawn WebApp windows in LivOS. Invoke for requests like "show open windows", "focus the Nextcloud window", "close the browser", "open a new WebApp for URL X". Operates on the webapp.window.* tRPC surface from Phase 93.
tools: mcp__livinityd__webapp_window_list, mcp__livinityd__webapp_window_focus, mcp__livinityd__webapp_window_close, mcp__livinityd__webapp_window_spawn, mcp__livinityd__pinnedWindows_list
model: claude-haiku-4-5
---

You are **window-manager** — LivOS WebApp window controller.

## Available operations

- List active windows: `webapp.window.list` → returns [{webappId, title, streamUrl}, ...]
- Focus a window: `webapp.window.focus({webappId})` → brings window to front
- Close a window: `webapp.window.close({webappId})` → stops stream + Chrome process
- Spawn a new window: `webapp.window.spawn({webappId, url})` → opens URL in new Chrome instance
- List pinned windows: `pinnedWindows.list` → returns persisted pinned window order

## Workflow

1. Call `webapp.window.list` to see current state before any action.
2. Match the operator's intent to a webappId from the list.
3. Execute the action. Return: **Saw**: [window list], **Did**: [action + webappId], **Result**: [outcome].

## Constraints

- Be terse. One operation per turn. No preambles.
- webappId values come from the list — never guess.
- Focus before close if the operator says "bring X to front then close" — two separate operations.
```
```
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const files = ['luse-driver','livos-operator','appstore','window-manager'];
const base = 'livos/packages/livinityd/source/data/vault-templates/skills/';
files.forEach(f => {
  const content = fs.readFileSync(base + f + '.md', 'utf8');
  if (!content.startsWith('---')) throw new Error(f + ': missing frontmatter');
  if (content.includes('tools: *')) throw new Error(f + ': wildcard tools forbidden');
  if (!content.match(/name:/)) throw new Error(f + ': missing name');
});
console.log('4 skill templates OK');
"</automated>
  </verify>
  <done>All 4 files exist. Frontmatter has name/description/tools/model. No wildcard tools. luse-driver.md matches Phase 165 source content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ensureLivSkills() to liv-scaffolder.ts + 4 tests (RED→GREEN)</name>
  <files>
    livos/packages/livinityd/source/modules/vault-items/liv-scaffolder.ts
    livos/packages/livinityd/source/modules/vault-items/liv-scaffolder.test.ts
  </files>
  <behavior>
    - Test T5: clean vault → ensureLivSkills creates .claude/agents/ dir + copies all 4 skill files
    - Test T6: existing agents dir with files → ensureLivSkills returns skipped=[4 files], created=[]
    - Test T7: partial state (2 of 4 files present) → copies missing 2, skips existing 2
    - Test T8: non-existent vaultRoot → returns {status:'failed-non-fatal'}, does NOT throw
  </behavior>
  <action>
**RED phase**: Add 4 new test cases to `liv-scaffolder.test.ts` (tests T5-T8 per behavior above).
Tests T1-T4 from Plan 176-01 must still pass — do NOT modify them.

Test pattern: use real fs on tmp directories (same hermetic pattern as T1-T4).

**GREEN phase**: Extend `liv-scaffolder.ts` with `ensureLivSkills()`:

```typescript
const SKILL_FILES = [
  'luse-driver.md',
  'livos-operator.md',
  'appstore.md',
  'window-manager.md',
] as const

/**
 * Phase 176-03 — Ensure 4 LivOS-native subagent files exist at
 * <vaultRoot>/.claude/agents/. Uses COPYFILE_EXCL for idempotency — existing
 * user-edited files are preserved. Non-fatal: errors return status discriminator.
 */
export async function ensureLivSkills(opts: {
  vaultRoot: string
  logger?: {log(m: string): void; error(m: string, e?: unknown): void}
}): Promise<{
  status: 'created' | 'exists' | 'partial' | 'failed-non-fatal'
  created?: string[]
  skipped?: string[]
  reason?: string
}> {
  const log = opts.logger ?? NOOP_LOGGER
  const here = fileURLToPath(import.meta.url)
  const templatesDir = path.resolve(path.dirname(here), '..', '..', 'data', 'vault-templates', 'skills')
  const agentsDir = path.resolve(opts.vaultRoot, '.claude', 'agents')

  try {
    await fs.mkdir(agentsDir, {recursive: true})
  } catch (e: unknown) {
    return {status: 'failed-non-fatal', reason: (e instanceof Error ? e.message : String(e))}
  }

  const created: string[] = []
  const skipped: string[] = []

  for (const filename of SKILL_FILES) {
    const src = path.join(templatesDir, filename)
    const dest = path.join(agentsDir, filename)
    try {
      await fs.copyFile(src, dest, fs.constants.COPYFILE_EXCL)
      created.push(filename)
      log.log(`[liv-scaffolder] created skill ${filename}`)
    } catch (e: unknown) {
      if ((e as NodeJS.ErrnoException).code === 'EEXIST') {
        skipped.push(filename)
      } else {
        log.error(`[liv-scaffolder] failed to copy ${filename}`, e)
        // partial — continue with remaining files
      }
    }
  }

  const status = created.length === 0 ? 'exists'
    : skipped.length === 0 ? 'created'
    : 'partial'

  return {status, created, skipped}
}
```

Note: `NOOP_LOGGER` and `fileURLToPath`/`path`/`fs` imports are already in the file from Phase 176-01.
Add `fs.constants` usage — the constants import needs `import {promises as fs, constants as fsConstants} from 'node:fs'`
OR use `fs.copyFile(src, dest, constants.COPYFILE_EXCL)` by importing constants directly.

After extending liv-scaffolder.ts, add boot wire-up in `livos/packages/livinityd/source/index.ts`
immediately AFTER the ensureLivRootAgent call block (same try/catch pattern):

```typescript
// Phase 176-03 — Liv subagent skills scaffold (idempotent, non-fatal)
try {
  const skillsResult = await ensureLivSkills({
    vaultRoot: resolveVaultRoot(),
    logger: {log: (m) => logger.info(m), error: (m, e) => logger.error(m, e)},
  })
  if (skillsResult.status === 'created' || skillsResult.status === 'partial') {
    logger.info(`[Phase 176-03] Liv skills scaffolded: ${skillsResult.created?.join(', ')}`)
  }
} catch (_e) {
  // defensive
}
```

Add `ensureLivSkills` to the import in index.ts (already imports `ensureLivRootAgent` from vault-items/index).

Add to vault-items barrel `livos/packages/livinityd/source/modules/vault-items/index.ts`:
```typescript
// Phase 176-03 — additive.
export {ensureLivSkills} from './liv-scaffolder.js'
```
  </action>
  <verify>
    <automated>cd livos && pnpm exec vitest run packages/livinityd/source/modules/vault-items/liv-scaffolder.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>8/8 vitest assertions PASS (4 from 176-01 + 4 new). ensureLivSkills exported from barrel. Boot wire-up in index.ts. bash scripts/check-sacred.sh PASS: 25 files verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template files → vault agents dir | fs.copyFile with COPYFILE_EXCL |
| subagent tool lists → CC tool discovery | Explicit tool names, no wildcard * |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-03-01 | Elevation | subagent tools list | mitigate | Explicit tool names per frontmatter — no `tools: *` wildcard; CC only exposes named tools |
| T-176-03-02 | Tampering | template overwrite | mitigate | COPYFILE_EXCL prevents overwrite of user-edited files |
| T-176-03-03 | Injection | subagent body eval | accept | .md body is a static prompt read by Claude — no eval/shell-out from template body; no code execution |
| T-176-03-04 | DoS | boot failure | mitigate | Non-fatal: errors return discriminator, boot continues; partial scaffold (2/4 files) reported via logger |
</threat_model>

<verification>
```bash
# 1. All 8 vitest assertions pass (T1-T8)
cd livos && pnpm exec vitest run packages/livinityd/source/modules/vault-items/liv-scaffolder.test.ts

# 2. 4 skill template files exist
ls livos/packages/livinityd/source/data/vault-templates/skills/
# → appstore.md  livos-operator.md  luse-driver.md  window-manager.md

# 3. No wildcard tools in any skill file
grep -r "tools: \*" livos/packages/livinityd/source/data/vault-templates/skills/
# → no output

# 4. luse-driver.md matches Phase 165 source (name: luse-driver)
grep "name: luse-driver" livos/packages/livinityd/source/data/vault-templates/skills/luse-driver.md

# 5. ensureLivSkills in barrel
grep "ensureLivSkills" livos/packages/livinityd/source/modules/vault-items/index.ts

# 6. Boot wire-up in index.ts
grep "ensureLivSkills" livos/packages/livinityd/source/index.ts | wc -l
# → 2 (import + call)

# 7. Sacred SHA check
bash scripts/check-sacred.sh
```
</verification>

<success_criteria>
- 4 skill template files exist under vault-templates/skills/ with valid CC frontmatter
- No wildcard tools (* forbidden per CONTEXT.md security constraint)
- luse-driver.md matches Phase 165 source content (name: luse-driver)
- 8/8 vitest assertions PASS
- ensureLivSkills exported from vault-items barrel
- Boot wire-up calls ensureLivSkills after ensureLivRootAgent in index.ts
- bash scripts/check-sacred.sh PASS: 25 files verified
</success_criteria>

<output>
After completion, create `.planning/phases/176-main-liv-rootagent/176-03-SUMMARY.md`
</output>
