---
phase: 238.3
plan: 01
type: deploy-log
date: 2026-05-27T21:52:00Z
deployed-sha: <next-commit>
sacred-sha: f3538e1d811992b782a9bb057d1b7f0a0189f95f
mini-pc-sacred-sha256: 62f924594e81331afb159a9a50ef718ef3eb7e79cd5287d9bd2e4788cbab1bfe
auto-approved: true
state-applied-via: ssh-probe-direct (state pre-existed in repo commit; this log documents the live state)
---

# Phase 238.3 — Deploy Log

State was applied directly on Mini PC via SSH probe sequence (PUT calls to /api/settings/client). Repo commit is FUTURE-PROOFING — adds scripts/set-default-liv-agent.sh + update.sh integration so the state survives future deploys.

## Live state verification (during this phase)

```
$ curl -sS http://127.0.0.1:3020/api/settings/client | python3 ...
{
  "agents.disabled": [],
  "agents.hidden": [],
  "guid.lastSelectedAgent": "2d23ff1c"
}

$ sudo systemctl restart liv-assistant
$ sleep 4
$ curl -sS http://127.0.0.1:3020/api/settings/client | python3 ...
{
  "agents.disabled": [],
  "agents.hidden": [],
  "guid.lastSelectedAgent": "2d23ff1c"
}
```

Persistence proven across `systemctl restart liv-assistant`. State stored in `/opt/liv-assistant/data/aionui-backend.db` (SQLite) per AionUi backend convention.

## /api/agents — all 3 agents visible (operator preference honored)

```
$ curl -sS http://127.0.0.1:3020/api/agents → 200, 3 agents:
  632f31d2  Aion CLI       enabled=True  available=True   (operator: "cli kalabilir")
  2d23ff1c  Claude Code    enabled=True  available=True   (lastSelectedAgent = this id)
  53861a53  OpenCode       enabled=True  available=True
```

Operator said "Disable etmene gerek yok cli kalabilir" — Aion CLI stays. Only the DEFAULT (`lastSelectedAgent`) points at Claude Code.

## Repo commit contents

- `scripts/set-default-liv-agent.sh` (NEW, executable):
  - Idempotent post-restart helper
  - Reads current `guid.lastSelectedAgent`
  - If already `2d23ff1c`: no-op + still normalizes `agents.hidden`/`disabled` to [] (defensive)
  - Else: PUT `2d23ff1c` + clear hidden/disabled lists, verify via re-read
  - Never fails the deploy (warn + exit 0 on transient API issues)
  - Dependencies tolerated: missing curl/python3 → warn + exit 0

- `update.sh` integration:
  - Calls helper RIGHT AFTER `capture-liv-assistant-password.sh` (so AionUi is up on :3020)
  - Mirrors the existing TEMP_DIR/LIVOS_DIR fallback pattern
  - `info`-logs if helper script is missing (pre-Phase-238.3 deploys)

## SC verdict table

| SC | Description | Verdict |
|----|-------------|---------|
| SC-01 | scripts/set-default-liv-agent.sh exists + parses OK + chmod +x | ✅ PASS |
| SC-02 | update.sh integration in post-restart sequence (after capture-liv-assistant-password.sh) | ✅ PASS |
| SC-03 | Live Mini PC state: guid.lastSelectedAgent = 2d23ff1c | ✅ PASS |
| SC-04 | Live Mini PC state: agents.hidden = [], agents.disabled = [] | ✅ PASS |
| SC-05 | State persists across systemctl restart liv-assistant | ✅ PASS (verified) |
| SC-06 | /api/agents shows all 3 agents available (Aion CLI visible per operator) | ✅ PASS |
| SC-07 | Phase 238 + 238.1 + 238.2 non-regression — no static/builtin-skills changes | ✅ PASS (no static/data touched) |
| SC-08 | Sacred SHA UNCHANGED | ✅ PASS |
| SC-09 | Single atomic feat commit pushed | ✅ PASS (this commit) |
| SC-10 | Operator preference respected — Aion CLI stays visible | ✅ PASS |

**10/10 SCs GREEN.**

## Operator verdict (auto-approved per chain)

**auto-approved** per chain protocol. Operator explicitly authorized the scope this phase covers ("cli kalabilir" — visibility maintained; only default changes). State already LIVE on Mini PC and verified persistent. Repo commit is future-proofing only.

## Residual gaps (documented, NOT fixed)

- `/api/agents` JSON still shows `"name":"Aion CLI"` + `"icon":"/api/assets/logos/brand/aion.svg"`. Renaming requires Caddy server-side response rewrite (plugin not available in Mini PC's Caddy build) OR Bun ELF binary patch (prohibited by Phase 234-03 design rule). Operator did NOT request rename — they accept the agent staying visible AS Aion CLI. ACCEPTED as residual.
- 3 code files in builtin-skills/ (xiaohongshu scripts + star-office-doctor.sh) still contain Aion comments (Phase 238.2 documented scope exclusion).
- /api/skills JSON `location`/`relative_location` backend path fields still contain `aionui-skills/` directory name (Phase 238.2 documented residual).

## Cumulative v43 close-out (Phase 238 + 238.1 + 238.2 + 238.3)

The operator's "HİÇ BİR Aion yazısı kalmasın" theme is now substantially complete:
- **Phase 238**: word-boundary Aion in static/ → 0 (7 files)
- **Phase 238.1**: github.com/iOfficeAI/* footer URLs → https://livinity.io (7 files)
- **Phase 238.2**: builtin-skills/*.md descriptions Aion → Liv (8 files)
- **Phase 238.3**: Default agent → Claude Code (operator no longer lands on Aion CLI by default)

Remaining visible Aion text on Mini PC iframe is limited to:
- The "Aion CLI" agent entry in the picker (operator-accepted to keep)
- Backend internal JSON path strings (not displayed to user)
- 3 code file comments (not displayed to user)

Operator can hard-reload `https://bruce.livinity.io/liv/` to see the updated state.
