---
phase: 132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-
plan: 07
type: execute
wave: 3
depends_on: ["132-01", "132-02", "132-03", "132-04", "132-05", "132-06"]
files_modified:
  - .planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-UAT-CHECKLIST.md
  - .planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-07-SUMMARY.md
  - .planning/STATE.md
  - memory/project_phase_132_complete.md
autonomous: false
requirements:
  - V34-INST-07
tags:
  - uat
  - fresh-vps
  - end-to-end
blocking_questions:
  - "Operator must provision a fresh Ubuntu 24.04 VPS (Hetzner / Contabo / DigitalOcean — operator choice)."
  - "Operator must have a fresh subdomain (e.g. uat132.bruceoz.com or uat132.livinity.live) + Cloudflare token with Zone:DNS:Edit scope + zone ID."
  - "If 132-02 chose Path A (remove emailVerified gate), confirm with operator that the tradeoff is acceptable for the v34 ship."
must_haves:
  truths:
    - "On a brand-new Ubuntu 24.04 VPS (no LivOS history), the canonical wizard one-liner pasted into a root shell completes the install within 10 wall-clock minutes."
    - "After install, `https://<chosen-subdomain>/` returns HTTP 200 with the LivOS UI loaded and valid LE wildcard cert (no browser warning)."
    - "A newly-registered user (different from bruce) can use the wizard end-to-end with no 403s."
    - "All seven bug reproductions from 132-CONTEXT.md no longer trigger."
    - "Sacred SHA invariant holds after the UAT (no on-server patches that would have touched repo code)."
  artifacts:
    - path: ".planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-UAT-CHECKLIST.md"
      provides: "PASS/FAIL boxes per bug + per acceptance criterion"
      contains: "UAT TARGET: FRESH VPS"
    - path: ".planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-07-SUMMARY.md"
      provides: "VPS info, wall-clock timing, every bug recheck, final verdict"
      contains: "PHASE_132_FRESH_VPS_INSTALL_PASSED"
---

<objective>
End-to-end UAT on a **fresh Ubuntu 24.04 VPS** that proves the canonical
install path (wizard → one-liner → working LivOS in browser) works without
any manual intervention. This is the gate that flips Phase 132 from
CODE-COMPLETE to SHIPPED.

This plan is `autonomous: false` because:
- Operator must provision the VPS (~$5-10/mo, Hetzner/Contabo/DO)
- Operator must own a domain + CF token for the test subdomain
- Operator must visually verify the LivOS UI loads in their browser
- Operator must decide what to do if a bug recurs (escalate to follow-up plan)
</objective>

<context>
@.planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-CONTEXT.md
@.planning/phases/132-v34-install-sh-hardening-uat-driven-fixes-7-bugs-discovered-/132-PLAN.md

<read_first>
All 132-01..06 SUMMARY.md files (verify each shipped + the `*_VERIFIED` markers
exist).
</read_first>
</context>

<tasks>

<task id="07-01" type="execute">
<action>
Write `132-UAT-CHECKLIST.md` with PASS/FAIL boxes per bug + per acceptance criterion. Operator fills it in during the live walk.

Structure (model on the Phase 131 checklist):

```markdown
# Phase 132 — UAT Checklist (Fresh VPS Install)

**UAT TARGET: FRESH VPS** (operator-provisioned Ubuntu 24.04, no LivOS history).

## Pre-flight

| Step | Action | PASS / FAIL |
|------|--------|-------------|
| P1 | Provision fresh Ubuntu 24.04 VPS (record provider + IP + sudo creds) | ☐ ☐ |
| P2 | Verify SSH root login works | ☐ ☐ |
| P3 | Cloudflare API token created (Zone:DNS:Edit scope) | ☐ ☐ |
| P4 | Subdomain chosen + zone ID recorded | ☐ ☐ |
| P5 | livinity.io platform user logged in, can reach `/dashboard` | ☐ ☐ |

## Wizard walk (test Bugs #1, #2, #3)

| Step | Action | Expected | PASS / FAIL |
|------|--------|----------|-------------|
| W1 | Navigate to `https://livinity.io/dashboard` | Dashboard loads, "Install" link visible in nav (Bug #2) | ☐ ☐ |
| W2 | Click "Install" | Wizard loads within 5s, no babel errors in DevTools console (Bug #1) | ☐ ☐ |
| W3 | Select Hybrid mode, fill domain/cf-token/cf-zone-id | Step 2 renders correctly | ☐ ☐ |
| W4 | Click "Generate API Key" | API key returned, install command displayed (Bug #3) | ☐ ☐ |
| W5 | Copy the install one-liner | Clipboard contains valid curl command with all 5 flags | ☐ ☐ |

## Install run (test Bugs #4, #5, #6, #7)

| Step | Action | Expected | PASS / FAIL |
|------|--------|----------|-------------|
| I1 | Paste one-liner into VPS root shell from /root | Self-bootstrap log appears + install begins (Bug #4) | ☐ ☐ |
| I2 | Observe install through pnpm install step | No ERR_PNPM_CONFIG_CONFLICT (Bug #5) | ☐ ☐ |
| I3 | Observe install through liv/ build step | "Building liv/core" etc. log lines visible (Bug #6) | ☐ ☐ |
| I4 | Observe install through Caddy start step | "Caddy active after Ns" log line (Bug #7) | ☐ ☐ |
| I5 | Install completes with banner (≤ 10 min wall-clock) | Final OK banner, no errors | ☐ ☐ |

## Post-install verification

| Step | Action | Expected | PASS / FAIL |
|------|--------|----------|-------------|
| V1 | `systemctl is-active livos caddy` | Both `active` | ☐ ☐ |
| V2 | Open `https://<subdomain>/` in browser | LivOS UI loads, valid LE cert (no warning) | ☐ ☐ |
| V3 | Onboard a user via the UI | Setup completes | ☐ ☐ |
| V4 | Register an additional user on livinity.io (different from bruce), open wizard, Generate | API key returned without 403 (Bug #3 final check) | ☐ ☐ |

## Sacred SHA + repo hygiene

| Check | Expected | PASS / FAIL |
|-------|----------|-------------|
| `git ls-tree HEAD liv/packages/core/src/sdk-agent-runner.ts` on Mini PC dev repo | `f3538e1d811992b782a9bb057d1b7f0a0189f95f` | ☐ ☐ |
| No on-server patches required to complete the install | True | ☐ ☐ |

## Verdict

- If ALL boxes PASS: flip `phase_132_status` in STATE.md to SHIPPED, add commit hashes to PROJECT.md, write `project_phase_132_complete.md` to auto-memory.
- If ANY box FAILS: copy failing rows into a new `132-UAT-FAILURES.md`, open a 132.x hotfix plan for each bug recurrence.

OPERATOR NAME: ________________
OPERATOR DATE: ________________
VPS PROVIDER + IP: ________________
SUBDOMAIN USED: ________________
WALL-CLOCK INSTALL TIME: ________________
```
</action>
<acceptance_criteria>
- 132-UAT-CHECKLIST.md exists with `UAT TARGET: FRESH VPS` literal.
- All bugs (1-7) have at least one explicit PASS/FAIL row.
</acceptance_criteria>
</task>

<task id="07-02" type="execute">
<action>
After the operator walks the checklist + everything passes, write
`132-07-SUMMARY.md`:

Sections:
1. **VPS provisioned** — provider, IP, region, cost.
2. **Subdomain + zone** used.
3. **Wall-clock install time** (target ≤10 min).
4. **Each of the 7 bug reproductions re-tested** — recheck the original UAT reproduction commands → all should now fail to trigger (i.e. install succeeds).
5. **PHASE_132_FRESH_VPS_INSTALL_PASSED** (literal).
6. **Cost retrospective** — engineer time + cloud spend for the UAT.
7. **Hand-off to v35**: anything that DIDN'T get fixed (e.g., if 132-02 chose Path A, document that email-verification UX is a v35 carryover).

Commit format:

```
docs(132/uat): fresh-VPS install passed — Phase 132 SHIPPED

Operator-walked UAT on {provider} VPS at {IP}. Wizard → one-liner →
working LivOS at https://{subdomain}/ in {N} minutes.

All 7 bugs from 132-CONTEXT.md no longer reproduce.

Sacred SHA preserved.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

Also update STATE.md (`phase_132_status: SHIPPED`, commit hashes) and
write auto-memory `project_phase_132_complete.md` following the
[project_phase_131_partial_state](../../../memory/project_phase_131_partial_state.md)
pattern.
</action>
<acceptance_criteria>
- 132-07-SUMMARY.md with `PHASE_132_FRESH_VPS_INSTALL_PASSED`.
- STATE.md updated.
- Memory entry written.
- Sacred SHA preserved.
</acceptance_criteria>
</task>

</tasks>

<verification>
Operator walks `132-UAT-CHECKLIST.md` end-to-end. If all PASS → ship.
If any FAIL → open hotfix sub-plan.
</verification>
