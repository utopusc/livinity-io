---
phase: 40-per-user-claude-oauth-home-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md
autonomous: true
requirements:
  - FR-AUTH-01
  - FR-AUTH-02
  - FR-AUTH-03

must_haves:
  truths:
    - "Plan 02 executor knows the exact line number in sdk-agent-runner.ts that needs to change (currently line 266 per CONTEXT.md, must re-verify because Plan 02 will run on a possibly-updated file)."
    - "Plan 02 executor knows the exact AgentConfig interface location in agent.ts so the homeOverride field can be added without ambiguity."
    - "Plan 03 executor knows the exact tRPC router file + position to add the new claude-login-per-user route + the exact function name pattern that exposes ctx.currentUser."
    - "Plan 03 executor knows the canonical Redis key for multi-user-mode + the exact existing pattern for reading it (so the multi-user gate behaves identically to existing app-gateway code)."
    - "Plan 04 executor knows the exact file (livos/packages/ui/src/routes/settings/ai-config.tsx) and the exact section/JSX block where the per-user claude card extension goes."
    - "Plan 05 executor knows which existing tests must stay green + which new tests are needed + the canonical assert library + tsx invocation pattern."
  artifacts:
    - path: ".planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md"
      provides: "Complete read-only inventory: file paths, line numbers, function names, Redis keys, every concrete coordinate Plans 02-05 need."
      min_lines: 80
  key_links:
    - from: ".planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md"
      to: "Plan 02 (sacred file edit)"
      via: "Section 1 — sacred file SHA + line 266 confirmation + AgentConfig interface coordinates"
      pattern: "Section 1.*sacred"
    - from: ".planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md"
      to: "Plan 03 (per-user dir + claude login backend)"
      via: "Section 2 — multi-user gate Redis key + ai/routes.ts insertion point + ctx.currentUser pattern"
      pattern: "Section 2.*per-user"
    - from: ".planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md"
      to: "Plan 04 (UI per-user-awareness)"
      via: "Section 3 — ai-config.tsx anchor + existing claudeStartLogin call site"
      pattern: "Section 3.*UI"
    - from: ".planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md"
      to: "Plan 05 (tests + verification)"
      via: "Section 4 — existing test suite + integrity test BASELINE_SHA update spec"
      pattern: "Section 4.*tests"
---

<objective>
Read-only investigation that produces 40-AUDIT.md — the single source of concrete coordinates (file paths, line numbers, function names, Redis keys, exact JSX sections) that Plans 02-05 will consume. Eliminates the "scavenger hunt" anti-pattern: every downstream plan executor reads AUDIT.md once and never has to re-grep the codebase to find what to modify.

Purpose: Plans 02-05 each touch a different layer (sacred file, backend route, UI, tests). Doing one fast audit upfront means each downstream plan can be a tight, atomic edit instead of an exploration session.

Output: `.planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md` with 4 sections, one per downstream plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/40-per-user-claude-oauth-home-isolation/40-CONTEXT.md
@.planning/phases/39-risk-fix-close-oauth-fallback/39-SUMMARY.md
@.planning/phases/39-risk-fix-close-oauth-fallback/39-AUDIT.md

<interfaces>
<!-- Key contracts the AUDIT.md must record verbatim. Pre-extracted to save executor time. -->

From nexus/packages/core/src/sdk-agent-runner.ts (sacred file — Phase 39 baseline SHA `2b3b005bf1594821be6353268ffbbdddea5f9a3a`):
```
// Line 264-274 (the safeEnv block — line 266 is the HOME line that Plan 02 will change):
// Minimal subprocess environment to avoid leaking secrets (Pitfall 12)
const safeEnv: Record<string, string | undefined> = {
  HOME: process.env.HOME || '/root',          // <-- line 266
  PATH: process.env.PATH || '/usr/local/bin:/usr/bin:/bin',
  NODE_ENV: process.env.NODE_ENV || 'production',
  LANG: process.env.LANG || 'en_US.UTF-8',
};
```

From nexus/packages/core/src/agent.ts (NON-sacred — Plan 02 adds homeOverride field here):
```typescript
export interface AgentConfig {
  brain: Brain;
  toolRegistry: ToolRegistry;
  nexusConfig?: NexusConfig;
  maxTurns?: number;
  maxTokens?: number;
  // ... existing fields ...
}
```

From livos/packages/livinityd/source/modules/server/index.ts (lines ~353-358 — canonical multi-user gate):
```typescript
const multiUserEnabled = await this.livinityd.ai.redis.get('livos:system:multi_user')
if (multiUserEnabled === 'true') {
  // multi-user code path
}
```

From livos/packages/livinityd/source/modules/server/trpc/is-authenticated.ts:
```typescript
ctx.currentUser = {
  id: dbUser.id,
  username: dbUser.username,
  role: dbUser.role,
}
```

From livos/packages/livinityd/source/modules/ai/routes.ts (~line 230 — Claude Auth section anchor):
```typescript
// ── Claude Auth ──────────────────────────────────────────
getClaudeStatus: privateProcedure.query(async ({ctx}) => { ... }),
claudeStartLogin: privateProcedure.mutation(async ({ctx}) => { ... }),
// ... other claude routes ...
```

From livos/packages/livinityd/source/modules/apps/apps.ts (~line 892 — per-user dir pattern):
```typescript
// Per-user data directory
const userDataDir = `${this.#livinityd.dataDirectory}/users/${user.username}/app-data/${appId}`
await fse.mkdirp(userDataDir)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify sacred file is unchanged + capture exact line for HOME</name>
  <files>(read-only)</files>
  <action>
Run these commands and capture the output verbatim into AUDIT.md Section 1:

1. `cd nexus/packages/core && git hash-object src/sdk-agent-runner.ts` — confirm matches Phase 39 baseline `2b3b005bf1594821be6353268ffbbdddea5f9a3a`. If it does NOT match, STOP and report: another phase has touched the sacred file and the rest of Phase 40 needs replanning.

2. `grep -n "HOME: process.env.HOME" nexus/packages/core/src/sdk-agent-runner.ts` — confirm exactly ONE match and capture the line number (expected 266 per CONTEXT.md, but record the actual).

3. `grep -n "interface AgentConfig" nexus/packages/core/src/agent.ts` — capture the line number where the interface is declared.

4. `grep -n "^}" nexus/packages/core/src/agent.ts | head -10` — find the closing brace line numbers near the interface so Plan 02 knows where to insert the new field.

Record all outputs verbatim in AUDIT.md Section 1: "Sacred File Coordinates (for Plan 02)". Include the exact 5-line context around the HOME line so Plan 02's executor can pattern-match without ambiguity.

Per D-40-01 / D-40-02: the sacred-file edit is "behavior-preserving NOT byte-identical" for this surgical change. The integrity test's BASELINE_SHA constant gets re-pinned in Plan 02 (per D-40-11 / D-40-15).
  </action>
  <verify>
    <automated>test -f .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; grep -q "Section 1" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; grep -q "2b3b005bf1594821be6353268ffbbdddea5f9a3a" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md</automated>
  </verify>
  <done>AUDIT.md Section 1 exists with: confirmed Phase 39 baseline SHA match, line number for HOME assignment, line number for AgentConfig interface declaration + closing brace, and 5-line context block around the HOME line.</done>
</task>

<task type="auto">
  <name>Task 2: Discover backend integration points for per-user claude login + multi-user gate</name>
  <files>(read-only)</files>
  <action>
Run these commands and capture into AUDIT.md Section 2: "Backend Integration Coordinates (for Plan 03)".

1. `grep -rn "livos:system:multi_user" livos/packages/livinityd/source/` — capture every call site verbatim. Confirm the canonical pattern is `await this.livinityd.ai.redis.get('livos:system:multi_user')` and `=== 'true'` for the truthy check.

2. `grep -n "ctx.currentUser" livos/packages/livinityd/source/modules/ai/routes.ts livos/packages/livinityd/source/modules/server/trpc/is-authenticated.ts` — capture lines showing how user_id is read from ctx.

3. `grep -n "claudeStartLogin\|getClaudeStatus\|claudeLogout\|claudeSubmitCode" livos/packages/livinityd/source/modules/ai/routes.ts` — capture the line numbers of all 4 existing single-user Claude routes. The new per-user routes go RIGHT BEFORE or RIGHT AFTER this block (within the same `// ── Claude Auth ──` section).

4. `grep -n "observable\|subscription:" livos/packages/livinityd/source/modules/ai/routes.ts | head -20` — find any existing tRPC subscription pattern in this file (or report "none — Plan 03 will introduce the first").

5. `grep -rn "fse.mkdirp\|fs.mkdir\|chmod.*0o700\|0o700" livos/packages/livinityd/source/modules/apps/apps.ts | head -10` — capture the per-user-dir creation pattern (mode bits, library used).

6. `grep -n "execFile\|child_process\|spawn" livos/packages/livinityd/source/modules/ai/routes.ts livos/packages/livinityd/source/modules/ai/index.ts | head -10` — find existing subprocess invocation pattern. If none, note: Plan 03 will introduce.

7. `which claude || ls -la /usr/local/bin/claude /usr/bin/claude 2>&amp;&amp;1` (best-effort — may not exist on planner's machine; document the assumed Mini PC path: `claude` resolves via PATH).

Record all outputs verbatim. Write a "Recommended insertion point" subsection that names the exact file (`livos/packages/livinityd/source/modules/ai/routes.ts`), the exact line BEFORE which Plan 03 should insert the new routes (anchor: BEFORE the `// ── Provider Management ──` divider line ~373), and the recommended route names per D-40-08/09 (suggested: `claudePerUserStatus`, `claudePerUserStartLogin` (subscription), `claudePerUserLogout`).
  </action>
  <verify>
    <automated>grep -q "Section 2" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; grep -q "livos:system:multi_user" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; grep -q "ctx.currentUser" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md</automated>
  </verify>
  <done>AUDIT.md Section 2 exists with: every multi-user-gate call site, ctx.currentUser pattern, all 4 existing Claude route line numbers, subscription-pattern survey, per-user dir mode bits + library, recommended insertion point + route names.</done>
</task>

<task type="auto">
  <name>Task 3: Discover UI integration point in ai-config.tsx + tests inventory + commit AUDIT.md</name>
  <files>.planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md</files>
  <action>
Run these commands and capture into AUDIT.md Sections 3 and 4.

**Section 3: UI Coordinates (for Plan 04)**

1. `grep -n "claudeStartLogin\|getClaudeStatus\|claudeLogout\|claudeSubmitCode\|isClaudeConnected\|claudeAuthMethod" livos/packages/ui/src/routes/settings/ai-config.tsx` — capture every line so Plan 04 knows exactly which JSX/state blocks to extend.

2. `wc -l livos/packages/ui/src/routes/settings/ai-config.tsx` — note total line count (~547) so Plan 04 sizes its scope correctly.

3. `grep -n "Sign in with Claude\|Connected to Claude\|Authenticated via\|Claude.*sub" livos/packages/ui/src/routes/settings/ai-config.tsx` — capture the user-visible text strings around the Claude section so Plan 04 can find the exact JSX block.

4. `grep -rn "claudeStartLogin\|claudePerUserStartLogin" livos/packages/ui/src/ | head -10` — confirm ai-config.tsx is the SOLE consumer (so Plan 04's UI edits stay confined to one file).

5. Document Plan 04's user-visible behavior contract per D-40-08:
   - When `multi_user_mode === false` (single-user): UI behavior unchanged. Existing PKCE OAuth flow (Nexus `/api/claude/start-login` via `claudeStartLogin` tRPC) keeps working.
   - When `multi_user_mode === true`: each logged-in user sees their own connection status. "Sign in" button calls Plan 03's new per-user route (e.g., `ai.claudePerUserStartLogin`), which spawns `claude login --no-browser` server-side with HOME set to that user's synthetic dir. UI displays the device code + verification URL emitted by the server-side subprocess.

**Section 4: Tests Inventory (for Plan 05)**

6. `cat nexus/packages/core/package.json | grep -A 5 '"test"' | head -20` — capture all test scripts.

7. `ls nexus/packages/core/src/providers/*.test.ts` — list existing test files (Phase 39's: claude.test.ts, no-authtoken-regression.test.ts, sdk-agent-runner-integrity.test.ts).

8. `grep -n "BASELINE_SHA\|2b3b005bf1594821be6353268ffbbdddea5f9a3a" nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts` — capture the exact constant Plan 02 must update + the comment Plan 02 must add per D-40-11.

9. `grep -rn "node:assert\|from 'node:assert" nexus/packages/core/src/providers/ livos/packages/livinityd/source/ | head -10` — confirm `node:assert/strict` is the canonical assertion library (per Phase 39 conventions; vitest is NOT in the project per planning context).

10. Write Plan 05's test contract:
    - **Sacred file behavior test** (`sdk-agent-runner-home-override.test.ts`): assert AgentConfig.homeOverride is honored when set (mock spawn / env probe), AND falls back to `process.env.HOME || '/root'` when unset (backward compat).
    - **Per-user dir integration test** (`per-user-claude-dir.test.ts` in livinityd): create dir for user A (mode 0700), confirm user B's dir is separate, confirm livinityd never reads A's dir from B's request context (honest framing per D-40-05).
    - **Updated integrity test**: BASELINE_SHA re-pinned to new post-Phase-40 SHA (Plan 02 does the re-pinning; Plan 05 confirms test passes against new SHA).
    - **Phase 39 regression preservation**: `npm run test:phase39` STILL passes (because integrity test now pins NEW baseline; no-authtoken-regression unaffected; claude.test.ts unaffected).

**Final step:**

Create `.planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md` with all 4 sections. Then commit:

```
git add .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md
git commit -m "$(cat <&lt;'EOF'
docs(40-01): codebase audit for per-user Claude OAuth + HOME isolation (FR-AUTH-01..03)

Read-only investigation producing concrete coordinates for Plans 02-05:
- Sacred file SHA confirmed at Phase 39 baseline; line 266 verified.
- Backend insertion point: livos/packages/livinityd/source/modules/ai/routes.ts.
- Multi-user gate: Redis key livos:system:multi_user === 'true'.
- UI anchor: livos/packages/ui/src/routes/settings/ai-config.tsx.
- Test conventions: node:assert/strict + tsx (vitest is NOT in project).

No source files modified.
EOF
)"
```
  </action>
  <verify>
    <automated>grep -q "Section 3" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; grep -q "Section 4" .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md &amp;&amp; git log --oneline -1 -- .planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md | grep -q "40-01"</automated>
  </verify>
  <done>AUDIT.md has all 4 sections. Committed via "docs(40-01): ..." with no source file modifications. Plans 02-05 have everything they need.</done>
</task>

</tasks>

<verification>
- AUDIT.md exists at `.planning/phases/40-per-user-claude-oauth-home-isolation/40-AUDIT.md`.
- All 4 sections present (Sections 1-4 mapping to Plans 02-05).
- `git status` shows ONLY the new AUDIT.md committed; zero source files touched.
- `git log -1 --name-only` shows only AUDIT.md in the commit.
- Sacred file SHA still matches `2b3b005bf1594821be6353268ffbbdddea5f9a3a` (this plan does not modify it).
</verification>

<success_criteria>
- ROADMAP success criteria 1-4 are NOT YET satisfied — this plan only produces the audit. Plans 02-05 satisfy them.
- D-40-01 (sacred relaxation) acknowledged in AUDIT Section 1: ready for Plan 02 to perform the surgical edit.
- D-40-04 (synthetic dir path `/opt/livos/data/users/&lt;user_id&gt;/.claude/`) recorded in AUDIT Section 2.
- D-40-12 (multi-user gate Redis key + read pattern) recorded in AUDIT Section 2.
- Phase 39 sacred-file SHA still intact (this plan is read-only).
</success_criteria>

<output>
After completion, create `.planning/phases/40-per-user-claude-oauth-home-isolation/40-01-SUMMARY.md` summarizing:
- Sacred file SHA verification result (still `2b3b005bf...`?)
- Confirmed line number for HOME (expected 266 per CONTEXT)
- Confirmed AgentConfig interface location in agent.ts
- Recommended new tRPC route names + insertion line in ai/routes.ts
- Multi-user gate canonical pattern (one-line snippet)
- ai-config.tsx anchor (line range of Claude card)
- Test conventions (node:assert/strict + tsx)
- Plans 02-05 are now unblocked
</output>
