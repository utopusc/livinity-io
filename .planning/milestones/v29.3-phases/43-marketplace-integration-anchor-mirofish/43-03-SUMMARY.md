# Plan 43-03 Summary — MiroFish Manifest Draft

**Plan:** 43-03
**Phase:** 43 — Marketplace Integration (Anchor: MiroFish)
**Status:** COMPLETE
**Requirement:** FR-MARKET-02

## Files Created

| File | Lines | Purpose |
|------|-------|---------|
| `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml` | 28 | MiroFish AppManifest with `requiresAiProvider: true` |
| `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml` | 13 | Compose stanza referencing future image `ghcr.io/utopusc/mirofish:v29.3` (port 3000) |
| `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md` | 107 | Operator copy-and-PR flow (Path B fork+build commands + sibling-repo PR + Mini PC auto-pull verification) |

## Image Reference

- **Path B (fork+build)** — chosen per Plan 43-01 audit Section 4
- Image string: `ghcr.io/utopusc/mirofish:v29.3` (FUTURE — operator publishes before sibling-repo PR merge)
- Web port: `3000` (frontend, per upstream Dockerfile EXPOSE)
- Backend port `5001` is internal-only (not exposed)

## Manifest Field Coverage

All required AppManifest fields included:
- manifestVersion: 1.0.0
- id: mirofish
- name: MiroFish
- tagline: Open-source AI prediction engine powered by your Claude subscription
- category: ai-agents
- version: 1.0.0
- port: 3000
- description (multi-line)
- website: https://github.com/666ghj/MiroFish
- support: https://github.com/666ghj/MiroFish/issues
- gallery: []

Optional fields:
- developer: 666ghj
- submitter: utopusc
- submission: https://github.com/utopusc/livinity-apps
- repo: https://github.com/666ghj/MiroFish
- releaseNotes (multi-line)
- **requiresAiProvider: true** ← the Phase 43 anchor flag

## Compose Stanza

The draft compose intentionally OMITS:
- `environment:` (broker env vars are auto-injected at install time by `inject-ai-provider.ts`)
- `extra_hosts:` (auto-injected at install time)
- `container_name:` (per-user name auto-generated by installForUser at apps.ts:936)

This keeps the manifest AI-provider-agnostic per the Phase 43-02 contract.

## Sacred File + Source Tree Verification

```
$ git hash-object nexus/packages/core/src/sdk-agent-runner.ts
623a65b9a50a89887d36f770dcd015b691793a7f
$ git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l
0
$ git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l
0
$ git diff HEAD -- livos/ | wc -l
0
$ git diff HEAD -- nexus/ | wc -l
0
```

This plan touches NO LivOS source and NO Nexus source. Pure documentation deliverable.

## Operator Copy-and-PR Workflow

Documented in `draft-mirofish-manifest/README.md`:
1. Build + push image to GHCR (`ghcr.io/utopusc/mirofish:v29.3`)
2. Clone sibling repo, copy 2 manifest files into `mirofish/` subdir, commit, push, open PR
3. Merge PR
4. Wait ≤ 5 min for Mini PC auto-pull (or force `sudo systemctl restart livos`)
5. Verify in marketplace UI + run Plan 43-05 UAT
