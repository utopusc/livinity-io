---
phase: 43-marketplace-integration-anchor-mirofish
plan: 03
type: execute
wave: 3
depends_on: [43-01, 43-02]
files_modified:
  - .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml
  - .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml
  - .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md
autonomous: true
requirements:
  - FR-MARKET-02
must_haves:
  truths:
    - "MiroFish manifest exists at audit-determined draft path with all standard AppManifest required fields"
    - "Manifest declares `requiresAiProvider: true` (D-43-09)"
    - "Compose stanza references the image source decided in 43-AUDIT.md (Path A: published image; Path B: fork+build target)"
    - "Manifest passes `AppManifestSchema.safeParse()` (validated in Plan 43-04)"
    - "Manifest is in this repo's `.planning/` (REVIEWABLE artifact); operator-controlled sibling-repo PR is the deployment step (NOT executed by Claude)"
    - "MiroFish source code is NOT modified (D-43-11) — only manifest authoring"
    - "Sacred file SHA `623a65b9...` byte-identical; broker module byte-identical"
  artifacts:
    - path: ".planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml"
      provides: "Phase 43's MiroFish manifest with requiresAiProvider: true and all standard AppManifest fields"
      contains: "requiresAiProvider: true"
    - path: ".planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml"
      provides: "Compose stanza for MiroFish (image from audit Path A or B; port matching upstream HTTP port)"
      contains: "image:"
    - path: ".planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md"
      provides: "Operator instructions: where to copy these files in the sibling repo, how to PR, expected merge → Mini PC propagation flow (5-min auto-pull)"
      min_lines: 50
  key_links:
    - from: ".planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml"
      to: ".planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml"
      via: "manifest port matches compose service exposed port"
      pattern: "port:"
---

<objective>
Author the MiroFish marketplace manifest as a REVIEWABLE DRAFT inside this repo's `.planning/` directory. Per audit decision (Plan 43-01 Section 3), the actual marketplace store lives in the sibling repo `https://github.com/utopusc/livinity-apps.git` — the operator (user) commits + pushes to that repo separately. This plan produces the verbatim file contents in a draft directory + a README explaining the copy-and-PR flow.

Purpose: deliver the anchor app manifest (FR-MARKET-02) without modifying LivOS source for marketplace content (respects two-repo separation) AND without forcing the operator to author YAML by hand. The draft files are byte-identical to what should land in the sibling repo at `livinity-apps/mirofish/`.

Output: 3 files in a new draft directory + 1 atomic commit. Sacred file + broker module untouched. MiroFish UPSTREAM source code is NOT modified — only manifest + compose authoring per D-43-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-marketplace-integration-anchor-mirofish/43-CONTEXT.md
@.planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md
@.planning/phases/43-marketplace-integration-anchor-mirofish/43-01-SUMMARY.md
@.planning/phases/43-marketplace-integration-anchor-mirofish/43-02-SUMMARY.md
@livos/packages/livinityd/source/modules/apps/schema.ts
@livos/packages/livinityd/source/modules/apps/builtin-apps.ts
@livos/packages/livinityd/source/constants.ts

<interfaces>
<!-- Established contracts this plan must satisfy. -->

AppManifestSchema required fields (livos/packages/livinityd/source/modules/apps/schema.ts:26-63):
  - manifestVersion: semver-valid string
  - id: string
  - name: string
  - tagline: string
  - category: string
  - version: string
  - port: integer
  - description: string
  - website: valid URL
  - support: string
  - gallery: string[]

AppManifestSchema optional fields used by MiroFish:
  - icon: string (URL to icon SVG/PNG)
  - developer: string
  - submitter: string
  - submission: URL
  - repo: URL (GitHub repo URL)
  - releaseNotes: string
  - dependencies: string[]
  - permissions: string[]
  - defaultUsername: string
  - defaultPassword: string
  - **requiresAiProvider: boolean** (NEW from Plan 43-02 — set true here)

Sibling repo path Plan 43-01's audit pinned (default repo from constants.ts:2):
  https://github.com/utopusc/livinity-apps.git
  → cloned at runtime to `<dataDir>/app-stores/<cleanUrl>/`
  → expected layout per app: `<repoRoot>/<app_id>/livinity-app.yml`, `<repoRoot>/<app_id>/docker-compose.yml`

MiroFish UPSTREAM source (NOT modified — D-43-11):
  https://github.com/666ghj/MiroFish

MiroFish image reference: TO BE READ FROM 43-AUDIT.md Section 4 (Path A or Path B).
  - Path A example: `ghcr.io/666ghj/mirofish:latest`
  - Path B example: `ghcr.io/utopusc/mirofish:v29.3` (fork + rebuild target)

Sacred file (NOT modified): `nexus/packages/core/src/sdk-agent-runner.ts` baseline `623a65b9a50a89887d36f770dcd015b691793a7f`
Broker module (NOT modified): `livos/packages/livinityd/source/modules/livinity-broker/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read audit decisions, draft MiroFish manifest + compose + README, atomic commit</name>
  <files>.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml, .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml, .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md</files>
  <action>
**Step 1 — Read audit decisions:**

Read `.planning/phases/43-marketplace-integration-anchor-mirofish/43-AUDIT.md` Sections 3 + 4. Extract:
- The chosen MiroFish image reference (Path A: published image string OR Path B: fork+build target string)
- The MiroFish HTTP port (from upstream README — audit may or may not have pinned this; if absent, default to 7860 which is the most common Gradio/web-app port; document the chosen value)
- Whether the upstream image needs a build context or runs from registry image alone

If the audit chose Path B (fork+build) and the rebuilt image isn't published yet, the manifest still gets authored — it points to the FUTURE image reference (e.g., `ghcr.io/utopusc/mirofish:v29.3`). The README documents the operator's pre-merge step: build + push that image BEFORE the sibling-repo PR is merged. This keeps the planning artifact self-contained.

**Step 2 — Create draft directory + manifest:**

`mkdir -p .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest`

Create `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml`:

```yaml
manifestVersion: 1.0.0
id: mirofish
name: MiroFish
tagline: Open-source AI agent powered by your Claude subscription
category: ai-agents
version: 1.0.0
port: <PORT_FROM_AUDIT_OR_7860>
description: >-
  MiroFish is an open-source AI agent runtime. On Livinity it runs on your own
  hardware and uses your personal Claude subscription via the Livinity AI
  broker — no API key, no extra billing, no cloud dependency.
website: https://github.com/666ghj/MiroFish
developer: 666ghj
submitter: utopusc
submission: https://github.com/utopusc/livinity-apps
repo: https://github.com/666ghj/MiroFish
support: https://github.com/666ghj/MiroFish/issues
gallery: []
releaseNotes: |
  Initial Livinity packaging. AI provider auto-configured to use your Claude
  subscription via the Livinity AI broker (livinity-broker:8080). No API key
  entry required.
requiresAiProvider: true
```

Notes for the executor:
- Replace `<PORT_FROM_AUDIT_OR_7860>` with the actual port from audit Section 4 (or 7860 if audit did not pin one — document the choice in the README).
- `gallery: []` is acceptable per AppManifestSchema (it accepts an empty array). If the audit found gallery image URLs in upstream README/docs, list them; otherwise leave empty.
- Do NOT add `defaultUsername` / `defaultPassword` — MiroFish does not have built-in auth (the AI provider IS the auth).
- `requiresAiProvider: true` is the entire point of this manifest — verify it is the LAST top-level key for visual prominence in code review.

**Step 3 — Create the compose stanza:**

Create `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml`:

For Path A (published image — audit found one):
```yaml
version: "3.7"
services:
  server:
    image: <IMAGE_REF_FROM_AUDIT_PATH_A>
    restart: unless-stopped
    ports:
      - "127.0.0.1:<PORT>:<PORT>"
    volumes:
      - "${APP_DATA_DIR}/data:/app/data"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:<PORT>/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
```

For Path B (fork+build — audit chose this):
```yaml
version: "3.7"
services:
  server:
    image: <FUTURE_IMAGE_REF_FROM_AUDIT_PATH_B>
    restart: unless-stopped
    ports:
      - "127.0.0.1:<PORT>:<PORT>"
    volumes:
      - "${APP_DATA_DIR}/data:/app/data"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:<PORT>/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
```

Notes:
- Replace `<PORT>` (3 places) with the same port value from the manifest.
- Replace `<IMAGE_REF...>` with the verbatim image string from audit Section 4.
- Do NOT add `environment:` or `extra_hosts:` — Phase 43-02's `injectAiProviderConfig` adds those at install time when `requiresAiProvider: true`. Adding them in the source compose would either (a) duplicate (harmless but ugly) or (b) hardcode `<userId>` (broken). Leave the compose AI-provider-agnostic.
- Do NOT add `container_name:` — `installForUser` (apps.ts:936) generates the per-user name automatically.
- The `${APP_DATA_DIR}/data` volume path is the LivOS convention (see `livos/packages/livinityd/source/modules/apps/builtin-apps.ts:106` n8n example). `installForUser` resolves `${APP_DATA_DIR}` at install time (apps.ts:907).
- The healthcheck is conservative; tune `start_period` upward if MiroFish has a slow boot. The audit may have pinned this; if not, 60s is a safe default for an AI agent runtime.

**Step 4 — Create the operator README:**

Create `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md`:

```markdown
# MiroFish Marketplace Manifest — Draft

**Phase:** 43 — Marketplace Integration (Anchor: MiroFish)
**Authored by:** Plan 43-03 (executor)
**Owner of next step:** Operator (user). Files in this directory are READY-TO-COPY into the sibling repo `https://github.com/utopusc/livinity-apps`.

## Why this lives here, not in the sibling repo

Per Plan 43-01 audit Section 3: marketplace manifests live in the sibling repo `utopusc/livinity-apps`, NOT in the LivOS source tree. This directory is a REVIEWABLE DRAFT so the manifest content is visible in this repo's PR; the operator separately commits + pushes to the sibling repo as the actual deployment step.

## Files in this directory

- `livinity-app.yml` — MiroFish manifest with `requiresAiProvider: true`
- `docker-compose.yml` — MiroFish compose stanza (broker env injection happens at install time, NOT here)
- `README.md` — this file

## What the operator does to deploy

### Step 1 — Build + publish image (only if audit chose Path B fork+build)

If Plan 43-01's audit Section 4 chose **Path B** (image not published upstream), the operator must first publish the rebuilt image:

```bash
# On a machine with Docker
git clone https://github.com/666ghj/MiroFish /tmp/mirofish
cd /tmp/mirofish
docker build -t <IMAGE_REF_FROM_DOCKER_COMPOSE> .
docker push <IMAGE_REF_FROM_DOCKER_COMPOSE>
```

If the audit chose **Path A** (image already published), skip this step.

### Step 2 — Sibling-repo PR

```bash
git clone https://github.com/utopusc/livinity-apps /tmp/livinity-apps
mkdir /tmp/livinity-apps/mirofish
cp .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml /tmp/livinity-apps/mirofish/
cp .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml /tmp/livinity-apps/mirofish/
cd /tmp/livinity-apps
git checkout -b add-mirofish-v29.3
git add mirofish/
git commit -m "feat: add MiroFish marketplace app (subscription-powered via livinity-broker)"
git push origin add-mirofish-v29.3
# Open PR on GitHub; merge to default branch
```

### Step 3 — Wait for Mini PC auto-pull (≤ 5 min)

The Mini PC's `app-store.ts:56` runs `update()` every 5 minutes (`updateInterval: '5m'`). After the sibling-repo PR is merged into the default branch, MiroFish appears in the marketplace UI within 5 minutes. No livinityd restart required.

To force-pull immediately (impatient operator path):
```bash
ssh -i .../minipc bruce@10.69.31.68
sudo systemctl restart livos
# Or trigger via tRPC: livinityd.client.appStore.update.mutate()
```

### Step 4 — Verify the manifest is live

In the LivOS UI, open the marketplace and confirm:
- MiroFish card appears
- The "Uses your Claude subscription" pill is rendered (Plan 43-04 wires this UI)
- "Install" button is functional

## Image reference chosen

| Path | Reference | Source |
|------|-----------|--------|
| Chosen by audit | `<IMAGE_REF_FROM_AUDIT>` | Plan 43-01 Section 4 |

## Port chosen

| Source | Port |
|--------|------|
| Upstream README / Plan 43-01 audit | `<PORT_FROM_AUDIT>` (default 7860 if audit unable to pin) |

## What is NOT in this directory

- The 3 broker env vars (`ANTHROPIC_BASE_URL`, `ANTHROPIC_REVERSE_PROXY`, `LLM_BASE_URL`) — these are added at install time by `livos/packages/livinityd/source/modules/apps/inject-ai-provider.ts` (Plan 43-02). Hardcoding them in compose would either duplicate (if `requiresAiProvider: true`) or break (if `<userId>` were hardcoded).
- `extra_hosts: ["livinity-broker:host-gateway"]` — same reason. Auto-injected at install time.
- `container_name:` — `installForUser` (apps.ts:936) generates the per-user name `mirofish_server_user_<username>_1` automatically.

## Sacred file integrity

This plan does NOT modify any source code. Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA `623a65b9a50a89887d36f770dcd015b691793a7f` is byte-identical. Broker module `livos/packages/livinityd/source/modules/livinity-broker/` byte-identical. MiroFish UPSTREAM source code (per D-43-11) is NOT modified.
```

**Step 5 — Verify integrity (sacred + broker freeze):**

Run: `git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l` → must be `0`
Run: `git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l` → must be `0`
Run: `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` → must be `623a65b9a50a89887d36f770dcd015b691793a7f`
Run: `git diff HEAD -- livos/ | wc -l` → must be `0` (this plan touches NO LivOS source)
Run: `git diff HEAD -- nexus/ | wc -l` → must be `0` (this plan touches NO Nexus source)

**Step 6 — Atomic commit:**

```
git add .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/
git commit -m "docs(43-03): MiroFish marketplace manifest draft (FR-MARKET-02)"
```
  </action>
  <verify>
    <automated>test -f .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml && test -f .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml && test -f .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/README.md && grep -q "requiresAiProvider: true" .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml && grep -q "id: mirofish" .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/livinity-app.yml && grep -q "image:" .planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/docker-compose.yml && [ "$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)" = "623a65b9a50a89887d36f770dcd015b691793a7f" ] && [ "$(git diff HEAD -- nexus/packages/core/src/sdk-agent-runner.ts | wc -l)" = "0" ] && [ "$(git diff HEAD -- livos/packages/livinityd/source/modules/livinity-broker/ | wc -l)" = "0" ] && [ "$(git diff HEAD -- livos/ | wc -l)" = "0" ] && [ "$(git diff HEAD -- nexus/ | wc -l)" = "0" ]</automated>
  </verify>
  <done>3 draft files exist with verbatim contents; manifest declares `requiresAiProvider: true`; compose references the audit-chosen image; README documents the operator's copy-and-PR flow including the Path A/B build step; sacred file SHA byte-identical; broker module + ALL LivOS source byte-identical (this plan is `.planning/`-only); one atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketplace manifest → user install | Untrusted: a published manifest with `requiresAiProvider: true` triggers broker env injection on install. The MiroFish manifest is owned by Livinity (`utopusc/livinity-apps` repo); upstream MiroFish source is NOT modified per D-43-11. |
| Image registry → user container | Untrusted: the chosen image runs as the user's app. If Path B (fork+build), Livinity controls the build pipeline and can verify the image content. If Path A (upstream image), Livinity trusts upstream `666ghj/MiroFish`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Tampering | upstream MiroFish image content | accept (Path A) / mitigate (Path B) | Path A: trust upstream `666ghj/MiroFish` and document the SHA digest in the manifest's `image:` (operator action — pin tag to immutable digest if security paranoia warrants). Path B: Livinity-controlled build pipeline; image content verified at build time. |
| T-43-03-02 | Information disclosure | manifest checked into public sibling repo | accept | Manifest contains no secrets — only the broker URL pattern (host-internal) and image reference (public). All sensitive bits (user_id, OAuth credentials) are runtime-injected per-user. |
| T-43-03-03 | Tampering | sacred file | mitigate | Step 5 + automated `<verify>` re-checks SHA + zero diff for nexus/ AND livos/. |
</threat_model>

<verification>
- 3 files exist in `.planning/phases/43-marketplace-integration-anchor-mirofish/draft-mirofish-manifest/`
- `livinity-app.yml` contains `requiresAiProvider: true` and `id: mirofish`
- `docker-compose.yml` contains an `image:` line referencing the audit-chosen image string
- README.md ≥ 50 lines, documents operator copy-and-PR flow, mentions the 5-min auto-pull window
- Sacred file SHA `623a65b9a50a89887d36f770dcd015b691793a7f` byte-identical
- `git diff HEAD -- livos/` produces zero output (no LivOS source modified)
- `git diff HEAD -- nexus/` produces zero output (no Nexus source modified)
- One atomic commit
</verification>

<success_criteria>
- The MiroFish manifest is reviewable in this repo's PR (operator can sanity-check before propagating to sibling repo)
- The manifest cleanly satisfies AppManifestSchema (verified by Plan 43-04's schema test)
- The compose stanza is AI-provider-agnostic — broker env injection happens at install time per the Phase 43-02 contract
- MiroFish UPSTREAM source code is unmodified per D-43-11
- Sacred file + broker module + ALL livos/ + ALL nexus/ unchanged (this plan is documentation-only)
</success_criteria>

<output>
After completion, create `.planning/phases/43-marketplace-integration-anchor-mirofish/43-03-SUMMARY.md` with: commit SHA, the 3 file paths, the chosen image reference (Path A or B), the chosen port, sacred file SHA verification, and explicit confirmation that the operator's copy-and-PR workflow is documented in the draft README.
</output>
