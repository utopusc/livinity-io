---
phase: 28-system-prompt-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nexus/packages/core/src/agent.ts
  - nexus/packages/core/src/daemon.ts
autonomous: true
requirements: [SPRT-01, SPRT-02, SPRT-03]

must_haves:
  truths:
    - "NATIVE_SYSTEM_PROMPT is measurably shorter (target: <150 lines, <2000 tokens, down from 221 lines / ~3214 tokens)"
    - "Tool descriptions in daemon.ts are shortened to one-line essentials (no multi-sentence explanations)"
    - "System prompt contains a Self-Awareness section telling the AI what it can do, cannot do, and when to escalate"
    - "All existing behavioral instructions are preserved (Computer Use, Messaging, Memory, Self-Improvement, Scheduling, Self-Eval, Domain/Caddy, Browser Safety)"
    - "nexus-core builds without errors after changes"
  artifacts:
    - path: "nexus/packages/core/src/agent.ts"
      provides: "Optimized NATIVE_SYSTEM_PROMPT and cleaned-up legacy AGENT_SYSTEM_PROMPT"
      contains: "Self-Awareness"
    - path: "nexus/packages/core/src/daemon.ts"
      provides: "Shortened tool descriptions for all 43 registered tools"
  key_links:
    - from: "nexus/packages/core/src/agent.ts"
      to: "NATIVE_SYSTEM_PROMPT"
      via: "template literal function"
      pattern: "NATIVE_SYSTEM_PROMPT.*canSpawnSubagent"
    - from: "nexus/packages/core/src/daemon.ts"
      to: "toolRegistry.register"
      via: "description field in each register call"
      pattern: "description:"
---

<objective>
Optimize the agent system prompt and tool descriptions for conciseness and context-window efficiency, and add self-awareness instructions.

Purpose: The system prompt currently consumes ~3,214 tokens with redundant sections (Tool Overview duplicates native tool definitions) and verbose explanations. Tool descriptions add ~2,518 tokens of often-redundant text. Condensing these frees context window for actual conversation while maintaining all behavioral guidance. Adding self-awareness prevents the AI from attempting tasks beyond its capabilities.

Output: Optimized agent.ts (NATIVE_SYSTEM_PROMPT <150 lines) and daemon.ts (shortened tool descriptions)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@nexus/packages/core/src/agent.ts
@nexus/packages/core/src/daemon.ts
@nexus/packages/core/src/tool-registry.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From nexus/packages/core/src/agent.ts:
```typescript
// NATIVE_SYSTEM_PROMPT is a template literal function (line 236)
const NATIVE_SYSTEM_PROMPT = (canSpawnSubagent: boolean) => `...`;

// AGENT_SYSTEM_PROMPT is the deprecated legacy ReAct prompt (line 163)
const AGENT_SYSTEM_PROMPT = (toolDescriptions: string, canSpawnSubagent: boolean) => `...`;

// Both are used in AgentLoop.run() (line 576-581):
if (this.config.systemPromptOverride) {
  systemPrompt = this.config.systemPromptOverride;
} else if (useNativeTools && nativeTools) {
  systemPrompt = NATIVE_SYSTEM_PROMPT(canSpawnSubagent);
} else {
  systemPrompt = AGENT_SYSTEM_PROMPT(toolDescriptions, canSpawnSubagent);
}
```

From nexus/packages/core/src/daemon.ts:
```typescript
// Tool registrations use this pattern (43 tools total, line 1311+):
toolRegistry.register({
  name: 'tool_name',
  description: 'Tool description string',  // <-- these get shortened
  parameters: [
    { name: 'param', type: 'string', description: 'Param description', required: true },
  ],
  execute: async (params) => { ... },
});
```

From nexus/packages/core/src/tool-registry.ts:
```typescript
// Tool descriptions flow into native tool definitions via:
toToolDefinitionsFiltered(policy?: ToolPolicy): ToolDefinition[]
// Which creates { name, description, input_schema: { properties, required } }
// Parameter descriptions also flow into input_schema.properties[name].description
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Optimize NATIVE_SYSTEM_PROMPT and add Self-Awareness section</name>
  <files>nexus/packages/core/src/agent.ts</files>
  <read_first>
    - nexus/packages/core/src/agent.ts (lines 236-456 for NATIVE_SYSTEM_PROMPT)
    - nexus/packages/core/src/agent.ts (lines 162-234 for legacy AGENT_SYSTEM_PROMPT)
  </read_first>
  <action>
Rewrite NATIVE_SYSTEM_PROMPT (lines 236-456) to be concise and context-efficient. Target: under 150 lines, under 2000 tokens (down from 221 lines / ~3214 tokens). Per SPRT-01 and SPRT-03.

**Specific changes:**

1. **Remove "Tool Overview" section entirely** (lines 238-254, ~17 lines). When using native tool calling, tool names and descriptions are already sent as structured tool definitions. This section is 100% redundant. The AI already knows what tools it has from the tool definitions.

2. **Condense "Computer Use" section** (lines 256-291, ~36 lines). Keep the essential workflow but cut verbose explanations. Target: ~18 lines. Preserve these critical rules:
   - Elements-first workflow (screen_elements before screenshots)
   - raw:true for element coords, no raw:true for screenshot coords
   - Center of UI elements, click-then-type pattern
   - Fall back to screenshots only when elements return 0 or need visual context
   Merge "Important Guidelines" into the workflow steps instead of a separate subsection. Remove the "Screenshot Coordinate System" paragraph (the agent handles DPI conversion automatically — just say "coordinates auto-scale").

3. **Condense "Messaging Context" section** (lines 293-303, ~11 lines). Target: ~5 lines. Merge the two paragraphs into one concise block: channel indicated by [Channel: ...] prefix, use correct tool per channel, whatsapp_send for specific contacts.

4. **Remove "How You Work" section** (lines 305-311, ~7 lines). This is obvious when using native tool calling — the model knows how to call tools.

5. **Condense "Sub-agent Guidance"** (lines 312-316). Keep the 3 bullet points but make them shorter (one line each, no dashes before colons).

6. **Condense "Rules" section** (lines 318-324). Merge into 3 rules max: (1) Think then act, one tool per turn, (2) If tool fails try different approach, (3) Be concise in final answer.

7. **Keep "Browser Safety" as-is** (lines 326-331, 4 lines). Already concise and critical.

8. **Condense "Memory" section** (lines 333-337). One line: "Use memory_search to recall past knowledge before answering. Use memory_add to save important facts/preferences."

9. **Condense "Self-Improvement" section** (lines 339-361, ~23 lines). Target: ~10 lines. Cut the numbered step-by-step formats. Keep: (a) skill_generate for recurring capabilities, (b) mcp_registry_search + mcp_install for external tools, (c) When to act vs ask (one line each).

10. **Condense "Autonomous Scheduling" section** (lines 363-387, ~25 lines). Target: ~8 lines. Cut the example cron expressions (the AI knows cron syntax). Keep: trigger words ("every", "regularly", "monitor"), subagent_create with schedule or loop, check subagent_list first, ask user if unsure.

11. **Condense "Self-Evaluation" section** (lines 389-408, ~20 lines). Target: ~8 lines. Cut the "What to Evaluate" list (redundant with "When to Take Action"). Keep only the action triggers.

12. **Condense "Domain & Caddy Configuration" section** (lines 410-456, ~47 lines). Target: ~15 lines. Remove the Caddyfile structure example (the AI can `cat /etc/caddy/Caddyfile` to see it). Remove shell commands list (the AI knows these). Keep: Caddy as reverse proxy, 3 modes (IP/direct/tunnel), subdomain routing basics, "use tRPC API for changes, don't manually edit Caddyfile".

13. **Add new "Self-Awareness" section** (per SPRT-03). Place it after Rules, before Browser Safety. ~10 lines:
```
## Self-Awareness

You are Nexus, running on the user's Linux server. Know your boundaries:

**Can do**: Execute shell commands, manage Docker/PM2, read/write files, search the web, send messages, create skills/schedules, control connected devices, render canvas artifacts, manage email.
**Cannot do**: Access the internet without tools (no browsing in your head), modify your own code, access other users' servers, make purchases or financial transactions, access systems without configured credentials.
**Escalate to user when**: Task requires credentials you don't have, action is irreversible and high-impact (e.g., deleting production data), multiple valid approaches exist and user preference is unclear, task involves personal/sensitive decisions.
**Never assume**: Don't guess passwords or API keys. Don't assume services are running — check first. Don't assume file contents — read first.
```

14. **Also condense AGENT_SYSTEM_PROMPT** (the deprecated legacy prompt, lines 163-234) proportionally — apply same optimizations. This ensures consistency if the fallback path is ever triggered.

**What to preserve exactly (do NOT change these):**
- The function signature: `const NATIVE_SYSTEM_PROMPT = (canSpawnSubagent: boolean) => \`...\`;`
- The opening line: "You are Nexus, an autonomous AI assistant..."
- The `${canSpawnSubagent ? ... : ''}` conditional blocks (sub-agent guidance)
- All behavioral rules (just make them shorter)
- The template literal backtick syntax

**CRITICAL**: This is a pure text optimization. Do NOT change any TypeScript code outside the two prompt template literals. Do NOT modify the AgentLoop class, resolveAgentConfig, or any other functions.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/nexus && npm run build --workspace=packages/core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NATIVE_SYSTEM_PROMPT is under 150 lines (grep -c '' on the extracted prompt)
    - The string "Self-Awareness" appears in agent.ts
    - The string "Cannot do" appears in agent.ts (self-awareness capabilities section)
    - The string "Escalate to user" appears in agent.ts
    - "Tool Overview" section heading is removed from NATIVE_SYSTEM_PROMPT
    - "How You Work" section heading is removed from NATIVE_SYSTEM_PROMPT
    - All section headings preserved: "Computer Use", "Messaging", "Rules", "Browser Safety", "Memory", "Self-Improvement", "Autonomous Scheduling", "Self-Evaluation", "Domain & Caddy"
    - nexus-core builds without errors
  </acceptance_criteria>
  <done>NATIVE_SYSTEM_PROMPT is under 150 lines and under 2000 tokens. Contains Self-Awareness section with capabilities, limits, and escalation rules. All essential behavioral instructions preserved in condensed form. Legacy AGENT_SYSTEM_PROMPT also condensed. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Shorten tool descriptions in daemon.ts</name>
  <files>nexus/packages/core/src/daemon.ts</files>
  <read_first>
    - nexus/packages/core/src/daemon.ts (lines 1311-1370 for first few tool registrations pattern)
    - nexus/packages/core/src/daemon.ts (lines 1595-1640 for scrape/whatsapp pattern)
    - nexus/packages/core/src/daemon.ts (lines 2810-2870 for canvas pattern)
  </read_first>
  <action>
Shorten all tool `description` strings and parameter `description` strings in daemon.ts to essential information only. Per SPRT-02.

**Rules for shortening:**
- Tool descriptions: One sentence max. State what it does, not how or why. Remove instructional phrases like "Use this to...", "Use this when...", "Returns...".
- Parameter descriptions: Under 10 words. Just name the parameter's purpose. Remove examples, defaults restated from the default field, and format details.
- Do NOT change tool `name`, `execute` logic, parameter `name`/`type`/`required`/`enum`/`default` fields.

**Specific tool description rewrites (all 43 tools):**

Core tools:
- `status`: "Get daemon health: uptime, containers, cycles" -> shorten from current
- `logs`: "Read daemon log file" (drop "the last N lines from the Nexus daemon log file")
- `shell`: "Execute a shell command on the server" (drop ". Supports any Linux command. Dangerous commands are blocked.")
- `docker_list`: "List Docker containers and their state"
- `docker_manage`: "Start, stop, restart, inspect, or get logs for a Docker container"
- `docker_exec`: "Execute a command inside a Docker container"
- `pm2`: "Manage PM2 processes: list, restart, stop, start, reload, logs, status"
- `sysinfo`: "Get system resource info: CPU, RAM, disk, network"
- `files`: "File operations: read, write, list, stat, delete, mkdir"
- `cron`: "Schedule a delayed task for later execution" (drop the 2-sentence explanation about re-entering inbox)
- `scrape`: "Fetch a URL and return content as markdown" (drop "using Firecrawl. Use this for reading web pages, extracting text, or analyzing websites.")

Messaging tools:
- `whatsapp_send`: "Send a WhatsApp message to a contact by name" (drop the 2-sentence instructional text)
- `channel_send`: "Send a message to a Telegram, Discord, Slack, or Matrix channel" (drop "Use this when..." and "Defaults to...")

Memory tools:
- `memory_search`: "Search long-term memory for stored knowledge"
- `memory_add`: "Store information in long-term memory"

Search/state tools:
- `web_search`: "Search Google and return results as markdown" (drop "Use this to research topics, find documentation, discover tools, or gather information before executing tasks.")
- `task_state`: "Persistent key-value store for multi-phase workflows"
- `progress_report`: "Send a progress update to user during long tasks"

Agent tools:
- `subagent_create`: "Create a persistent subagent with role, tools, and optional schedule/loop"
- `subagent_list`: "List all subagents with status, schedule, and run history"
- `subagent_message`: "Send a task to a subagent for execution"
- `subagent_schedule`: "Manage subagent cron schedules: add, remove, list"
- `skill_generate`: "Generate a new AI skill file from a description"
- `loop_manage`: "Manage subagent loops: start, stop, list, status"

MCP tools:
- `mcp_registry_search`: "Search the MCP Registry for available servers"
- `mcp_install`: "Install an MCP server from registry or manually"
- `mcp_list`: "List installed MCP servers with status and tools"
- `mcp_manage`: "Manage an MCP server: restart, enable, disable, remove"
- `mcp_config`: "Get or set raw JSON config for MCP servers"

Webhook tools:
- `webhook_create`: "Create a webhook endpoint with HMAC secret"
- `webhook_list`: "List registered webhook endpoints"
- `webhook_delete`: "Delete a webhook endpoint by ID"

Gmail tools:
- `gmail_read`: "Read a specific email by ID"
- `gmail_reply`: "Reply to an email thread"
- `gmail_send`: "Compose and send a new email"
- `gmail_search`: "Search emails with Gmail query operators"
- `gmail_archive`: "Archive an email (remove from inbox)"

Session tools:
- `sessions_create`: "Spawn a sub-agent session for a specific task"
- `sessions_list`: "List active sub-agent sessions"
- `sessions_send`: "Send a message to a sub-agent session"
- `sessions_history`: "Read sub-agent session history and result"

Canvas tools:
- `canvas_render`: Keep the type/format documentation (react, html, svg, mermaid, recharts) but remove the full examples. Shorten to: type descriptions only, one line each. Remove the 5 multi-line examples. The description should be ~8 lines max (down from ~18 lines).
- `canvas_update`: "Update content of an existing Live Canvas artifact"

**Parameter description rewrites — apply these patterns globally:**
- "The shell command to execute" -> "Shell command"
- "Timeout in milliseconds" -> "Timeout (ms)"
- "Number of log lines to return" -> "Lines to return"
- "Container name" stays (already short)
- "Operation to perform" stays (already short)
- "Process name (required for restart/stop/start/reload/logs)" -> "Process name"
- "What system info to retrieve" -> "Info topic"
- "File or directory path" -> "Path"
- "Content to write (required for write operation)" -> "Content to write"
- "Delay amount" stays
- "Time unit" stays
- "Task/command to execute when timer fires (goes through normal classification)" -> "Task to execute when timer fires"
- "The URL to scrape" -> "URL"
- "Output format" stays
- "Contact name (case-insensitive)" -> "Contact name"
- "Message text to send" -> "Message text"
- "Search query — what are you looking for?" -> "Search query"
- "Max results to return" stays
- "The information to remember" -> "Content to store"
- "Comma-separated tags for categorization (e.g. 'user_pref,server,config')" -> "Comma-separated tags"
- "Where this info came from (e.g. 'whatsapp', 'agent', 'user')" -> "Source identifier"
- "The search query" -> "Search query"
- "Max results to return (1-10)" -> "Max results (1-10)"
- All "JSON string data to save (required for save operation)" -> "JSON data to save"
- "Time-to-live in seconds (default: 86400 = 24h)" -> "TTL in seconds"
- "Progress message to send" -> "Progress message"
- "WhatsApp JID to send to (auto-detected if from WhatsApp context)" -> "WhatsApp JID (auto-detected)"
- Long subagent_create param descriptions: shorten each to under 10 words
- Long MCP param descriptions: shorten each to under 10 words
- Canvas: "The source code..." multi-sentence -> "Source code for the artifact"
- Canvas: "The artifact ID from a previous canvas_render call" -> "Artifact ID"
- Canvas: "The complete updated source code (replaces the entire content)" -> "Updated source code"

**CRITICAL**: Do NOT modify any `execute` function bodies. Only change `description` string values in `toolRegistry.register({...})` calls and `{ name, type, description, ... }` parameter objects. Do NOT change tool names, parameter names, types, enums, defaults, or required flags.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/nexus && npm run build --workspace=packages/core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 43 tool descriptions are single sentences (no multi-sentence descriptions except canvas_render which can be up to 8 lines)
    - No tool description contains "Use this to" or "Use this when" or "Use this for"
    - Parameter descriptions are under 10 words each (with exception of enum-like clarifications)
    - Tool names unchanged: grep for all 43 tool names confirms they still exist
    - No execute function bodies modified
    - nexus-core builds without errors
  </acceptance_criteria>
  <done>All 43 tool descriptions shortened to one-line essentials. Parameter descriptions shortened to under 10 words. canvas_render type docs preserved but examples removed. Build succeeds. Total description token savings: target 40%+ reduction (from ~2518 tokens to ~1500 tokens).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd nexus && npm run build --workspace=packages/core` succeeds
2. NATIVE_SYSTEM_PROMPT line count < 150 (verify with node script extracting the prompt)
3. "Self-Awareness" section exists in agent.ts with "Can do", "Cannot do", "Escalate to user"
4. "Tool Overview" heading removed from NATIVE_SYSTEM_PROMPT
5. No tool description in daemon.ts exceeds 150 characters (except canvas_render)
6. All 43 tool names still present in daemon.ts
7. No TypeScript logic changed — only string literals modified
</verification>

<success_criteria>
1. NATIVE_SYSTEM_PROMPT is under 150 lines and under 2000 tokens (~38% reduction from 3214)
2. Tool descriptions total under 1500 tokens (~40% reduction from 2518)
3. Self-Awareness section present with capabilities, limits, and escalation rules
4. nexus-core compiles cleanly
5. No behavioral instructions lost — all sections preserved in condensed form
</success_criteria>

<output>
After completion, create `.planning/phases/28-system-prompt-optimization/28-01-SUMMARY.md`
</output>
