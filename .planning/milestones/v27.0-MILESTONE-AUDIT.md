---
milestone: v27.0
milestone_name: Docker Management Upgrade
audited: 2026-04-25
status: passed
scores:
  requirements: 33/33
  phases: 7/7
  integration: 8/8
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 17-docker-quick-wins
    items:
      - "xterm.js search addon wired but no-op (search input sets state, does not search log buffer) — TODO at container-detail-sheet.tsx:412 (deferred-feature, v28)"
      - "AI docker_manage stack-deploy does not expose secret:true env vars — secret store is livinityd-owned (deferred-feature, v28)"
  - phase: 18-container-file-browser
    items:
      - "No file preview / chmod UI / chunked upload — single-file textarea editor only, 1MB cap (deferred-feature, v28)"
      - "Volume delete recursive-confirm pattern reusable from FilesTab — not yet wired into volumes UI (deferred-feature, v28)"
  - phase: 19-compose-graph-vuln-scan
    items:
      - "Topological grid layout instead of dagre/elkjs — sufficient for ≤10-service stacks (deferred-feature, v28)"
      - "SBOM / license / Grype-alt-scanner deferred (deferred-feature, v28)"
      - "Pre-existing typecheck noise (~338 livinityd + ~38 ActionButton-icon) at deferred-items.md (pre-existing-noise, pre-Phase-17)"
  - phase: 20-scheduled-tasks-backup
    items:
      - "ctx.livinityd is possibly undefined typecheck noise across ai/routes.ts and widgets/routes.ts pre-dates Phase 20 (pre-existing-noise)"
      - "Idempotent default seed via ON CONFLICT DO NOTHING means existing v27.0 installs (server4) keep prior disabled rows — operator UPDATE required (runtime-validation-needed)"
  - phase: 21-gitops-stack-deployment
    items:
      - "editStack git mode not implemented — switching modes requires Remove + Add today (deferred-feature, v28)"
      - "Webhook secret rotation UI not implemented — secret shown ONCE, never retrievable (deferred-feature, v28)"
      - "git-backed stack visual badge in Stacks tab not implemented (deferred-feature, v28)"
      - "Pre-existing typecheck noise in ai/routes.ts and server/index.ts pre-dates Phase 21 (pre-existing-noise)"
      - "git-stack-sync default flip applies only to fresh PG installs — operator UPDATE required for existing server4 (runtime-validation-needed)"
  - phase: 22-multi-host-docker
    items:
      - "AgentDockerClient streaming methods (exec, attach, follow logs, stream stats, putArchive, getArchive) throw [agent-streaming-unsupported] (deferred-feature, v28)"
      - "scanImage / controlStack / editStack / removeStack / deployStack stay envId-less even with remote env selected (deferred-feature, v28: compose-file replication + remote/proxied Trivy)"
      - "ai-resource-watch is local-only — multi-host watching needs per-env throttle caches + per-env Kimi spend (deferred-feature, v28)"
      - "agent_id in environments has NO foreign-key to docker_agents — deferred to avoid circular dependency (deferred-feature, v28)"
      - "useGenerateAgentToken stub branch dead code — intentionally preserved as defensive fallback (pre-existing-noise)"
      - "Live agent end-to-end + tcp-tls + 5s revoke SLA + <100ms RTT need second host (runtime-validation-needed; tracked in 22-HUMAN-UAT.md)"
  - phase: 23-ai-powered-docker-diagnostics
    items:
      - "ai-resource-watch ships enabled:false by design — operator must opt-in via Settings > Scheduler (runtime-validation-needed)"
      - "docker_diagnostics tool inlines redaction regex + DIAGNOSE_SYSTEM_PROMPT — intentional architectural seam (pre-existing-noise)"
      - "dockerManager.inspectContainer() return shape strips health status / exit code (deferred-feature, v28)"
      - "Tool input schema is {containerName: string} — multi-host diagnose deferred (deferred-feature, v28)"
      - "Live Kimi quality + cron+Kimi+PG end-to-end need deployed server (runtime-validation-needed; tracked in 23-HUMAN-UAT.md)"
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases:
    - 17-docker-quick-wins
    - 18-container-file-browser
    - 19-compose-graph-vuln-scan
    - 20-scheduled-tasks-backup
    - 21-gitops-stack-deployment
    - 22-multi-host-docker
    - 23-ai-powered-docker-diagnostics
  overall: skipped
human_verification_pending:
  total: 11
  by_phase:
    22-multi-host-docker: 4
    23-ai-powered-docker-diagnostics: 7
---

# v27.0 Docker Management Upgrade — Milestone Audit

**Milestone:** v27.0 — Docker Management Upgrade
**Phases:** 17, 18, 19, 20, 21, 22, 23 (7 phases, 15 plans)
**Audited:** 2026-04-25
**Status:** **PASSED** — 33/33 requirements satisfied, 7/7 phases code-complete, 11 human-verification items deferred to deployment-time.

---

## Definition of Done

Quoted from `.planning/ROADMAP.md` (v27.0 milestone goal):

> Elevate Livinity's Docker management to best-in-class self-hosted Docker platform with Dockhand-inspired features (file browser, GitOps stacks, vulnerability scanning, compose graph viewer, multi-host) plus AI-powered diagnostics as Livinity's unique moat.

| Criterion | Source | Status |
|-----------|--------|--------|
| Real-time log streaming + stack secrets + redeploy + AI tool breadth (Phase 17 / QW-01..04) | ROADMAP Phase 17 | SATISFIED |
| Container file browser via exec + tar (Phase 18 / CFB-01..05) | ROADMAP Phase 18 | SATISFIED |
| Compose graph viewer + Trivy vuln scan (Phase 19 / CGV-01..04) | ROADMAP Phase 19 | SATISFIED |
| Scheduler + S3/SFTP/local backup (Phase 20 / SCH-01..05) | ROADMAP Phase 20 | SATISFIED |
| GitOps stack deploy with HMAC webhooks (Phase 21 / GIT-01..05) | ROADMAP Phase 21 | SATISFIED |
| Multi-host Docker via socket / TCP-TLS / outbound agent (Phase 22 / MH-01..05) | ROADMAP Phase 22 | SATISFIED (code) |
| AI-powered diagnostics + autonomous tool routing (Phase 23 / AID-01..05) | ROADMAP Phase 23 | SATISFIED (code) |

All 7 phases marked complete in ROADMAP.md and STATE.md (`status: milestone_complete`).


---

## Requirements Coverage Matrix

3-source cross-reference: REQUIREMENTS.md `[x]` checkbox + phase VERIFICATION.md status + plan SUMMARY.md `requirements-completed` frontmatter (or `tags:` for Phase 22).

| REQ-ID | Description | Phase | REQ.md | VERIFICATION | SUMMARY frontmatter | Status |
|--------|-------------|-------|--------|--------------|---------------------|--------|
| QW-01 | Real-time log WebSocket + ANSI colors | 17 | [x] | passed | 17-01 | **SATISFIED** |
| QW-02 | Stack secrets injected as env, never `.env` | 17 | [x] | passed | 17-01 | **SATISFIED** |
| QW-03 | Redeploy (pull latest) button | 17 | [x] | passed | 17-02 | **SATISFIED** |
| QW-04 | AI docker_manage extended to stacks/images/containers | 17 | [x] | passed | 17-02 | **SATISFIED** |
| CFB-01 | Browse container fs with breadcrumbs | 18 | [x] | passed | 18-01, 18-02 | **SATISFIED** |
| CFB-02 | Download via tar stream | 18 | [x] | passed | 18-01, 18-02 | **SATISFIED** |
| CFB-03 | Upload via putArchive | 18 | [x] | passed | 18-01, 18-02 | **SATISFIED** |
| CFB-04 | Edit small text files (<1MB) inline | 18 | [x] | passed | 18-02 | **SATISFIED** |
| CFB-05 | Delete with recursive-confirm for dirs | 18 | [x] | passed | 18-01, 18-02 | **SATISFIED** |
| CGV-01 | Compose graph (React Flow + depends_on + ports + nets) | 19 | [x] | passed | 19-01 | **SATISFIED** |
| CGV-02 | Trivy scan with severity badges | 19 | [x] | passed | 19-02 | **SATISFIED** |
| CGV-03 | Redis SHA256-keyed scan cache | 19 | [x] | passed | 19-02 | **SATISFIED** |
| CGV-04 | On-demand only — no auto-scan | 19 | [x] | passed | 19-02 | **SATISFIED** |
| SCH-01 | node-cron + scheduled_jobs PG table | 20 | [x] | passed | 20-01 | **SATISFIED** |
| SCH-02 | Built-in image-prune + update-check + git-sync | 20 | [x] | passed | 20-01 | **SATISFIED** |
| SCH-03 | Backup destinations S3 / SFTP / local | 20 | [x] | passed | 20-02 | **SATISFIED** |
| SCH-04 | Ephemeral alpine tar streaming | 20 | [x] | passed | 20-02 | **SATISFIED** |
| SCH-05 | Settings > Scheduler UI | 20 | [x] | passed | 20-02 | **SATISFIED** |
| GIT-01 | stacks + git_credentials schema (AES-256 encrypted) | 21 | [x] | passed | 21-01 | **SATISFIED** |
| GIT-02 | deployStack blobless clone | 21 | [x] | passed | 21-01 | **SATISFIED** |
| GIT-03 | Webhook HMAC-SHA256 verification | 21 | [x] | passed | 21-01 | **SATISFIED** |
| GIT-04 | "Deploy from Git" UI tab | 21 | [x] | passed | 21-02 | **SATISFIED** |
| GIT-05 | Auto-sync via Phase 20 scheduler | 21 | [x] | passed | 21-02 | **SATISFIED** |
| MH-01 | environments PG table | 22 | [x] | passed (T-01) | 22-01 (tags: mh-01) | **SATISFIED** |
| MH-02 | environmentId on every docker.* route + factory client | 22 | [x] | passed (T-02..T-04) | 22-01 (tags: mh-02) | **SATISFIED** |
| MH-03 | EnvironmentSelector dropdown in Server Control header | 22 | [x] | passed (T-05..T-11) | 22-02 (tags: mh-03) | **SATISFIED** |
| MH-04 | Outbound agent WebSocket transport (no open TCP) | 22 | [x] | passed (T-12..T-18) | 22-03 (tags: mh-04) | **SATISFIED** (code) |
| MH-05 | Per-agent token + revocable from Settings (5s SLA) | 22 | [x] | passed (T-15) | 22-03 (tags: mh-05) | **SATISFIED** (code) |
| AID-01 | AI Diagnose container logs | 23 | [x] | passed (T-1) | 23-01 | **SATISFIED** (code) |
| AID-02 | Proactive resource-limit alerts | 23 | [x] | passed (T-4) | 23-02 | **SATISFIED** (code) |
| AID-03 | Generate compose from natural-language prompt | 23 | [x] | passed (T-2) | 23-01 | **SATISFIED** (code) |
| AID-04 | Explain CVEs after Trivy scan | 23 | [x] | passed (T-3) | 23-01 | **SATISFIED** (code) |
| AID-05 | AI Chat autonomous docker_diagnostics tool routing | 23 | [x] | passed (T-5) | 23-02 | **SATISFIED** (code) |

**Score:** 33 / 33 requirements satisfied. **Zero unsatisfied, zero partial, zero orphaned.**

Phase 22 SUMMARY frontmatter uses `tags: [mh-01, ...]` instead of `requirements-completed:` — semantically equivalent. Cross-referenced via VERIFICATION.md REQ table.


---

## Phase Status Table

| Phase | Plans | Plans Complete | Verification Status | Score (must-haves) | UAT items |
|-------|-------|----------------|---------------------|--------------------|-----------|
| 17 — Docker Quick Wins | 2 (17-01, 17-02) | 2/2 | passed | 12/12 | 3 (in VERIFICATION human_verification, no separate UAT file) |
| 18 — Container File Browser | 2 (18-01, 18-02) | 2/2 | passed | 16/16 | 6 (in VERIFICATION human_verification, no separate UAT file) |
| 19 — Compose Graph + Vuln Scan | 2 (19-01, 19-02) | 2/2 | passed | 10/10 | 3 (in VERIFICATION human_verification, no separate UAT file) |
| 20 — Scheduled Tasks + Backup | 2 (20-01, 20-02) | 2/2 | passed | 11/11 | 4 (in VERIFICATION human_verification, no separate UAT file) |
| 21 — GitOps Stack Deployment | 2 (21-01, 21-02) | 2/2 | passed | 11/11 | 3 (in VERIFICATION human_verification, no separate UAT file) |
| 22 — Multi-host Docker | 3 (22-01, 22-02, 22-03) | 3/3 | passed (`human_needed` for runtime) | 14/18 verified, 4 await live runtime | 4 (22-HUMAN-UAT.md, all pending) |
| 23 — AI-Powered Diagnostics | 2 (23-01, 23-02) | 2/2 | passed | 16/16 | 7 (23-HUMAN-UAT.md, all pending) |

**Aggregate:** 7/7 phases passed. 90 must-haves verified across phase VERIFICATION reports (12+16+10+11+11+18+16; phase-22's 4 live-runtime items count as code-complete per the `human_needed` frontmatter rationale).

---

## Cross-Phase Integration

Verified that established patterns from earlier phases are correctly imported and reused by downstream phases.

| From | To | Pattern | Status | Evidence |
|------|----|---------|--------|----------|
| Phase 17 (`stack-secrets.ts` AES-256-GCM + JWT-derived key) | Phase 21 (`git-credentials.ts`) | AES-256-GCM with `sha256(readFile('/opt/livos/data/secrets/jwt').trim())` -> 32-byte key, output `base64(iv12 \|\| tag16 \|\| ciphertext)` | **WIRED** | git-credentials.ts lines 23-35 mirror stack-secrets.ts exactly. Verified by Phase 21 VERIFICATION T-3. |
| Phase 17 (WebSocket streaming `/ws/docker/logs`) | Phase 18 (Files tab REST endpoints) | REST `/api/docker/container/:name/file` GET tar + POST multipart gated by `LIVINITY_SESSION` cookie via `verifyToken`, mirroring `/ws/docker/logs` JWT pattern | **WIRED** | server/index.ts lines 1207 (GET) and 1253 (POST). Verified by Phase 18 VERIFICATION T-2/T-3. |
| Phase 17 (WebSocket factory `mountWebSocketServer`) | Phase 20 (potential scheduler tail) | Pattern provided; Phase 20 chose 10s polling for last-run UI rather than WS tail (intentional UX trade-off documented in 20-02 SUMMARY) | **WIRED (alt chosen)** | scheduler-section.tsx polls listJobs every 10s — not a gap. |
| Phase 19 (`scanImage` Trivy cache `nexus:vuln:<sha256>`) | Phase 23 (`explainVulnerabilities`) | explainVulnerabilities consumes `getCachedScan` to retrieve top-5 CVEs; short-circuits if CRITICAL+HIGH = 0 | **WIRED** | ai-diagnostics.ts:532 reads from same Redis prefix; 23-01 SUMMARY explicitly documents the dependency. UI gates Explain CVEs button on CRITICAL+HIGH > 0. |
| Phase 20 (`BUILT_IN_HANDLERS` scheduler registry) | Phase 21 (`gitStackSyncHandler`) | Phase 20-01 left `BUILT_IN_HANDLERS['git-stack-sync']` as a `status:'skipped'` placeholder; Phase 21-02 replaced it with the real handler iterating `listGitStacks()` -> per-stack `syncRepo` -> `controlStack('pull-and-up')` | **WIRED** | scheduler/jobs.ts lines 169-252 + DEFAULT_JOB_DEFINITIONS line 282 with `enabled:true`. Verified by Phase 21 VERIFICATION T-10/T-11. |
| Phase 20 (`BUILT_IN_HANDLERS` scheduler registry) | Phase 23 (`aiResourceWatchHandler`) | Phase 23-02 added new JobType `'ai-resource-watch'` to types.ts union; registered handler in BUILT_IN_HANDLERS line 269; seeded with `enabled:false` (operator opt-in) | **WIRED** | scheduler/jobs.ts:13 import + :269 binding + :295 seed. Verified by Phase 23 VERIFICATION T-4. |
| Phase 22 (`getDockerClient(envId)` factory) | Phase 23 diagnostics | Phase 23 routes accept `environmentId` for `diagnoseContainer` (uses Phase 22 envIdField). `docker_diagnostics` AI tool input is `{containerName: string}` only — multi-host diagnose explicitly deferred to v28 per 23-02 SUMMARY | **WIRED (with documented v28 narrowing)** | ai-resource-watch handler runs against LOCAL socket only (`listContainers(null)`); 22-01 D-06 + 23-02 SUMMARY document v28 follow-up. NOT a milestone gap — single-host scope. |
| Phase 22 (`environments` table CASCADE) | Phase 23 (`ai_alerts.environment_id`) | ai_alerts has `environment_id UUID` referencing environments. CASCADE behavior: deleting env removes alerts for that env | **WIRED** | schema.sql:268 ai_alerts table; Phase 22 environments CASCADE pattern carried forward. |
| All v27.0 phases (`httpOnlyPaths` registry in `common.ts`) | Cross-cutting tRPC mutation routing | Each phase registers long-running or admin-mutation routes to bypass WS-disconnect-hang gotcha | **COMPLETE** | Phase 18: containerWriteFile + containerDeleteFile (lines 84-85). Phase 19: docker.scanImage (line 72). Phase 20: scheduler.upsertJob/deleteJob/runNow/testBackupDestination (lines 90-93). Phase 21: docker.{listGit,createGit,deleteGit}Credential. Phase 22: docker.{create,update,delete}Environment + generateAgentToken + revokeAgentToken (lines 89-97). Phase 23: 3 reactive AI mutations + 2 dismissal mutations (lines 108-114). **No phase missed registering its mutations.** |

**Score:** 8/8 cross-phase integrations wired. **Zero broken connections, zero orphaned exports.**


---

## End-to-End Flows

Three milestone-critical user journeys traced through the codebase. Each step verified against actual file/route locations.

### Flow 1: Container Lifecycle (list -> inspect -> logs -> exec -> files -> backup)

| Step | Layer | File / Route | Status |
|------|-------|--------------|--------|
| List containers | tRPC query | `docker.listContainers` (routes.ts) -> `getDockerClient(envId).listContainers()` | OK |
| Inspect container | tRPC query | `docker.inspectContainer` -> `Dockerode.getContainer(name).inspect()` | OK |
| Real-time logs | WebSocket | `/ws/docker/logs` -> `docker-logs-socket.ts` `container.logs({follow:true})` -> xterm renders ANSI | OK |
| Exec shell | WebSocket | `/ws/docker-exec` (existing pre-v27.0) | OK |
| Browse files | tRPC + REST | `docker.containerListDir` -> `container-files.ts:listDir`; tar download via GET `/api/docker/container/:name/file`; multipart upload via POST | OK |
| Edit file inline | tRPC | `docker.containerReadFile` (1MB cap) -> modal textarea -> `docker.containerWriteFile` | OK |
| Delete file/dir | tRPC | `docker.containerDeleteFile` with recursive flag (mandatory for dirs) | OK |
| Backup volume | scheduler | `Settings > Scheduler > Add Backup` -> `backup.ts:streamVolumeAsTarGz` (alpine container, AutoRemove:true) -> S3/SFTP/local | OK |

**Status:** **COMPLETE** — every step verified with code evidence in 17/18/20 VERIFICATION reports.

### Flow 2: Stack Lifecycle (deploy -> edit -> git-sync -> vuln scan -> AI diagnose -> remove)

| Step | Layer | File / Route | Status |
|------|-------|--------------|--------|
| Deploy YAML stack with secrets | tRPC | `docker.deployStack({composeYaml, envVars[{secret:true}]})` -> `stacks.ts` splits secrets (Redis) from plain (.env) -> `docker compose up -d` | OK |
| Deploy git stack | tRPC | `docker.deployStack({git:{url, branch, credentialId, composePath}})` -> `git-deploy.ts:cloneOrPull --filter=blob:none` -> INSERT stacks row -> compose up | OK |
| Webhook redeploy | REST | POST `/api/webhooks/git/:stackName` HMAC-SHA256 verify -> 202 -> background `syncRepo` + `controlStack('pull-and-up')` | OK |
| Edit stack | tRPC | `docker.editStack` (YAML mode only — git editStack deferred to v28) | OK (with v28 follow-up) |
| Hourly auto-sync | scheduler | node-cron `0 * * * *` -> `gitStackSyncHandler` -> `listGitStacks()` -> per-stack syncRepo + redeploy on HEAD change | OK |
| Compose graph view | UI | StacksTab expanded row -> "Graph" tab -> `ComposeGraphViewer` -> `getStackCompose` query -> React Flow canvas | OK |
| Vulnerability scan | tRPC | Images tab -> Scan button -> `docker.scanImage` -> Trivy ephemeral container -> Redis cache `nexus:vuln:<sha256>` | OK |
| AI diagnose container | tRPC | Container detail -> "AI Diagnose" -> `docker.diagnoseContainer` -> `callKimi` -> `nexus:ai:diag:` cache | OK |
| Explain CVEs | tRPC | ScanResultPanel -> "Explain CVEs" (gated CRITICAL+HIGH > 0) -> `docker.explainVulnerabilities` -> reads getCachedScan top-5 -> callKimi | OK |
| Generate compose from prompt | tRPC | Stack create -> "AI" tab -> `docker.generateComposeFromPrompt` -> callKimi (no cache) | OK |
| Redeploy (pull latest) | tRPC | Stack row -> Redeploy button -> `controlStack(name, 'pull-and-up')` -> docker compose pull + up -d | OK |
| Remove stack | tRPC | `docker.removeStack(name, removeVolumes)` -> docker compose down -> cleanup `/opt/livos/data/git/<name>` + DELETE FROM stacks + Redis secret purge (best-effort) | OK |

**Status:** **COMPLETE** — every step verified across 17/19/21/23 VERIFICATION reports + cross-phase integration.

### Flow 3: Multi-Host (env-add -> agent-token -> agent-handshake -> stack-deploy-via-env -> revoke)

| Step | Layer | File / Route | Status |
|------|-------|--------------|--------|
| Add agent env | tRPC | `Settings > Environments > Add > agent` -> `docker.createEnvironment({type:'agent', name})` -> INSERT environments row (agent_id=null) | OK |
| Generate token | tRPC | GenerateAgentTokenDialog auto-fires `useGenerateAgentToken().mutateAsync({environmentId})` -> `agents.ts:createAgent` (randomBytes(32).hex + SHA-256 hash + backfill environments.agent_id) -> returns `{agentId, token, agentInstallSnippet}` ONCE | OK |
| Display token + curl snippet | UI | Token shown ONCE with copy button + curl install snippet; closing dialog drops React state | OK |
| Install agent on remote host | shell | `curl -fsSL https://livinity.cloud/install-agent.sh \| bash -s -- --token <T> --server wss://livinity.cloud/agent/connect` -> install.sh writes systemd unit + LIVOS_AGENT_TOKEN env | OK (code) — runtime needs second host |
| Agent handshake | WebSocket | docker-agent runs `wss://.../agent/connect` -> first message `{type:'register', token}` -> `findAgentByToken(hashToken(token))` -> `registerAgent` -> environments.agent_status='online' | OK (code) — runtime needs second host |
| Select agent env in UI | UI | `<EnvironmentSelector>` in Server Control header -> `useEnvironmentStore.setEnvironment(agentEnvId)` -> React Query queryKey changes -> all docker hooks refetch via env-aware `{environmentId}` input | OK |
| Stack deploy via env | tRPC | listContainers/Images/Volumes/Networks/Stacks scoped to envId; route through `getDockerClient(envId)` factory -> AgentDockerClient -> agentRegistry.sendRequest -> docker-agent dispatch table -> local Dockerode on remote host | OK (code) — runtime needs second host |
| Revoke token | tRPC | Settings > Environments > Revoke -> `docker.revokeAgentToken(id)` -> `revokeAgent(id)` (UPDATE docker_agents SET revoked_at=NOW()) + Redis publish `livos:agent:revoked` | OK |
| 5s SLA disconnect | pub/sub | `agent-socket.ts:startAgentRevocationSubscriber` (Redis duplicate connection in subscribe-mode) -> receives message -> `agentRegistry.forceDisconnect(agentId, 'token-revoked')` -> WS close code 4403 -> agent process exits with code 3 -> environments.agent_status='offline' | OK (code) — 5s latency needs runtime |

**Status:** **COMPLETE (code-wise)** — full path wired end-to-end. Runtime validation tracked in `22-HUMAN-UAT.md` (4 items: tcp-tls connectivity, agent handshake, 5s revoke SLA, <100ms LAN RTT).

**Aggregate flow score:** 3/3 critical user journeys complete in code. No broken steps. Items needing live infrastructure are tracked in HUMAN-UAT files, not classified as gaps.


---

## Tech Debt Summary

Categorized aggregation across all 7 phases.

| Category | Count | Severity | Notes |
|----------|-------|----------|-------|
| **deferred-feature (v28)** | 14 | low | Explicit v28 follow-ups documented in plan summaries — file preview, chmod UI, dagre layout, SBOM/Grype, editStack git mode, webhook secret rotation, agent streaming methods, multi-host stack deploy/Trivy/exec, multi-host AI watch, FK env->agent, dockerManager health/exit-code, multi-host AI diagnose, AI stack-deploy with secrets |
| **pre-existing-noise** | 5 | informational | Pre-Phase-17 typecheck noise (~338 livinityd errors + ~38 ActionButton-icon errors + ctx.livinityd-undefined pattern). useGenerateAgentToken stub branch dead code (intentional). docker_diagnostics inlined regex duplication (intentional architectural seam) |
| **runtime-validation-needed** | 4 | low (deployment-time) | git-stack-sync existing-install ON CONFLICT DO NOTHING (operator UPDATE required); ai-resource-watch ships `enabled:false` (operator opt-in via Settings); MH-04/MH-05/AID-* live LLM and second-host validation captured in HUMAN-UAT files |

**No blocker debt.** No anti-patterns flagged in any VERIFICATION report. All pre-existing noise pre-dates v27.0 scope and is documented in scope-boundary deferred-items per the project convention.

---

## Human Verification Pending

11 items consolidated from `22-HUMAN-UAT.md` (4) and `23-HUMAN-UAT.md` (7). All pending live-deploy validation, NOT implementation gaps.

**Phase 22 (4 items):**

1. **Remote TCP/TLS daemon connectivity** — Create tcp-tls env pointing at remote `dockerd --tlsverify`; list containers; updateEnvironment + invalidateClient re-fetch (needs second host with TLS-enabled dockerd).
2. **Agent end-to-end connection + handshake** — Run install.sh on remote VM; verify systemd active; livinityd logs `agent <id> connected`; environments.agent_status='online'; remote containers listed in UI (needs Node 20+ remote host with reachable WSS to livinityd).
3. **Token revocation 5s SLA (MH-05)** — Click Revoke -> measure latency to WS close code 4403 + agent exit code 3 + agent_status='offline' (needs live agent + co-located Redis).
4. **Agent round-trip latency < 100ms (MH-04)** — Measure docker.listContainers median across 10 calls on same-LAN agent (needs live agent + perf measurement).

**Phase 23 (7 items):**

5. **AI Diagnose end-to-end (AID-01)** — Click on container with restarts; expect `{Likely Cause, Suggested Action, Confidence}` within 30s (needs deployed Kimi).
6. **Generate from prompt end-to-end (AID-03)** — "Nextcloud with Redis and MariaDB on port 8080" -> valid compose YAML (needs live LLM).
7. **Explain CVEs end-to-end (AID-04)** — After Trivy on nginx:1.21, click Explain CVEs -> plain-English remediation + concrete `image:tag` upgrade target (needs live LLM).
8. **Proactive resource-watch alert generation (AID-02)** — Toggle ai-resource-watch enabled; run memhog container; alert appears in bell within 5min (needs cron tick + Kimi + PG insert).
9. **Resource-watch dedupe regression (AID-02)** — Second cron tick should NOT duplicate alert; alertsSkippedDeduped increments (needs multiple cron invocations).
10. **AI Chat autonomous tool invocation (AID-05)** — Ask "why is my postgres container restarting?" -> LLM autonomously invokes docker_diagnostics; tool-call visible in tool panel (needs live LLM router decision).
11. **httpOnlyPaths runtime check** — DevTools disconnect WS, fire AI Diagnose; mutation completes via HTTP within 30s rather than hanging (Phase 18 mutation-hang regression check).

These are deployment-time smoke tests for live LLM quality + multi-host runtime — **not milestone blockers**. Tracked in HUMAN-UAT files for handoff.


---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|------------------|--------|-------|
| QW-01 | Phase 17 docker-logs-socket exports `createDockerLogsHandler` -> server/index.ts mounts `/ws/docker/logs` -> container-detail-sheet.tsx LogsTab consumes | WIRED | — |
| QW-02 | Phase 17 stack-secrets export -> stacks.ts deployStack/editStack/controlStack consume via `getStore().getSecrets(name)` | WIRED | — |
| QW-03 | Phase 17 controlStack('pull-and-up') -> routes.ts zod enum -> UI RedeployStackDialog | WIRED | — |
| QW-04 | Phase 17 nexus daemon.ts docker_manage operation enum -> DockerManager methods | WIRED | — |
| CFB-01..05 | Phase 18 container-files.ts -> routes.ts 4 procedures + 2 REST endpoints -> FilesTab UI consumes via tRPC + fetch | WIRED | — |
| CGV-01 | Phase 19 ComposeGraphViewer -> trpc.docker.getStackCompose query -> ReactFlow render in StacksTab Tabs | WIRED | — |
| CGV-02..04 | Phase 19 vuln-scan.ts -> routes.ts scanImage/getCachedScan -> use-images.ts hook -> ScanResultPanel + ImagesTab integration | WIRED | — |
| SCH-01..05 | Phase 20 Scheduler class boots in Livinityd lifecycle -> BUILT_IN_HANDLERS registry -> backup-secrets vault + 3 destination uploaders -> tRPC scheduler.* routes -> SchedulerSection UI lazy-loaded in settings-content | WIRED | — |
| GIT-01..03 | Phase 21 git-credentials.ts (AES-256-GCM mirrors Phase 17) + git-deploy.ts simple-git -> routes.ts deployStack git input -> server/index.ts /api/webhooks/git/:stackName HMAC | WIRED | — |
| GIT-04 | Phase 21-02 Tabs(YAML, Git) in DeployStackForm -> useStacks lastDeployResult -> webhook URL panel | WIRED | — |
| GIT-05 | Phase 21-02 gitStackSyncHandler replaces 20-01 placeholder in BUILT_IN_HANDLERS['git-stack-sync']; default flipped to enabled:true (fresh installs) | WIRED | — |
| MH-01..02 | Phase 22-01 environments PG + getDockerClient(envId) factory threaded through 30+ docker.* routes (88 envId occurrences) | WIRED | — |
| MH-03 | Phase 22-02 zustand store + EnvironmentSelector -> 7 docker hooks read useSelectedEnvironmentId() -> React Query queryKey drives auto-refetch | WIRED | — |
| MH-04 | Phase 22-03 @livos/docker-agent package + AgentDockerClient + agent-registry + /agent/connect WS branch -> replaces 22-01 [agent-not-implemented] placeholder | WIRED | — |
| MH-05 | Phase 22-03 docker_agents PG table + revokeAgentToken Redis publish + agent-socket.ts subscriber + forceDisconnect(4403) | WIRED | — |
| AID-01 | Phase 23-01 ai-diagnostics.ts:diagnoseContainer + nexus POST /api/kimi/chat -> useAiDiagnostics hook -> DiagnosticPanel in container-detail-sheet | WIRED | — |
| AID-02 | Phase 23-02 ai-resource-watch handler + ai_alerts PG (with FK to Phase 22 environments) + AlertsBell in Server Control header (next to EnvironmentSelector) | WIRED | — |
| AID-03 | Phase 23-01 generateComposeFromPrompt -> AiComposeTab in DeployStackForm Tabs (extending Phase 21's yaml/git Tabs) | WIRED | — |
| AID-04 | Phase 23-01 explainVulnerabilities reads Phase 19 getCachedScan -> ScanResultPanel "Explain CVEs" button (gated CRITICAL+HIGH > 0) | WIRED | — |
| AID-05 | Phase 23-02 nexus daemon.ts docker_diagnostics tool registration with router-targeted description; in-process brain.chat() (no HTTP roundtrip) | WIRED | — |

**Requirements with no cross-phase wiring:** None. All 33 v27.0 requirements have at least one cross-phase integration touchpoint (most have multiple — e.g., AID-04 spans Phase 19 cache + Phase 23 explainer + Phase 22 envIdField). The milestone is genuinely a system, not a collection of isolated features.

---

## Recommendation

**MILESTONE PASSED — v27.0 Docker Management Upgrade is code-complete and ready for deployment-time smoke testing.**

- 33 / 33 requirements satisfied across 3 sources (REQUIREMENTS.md `[x]` + VERIFICATION.md status + SUMMARY frontmatter).
- 7 / 7 phases verified with passing scores totaling 90 must-haves.
- 8 / 8 cross-phase integrations correctly wired (AES-256-GCM crypto, WebSocket streaming pattern, scheduler BUILT_IN_HANDLERS registry, getDockerClient factory threading, httpOnlyPaths discipline).
- 3 / 3 end-to-end user flows complete (container lifecycle, stack lifecycle, multi-host).
- **Zero blocker tech debt.** 14 deferred-feature items explicitly tagged for v28; 5 pre-existing-noise items pre-date v27.0 scope; 4 runtime-validation items captured in HUMAN-UAT files.
- 11 human-verification items pending live deployment to server4 + a second host. These are NOT gaps — they are deployment-time smoke tests for live LLM quality and multi-host runtime that cannot be machine-verified.

**Next steps:**

1. Update STATE.md milestone status to `passed_audit`.
2. Deploy to server4 per documented procedure (`git pull && npm run build --workspace=packages/core && pm2 restart nexus-core; cd /opt/livos/livos && pnpm --filter @livos/config build && pnpm --filter ui build && pm2 restart livos`).
3. Host `livos-docker-agent.tar.gz` + `/install-agent.sh` endpoint for MH-04 install flow.
4. Provision a second host to execute the 4 Phase 22 UAT items (tcp-tls + agent handshake + 5s revoke + <100ms RTT).
5. Validate `kimi login` is current; execute the 7 Phase 23 UAT items (AI Diagnose, Generate, Explain CVEs, resource-watch, dedupe, autonomous tool, httpOnlyPaths).
6. Run `/gsd:plan-milestone v28.0` once UAT items pass — v28.0 candidates already surfaced during execution (multi-host AI watch, editStack git mode, multi-host stack deploy via compose replication, multi-host Trivy, real-time WS exec/logs over remote envs, dockerManager.inspectContainer health+exit-code expansion, prompt-tuning analytics from ai_alerts.payload_json).

---

*Audited: 2026-04-25*
*Auditor: Claude (gsd-milestone-auditor) — agent thread for `/gsd:audit-milestone v27.0`*
