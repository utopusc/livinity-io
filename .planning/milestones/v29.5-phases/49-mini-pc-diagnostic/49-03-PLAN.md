---
phase: 49-mini-pc-diagnostic
plan: 03
type: execute
wave: 2
depends_on: [49-01, 49-02]
files_modified:
  - .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md
autonomous: true
requirements: [FR-A1-01, FR-A2-01, FR-A4-01, FR-A3-04]
must_haves:
  truths:
    - "49-DIAGNOSTIC-FIXTURE.md contains exactly 4 verdict blocks (A1, A2, A3, A4)"
    - "Each verdict block has all 5 fields filled (Status / Hypothesis from REGRESSIONS.md / Evidence / Verdict / Recommended Fix Path) — none TBD"
    - "Every evidence line cites raw-minipc.txt:<marker> OR raw-server5.txt:<marker> OR REGRESSIONS.md:<section>"
    - "No mocked or fabricated SSH output — all evidence is grep-able in raw-*.txt"
  artifacts:
    - path: ".planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md"
      provides: "Canonical root-cause fixture for v29.5 phases 50-53 to consume"
      contains: "### A1 verdict, ### A2 verdict, ### A3 verdict, ### A4 verdict, **Status:**, **Hypothesis from REGRESSIONS.md:**, **Evidence captured:**, **Verdict:**, **Recommended Phase 5x fix path:**"
  key_links:
    - from: "49-DIAGNOSTIC-FIXTURE.md A1 verdict"
      to: "raw-minipc.txt REDIS_TOOL_KEYS marker"
      via: "evidence citation"
      pattern: "raw-minipc\\.txt"
    - from: "49-DIAGNOSTIC-FIXTURE.md A3 verdict"
      to: "raw-server5.txt PLATFORM_APPS_STATE marker"
      via: "evidence citation"
      pattern: "raw-server5\\.txt"
    - from: "49-DIAGNOSTIC-FIXTURE.md header"
      to: "current git SHA"
      via: "git rev-parse --short HEAD"
      pattern: "(SHA|commit)[:\\s]+[a-f0-9]{7,}"
---

<objective>
Synthesize `49-DIAGNOSTIC-FIXTURE.md` from raw-minipc.txt + raw-server5.txt + the prior `.planning/v29.4-REGRESSIONS.md` transcript. The fixture is the canonical root-cause document Phases 50-53 consume — they MUST NOT re-probe Mini PC.

Purpose: Per D-49-04 (LOCKED), each of A1, A2, A3, A4 gets a verdict block in the exact template from CONTEXT.md. Every fact cites a specific marker line in the raw-*.txt files (or a specific REGRESSIONS.md section if the data point still holds and was not re-captured per D-49-02).
Output: `.planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/49-mini-pc-diagnostic/49-CONTEXT.md
@.planning/v29.4-REGRESSIONS.md
@.planning/phases/49-mini-pc-diagnostic/raw-minipc.txt
@.planning/phases/49-mini-pc-diagnostic/raw-server5.txt
@.planning/phases/49-mini-pc-diagnostic/49-01-SUMMARY.md
@.planning/phases/49-mini-pc-diagnostic/49-02-SUMMARY.md

<!-- D-49-04 (LOCKED): verdict block template — Status / Hypothesis from REGRESSIONS.md / Evidence captured / Verdict / Recommended Phase 5x fix path.
     D-49-02 (LOCKED): reuse REGRESSIONS.md as primary fixture — cite still-valid data points instead of re-discovering.
     NO MOCKED VALUES: every evidence claim must be grep-able in raw-*.txt or REGRESSIONS.md. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read raw fixtures + REGRESSIONS.md, draft and write 49-DIAGNOSTIC-FIXTURE.md</name>
  <files>.planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md</files>
  <read_first>
    - .planning/phases/49-mini-pc-diagnostic/raw-minipc.txt (output of 49-01 — REDIS_TOOL_KEYS, UI_BUNDLE_MTIME, USER_PREFS, SECURITY_SECTION_CHUNK, etc.)
    - .planning/phases/49-mini-pc-diagnostic/raw-server5.txt (output of 49-02 — PLATFORM_APPS_STATE, PLATFORM_GIT_LOG)
    - .planning/v29.4-REGRESSIONS.md (full file — primary fixture per D-49-02; cite sections)
    - .planning/phases/49-mini-pc-diagnostic/49-CONTEXT.md (D-49-04 verdict template)
  </read_first>
  <action>
Write `.planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md` using the Write tool. Document MUST follow this exact structure (note: per D-49-04 verdict template):

```markdown
# Phase 49 — Diagnostic Fixture (v29.4 Regressions Root Cause)

**Captured:** <ISO date YYYY-MM-DD>
**Phase:** 49 (Mini PC Live Diagnostic — single-batch SSH)
**Repo SHA at capture:** <output of `git rev-parse --short HEAD` at start of this task>
**Source fixtures:**
- `raw-minipc.txt` — single SSH session to bruce@10.69.31.68 (D-49-01, D-49-03)
- `raw-server5.txt` — single SSH session to root@45.137.194.102 (D-49-01, D-49-03)
- `.planning/v29.4-REGRESSIONS.md` — prior diagnostic transcript (D-49-02 primary fixture; cited where still valid)

**Constraint compliance:**
- D-49-01: ONE SSH per host. Verified by 49-01-SUMMARY.md and 49-02-SUMMARY.md.
- D-49-02: REGRESSIONS.md cited where its data points still hold; gaps filled by raw-*.txt.
- D-49-04: every regression has the locked verdict block template below.

___

## Summary table

| Regression | Status                           | Recommended Phase |
|------------|----------------------------------|-------------------|
| A1         | <CONFIRMED|NEW HYPOTHESIS|...>   | 50                |
| A2         | <...>                            | 51                |
| A3         | <...>                            | 52                |
| A4         | <...>                            | 53                |

___

### A1 verdict (Tool registry — built-in tools missing)

- **Status:** <CONFIRMED | NEW HYPOTHESIS | INSUFFICIENT EVIDENCE>
- **Hypothesis from REGRESSIONS.md:** <quote/paraphrase REGRESSIONS.md A1 hypothesis with section citation, e.g. "REGRESSIONS.md §A1: Phase 22 syncTools() seed flow not running at boot OR D-WAVE5-SYNCALL-STUB stub never wires real seed">
- **Evidence captured:** <bulleted citations>
  - `raw-minipc.txt § REDIS_TOOL_KEYS`: count = <N> (the smoking gun number)
  - `raw-minipc.txt § REDIS_CAP_PREFIX`: count = <N> (registry not entirely empty — cap:* exists, cap:tool:* doesn't)
  - `raw-minipc.txt § SERVICES`: livinityd state = <active|...>
  - `REGRESSIONS.md §A1`: <still-valid claim cited>
- **Verdict:** <1-2 sentence root cause statement, e.g. "BUILT_IN_TOOL_IDS are never written to nexus:cap:tool:* on livinityd boot — no module performs the eager seed; Phase 47-02's D-WAVE5-SYNCALL-STUB confirms production Re-sync also writes zero keys.">
- **Recommended Phase 5x fix path:** Phase 50 — defensive eager seed module (Path 2 from REGRESSIONS.md A1 fix options) writing all 9 BUILT_IN_TOOL_IDS idempotently as the first step after Redis connection in livinityd boot.

### A2 verdict (Streaming gone — full response after completion)

- **Status:** <...>
- **Hypothesis from REGRESSIONS.md:** <quote/paraphrase REGRESSIONS.md A2 hypothesis with section citation>
- **Evidence captured:**
  - `raw-minipc.txt § UI_BUNDLE_MTIME`: stat output = <date+time> (compare to last commit time to detect stale bundle)
  - `raw-minipc.txt § UPDATE_LOG_TAIL`: last vite build output = <line snippet> (was vite invoked? duration?)
  - `raw-minipc.txt § PAST_DEPLOYS_TAIL`: last deploy duration = <Xm Ys> (per REGRESSIONS.md, 1m 2s is suspiciously short)
  - `raw-minipc.txt § LIVOS_JOURNAL`: streaming-related log lines = <snippet or "no streaming events">
- **Verdict:** <1-2 sentence root cause statement choosing among UI bundle stale / PWA SW cache / sacred file model preset / upstream buffer based on the evidence above>
- **Recommended Phase 5x fix path:** Phase 51 — <name the path: full vite rebuild + cache bust / D-40-01 sacred file surgical edit / SW version bump / preset reversal>. Cite REGRESSIONS.md A2 fix options.

### A3 verdict (Marketplace state — Bolt.diy gone, MiroFish present)

- **Status:** <...>
- **Hypothesis from REGRESSIONS.md:** <quote/paraphrase REGRESSIONS.md A3 hypothesis with section citation>
- **Evidence captured:**
  - `raw-server5.txt § PLATFORM_APPS_STATE`: bolt.diy row = <present|missing>, mirofish row = <present|missing>
  - `raw-server5.txt § PLATFORM_APPS_FULL_COUNT`: <count + status grouping>
  - `raw-server5.txt § PLATFORM_GIT_LOG`: most recent commits touching platform_apps/marketplace/migrations = <SHA + subject lines>
  - `raw-server5.txt § PLATFORM_DB_DISCOVERY`: actual DB name on Server5 = <livinity|...>
  - `REGRESSIONS.md §A3`: <still-valid claim cited>
- **Verdict:** <1-2 sentence root cause statement attributing the Bolt.diy wipe to a specific commit OR a manual SQL OR cache desync OR factory reset>
- **Recommended Phase 5x fix path:** Phase 52 — idempotent SQL migration `<ts>-v29.5-marketplace-state.sql` that re-INSERTs Bolt.diy and removes MiroFish (per REGRESSIONS.md A3 path); FR-A3-04 root cause documented in this section.

### A4 verdict (Security panel not rendering)

- **Status:** <...>
- **Hypothesis from REGRESSIONS.md:** <quote/paraphrase REGRESSIONS.md A4 hypothesis with section citation>
- **Evidence captured:**
  - `raw-minipc.txt § SECURITY_SECTION_CHUNK`: grep result = <filename of dist asset chunk that contains 'security-section' OR "no match" if absent>
  - `raw-minipc.txt § USER_PREFS`: row for admin user → security_panel_visible = <true|false|null|no row>
  - `raw-minipc.txt § UI_BUNDLE_MTIME`: bundle mtime (cross-references A2)
  - `raw-minipc.txt § PAST_DEPLOYS_TAIL`: last deploy duration (cross-references A2)
- **Verdict:** <1-2 sentence root cause among: stale UI bundle / PWA SW cache / user_preferences default OFF / sidebar useMemo filter logic>
- **Recommended Phase 5x fix path:** Phase 53 — <DB migration default ON / sidebar filter undefined-as-visible / SW version bump / full vite rebuild>. Cite REGRESSIONS.md A4 fix options.
```

Filling rules (apply per regression):
- **Status MUST be one of** `CONFIRMED`, `NEW HYPOTHESIS`, `INSUFFICIENT EVIDENCE`. No other values. Never TBD.
- **Hypothesis from REGRESSIONS.md** MUST be a citation — quote or paraphrase + `§<section name>`. If REGRESSIONS.md offers multiple hypotheses for that regression, list them all and indicate which one this fixture confirms.
- **Evidence captured** is a bulleted list. Every bullet MUST cite either `raw-minipc.txt § MARKER`, `raw-server5.txt § MARKER`, or `REGRESSIONS.md §<section>`. NEVER write a claim without a citation. NEVER invent a number — extract literally from raw-*.txt by reading the file.
- **Verdict** is 1-2 sentences. Plain prose. Names a root cause that downstream Phase 50-53 will fix.
- **Recommended Phase 5x fix path** names the specific phase (50/51/52/53) and the option from REGRESSIONS.md that this fixture endorses.
- If a marker section is empty / errored in raw-*.txt (e.g., USER_PREFS shows `permission denied` because postgres role mismatch), Status becomes `INSUFFICIENT EVIDENCE` for that regression, the Verdict explicitly says what's missing, and the Recommended Path notes that Phase 5x must capture the missing data with its own SSH probe at start.

Steps to execute:
1. Run `git rev-parse --short HEAD` and `date -u +%Y-%m-%d` (Bash); embed both in the header.
2. Read raw-minipc.txt and raw-server5.txt fully (they are small).
3. Read .planning/v29.4-REGRESSIONS.md (the §A1, §A2, §A3, §A4 sections).
4. For each regression, fill the verdict block. NEVER paraphrase a number — write the exact value from raw-*.txt.
5. Fill the Summary table at top.
6. Use the Write tool to save the fixture.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md &amp;&amp; grep -c "^### A[1-4] verdict$" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md | grep -q "^4$" &amp;&amp; ! grep -qiE "\\bTBD\\b|\\bTODO\\b|<[A-Z _|]+>" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md &amp;&amp; grep -q "raw-minipc.txt" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md &amp;&amp; grep -q "raw-server5.txt" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md &amp;&amp; grep -q "REGRESSIONS.md" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md &amp;&amp; grep -cE "^- \\*\\*(Status|Hypothesis from REGRESSIONS\\.md|Evidence captured|Verdict|Recommended Phase 5x fix path):\\*\\*" .planning/phases/49-mini-pc-diagnostic/49-DIAGNOSTIC-FIXTURE.md | grep -q "^20$"</automated>
  </verify>
  <done>
49-DIAGNOSTIC-FIXTURE.md exists. Contains exactly 4 `### AN verdict` blocks. Each block has all 5 D-49-04 fields filled with non-placeholder text (no TBD, no `<...>` placeholders left over). Each evidence section cites at least one of raw-minipc.txt, raw-server5.txt, or REGRESSIONS.md. Header includes a captured date and git SHA. The 5 fields × 4 regressions = 20 bolded field labels (the regex above counts them).
  </done>
</task>

</tasks>

<verification>
Single check: 4 verdict blocks present, all 20 D-49-04 field labels present (5 fields × 4 regressions), no placeholder tokens remain, all three source files cited at least once.
</verification>

<success_criteria>
49-DIAGNOSTIC-FIXTURE.md is a self-contained root-cause document Phases 50-53 can read without re-probing Mini PC. All facts trace to raw-*.txt or REGRESSIONS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mini-pc-diagnostic/49-03-SUMMARY.md` recording:
- Per-regression Status field value (CONFIRMED / NEW HYPOTHESIS / INSUFFICIENT EVIDENCE)
- Per-regression Recommended Phase
- Number of distinct evidence citations (count of `raw-minipc.txt §` + `raw-server5.txt §` + `REGRESSIONS.md §` occurrences)
- Whether all 4 regressions reached CONFIRMED or some need further capture in the downstream phase
</output>
