---
phase: 59-bearer-token-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - livos/packages/livinityd/source/modules/api-keys/database.test.ts
  - livos/packages/livinityd/source/modules/api-keys/cache.test.ts
  - livos/packages/livinityd/source/modules/api-keys/bearer-auth.test.ts
  - livos/packages/livinityd/source/modules/api-keys/routes.test.ts
  - livos/packages/livinityd/source/modules/api-keys/schema-migration.test.ts
  - livos/packages/livinityd/source/modules/api-keys/mount-order.test.ts
autonomous: true
requirements:
  - FR-BROKER-B1-01
  - FR-BROKER-B1-02
  - FR-BROKER-B1-03
  - FR-BROKER-B1-04
  - FR-BROKER-B1-05
tags:
  - bearer-auth
  - test-infra
  - api-keys
must_haves:
  truths:
    - "Six failing test files exist for the api-keys module covering all 5 reqs (RED phase of TDD)"
    - "Schema convention is verified: zero `CREATE EXTENSION` lines in schema.sql (so the Wave 1 migration must NOT add one)"
    - "Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` SHA `4f868d318abff71f8c8bfbcf443b2393a553018b` is unchanged"
  artifacts:
    - path: "livos/packages/livinityd/source/modules/api-keys/database.test.ts"
      provides: "FR-BROKER-B1-02 tests — key generation regex, SHA-256 storage, plaintext NOT in DB"
      min_lines: 30
    - path: "livos/packages/livinityd/source/modules/api-keys/cache.test.ts"
      provides: "Positive 60s TTL, negative 5s TTL, explicit invalidate(), last_used_at debouncer flush behavior"
      min_lines: 30
    - path: "livos/packages/livinityd/source/modules/api-keys/bearer-auth.test.ts"
      provides: "FR-BROKER-B1-03 tests — Authorization parse, 401 Anthropic shape, no fall-through on invalid Bearer"
      min_lines: 30
    - path: "livos/packages/livinityd/source/modules/api-keys/routes.test.ts"
      provides: "FR-BROKER-B1-04 tests — create returns plaintext once, list excludes plaintext+hash, revoke is user-scoped + idempotent"
      min_lines: 30
    - path: "livos/packages/livinityd/source/modules/api-keys/schema-migration.test.ts"
      provides: "FR-BROKER-B1-01 tests — table columns, FK CASCADE, IF NOT EXISTS guards, partial index, NO CREATE EXTENSION"
      min_lines: 30
    - path: "livos/packages/livinityd/source/modules/api-keys/mount-order.test.ts"
      provides: "Mount order assertion — Bearer middleware between mountUsageCaptureMiddleware (line 1228) and mountBrokerRoutes (line 1234)"
      min_lines: 20
  key_links:
    - from: "Wave 0 test files"
      to: "Wave 1+ implementation files (api-keys/{database,cache,bearer-auth,routes,events,index}.ts)"
      via: "Vitest RED-phase contract — implementation must satisfy these tests"
      pattern: "vi.mock\\('\\.\\./database/index\\.js'"
    - from: "schema-migration.test.ts"
      to: "schema.sql line 339+"
      via: "Wave 1 will append api_keys block here; test asserts the exact shape"
      pattern: "CREATE TABLE IF NOT EXISTS api_keys"
---

<objective>
Wave 0: Stand up the test infrastructure for the new `api-keys/` module before any production code is written. Six failing test files describe the exact contract Waves 1-3 must satisfy — RED phase per the project's test-first convention (precedent: usage-tracking schema-migration test, common.test.ts, fail2ban-admin tests).

Also performs the schema-convention pre-flight: confirm by grep that the codebase has ZERO `CREATE EXTENSION` lines in `schema.sql`. This locks in the convention before Wave 1 appends the `api_keys` block (and codifies the verdict on RESEARCH.md Open Question 1: drop the `CREATE EXTENSION pgcrypto` line that CONTEXT.md suggests but the codebase contradicts).

Purpose: Every downstream wave (1, 2, 3) gets a deterministic, executable specification. Without Wave 0, executors would invent their own contracts and drift.

Output:
- `livos/packages/livinityd/source/modules/api-keys/` directory with 6 test files (all failing — implementation does not yet exist)
- Verified codebase invariants:
  - `grep -c "CREATE EXTENSION" livos/packages/livinityd/source/modules/database/schema.sql` returns 0
  - Sacred file SHA still `4f868d31...` (no edits)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-bearer-token-auth/59-CONTEXT.md
@.planning/phases/59-bearer-token-auth/59-RESEARCH.md
@livos/packages/livinityd/source/modules/usage-tracking/schema-migration.test.ts
@livos/packages/livinityd/source/modules/usage-tracking/database.test.ts
@livos/packages/livinityd/source/modules/server/trpc/common.test.ts

<interfaces>
<!-- Test-only contracts the executor needs. Extracted from twin patterns + CONTEXT.md. -->
<!-- Implementation files DO NOT yet exist; tests use vi.mock to stub PG. -->

Mock pattern (mirrors usage-tracking/database.test.ts:8-22):
```typescript
import {beforeEach, describe, expect, test, vi} from 'vitest'

const queryMock = vi.fn()
vi.mock('../database/index.js', () => ({
  getPool: () => ({query: queryMock}),
}))

// Import AFTER mock setup
import {createApiKey, findApiKeyByHash, revokeApiKey, ...} from './database.js'
```

Anthropic-spec error body shape (mirrors livinity-broker/auth.ts:62-67):
```typescript
{
  type: 'error',
  error: {type: 'authentication_error', message: 'API key invalid' | 'API key revoked'},
}
```

Key generation regex (per CONTEXT.md + RESEARCH.md):
```
^liv_sk_[A-Za-z0-9_-]{32}$
```

Schema convention check (RESEARCH.md Open Question 1 verdict):
```bash
grep -c "CREATE EXTENSION" livos/packages/livinityd/source/modules/database/schema.sql
# MUST return 0 — Wave 1 follows the established codebase convention.
```

Vitest run command (per livos/packages/livinityd/package.json):
```bash
pnpm --filter @livos/livinityd test livos/packages/livinityd/source/modules/api-keys/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema convention pre-flight + create test directory + database.test.ts + schema-migration.test.ts + mount-order.test.ts</name>
  <files>livos/packages/livinityd/source/modules/api-keys/database.test.ts, livos/packages/livinityd/source/modules/api-keys/schema-migration.test.ts, livos/packages/livinityd/source/modules/api-keys/mount-order.test.ts</files>
  <read_first>
    - .planning/phases/59-bearer-token-auth/59-CONTEXT.md (locked decisions, schema, key format)
    - .planning/phases/59-bearer-token-auth/59-RESEARCH.md §Pitfall 6 (pgcrypto convention) + §Pattern 1 (database twin) + §Schema Append
    - livos/packages/livinityd/source/modules/usage-tracking/schema-migration.test.ts (template for schema-migration.test.ts)
    - livos/packages/livinityd/source/modules/usage-tracking/database.test.ts (template for database.test.ts mocking pattern)
    - livos/packages/livinityd/source/modules/docker/agents.ts (twin shape for createApiKey/findApiKeyByHash/revokeApiKey functions under test)
    - livos/packages/livinityd/source/modules/database/schema.sql (convention check — confirm zero `CREATE EXTENSION`)
    - livos/packages/livinityd/source/modules/server/index.ts lines 1215-1240 (mount slot)
  </read_first>
  <behavior>
    - Test 1 (database.test.ts T1): `createApiKey({userId, name})` issues exactly one `INSERT INTO api_keys (user_id, key_hash, key_prefix, name) VALUES ($1, $2, $3, $4) RETURNING ...` — params position-correct, returns `{row, plaintext}` where `plaintext` matches `/^liv_sk_[A-Za-z0-9_-]{32}$/` and `key_hash` is the 64-hex-char SHA-256 of the plaintext.
    - Test 2 (database.test.ts T2): `findApiKeyByHash(hash)` issues `SELECT ... FROM api_keys WHERE key_hash = $1 AND revoked_at IS NULL` (mock returns row → returns mapped object; mock returns empty → returns null).
    - Test 3 (database.test.ts T3): `revokeApiKey({id, userId})` issues `UPDATE api_keys SET revoked_at = NOW() WHERE id = $1 AND user_id = $2 AND revoked_at IS NULL` — user-scoped + idempotent.
    - Test 4 (database.test.ts T4): plaintext is NEVER stored — assert no `INSERT` query call carries the raw plaintext as a param (only its SHA-256 digest).
    - Test 5 (database.test.ts T5): `listApiKeysForUser(userId)` SELECT excludes `key_hash` from columns; orders by `created_at DESC`.
    - Test 6 (schema-migration.test.ts T1): schema.sql contains `CREATE TABLE IF NOT EXISTS api_keys` and indexes `idx_api_keys_user_id` + `idx_api_keys_active` (partial — `WHERE revoked_at IS NULL`).
    - Test 7 (schema-migration.test.ts T2): every CREATE in the api_keys block uses `IF NOT EXISTS`.
    - Test 8 (schema-migration.test.ts T3): column shape — `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `key_hash CHAR(64) NOT NULL UNIQUE`, `key_prefix VARCHAR(16) NOT NULL`, `name VARCHAR(64) NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `last_used_at TIMESTAMPTZ`, `revoked_at TIMESTAMPTZ`.
    - Test 9 (schema-migration.test.ts T4 — convention guard, RESEARCH.md Pitfall 6): assert `sql.match(/CREATE EXTENSION/g)` is null. This locks in the codebase convention and prevents a regression where the `pgcrypto` line creeps back in (CONTEXT.md says yes; codebase says no — codebase wins).
    - Test 10 (mount-order.test.ts): read `server/index.ts` source as text; find positions of `mountUsageCaptureMiddleware(`, `mountBearerAuthMiddleware(`, and `mountBrokerRoutes(`; assert position(usage) < position(bearer) < position(broker). Mirrors common.test.ts:160-178 source-string-assert pattern.
  </behavior>
  <action>
    1. Create directory `livos/packages/livinityd/source/modules/api-keys/`.
    2. Pre-flight grep (record output as evidence in commit message):
       ```bash
       grep -c "CREATE EXTENSION" livos/packages/livinityd/source/modules/database/schema.sql
       # Expected: 0. If non-zero, STOP and flag the planner — convention has shifted.
       ```
    3. Sacred file SHA pre-flight:
       ```bash
       git hash-object nexus/packages/core/src/sdk-agent-runner.ts
       # Expected: 4f868d318abff71f8c8bfbcf443b2393a553018b
       ```
    4. Create `database.test.ts` (per FR-BROKER-B1-01..02 + FR-BROKER-B1-04). Mirror `usage-tracking/database.test.ts:8-60` mock pattern. Write Tests 1-5 above. Do NOT implement the production module — tests must FAIL with "module not found" or "function not exported".
    5. Create `schema-migration.test.ts` (per FR-BROKER-B1-01). Mirror `usage-tracking/schema-migration.test.ts:14-66` exactly. Write Tests 6-9 above. Test 9 is the explicit `CREATE EXTENSION` absence guard.
    6. Create `mount-order.test.ts` (per FR-BROKER-B1-03). Read `server/index.ts` via fs and assert string-positional ordering of the three mount calls. Test must FAIL because `mountBearerAuthMiddleware` is not yet referenced in `server/index.ts`.
    7. Run `pnpm --filter @livos/livinityd test livos/packages/livinityd/source/modules/api-keys/` — expect ALL three test files to FAIL (RED phase). Capture failure output.
    8. NO edit to `nexus/packages/core/src/sdk-agent-runner.ts`. Confirm SHA unchanged at end of task.
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/api-keys/database.test.ts source/modules/api-keys/schema-migration.test.ts source/modules/api-keys/mount-order.test.ts --reporter=verbose 2>&1 | grep -E "(FAIL|database.test|schema-migration|mount-order)"</automated>
  </verify>
  <done>
    - Three test files exist under `livos/packages/livinityd/source/modules/api-keys/`.
    - All three test files FAIL when run (expected — implementation comes in later waves).
    - `grep -c "CREATE EXTENSION" livos/packages/livinityd/source/modules/database/schema.sql` returns `0`.
    - `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cache.test.ts + bearer-auth.test.ts + routes.test.ts</name>
  <files>livos/packages/livinityd/source/modules/api-keys/cache.test.ts, livos/packages/livinityd/source/modules/api-keys/bearer-auth.test.ts, livos/packages/livinityd/source/modules/api-keys/routes.test.ts</files>
  <read_first>
    - .planning/phases/59-bearer-token-auth/59-CONTEXT.md §Bearer Middleware Mount Order + §last_used_at Update Debouncing + §tRPC Routes
    - .planning/phases/59-bearer-token-auth/59-RESEARCH.md §Pattern 3 (Bearer middleware) + §Pattern 4 (tRPC routes) + §Pitfall 1 (cache invalidation)
    - livos/packages/livinityd/source/modules/livinity-broker/auth.ts (Anthropic-spec 401 body shape, lines 62-67)
    - livos/packages/livinityd/source/modules/usage-tracking/routes.test.ts (template for tRPC route tests)
    - livos/packages/livinityd/source/modules/usage-tracking/capture-middleware.test.ts (Express middleware test pattern with req/res mocks)
    - livos/packages/livinityd/source/modules/server/trpc/trpc.ts (privateProcedure / adminProcedure definitions for ctx shape)
  </read_first>
  <behavior>
    - cache.test.ts T1: `cache.setValid(hash, {userId, id})` then `cache.get(hash)` returns `{kind:'valid', userId, id}` within 60s; after >60s of mocked time advance, returns `undefined`.
    - cache.test.ts T2: `cache.setInvalid(hash)` returns `{kind:'invalid'}` within 5s; after >5s, `undefined`.
    - cache.test.ts T3: `cache.invalidate(hash)` IMMEDIATELY removes a previously-valid entry — a follow-up `cache.get(hash)` returns `undefined`. Asserts the synchronous invalidation contract that Wave 3's `apiKeys.revoke` will call (closes RESEARCH.md Pitfall 1 + Open Question 3).
    - cache.test.ts T4: `cache.touchLastUsed(hash)` queues a timestamp; calling `cache.flushLastUsed()` issues a single batched `UPDATE api_keys SET last_used_at = $1 WHERE key_hash IN ($2, $3, ...)` (or per-key updates — implementation detail) for all queued hashes; after flush, internal queue is empty.
    - cache.test.ts T5: multiple `touchLastUsed` calls on the same hash within 1 minute coalesce into ONE flushed write (debouncing per CONTEXT.md).
    - bearer-auth.test.ts T1: missing Authorization header → middleware calls `next()` without setting `req.userId`. (Legacy URL-path resolver still runs.)
    - bearer-auth.test.ts T2: Authorization not starting with `Bearer liv_sk_` → `next()` without setting `req.userId`.
    - bearer-auth.test.ts T3: valid Bearer (cache HIT positive) → sets `req.userId`, `req.authMethod = 'bearer'`, `req.apiKeyId`, calls `next()`. PG mock NOT queried.
    - bearer-auth.test.ts T4: valid Bearer (cache MISS, PG returns row) → SHA-256 of plaintext queried via `findApiKeyByHash`, cache populated `setValid`, `req.userId` set, `next()` called.
    - bearer-auth.test.ts T5: invalid Bearer (cache MISS, PG returns null) → `res.status(401).json({type:'error', error:{type:'authentication_error', message:'API key invalid'}})`, NEVER calls `next()`. Cache populated with `setInvalid` (5s negative TTL).
    - bearer-auth.test.ts T6: invalid Bearer (cache HIT negative) → 401 IMMEDIATELY without PG query (negative cache fast-path).
    - bearer-auth.test.ts T7: revoked key (PG returns null because of `WHERE revoked_at IS NULL` filter) → 401 with `message: 'API key invalid'` (revoked rows are indistinguishable from unknown rows at the DB layer; this is acceptable per CONTEXT.md error-shape spec — both map to authentication_error). Note: the integration test (Wave 4) verifies that the revoke flow specifically returns this 401.
    - bearer-auth.test.ts T8 (constant-time compare belt-and-suspenders, RESEARCH.md Pattern 2): when middleware compares `presentedKeyHash` against `row.key_hash`, the comparison uses `crypto.timingSafeEqual` (mock the function and assert it was called).
    - routes.test.ts T1: `apiKeys.create({name:'my-key'})` returns `{id, plaintext, prefix, name, created_at, oneTimePlaintextWarning:true}`; `plaintext` matches `/^liv_sk_[A-Za-z0-9_-]{32}$/`; `prefix` is the first 8 chars of `plaintext`.
    - routes.test.ts T2: a SECOND call to `apiKeys.list({})` for the same user returns rows that DO NOT include the `plaintext` field nor the `key_hash` field — only `id, key_prefix, name, created_at, last_used_at, revoked_at`.
    - routes.test.ts T3: `apiKeys.revoke({id})` issues UPDATE scoped by `user_id = ctx.currentUser.id`; on zero rows affected throws `TRPCError({code:'NOT_FOUND'})`.
    - routes.test.ts T4: `apiKeys.revoke` calls `cache.invalidate(key_hash)` AFTER successful UPDATE (mock cache module; assert call). Closes RESEARCH.md Pitfall 1.
    - routes.test.ts T5: `apiKeys.create` and `apiKeys.revoke` BOTH emit `recordApiKeyEvent({action:'create_key'|'revoke_key', keyId, userId, username, success:true})` — REUSE per Phase 46 audit pattern. Mock the events module; assert call.
    - routes.test.ts T6: `apiKeys.listAll` is wired as `adminProcedure` — read `routes.ts` source string, assert regex `/listAll:\s*adminProcedure/` matches (defense-in-depth check mirrors common.test.ts:160-178).
    - routes.test.ts T7: input validation — `apiKeys.create({name:''})` rejects via zod; `apiKeys.create({name:'a'.repeat(65)})` rejects (max 64 chars per CONTEXT.md schema).
  </behavior>
  <action>
    1. Create `cache.test.ts`. Use `vi.useFakeTimers()` for TTL tests. Mock the PG query for the flush test (mirror database.test.ts mock pattern). Tests must FAIL with module-not-found until Wave 2 creates `cache.ts`.
    2. Create `bearer-auth.test.ts`. Mock Express `req`/`res`/`next` (mirror `usage-tracking/capture-middleware.test.ts` setup). Mock `findApiKeyByHash` and `cache.get/setValid/setInvalid`. For T8, mock `crypto.timingSafeEqual` via `vi.spyOn(crypto, 'timingSafeEqual')`. Tests must FAIL until Wave 2 creates `bearer-auth.ts`.
    3. Create `routes.test.ts`. Mirror `usage-tracking/routes.test.ts` setup — call procedures via `usageRouter.createCaller(ctx)` pattern. Mock `ctx.currentUser = {id:'user-A', username:'alice'}` for privateProcedure tests; `{id:'admin-A', username:'admin', role:'admin'}` for adminProcedure tests. Tests must FAIL until Wave 3 creates `routes.ts`.
    4. Run `pnpm --filter @livos/livinityd test livos/packages/livinityd/source/modules/api-keys/cache.test.ts livos/packages/livinityd/source/modules/api-keys/bearer-auth.test.ts livos/packages/livinityd/source/modules/api-keys/routes.test.ts` — expect ALL to fail.
    5. Sacred file SHA assertion (no edits).
  </action>
  <verify>
    <automated>cd livos/packages/livinityd && npx vitest run source/modules/api-keys/cache.test.ts source/modules/api-keys/bearer-auth.test.ts source/modules/api-keys/routes.test.ts --reporter=verbose 2>&1 | grep -E "(FAIL|cache.test|bearer-auth|routes.test)"</automated>
  </verify>
  <done>
    - Three test files exist under `livos/packages/livinityd/source/modules/api-keys/`.
    - All three FAIL with "module not found" or "import error" (RED phase — implementation does not yet exist).
    - Total Wave 0 test file count = 6 (database, schema-migration, mount-order, cache, bearer-auth, routes).
    - `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → mocked PG | Tests must NEVER hit a real PG; all PG access via `vi.mock('../database/index.js')` |
| Test files → sacred file | Tests must NEVER import from `nexus/packages/core/src/sdk-agent-runner.ts`; SHA pin enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | T (Tampering) | schema-migration.test.ts | mitigate | Test 9 explicitly asserts `CREATE EXTENSION` absence, so a future drift adding the line is blocked at CI |
| T-59-02 | I (Information disclosure) | database.test.ts | mitigate | Test 4 asserts plaintext is NEVER passed as a query param (only the SHA-256 digest) — protects against a regression where someone "stores plaintext for debugging" |
| T-59-03 | R (Repudiation) | routes.test.ts | mitigate | Test 5 asserts every create/revoke emits a `recordApiKeyEvent` call so audit trail is non-bypassable |
| T-59-04 | T (Tampering) | mount-order.test.ts | mitigate | Source-string positional check on server/index.ts catches any reorder regression that would silently bypass Bearer auth |
</threat_model>

<verification>
- All 6 test files exist under `livos/packages/livinityd/source/modules/api-keys/`
- `pnpm --filter @livos/livinityd test livos/packages/livinityd/source/modules/api-keys/` runs all 6 files and ALL fail (RED phase confirmed; no test passes prematurely)
- `grep -c "CREATE EXTENSION" livos/packages/livinityd/source/modules/database/schema.sql` returns `0`
- `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b`
</verification>

<success_criteria>
- 6 failing test files in `api-keys/` cover all 5 phase requirements (FR-BROKER-B1-01..05) — Wave 0 to Wave 3 contracts are fully specified before any production code is written
- Schema convention pre-flight closes RESEARCH.md Open Question 1: zero `CREATE EXTENSION` lines in schema.sql, so Wave 1 follows the established convention (drops the `CREATE EXTENSION pgcrypto` line CONTEXT.md suggests)
- Sacred file untouched
</success_criteria>

<output>
After completion, create `.planning/phases/59-bearer-token-auth/59-01-SUMMARY.md` with:
- Schema convention pre-flight result (grep output as evidence)
- Sacred file SHA pre-flight result
- Test file list with failure reasons (one line per file)
- Sacred file SHA at end of plan (must equal start)
</output>
