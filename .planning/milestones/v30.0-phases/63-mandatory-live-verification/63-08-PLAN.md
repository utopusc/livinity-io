---
phase: 63-mandatory-live-verification
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
autonomous: false
requirements:
  - FR-VERIFY-V30-07
tags:
  - phase-63
  - uat-walks
  - v29.4
  - carry-forward
  - sacred-file

must_haves:
  truths:
    - "All 4 v29.4 VERIFICATION/UAT files walked: 45-VERIFICATION.md, 46-UAT.md, 47-UAT.md, 48-UAT.md"
    - "Phase 45 — 8 sections + 2 explicit human-verify steps (OpenAI streaming usage + WS reconnect survival)"
    - "Phase 46 — 9 sections (Pre-flight + 8 numbered + closing checklist): Sidebar+jail / 3 service-state banners / Unban+whitelist / Manual ban + self-ban gate / Audit log immutability / Mobile cellular toggle / Settings backout toggle / httpOnlyPaths restart / End-to-end SSH lockout recovery"
    - "Phase 47 — 9 success criteria SC-1..SC-9 (Diagnostics 3 cards / Capability Registry 5-cat / Re-sync atomic-swap / Model Identity 6-step / Verdict-driven remediation / Post-fix re-diagnostic / apps.healthProbe shape / healthProbe scoped to currentUser / Branch B audit append-only)"
    - "Phase 48 — 9 numbered steps + Prep + master gate"
    - "ZERO BLOCKED rows for the v29.4 batch"
    - "Sacred file SHA = 4f868d318abff71f8c8bfbcf443b2393a553018b at end of plan"
  artifacts:
    - path: ".planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md"
      provides: "Append-only UAT results table; Wave 4b populates the v29.4 phases section (~35-40 rows)"
      contains: "v29.4"
  key_links:
    - from: "63-UAT-RESULTS.md table rows"
      to: "Phase 45/46/47/48 *-VERIFICATION/UAT.md sections"
      via: "1:1 row-per-section/numbered-step mapping"
      pattern: "\\|\\s*v29.4\\s*/\\s*(45|46|47|48)"
---

<objective>
Wave 4b — UAT walks for v29.4 carry-forward (Phases 45, 46, 47, 48). Continues FR-VERIFY-V30-07; covers 4 of 16 files.

Purpose: v29.4 milestone audit was `passed` mechanism-wise but 4 manual UAT files were un-executed. This wave closes that debt.

Output:
- `63-UAT-RESULTS.md` — appended with ~35-40 rows for v29.4 phases.

Step inventory (per 63-RESEARCH.md UAT inventory rows 7-10):
- Phase 45 (45-VERIFICATION.md): 8 sections + 2 human-verify steps (OpenAI streaming usage non-zero round-trip; WS reconnect survival).
- Phase 46 (46-UAT.md): 9 sections — fail2ban admin panel.
- Phase 47 (47-UAT.md): 9 success criteria SC-1..SC-9 — AI Diagnostics.
- Phase 48 (48-UAT.md): 9 numbered steps + Prep + master gate — Live SSH Session Viewer.

This plan implements ONE phase requirement (FR-VERIFY-V30-07) for 4 of 16 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-mandatory-live-verification/63-CONTEXT.md
@.planning/phases/63-mandatory-live-verification/63-RESEARCH.md
@.planning/phases/63-mandatory-live-verification/63-07-SUMMARY.md
@.planning/milestones/v29.4-phases/45-carry-forward-sweep/45-VERIFICATION.md
@.planning/milestones/v29.4-phases/46-fail2ban-admin-panel/46-UAT.md
@.planning/milestones/v29.4-phases/47-ai-diagnostics/47-UAT.md
@.planning/milestones/v29.4-phases/48-live-ssh-session-viewer/48-UAT.md

<interfaces>
<!-- 63-UAT-RESULTS.md table format (canonical): -->
<!-- | phase | uat_id | step_id | description | result | evidence | timestamp | -->

<!-- Step ID convention for v29.4 batch: -->
<!--   Phase 45: section-1 .. section-8; human-verify-1, human-verify-2 -->
<!--   Phase 46: pre-flight; section-1 .. section-8; closing-checklist -->
<!--   Phase 47: SC-1 .. SC-9 -->
<!--   Phase 48: prep; step-1 .. step-9; master-gate -->

<!-- Phase 45 specific: -->
<!-- - human-verify-1: OpenAI streaming usage non-zero round-trip — RUN Phase 42 verbatim openai SDK smoke + assert total_tokens > 0 AND broker_usage row -->
<!-- - human-verify-2: WS reconnect survival — `systemctl restart livos` + within 2s trigger usage.getMine poll + ai.claudePerUserStartLogin -->

<!-- Phase 46 specific (fail2ban admin panel): -->
<!-- - Browser walk through Settings → Security → fail2ban panel -->
<!-- - Test self-ban gate (do NOT actually self-ban — verify the gate UI). -->
<!-- - Test mobile cellular toggle (Settings backout) — confirm visibility/toggle. -->
<!-- - End-to-end SSH lockout recovery — re-walk if previously deferred. CAUTION: do NOT lock yourself out; test on a separate non-essential IP if possible. -->

<!-- Phase 47 specific: -->
<!-- - SC-3 Re-sync atomic-swap — trigger via Diagnostics UI; verify Lua RENAME script ran (server log inspection). -->
<!-- - SC-4 Model Identity 6-step diagnostic — already verdict=`neither` per v29.4 close; re-run on live to confirm. -->

<!-- Phase 48 specific: -->
<!-- - SSH Sessions tab in Security section. -->
<!-- - Trigger an actual SSH attempt (intentional bad password from a separate machine) → confirm session row appears in real-time. -->
<!-- - Click-to-copy and click-to-ban (test ban gate but don't actually self-ban orchestrator IP). -->
<!-- - Master gate: `npm run test:phase48` — run via single SSH (it's the integration test suite). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk Phase 45 — Carry-Forward Sweep (8 sections + 2 human-verify steps)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
Read `45-VERIFICATION.md`. Walk all 8 sections + 2 human-verify steps.

The 2 human-verify steps explicitly:

**human-verify-1 (FR-CF-04 OpenAI streaming usage non-zero round-trip):**
This is the v29.3 Phase 42 verbatim openai SDK smoke pattern. Reuse the curl + Python SDK results from Wave 2 (63-03), since they already validated streaming usage from the same broker. Also add a fresh OpenAI SDK smoke (not Anthropic):

```bash
# Single-shot OpenAI SDK smoke against /v1/chat/completions
{
  echo "=== OPENAI SDK SMOKE FOR PHASE 45 HUMAN-VERIFY-1 ==="
  cd .planning/phases/63-mandatory-live-verification/scripts
  source .venv-phase63/Scripts/activate 2>/dev/null || source .venv-phase63/bin/activate
  pip install --quiet openai
  python3 -c "
import os
from openai import OpenAI
client = OpenAI(api_key=os.environ['LIV_SK_PHASE63'], base_url='https://api.livinity.io/v1')
resp = client.chat.completions.create(
    model='claude-sonnet-4-6',
    messages=[{'role':'user','content':'Hi'}],
    max_tokens=50,
    stream=False
)
print('id=', resp.id)
print('usage=', resp.usage)
assert resp.usage.total_tokens > 0, 'total_tokens must be > 0'
print('PASS: openai sdk total_tokens > 0')
"
  deactivate
} | tee -a .planning/phases/63-mandatory-live-verification/63-uat-phase45.log
```

Then single SSH for broker_usage row:
```bash
ssh -i ... bruce@10.69.31.68 \
  "source /opt/livos/.env; \
   psql \"\$DATABASE_URL\" -c \"SELECT id, route, prompt_tokens, completion_tokens FROM broker_usage WHERE created_at > NOW() - INTERVAL '2 minutes' AND route LIKE '%chat/completions%' ORDER BY created_at DESC LIMIT 1;\""
```

**human-verify-2 (WS reconnect survival):**
Browser walk:
1. Open LivOS UI in Chromium → leave on a page that uses tRPC subscriptions (e.g., AI Chat or Usage panel).
2. SSH single-batched: `systemctl restart livos && sleep 5 && systemctl is-active livos`.
3. Within 2s of restart-complete (livos active again), trigger a tRPC poll in the browser by clicking refresh on Usage panel OR navigating to Settings → AI Configuration.
4. ASSERT: UI does not show "disconnected" error for >5s; tRPC subscriptions reconnect; query results populate.
5. Capture screenshot to `evidence/63-uat-phase45-ws-reconnect.png`.

**Other 8 sections of 45-VERIFICATION.md:**
Most are about C1/C2/C3/C4 carry-forward integration gaps that landed as commits. Each is verifiable by:
- Source code grep (e.g., for C1 broker 429 forwarding: grep `Retry-After` in `livinity-broker/router.ts`).
- Server log inspection (e.g., for C3 httpOnlyPaths: a tRPC call should not 502; verify by triggering one).
- Sacred file SHA test (C2 — sacred-file integrity test passes; this is also a Wave 5 task but record here).

Append rows to 63-UAT-RESULTS.md.

Example:
```markdown
| v29.4 / 45 | FR-CF-01 | section-1 | C1 broker 429 forwarding lands | PASS | source grep livinity-broker/router.ts L159 / Retry-After preservation pattern present | <timestamp> |
| v29.4 / 45 | FR-CF-04 | human-verify-1 | OpenAI streaming usage non-zero round-trip | PASS | 63-uat-phase45.log openai SDK total_tokens=N; broker_usage row with non-zero tokens | <timestamp> |
| v29.4 / 45 | FR-CF-05 | human-verify-2 | WS reconnect survival | PASS | evidence/63-uat-phase45-ws-reconnect.png; restart livos + UI reconnect <5s | <timestamp> |
```
  </what-built>
  <how-to-verify>
1. ≥10 rows added with phase column "v29.4 / 45".
2. ZERO BLOCKED.
3. `evidence/63-uat-phase45-ws-reconnect.png` exists.
  </how-to-verify>
  <resume-signal>Type "phase-45-walked" with PASS counts, OR list BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-45-walked" with PASS counts, OR list BLOCKED + remediation"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walk Phase 46 — Fail2ban Admin Panel (9 sections)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
Read `46-UAT.md`. 9 sections (per 63-RESEARCH.md UAT inventory row 8): Pre-flight + 8 numbered + closing checklist.

Browser walk (LivOS UI, NOT Mini PC SSH — this is a UI feature):

1. **Pre-flight:** Open LIVINITY_docker → Security section appears in 13-entry sidebar (already verified in 63-07 task 3 — cross-reference).
2. **Section 1 — Sidebar+jail:** Click Security → fail2ban panel loads → jail list populates from `/etc/fail2ban/jail.local` via `fail2ban-client status`.
3. **Section 2 — 3 service-state banners:** UI shows running/stopped/disabled states.
4. **Section 3 — Unban + whitelist:** Pick an arbitrary banned IP (or simulate one) → click Unban → modal opens → check "Add to whitelist" → submit → IP appears in `ignoreip` list.
5. **Section 4 — Manual ban + self-ban gate:** Try to ban an IP → modal demands typing "LOCK ME OUT" exact-string. Try CIDR /0 — Zod rejects. Try the orchestrator's own IP — dual-IP self-ban detection blocks. (DO NOT actually self-ban.)
6. **Section 5 — Audit log immutability:** Open audit log → confirm rows for prior bans/unbans visible; trigger an action (e.g., re-ban a test IP) → row appears with sentinel `device_id='fail2ban-host'`.
7. **Section 6 — Mobile cellular toggle:** Settings → AdvancedSection → "Mobile cellular" toggle visible.
8. **Section 7 — Settings backout toggle:** Settings → AdvancedSection → "Settings backout" toggle visible.
9. **Section 8 — httpOnlyPaths restart:** SSH single-batched `systemctl restart livos`; within 5s trigger fail2ban panel reload → no 502 / no WebSocket failure.
10. **Section 9 — End-to-end SSH lockout recovery:** Documented procedure works (DO NOT actually trigger lockout; verify procedure documentation visible in Settings).
11. **Closing checklist:** All 8 sections have evidence (screenshots OR log lines OR source grep).

Capture screenshots to `evidence/63-uat-phase46-{section}.png`.

Append 11 rows (Pre-flight + 8 sections + closing-checklist + section-9) to 63-UAT-RESULTS.md.
  </what-built>
  <how-to-verify>
1. ≥10 rows added with "v29.4 / 46".
2. ZERO BLOCKED.
3. ≥3 screenshots in evidence/ with names matching `63-uat-phase46-*.png`.
  </how-to-verify>
  <resume-signal>Type "phase-46-walked" with PASS counts + screenshot list, OR list BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-46-walked" with PASS counts + screenshot list, OR list BLOCKED + rem"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk Phase 47 — AI Diagnostics (SC-1..SC-9, 9 success criteria)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
Read `47-UAT.md`. 9 SCs.

Browser walk:

- **SC-1 — Diagnostics 3 cards:** LIVINITY_docker → Diagnostics section → 3 cards visible (Capability Registry / Model Identity / App Health).
- **SC-2 — Capability Registry 5-cat:** Capability Registry card → 5 categories: missing_lost / missing_precondition / disabled_by_user / present / unknown.
- **SC-3 — Re-sync atomic-swap:** Click "Resync" button → server-side temp Redis prefix populated → Lua RENAME script runs → swap completes; user override re-applied. Verify via single SSH:
  ```bash
  ssh ... "source /opt/livos/.env; \
    REDIS_PASS=\$(echo \$REDIS_URL | sed -E 's|.*://[^:]*:([^@]+)@.*|\1|' | sed 's|%21|!|g'); \
    redis-cli -a \"\$REDIS_PASS\" --no-auth-warning KEYS 'nexus:cap:tool:*' | head -3; \
    journalctl -u livos -n 100 --since '1 min ago' | grep -E 'RENAME|atomic-swap|resync' | head -5"
  ```
- **SC-4 — Model Identity 6-step:** Click "Run diagnostic" on Model Identity card → 6 steps run → verdict displayed (`neither` per v29.4 close, OR re-evaluated on v30 broker).
- **SC-5 — Verdict-driven remediation:** Click "Remediate" → either no-op (if `neither`) OR concrete fix-action.
- **SC-6 — Post-fix re-diagnostic:** Run diagnostic again after remediation → verdict updates.
- **SC-7 — apps.healthProbe shape:** Trigger via Diagnostics UI → check JSON shape via DevTools network tab.
- **SC-8 — healthProbe scoped to currentUser:** Open second browser session for different user → confirm probe returns only their apps.
- **SC-9 — Branch B audit append-only:** Trigger any diagnostics action → audit row appears in `device_audit_log`. Verify via single SSH psql.

Append 9 rows.
  </what-built>
  <how-to-verify>
1. ≥9 rows added with "v29.4 / 47".
2. ZERO BLOCKED.
3. SC-3 evidence shows resync succeeded (Lua RENAME log line).
  </how-to-verify>
  <resume-signal>Type "phase-47-walked" with PASS counts, OR list BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-47-walked" with PASS counts, OR list BLOCKED + remediation"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Walk Phase 48 — Live SSH Session Viewer (Prep + 9 steps + master gate)</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <what-built>
Read `48-UAT.md`. Prep + Steps 1-9 + master gate.

- **Prep:** Open LIVINITY_docker → Security → SSH Sessions tab.
- **Step 1 — UI loads:** Tab opens; live tail panel visible.
- **Step 2 — Trigger SSH attempt:** From a SECOND machine (not the orchestrator), attempt SSH with bad password to bruce@10.69.31.68. Confirm a row appears in real-time in the tab.
- **Step 3 — Click-to-copy:** Click an IP → clipboard contains the IP.
- **Step 4 — Click-to-ban:** Click the failed IP → ban-ip-modal opens with the IP pre-populated. (DO NOT actually submit unless that IP is genuinely bad.)
- **Step 5 — Scroll-tolerance + Resume tail:** Scroll up in the panel → "Resume tailing" button appears → click resumes auto-scroll.
- **Step 6 — RBAC gate:** Open second session as non-admin user → SSH Sessions tab denies access (WS handshake closes with code 4403).
- **Step 7 — Backout:** Settings → AdvancedSection → "SSH Sessions" toggle exists; toggle off → tab disappears; toggle on → returns.
- **Step 8 — Sacred file integrity:** Wave 5 will run this; record as PASS-pending.
- **Step 9 — `test:phase48` master gate:** Single SSH to Mini PC: `cd /opt/livos && npm run test:phase48 2>&1 | tail -30`. Confirm exit 0.

Append 11 rows (Prep + 9 steps + master-gate). Capture key screenshots.

NOTE: If `test:phase48` script doesn't exist on Mini PC (script may have been a development-time alias only, not deployed to /opt/livos), record OBSOLETE for that one step with evidence "test:phase48 script not present in /opt/livos package.json — covered by deploy-time vitest run". Do NOT mark BLOCKED — the integration tests are exercised at update.sh build time.
  </what-built>
  <how-to-verify>
1. ≥11 rows added with "v29.4 / 48".
2. ZERO BLOCKED. (OBSOLETE acceptable for Step 9 if script not present.)
3. Step 2 evidence shows real-time row appearance (screenshot or recording).
  </how-to-verify>
  <resume-signal>Type "phase-48-walked" with PASS counts, OR list BLOCKED + remediation</resume-signal>

  <action>
    Human-in-the-loop checkpoint. Execute the steps described in <what-built> above, capture evidence into the listed <files>, and verify per <how-to-verify>. The <action> tag is repeated here for tooling-validator compatibility; the substantive instructions live in <what-built> + <how-to-verify> above.
  </action>
  <verify>
    <automated>echo "checkpoint task -- resume signal: Type "phase-48-walked" with PASS counts, OR list BLOCKED + remediation"</automated>
  </verify>
  <done>Human types the resume-signal per <resume-signal> tag above; evidence files exist per <files> tag.</done>
</task>

<task type="auto">
  <name>Task 5: v29.4 batch closeout — count rows + sacred file SHA</name>
  <files>
    .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
  </files>
  <action>
Append closing summary to 63-UAT-RESULTS.md:

```bash
{
  echo ""
  echo "=== PLAN 63-08 CLOSE: v29.4 batch summary ==="
  date -u +%Y-%m-%dT%H:%M:%SZ
  V294_ROWS=$(grep -c '^| v29.4' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)
  BLOCKED=$(grep -c '^| v29.4.*| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)
  echo "v29.4 rows: $V294_ROWS"
  echo "v29.4 BLOCKED count: $BLOCKED (must be 0 for milestone close)"
  EXPECTED=4f868d318abff71f8c8bfbcf443b2393a553018b
  ACTUAL=$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)
  echo "expected: $EXPECTED"
  echo "actual:   $ACTUAL"
  if [ "$ACTUAL" = "$EXPECTED" ]; then echo "OK: sacred SHA holds"; else echo "BLOCK: sacred SHA drifted"; fi
  echo "=== END ==="
} | tee -a .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md
```
  </action>
  <verify>
    <automated>powershell -Command "$rows = (Select-String '^\| v29.4' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md).Count; $blocked = (Select-String '^\| v29.4.*\| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md).Count; if ($rows -ge 35 -and $blocked -eq 0 -and (git hash-object nexus/packages/core/src/sdk-agent-runner.ts) -eq '4f868d318abff71f8c8bfbcf443b2393a553018b') { exit 0 } else { Write-Output \"rows=$rows blocked=$blocked\"; exit 1 }"</automated>
  </verify>
  <done>v29.4 batch walked; ≥35 rows added; ZERO BLOCKED; sacred SHA holds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → Mini PC SSH (per task) | single-batched per task |
| orchestrator → LivOS browser (Security panel, Diagnostics) | per-user subdomain |
| second machine → bruce@10.69.31.68 SSH | for Phase 48 step-2 trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08-01 | Denial-of-Service | Phase 46 self-ban gate misfires; orchestrator IP gets banned | mitigate | Tasks explicitly say DO NOT actually self-ban; only verify the gate UI |
| T-63-08-02 | Tampering | Phase 47 SC-3 atomic-swap leaves Redis in inconsistent state | mitigate | Lua RENAME is atomic by design; verify via key sample after resync |
| T-63-08-03 | Spoofing | Phase 48 RBAC gate fails (non-admin sees SSH sessions) | mitigate | Step 6 explicit second-session test as non-admin; FAIL → Phase 48 hot-patch |
| T-63-08-04 | Denial-of-Service | Phase 48 step-2 SSH attempt triggers fail2ban; orchestrator IP banned | mitigate | Use a SECOND machine for the bad-password trigger; orchestrator's good SSH is separate |
</threat_model>

<verification>
**Plan-level verification:**
```bash
test -f .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md && \
  test "$(grep -c '^| v29.4' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)" -ge 35 && \
  test "$(grep -c '^| v29.4.*| BLOCKED' .planning/phases/63-mandatory-live-verification/63-UAT-RESULTS.md)" -eq 0 && \
  test "$(git hash-object nexus/packages/core/src/sdk-agent-runner.ts)" = "4f868d318abff71f8c8bfbcf443b2393a553018b"
```

**Sacred file SHA assertion:**
```bash
git hash-object nexus/packages/core/src/sdk-agent-runner.ts
# expected: 4f868d318abff71f8c8bfbcf443b2393a553018b
```
</verification>

<success_criteria>
- All 4 v29.4 UAT/VERIFICATION files walked (45, 46, 47, 48).
- ~35+ rows appended to 63-UAT-RESULTS.md with phase column showing v29.4/{N}.
- Each row has populated evidence column.
- ZERO BLOCKED rows (OBSOLETE acceptable for known-removed features).
- Sacred file SHA = `4f868d318abff71f8c8bfbcf443b2393a553018b`.
- ZERO `--accept-debt`. ZERO new override row in MILESTONES.md.
</success_criteria>

<output>
After completion, create `.planning/phases/63-mandatory-live-verification/63-08-SUMMARY.md` with:
- v29.4 batch row count + PASS/FAIL/OBSOLETE breakdown.
- Hot-patch loops triggered (if any).
- Sacred file SHA confirmation.
- "v29.4 batch DONE — proceed to 63-09 (v29.3 part 1)" verdict.
</output>
