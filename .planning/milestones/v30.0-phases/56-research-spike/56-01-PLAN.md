---
phase: 56-research-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-research-spike/SPIKE-FINDINGS.md
  - .planning/phases/56-research-spike/notes-q1-passthrough.md
  - .planning/phases/56-research-spike/notes-q2-tools.md
  - .planning/phases/56-research-spike/notes-q7-streaming.md
autonomous: true
requirements: []
tags: [research, spike, anthropic-sdk, passthrough, streaming]

must_haves:
  truths:
    - "SPIKE-FINDINGS.md exists with a Q1 verdict block answering SDK-direct vs HTTP-proxy with code-level integration point named (file:line in livinity-broker/router.ts)"
    - "SPIKE-FINDINGS.md Q2 verdict block names whether passthrough forwards client tools[] verbatim or ignores them, with Anthropic doc citation"
    - "SPIKE-FINDINGS.md Q7 verdict block confirms whether Agent SDK aggregates by design (block-streaming) and whether passthrough bypasses sacred file"
    - "Sacred file nexus/packages/core/src/sdk-agent-runner.ts SHA matches 4f868d318abff71f8c8bfbcf443b2393a553018b at end of plan execution (read-only access only)"
    - "Each verdict has >=2 alternatives evaluated, >=3 rationale reasons, and a risk/mitigation pair"
  artifacts:
    - path: ".planning/phases/56-research-spike/SPIKE-FINDINGS.md"
      provides: "Verdict blocks for Q1, Q2, Q7"
      contains: "## Q1"
    - path: ".planning/phases/56-research-spike/notes-q1-passthrough.md"
      provides: "Raw research notes on Anthropic SDK + HTTP-proxy candidates"
    - path: ".planning/phases/56-research-spike/notes-q2-tools.md"
      provides: "Raw research notes on tools forwarding behavior"
    - path: ".planning/phases/56-research-spike/notes-q7-streaming.md"
      provides: "Raw research notes on Agent SDK aggregation"
  key_links:
    - from: "SPIKE-FINDINGS.md Q1 verdict"
      to: "livinity-broker/router.ts:36-187 (passthrough integration point)"
      via: "explicit file:line reference in verdict block"
      pattern: "router\\.ts:[0-9]+"
    - from: "SPIKE-FINDINGS.md Q7 verdict"
      to: "sacred file lines 378-389 (aggregation site)"
      via: "read-only reference, no edits"
      pattern: "sdk-agent-runner\\.ts:378"
---

<objective>
Produce verdict blocks for Q1 (passthrough strategy: SDK-direct vs HTTP-proxy), Q2 (external client tools forward vs ignore), and Q7 (Agent mode block-level streaming confirmation) — the three architectural questions that share the "what does the broker actually do with an external request" cluster.

Purpose: Phase 57 (passthrough mode) cannot enter `/gsd-plan-phase` without these three decisions resolved. Q1 names the integration point, Q2 fixes the tools-forwarding behavior, Q7 confirms agent-mode keeps current behavior (sacred file untouched).

Output:
- `SPIKE-FINDINGS.md` (Q1 + Q2 + Q7 sections — verdict blocks with code-level integration points)
- `notes-q1-passthrough.md`, `notes-q2-tools.md`, `notes-q7-streaming.md` (intermediate research notes)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-research-spike/56-CONTEXT.md
@.planning/phases/56-research-spike/56-RESEARCH.md

# Existing-code references (read; do NOT edit any of these except the planning artifacts above)
@livos/packages/livinityd/source/modules/livinity-broker/router.ts
@livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts
@livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts
@livos/packages/livinityd/source/modules/livinity-broker/openai-sse-adapter.ts
@nexus/packages/core/src/providers/claude.ts
@nexus/packages/core/package.json
</context>

<sacred_boundary>
**STRICT RULE for this plan:** `nexus/packages/core/src/sdk-agent-runner.ts` is the sacred file (SHA `4f868d318abff71f8c8bfbcf443b2393a553018b`).

- Tasks MAY use Read on this file (lines 260-300 and 375-400 are referenced by RESEARCH.md).
- Tasks MUST NOT use Edit, Write, or any other modification tool on this file.
- At the end of every task that touched this file, run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and confirm it equals `4f868d318abff71f8c8bfbcf443b2393a553018b`. Drift = task failure.
</sacred_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Q1 verdict — Anthropic SDK-direct vs HTTP-proxy passthrough</name>
  <files>.planning/phases/56-research-spike/notes-q1-passthrough.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md (sacred file boundary, methodology, no-source-edit rule)
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q1 source inventory, decision framework, common pitfalls P1+P2)
    - livos/packages/livinityd/source/modules/livinity-broker/router.ts (lines 36-187 — current Anthropic Messages route handler)
    - livos/packages/livinityd/source/modules/livinity-broker/agent-runner-factory.ts (lines 64-173 — current HTTP-proxy strategy to nexus)
    - nexus/packages/core/src/providers/claude.ts (lines 1-100 — existing @anthropic-ai/sdk init pattern, ClaudeAuthMethodMismatchError)
    - nexus/packages/core/package.json (confirm @anthropic-ai/sdk version + @anthropic-ai/claude-agent-sdk version)
    - nexus/packages/core/src/sdk-agent-runner.ts (lines 260-300 ONLY — homeOverride pattern; READ-ONLY)
  </read_first>
  <action>
1. Read all files in `read_first`.
2. WebFetch https://docs.anthropic.com/en/api/messages and extract: request shape, response shape, supported auth headers (`x-api-key` vs `Authorization: Bearer`), version header (`anthropic-version`).
3. WebFetch https://docs.anthropic.com/en/api/messages-streaming and extract: SSE event sequence (`message_start`, `content_block_start`, `content_block_delta`, `content_block_stop`, `message_delta`, `message_stop`), event frame format, ping events.
4. WebFetch https://docs.anthropic.com/en/api/getting-started and confirm whether Anthropic supports `Authorization: Bearer` for subscription/OAuth tokens vs `x-api-key` for raw API keys.
5. WebFetch https://docs.anthropic.com/en/api/client-sdks and extract Anthropic's SDK-vs-direct guidance.
6. WebFetch https://github.com/anthropics/anthropic-sdk-typescript/blob/main/src/streaming.ts (use raw.githubusercontent.com if needed) and extract the SSE event handler logic — document event names emitted by `messages.stream()`.
7. WebFetch https://github.com/anthropics/anthropic-sdk-typescript/blob/main/src/index.ts and extract the auth-header construction (does the SDK accept access_token vs only `apiKey`?).
8. Optionally WebSearch for "LiteLLM anthropic passthrough" and "OpenRouter anthropic forwarding" — competitor patterns for HTTP-proxy strategy.
9. Write `notes-q1-passthrough.md` with raw findings: doc URLs visited, key quotes, SDK source extracts, and a candidate-evaluation table comparing Strategy A (HTTP-proxy direct to api.anthropic.com), Strategy B (SDK-direct via `new Anthropic({...}).messages.stream()`), and Strategy C (Hybrid token-extraction in livinityd).
10. Choose ONE verdict path. Write the Q1 verdict block in `SPIKE-FINDINGS.md` (create the file if it doesn't exist; if it does exist, append Q1 section without disturbing other content). Verdict block structure:
    ```
    ## Q1: Anthropic Passthrough Strategy — SDK-direct vs HTTP-proxy
    
    ### Verdict
    Chosen path: <A | B | C> — <one-sentence summary>
    
    ### Rationale (>=3 reasons)
    1. ...
    2. ...
    3. ...
    
    ### Alternatives Considered (>=2)
    - Alt 1: <name> — disqualified because <reason>
    - Alt 2: <name> — disqualified because <reason>
    
    ### Code-Level Integration Point
    File: livos/packages/livinityd/source/modules/livinity-broker/router.ts
    Symbol/line: <exact function + line range, e.g., "POST /:userId/v1/messages handler, lines 36-187">
    Behavioral expectation: <what the new branch does>
    
    ### Risk + Mitigation
    Risk: <named risk>
    Mitigation: <concrete countermeasure>
    
    ### D-NO-NEW-DEPS Implications
    <"zero new deps — uses existing @anthropic-ai/sdk@<version> already at nexus/packages/core/package.json:NN" OR "verdict requires new dep X — Phase 57 must budget">
    ```
11. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append output to `notes-q1-passthrough.md` under heading "Sacred file SHA after Q1 task" — must equal `4f868d318abff71f8c8bfbcf443b2393a553018b`.
12. Document the verdict's preference order from RESEARCH.md (D-NO-BYOK preserved → sacred file untouched → per-user homeOverride preserved → SSE flows token-by-token → fewest moving parts).

NOTE: If the SDK source on GitHub is unavailable via WebFetch (e.g., 404), fall back to fetching the npm tarball metadata via https://registry.npmjs.org/@anthropic-ai/sdk/0.80.0 and document the tarball URL in `notes-q1-passthrough.md` as the source. Do NOT npm install.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const has=s.includes('## Q1') && /Verdict/.test(s) && /Alternatives Considered/.test(s) && /Integration Point/.test(s) && /router\.ts:[0-9]+/.test(s); if(!has){console.error('Q1 verdict block missing required structure'); process.exit(1)} else {console.log('Q1 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/56-research-spike/notes-q1-passthrough.md` exists and contains at minimum: 5 fetched URLs (each on its own line under a "Sources Fetched" heading), candidate-evaluation table for A/B/C, and a "Sacred file SHA after Q1 task" line equal to `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - File `.planning/phases/56-research-spike/SPIKE-FINDINGS.md` exists.
    - SPIKE-FINDINGS.md contains a `## Q1` heading.
    - SPIKE-FINDINGS.md Q1 section contains substrings: "Verdict", "Rationale", "Alternatives Considered", "Integration Point", "Risk", "Mitigation", "D-NO-NEW-DEPS".
    - SPIKE-FINDINGS.md Q1 section references `router.ts:` followed by digit(s) (regex `router\.ts:[0-9]+`).
    - At least 3 distinct rationale bullets present in the Q1 section (count of lines starting with `1.`, `2.`, `3.` under Rationale).
    - Sacred file SHA `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` returns `4f868d318abff71f8c8bfbcf443b2393a553018b` at the end of the task.
    - No Edit or Write tool calls were made to `nexus/packages/core/src/sdk-agent-runner.ts`. (Verifiable: search the conversation transcript for any Write/Edit operation targeting that path — must be zero.)
    - WebFetch was invoked for at least 4 of these 5 URLs (executor logs show the calls): docs.anthropic.com/en/api/messages, docs.anthropic.com/en/api/messages-streaming, docs.anthropic.com/en/api/getting-started, github.com/anthropics/anthropic-sdk-typescript/blob/main/src/streaming.ts, github.com/anthropics/anthropic-sdk-typescript/blob/main/src/index.ts (or the npmjs.org fallback for the SDK source).
  </acceptance_criteria>
  <done>Q1 verdict block written; notes file with raw research exists; sacred SHA unchanged; D-NO-NEW-DEPS implication recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Q2 verdict — External client tools forward vs ignore</name>
  <files>.planning/phases/56-research-spike/notes-q2-tools.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q2 source inventory, decision framework)
    - .planning/phases/56-research-spike/notes-q1-passthrough.md (Q1 verdict — Q2 must align with Q1's chosen passthrough strategy)
    - livos/packages/livinityd/source/modules/livinity-broker/router.ts (lines 66-70 — current D-41-14 warn-and-ignore tools)
    - livos/packages/livinityd/source/modules/livinity-broker/openai-router.ts (lines 110-124 — current D-42-12 warn-and-ignore tools/tool_choice/functions)
  </read_first>
  <action>
1. Read all files in `read_first`.
2. WebFetch https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview and extract: full tools[] schema (name, description, input_schema), how Anthropic invokes tools (`tool_use` content block in response), expected response handling.
3. WebFetch https://docs.anthropic.com/en/docs/build-with-claude/computer-use and extract any subscription-tier vs API-key-tier statement about tools support.
4. WebFetch https://docs.anthropic.com/en/api/managing-api-keys (or whatever Anthropic page best describes tier matrix) and search for any text indicating tools are gated by tier. If no clear citation found, document that as a finding.
5. WebFetch https://platform.openai.com/docs/api-reference/chat/create and search for the `tools` parameter definition — extract the OpenAI tools shape so a passthrough verdict can match it.
6. Cross-reference Q1's verdict (from `notes-q1-passthrough.md`): if Q1 chose SDK-direct via subscription auth, Q2's verdict must verify subscription-auth path supports tools; if Q1 chose HTTP-proxy with x-api-key, tools support is universal.
7. Write `notes-q2-tools.md` with: Anthropic tools schema quote, OpenAI tools schema quote, finding on whether subscription tier rejects tools (cite or note absence-of-evidence), candidate evaluation: "Forward verbatim in passthrough" vs "Ignore-warn (status quo)".
8. Choose verdict. Append `## Q2` section to `SPIKE-FINDINGS.md` with same structure as Q1 (Verdict / Rationale / Alternatives / Integration Point / Risk + Mitigation). Integration point MUST cite both `router.ts:66-70` and `openai-router.ts:110-124`. Worked example MUST show: a sample request body with `tools: [...]`, expected upstream forward, expected response `tool_use` content block.
9. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append SHA-check line to `notes-q2-tools.md`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const ok=s.includes('## Q2') && /Verdict/.test(s) && /tools/.test(s) && /openai-router\.ts:1[01][0-9]/.test(s); if(!ok){console.error('Q2 missing structure or integration point'); process.exit(1)} else {console.log('Q2 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-q2-tools.md` exists with: Anthropic tools schema citation, OpenAI tools schema citation, candidate evaluation table, sacred SHA check line.
    - SPIKE-FINDINGS.md contains `## Q2` heading.
    - SPIKE-FINDINGS.md Q2 section contains substrings: "Verdict", "Rationale", "Alternatives Considered", "Integration Point", "Risk", "Mitigation", "Worked Example".
    - SPIKE-FINDINGS.md Q2 section references both `router.ts:66` (or :67/:68/:69/:70) AND `openai-router.ts:1` followed by 10..24 (regex `openai-router\.ts:1[01][0-9]`).
    - WebFetch was invoked for at least 3 of the listed Anthropic + OpenAI URLs.
    - Q2 verdict explicitly aligns with Q1 verdict (paragraph in Q2 starting "Aligns with Q1: ..." or equivalent — substring "Q1" appears within the Q2 section).
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
  </acceptance_criteria>
  <done>Q2 verdict written with worked example; integration points name both router.ts and openai-router.ts; alignment with Q1 documented.</done>
</task>

<task type="auto">
  <name>Task 3: Q7 verdict — Agent mode block-level streaming confirmation</name>
  <files>.planning/phases/56-research-spike/notes-q7-streaming.md, .planning/phases/56-research-spike/SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-research-spike/56-CONTEXT.md
    - .planning/phases/56-research-spike/56-RESEARCH.md (Q7 source inventory + decision framework, common pitfall P3 about sacred-file recommendations)
    - nexus/packages/core/src/sdk-agent-runner.ts (lines 375-400 ONLY — block-streaming aggregation site; READ-ONLY)
    - livos/packages/livinityd/source/modules/livinity-broker/openai-sse-adapter.ts (lines 89-177 — current adapter consuming aggregated chunks)
    - .planning/milestones/v29.5-phases/51-a2-streaming-fix/51-01-SUMMARY.md (D-51-03 deferral rationale — feeds D-51-03 sub-question)
  </read_first>
  <action>
1. Read all files in `read_first`.
2. WebFetch https://docs.anthropic.com/en/api/messages-streaming and confirm the canonical Anthropic SSE event sequence (`message_start` → `content_block_start` → `content_block_delta` ×N → `content_block_stop` → `message_delta` → `message_stop`). Document event-by-event in `notes-q7-streaming.md`.
3. WebFetch https://github.com/anthropics/claude-agent-sdk-typescript (or the canonical agent-sdk repo URL — try https://github.com/anthropics/claude-agent-sdk-python first; if TS variant exists at https://github.com/anthropics/claude-agent-sdk-typescript fetch that) and locate the streaming/query API. Extract:
   - Does the Agent SDK's `query()` API expose token-level streaming events?
   - Or does it only callback on aggregated assistant turns?
4. If GitHub URL 404s, WebFetch https://www.npmjs.com/package/@anthropic-ai/claude-agent-sdk and follow links from there.
5. WebSearch for "claude-agent-sdk streaming tokens" to surface any developer-blog confirmation of aggregation behavior.
6. Read sacred file lines 375-400 ONLY (Read tool with offset/limit). Document what the loop does WITHOUT proposing any edit. Per CONTEXT.md sacred boundary, any "we should change line N" recommendation goes to a deferred D-30-XX row, NOT a code edit.
7. Write `notes-q7-streaming.md` with: Anthropic SSE sequence (verbatim), Agent SDK streaming behavior finding, sacred-file lines 378-389 description (aggregation behavior — read-only summary).
8. Choose verdict. Three candidates per RESEARCH.md framework:
   - A. "Confirm: Agent SDK fundamentally aggregates → Agent mode keeps current behavior; passthrough fixes via direct Anthropic SSE" (most likely)
   - B. "Find that Agent SDK can stream token-by-token but sacred-file's loop forces aggregation → flag D-30-XX deferred sacred-edit"
   - C. "Find that Agent SDK streams natively and sacred-file aggregation is incidental → triggers D-51-03 reversal re-eval"
9. Append `## Q7` section to `SPIKE-FINDINGS.md` with: Verdict / Rationale (>=3 reasons) / Alternatives Considered (>=2) / Integration Point (must cite `nexus/packages/core/src/sdk-agent-runner.ts:378` line range as READ-ONLY reference, plus passthrough's bypass site `livinity-broker/router.ts:88` SSE branch) / Risk + Mitigation / D-51-03 implication ("This Q7 verdict's effect on D-51-03 sub-question is: ...").
10. Run `git hash-object nexus/packages/core/src/sdk-agent-runner.ts` and append SHA check.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/56-research-spike/SPIKE-FINDINGS.md','utf8'); const ok=s.includes('## Q7') && /sdk-agent-runner\.ts:37[8-9]|sdk-agent-runner\.ts:38[0-9]/.test(s) && /D-51-03/.test(s); if(!ok){console.error('Q7 missing required references'); process.exit(1)} else {console.log('Q7 OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-research-spike/notes-q7-streaming.md` exists with: Anthropic SSE event sequence (all 6 event names), Agent SDK streaming-behavior finding, sacred-file 378-389 read-only description, sacred SHA check line.
    - SPIKE-FINDINGS.md contains `## Q7` heading.
    - SPIKE-FINDINGS.md Q7 section contains substrings: "Verdict", "Rationale", "Alternatives Considered", "Integration Point", "D-51-03".
    - SPIKE-FINDINGS.md Q7 section cites `sdk-agent-runner.ts:378` (or any line in 378-389 range — regex `sdk-agent-runner\.ts:37[8-9]|sdk-agent-runner\.ts:38[0-9]`) AS A READ-ONLY REFERENCE (not an edit recommendation; the cited section MUST contain the substring "read-only" or "untouched" or "no edit" within 200 chars of the line cite).
    - WebFetch was invoked for at least docs.anthropic.com/en/api/messages-streaming and the claude-agent-sdk GitHub or npm URL.
    - Sacred file SHA at end of task equals `4f868d318abff71f8c8bfbcf443b2393a553018b`.
    - No Edit/Write to `nexus/packages/core/src/sdk-agent-runner.ts`.
    - Q7 section explicitly states the verdict's effect on D-51-03 sub-question (substring "D-51-03" appears in the Q7 section in a sentence stating the implication).
  </acceptance_criteria>
  <done>Q7 verdict written; Anthropic SSE event sequence cited verbatim; Agent SDK aggregation behavior documented; sacred file referenced read-only; D-51-03 implication noted.</done>
</task>

</tasks>

<verification>
- All three verdict blocks (Q1, Q2, Q7) present in SPIKE-FINDINGS.md
- All three intermediate notes files exist
- Sacred file SHA matches `4f868d318abff71f8c8bfbcf443b2393a553018b` at end (validated by `git hash-object`)
- No Write/Edit tool calls targeted `nexus/packages/core/src/sdk-agent-runner.ts`
- Each verdict has >=2 alternatives, >=3 rationale reasons, code-level integration point, risk/mitigation pair
</verification>

<success_criteria>
- ROADMAP Phase 56 success criterion #1 partially satisfied: Q1, Q2, Q7 have verdicts (no "TBD")
- ROADMAP Phase 56 success criterion #2 satisfied: Q1 verdict names code-level integration point in `livinity-broker/router.ts` with file:line
- ROADMAP Phase 56 success criterion #5 input prepared: Q7 verdict feeds D-51-03 re-evaluation in plan 56-03
</success_criteria>

<output>
After completion, create `.planning/phases/56-research-spike/56-01-SUMMARY.md` summarizing: which verdicts were reached, what was the strongest evidence for each, and any [ASSUMED] flags from RESEARCH.md that flipped to [VERIFIED: <source>] during this plan.
</output>
