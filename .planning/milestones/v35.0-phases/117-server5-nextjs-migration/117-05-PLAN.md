---
phase: 117-server5-nextjs-migration
plan: 05
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - .planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md
autonomous: true
requirements: []
tags: [v35, design-system, server5, download, dashboard, nextjs, restyle]
must_haves:
  truths:
    - "`/download` (Livinity Agent) page restyled to canonical tokens; OS SVG icons + motion-primitives preserved"
    - "Next.js `/dashboard` page (566 lines, currently NOT live per INVENTORY) restyled to match dashboard.html (canonical b-card + hero-grad + dash spacing)"
    - "Both files have `.pre-117-05.bak` backups on Server5"
    - "Data fetching / state / motion-primitive imports / framer-motion usage byte-identical to backups (D-117-NO-API-CHANGES, D-117-NO-AUTH-FLOW-CHANGES)"
    - "`/download` returns 200 via curl"
    - "Next.js `/dashboard` is not currently routed by Caddy (per INVENTORY note); build MUST still succeed and the file MUST render without runtime errors when reached directly through Next.js dev/prod server"
    - "pm2 `web` online after the restart at the end of this plan"
  artifacts:
    - path: "/opt/platform/web/src/app/download/page.tsx"
      provides: "Restyled download page (canonical hero-card + b-card patterns; motion-primitives preserved)"
    - path: "/opt/platform/web/src/app/dashboard/page.tsx"
      provides: "Restyled Next.js dashboard (566-line port of dashboard.html visual language)"
    - path: ".planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md"
      provides: "Plan completion record"
  key_links:
    - from: "/opt/platform/web/src/app/download/page.tsx"
      to: "/opt/platform/web/src/components/motion-primitives/*"
      via: "TSX import"
      pattern: "from\\s+['\"]@?/?components/motion-primitives"
    - from: "/opt/platform/web/src/app/dashboard/page.tsx"
      to: "/opt/landing/livinity.io/dashboard.html"
      via: "visual parity target (no code import)"
      pattern: ".b-card|var\\(--dash-pad\\)|var\\(--hero-grad\\)"
---

<objective>
Restyle the final two routes in Phase 117:

1. **`/download`** — currently a live route. Restyle to canonical tokens while preserving the bespoke OS-icon SVGs and the motion-primitives (`TextEffect`, `AnimatedGroup`, `InView`) — those are pure animation utilities and are out-of-scope for visual change per the inventory's `needs-migration` tag (restyle = caller-side, not primitive-side).

2. **`/dashboard`** (Next.js) — currently NOT live per INVENTORY (Caddy serves `/opt/landing/livinity.io/dashboard.html` instead). However, the master plan calls for porting dashboard.html's visual language into this React file so that when/if the React tree replaces the static HTML, the visual continuity is preserved. This plan restyles `app/dashboard/page.tsx` (566 lines) to use the canonical b-card / hero-grad / dash-pad / token pattern.

**Scope locks:**
- D-117-NO-API-CHANGES — `/api/**` unchanged.
- D-117-NO-AUTH-FLOW-CHANGES — `getSession` in `/dashboard` preserved (the React dashboard route's auth gating MUST stay byte-identical).
- D-117-OPERATOR-CAN-RESTART-AT-WILL — plan deployable / revertable independently.

Purpose: Close the Phase 117 surface coverage for /download + /dashboard.
Output: 2 files restyled with backups, pm2 restarted, smoke-tested, 117-05-SUMMARY.md committed in-repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/117-server5-nextjs-migration/117-CONTEXT.md
@.planning/phases/117-server5-nextjs-migration/117-01-PLAN.md
@.planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/design-tokens/tokens.css
@livos/packages/design-tokens/tailwind.preset.cjs
@./CLAUDE.md

<interfaces>
**Combined class map (base + download/dashboard specifics):**

```
# Base zinc → tokens (same as 117-02..04)
bg-zinc-50 / bg-white / bg-gray-50    → bg-card-bg-2
bg-zinc-900 / bg-zinc-950             → bg-card-bg  (dark surface)
border-zinc-200 / border-gray-200     → border-dash-line
border-zinc-300 / border-gray-300     → border-dash-line-strong
text-zinc-500/600 / text-gray-500/600 → text-[color:var(--muted-fg)]
text-zinc-700/900 / text-gray-700/900 → text-[color:var(--fg)]
bg-blue-600                           → bg-accent-blue
hover:bg-blue-700                     → hover:bg-[color:rgb(37_99_235/0.9)]
text-blue-600                         → text-accent-blue
rounded-2xl                           → rounded-dash
shadow-md / shadow-lg                 → shadow-card

# Download page motion+glow extras (per INVENTORY: TextEffect/AnimatedGroup/InView/BorderTrail/TextShimmer + dark sections + glow)
bg-zinc-900 (full-bleed dark section) → bg-card-bg dark:bg-card-bg-2 (or keep dark theme via body class; use `bg-[color:var(--card-bg)]` if explicit)
text-zinc-300                         → text-[color:var(--muted-fg)] / text-[color:var(--fg)] depending on context (judgment)

# Dashboard page (Next.js — porting dashboard.html idiom)
# Canonical b-card shape:
<div className="rounded-dash border border-dash-line bg-card-bg shadow-card p-dash">
# Hero (gradient backdrop matching dashboard.html .b-hero):
<section className="rounded-dash bg-hero-grad p-dash">
# Section padding rhythm:
gap-6 / gap-8                         → gap-dash
p-6                                   → p-dash
```

**Preserve verbatim (D-117-NO-AUTH-FLOW-CHANGES + motion-primitive contract):**
- Every `import` line.
- Every framer-motion / motion-primitive component usage (`<TextEffect>`, `<AnimatedGroup>`, `<InView>`, `<BorderTrail>`, `<TextShimmer>`).
- Inline `<svg>` blocks for OS icons in `/download`.
- `getSession()` / `auth()` / `redirect()` calls in dashboard/page.tsx.
- All hook calls and their argument shapes.

**Out-of-scope:**
- `app/download/layout.tsx` — metadata wrapper only, tagged `wontfix` in INVENTORY.
- `components/motion-primitives/*` — restyle is caller-side per INVENTORY rule #5; primitives themselves stay (Phase 119/120 may re-home them).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle /download/page.tsx — preserve motion-primitives + OS SVG icons</name>
  <files>
    <!-- Server5:
         /opt/platform/web/src/app/download/page.tsx + .pre-117-05.bak -->
  </files>
  <read_first>
    1. INVENTORY-SERVER5.md `app/download/` row — confirms `needs-migration`, lists motion-primitives used: `TextEffect`, `AnimatedGroup`, `InView`. Heavy SVG icons inline.
    2. Server5 live source (SINGLE SSH):
       ```
       ssh root@45.137.194.102 "set -e; \
         echo '===download/page==='; cat /opt/platform/web/src/app/download/page.tsx; \
         echo '===motion-primitives dir==='; ls -la /opt/platform/web/src/components/motion-primitives/"
       ```
    3. **Pre-flight**: 117-01 wired:
       ```
       ssh root@45.137.194.102 "test -f /opt/platform/web/node_modules/@livinity/design-tokens/tokens.css && echo OK || (echo MISSING_117_01; exit 1)"
       ```
  </read_first>
  <action>
    1. **Backup**:
       ```
       ssh root@45.137.194.102 "cp /opt/platform/web/src/app/download/page.tsx /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak"
       ```

    2. **Apply combined class-map sed** (in-place):
       ```
       ssh root@45.137.194.102 "sed -E -i \
         -e 's/bg-zinc-50/bg-card-bg-2/g' \
         -e 's/bg-zinc-900/bg-card-bg/g' \
         -e 's/bg-zinc-950/bg-card-bg/g' \
         -e 's/\\bbg-white\\b/bg-card-bg/g' \
         -e 's/bg-gray-50/bg-card-bg-2/g' \
         -e 's/bg-gray-100/bg-card-bg-2/g' \
         -e 's/border-zinc-200/border-dash-line/g' \
         -e 's/border-zinc-300/border-dash-line-strong/g' \
         -e 's/border-gray-200/border-dash-line/g' \
         -e 's/text-zinc-500/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-600/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-300/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-700/text-[color:var(--fg)]/g' \
         -e 's/text-zinc-900/text-[color:var(--fg)]/g' \
         -e 's/text-gray-500/text-[color:var(--muted-fg)]/g' \
         -e 's/text-gray-700/text-[color:var(--fg)]/g' \
         -e 's/bg-blue-600/bg-accent-blue/g' \
         -e 's/hover:bg-blue-700/hover:bg-[color:rgb(37_99_235\\/0.9)]/g' \
         -e 's/text-blue-600/text-accent-blue/g' \
         -e 's/rounded-2xl/rounded-dash/g' \
         -e 's/shadow-lg/shadow-card/g' \
         -e 's/shadow-md/shadow-card/g' \
         /opt/platform/web/src/app/download/page.tsx"
       ```

    3. **Verify motion-primitive imports + JSX usage unchanged**:
       ```
       ssh root@45.137.194.102 "diff \
         <(grep -E 'motion-primitives|TextEffect|AnimatedGroup|InView|BorderTrail|TextShimmer|<svg|<path' /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak) \
         <(grep -E 'motion-primitives|TextEffect|AnimatedGroup|InView|BorderTrail|TextShimmer|<svg|<path' /opt/platform/web/src/app/download/page.tsx) \
         && echo 'MOTION_AND_SVG_UNCHANGED' || echo 'DRIFT'"
       ```
       MUST print `MOTION_AND_SVG_UNCHANGED`.

    4. **Logic-diff guard** (state/handlers):
       ```
       ssh root@45.137.194.102 "diff \
         <(grep -E 'useState|useEffect|fetch\\(|onClick|onSubmit|href=' /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak) \
         <(grep -E 'useState|useEffect|fetch\\(|onClick|onSubmit|href=' /opt/platform/web/src/app/download/page.tsx) \
         && echo 'LOGIC_UNCHANGED' || echo 'LOGIC_DRIFT'"
       ```

    5. **Build + restart deferred to Task 3.**
  </action>
  <verify>
    <automated>
      ```
      ssh root@45.137.194.102 "set -e; \
        test -f /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak && echo BAK_OK; \
        drift=\$(grep -cE 'bg-zinc-[0-9]+|text-zinc-[0-9]+|border-zinc-[0-9]+|bg-blue-6[0-9]+|text-blue-600|rounded-2xl|bg-gray-1[0-9]+|text-gray-[5-9][0-9]+' /opt/platform/web/src/app/download/page.tsx || true); \
        [ \"\${drift}\" = '0' ] && echo CLEAN || echo \"RESIDUE:\${drift}\"; \
        diff <(grep -E 'motion-primitives|TextEffect|AnimatedGroup|InView|<svg|<path|useState|useEffect|fetch\\(' /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak) \
             <(grep -E 'motion-primitives|TextEffect|AnimatedGroup|InView|<svg|<path|useState|useEffect|fetch\\(' /opt/platform/web/src/app/download/page.tsx) \
          >/dev/null && echo MOTION_LOGIC_UNCHANGED || echo MOTION_LOGIC_DRIFT"
      ```
      Expected: `BAK_OK`, `CLEAN`, `MOTION_LOGIC_UNCHANGED`.
    </automated>
  </verify>
  <done>
    `/download/page.tsx` backed up + restyled; zero zinc/blue-600 residue; motion-primitive and SVG references identical to backup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle /dashboard/page.tsx — port dashboard.html visual language into the 566-line React tree</name>
  <files>
    <!-- Server5:
         /opt/platform/web/src/app/dashboard/page.tsx + .pre-117-05.bak -->
  </files>
  <read_first>
    1. INVENTORY-SERVER5.md `app/dashboard/` row — confirms 566 lines, NOT currently live (Caddy serves landing/livinity.io/dashboard.html), Phase 117-05 target.
    2. The canonical reference: `/opt/landing/livinity.io/dashboard.html` (the source of truth for the visual language). Read first 200 lines for `.b-card`, `.b-hero`, `--dash-pad`, `--dash-radius` patterns:
       ```
       ssh root@45.137.194.102 "head -200 /opt/landing/livinity.io/dashboard.html"
       ```
    3. Server5 live source for the React dashboard:
       ```
       ssh root@45.137.194.102 "cat /opt/platform/web/src/app/dashboard/page.tsx"
       ```
       Note size — if >600 lines, plan to scp the new version rather than try inline sed-only.
  </read_first>
  <action>
    1. **Backup**:
       ```
       ssh root@45.137.194.102 "cp /opt/platform/web/src/app/dashboard/page.tsx /opt/platform/web/src/app/dashboard/page.tsx.pre-117-05.bak"
       ```

    2. **Apply combined class-map sed** (in-place, same as Task 1 step 2 — the regex set is unchanged):
       ```
       ssh root@45.137.194.102 "sed -E -i \
         -e 's/bg-zinc-50/bg-card-bg-2/g' \
         -e 's/bg-zinc-900/bg-card-bg/g' \
         -e 's/bg-zinc-950/bg-card-bg/g' \
         -e 's/\\bbg-white\\b/bg-card-bg/g' \
         -e 's/bg-gray-50/bg-card-bg-2/g' \
         -e 's/bg-gray-100/bg-card-bg-2/g' \
         -e 's/border-zinc-200/border-dash-line/g' \
         -e 's/border-zinc-300/border-dash-line-strong/g' \
         -e 's/border-gray-200/border-dash-line/g' \
         -e 's/text-zinc-500/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-600/text-[color:var(--muted-fg)]/g' \
         -e 's/text-zinc-700/text-[color:var(--fg)]/g' \
         -e 's/text-zinc-900/text-[color:var(--fg)]/g' \
         -e 's/text-gray-500/text-[color:var(--muted-fg)]/g' \
         -e 's/text-gray-700/text-[color:var(--fg)]/g' \
         -e 's/bg-blue-600/bg-accent-blue/g' \
         -e 's/hover:bg-blue-700/hover:bg-[color:rgb(37_99_235\\/0.9)]/g' \
         -e 's/text-blue-600/text-accent-blue/g' \
         -e 's/text-green-600/text-accent-green/g' \
         -e 's/text-red-600/text-accent-red/g' \
         -e 's/text-amber-600/text-accent-amber/g' \
         -e 's/rounded-2xl/rounded-dash/g' \
         -e 's/shadow-lg/shadow-card/g' \
         -e 's/shadow-md/shadow-card/g' \
         /opt/platform/web/src/app/dashboard/page.tsx"
       ```

    3. **Add hero-grad surface for the page's hero/header section IF the file has a recognizable hero block.** Conservative approach: do NOT inject new JSX. Instead, if the existing hero uses `bg-gradient-to-br from-... to-...` (Tailwind gradient), replace with the canonical `bg-hero-grad`:
       ```
       ssh root@45.137.194.102 "sed -E -i \
         -e 's/bg-gradient-to-(br|r|tr) from-[a-z0-9-]+ (via-[a-z0-9-]+ )?to-[a-z0-9-]+/bg-hero-grad/g' \
         /opt/platform/web/src/app/dashboard/page.tsx"
       ```
       If no such gradient existed, this is a NOOP. Document either way in SUMMARY.

    4. **Logic-diff guard** — auth & data fetching MUST stay byte-identical (D-117-NO-AUTH-FLOW-CHANGES + D-117-NO-API-CHANGES):
       ```
       ssh root@45.137.194.102 "diff \
         <(grep -E 'getSession|auth\\(|redirect\\(|useEffect|useState|useTransition|useRouter|fetch\\(|drizzle|db\\.|sql\\(|onClick|onSubmit|action=|href=' /opt/platform/web/src/app/dashboard/page.tsx.pre-117-05.bak) \
         <(grep -E 'getSession|auth\\(|redirect\\(|useEffect|useState|useTransition|useRouter|fetch\\(|drizzle|db\\.|sql\\(|onClick|onSubmit|action=|href=' /opt/platform/web/src/app/dashboard/page.tsx) \
         && echo 'LOGIC_UNCHANGED' || echo 'LOGIC_DRIFT'"
       ```
       LOGIC_UNCHANGED MUST print. Any drift → restore from .bak and investigate (sed may have caught a string literal inside a server action, etc.).

    5. **Build + restart deferred to Task 3.**
  </action>
  <verify>
    <automated>
      ```
      ssh root@45.137.194.102 "set -e; \
        test -f /opt/platform/web/src/app/dashboard/page.tsx.pre-117-05.bak && echo BAK_OK; \
        drift=\$(grep -cE 'bg-zinc-[0-9]+|text-zinc-[0-9]+|border-zinc-[0-9]+|bg-blue-6[0-9]+|text-blue-600|text-green-600|text-red-600|text-amber-600|rounded-2xl|bg-gray-1[0-9]+' /opt/platform/web/src/app/dashboard/page.tsx || true); \
        [ \"\${drift}\" = '0' ] && echo CLEAN || echo \"RESIDUE:\${drift}\"; \
        diff <(grep -E 'getSession|auth\\(|redirect\\(|useEffect|useState|useTransition|useRouter|fetch\\(|drizzle|db\\.|sql\\(|onClick|onSubmit|action=' /opt/platform/web/src/app/dashboard/page.tsx.pre-117-05.bak) \
             <(grep -E 'getSession|auth\\(|redirect\\(|useEffect|useState|useTransition|useRouter|fetch\\(|drizzle|db\\.|sql\\(|onClick|onSubmit|action=' /opt/platform/web/src/app/dashboard/page.tsx) \
          >/dev/null && echo LOGIC_UNCHANGED || echo LOGIC_DRIFT"
      ```
      Expected: `BAK_OK`, `CLEAN`, `LOGIC_UNCHANGED`.
    </automated>
  </verify>
  <done>
    `/dashboard/page.tsx` backed up + restyled; zero zinc/blue-600/green-600/red-600/amber-600 residue; auth + data-fetching references identical to backup; build succeeds (validated in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + pm2 restart + smoke /download (+ /dashboard via direct Next route if reachable) + write SUMMARY.md</name>
  <files>
    - .planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md
  </files>
  <read_first>
    1. Task 1 + 2 verify logs.
    2. Caddy config to confirm whether `/dashboard` reaches the Next.js app or the static HTML (per INVENTORY note, static currently wins):
       ```
       ssh root@45.137.194.102 "grep -E 'dashboard|/dashboard.html|reverse_proxy' /etc/caddy/Caddyfile 2>&1 | head -20"
       ```
       This is read-only — we do NOT change Caddy routing in Phase 117 (out of scope; that's an operator decision for a later phase). We just note in SUMMARY which surface a browser hits.
  </read_first>
  <action>
    Single SSH:
    ```
    ssh root@45.137.194.102 "set -e; \
      cd /opt/platform/web; \
      npm run build 2>&1 | tee /tmp/117-05-build.log | tail -50; \
      pm2 restart web && sleep 3 && pm2 list | grep web; \
      echo '===SMOKE==='; \
      code_dl=\$(curl -s -o /tmp/117-05-download.html -w '%{http_code}' 'https://livinity.io/download'); \
      echo \"/download -> HTTP \${code_dl}\"; \
      code_db=\$(curl -s -o /tmp/117-05-dashboard.html -w '%{http_code}' 'https://livinity.io/dashboard'); \
      echo \"/dashboard -> HTTP \${code_db} (note: Caddy may route to static HTML; check served body)\"; \
      grep -E 'Geist|@livinity|--accent|dashboard\\.html|<h1' /tmp/117-05-dashboard.html | head -5 || true; \
      echo '===TOKEN MARKERS IN BUILT CSS==='; \
      grep -roE -- '--accent-blue|--dash-pad|--hero-grad|--card-bg' /opt/platform/web/.next/static/css/ 2>&1 | head -8"
    ```

    Acceptance:
    - Build exits 0 (the dashboard's 566 lines compile cleanly after token swaps).
    - pm2 `web` `online`.
    - `/download` → 200 (public page).
    - `/dashboard` → 200 or 307 or 308. If Caddy routes it to the static HTML, response body will contain dashboard.html markers — note in SUMMARY. Either is acceptable for THIS plan because we are NOT changing routing (D-117-OPERATOR-CAN-RESTART-AT-WILL).
    - `--hero-grad` token may or may not appear in built CSS (depending on whether dashboard/page.tsx used a gradient hero pre-edit). Either way is acceptable.

    Write `.planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md`:
    - **Files modified (Server5):**
      - `/opt/platform/web/src/app/download/page.tsx` (+ `.pre-117-05.bak`)
      - `/opt/platform/web/src/app/dashboard/page.tsx` (+ `.pre-117-05.bak`)
    - **Caddy routing note:** which URL currently lands where (Next.js vs static dashboard.html).
    - **Build log tail** (last 30 lines).
    - **Smoke matrix:** `/download` -> HTTP code; `/dashboard` -> HTTP code + which surface served.
    - **Logic-diff results:** LOGIC_UNCHANGED + MOTION_LOGIC_UNCHANGED for both files.
    - **Rollback recipe:**
      ```
      ssh root@45.137.194.102 "set -e; \
        cp /opt/platform/web/src/app/download/page.tsx.pre-117-05.bak /opt/platform/web/src/app/download/page.tsx; \
        cp /opt/platform/web/src/app/dashboard/page.tsx.pre-117-05.bak /opt/platform/web/src/app/dashboard/page.tsx; \
        cd /opt/platform/web && npm run build && pm2 restart web"
      ```
    - **Phase 117 completion checklist** (since this is the last plan):
      - [ ] 117-01 SUMMARY exists + foundation green
      - [ ] 117-02 SUMMARY exists + 6 auth routes green
      - [ ] 117-03 SUMMARY exists + /dashboard/install audit done
      - [ ] 117-04 SUMMARY exists + /store green
      - [ ] 117-05 SUMMARY exists + /download green + /dashboard React tree compiled
      - [ ] All Server5 `.pre-117-NN.bak` backups present
      - [ ] `_pre-117-src-src.tar.gz` present
      - [ ] Operator UAT pass logged
    - **Phase 119 followups consolidated:** AuthCard primitive, AppCard primitive, CategoryNav pill, wizard-stepper primitive, motion-primitive re-home as `@livinity/ui-kit/motion`, store gradient policy revisit, Caddy routing flip for /dashboard.
  </action>
  <verify>
    <automated>
      ```
      test -f .planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md && echo SUMMARY_OK
      ssh root@45.137.194.102 "pm2 list | grep -E 'web.*online' && echo PM2_ONLINE; \
        c_dl=\$(curl -s -o /dev/null -w '%{http_code}' https://livinity.io/download); \
        [[ \$c_dl =~ ^[23] ]] && echo OK_download:\$c_dl || echo FAIL_download:\$c_dl; \
        c_db=\$(curl -s -o /dev/null -w '%{http_code}' https://livinity.io/dashboard); \
        [[ \$c_db =~ ^[23] ]] && echo OK_dashboard:\$c_db || echo FAIL_dashboard:\$c_db"
      ```
      Expected: `SUMMARY_OK`, `PM2_ONLINE`, `OK_download:2xx-or-3xx`, `OK_dashboard:2xx-or-3xx`.
    </automated>
  </verify>
  <done>
    pm2 `web` online, /download + /dashboard return 2xx/3xx, SUMMARY committed with Phase 117 completion checklist + Phase 119 consolidated followups.
  </done>
</task>

</tasks>

<verification>
- 2 files modified on Server5 (+2 .pre-117-05.bak backups).
- Motion-primitive imports + framer-motion usage byte-identical to backup in `/download`.
- Auth + Drizzle / data-fetching references byte-identical to backup in `/dashboard/page.tsx`.
- `/api/**` byte-unchanged: `find /opt/platform/web/src/app/api -newer /opt/platform/web/_pre-117-src-src.tar.gz` empty.
- Build succeeds; pm2 `web` online.
- /download + /dashboard return 2xx/3xx.
- Phase 117 completion checklist in SUMMARY.
</verification>

<success_criteria>
- 7 must_haves truths satisfied.
- 3 artifacts produced (2 Server5 files + SUMMARY).
- Smoke matrix green for /download + /dashboard.
- Phase 117 completion checklist recorded.
- Phase 119 followups consolidated in this SUMMARY (last plan in phase).
</success_criteria>

<output>
After completion, create `.planning/phases/117-server5-nextjs-migration/117-05-SUMMARY.md`. As this is the final plan in Phase 117, include:
- This plan's per-file matrix + smoke result + rollback recipe
- Phase-117-wide completion checklist (all 5 SUMMARYs accounted for)
- Consolidated Phase 119 followups (from 117-02..05 SUMMARYs)
</output>
