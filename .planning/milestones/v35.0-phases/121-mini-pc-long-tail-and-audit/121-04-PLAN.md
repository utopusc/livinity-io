---
phase: 121-mini-pc-long-tail-and-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 121-01
  - 121-02
  - 121-03
files_modified:
  - livos/packages/ui/src/routes/settings/**/*.tsx
  - livos/packages/ui/src/routes/app-store/**/*.tsx
  - livos/packages/ui/src/routes/community-app-store/**/*.tsx
  - livos/packages/ui/src/routes/agent-marketplace/**/*.tsx
  - livos/packages/ui/src/routes/subagents/**/*.tsx
  - livos/packages/ui/src/routes/docker/**/*.tsx
  - livos/packages/ui/src/routes/factory-reset/**/*.tsx
  - livos/packages/ui/src/routes/ai-chat/**/*.tsx
  - livos/packages/ui/src/routes/login/**/*.tsx
  - livos/packages/ui/src/routes/server-control/**/*.tsx
  - livos/packages/ui/src/routes/onboarding/**/*.tsx
  - livos/packages/ui/src/routes/playground/**/*.tsx
  - livos/packages/ui/src/routes/my-devices/**/*.tsx
  - livos/packages/ui/src/routes/help/**/*.tsx
  - livos/packages/ui/src/routes/schedules/**/*.tsx
  - livos/packages/ui/src/routes/invite.tsx
  - livos/packages/ui/src/routes/live-usage.tsx
  - livos/packages/ui/src/routes/login.tsx
  - livos/packages/ui/src/routes/not-found.tsx
  - livos/packages/ui/src/routes/notifications.tsx
autonomous: false
requirements:
  - V35-AC1
  - V35-AC2
  - V35-AC4
  - V35-AC5
  - V35-AC8
user_setup: []

must_haves:
  truths:
    - "All 219 .tsx files under routes/ migrate to canonical tokens (3 sub-batches: 04a settings/*, 04b apps/* aggregate = app-store + community-app-store + agent-marketplace + subagents + docker + factory-reset, 04c misc = ai-chat + login + server-control + onboarding + playground + my-devices + help + schedules + 5 top-level)"
    - "Each sub-batch ships independently with its own commit + operator UAT checkpoint between (per D-121-OPERATOR-CHECKPOINTS)"
    - "`pnpm --filter ui build` PASS after each sub-batch"
    - "Sacred SHA preserved every commit"
    - "Behavioral-guard regex PASS every diff"
    - "After 04a, operator UAT validates settings; after 04b, operator UAT validates app-store + docker; after 04c, operator UAT validates remaining surfaces"
  artifacts:
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md"
      provides: "3-sub-batch tally + commit hashes per sub-batch + sacred SHA + 3 operator UAT checkpoints documented with outcomes"
      min_lines: 80
    - path: "livos/packages/ui/src/routes/settings"
      provides: "Restyled settings routes (92 tsx files)"
      contains: "bg-card-bg"
    - path: "livos/packages/ui/src/routes/docker"
      provides: "Restyled docker routes (75 tsx files)"
      contains: "bg-card-bg"
  key_links:
    - from: "livos/packages/ui/src/routes/settings/**"
      to: "@livinity/ui-kit"
      via: "ui-kit imports for forms, buttons, pills, cards"
      pattern: "from ['\"]@livinity/ui-kit['\"]"
    - from: "livos/packages/ui/src/routes/docker/**"
      to: "@livinity/design-tokens"
      via: "canonical token classes"
      pattern: "bg-card-bg|bg-accent-(blue|green|amber|red)"
---

<objective>
Migrate Mini PC livinityd UI long-tail batch #4 — the massive `routes/*` tree (219 tsx files) — from raw Tailwind classes to canonical design tokens + `@livinity/ui-kit`. Split into 3 sub-batches with operator UAT checkpoints between, per D-121-OPERATOR-CHECKPOINTS.

Purpose: routes/ is the largest single migration area in v35 (219 files = ~35% of all Mini PC UI). It contains every router page: settings panels (92), app-store pages (4) + community-app-store (2) + agent-marketplace (3) + subagents (1) + docker (75) + factory-reset (10), plus misc routes (ai-chat 15 + login + server-control + onboarding 6 + playground + my-devices + help + schedules + 5 top-level files). Splitting into 3 sub-batches with mandatory operator UAT between them means a regression introduced in one sub-batch is caught before the next one ships — protecting bruce's daily OwnCloud use per D-121-MINI-PC-OPERATOR-PRIORITY.

Output:
- All 219 routes tsx files token-migrated across 3 sub-batches (04a/04b/04c).
- Each sub-batch = 1+ atomic commits + ONE operator UAT checkpoint.
- `pnpm --filter ui build` PASS after each sub-batch.
- 121-04-SUMMARY.md with 3 UAT checkpoint outcomes documented.

`autonomous: false` because this plan contains 3 explicit `checkpoint:human-verify` tasks (one between each sub-batch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-CONTEXT.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-01-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md
@livos/packages/ui-kit/src/index.ts
@livos/packages/design-tokens/tailwind.preset.cjs

<interfaces>
<!-- Same ui-kit exports as 121-01. -->
<!-- routes/* is the highest ui-kit-applicability surface — most routes are forms (settings) -->
<!-- or content grids (app-store, docker) — both map well to <Input>, <Button>, <Card>, <Pill>. -->

<!-- Token map (same as 121-01..03): -->
<!-- bg-zinc-* → bg-card-bg / bg-card-bg-2 -->
<!-- bg-blue-600 → bg-accent-blue -->
<!-- rounded-2xl (card shells) → rounded-dash -->
<!-- p-7 (card shells) → p-dash -->
<!-- text-zinc-* → text-card-fg -->
<!-- border-zinc-* → border-dash-line -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Sub-batch 04a): Migrate routes/settings (92 tsx files)</name>
  <files>livos/packages/ui/src/routes/settings/**/*.tsx</files>

  <read_first>
    Read every tsx file under `livos/packages/ui/src/routes/settings/`. settings is mostly forms — high ui-kit-swap applicability.
  </read_first>

  <action>
    1. Token migration (Plan 121-01 Task 1 pattern) across all 92 settings files.

    2. **Aggressive ui-kit swap targets in settings/**:**
       - `<Button>` for Save / Cancel / Reset / Confirm CTAs — apply where onClick signature matches.
       - `<Card>` for settings-panel section shells — apply where inner content is plain JSX.
       - `<Pill>` for status indicators (2fa: enabled/disabled, mobile pairing: paired/unpaired, software-update: up-to-date/outdated).
       - `<Input>` for text fields (change-name, change-password, domain-setup, mobile-pairing token entry) — ONLY if existing usage is `onChange={(e) => ...}` standard event. Defer shadcn `onValueChange` adapters to 121-05.
       - `<PasswordInput>` for change-password.tsx, 2fa.tsx if password fields exist.
       - `<Modal>` for confirm dialogs (shutdown, restart, software-update-confirm) if shadcn Dialog props map cleanly.

    3. **Per-sub-folder commits** — 92 files is large; split into ~5-7 commits by sub-area:
       - `feat(ui-121-04a-settings-core)`: core settings (index, change-name, change-password, advanced, troubleshoot)
       - `feat(ui-121-04a-settings-security)`: 2fa, 2fa-enable, 2fa-disable, local-access, dm-pairing
       - `feat(ui-121-04a-settings-integrations)`: gmail, voice, ai-config, integrations, liv-agent, memory
       - `feat(ui-121-04a-settings-system)`: device-info, software-update, restart, shutdown, migration-assistant, factory-reset, chrome-master
       - `feat(ui-121-04a-settings-store)`: app-store-preferences, domain-setup, users, usage-dashboard
       - `feat(ui-121-04a-settings-misc)`: diagnostics, terminal, mobile, _components
       - `feat(ui-121-04a-settings-confirm)`: software-update-confirm + any remaining

    4. Behavioral-guard regex per commit. Sacred SHA pre-flight.
  </action>

  <acceptance_criteria>
    - All 92 settings files token-migrated.
    - ui-kit imports across settings ≥ 20 (high-applicability surface).
    - Build PASS after each commit.
    - Sacred SHA preserved every commit.
    - 5-7 atomic commits shipped.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/routes/settings/ | wc -l && \
      grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/routes/settings/ | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Sub-batch 04a (settings/*) shipped as 5-7 commits. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="checkpoint:operator-uat" gate="blocking">
  <name>Task 2 (Checkpoint 04a-UAT): Operator deploys + browses settings on Mini PC</name>

  <what-built>
    All 92 routes/settings/* tsx files migrated to canonical tokens + ui-kit primitives across 5-7 atomic commits. Build PASS. Behavioral handlers untouched (regex PASS). Sacred SHA preserved.
  </what-built>

  <how-to-verify>
    1. SSH to Mini PC: `ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@10.69.31.68`
    2. Deploy: `bash /opt/livos/update.sh` (await success)
    3. Browse `https://bruce.livinity.io` in a real browser (hard-reload)
    4. Open Settings (gear icon or `Cmd+,`)
    5. Walk every settings panel left-rail entry:
       - Account: Change name, Change password, 2FA (enable/disable wizard), Device pairing, Local access
       - System: Device info, Software update (check + apply), Restart, Shutdown, Factory reset
       - Apps: App Store preferences, Domain setup, Users
       - Integrations: Gmail, Voice, AI config, Liv Agent, Memory
       - Diagnostics: Usage dashboard, Terminal, Mobile pairing
       - Advanced: Migration assistant, Chrome master profile, Troubleshoot
    6. Per-panel checklist:
       - [ ] Renders with canonical tokens (visual diff vs dashboard.html — Geist font, card-bg background, dash-radius)
       - [ ] All form inputs accept text + submit correctly
       - [ ] Save buttons work + show success state
       - [ ] Cancel/back navigation works
       - [ ] Light/dark/iridescent toggle propagates to all panels
       - [ ] No console errors in DevTools

    7. **If any panel regresses:** identify the failing commit (`git log --oneline | grep ui-121-04a`), revert with `git revert <hash> && bash /opt/livos/update.sh`, document in 121-04-SUMMARY.md, retry the failing migration in a follow-up task.
  </how-to-verify>

  <resume-signal>
    Operator types: "04a approved" (all settings panels visually canonical + functionally byte-identical), OR
    Operator types: "04a regressed: <description>" (proceed to revert + retry workflow before 04b begins).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3 (Sub-batch 04b): Migrate routes/{app-store,community-app-store,agent-marketplace,subagents,docker,factory-reset} (95 tsx files)</name>
  <files>livos/packages/ui/src/routes/app-store/**/*.tsx, livos/packages/ui/src/routes/community-app-store/**/*.tsx, livos/packages/ui/src/routes/agent-marketplace/**/*.tsx, livos/packages/ui/src/routes/subagents/**/*.tsx, livos/packages/ui/src/routes/docker/**/*.tsx, livos/packages/ui/src/routes/factory-reset/**/*.tsx</files>

  <read_first>
    Read each file. apps-aggregate sub-batch is dominated by docker/* (75 files) and settings-style detail pages. app-store-content already touched in Phase 120-05 — confirm whether the route-level wrappers also need migration.
  </read_first>

  <action>
    1. Token migration across all 95 files in this sub-batch (4 app-store + 2 community + 3 agent-marketplace + 1 subagents + 75 docker + 10 factory-reset).

    2. **ui-kit swap targets:**
       - `<Pill>` for app-card status (installed/not-installed/updating/featured/verified) — docker container status (running/stopped/restarting) — agent-marketplace tags
       - `<Button>` for Install / Uninstall / Update / Start / Stop / Restart CTAs (where signatures match)
       - `<Card>` for app-detail cards, container-detail cards, agent-detail cards (plain JSX children)
       - `<Input>` for search boxes (app-store, community, agent-marketplace, docker container search)
       - `<Modal>` for confirm-uninstall dialogs (where shadcn Dialog props match)

    3. **docker-specific guidance:**
       - 75 docker files contain `useQuery` / `useMutation` for container CRUD — class-only migration on those handler lines is critical. Apply behavioral-guard regex with `useQuery|useMutation|trpc\\.docker` in pattern.
       - Container status badges → `<Pill tone="ok|warn|err|neutral">` is a strong swap.

    4. **factory-reset routes vs features:** Plan 121-01 already migrated `features/factory-reset/*`. The `routes/factory-reset/*` files are the route-level wrappers (10 files) — likely just import + render the feature. Audit each: if pure import + render, audit-only NOOP; if has its own classes, migrate.

    5. Per-sub-folder commits — split into ~5-6 commits:
       - `feat(ui-121-04b-app-store)`: app-store + community-app-store
       - `feat(ui-121-04b-agent-marketplace)`: agent-marketplace + subagents
       - `feat(ui-121-04b-docker-containers)`: docker container-list + container-detail + actions
       - `feat(ui-121-04b-docker-images)`: docker image-list + image-detail + pull
       - `feat(ui-121-04b-docker-misc)`: docker remaining (networks, volumes, compose, etc.)
       - `feat(ui-121-04b-factory-reset-routes)`: routes/factory-reset wrappers (audit + migrate or NOOP)

    6. Behavioral-guard regex per commit. Sacred SHA pre-flight.
  </action>

  <acceptance_criteria>
    - All 95 files token-migrated (or audited as NOOP for thin wrappers).
    - ui-kit imports ≥ 15 across this sub-batch.
    - Build PASS after each commit.
    - Sacred SHA preserved every commit.
    - 5-6 atomic commits.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/routes/app-store/ livos/packages/ui/src/routes/docker/ livos/packages/ui/src/routes/agent-marketplace/ livos/packages/ui/src/routes/subagents/ livos/packages/ui/src/routes/community-app-store/ livos/packages/ui/src/routes/factory-reset/ | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Sub-batch 04b (apps-aggregate + docker) shipped as 5-6 commits. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="checkpoint:operator-uat" gate="blocking">
  <name>Task 4 (Checkpoint 04b-UAT): Operator deploys + browses app-store + docker on Mini PC</name>

  <what-built>
    All 95 files in routes/{app-store,community-app-store,agent-marketplace,subagents,docker,factory-reset}/ migrated. Build PASS. Sacred SHA preserved.
  </what-built>

  <how-to-verify>
    1. SSH + deploy + browse (same prep as 04a UAT).
    2. App Store: open from dock, browse categories, click a featured app, click Install → confirm install dialog renders + executes + status updates.
    3. Community App Store: same flow.
    4. Agent Marketplace: browse, click agent detail.
    5. Subagents: list renders, detail view works.
    6. Docker: open from dock; container list renders with status pills; click a running container → detail view + Stop button works; image list renders; pull-image flow works (or at least the dialog opens).
    7. Factory Reset: open from Settings → Factory Reset → wizard step 1 renders (DO NOT actually execute factory reset).
    8. Per-surface checklist (visual + functional):
       - [ ] Canonical tokens applied
       - [ ] Status pills render correct tone
       - [ ] Install/Stop/Start/Restart/Uninstall buttons work
       - [ ] Search boxes filter results
       - [ ] Modals open + dismiss + execute
       - [ ] Light/dark/iridescent propagates
       - [ ] No console errors

    Revert + retry workflow same as 04a.
  </how-to-verify>

  <resume-signal>
    Operator types: "04b approved" (apps + docker all good), OR
    Operator types: "04b regressed: <description>" (proceed to revert + retry before 04c).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5 (Sub-batch 04c): Migrate routes/{ai-chat,login,server-control,onboarding,playground,my-devices,help,schedules} + 5 top-level + SUMMARY (~32 files)</name>
  <files>livos/packages/ui/src/routes/ai-chat/**/*.tsx, livos/packages/ui/src/routes/login/**/*.tsx, livos/packages/ui/src/routes/server-control/**/*.tsx, livos/packages/ui/src/routes/onboarding/**/*.tsx, livos/packages/ui/src/routes/playground/**/*.tsx, livos/packages/ui/src/routes/my-devices/**/*.tsx, livos/packages/ui/src/routes/help/**/*.tsx, livos/packages/ui/src/routes/schedules/**/*.tsx, livos/packages/ui/src/routes/invite.tsx, livos/packages/ui/src/routes/live-usage.tsx, livos/packages/ui/src/routes/login.tsx, livos/packages/ui/src/routes/not-found.tsx, livos/packages/ui/src/routes/notifications.tsx, .planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md</files>

  <read_first>
    Read each file in misc + top-level routes (~32 total: ai-chat 15 + login 1 + server-control 1 + onboarding 6 + playground 1 + my-devices 1 + help 1 + schedules 1 + 5 top-level).
  </read_first>

  <action>
    1. Token migration across all ~32 misc/top-level routes files.

    2. **Sub-batch-specific guidance:**
       - **ai-chat routes (15 files)** — most of the AI Chat surface was touched in Plan 120-04. These route-level wrappers may already be canonical. Audit + migrate where needed.
       - **login route + login.tsx (top-level)** — login screen was deferred from Plan 120 carry-over. Migrate to canonical tokens + use ui-kit `<Input>`, `<PasswordInput>`, `<Button>` for the form.
       - **onboarding (6 files)** — first-run wizard. Use ui-kit `<Stepper>` if wizard step-indicator is statically derivable. ui-kit `<CommandBox>` for any displayed commands.
       - **server-control + factory-reset routes + restart/shutdown CTAs** — destructive actions; class-only safety, leave shadcn AlertDialog intact, swap inner Buttons to `<Button variant="danger">` only if onClick signatures match.
       - **5 top-level files** — invite, live-usage, login.tsx, not-found, notifications — each is single-file; token-migrate, swap ui-kit where applicable, atomic commit.

    3. Per-sub-folder commits — split into ~4-5 commits:
       - `feat(ui-121-04c-ai-chat-routes)`: ai-chat/*
       - `feat(ui-121-04c-onboarding)`: onboarding/*
       - `feat(ui-121-04c-login)`: login/* + login.tsx (use ui-kit Input/PasswordInput/Button)
       - `feat(ui-121-04c-server-control)`: server-control + my-devices + schedules + help
       - `feat(ui-121-04c-misc)`: playground + 5 top-level (invite, live-usage, not-found, notifications)

    4. Behavioral-guard regex per commit. Sacred SHA pre-flight.

    5. **Author 121-04-SUMMARY.md** (≥80 lines — this is the largest plan):
       - Header
       - Plans-shipped table: ALL 04a + 04b + 04c commits with hashes + status
       - 3 sub-batch summaries (04a settings 92 / 04b apps-aggregate 95 / 04c misc 32) = 219 total files reconciled
       - ui-kit import counts per sub-batch (settings ≥ 20, apps ≥ 15, misc ≥ 10)
       - Behavioral-guard PASS evidence per commit (table)
       - Sacred SHA preserved N/N across all 04a+04b+04c commits
       - **3 operator UAT checkpoint outcomes documented:**
         - Checkpoint 04a-UAT: operator response + any regressions caught + fixes
         - Checkpoint 04b-UAT: operator response + any regressions caught + fixes
         - Checkpoint 04c-UAT: operator response (final UAT in this plan — comprehensive walk of remaining surfaces) + any regressions
       - Deviations: deferred shadcn-Modal/AlertDialog/Select swaps (tagged TODO 121-05), any audit-only NOOPs
       - Carry-overs to 121-05 (generic components + shadcn audit), 121-06 (cross-surface report, Playwright suite, STYLE-GUIDE)
       - 121-04 final verdict per sub-batch

    6. Commit SUMMARY: `docs(121-04): SUMMARY — routes/* migrated across 3 sub-batches`.
  </action>

  <acceptance_criteria>
    - All ~32 misc/top-level files token-migrated.
    - ui-kit imports ≥ 10 in misc/top-level.
    - 121-04-SUMMARY.md ≥ 80 lines.
    - All 3 UAT checkpoint outcomes documented.
    - Build PASS after final commit. Sacred SHA preserved across ALL 04a+04b+04c commits (likely 15-20 total).
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      find livos/packages/ui/src/routes -name "*.tsx" | wc -l && \
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/routes/ | wc -l && \
      grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/routes/ | wc -l && \
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts && \
      git log --oneline -20
    </automated>
  </verify>

  <done>
    All 219 routes/* files migrated. Build PASS. SUMMARY ≥ 80 lines with 3 UAT checkpoint outcomes. Sacred SHA verified. Plan 121-04 closed.
  </done>
</task>

<task type="checkpoint:operator-uat" gate="blocking">
  <name>Task 6 (Checkpoint 04c-UAT): Operator final UAT for routes/* sub-batch 04c</name>

  <what-built>
    Final sub-batch (04c misc + top-level) migrated. 121-04-SUMMARY.md authored documenting all 3 sub-batches.
  </what-built>

  <how-to-verify>
    1. SSH + deploy + browse (same prep).
    2. Walk remaining surfaces:
       - AI Chat (full chat flow — already validated in Phase 120-04 but verify route wrappers)
       - Login screen (logout, then re-login)
       - Onboarding (if accessible — usually first-run only; can trigger via dev flag or fresh-install simulation)
       - Server control (restart preview only; do NOT execute)
       - Schedules + My Devices + Help + Playground
       - Top-level: notifications panel, live-usage dashboard, 404 page (visit /nonexistent), invite acceptance flow
    3. Light/dark/iridescent toggle on every surface.
    4. Per-surface checklist (same template as 04a, 04b).
    5. Revert + retry workflow same as prior checkpoints.
  </how-to-verify>

  <resume-signal>
    Operator types: "04c approved" → plan 121-04 closes, wave 4 (121-05) begins. OR
    Operator types: "04c regressed: <description>" → revert + retry before plan closes.
  </resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter ui build` PASS after every sub-batch.
- `grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/routes/` returns 0.
- `find livos/packages/ui/src/routes -name "*.tsx" | wc -l` = 219.
- Sacred SHA = `f3538e1d811992b782a9bb057d1b7f0a0189f95f` after every commit (likely 15-20 total).
- Behavioral-guard regex PASS on every diff.
- 3 operator UAT checkpoints completed + outcomes documented.
- `livos/packages/livinityd/` + `liv/` untouched.
</verification>

<success_criteria>
- 219 routes/* tsx files migrated across 3 sub-batches.
- ui-kit import count ≥ 45 across all of routes/ (settings 20 + apps 15 + misc 10).
- 3 operator UAT checkpoints PASS (or regressions caught + fixed before next sub-batch).
- Sacred SHA preserved every commit.
- 121-04-SUMMARY.md committed with all 3 UAT outcomes.
- D-121-OPERATOR-CHECKPOINTS honored (3 checkpoints inserted).
- D-121-NO-FUNCTIONAL-CHANGES, D-121-MINI-PC-OPERATOR-PRIORITY, D-V35-SACRED-SHA, D-V35-INCREMENTAL-COMMITS — all honored.
</success_criteria>

<output>
After completion, create `.planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md` per Task 5 step 5 spec.
</output>
