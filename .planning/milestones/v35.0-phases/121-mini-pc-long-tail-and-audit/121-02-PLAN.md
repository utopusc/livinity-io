---
phase: 121-mini-pc-long-tail-and-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 121-01
files_modified:
  - livos/packages/ui/src/features/files/**/*.tsx
autonomous: true
requirements:
  - V35-AC1
  - V35-AC2
  - V35-AC4
  - V35-AC5
user_setup: []

must_haves:
  truths:
    - "All .tsx files under features/files migrate from raw zinc/blue tailwind classes to canonical token classes"
    - "ui-kit Button / Card / Pill / Input swapped in where prop API matches without behavioral shim"
    - "`pnpm --filter ui build` exits 0 after the plan"
    - "Sacred SHA f3538e1d811992b782a9bb057d1b7f0a0189f95f preserved on every commit"
    - "Behavioral-diff guard PASS: file-tree expand/collapse, drag-drop upload, multi-select, breadcrumb navigation, context menu actions all byte-identical to pre-migration"
    - "OwnCloud-equivalent file browsing experience preserved — per D-121-MINI-PC-OPERATOR-PRIORITY this is bruce's daily-driver surface, zero regression tolerance"
  artifacts:
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md"
      provides: "Per-file migration tally + commit hashes + sacred SHA verification + build PASS evidence + operator UAT block + files-feature behavioral checklist"
      min_lines: 50
    - path: "livos/packages/ui/src/features/files"
      provides: "Restyled files feature using canonical tokens"
      contains: "bg-card-bg"
  key_links:
    - from: "livos/packages/ui/src/features/files/**"
      to: "livos/packages/ui-kit/src/index.ts"
      via: "import { Button, Card, Pill, Input, Modal } from '@livinity/ui-kit'"
      pattern: "from ['\"]@livinity/ui-kit['\"]"
    - from: "livos/packages/ui/src/features/files/**"
      to: "@livinity/design-tokens"
      via: "canonical token classes from tailwind preset"
      pattern: "bg-card-bg|bg-accent-(blue|green|amber|red)|rounded-dash|p-dash"
---

<objective>
Migrate Mini PC livinityd UI long-tail batch #2 — the entire `features/files/` tree (119 tsx files; the planning_context calls this "~25 components" referring to the top-level exported component surface — the leaf-file count is 119 because of deeply nested helper components) — from raw Tailwind classes to canonical design tokens + `@livinity/ui-kit` primitives.

Purpose: Files is the second-highest-traffic Mini PC surface after the dock/desktop (Phase 120). It IS the OwnCloud replacement experience — bruce's daily-driver use case per `feedback_minipc_is_owncloud_primary`. Migrating to canonical tokens makes the file browser visually consistent with `livinity.io/dashboard` while preserving every single behavioral path.

Output:
- All `.tsx` files under `livos/packages/ui/src/features/files/` migrated to canonical tokens.
- ui-kit imports where prop API matches.
- `pnpm --filter ui build` PASS.
- One commit per logical sub-area (file-list, file-grid, breadcrumb, context-menu, upload-dialog, share-dialog) per D-V35-INCREMENTAL-COMMITS — target 4-6 commits.
- 121-02-SUMMARY.md authored, includes a Files behavioral checklist for operator UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-CONTEXT.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-01-SUMMARY.md
@.planning/phases/115-ui-component-inventory/INVENTORY-MINI-PC.md
@livos/packages/ui-kit/src/index.ts
@livos/packages/design-tokens/tailwind.preset.cjs

<interfaces>
<!-- Same ui-kit exports as Plan 121-01 (see its <interfaces> block). -->
<!-- Files feature uses these primitives heavily and is a strong ui-kit-swap target: -->
<!-- - <Button> for toolbar actions (upload, new folder, sort) -->
<!-- - <Modal> for upload-dialog, share-dialog, rename-dialog (if shadcn Dialog props map cleanly) -->
<!-- - <Pill> for file-type badges (folder/file/image/video tags) -->
<!-- - <Input> for breadcrumb-path-editor and search-box -->
<!-- - <CommandBox> is generally NOT applicable here -->
<!-- - <Stepper> NOT applicable -->

<!-- Canonical tokens (same map as 121-01): -->
<!-- bg-zinc-* → bg-card-bg / bg-card-bg-2 -->
<!-- bg-blue-600 → bg-accent-blue -->
<!-- rounded-2xl → rounded-dash (card shells only) -->
<!-- p-7 → p-dash (card shells only) -->
<!-- text-zinc-* → text-card-fg -->
<!-- border-zinc-* → border-dash-line -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory features/files tree + sub-area grouping</name>
  <files>livos/packages/ui/src/features/files/**/*.tsx</files>

  <read_first>
    `ls livos/packages/ui/src/features/files/` to discover sub-folders (likely: list, grid, breadcrumb, context-menu, dialogs, hooks). `grep -rE "bg-(zinc|blue|green|amber|red)-" livos/packages/ui/src/features/files/ -l | sort` to enumerate every file with a class to migrate.
  </read_first>

  <action>
    1. Enumerate all 119 tsx files. Group into 4-6 logical sub-areas:
       - File list/grid views (rows, icons, layout)
       - Toolbar + breadcrumb + path-editor
       - Context menu + bulk-action bar
       - Dialogs (upload, share, rename, delete-confirm, move, properties)
       - Hooks / providers / non-rendering files (most need no migration)
       - Misc leaf components

    2. **Per sub-area, plan one commit.** Per D-V35-INCREMENTAL-COMMITS — atomic per logical chunk so a single sub-area can be reverted without dragging in the others.

    3. Write the sub-area grouping into the SUMMARY scratch (do NOT commit SUMMARY yet; final SUMMARY at end of Task 3).

    No code changes in this task — pure inventory + grouping. No commit.
  </action>

  <acceptance_criteria>
    - 119 tsx files enumerated; each tagged with sub-area name.
    - 4-6 sub-areas defined.
    - Sub-area grouping captured for Task 2/3 to consume.
  </acceptance_criteria>

  <verify>
    <automated>
      find livos/packages/ui/src/features/files -name "*.tsx" | wc -l && \
      grep -rlE "bg-(zinc|blue|green|amber|red)-" livos/packages/ui/src/features/files/ | wc -l
    </automated>
  </verify>

  <done>
    Inventory complete. Sub-area grouping noted. No commits yet. Proceed to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate file-list/grid + toolbar/breadcrumb (sub-areas 1-2)</name>
  <files>livos/packages/ui/src/features/files/**/*.tsx</files>

  <read_first>
    Read each file in sub-areas 1 + 2 before editing.
  </read_first>

  <action>
    Apply token-migration + ui-kit-swap (Plan 121-01 Task 1 pattern) to sub-areas 1 + 2.

    **Files-specific guidance:**
    - File-list rows: replace per-row `bg-zinc-50 hover:bg-zinc-100` with `bg-card-bg hover:bg-card-bg-2`. Keep all `onClick={() => navigate(...)}` and `onDoubleClick={() => openFile(...)}` byte-identical.
    - Breadcrumb chips: if they render as `<button>`s with `bg-zinc-100 rounded-md px-2`, leave native button + class-only migrate (event signatures usually mismatch ui-kit Button).
    - Path-editor input: shadcn `<Input>` swap to ui-kit `<Input>` ONLY if existing prop usage is `value`+`onChange={(e) => ...}` standard React events. If it uses shadcn `onValueChange={fn}`, leave shadcn + class-only migrate — adapter pass deferred to Plan 121-05.

    **Drag-drop preservation (CRITICAL):**
    - `onDragOver`, `onDragLeave`, `onDrop`, `onDragStart`, `draggable` attrs MUST be byte-identical pre/post. Behavioral-guard regex must include these handlers.

    **Behavioral-guard regex (extended for files):**
    ```bash
    git diff --unified=0 -- livos/packages/ui/src/features/files/ | \
      grep -E "^[-+].*(onClick|onSubmit|onChange|onDragOver|onDragLeave|onDrop|onDragStart|draggable|onDoubleClick|onContextMenu|useMutation|useQuery|useEffect|useState|useRef|fetch\\(|axios|trpc\\.).*" || echo "BEHAVIORAL-GUARD: PASS"
    ```

    Sacred SHA pre-flight: same as 121-01.

    Commit per sub-area:
    - `feat(ui-121-02-list-grid): migrate features/files list+grid to canonical tokens`
    - `feat(ui-121-02-toolbar): migrate features/files toolbar+breadcrumb to canonical tokens`
  </action>

  <acceptance_criteria>
    - Sub-area 1 (list/grid) and sub-area 2 (toolbar/breadcrumb) token-migrated.
    - Drag-drop + double-click + context-menu handlers byte-identical (behavioral-guard regex PASS).
    - Build PASS. Sacred SHA preserved 2/2 commits.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts && \
      git log --oneline -3
    </automated>
  </verify>

  <done>
    Sub-areas 1+2 shipped as 2 atomic commits. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate context-menu + dialogs + misc + SUMMARY (sub-areas 3-6)</name>
  <files>livos/packages/ui/src/features/files/**/*.tsx, .planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md</files>

  <read_first>
    Read each file in sub-areas 3+4+5+6 before editing. Dialogs are the highest-risk sub-area — `<Modal>` ui-kit swap candidates here, but only if shadcn Dialog props (open / onOpenChange) map cleanly to ui-kit Modal.
  </read_first>

  <action>
    1. Apply token-migration + ui-kit-swap to sub-areas 3-6.

    2. **Dialogs (sub-area 4) ui-kit swap strategy:**
       - shadcn `<Dialog open={x} onOpenChange={setX}>` → ui-kit `<Modal open={x} onClose={() => setX(false)}>` ONLY if:
         - No `<DialogTrigger>` is used (or it's separable)
         - No `<DialogPortal>` portal customization
         - Inner content is plain JSX (no `<DialogHeader>` / `<DialogFooter>` subcomponents that ui-kit Modal doesn't export)
       - If ANY of those constraints fail, leave shadcn Dialog + class-only migrate. Tag with `// TODO(121-05): swap to ui-kit <Modal> after subcomponent parity`.

    3. **Context menu (sub-area 3):**
       - shadcn `<ContextMenu>` has NO ui-kit equivalent — class-only migration. Tag for v0.2.0 ui-kit candidate in Plan 121-05 audit.

    4. Behavioral-guard regex + sacred SHA pre-flight per sub-area commit.

    5. Commits:
       - `feat(ui-121-02-context-menu): migrate features/files context-menu to canonical tokens`
       - `feat(ui-121-02-dialogs): migrate features/files dialogs to canonical tokens (+ ui-kit Modal where applicable)`
       - `feat(ui-121-02-misc): migrate features/files misc leaves to canonical tokens` (combine sub-areas 5+6)

    6. **Author 121-02-SUMMARY.md** (≥50 lines):
       - Header (phase / plan / wave / status / date)
       - Plans-shipped table: 4-6 sub-area commits with hashes + status
       - Per-sub-area tally: file count + ui-kit-import count + behavioral-guard PASS evidence
       - Sacred SHA: preserved N/N commits, verified hash matches
       - Operator UAT block: `ssh -i ... bruce@10.69.31.68 → bash /opt/livos/update.sh → browse files in Mini PC → checklist:`
         - File-list renders with canonical tokens (visual diff vs baseline)
         - Hover state works
         - Single-click selects, double-click opens
         - Right-click context menu opens, all actions work
         - Drag-drop upload works
         - Multi-select with shift+click / cmd+click works
         - Breadcrumb navigation works
         - Path-editor input works
         - Upload / share / rename / delete dialogs all open + dismiss + execute correctly
         - Light/dark/iridescent toggle propagates to files surface
       - Deviations: deferred shadcn-Dialog swaps tagged with `TODO(121-05)`, context-menu deferred entirely
       - Carry-overs to 121-05 (shadcn audit) and 121-06 (consistency report — files vs dashboard.html parity)

    7. Commit SUMMARY: `docs(121-02): SUMMARY — files feature migrated`.
  </action>

  <acceptance_criteria>
    - All 119 files token-migrated where applicable (no `bg-zinc-50` / `bg-zinc-100` / `bg-blue-600` literal classes remain on JSX elements).
    - ui-kit imports present in ≥ 8 files across the feature.
    - 121-02-SUMMARY.md ≥ 50 lines.
    - Build PASS. Sacred SHA preserved 4-6/4-6 commits.
    - Behavioral-guard regex PASS on every diff.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/features/files/ | wc -l && \
      grep -rE "bg-(blue|green|amber|red)-(500|600)\b" livos/packages/ui/src/features/files/ | wc -l && \
      grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/features/files/ | wc -l && \
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    All sub-areas migrated. Build PASS. SUMMARY ≥ 50 lines. Sacred SHA verified. Plan 121-02 closed.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter ui build` PASS after every commit.
- Zinc/blue/green/amber/red bg literal counts in features/files = 0.
- Behavioral-guard regex (extended with drag-drop + dblclick + contextmenu) = PASS for every diff.
- Sacred SHA = `f3538e1d811992b782a9bb057d1b7f0a0189f95f` after every commit.
- `livos/packages/livinityd/` + `liv/` untouched.
</verification>

<success_criteria>
- 119 tsx files token-migrated across 4-6 atomic commits.
- ui-kit import count ≥ 8 across the feature.
- Zero behavioral regression on file-browser core interactions.
- Sacred SHA preserved every commit.
- 121-02-SUMMARY.md committed with files-specific behavioral UAT checklist.
- D-121-NO-FUNCTIONAL-CHANGES, D-121-MINI-PC-OPERATOR-PRIORITY, D-V35-SACRED-SHA — all honored.
</success_criteria>

<output>
After completion, create `.planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md` per Task 3 step 6 spec.
</output>
