---
phase: 121-mini-pc-long-tail-and-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 121-01
files_modified:
  - livos/packages/ui/src/modules/window/**/*.tsx
autonomous: true
requirements:
  - V35-AC1
  - V35-AC2
  - V35-AC4
  - V35-AC5
user_setup: []

must_haves:
  truths:
    - "All .tsx files under modules/window (window chrome, webapp drawer/stream/skills/teach, skill replay) AND modules/window/app-contents (per-app content dialogs: ai-chat-content, app-store-content, chrome-content, docker-content, files-content, my-devices-content, remote-desktop-content, schedules-content, server-control-content, settings-content, subagents-content, terminal-content, webapp-* family) migrate to canonical tokens"
    - "Window chrome (window.tsx, window-chrome.tsx, window-content.tsx, windows-container.tsx) — visually canonical AND functionally byte-identical: drag, resize, minimize, maximize, close, focus, z-index ordering, snap regions all preserved"
    - "WebApp Launcher overlay (webapp-stream-window, webapp-floating-action-bar, webapp-skills-sidebar, webapp-teach-drawer, webapp-mode-selector, webapp-chat-drawer) — visually canonical AND behaviorally untouched per Phase 99/110 carry-over locks"
    - "`pnpm --filter ui build` PASS"
    - "Sacred SHA f3538e1d preserved every commit"
    - "Strong behavioral-guard regex output PASS — window chrome handlers are load-bearing; any onMouseDown / onMouseMove / onMouseUp / onPointerDown / onResize / onFocus / onBlur diff is forbidden"
  artifacts:
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md"
      provides: "Per-file migration tally + commit hashes + sacred SHA verification + build PASS + window-chrome behavioral checklist + WebApp Launcher regression checklist"
      min_lines: 60
    - path: "livos/packages/ui/src/modules/window"
      provides: "Restyled window chrome + WebApp Launcher surface + per-app dialogs using canonical tokens"
      contains: "bg-card-bg"
  key_links:
    - from: "livos/packages/ui/src/modules/window/window-chrome.tsx"
      to: "@livinity/design-tokens"
      via: "canonical token classes"
      pattern: "bg-card-bg|rounded-dash"
    - from: "livos/packages/ui/src/modules/window/app-contents/**"
      to: "@livinity/ui-kit"
      via: "ui-kit imports where prop API matches"
      pattern: "from ['\"]@livinity/ui-kit['\"]"
---

<objective>
Migrate Mini PC livinityd UI long-tail batch #3 — `modules/window/` (38 tsx files including window chrome + WebApp Launcher overlay + per-app content dialogs) — from raw Tailwind classes to canonical design tokens + `@livinity/ui-kit` primitives.

Purpose: Window chrome is the FRAME of the entire Mini PC experience — every app renders inside one of these windows. Per-app content dialogs are the Phase 100 "click app icon → opens window with content" inner-surface. WebApp Launcher (Phase 92-98 / 110) is the bytebot + Chrome stream overlay that runs ON TOP of native windows. All three sub-surfaces must visually unify to dashboard.html aesthetic WITHOUT touching event handlers.

Output:
- All `.tsx` files under `livos/packages/ui/src/modules/window/` (including `app-contents/`) migrated to canonical tokens.
- ui-kit imports where prop API matches.
- `pnpm --filter ui build` PASS.
- Multiple atomic commits per sub-area (chrome / webapp launcher / per-app content groups).
- 121-03-SUMMARY.md with extra-detailed behavioral checklist (window chrome is load-bearing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-CONTEXT.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-01-SUMMARY.md
@.planning/phases/115-ui-component-inventory/INVENTORY-MINI-PC.md
@.planning/phases/120-mini-pc-ui-migration-wave-1/120-VERIFICATION.md
@livos/packages/ui-kit/src/index.ts
@livos/packages/design-tokens/tailwind.preset.cjs

<interfaces>
<!-- Same ui-kit exports as Plan 121-01. -->
<!-- Window chrome files (window.tsx, window-chrome.tsx, window-content.tsx, windows-container.tsx) -->
<!-- carry the most load-bearing event-handler logic in the entire UI. Treat as token-only migration. -->
<!-- DO NOT swap the chrome frame to ui-kit Card — it has resize / drag / position absolute styling -->
<!-- ui-kit Card doesn't expose. -->

<!-- Per-app content files (modules/window/app-contents/*.tsx) are inner surfaces — -->
<!-- much more amenable to ui-kit primitives (Buttons, Cards, Pills, Inputs). -->

<!-- WebApp Launcher family (webapp-*.tsx in modules/window/) — class-only migration safest -->
<!-- because the Phase 99/110 VNC swap is still recent and behavior is fragile. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate window chrome (window.tsx, window-chrome.tsx, window-content.tsx, windows-container.tsx)</name>
  <files>livos/packages/ui/src/modules/window/window.tsx, livos/packages/ui/src/modules/window/window-chrome.tsx, livos/packages/ui/src/modules/window/window-content.tsx, livos/packages/ui/src/modules/window/windows-container.tsx</files>

  <read_first>
    Read all 4 files end-to-end. These contain `onMouseDown` (drag start), `onMouseMove` (drag tracking), `onPointerDown` (resize start), `onFocus` / `onBlur` (focus mgmt), `useRef` for window position, `useState` for size+pos, `useEffect` for snap-zone detection. Every one of these handlers is load-bearing — Plan 100 spent multiple plans debugging window-drag/snap behavior.
  </read_first>

  <action>
    1. **Class-only migration** (NO ui-kit swaps in this task — window chrome is too fragile):
       - `bg-zinc-50` / `bg-white` (window background) → `bg-card-bg`
       - `bg-zinc-100` (title-bar) → `bg-card-bg-2`
       - `border-zinc-200` / `border-zinc-300` → `border-dash-line`
       - `rounded-2xl` (window corner radius if used) → `rounded-dash`
       - `shadow-xl` / `shadow-2xl` on the window frame → leave alone (canonical shadow is set via tokens.css `--card-shadow`; if the file uses inline `style={{ boxShadow: 'var(--card-shadow)' }}` already, no change; if it uses Tailwind shadow, leave it — the visual diff is acceptable)
       - Close/min/max button colors: `bg-red-500` → `bg-accent-red`, `bg-amber-500` → `bg-accent-amber`, `bg-green-500` → `bg-accent-green`

       Leave `position: absolute`, `transform`, `cursor-*`, `select-none`, `pointer-events-*` ALL untouched.

    2. **Behavioral-guard regex (strict for window chrome):**
       ```bash
       git diff --unified=0 -- livos/packages/ui/src/modules/window/window.tsx \
                                livos/packages/ui/src/modules/window/window-chrome.tsx \
                                livos/packages/ui/src/modules/window/window-content.tsx \
                                livos/packages/ui/src/modules/window/windows-container.tsx | \
         grep -E "^[-+].*(onMouseDown|onMouseMove|onMouseUp|onPointerDown|onPointerMove|onPointerUp|onFocus|onBlur|onClick|onResize|useRef|useState|useEffect|useMemo|useCallback|position|transform|setSnap|drag|resize|zIndex|focus|blur).*" || echo "BEHAVIORAL-GUARD: PASS"
       ```
       MUST be `BEHAVIORAL-GUARD: PASS`. If even one such line appears, REVERT and retry as a pure class-string-only edit.

    3. Sacred SHA pre-flight.

    4. Commit: `feat(ui-121-03-chrome): migrate window chrome to canonical tokens (class-only, behavioral untouched)`.
  </action>

  <acceptance_criteria>
    - 4 window-chrome files class-migrated; zero handler/hook/ref/state line modified.
    - Behavioral-guard regex output is exactly `BEHAVIORAL-GUARD: PASS`.
    - Build PASS. Sacred SHA preserved.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Chrome migrated class-only. Build PASS. Sacred SHA verified. Commit shipped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate WebApp Launcher family (webapp-*.tsx) + skill-replay-scrubber + module top-level</name>
  <files>livos/packages/ui/src/modules/window/webapp-chat-bottom-bar.tsx, livos/packages/ui/src/modules/window/webapp-chat-drawer.tsx, livos/packages/ui/src/modules/window/webapp-floating-action-bar.tsx, livos/packages/ui/src/modules/window/webapp-floating-skills-button.tsx, livos/packages/ui/src/modules/window/webapp-mode-selector.tsx, livos/packages/ui/src/modules/window/webapp-skills-sidebar.tsx, livos/packages/ui/src/modules/window/webapp-stream-window.tsx, livos/packages/ui/src/modules/window/webapp-teach-drawer.tsx, livos/packages/ui/src/modules/window/webapp-teach-popup-host.tsx, livos/packages/ui/src/modules/window/skill-replay-scrubber.tsx, livos/packages/ui/src/modules/window/teach-popover.tsx</files>

  <read_first>
    Read each file. WebApp Launcher (Phase 92-98 + Phase 99/110 VNC swap) is recent + fragile. The teach/auto/chat/watch mode-selector buttons have onClick handlers that dispatch to `webappWindowManager` — leave these handlers byte-identical.
  </read_first>

  <action>
    1. Class-only migration (same map as Task 1).
       - WebApp launcher floating action bar: `bg-zinc-900/80 backdrop-blur` (its iconic floating chrome) → `bg-card-bg/80 backdrop-blur` so it inherits dark/iridescent.
       - Mode-selector button states: `bg-blue-600` (active) → `bg-accent-blue`, `bg-zinc-200` (inactive) → `bg-card-bg-2`.
       - Skills sidebar: `bg-zinc-50` → `bg-card-bg`.

    2. ui-kit swaps in this batch are PERMITTED for `<Button>` and `<Pill>` ONLY where:
       - onClick signature is plain `(e: MouseEvent) => void`
       - No `forwardRef` requirement
       - No `asChild` shadcn polymorphism

       Otherwise class-only.

    3. **Strong behavioral-guard regex** (includes WebApp + skill-replay handlers):
       ```bash
       git diff --unified=0 -- livos/packages/ui/src/modules/window/webapp-*.tsx \
                                livos/packages/ui/src/modules/window/skill-replay-scrubber.tsx \
                                livos/packages/ui/src/modules/window/teach-popover.tsx | \
         grep -E "^[-+].*(onClick|onChange|onMouseDown|onPointerDown|useMutation|useQuery|useEffect|useState|useRef|trpc\\.|fetch\\(|streamManager|webappWindowManager|skillReplay|teach|watch|auto|chat).*" || echo "BEHAVIORAL-GUARD: PASS"
       ```

    4. Sacred SHA pre-flight.

    5. Commit: `feat(ui-121-03-webapp): migrate WebApp Launcher overlay + skill-replay + teach-popover to canonical tokens`.
  </action>

  <acceptance_criteria>
    - 11 webapp-family + skill-replay + teach-popover files token-migrated.
    - Behavioral-guard regex PASS.
    - Phase 99/110 VNC-stream handlers byte-identical.
    - Build PASS. Sacred SHA preserved.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    WebApp Launcher migrated. Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate per-app content dialogs (modules/window/app-contents/*.tsx) + SUMMARY</name>
  <files>livos/packages/ui/src/modules/window/app-contents/**/*.tsx, .planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md</files>

  <read_first>
    Read each per-app content file (25 files). These are the inner surfaces of each app window — AI Chat content, App Store content, Chrome content, Docker content (75 docker route files use this as the host), Files content, My Devices, Remote Desktop, Schedules, Server Control, Settings content, Subagents content, Terminal content. Each one renders an app's main UI inside a window frame.
  </read_first>

  <action>
    1. **Token + ui-kit migration** (Plan 121-01 Task 1 pattern) for the 25 per-app content files.

    2. **ui-kit swap candidates** (apply where prop API matches):
       - `<Button>` for primary CTAs (terminal "Run", server-control "Restart", docker "Start container")
       - `<Card>` for content section shells (app-store-content's app-detail card)
       - `<Pill>` for status badges (running/stopped/connected/disconnected/etc.)
       - `<Input>` for search boxes (subagents-content, schedules-content)

    3. **Per-content-file caveat — ai-chat-content.tsx + app-store-content.tsx are Phase 120 territory:**
       - These were already touched in Plan 120-04 (AI Chat) and 120-05 (App Store). Check `git log -p -- livos/packages/ui/src/modules/window/app-contents/ai-chat-content.tsx livos/packages/ui/src/modules/window/app-contents/app-store-content.tsx` to see if Phase 120 already migrated their tokens.
       - If they're already on canonical tokens, audit-only NOOP and document in SUMMARY. If not, restyle now.

    4. Behavioral-guard regex per commit. Sacred SHA pre-flight.

    5. Commit per logical group of content files (e.g. group "system" = settings/terminal/server-control/factory-reset; group "media" = chrome/remote-desktop; group "tools" = subagents/schedules/my-devices/docker; group "phase120-audit" = ai-chat + app-store + files). 3-4 commits.

    6. **Author 121-03-SUMMARY.md** (≥60 lines):
       - Header
       - Plans-shipped table: all 121-03 commits (chrome, webapp, app-contents groups) with hashes
       - Per-sub-area tally: chrome (4) + webapp (11) + app-contents (25) = 40 files total (38 in modules/window root + 25 in app-contents minus overlaps; reconcile actual count in SUMMARY)
       - Phase 120 audit results (ai-chat-content + app-store-content + files-content — already canonical or needs migration?)
       - ui-kit import counts per group
       - Behavioral-guard PASS evidence per commit
       - Sacred SHA preserved N/N
       - **Window-chrome operator UAT checklist (mandatory, extra-detailed):**
         - Drag a window — position updates smoothly, no jank
         - Resize a window from each corner / each edge — all 8 handles work
         - Minimize → restore — preserved size/position
         - Maximize / un-maximize — fills viewport, returns to prior size
         - Close — window removed, no orphan DOM
         - Click on window — gains focus, z-index jumps to top
         - Snap to left/right/top edge — snap zones trigger correctly
       - **WebApp Launcher operator UAT checklist:**
         - Right-click desktop → "Open as Chrome window" → stream renders
         - Mode selector Watch/Teach/Auto/Chat — all four modes click + activate
         - Floating action bar drag + reposition works
         - Skills sidebar opens + closes
         - Teach drawer captures + replay works
         - Skill replay scrubber tracks playback position
         - Light/dark/iridescent — chrome AND webapp surfaces update theme
       - **Per-app content UAT:** Open each app from dock, verify inner content renders with canonical tokens.
       - Deviations + carry-overs (mark TODO 121-05 for shadcn audit, 121-06 for cross-surface diff)

    7. Commit: `docs(121-03): SUMMARY — window chrome + WebApp Launcher + app-contents migrated`.
  </action>

  <acceptance_criteria>
    - All 25 app-contents files token-migrated (or audited as already canonical).
    - ui-kit import count across modules/window total ≥ 10.
    - SUMMARY ≥ 60 lines, window-chrome + WebApp checklists complete.
    - Build PASS. Sacred SHA preserved across ALL 121-03 commits.
  </acceptance_criteria>

  <verify>
    <automated>
      cd livos/packages/ui && pnpm --filter ui build 2>&1 | tail -20 && \
      grep -rE "bg-zinc-(50|100|800|900)\b" livos/packages/ui/src/modules/window/ | wc -l && \
      grep -rE "bg-(blue|green|amber|red)-(500|600)\b" livos/packages/ui/src/modules/window/ | wc -l && \
      grep -rE "from ['\"]@livinity/ui-kit['\"]" livos/packages/ui/src/modules/window/ | wc -l && \
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts && \
      git log --oneline -8
    </automated>
  </verify>

  <done>
    Window chrome + WebApp Launcher + per-app contents all migrated. Build PASS. SUMMARY complete. Sacred SHA verified. Plan 121-03 closed.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter ui build` PASS after every commit.
- Zinc/blue/green/amber/red bg literal counts in `modules/window/` = 0 (or any survivor is a non-bg utility, justified).
- Behavioral-guard regex (extended with window-chrome handlers) PASS on every diff.
- Sacred SHA = `f3538e1d811992b782a9bb057d1b7f0a0189f95f` after every commit.
- `livos/packages/livinityd/` + `liv/` untouched.
</verification>

<success_criteria>
- ~38 tsx files in modules/window + 25 in app-contents migrated across 5-7 atomic commits.
- ui-kit import count ≥ 10 across modules/window.
- Window chrome handlers (drag/resize/focus/snap) untouched (regex PASS).
- WebApp Launcher Phase 99/110 surface handlers untouched (regex PASS).
- Sacred SHA preserved every commit.
- 121-03-SUMMARY.md committed with extra-detailed window-chrome + WebApp Launcher UAT checklists.
- D-121-NO-FUNCTIONAL-CHANGES, D-121-MINI-PC-OPERATOR-PRIORITY, D-V35-SACRED-SHA — all honored.
</success_criteria>

<output>
After completion, create `.planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md` per Task 3 step 6 spec.
</output>
