---
phase: 121-mini-pc-long-tail-and-audit
plan: 06
type: execute
wave: 5
depends_on:
  - 121-05
files_modified:
  - .planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md
  - livos/packages/ui-kit/playwright/playwright.config.ts
  - livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts
  - livos/packages/ui-kit/playwright/tests/storybook.spec.ts
  - livos/packages/ui-kit/playwright/__snapshots__/**
  - livos/packages/ui-kit/package.json
  - .github/workflows/visual-regression.yml
  - livos/packages/design-tokens/STYLE-GUIDE.md
  - .planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md
autonomous: true
requirements:
  - V35-AC1
  - V35-AC2
  - V35-AC3
  - V35-AC6
  - V35-AC7
  - V35-AC8
user_setup: []

must_haves:
  truths:
    - "Three concrete v35.0 close-out deliverables shipped: (a) CONSISTENCY-REPORT.md with side-by-side headless Chrome captures of Button/Card/Stepper/Modal/CommandBox/Pill/NavBar/ThemeToggle across Mini PC + Server5 + landing; per-primitive consistency score 0-100% (b) Playwright visual-regression suite at livos/packages/ui-kit/playwright/ + GitHub Actions workflow (c) STYLE-GUIDE.md expanded from skeleton to 5-section developer guide"
    - "Playwright added as new dependency to livos/packages/ui-kit with version locked + rationale documented in SUMMARY per D-121-PLAYWRIGHT-IS-NEW-DEPENDENCY"
    - "STYLE-GUIDE.md lives in livos/packages/design-tokens/ per D-121-STYLE-GUIDE-LIVES-WITH-TOKENS"
    - "121-06-SUMMARY.md documents v35.0 acceptance criteria AC#1-8 close-out via cross-reference table"
    - "`pnpm --filter ui-kit build` PASS after Playwright is added"
    - "Sacred SHA f3538e1d preserved every commit"
  artifacts:
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md"
      provides: "Headless Chrome side-by-side captures of 8 canonical primitives across 3 surfaces; per-primitive consistency score; documented residual diffs + fixes applied"
      min_lines: 80
    - path: "livos/packages/ui-kit/playwright/playwright.config.ts"
      provides: "Playwright config with chromium project + 0.5% snapshot threshold"
      contains: "maxDiffPixelRatio"
    - path: "livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts"
      provides: "Playwright spec visiting livinity.io/{login,register,store,dashboard/install} + dashboard.html"
      contains: "test("
    - path: "livos/packages/ui-kit/playwright/tests/storybook.spec.ts"
      provides: "Playwright spec visiting Storybook static + screenshotting 11 stories × 3 themes (33 snapshots)"
      contains: "test("
    - path: "livos/packages/ui-kit/playwright/__snapshots__"
      provides: "Seed PNG snapshots from first headless run committed to git"
      contains: ""
    - path: ".github/workflows/visual-regression.yml"
      provides: "GitHub Actions workflow running Playwright on every PR"
      contains: "playwright"
    - path: "livos/packages/design-tokens/STYLE-GUIDE.md"
      provides: "5-section developer style guide: How to add a component / Token rules / PR checklist / Cross-surface matrix / Migration recipe"
      min_lines: 150
    - path: ".planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md"
      provides: "v35.0 AC#1-8 close-out cross-reference table + Playwright dependency rationale + 121-06 deliverable tally"
      min_lines: 100
  key_links:
    - from: "livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts"
      to: "https://livinity.io/login + https://livinity.io/register + https://livinity.io/store + https://livinity.io/dashboard/install + /opt/landing/livinity.io/dashboard.html"
      via: "page.goto() + page.screenshot() with toMatchSnapshot()"
      pattern: "page.goto"
    - from: "livos/packages/ui-kit/playwright/tests/storybook.spec.ts"
      to: "livos/packages/ui-kit/storybook-static/"
      via: "Storybook static-build pages screenshotted per story per theme"
      pattern: "page.goto.*storybook-static"
    - from: ".github/workflows/visual-regression.yml"
      to: "livos/packages/ui-kit/playwright/"
      via: "GitHub Actions workflow runs Playwright on PR"
      pattern: "playwright test"
---

<objective>
Ship the 3 v35.0 close-out deliverables for Phase 121 / milestone close:

1. **CONSISTENCY-REPORT.md** — Headless Chrome captures + side-by-side rendering of 8 canonical primitives across Mini PC livinityd dashboard + Server5 platform + landing static HTML. Per-primitive consistency score 0-100%. Document residual diffs + apply fixes.
2. **Playwright visual-regression suite** at `livos/packages/ui-kit/playwright/` — config, 2 test specs (canonical-pages + storybook), seed snapshots, GitHub Actions workflow.
3. **STYLE-GUIDE.md** at `livos/packages/design-tokens/STYLE-GUIDE.md` — expanded from skeleton to 5-section developer guide.

Plus a 121-06-SUMMARY.md that documents the v35.0 AC#1-8 close-out via cross-reference table.

Purpose: Closes the v35.0 milestone. Without these 3 deliverables, AC#3 (visual parity), AC#6 (visual regression CI), and AC#7 (inventory accuracy) cannot be ticked. STYLE-GUIDE.md ensures the design system is maintainable by any future developer.

Output:
- `.planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md` (≥80 lines)
- `livos/packages/ui-kit/playwright/` directory with config + 2 specs + seed snapshots
- `.github/workflows/visual-regression.yml`
- `livos/packages/design-tokens/STYLE-GUIDE.md` (≥150 lines)
- `livos/packages/ui-kit/package.json` updated with `@playwright/test` as devDependency + version locked
- `.planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md` with v35.0 AC#1-8 close-out table
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-CONTEXT.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-01-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-02-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-03-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-04-SUMMARY.md
@.planning/phases/121-mini-pc-long-tail-and-audit/121-05-SUMMARY.md
@livos/packages/ui-kit/src/index.ts
@livos/packages/design-tokens/STYLE-GUIDE.md

<interfaces>
<!-- Playwright API reference -->
<!-- - import { test, expect } from '@playwright/test' -->
<!-- - page.goto(url) -->
<!-- - page.screenshot({ path, fullPage: true }) -->
<!-- - await expect(page).toHaveScreenshot('name.png', { maxDiffPixelRatio: 0.005 }) -->
<!-- - Snapshot dir: configured via testDir + project.snapshotDir -->

<!-- Surfaces being screenshotted -->
<!-- Mini PC: https://bruce.livinity.io (auth required — for v35 close use anon-readable dashboard.html capture as proxy) -->
<!-- Server5: https://livinity.io/{login,register,store,dashboard/install} (public) -->
<!-- Landing: file:///opt/landing/livinity.io/dashboard.html (read locally via http-server during CI) -->

<!-- Storybook static -->
<!-- livos/packages/ui-kit/storybook-static/ is built by `pnpm --filter ui-kit storybook:build` -->
<!-- Phase 119 produced 11 stories: Button, Card, Pill, Input, PasswordInput, Stepper, CommandBox, Modal, Toast, NavBar, ThemeToggle -->
<!-- Each rendered under light/dark/iridescent body class → 11 × 3 = 33 snapshots -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CONSISTENCY-REPORT.md (cross-surface audit + headless Chrome captures)</name>
  <files>.planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md, .planning/phases/121-mini-pc-long-tail-and-audit/captures/**</files>

  <read_first>
    Identify the 8 canonical primitives + each surface's representative element:
    - Button: Mini PC = AI Chat Send / Server5 = login Submit / landing = dashboard.html `.h-btn.solid`
    - Card: Mini PC = settings panel card / Server5 = dashboard install card / landing = `.b-card`
    - Stepper: Mini PC = local-setup wizard / Server5 = dashboard/install wizard / landing = `.stepper`
    - Modal: Mini PC = upload-confirm dialog / Server5 = register-success / landing = (likely no native modal — note)
    - CommandBox: Mini PC = local-setup command display / Server5 = install wizard CLI / landing = `.cmd-box`
    - Pill: Mini PC = app-store status / Server5 = dashboard install status / landing = `.pill.ok`
    - NavBar: Mini PC = top chrome (dock proxy) / Server5 = top nav / landing = landing header
    - ThemeToggle: Mini PC = settings or top chrome / Server5 = top-right / landing = top-right
  </read_first>

  <action>
    1. **Capture headless Chrome screenshots** (use chrome-devtools-mcp on port 9223 OR a one-off `npx playwright open --browser=chromium <url> --save-screenshot`):

       Per primitive × per surface = 8 × 3 = 24 captures. Plus light/dark/iridescent on at least 3 primitives = 33 total captures.

       Store under `.planning/phases/121-mini-pc-long-tail-and-audit/captures/<primitive>/<surface>-<theme>.png`.

       For Mini PC: dashboard.html is the canonical reference. Auth-walled Mini PC surfaces can be captured manually by operator OR proxied via Storybook stories (which now render with canonical tokens — they ARE Mini PC primitives).

    2. **Author CONSISTENCY-REPORT.md** (≥80 lines):

       Structure:
       ```
       # v35.0 Cross-Surface Consistency Report

       ## Methodology
       Headless Chrome captures of <primitives> across <surfaces>; per-primitive parity score 0-100%.

       ## Per-primitive score table

       | Primitive | Mini PC | Server5 | Landing | Score | Notes |
       |---|---|---|---|---|---|
       | Button (light) | [img] | [img] | [img] | 95% | Geist weight matches; Server5 border-radius 6px vs landing 8px — fix needed |
       | Button (dark) | [img] | [img] | [img] | 90% | accent-blue tone diff: Server5 0.95 sat vs landing 1.0 sat |
       | Card | [img] | [img] | [img] | 100% | All using --card-bg + --dash-radius |
       | Stepper | [img] | [img] | [img] | 85% | Step-indicator dot size diff |
       | ... |

       ## Residual diffs identified
       (List each <100% primitive with the cause)

       ## Fixes applied (or deferred)
       (Per-diff: action taken — class patch, token tweak, or accepted-as-out-of-tolerance with rationale)

       ## v35.0 AC#3 (cross-surface visual parity) verdict
       PASS / partial / fail with score average.
       ```

    3. **For any residual diff at <90%:** patch the offending surface in this commit if the fix is local (e.g., Mini PC token swap). Tokens.css / fonts.css patches go through `livos/packages/design-tokens/`. Server5 / landing fixes are documented as carry-overs (Server5 is not in this git repo per D-V35-SERVER5-IN-TREE-PATCH-LOG).

    4. Commit: `docs(121-06): CONSISTENCY-REPORT.md — cross-surface audit + captures + score table`.
  </action>

  <acceptance_criteria>
    - CONSISTENCY-REPORT.md ≥ 80 lines
    - ≥ 24 capture PNGs committed under captures/
    - Per-primitive score table present (8 primitives × at least light theme)
    - Residual diffs documented + each tagged "fixed in this plan" or "deferred to <X>" or "accepted as out-of-tolerance"
  </acceptance_criteria>

  <verify>
    <automated>
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md && \
      find .planning/phases/121-mini-pc-long-tail-and-audit/captures -name "*.png" 2>/dev/null | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    CONSISTENCY-REPORT.md committed. Captures committed. Score table populated. Residual diffs handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright dependency + scaffold playwright/ directory + author 2 test specs + seed snapshots</name>
  <files>livos/packages/ui-kit/package.json, livos/packages/ui-kit/playwright/playwright.config.ts, livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts, livos/packages/ui-kit/playwright/tests/storybook.spec.ts, livos/packages/ui-kit/playwright/__snapshots__/.gitkeep</files>

  <read_first>
    Read `livos/packages/ui-kit/package.json` to confirm current devDependency structure + workspace conventions.
  </read_first>

  <action>
    1. **Add Playwright as devDependency** to `livos/packages/ui-kit/package.json`. Lock to a specific version (latest stable as of 2026-05 → `@playwright/test@^1.50.0` — pick a version that's at least 6 months old + actively maintained). Document the rationale in SUMMARY per D-121-PLAYWRIGHT-IS-NEW-DEPENDENCY:
       - Why Playwright (vs Cypress, Chromatic, Loki): cross-browser, snapshot-native, free, GitHub-Actions friendly, project already uses chrome-devtools-mcp so Chromium tooling is familiar
       - Why this version: stable + LTS-window
       - Audit per D-V35-NO-NEW-DEPENDENCIES-WITHOUT-AUDIT: official Microsoft package, widely adopted, MIT licensed.

       Run `pnpm install` if executor environment supports it. Otherwise note in SUMMARY that operator must `cd livos/packages/ui-kit && pnpm install` before running the Playwright tests for the first time.

    2. **Create `livos/packages/ui-kit/playwright/playwright.config.ts`:**
       ```typescript
       import { defineConfig, devices } from '@playwright/test'

       export default defineConfig({
         testDir: './tests',
         snapshotDir: './__snapshots__',
         fullyParallel: true,
         retries: process.env.CI ? 2 : 0,
         reporter: [['html', { open: 'never' }], ['list']],
         expect: {
           toHaveScreenshot: { maxDiffPixelRatio: 0.005 }, // 0.5% threshold per planning_context
         },
         projects: [
           { name: 'chromium', use: { ...devices['Desktop Chrome'], viewport: { width: 1280, height: 800 } } },
         ],
       })
       ```

    3. **Create `livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts`:**
       - test('login page snapshot light', visits `https://livinity.io/login`, screenshots, toHaveScreenshot)
       - test('register page snapshot light', visits `https://livinity.io/register`)
       - test('store page snapshot light', visits `https://livinity.io/store`)
       - test('dashboard install snapshot light', visits `https://livinity.io/dashboard/install`)
       - test('dashboard.html snapshot light', visits a CI-served copy of `/opt/landing/livinity.io/dashboard.html` — see CI section)
       - Each test also runs dark + iridescent themes via `localStorage.setItem('liv_theme', '<theme>')` then reload before screenshot

    4. **Create `livos/packages/ui-kit/playwright/tests/storybook.spec.ts`:**
       - Pre-step: serve `livos/packages/ui-kit/storybook-static/` via `npx http-server` on a fixed port (e.g. 6006). Configure in `playwright.config.ts` `webServer` field OR as a global setup.
       - For each of the 11 Phase 119 stories (Button, Card, Pill, Input, PasswordInput, Stepper, CommandBox, Modal, Toast, NavBar, ThemeToggle):
         - For each of 3 themes (light, dark, iridescent):
           - test('<story> <theme>', goto `http://localhost:6006/iframe.html?id=<story-id>`, set body class to theme, screenshot, toHaveScreenshot)
       - Total: 33 snapshots

    5. **Seed snapshots:** Run `pnpm --filter ui-kit playwright test --update-snapshots` (or manually generate from the first headless pass) to commit the seed PNGs under `__snapshots__/`.

       If executor can't run Playwright in CI environment, leave `__snapshots__/.gitkeep` + note in SUMMARY that operator must seed snapshots locally with `cd livos/packages/ui-kit && pnpm playwright install chromium && pnpm playwright test --update-snapshots` then commit.

    6. **Update `livos/packages/ui-kit/package.json` scripts:**
       ```json
       "scripts": {
         "playwright:test": "playwright test",
         "playwright:update": "playwright test --update-snapshots",
         "playwright:report": "playwright show-report"
       }
       ```

    7. Sacred SHA pre-flight.

    8. Commit: `feat(121-06-playwright): add Playwright dep + visual regression suite + seed snapshots`.
  </action>

  <acceptance_criteria>
    - `@playwright/test` in `livos/packages/ui-kit/package.json` devDependencies with locked version
    - `playwright.config.ts` present with 0.5% threshold
    - `tests/canonical-pages.spec.ts` covers 5 canonical pages × 3 themes (15 tests minimum)
    - `tests/storybook.spec.ts` covers 11 stories × 3 themes (33 tests)
    - `__snapshots__/` directory exists (with seeds OR .gitkeep + operator instructions)
    - `pnpm --filter ui-kit build` PASS (verifying package.json + tsconfig is valid)
  </acceptance_criteria>

  <verify>
    <automated>
      grep "@playwright/test" livos/packages/ui-kit/package.json && \
      test -f livos/packages/ui-kit/playwright/playwright.config.ts && \
      test -f livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts && \
      test -f livos/packages/ui-kit/playwright/tests/storybook.spec.ts && \
      cd livos/packages/ui-kit && pnpm --filter ui-kit build 2>&1 | tail -10 && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Playwright suite scaffolded + seed snapshots present (or operator instruction for seed-on-first-run). Build PASS. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author .github/workflows/visual-regression.yml</name>
  <files>.github/workflows/visual-regression.yml</files>

  <action>
    Create GitHub Actions workflow that runs Playwright on every PR:

    ```yaml
    name: Visual Regression
    on:
      pull_request:
        paths:
          - 'livos/packages/ui-kit/**'
          - 'livos/packages/design-tokens/**'
          - 'livos/packages/ui/**'
          - '.github/workflows/visual-regression.yml'

    jobs:
      visual:
        runs-on: ubuntu-latest
        timeout-minutes: 30
        steps:
          - uses: actions/checkout@v4
          - uses: pnpm/action-setup@v3
            with: { version: 9 }
          - uses: actions/setup-node@v4
            with: { node-version: '20', cache: 'pnpm' }
          - run: pnpm install --frozen-lockfile
          - run: pnpm --filter ui-kit build
          - run: pnpm --filter ui-kit storybook:build
          - run: cd livos/packages/ui-kit && pnpm playwright install --with-deps chromium
          - run: cd livos/packages/ui-kit && pnpm playwright test
          - uses: actions/upload-artifact@v4
            if: failure()
            with:
              name: playwright-report
              path: livos/packages/ui-kit/playwright-report/
              retention-days: 14
          - uses: actions/upload-artifact@v4
            if: failure()
            with:
              name: playwright-diff-snapshots
              path: livos/packages/ui-kit/test-results/
              retention-days: 14
    ```

    Notes:
    - `paths:` filter so the workflow only triggers on UI changes
    - On failure, upload report + diff snapshots so operator can review
    - `--frozen-lockfile` enforces deterministic deps
    - Cross-surface tests (canonical-pages.spec.ts) hit live Server5 — for CI we either (a) skip those when CI=true OR (b) accept they fail-soft. Default: skip with `test.skip(!!process.env.CI, 'canonical-pages requires live Server5')` and only run storybook.spec.ts in CI. Document this in SUMMARY.

    Sacred SHA pre-flight. Commit: `feat(ci-121-06): visual-regression GitHub Actions workflow`.
  </action>

  <acceptance_criteria>
    - `.github/workflows/visual-regression.yml` valid YAML
    - Triggers on PR with path filter
    - Uses pinned actions versions
    - Uploads report + diffs on failure
  </acceptance_criteria>

  <verify>
    <automated>
      test -f .github/workflows/visual-regression.yml && \
      cat .github/workflows/visual-regression.yml | grep -E "^name:|playwright|pnpm" | head -10 && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    Workflow shipped. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Expand STYLE-GUIDE.md from skeleton to 5-section developer guide</name>
  <files>livos/packages/design-tokens/STYLE-GUIDE.md</files>

  <read_first>
    Read the existing skeleton at `livos/packages/design-tokens/STYLE-GUIDE.md` (Phase 116 output).
  </read_first>

  <action>
    Expand to ≥150 lines with 5 sections:

    1. **How to add a new component to LivOS UI**
       - Always start with `@livinity/ui-kit` — check if your component already exists
       - If a similar primitive exists but lacks your variant → propose adding a `variant=` prop to ui-kit (PR against ui-kit, not the consumer)
       - If no equivalent exists → propose a new ui-kit component (with Storybook story + Vitest test)
       - Only fork into the consumer package if the component is truly consumer-specific (e.g., a Mini-PC-only `<DockIcon>` that has no analogue elsewhere)
       - Component must work in light + dark + iridescent themes from day 1 per D-V35-LIGHT-DARK-IRIDESCENT-PARITY

    2. **Token rules**
       - Use canonical Tailwind classes from the design-tokens preset: `bg-card-bg`, `bg-accent-blue`, `rounded-dash`, `p-dash`, `text-card-fg`, `border-dash-line`, etc.
       - NEVER use raw `bg-zinc-*`, `bg-blue-600`, `rounded-2xl` (as card radius), `p-7` (as card padding)
       - Color tokens map to CSS vars in `tokens.css` — modify there if you need to add a new color
       - Typography: `font-sans` = Geist, `font-mono` = Geist Mono, `font-serif` = Instrument Serif (italic accent)
       - Motion: `transition-dash` = 0.18s ease (canonical)

    3. **PR checklist**
       - [ ] Used `@livinity/ui-kit` primitives where applicable
       - [ ] No raw `bg-zinc-*` / `bg-blue-600` / `rounded-2xl` (card-shell) / `p-7` (card-shell) literals
       - [ ] Component works in light / dark / iridescent themes
       - [ ] Storybook story added (for ui-kit additions)
       - [ ] Visual regression Playwright snapshot updated (`pnpm playwright test --update-snapshots`)
       - [ ] Behavioral handlers (onClick / onChange / onSubmit / useEffect) preserved byte-for-byte if migrating existing code
       - [ ] Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved (verify with `git hash-object liv/packages/core/src/sdk-agent-runner.ts`)

    4. **Cross-surface matrix** (which surface uses which primitive)

       | Primitive | Mini PC | Server5 (Next.js) | Landing (HTML/UMD) |
       |---|---|---|---|
       | Button | `@livinity/ui-kit` Button | `@livinity/ui-kit` Button (ESM) | `window.LivKit.Button` (UMD) |
       | Card | ui-kit Card | ui-kit Card | window.LivKit.Card |
       | Stepper | ui-kit Stepper | ui-kit Stepper | window.LivKit.Stepper |
       | ... (all 11 components) |

       Plus: which Phase added it (119, 120, 121-05 audit redirect, etc.)

    5. **Migration recipe** (how to migrate a legacy component to canonical tokens + ui-kit)

       1. Read the existing component end-to-end
       2. Identify zinc/blue literal classes
       3. Apply token map (table from D-121-NO-FUNCTIONAL-CHANGES — `bg-zinc-50 → bg-card-bg`, etc.)
       4. Identify ui-kit-swap candidates by prop API match
       5. Run behavioral-guard regex diff: any onClick/onChange/onSubmit/useEffect diff = REVERT
       6. Sacred SHA pre-flight
       7. Atomic commit per logical chunk
       8. `pnpm --filter ui build` PASS
       9. Operator UAT walk (Mini PC `bash /opt/livos/update.sh` + browse)
       10. Document deviations in SUMMARY

       Worked example: take one file from Phase 121-01 (e.g., `features/backups/backups-list.tsx`) and walk through the recipe step-by-step with code blocks showing before/after.

    Sacred SHA pre-flight. Commit: `docs(121-06): STYLE-GUIDE.md — expand to 5-section developer guide`.
  </action>

  <acceptance_criteria>
    - STYLE-GUIDE.md ≥ 150 lines
    - All 5 sections present + headed correctly
    - PR checklist has ≥ 7 items
    - Cross-surface matrix has all 11 ui-kit primitives
    - Migration recipe includes a worked example with before/after code
  </acceptance_criteria>

  <verify>
    <automated>
      wc -l livos/packages/design-tokens/STYLE-GUIDE.md && \
      grep -E "^## (How to add|Token rules|PR checklist|Cross-surface matrix|Migration recipe)" livos/packages/design-tokens/STYLE-GUIDE.md && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts
    </automated>
  </verify>

  <done>
    STYLE-GUIDE.md expanded + committed. Sacred SHA verified.
  </done>
</task>

<task type="auto">
  <name>Task 5: Author 121-06-SUMMARY.md (v35.0 milestone close-out)</name>
  <files>.planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md</files>

  <action>
    Author 121-06-SUMMARY.md (≥100 lines — this is the v35.0 close-out):

    Structure:
    - Header: phase / plan / wave / status / date
    - **Plans-shipped table:** all 121-06 commits
    - **3 deliverables tally:**
      - (a) CONSISTENCY-REPORT.md — N captures, M-primitive score table, average score X%
      - (b) Playwright suite — N test files, M tests, dependency version locked
      - (c) STYLE-GUIDE.md — 5 sections, K lines
    - **Playwright dependency rationale (D-121-PLAYWRIGHT-IS-NEW-DEPENDENCY):**
      - Version locked: `@playwright/test@^X.Y.Z`
      - Why this tool: cross-browser, snapshot-native, MIT, GitHub-Actions friendly
      - Audit pass: Microsoft official package, widely adopted
    - **v35.0 Acceptance Criteria close-out cross-reference table:**

      | AC# | Description | Closed by | Status |
      |---|---|---|---|
      | AC#1 | Single design token source | Phase 116 + consumers in 117-121 | PASS |
      | AC#2 | Single component library (ui-kit default; audit confirms no hand-rolled duplicates) | Phase 119 + 121-05 SHADCN-AUDIT.md | PASS |
      | AC#3 | Cross-surface visual parity (CONSISTENCY-REPORT) | 121-06 Task 1 | PASS / partial-with-deferrals |
      | AC#4 | Geist + Instrument Serif everywhere | Phase 116 + 117-121 token consumers | PASS |
      | AC#5 | Light/dark/iridescent everywhere | Phase 116 + every consumer | PASS |
      | AC#6 | Visual regression CI (Playwright snapshots on PR) | 121-06 Task 2 + Task 3 | PASS |
      | AC#7 | Inventory accuracy (Phase 115 INVENTORY-*.md re-affirmed post-migration) | 121-05 SHADCN-AUDIT + 121-06 SUMMARY re-run | PASS |
      | AC#8 | Operator-walked UAT (final end-to-end) | 121-04 checkpoints + post-121-06 walk | PENDING operator |

    - **Sacred SHA:** preserved across all 121-06 commits (verified hash) — and across all of Phase 121 (count 121-01..06 commits) — and across all of v35 milestone (Phase 115-121)
    - **Operator post-121-06 final UAT block:** the comprehensive end-to-end walk closing AC#8
      ```
      1. ssh -i ... bruce@10.69.31.68
      2. bash /opt/livos/update.sh
      3. Browse: livinity.io/login → livinity.io/register → livinity.io/dashboard → livinity.io/dashboard/install → livinity.io/store → bruce.livinity.io (Mini PC) → cycle every theme on every surface
      4. Verify: cannot tell where one surface ends and the next begins — same Geist font, same card-bg color, same dash-radius, same accent-blue, same stepper, same pill
      5. Sign off as "v35.0 UAT approved" → close milestone
      ```
    - **Deferred to v36+ (per master plan out-of-scope list):**
      - Mobile design
      - Email templates
      - Storybook → Figma sync
      - Marketplace + changelog services visual unify
      - `apps.livinity.io` dark/iridescent
    - **Carry-overs:**
      - ui-kit v0.2.0 candidate set from 121-05 SHADCN-AUDIT.md → schedule as Phase 122
      - Any deferred fixes from 121-06 CONSISTENCY-REPORT.md residual diffs
    - **121-06 verdict:** PASS — v35.0 code-complete pending AC#8 operator UAT.

    Commit: `docs(121-06): SUMMARY — v35.0 milestone close-out + AC#1-8 cross-reference`.
  </action>

  <acceptance_criteria>
    - SUMMARY ≥ 100 lines
    - AC#1-8 cross-reference table complete
    - Playwright rationale documented
    - Sacred SHA verification across full phase + milestone
    - Operator post-121-06 UAT instructions present
  </acceptance_criteria>

  <verify>
    <automated>
      wc -l .planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md && \
      grep -E "^\| AC#[1-8]" .planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md | wc -l && \
      git hash-object liv/packages/core/src/sdk-agent-runner.ts && \
      git log --oneline -10
    </automated>
  </verify>

  <done>
    121-06-SUMMARY.md committed. v35.0 close-out documented. Sacred SHA verified. Plan 121-06 closed. v35.0 code-complete (pending AC#8 operator UAT).
  </done>
</task>

</tasks>

<verification>
- CONSISTENCY-REPORT.md ≥ 80 lines with 8-primitive × 3-surface score table.
- Playwright suite scaffolded: config + 2 specs + snapshot seed (or operator-seed instruction).
- `livos/packages/ui-kit/package.json` has `@playwright/test` devDependency with locked version.
- `.github/workflows/visual-regression.yml` valid + path-filtered.
- STYLE-GUIDE.md ≥ 150 lines with all 5 sections.
- 121-06-SUMMARY.md ≥ 100 lines with AC#1-8 close-out table.
- Sacred SHA preserved every commit.
- `livos/packages/livinityd/` + `liv/` untouched (except design-tokens + ui-kit + ui scoped paths).
</verification>

<success_criteria>
- 3 v35.0 close-out deliverables shipped + committed:
  1. CONSISTENCY-REPORT.md with cross-surface score
  2. Playwright visual-regression suite + GitHub Actions workflow
  3. STYLE-GUIDE.md (5 sections)
- 121-06-SUMMARY.md with v35.0 AC#1-8 cross-reference table
- AC#1-7 documented PASS (AC#8 pending operator UAT walk post-121-06)
- D-121-PLAYWRIGHT-IS-NEW-DEPENDENCY honored (version locked + rationale)
- D-121-STYLE-GUIDE-LIVES-WITH-TOKENS honored (STYLE-GUIDE.md path = `livos/packages/design-tokens/STYLE-GUIDE.md`)
- Sacred SHA preserved every commit
- v35.0 code-complete
</success_criteria>

<output>
After completion, create:
- `.planning/phases/121-mini-pc-long-tail-and-audit/CONSISTENCY-REPORT.md`
- `livos/packages/ui-kit/playwright/playwright.config.ts`
- `livos/packages/ui-kit/playwright/tests/canonical-pages.spec.ts`
- `livos/packages/ui-kit/playwright/tests/storybook.spec.ts`
- `livos/packages/ui-kit/playwright/__snapshots__/.gitkeep` (or seeded PNGs)
- Update `livos/packages/ui-kit/package.json`
- `.github/workflows/visual-regression.yml`
- `livos/packages/design-tokens/STYLE-GUIDE.md` (expanded)
- `.planning/phases/121-mini-pc-long-tail-and-audit/121-06-SUMMARY.md`
</output>
