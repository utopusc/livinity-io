---
phase: 120-mini-pc-ui-migration-wave-1
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - livos/packages/ui/src/layouts/desktop.tsx
  - livos/packages/ui/src/modules/dock/dock.tsx
  - livos/packages/ui/src/components/apple-spotlight.tsx
  - livos/packages/ui/src/components/cmdk.tsx
  - livos/packages/ui/src/components/app-icon.tsx
  - livos/packages/ui/src/modules/window/window-content.tsx
  - livos/packages/ui/src/modules/window/windows-container.tsx
  - livos/packages/ui/src/routes/login/index.tsx
  - .planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md
autonomous: true
requirements:
  - D-120-NO-FUNCTIONAL-CHANGES
  - D-120-INCREMENTAL-DEPLOY
  - D-120-MINI-PC-OPERATOR-PRIORITY
  - D-120-SACRED-SHA
  - D-120-WAVE-1-IS-30-COMPONENTS
user_setup: []

must_haves:
  truths:
    - "Desktop layout uses canonical card-bg + dash-line tokens (visible by inspecting any background utility class)"
    - "Dock surface uses canonical tokens (background + border)"
    - "Spotlight + cmdk search palettes use canonical card-bg + accent-blue focus"
    - "App icon labels remain readable (no contrast regression vs pre-deploy screenshot)"
    - "Window content + windows-container chrome consume canonical tokens"
    - "Login screen uses canonical button + card (via `<Button>` / `<Card>` from ui-kit OR direct token utilities)"
    - "Every onClick/onSubmit/keyboard handler preserved verbatim (functional diff = zero)"
  artifacts:
    - path: "livos/packages/ui/src/layouts/desktop.tsx"
      provides: "Desktop root layout — restyled to canonical tokens"
    - path: "livos/packages/ui/src/modules/dock/dock.tsx"
      provides: "Dock — restyled to canonical tokens"
    - path: "livos/packages/ui/src/components/apple-spotlight.tsx"
      provides: "Apple-style spotlight — restyled"
    - path: "livos/packages/ui/src/components/cmdk.tsx"
      provides: "Command palette — restyled"
    - path: "livos/packages/ui/src/components/app-icon.tsx"
      provides: "App icon component — restyled"
    - path: "livos/packages/ui/src/modules/window/window-content.tsx"
      provides: "Window content wrapper — restyled"
    - path: "livos/packages/ui/src/modules/window/windows-container.tsx"
      provides: "Window orchestrator (root for window-manager surface) — restyled"
    - path: "livos/packages/ui/src/routes/login/index.tsx"
      provides: "Login screen — restyled with ui-kit primitives"
  key_links:
    - from: "any restyled file"
      to: "@livinity/ui-kit"
      via: "import { Button, Card, Pill } from '@livinity/ui-kit'"
      pattern: "from '@livinity/ui-kit'"
    - from: "any restyled file"
      to: "canonical Tailwind tokens"
      via: "className attributes"
      pattern: "(bg-accent-blue|bg-card-bg|rounded-dash|p-dash|text-accent-(green|red|amber))"
---

<objective>
Restyle the 8 highest-traffic Mini PC chrome/layout components to canonical design tokens + ui-kit primitives. Visual layer only — every prop, event handler, route binding, store subscription, and Framer Motion animation is preserved verbatim.

Targets (selected from `.planning/phases/115-ui-component-inventory/INVENTORY-MINI-PC.md` highest-traffic chrome):
1. `livos/packages/ui/src/layouts/desktop.tsx` — root desktop layout
2. `livos/packages/ui/src/modules/dock/dock.tsx` — bottom dock
3. `livos/packages/ui/src/components/apple-spotlight.tsx` — Apple-style spotlight overlay
4. `livos/packages/ui/src/components/cmdk.tsx` — command palette
5. `livos/packages/ui/src/components/app-icon.tsx` — app icon (used everywhere on desktop)
6. `livos/packages/ui/src/modules/window/window-content.tsx` — window body wrapper
7. `livos/packages/ui/src/modules/window/windows-container.tsx` — windows orchestrator
8. `livos/packages/ui/src/routes/login/index.tsx` — login screen

Purpose: After 120-01 wired the foundation, every consumer route loads tokens. This plan migrates the chrome that appears on EVERY route, so the rest of Wave 1 (Plans 03/04/05) inherit a token-consistent shell.

Output: 8 components restyled. `pnpm --filter ui build` PASS. Functional behavior unchanged. Operator UAT validates daily-driver Mini PC flows still work + visual style aligns with `livinity.io/dashboard`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-mini-pc-ui-migration-wave-1/120-CONTEXT.md
@.planning/phases/120-mini-pc-ui-migration-wave-1/120-01-SUMMARY.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/ui-kit/README.md

<interfaces>
Available ui-kit imports (already wired in Plan 120-01):
```ts
import {
  Button,       // variant: 'solid' | 'ghost' | 'danger'; size; loading
  Card,         // padding: 'default' | 'tight'; radius: 'default' | 'tight'
  Pill,         // tone: 'ok' | 'warn' | 'err' | 'neutral'
  Input,        // label, hint, error; auto-id via React.useId()
  PasswordInput,
  Modal,        // controlled: open, onClose, title; portal to body
  ThemeToggle,  // cycles light → dark → iridescent, persists to localStorage
  cn,           // clsx-style className merger
} from '@livinity/ui-kit'
```

Canonical Tailwind utilities (provided by `@livinity/design-tokens/tailwind.preset.cjs`, merged into `livos/packages/ui/tailwind.config.ts` via Plan 120-01):
- Colors: `bg-accent-blue`, `bg-accent-green`, `bg-accent-amber`, `bg-accent-red`, `text-accent-*`, `bg-card-bg`, `bg-card-bg-2`, `border-dash-line`, `border-dash-line-strong`
- Radius: `rounded-dash` (18px canonical card radius)
- Spacing: `p-dash`, `m-dash`, `gap-dash` (28px canonical card padding)
- Shadow: `shadow-card` (canonical two-stop card elevation)
- Motion: `duration-dash` (180ms canonical transition)

Token-to-existing-class mapping (Tailwind 3.4 utility form):
| Existing | Replace with |
|---|---|
| `bg-zinc-50` / `bg-white` (light surfaces) | `bg-card-bg-2` |
| `bg-zinc-900` (dark card surfaces) | `bg-card-bg` (dark mode override fires via body.dark) |
| `bg-blue-600` / primary action bg | `bg-accent-blue` |
| `text-green-600` / success | `text-accent-green` |
| `text-red-600` / destructive | `text-accent-red` |
| `text-amber-600` / warning | `text-accent-amber` |
| `rounded-2xl` (on cards) | `rounded-dash` |
| `p-7` (on cards) | `p-dash` |
| `border-zinc-200` / `border-black/10` | `border-dash-line` |
| `border-zinc-300` / `border-black/20` | `border-dash-line-strong` |
| `transition-all duration-200` / `duration-150` (on interactive chrome) | `transition-all duration-dash` |
| `shadow-md` / `shadow-lg` on cards | `shadow-card` |

Hero gradient (rare): `style={{background: 'var(--hero-grad)'}}` or `bg-[var(--hero-grad)]`.

**DO NOT** convert utility classes that are NOT in the mapping above. E.g., `flex`, `items-center`, `gap-2`, `text-sm`, layout utilities, animation utilities (`animate-pulse`), positioning (`absolute`, `top-4`) — leave verbatim. Only color/radius/padding/border/shadow that have a canonical replacement.

**DO NOT** replace `bg-zinc-*` / `bg-blue-*` etc. in route files NOT listed in this plan's `files_modified`. Scope is strictly the 8 components.

Functional-diff guard (executor must respect):
- Every `onClick=`, `onSubmit=`, `onChange=`, `onKeyDown=`, `onMouseEnter=`, `onPointerDown=` handler stays byte-identical.
- Every Framer Motion `initial=`, `animate=`, `exit=`, `transition=`, `variants=` prop stays byte-identical.
- Every `href=` / `to=` route binding stays byte-identical.
- Every zustand store call, tRPC hook call, i18n `t()` call stays byte-identical.
- Every Radix UI primitive (`@radix-ui/*`) wrap stays structurally identical (same primitive, same prop shape).
- Conditional rendering branches stay identical (`{isOpen && <X/>}`).
- React keys on map iterations stay identical.

If a component currently uses inline `style={{...}}` for colors/radii that have canonical token equivalents → convert to Tailwind utilities. If style block contains non-token values (gradients, transforms, complex calc) → leave verbatim.

If a component's existing shadcn primitive wraps something the ui-kit primitive can replace (e.g., a shadcn `<Button>` → `<Button>` from ui-kit) → ONLY swap if prop shape matches AND no consumer downstream relies on the shadcn-specific API. When in doubt, restyle the existing component in place rather than swap (lower risk per D-120-NO-FUNCTIONAL-CHANGES). The login screen (Task 4) is the SAFE place to introduce ui-kit `<Button>`/`<Input>`/`<Card>` since it's a fresh full-screen surface with no downstream consumer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle desktop layout + dock + spotlight + cmdk (4 files)</name>
  <files>
    livos/packages/ui/src/layouts/desktop.tsx
    livos/packages/ui/src/modules/dock/dock.tsx
    livos/packages/ui/src/components/apple-spotlight.tsx
    livos/packages/ui/src/components/cmdk.tsx
  </files>
  <action>
For each of the 4 files, apply the token-to-existing-class mapping table from `<interfaces>` to every matching className occurrence. Specifically:

1. **`livos/packages/ui/src/layouts/desktop.tsx`** — root desktop layout (wallpaper + dock + window stack root).
   - Read the file in full (single Read call).
   - Identify every className substring that matches a row in the token mapping table.
   - Replace each matching utility with its canonical equivalent.
   - DO NOT touch: position/flex/grid utilities, animation utilities, `<DndContext>` props, wallpaper rendering logic, drag-and-drop handlers.

2. **`livos/packages/ui/src/modules/dock/dock.tsx`** — bottom dock.
   - Background: dock currently uses a translucent surface (`bg-white/...` or similar). Replace base surface with `bg-card-bg/85` (preserves the translucency wash; canonical card-bg is `#ffffff` so `/85` mimics existing wash).
   - Border: replace `border-zinc-*` / `border-black/X` with `border-dash-line`.
   - Radius on dock container: if `rounded-2xl` → `rounded-dash`. If a non-standard radius like `rounded-3xl` or `rounded-[N]px` — LEAVE as-is (canonical only addresses 18px).
   - Padding on dock items: keep verbatim.
   - Preserve `framer-motion` animation props on dock-item hover.

3. **`livos/packages/ui/src/components/apple-spotlight.tsx`** — Apple-style spotlight overlay.
   - The shell (modal-ish overlay) consumes a card surface: use `bg-card-bg`, `border-dash-line`, `rounded-dash`, `shadow-card` on the container.
   - Focus ring on input: `focus-visible:ring-2 focus-visible:ring-accent-blue focus-visible:ring-offset-2` (consistent with DESIGN-SYSTEM.md focus-ring recommendation).
   - Preserve all `framer-motion` `AnimatePresence` wrapping + `motion.div` animation props.

4. **`livos/packages/ui/src/components/cmdk.tsx`** — command palette (uses shadcn primitives per inventory).
   - The cmdk uses `Command` from `cmdk` package (third-party). LEAVE its structural primitives intact (`<Command>`, `<Command.List>`, `<Command.Item>`).
   - ONLY touch className strings on the wrapping shell + on hover/selection states:
     * Wrapper: `bg-card-bg rounded-dash shadow-card border-dash-line`
     * Item hover/active: replace `bg-zinc-100` / `bg-blue-50` with `bg-card-bg-2` (subtle hover) or `bg-accent-blue/10` (selection accent).
   - Preserve every cmdk-provider registration call, every selection handler, every `<Command.Empty>` content.

Common rules across all 4 files:
- Use a single Read call per file (the file fits in one Read window).
- Use Edit (not Write) for each className substitution — preserves indentation and avoids whole-file rewrites.
- After all 4 files edited, run `pnpm --filter ui typecheck` (must PASS — pure className changes shouldn't introduce type errors).
- Sacred SHA check after each file: `git hash-object liv/packages/core/src/sdk-agent-runner.ts` MUST equal `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If the executor accidentally touches sdk-agent-runner.ts (extremely unlikely — different package) → STOP, revert, document.

Behavioral-diff guard: after editing each file, run a quick visual diff with `git diff --stat <file>` — the diff should ONLY touch className string contents (no `onClick=`, `useEffect`, import changes other than possibly adding `cn` from ui-kit if needed). If any non-className diff appears → STOP and review.
  </action>
  <verify>
    <automated>
cd livos &&
pnpm --filter ui typecheck 2>&1 | tail -10 &&
git diff --stat livos/packages/ui/src/layouts/desktop.tsx livos/packages/ui/src/modules/dock/dock.tsx livos/packages/ui/src/components/apple-spotlight.tsx livos/packages/ui/src/components/cmdk.tsx &&
[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] &&
echo "TASK_1_OK"
    </automated>
  </verify>
  <done>
- 4 files have canonical token classNames where mapping applies
- No onClick/handler/route/store-call drift
- `pnpm --filter ui typecheck` PASS
- Sacred SHA preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle app-icon + window-content + windows-container (3 files)</name>
  <files>
    livos/packages/ui/src/components/app-icon.tsx
    livos/packages/ui/src/modules/window/window-content.tsx
    livos/packages/ui/src/modules/window/windows-container.tsx
  </files>
  <action>
1. **`livos/packages/ui/src/components/app-icon.tsx`** — appears on every desktop view, file is ~small per inventory (uses Tailwind utility + 1 inline-style site).
   - The icon `<img>` itself: no token changes (image rendering).
   - The label below the icon: if it uses a color like `text-white` with a drop-shadow → leave (canonical doesn't redefine drop-shadow-desktop-label).
   - Hover state ring (if any `ring-zinc-*` or `ring-blue-*`): map to `ring-accent-blue/40` for blue ring or `ring-dash-line` for neutral.
   - Inline `style={{...}}` site: read it. If it's color-only → convert to className. If it computes a value (e.g., `transform: scale(${x})`) → leave verbatim.

2. **`livos/packages/ui/src/modules/window/window-content.tsx`** — wraps every open app window's body.
   - Background: `bg-card-bg` (light) with `dark:` variants left verbatim — body.dark fires the token override automatically.
   - Border: `border-dash-line` where existing has `border-black/10` or `border-zinc-200`.
   - Radius: if window content uses `rounded-2xl` → `rounded-dash`. Otherwise leave.
   - Padding: preserve verbatim (this is window CONTENT — padding is app-specific, not chrome).

3. **`livos/packages/ui/src/modules/window/windows-container.tsx`** — orchestrator that renders the window stack (z-index management, focus management, drag handles).
   - This file is mostly structural — most className are layout (`absolute inset-0`, `flex`). Touch only:
     * Backdrop/overlay color: `bg-black/40` → leave (overlay is theme-neutral); but if it uses `bg-zinc-900/X` for darken → `bg-[var(--card-bg)]/X` (preserves variable-driven theming).
     * Window shadow: if `shadow-2xl` → `shadow-card` (canonical card elevation).
   - DO NOT touch z-index manipulation, drag handlers, focus management, useWindowStore calls.

Same workflow as Task 1: Read each file in full → identify mapping rows → Edit each substring → run typecheck + sacred SHA gate + behavioral-diff check.
  </action>
  <verify>
    <automated>
cd livos &&
pnpm --filter ui typecheck 2>&1 | tail -10 &&
git diff --stat livos/packages/ui/src/components/app-icon.tsx livos/packages/ui/src/modules/window/window-content.tsx livos/packages/ui/src/modules/window/windows-container.tsx &&
[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] &&
echo "TASK_2_OK"
    </automated>
  </verify>
  <done>
- 3 files restyled to canonical tokens
- No functional diff in handlers/store calls/focus logic
- typecheck PASS, sacred SHA preserved
  </done>
</task>

<task type="auto">
  <name>Task 3: Restyle login screen with ui-kit primitives</name>
  <files>
    livos/packages/ui/src/routes/login/index.tsx
  </files>
  <action>
The login screen is the safest place to introduce ui-kit primitives because:
- It's a fresh full-screen surface (no shadcn-primitive entanglement with downstream consumers).
- It has standard form patterns (input + password + submit button) — exactly what ui-kit ships.

1. Read `livos/packages/ui/src/routes/login/index.tsx` in full (single Read call).

2. Identify the rendered structure. Per Phase 115 inventory + memory ("avatar grid + password entry + multi-user JWT generation") expect:
   - Outer wrapper (full-screen gradient or wallpaper backdrop)
   - Avatar grid (clickable avatars to pick a user)
   - Password input (when an avatar is selected)
   - Submit button
   - Possibly a "Sign in" / "Forgot password" link
   - Possibly an invite-link entry path

3. Restyle with the following strategy:
   - **Outer wrapper**: replace existing background with `bg-card-bg` (canonical) OR keep the wallpaper if one renders (login often shows wallpaper). Use `bg-card-bg/90 backdrop-blur` for the foreground card if a wallpaper renders behind.
   - **Centered login card**: wrap the form in `<Card padding="default" radius="default">` from ui-kit. If the existing implementation uses a `<div className="bg-white rounded-2xl p-7 shadow-md">` style — replace with `<Card>`. PRESERVE every child verbatim except where children themselves are migration targets.
   - **Avatar grid**: leave structurally untouched. Apply token classes to avatar border on selected state: `ring-accent-blue` instead of `ring-blue-600`.
   - **Password input**: if existing uses a raw `<input type="password" className="...">` or a shadcn `<Input>` — swap to `<PasswordInput label="Password" />` from ui-kit. Preserve the `value=`, `onChange=`, `onKeyDown=` (Enter key submit) handlers verbatim. Pass them through as the same prop names.
   - **Submit button**: swap to `<Button variant="solid" loading={isSubmitting} onClick={handleSubmit}>Sign in</Button>` (preserve existing label text from i18n `t(...)`). Preserve `onClick` / `onSubmit` handler.
   - **Secondary links** (forgot password, invite): keep as Tailwind `<a>` or `<Link>`, but apply `text-accent-blue hover:underline duration-dash` for the link color.

4. CRITICAL: every existing React Router `<Link to=...>`, `useNavigate()` call, every form `<form onSubmit=...>`, every `useState`, every tRPC mutation hook call → PRESERVED VERBATIM. The ONLY changes are JSX wrapper swaps (raw div → Card, raw button → Button, raw input → PasswordInput) and className substitutions on the avatar grid.

5. Imports: at the top of the file, add:
   ```ts
   import {Button, Card, PasswordInput} from '@livinity/ui-kit'
   ```
   Only import what you actually use in the file. Do not add unused imports.

6. After edits: typecheck must PASS. The behavioral-diff guard for this file is stronger — `git diff` should show:
   - 1 new import line
   - className changes on the avatar grid
   - JSX wrapper swaps for the form, password input, submit button
   - ZERO changes to handler bodies, useState, useEffect, useNavigate, tRPC hooks

7. If the file has a co-located test (`login.test.tsx` or `login.spec.tsx`), run it: `pnpm --filter ui test:run -- login` — must PASS (handler contract unchanged).
  </action>
  <verify>
    <automated>
cd livos &&
pnpm --filter ui typecheck 2>&1 | tail -10 &&
grep -q "from '@livinity/ui-kit'" packages/ui/src/routes/login/index.tsx &&
git diff packages/ui/src/routes/login/index.tsx | grep -E "^[+-].*onClick|onSubmit|onChange|useNavigate|useState" | grep -v "^[+-][[:space:]]*$" | tee /tmp/login-handler-diff.txt &&
[ ! -s /tmp/login-handler-diff.txt ] && echo "NO_HANDLER_DRIFT" &&
[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] &&
echo "TASK_3_OK"
    </automated>
  </verify>
  <done>
- Login uses `<Card>`, `<Button>`, `<PasswordInput>` from ui-kit
- Every onClick/onChange/onSubmit/useNavigate call preserved byte-identical
- typecheck PASS, sacred SHA preserved
- Co-located test (if any) PASS
  </done>
</task>

<task type="auto">
  <name>Task 4: Build, behavioral-diff audit, commit</name>
  <files>
    .planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md
  </files>
  <action>
1. From `livos/`, run the full production build:
   ```bash
   pnpm --filter ui build
   ```
   Build MUST exit 0. If a CSS class doesn't resolve (e.g., `bg-card-bg` not found) → Plan 120-01's preset wiring is incomplete; STOP and document. Do not patch the preset in this plan.

2. Behavioral-diff audit. For each file in `files_modified` (excluding the SUMMARY.md), run:
   ```bash
   git diff <file> | grep -E "^[+-][[:space:]]*(on(Click|Submit|Change|KeyDown|MouseEnter|PointerDown|Focus|Blur)|useEffect|useState|useNavigate|useMutation|useQuery|useStore|t\(|navigate\(|router\.)" | grep -v "^[+-][[:space:]]*$"
   ```
   Output MUST be empty for every file. If anything matches → review the diff, confirm it's intentional (e.g., the line moved due to wrapper swap but content unchanged) OR revert that hunk. Per D-120-NO-FUNCTIONAL-CHANGES, ANY handler body / hook call change in a Wave 2 plan is forbidden.

3. Sacred SHA final guard:
   ```bash
   git hash-object liv/packages/core/src/sdk-agent-runner.ts
   ```
   MUST equal `f3538e1d811992b782a9bb057d1b7f0a0189f95f`. If not — revert all 8 file edits + exit failure.

4. Write `.planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md`:
   - List each of the 8 components restyled with a one-line note on what changed (e.g., "dock.tsx: bg-zinc-* → bg-card-bg + border-dash-line + rounded-dash on container")
   - Diff stats (lines changed per file)
   - Behavioral-diff audit: PASS (no handler drift)
   - Build status: PASS
   - Sacred SHA: preserved
   - Component count contributed to Wave 1 cap: 8 (running total Wave 1: 8 / 30)
   - Operator UAT block:
     ```
     **Operator UAT (Mini PC):**
     1. SSH: existing session or `ssh -i .../minipc bruce@10.69.31.68`
     2. Run: `bash /opt/livos/update.sh`
     3. Open https://bruce.livinity.io (clear browser cache or hard-reload)
     4. Validate:
        a. Desktop loads → wallpaper renders, dock visible at bottom
        b. Dock surface color/border matches `livinity.io/dashboard` card aesthetic (light card-bg, subtle dash-line border, 18px radius)
        c. Click an app icon → window opens; window chrome/content background matches dashboard
        d. Open spotlight (Cmd/Ctrl+Space or whatever existing keybind) → palette surface matches dashboard cards
        e. Open command palette (cmdk binding) → same canonical surface
        f. Log out → login screen renders → password input + submit button styled with ui-kit primitives (Geist font, accent-blue submit)
        g. Log in → succeeds → desktop returns (FUNCTIONAL: login flow unchanged)
        h. Open 2-3 apps (Files, Settings, App Store) → daily-driver flows still work (OwnCloud-equivalent: nothing broken per D-120-MINI-PC-OPERATOR-PRIORITY)
     5. Report PASS/FAIL in chat
     6. On FAIL: `cd /opt/livos && git revert <plan-commit> && bash update.sh`
     ```

5. Stage + commit:
   ```bash
   git add livos/packages/ui/src/layouts/desktop.tsx \
           livos/packages/ui/src/modules/dock/dock.tsx \
           livos/packages/ui/src/components/apple-spotlight.tsx \
           livos/packages/ui/src/components/cmdk.tsx \
           livos/packages/ui/src/components/app-icon.tsx \
           livos/packages/ui/src/modules/window/window-content.tsx \
           livos/packages/ui/src/modules/window/windows-container.tsx \
           livos/packages/ui/src/routes/login/index.tsx \
           .planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md
   git commit -m "feat(120-02): restyle Mini PC chrome (desktop+dock+spotlight+cmdk+app-icon+window+login) to canonical tokens

8 highest-traffic chrome components restyled to @livinity/design-tokens
canonical surface palette. Login screen adopts ui-kit <Card>/<Button>/
<PasswordInput>. Visual layer only — zero behavioral diff (audited:
no handler/hook/route-binding drift).

Wave 1 component tally: 8 / 30.

Sacred SHA preserved: f3538e1d811992b782a9bb057d1b7f0a0189f95f
Per D-120-NO-FUNCTIONAL-CHANGES, D-120-MINI-PC-OPERATOR-PRIORITY,
D-120-INCREMENTAL-DEPLOY, D-120-WAVE-1-IS-30-COMPONENTS."
   ```
  </action>
  <verify>
    <automated>
cd livos && pnpm --filter ui build 2>&1 | tail -10 &&
[ "$(git hash-object liv/packages/core/src/sdk-agent-runner.ts)" = "f3538e1d811992b782a9bb057d1b7f0a0189f95f" ] &&
git log -1 --format="%s" | grep -q "feat(120-02)" &&
test -f .planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md &&
echo "PLAN_120_02_DONE"
    </automated>
  </verify>
  <done>
- `pnpm --filter ui build` PASS
- 8 components restyled, no handler drift
- Sacred SHA preserved
- One commit `feat(120-02): ...` on master
- SUMMARY.md with operator UAT block + component-count tally
  </done>
</task>

</tasks>

<verification>
Phase-level checks:
- [ ] 8 components restyled (desktop, dock, spotlight, cmdk, app-icon, window-content, windows-container, login)
- [ ] `pnpm --filter ui build` PASS
- [ ] `pnpm --filter ui typecheck` PASS
- [ ] Behavioral-diff audit clean (no handler/hook/route drift)
- [ ] Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f` preserved
- [ ] Plan ships independently revertable
- [ ] Wave 1 tally so far: 8 components
</verification>

<success_criteria>
- All 4 tasks complete with `<automated>` verifications PASS
- One git commit `feat(120-02): restyle Mini PC chrome ...` on master
- Operator UAT on Mini PC validates daily-driver flows + visual canonical alignment
- On operator PASS → Plan 120-02 contribution to Wave 1 closes
- On operator FAIL → revert this commit, Wave 1 continues with other plans (each ships independently per D-120-INCREMENTAL-DEPLOY)
</success_criteria>

<output>
After completion, create `.planning/phases/120-mini-pc-ui-migration-wave-1/120-02-SUMMARY.md`.
</output>
