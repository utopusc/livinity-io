---
phase: 119-reusable-component-library
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - livos/packages/ui-kit/src/components/Button.tsx
  - livos/packages/ui-kit/src/components/Button.types.ts
  - livos/packages/ui-kit/src/components/Button.stories.tsx
  - livos/packages/ui-kit/src/components/Button.test.tsx
  - livos/packages/ui-kit/src/components/Card.tsx
  - livos/packages/ui-kit/src/components/Card.types.ts
  - livos/packages/ui-kit/src/components/Card.stories.tsx
  - livos/packages/ui-kit/src/components/Card.test.tsx
  - livos/packages/ui-kit/src/components/Pill.tsx
  - livos/packages/ui-kit/src/components/Pill.types.ts
  - livos/packages/ui-kit/src/components/Pill.stories.tsx
  - livos/packages/ui-kit/src/components/Pill.test.tsx
  - livos/packages/ui-kit/src/components/Input.tsx
  - livos/packages/ui-kit/src/components/Input.types.ts
  - livos/packages/ui-kit/src/components/Input.stories.tsx
  - livos/packages/ui-kit/src/components/Input.test.tsx
  - livos/packages/ui-kit/src/components/PasswordInput.tsx
  - livos/packages/ui-kit/src/components/PasswordInput.stories.tsx
  - livos/packages/ui-kit/src/components/PasswordInput.test.tsx
  - livos/packages/ui-kit/src/styles/atoms.css
  - livos/packages/ui-kit/src/index.ts
autonomous: true
requirements:
  - D-119-DASHBOARD-HTML-IS-SOURCE
  - D-119-NO-CONSUMER-CHANGES
  - D-119-LIGHT-DARK-IRIDESCENT-PARITY
  - D-119-A11Y-FOCUS-RINGS
must_haves:
  truths:
    - "5 atom components exported from @livinity/ui-kit: Button, Card, Pill, Input, PasswordInput"
    - "Each component renders with canonical CSS variables (var(--accent-blue), var(--card-bg), var(--dash-pad), var(--dash-radius), etc.)"
    - "Each component ships a Storybook story file demonstrating all variants in light/dark/iridescent"
    - "Each component ships a Vitest test file with ARIA + variant + interaction coverage"
    - "Button and Input show a visible :focus-visible ring (outline: 2px solid var(--accent-blue); outline-offset: 2px)"
    - "PasswordInput toggles visibility via a button with aria-label and aria-pressed"
  artifacts:
    - path: "livos/packages/ui-kit/src/components/Button.tsx"
      provides: "<Button variant='solid'|'ghost'|'danger' size='sm'|'md'|'lg' loading={boolean}> with forwardRef"
      contains: "forwardRef<HTMLButtonElement"
    - path: "livos/packages/ui-kit/src/components/Card.tsx"
      provides: "<Card padding='default'|'tight' radius='default'|'tight'> with forwardRef"
    - path: "livos/packages/ui-kit/src/components/Pill.tsx"
      provides: "<Pill tone='ok'|'warn'|'err'|'neutral'>"
    - path: "livos/packages/ui-kit/src/components/Input.tsx"
      provides: "<Input label hint error /> with htmlFor + aria-describedby + aria-invalid"
    - path: "livos/packages/ui-kit/src/components/PasswordInput.tsx"
      provides: "<PasswordInput /> reusing Input contract + show/hide toggle"
    - path: "livos/packages/ui-kit/src/styles/atoms.css"
      provides: "h-btn / b-card / pill / i-text / focus-visible canonical classes referencing design-tokens vars only"
    - path: "livos/packages/ui-kit/src/index.ts"
      provides: "Public exports for all 5 atoms + their *Props types"
      contains: "export { Button } from"
  key_links:
    - from: "livos/packages/ui-kit/src/components/Button.tsx"
      to: "@livinity/design-tokens tokens.css"
      via: "className uses .h-btn / variants, atoms.css references var(--accent-blue)"
      pattern: "var\\(--accent-blue\\)"
    - from: "livos/packages/ui-kit/src/components/Card.tsx"
      to: "@livinity/design-tokens tokens.css"
      via: "atoms.css .b-card uses var(--card-bg) + var(--dash-pad) + var(--dash-radius) + var(--card-shadow)"
      pattern: "var\\(--card-bg\\)"
    - from: "livos/packages/ui-kit/src/components/Pill.tsx"
      to: "@livinity/design-tokens tokens.css"
      via: "atoms.css .pill.{ok,warn,err} -> var(--accent-{green,amber,red})"
      pattern: "var\\(--accent-(green|amber|red)\\)"
    - from: "livos/packages/ui-kit/src/index.ts"
      to: "Button, Card, Pill, Input, PasswordInput"
      via: "named re-exports"
      pattern: "export\\s*\\{\\s*Button"
---

<objective>
Ship the 5 atom components for `@livinity/ui-kit` matching the dashboard.html visual idiom verbatim (D-119-DASHBOARD-HTML-IS-SOURCE). Each component (a) consumes `@livinity/design-tokens` CSS variables exclusively for color/spacing/radius, (b) works in light + dark + iridescent themes from day one (D-119-LIGHT-DARK-IRIDESCENT-PARITY), (c) ships a Storybook story showing every variant, and (d) ships Vitest unit tests covering variant rendering + accessibility contracts. Button + Input get visible focus rings per D-119-A11Y-FOCUS-RINGS.

Purpose: These are the building blocks that 119-03 composites (Stepper, CommandBox, NavBar) compose on top of. The phase contract from CONTEXT.md locks the visual idiom to dashboard.html's `.h-btn`, `.b-card`, `.pill`, and the new canonical `.i-text` family (introduced here per Phase 115 finding F-115-MAP-04: Server5 has no shared Input primitive, dashboard.html has no canonical `.i-text` class yet — Phase 119 introduces one derived from the existing token set).

Output: 5 components × {Component.tsx, Component.types.ts, Component.stories.tsx, Component.test.tsx} = 20 files, plus one shared `atoms.css` (token-driven canonical class definitions) and an updated `src/index.ts` (re-exports). All non-test files imported by ESM/CJS/UMD builds (Wave 3 119-04 will verify UMD).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-reusable-component-library/119-CONTEXT.md
@.planning/phases/119-reusable-component-library/119-01-PLAN.md
@.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md
@livos/packages/design-tokens/DESIGN-SYSTEM.md
@livos/packages/design-tokens/tokens.css

<interfaces>
<!-- Provided by 119-01 chassis. Use directly. -->

From livos/packages/ui-kit/src/lib/cn.ts:
```ts
export function cn(...inputs: ClassValue[]): string;
```

From livos/packages/ui-kit/src/lib/theme-classes.ts:
```ts
export const LIV_THEME_STORAGE_KEY: "liv_theme";
export type LivTheme = "light" | "dark" | "iridescent";
export const LIV_THEMES: readonly LivTheme[];
export function applyLivTheme(theme: LivTheme, body?: HTMLElement): void;
export function readLivTheme(): LivTheme;
```

Existing src/index.ts (after 119-01) only exports `__ui_kit_version__`. This plan APPENDS the 5 atom exports.

Storybook preview (.storybook/preview.tsx) loads @livinity/design-tokens/tokens.css + fonts.css globally and exposes a `livTheme` toolbar that calls applyLivTheme(). Stories DO NOT need to inject the stylesheet themselves.
</interfaces>

<canonical_visual_contracts>
<!-- Locked to dashboard.html (Phase 115 COMPONENT-MAP.md). -->

`.h-btn` (Button):
  - radius 999px (pill — F-115-MAP-01)
  - hover lift: transform: translateY(-1px); transition: 0.18s ease
  - solid: background var(--accent-blue), color white
  - ghost: transparent background, 1px border var(--dash-line-strong), color inherit
  - danger: background var(--accent-red), color white (new in 119; derived from token)
  - sizes: sm = height 32px / padding 0 14px / font 13px; md = height 40px / padding 0 18px / font 14px; lg = height 48px / padding 0 22px / font 16px
  - :focus-visible: outline 2px solid var(--accent-blue); outline-offset 2px

`.b-card` (Card):
  - background var(--card-bg)
  - radius var(--dash-radius) [18px] (default) or 12px (tight)
  - padding var(--dash-pad) [28px] (default) or 16px (tight)
  - box-shadow var(--card-shadow)
  - 1px border via outline: 1px solid var(--dash-line) OR border (use border for layout safety)

`.pill` (Pill):
  - inline-flex, gap 6px, padding 4px 10px, font-family var(--font-mono), font-size 11px, font-weight 500
  - text-transform: uppercase; letter-spacing: 0.08em
  - radius 999px
  - tones map: ok -> color var(--accent-green) + tinted bg (color-mix(in srgb, var(--accent-green) 12%, transparent)),
              warn -> var(--accent-amber) + 12% tint,
              err -> var(--accent-red) + 12% tint,
              neutral -> color inherit + var(--dash-line) bg

`.i-text` (Input — new canonical class per F-115-MAP-04):
  - background var(--card-bg-2) [#fafafa light]
  - border 1px solid var(--dash-line)
  - radius 12px
  - padding 12px 16px
  - font-size 14px
  - :focus-visible: outline 2px solid var(--accent-blue); outline-offset 2px; border-color transparent
  - aria-invalid="true" -> border-color var(--accent-red)

Hint + Error pattern (Input label/hint/error):
  - <label> linked to <input> via htmlFor + id
  - <p class="hint"> (color var(--accent-amber-ish-muted)) linked via aria-describedby
  - <p class="error"> (color var(--accent-red)) linked via aria-describedby; sets aria-invalid

PasswordInput:
  - Reuse Input contract
  - Append a <button type="button" aria-label="Show password"|"Hide password" aria-pressed={visible}> with eye/eye-off icon (inline SVG, no external icon dep)
</canonical_visual_contracts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship atoms.css + Button + Card + Pill (the visual layer + 3 simplest components)</name>
  <files>
    livos/packages/ui-kit/src/styles/atoms.css,
    livos/packages/ui-kit/src/components/Button.types.ts,
    livos/packages/ui-kit/src/components/Button.tsx,
    livos/packages/ui-kit/src/components/Button.stories.tsx,
    livos/packages/ui-kit/src/components/Button.test.tsx,
    livos/packages/ui-kit/src/components/Card.types.ts,
    livos/packages/ui-kit/src/components/Card.tsx,
    livos/packages/ui-kit/src/components/Card.stories.tsx,
    livos/packages/ui-kit/src/components/Card.test.tsx,
    livos/packages/ui-kit/src/components/Pill.types.ts,
    livos/packages/ui-kit/src/components/Pill.tsx,
    livos/packages/ui-kit/src/components/Pill.stories.tsx,
    livos/packages/ui-kit/src/components/Pill.test.tsx
  </files>
  <behavior>
- Button.test.tsx:
  - Test 1: renders children
  - Test 2: variant='solid' adds class `solid`; variant='ghost' has neither `solid` nor `danger`; variant='danger' adds `danger`
  - Test 3: size 'sm'|'md'|'lg' adds `h-btn-sm`|`h-btn-md`|`h-btn-lg`
  - Test 4: loading=true sets aria-busy='true' and disables click handler
  - Test 5: forwardRef works (ref.current is the underlying HTMLButtonElement)
  - Test 6: disabled passes through; native disabled prop also blocks onClick
- Card.test.tsx:
  - Test 1: default radius + padding adds classes `b-card` + `b-card-pad-default` + `b-card-radius-default`
  - Test 2: padding='tight' swaps to `b-card-pad-tight`
  - Test 3: radius='tight' swaps to `b-card-radius-tight`
  - Test 4: arbitrary children render
  - Test 5: forwardRef yields HTMLDivElement
- Pill.test.tsx:
  - Test 1: tone='ok'|'warn'|'err'|'neutral' applies matching class
  - Test 2: renders text content
  - Test 3: role/aria-label not set by default (it's a stylistic span); custom aria-label passes through
  </behavior>
  <action>
**Read first:** `livos/packages/ui-kit/src/lib/cn.ts` (to know the import path), `livos/packages/design-tokens/tokens.css` (to verify every var() reference exists), `.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md` § "Primary primitives" rows for Button / Card / Pill (lock visual contract).

Per D-119-A11Y-FOCUS-RINGS, atoms.css MUST define `:focus-visible` on `.h-btn` and `.i-text` with `outline: 2px solid var(--accent-blue); outline-offset: 2px`.

Create `livos/packages/ui-kit/src/styles/atoms.css` with the full canonical class set (h-btn, h-btn-sm/md/lg, h-btn.solid, h-btn.danger, b-card, b-card-pad-{default,tight}, b-card-radius-{default,tight}, pill, pill.ok/warn/err/neutral, i-text, i-text-wrap, i-text-label, i-text-hint, i-text-error). ALL color / spacing / radius / shadow values MUST be `var(--token-name)` references — NO hardcoded hex / px values outside structural sizes (button heights, icon sizes, letter-spacing). Reference DESIGN-SYSTEM.md §Color Tokens + §Radius + §Spacing for the exact var names. Include the hover-lift rule on `.h-btn:hover` per DESIGN-SYSTEM.md §Motion (`transform: translateY(-1px); transition: 0.18s ease`). Include `:focus-visible` per D-119-A11Y-FOCUS-RINGS. The CSS file ends with no `@media` queries — light/dark/iridescent is handled by body-class overrides defined in design-tokens tokens.css.

Create `Button.types.ts`:
```ts
import type { ButtonHTMLAttributes, ReactNode } from "react";

export type ButtonVariant = "solid" | "ghost" | "danger";
export type ButtonSize = "sm" | "md" | "lg";

export interface ButtonProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, "size"> {
  variant?: ButtonVariant;
  size?: ButtonSize;
  loading?: boolean;
  children?: ReactNode;
}
```

Create `Button.tsx` using `forwardRef<HTMLButtonElement, ButtonProps>`. Apply classes: always `h-btn`, plus `h-btn-${size}`, plus `solid` if solid, `danger` if danger (ghost = no variant class). Inside the button, when `loading=true`, set `aria-busy="true"`, prepend a spinner span (a simple inline-SVG circle with a CSS animation defined in atoms.css), keep the children rendered, and short-circuit onClick (wrap onClick handler so it returns early when loading or disabled). Side-effect import the stylesheet at the top: `import "../styles/atoms.css"`. Set `displayName = "Button"`.

Create `Button.stories.tsx`:
```tsx
import type { Meta, StoryObj } from "@storybook/react";
import { Button } from "./Button";

const meta: Meta<typeof Button> = {
  title: "Atoms/Button",
  component: Button,
  args: { children: "Click me", variant: "solid", size: "md" },
  argTypes: {
    variant: { control: "select", options: ["solid", "ghost", "danger"] },
    size: { control: "select", options: ["sm", "md", "lg"] },
    loading: { control: "boolean" },
    disabled: { control: "boolean" },
  },
  tags: ["autodocs"],
};
export default meta;
type Story = StoryObj<typeof Button>;

export const Solid: Story = { args: { variant: "solid" } };
export const Ghost: Story = { args: { variant: "ghost" } };
export const Danger: Story = { args: { variant: "danger" } };
export const Loading: Story = { args: { loading: true } };
export const AllSizes: Story = {
  render: () => (
    <div style={{ display: "flex", gap: 12, alignItems: "center" }}>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </div>
  ),
};
```

Per D-119-LIGHT-DARK-IRIDESCENT-PARITY, NO story needs explicit theme decorators — the global `livTheme` toolbar (wired in 119-01 preview) cycles all three.

Create `Button.test.tsx` with the 6 tests from <behavior> using `@testing-library/react`. Use `render(<Button>...)`, `screen.getByRole('button')`, and a `vi.fn()` onClick. For the forwardRef test: `const ref = React.createRef<HTMLButtonElement>(); render(<Button ref={ref}>x</Button>); expect(ref.current).toBeInstanceOf(HTMLButtonElement);`. RED first (write tests, run, watch fail because component file doesn't exist yet). Then GREEN (write Button.tsx, run, watch pass).

Repeat for Card (5 tests) and Pill (3 tests):
- Card.types.ts: `padding?: "default" | "tight"; radius?: "default" | "tight"; children?: ReactNode` extending HTMLAttributes<HTMLDivElement>
- Card.tsx: forwardRef<HTMLDivElement, CardProps>, applies `b-card b-card-pad-${padding ?? 'default'} b-card-radius-${radius ?? 'default'}`
- Pill.types.ts: `tone?: "ok" | "warn" | "err" | "neutral"` extending HTMLAttributes<HTMLSpanElement>
- Pill.tsx: render as `<span class="pill ${tone}">{children}</span>` (no forwardRef strictly required for Pill but accept className via spread)

DO NOT update `src/index.ts` yet — that happens in Task 3 once all 5 atoms exist.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit test -- src/components/Button.test.tsx src/components/Card.test.tsx src/components/Pill.test.tsx &amp;&amp; node -e "const fs=require('fs'); const css=fs.readFileSync('packages/ui-kit/src/styles/atoms.css','utf8'); ['var(--accent-blue)','var(--accent-green)','var(--accent-red)','var(--accent-amber)','var(--card-bg)','var(--dash-pad)','var(--dash-radius)','var(--card-shadow)','var(--dash-line)','translateY(-1px)','focus-visible'].forEach(t=&gt;{if(!css.includes(t))throw new Error('atoms.css missing '+t);}); console.log('atoms.css token coverage ok');"</automated>
  </verify>
  <done>
- atoms.css references every required token (`--accent-blue/green/amber/red`, `--card-bg`, `--dash-pad/radius/line`, `--card-shadow`), the `translateY(-1px)` hover-lift, and a `:focus-visible` rule
- Button/Card/Pill `.tsx` + `.types.ts` + `.stories.tsx` + `.test.tsx` (12 files total) all exist
- `pnpm --filter @livinity/ui-kit test` passes Button (6) + Card (5) + Pill (3) = 14 assertions
- Each component story file declares ≥3 named exports (variants) and uses `tags: ["autodocs"]`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship Input + PasswordInput with a11y contracts</name>
  <files>
    livos/packages/ui-kit/src/components/Input.types.ts,
    livos/packages/ui-kit/src/components/Input.tsx,
    livos/packages/ui-kit/src/components/Input.stories.tsx,
    livos/packages/ui-kit/src/components/Input.test.tsx,
    livos/packages/ui-kit/src/components/PasswordInput.tsx,
    livos/packages/ui-kit/src/components/PasswordInput.stories.tsx,
    livos/packages/ui-kit/src/components/PasswordInput.test.tsx
  </files>
  <behavior>
- Input.test.tsx:
  - Test 1: renders an <input> with the provided label as an associated <label>
  - Test 2: hint prop renders below input, linked via aria-describedby
  - Test 3: error prop renders below input, replaces hint in aria-describedby, sets aria-invalid='true' on input
  - Test 4: typing fires onChange with the typed value
  - Test 5: disabled prop disables the input
  - Test 6: id prop is used as input id and label htmlFor target; if no id, an auto-generated id (React.useId) is used and the label still associates
  - Test 7: forwardRef yields HTMLInputElement
- PasswordInput.test.tsx:
  - Test 1: type defaults to 'password'
  - Test 2: clicking the visibility toggle flips input type to 'text' and aria-pressed to 'true'; clicking again flips back
  - Test 3: toggle has aria-label that switches between 'Show password' and 'Hide password'
  - Test 4: all Input behaviors still work (label, hint, error, disabled) — reuse Input internally
  </behavior>
  <action>
**Read first:** `livos/packages/ui-kit/src/components/Button.tsx` (just shipped in Task 1) for the forwardRef + className idiom; `livos/packages/ui-kit/src/styles/atoms.css` (confirm `.i-text`, `.i-text-wrap`, `.i-text-label`, `.i-text-hint`, `.i-text-error` classes exist from Task 1).

Create `Input.types.ts`:
```ts
import type { InputHTMLAttributes, ReactNode } from "react";

export interface InputProps extends Omit<InputHTMLAttributes<HTMLInputElement>, "size"> {
  label?: ReactNode;
  hint?: ReactNode;
  error?: ReactNode;
}
```

Create `Input.tsx` with `forwardRef<HTMLInputElement, InputProps>`. Resolve id with `const inputId = id ?? React.useId();`. Build a stable describedby:
- `const hintId = hint ? `${inputId}-hint` : undefined;`
- `const errorId = error ? `${inputId}-error` : undefined;`
- `const describedBy = errorId ?? hintId ?? props["aria-describedby"];`
Render structure:
```tsx
<div className={cn("i-text-wrap", className)}>
  {label && <label htmlFor={inputId} className="i-text-label">{label}</label>}
  <input
    ref={ref}
    id={inputId}
    className="i-text"
    aria-invalid={error ? "true" : undefined}
    aria-describedby={describedBy}
    {...rest}
  />
  {error ? (
    <p id={errorId} className="i-text-error" role="alert">{error}</p>
  ) : hint ? (
    <p id={hintId} className="i-text-hint">{hint}</p>
  ) : null}
</div>
```
Side-effect import atoms.css at top. `Input.displayName = "Input"`.

Create `Input.stories.tsx` with stories: `Plain`, `WithLabel`, `WithHint`, `WithError`, `Disabled`. Use Storybook controls for label/hint/error/disabled.

Create `Input.test.tsx` (7 assertions per <behavior>). For the React.useId fallback test: render `<Input label="Email" />` (no id), then assert `screen.getByLabelText('Email')` resolves to an HTMLInputElement and that `label.htmlFor === input.id` (non-empty).

Create `PasswordInput.tsx` that internally uses the Input contract (do NOT duplicate Input logic — wrap or reuse the rendering). Recommended approach: render an Input directly and then absolutely-position a toggle button inside the wrap. Implementation pattern:
```tsx
import React, { forwardRef, useState } from "react";
import { Input } from "./Input";
import type { InputProps } from "./Input.types";
import { cn } from "../lib/cn";

export const PasswordInput = forwardRef<HTMLInputElement, InputProps>((props, ref) => {
  const [visible, setVisible] = useState(false);
  return (
    <div className="i-text-password-wrap" style={{ position: "relative" }}>
      <Input ref={ref} {...props} type={visible ? "text" : "password"} />
      <button
        type="button"
        className="i-text-toggle"
        aria-label={visible ? "Hide password" : "Show password"}
        aria-pressed={visible}
        onClick={() => setVisible((v) => !v)}
      >
        {/* inline SVG eye / eye-off — see below */}
      </button>
    </div>
  );
});
PasswordInput.displayName = "PasswordInput";
```
For the eye / eye-off icon, embed two inline SVGs swapped via `visible`. Use a simple 16×16 viewBox path with `stroke="currentColor"`, `stroke-width="1.5"`, `fill="none"`. The button positions to the right via `.i-text-toggle` class — add this class to atoms.css (Task 1 already shipped the wider `.i-text-wrap` styles; this is an addendum: `position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: transparent; border: 0; color: inherit; cursor: pointer; padding: 4px;` plus `:focus-visible` ring). If atoms.css does not have this class yet, append it now in this task.

Create `PasswordInput.stories.tsx` with stories: `Default`, `WithLabel`, `WithError`.

Create `PasswordInput.test.tsx` (4 assertions per <behavior>). Use `userEvent.click(screen.getByRole('button', { name: /show password/i }))` to flip.

RED first for each component (write tests, expect failure), then GREEN.
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit test -- src/components/Input.test.tsx src/components/PasswordInput.test.tsx</automated>
  </verify>
  <done>
- Input renders label-htmlFor → input-id, hint vs error swap correctly, aria-invalid set when error
- PasswordInput defaults to type='password', toggle flips type + aria-pressed + aria-label
- 7 + 4 = 11 new test assertions pass
- Stories render in `pnpm --filter @livinity/ui-kit storybook:build` without warnings
- atoms.css contains `.i-text-toggle` rule with `:focus-visible` outline
  </done>
</task>

<task type="auto">
  <name>Task 3: Export atoms from src/index.ts + full build/typecheck/test sweep</name>
  <files>
    livos/packages/ui-kit/src/index.ts
  </files>
  <action>
**Read first:** the 5 atom `.tsx` + `.types.ts` files to confirm export names.

Rewrite `livos/packages/ui-kit/src/index.ts` (preserve the existing `__ui_kit_version__` export):
```ts
// @livinity/ui-kit — entry point.
// Atoms shipped by Phase 119-02; composites by Phase 119-03.

export const __ui_kit_version__ = "0.1.0";

// Atoms (Phase 119-02)
export { Button } from "./components/Button";
export type { ButtonProps, ButtonVariant, ButtonSize } from "./components/Button.types";

export { Card } from "./components/Card";
export type { CardProps } from "./components/Card.types";

export { Pill } from "./components/Pill";
export type { PillProps, PillTone } from "./components/Pill.types";

export { Input } from "./components/Input";
export type { InputProps } from "./components/Input.types";

export { PasswordInput } from "./components/PasswordInput";

// Re-export utility for downstream consumers who want canonical className merging
export { cn } from "./lib/cn";

// Re-export theme helpers (used by 119-03 ThemeToggle and any consumer who wants imperative theming)
export {
  LIV_THEME_STORAGE_KEY,
  LIV_THEMES,
  applyLivTheme,
  readLivTheme,
} from "./lib/theme-classes";
export type { LivTheme } from "./lib/theme-classes";
```

Run the full sweep: typecheck → build:lib → test (all suites). The build must produce updated `dist/index.mjs` + `dist/index.cjs` + `dist/index.d.ts` containing the new exports.

Verify D-119-NO-CONSUMER-CHANGES: `git diff --name-only` only shows files under `livos/packages/ui-kit/`.

Sanity check tokens-only invariant: every component renders with `var(--token)` exclusively for theming. No `#hex` colors in component .tsx files (run a grep — should return zero hits for `/#[0-9a-fA-F]{3,6}/` inside `src/components/`).
  </action>
  <verify>
    <automated>cd livos &amp;&amp; pnpm --filter @livinity/ui-kit typecheck &amp;&amp; pnpm --filter @livinity/ui-kit build:lib &amp;&amp; pnpm --filter @livinity/ui-kit test &amp;&amp; node -e "const dts=require('fs').readFileSync('packages/ui-kit/dist/index.d.ts','utf8'); ['Button','Card','Pill','Input','PasswordInput','ButtonProps','CardProps','PillProps','InputProps'].forEach(s=&gt;{if(!dts.includes(s))throw new Error('d.ts missing '+s);}); console.log('d.ts exports ok');"</automated>
  </verify>
  <done>
- `src/index.ts` re-exports all 5 atoms + their *Props types + cn + theme helpers
- `dist/index.d.ts` contains every atom + Props type name
- Full vitest run: ≥25 tests passing (Button 6 + Card 5 + Pill 3 + Input 7 + PasswordInput 4 + scaffold 5 = 30)
- `pnpm --filter @livinity/ui-kit typecheck` exits 0
- `git diff --name-only` (post-plan) lists only `livos/packages/ui-kit/**` paths — D-119-NO-CONSUMER-CHANGES upheld
- Component `.tsx` files contain zero hardcoded hex colors (grep `[0-9a-fA-F]{6}` in src/components yields zero matches outside `0` or numeric literals)
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @livinity/ui-kit test` → 30 assertions pass (5 scaffold + 6 Button + 5 Card + 3 Pill + 7 Input + 4 PasswordInput)
- `pnpm --filter @livinity/ui-kit build` → ESM + CJS + UMD + d.ts produced, d.ts exports all 5 atoms
- `pnpm --filter @livinity/ui-kit storybook:build` → produces `storybook-static/` with ≥5 component story groups + Scaffold tokens story (≥6 total story groups)
- Storybook `livTheme` toolbar cycles all 3 themes; manual smoke confirms components visually reskin in dark + iridescent (note: dark/iridescent values are stubs per D-116-FOLLOW-UP-DARK, so iridescent may visually equal light until tokens.css ships overrides — this is expected and not a 119-02 regression)
- ARIA: Button has `aria-busy` when loading; Input has `aria-invalid` + `aria-describedby`; PasswordInput toggle has `aria-pressed` + `aria-label`
- Focus rings: tab through the Storybook Button story → outline visible (`outline: 2px solid var(--accent-blue)`)
</verification>

<success_criteria>
- 5 atom components shipped: Button, Card, Pill, Input, PasswordInput
- Each has Component.tsx + Component.types.ts (for those with a Props alias) + Component.stories.tsx + Component.test.tsx
- One shared `src/styles/atoms.css` defines `.h-btn`, `.b-card`, `.pill`, `.i-text` families using ONLY design-tokens CSS variables
- All exports added to `src/index.ts`; `dist/index.d.ts` reflects them
- 25 new test assertions pass (cumulative ≥30 including scaffold)
- D-119-DASHBOARD-HTML-IS-SOURCE: visual contracts (radius 999px button, 18px card radius, 28px card padding, pill mono uppercase 11px) match dashboard.html
- D-119-NO-CONSUMER-CHANGES: only `livos/packages/ui-kit/**` touched
- D-119-LIGHT-DARK-IRIDESCENT-PARITY: all stories work in all 3 themes (any visual gap is a pending design-tokens issue, not a 119-02 issue)
- D-119-A11Y-FOCUS-RINGS: Button + Input + PasswordInput-toggle have `:focus-visible` outline; Input + PasswordInput have ARIA contracts
</success_criteria>

<output>
After completion, create `.planning/phases/119-reusable-component-library/119-02-SUMMARY.md` with:
- Final test count breakdown by component
- Bundle size of `dist/index.mjs` after this plan (pre-119-03)
- A reminder that 119-03 will add 6 more components to the same atoms.css family + new composites.css
- Confirmation: `git diff --name-only` lists only ui-kit paths
- Any token gaps observed (e.g., did we need a `--accent-amber-muted` that doesn't exist? If yes, document for v1.x of design-tokens — DO NOT add silently)
</output>
