---
phase: 115-ui-component-inventory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md
  - .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md
  - .planning/phases/115-ui-component-inventory/baseline-screenshots/
autonomous: true
requirements: []
tags: [ui-inventory, design-system, v35, landing-html, baseline-screenshots, chrome-devtools-mcp]

must_haves:
  truths:
    - "INVENTORY-LANDING.md exists with 8 entries (one per HTML file in /opt/landing/livinity.io/)"
    - "COMPONENT-MAP.md exists with cross-surface element identity table covering at minimum Button, Card, Input, Stepper, Modal, NavBar"
    - "baseline-screenshots/ directory has at least 20 PNG captures spanning landing pages + Server5 public routes + at least 2 Mini PC public routes"
    - "Zero source-tree edits across livos/, liv/, scripts/, packages/"
  artifacts:
    - path: ".planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md"
      provides: "Per-file inventory of landing static HTML pages"
      min_lines: 60
      contains: "dashboard.html"
    - path: ".planning/phases/115-ui-component-inventory/COMPONENT-MAP.md"
      provides: "Cross-surface element identity map (Button on Mini PC vs Server5 vs landing, etc.)"
      min_lines: 80
      contains: "Button"
    - path: ".planning/phases/115-ui-component-inventory/baseline-screenshots/"
      provides: "PNG screenshots of every public route at 1920px + 375px, light + dark"
      min_files: 20
  key_links:
    - from: "COMPONENT-MAP.md"
      to: "INVENTORY-MINI-PC.md + INVENTORY-SERVER5.md + INVENTORY-LANDING.md"
      via: "Cross-references same conceptual element across all 3 surface inventories"
      pattern: "Button|Card|Input|Stepper|Modal|NavBar"
    - from: "baseline-screenshots/"
      to: "Phase 121 cross-surface audit + visual regression suite"
      via: "Pre-migration baseline for visual diff after every v35 phase"
      pattern: ".*-1920\\.png|.*-375\\.png"
---

<objective>
Three deliverables in one plan (all touch the same `.planning/phases/115-ui-component-inventory/` directory; no file conflicts between them):

1. **INVENTORY-LANDING.md** — Inventory of all 8 HTML pages in `/opt/landing/livinity.io/` (Server5).
2. **COMPONENT-MAP.md** — Cross-surface element identity map. For each conceptual primitive (Button, Card, Input, Stepper, Modal, NavBar, ThemeToggle, CommandBox, Pill, Toast), name the concrete instance on each surface (Mini PC TSX, Server5 TSX, landing HTML class).
3. **baseline-screenshots/** — Chrome DevTools MCP captures of every public route at 1920px + 375px breakpoints, light + dark themes where applicable.

Purpose: Landing is the canonical design source (dashboard.html). The COMPONENT-MAP feeds Phase 119 (`@livinity/ui-kit` selection — what components must the kit ship to cover existing surface needs?). The screenshots are the pre-migration baseline against which every subsequent v35 phase (117-121) measures visual progress.

Output: 3 artifacts at `.planning/phases/115-ui-component-inventory/{INVENTORY-LANDING.md, COMPONENT-MAP.md, baseline-screenshots/*.png}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-ui-component-inventory/115-CONTEXT.md
@.planning/v35-DESIGN-SYSTEM-MILESTONE.md

# Server5 SSH:
# /c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
#   -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@45.137.194.102

# Chrome DevTools MCP must be running at http://127.0.0.1:9223 (per CLAUDE.md / MEMORY.md).
# Tool calls: mcp__chrome-devtools__* (navigate_page, take_screenshot, set_viewport, etc.).
# Per D-115-SCREENSHOT-EVERY-PUBLIC-ROUTE: only capture routes reachable without dev/admin auth.

# 8 HTML files (per milestone Surface 3 spec):
# - dashboard.html         (52KB — canonical design reference)
# - dashboard-install.html (15KB — built 2026-05-14 to match dashboard.html)
# - auth.html              (login/register)
# - profile.html
# - customize.html
# - download.html
# - index.html             (landing/marketing root)
# - forgot-password.html

# Canonical class vocabulary from dashboard.html (per milestone § Reusable classes):
# .h-btn / .h-btn.solid   — primary/secondary button
# .b-card.span-N          — bento card grid item
# .hero-card / .hero-status-row / .status-dot.{on,off}  — landing hero
# .cmd-box / .cmd-key / .copy   — command/key display
# .stepper .step.{active,done}  — progress indicator
# .pill.{ok,err,warn}     — status pill
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSH-fetch all 8 HTML headers and write INVENTORY-LANDING.md</name>
  <read_first>
    - .planning/phases/115-ui-component-inventory/115-CONTEXT.md
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "Surface 3: Landing static HTML" (8 files breakdown, current state: all 8 link Geist stylesheet but inline CSS vars drift)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "What 'the dashboard.html aesthetic' actually means" (canonical tokens + reusable classes)
  </read_first>
  <files>.planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md</files>
  <action>
**Step 1: SSH-fetch each HTML file's `<head>` block + a snapshot of its CSS variables.**

Single batched SSH command:

```
/c/Windows/System32/OpenSSH/ssh.exe \
  -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
  -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
  root@45.137.194.102 \
  "for f in /opt/landing/livinity.io/{dashboard,dashboard-install,auth,profile,customize,download,index,forgot-password}.html; do echo '===FILE: '\$f' ==='; echo '--- size ---'; wc -c \$f; echo '--- head 80 lines ---'; head -80 \$f; echo '--- :root block ---'; awk '/:root *{/,/^}/' \$f | head -40; echo '--- reusable class hits ---'; grep -oE 'class=\"[^\"]*(h-btn|b-card|hero-card|cmd-box|stepper|pill)[^\"]*\"' \$f | sort -u | head -20; echo; done"
```

Save output to `.planning/phases/115-ui-component-inventory/.work/landing-headers.txt`.

**Step 2: Write INVENTORY-LANDING.md.**

```markdown
# Landing Static HTML Inventory — `/opt/landing/livinity.io/`

**Phase:** 115 (v35.0 Design System Unification milestone foundation)
**Snapshot date:** 2026-05-14
**Host:** Server5 (45.137.194.102) — files served directly by Caddy
**Total HTML files:** 8
**Canonical reference:** `dashboard.html` (52KB — see D-V35-CANONICAL-IS-DASHBOARD-HTML)

## Migration tag taxonomy

| Tag | Meaning |
|---|---|
| `canonical` | Matches dashboard.html token definitions and reusable class vocabulary |
| `needs-migration` | Drift detected (token values or class definitions differ from canonical) |
| `replace-with-library` | (Not applicable for HTML pages — they consume tokens, not components) |
| `wontfix` | Page intentionally diverges (none expected) |
| `unknown` | Agent could not classify; operator review needed |

## Per-file inventory

| File | Size | Primary purpose | Public route | Idiom | Migration tag | Drift notes |
|---|---|---|---|---|---|---|
| `dashboard.html` | 52KB | THE canonical dashboard reference | `livinity.io/dashboard` | React UMD + custom CSS + Geist | canonical | Source of truth; no drift by definition |
| `dashboard-install.html` | 15KB | First-run install wizard (Phase 111-04) | `livinity.io/dashboard/install` (static fallback) | React UMD + dashboard.html tokens | canonical | Built 2026-05-14 to match dashboard.html |
| `auth.html` | {N}KB | Login + register page | `livinity.io/login` + `/register` | React UMD + custom CSS | {tag} | {note any drift in `:root` or reusable classes} |
| `profile.html` | {N}KB | User profile page | `livinity.io/profile` | ... | ... | ... |
| `customize.html` | {N}KB | Account customization | `livinity.io/customize` | ... | ... | ... |
| `download.html` | {N}KB | LivOS Agent download | `livinity.io/download` | ... | ... | ... |
| `index.html` | {N}KB | Landing/marketing home | `livinity.io/` | ... | ... | ... |
| `forgot-password.html` | {N}KB | Password reset | `livinity.io/forgot-password` | ... | ... | ... |

## Token-drift detail

For each non-canonical file, document:
- Which `:root` vars (if any) deviate from dashboard.html's values
- Which reusable classes (.h-btn, .b-card.span-N, .stepper, .pill, .cmd-box, .hero-card) appear vs are absent
- Whether the file loads the Geist + Geist Mono + Instrument Serif stylesheet link
- Theme support: does the file include `body.dark` + `body.iridescent` blocks?

```
### auth.html drift
- `:root` vars present: {list}
- vs dashboard.html canonical: {diff}
- Reusable classes used: {.h-btn, .b-card, ...}
- Theme blocks: {body.dark present? body.iridescent present?}
- Geist font link: {present | missing}
```

(Repeat for every file tagged `needs-migration`.)

## Aggregate counts

| Migration tag | Count |
|---|---|
| canonical | {N} |
| needs-migration | {N} |
| wontfix | {N} |
| unknown | {N} |
| **TOTAL** | 8 |

## Landing deployment notes (for Phase 118 executor)

- Files served by Caddy directly; no build step.
- Path on Server5: `/opt/landing/livinity.io/<file>.html`.
- Edit pattern: SSH + backup `<file>.pre-v35-NN.bak` → edit → `systemctl reload caddy` only if Caddyfile changed (HTML edits hot-served).
- React UMD compile is in-browser via `@babel/standalone` — no build/deploy bottleneck.
- `_shared/tokens.css` does NOT exist yet — Phase 118 will create it; this inventory pre-flags where each file's inline `:root` block needs replacement.
```

**Classification rules:**
- `dashboard.html` → `canonical` by definition (D-V35-CANONICAL-IS-DASHBOARD-HTML).
- `dashboard-install.html` → `canonical` (built 2026-05-14 specifically to match per Phase 111-04 spec).
- All other HTML files: compare each file's `:root` block to dashboard.html's. If exact match → `canonical`. If any deviation (different `--accent-blue` value, missing `--dash-pad`, missing `body.dark` block, missing Geist font link) → `needs-migration`.
- If a file lacks ANY recognizable canonical class (.h-btn, .b-card, .pill) AND has a totally different visual structure (e.g. bespoke landing animation) → still `needs-migration` (Phase 118 will reconcile).
- `unknown` only if SSH fetch failed for that file.

**ZERO SOURCE EDITS** (D-115-READ-ONLY): SSH walk is read-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md && [ "$(wc -l < .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md)" -ge 60 ] && grep -c "^| \`.*\\.html\`" .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md | awk '{exit ($1 >= 8) ? 0 : 1}' && grep -q "dashboard.html" .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md && grep -q "auth.html" .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md && grep -q "forgot-password.html" .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md && grep -q "## Landing deployment notes" .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md` exists with ≥60 lines
    - Per-file inventory table contains all 8 HTML files (dashboard, dashboard-install, auth, profile, customize, download, index, forgot-password)
    - Token-drift detail section present for at least every `needs-migration` file
    - Aggregate counts table sums to 8
    - "Landing deployment notes" section present with Caddy + backup pattern + tokens.css note
  </acceptance_criteria>
  <done>INVENTORY-LANDING.md ready; all 8 HTML files classified with drift detail; Phase 118 has a precise scope map.</done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline screenshots via Chrome DevTools MCP</name>
  <read_first>
    - .planning/phases/115-ui-component-inventory/115-CONTEXT.md (D-115-SCREENSHOT-EVERY-PUBLIC-ROUTE scope)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "What 'the dashboard.html aesthetic' actually means" (theme tokens: body / body.dark / body.iridescent → 3 themes)
    - CLAUDE.md / MEMORY.md note on chrome-devtools-mcp at http://127.0.0.1:9223
  </read_first>
  <files>.planning/phases/115-ui-component-inventory/baseline-screenshots/</files>
  <action>
**Pre-flight check:** Confirm chrome-devtools-mcp is reachable at `http://127.0.0.1:9223`. If MCP tools `mcp__chrome-devtools__*` are NOT available in this agent's tool set, FALL BACK to documenting the missing baseline in `.work/screenshot-blocked.md` and proceed — the operator will replay this task with MCP enabled. Do NOT spin up Chrome manually; the agent does not own Chrome lifecycle.

**Screenshot targets (D-115-SCREENSHOT-EVERY-PUBLIC-ROUTE — public routes only):**

| # | URL | Surface | Notes |
|---|---|---|---|
| 1 | https://livinity.io/ | landing (index.html) | landing root |
| 2 | https://livinity.io/dashboard | landing (dashboard.html) | canonical reference; may 401 — capture redirect if so |
| 3 | https://livinity.io/dashboard/install | landing (dashboard-install.html) | Phase 111 install wizard |
| 4 | https://livinity.io/login | Server5 Next.js or auth.html (Caddy choice) | login form |
| 5 | https://livinity.io/register | Server5 Next.js | register form |
| 6 | https://livinity.io/forgot-password | landing (forgot-password.html) or Next.js | password reset |
| 7 | https://livinity.io/store | Server5 Next.js | public app store |
| 8 | https://livinity.io/download | landing (download.html) | LivOS Agent download |
| 9 | https://livinity.io/profile | landing (profile.html) | may 401 — capture redirect |
| 10 | https://livinity.io/customize | landing (customize.html) | may 401 — capture redirect |
| 11 | https://bruce.livinity.io/ | Mini PC livinityd | public-ish; behind relay; if reachable → capture login screen |
| 12 | https://bruce.livinity.io/login | Mini PC livinityd | login |

**Capture matrix (per public URL):**
- 1920×1080 viewport, light theme → `<route-slug>-1920-light.png`
- 1920×1080 viewport, dark theme → `<route-slug>-1920-dark.png`
- 375×812 viewport (iPhone 13 mini), light theme → `<route-slug>-375-light.png`
- 375×812 viewport, dark theme → `<route-slug>-375-dark.png`

Where `<route-slug>` is a hyphen-safe rendition of the path:
- `/` → `index`
- `/dashboard` → `dashboard`
- `/dashboard/install` → `dashboard-install`
- `/login` → `login`
- `/register` → `register`
- `/forgot-password` → `forgot-password`
- `/store` → `store`
- `/download` → `download`
- `/profile` → `profile`
- `/customize` → `customize`
- `bruce.livinity.io/` → `minipc-root`
- `bruce.livinity.io/login` → `minipc-login`

**Theme switching procedure (per page):**
1. Navigate to URL.
2. Wait for `networkidle` (or 3 seconds — pages with React UMD compile take time).
3. Capture light screenshot.
4. Switch theme by executing JS via MCP: `document.body.classList.remove('dark', 'iridescent'); localStorage.setItem('liv_theme', 'dark'); document.body.classList.add('dark');`
5. Wait 500ms for transition.
6. Capture dark screenshot.
7. Resize viewport to 375×812 and repeat steps 1-6 for mobile.

For pages that do NOT support dark mode (e.g., a marketing landing with no `body.dark` block), the dark capture will look identical to light — that's OK, capture it anyway (Phase 118 will use the parity-or-not as a finding).

**For pages that 401 / 302 redirect to login:**
- Don't follow the redirect. Capture the redirected page (login screen or 401 page) and label the filename `<route-slug>-1920-light-redirected.png` so operator can see the actual landing point.

**Output directory:**
- Create `.planning/phases/115-ui-component-inventory/baseline-screenshots/` if not exists.
- Each PNG filename pattern: `<route-slug>-<viewport>-<theme>[-redirected].png`.
- Target file count: minimum 20 PNGs. With 12 routes × 4 captures each = 48 captures if all reachable. If MCP is unavailable and we fall back, still record planned filenames in `.work/screenshot-blocked.md`.

**MCP tool calls (sequence per URL):**
```
mcp__chrome-devtools__navigate_page(url=<url>)
mcp__chrome-devtools__set_viewport(width=1920, height=1080)
# wait via mcp__chrome-devtools__wait_for(network_idle or timeout=3000)
mcp__chrome-devtools__take_screenshot(filename=<route>-1920-light.png, full_page=true)
mcp__chrome-devtools__evaluate_script(script="document.body.classList.remove('iridescent'); document.body.classList.add('dark'); localStorage.setItem('liv_theme','dark');")
# wait 500ms
mcp__chrome-devtools__take_screenshot(filename=<route>-1920-dark.png, full_page=true)
mcp__chrome-devtools__set_viewport(width=375, height=812)
mcp__chrome-devtools__evaluate_script(script="document.body.classList.remove('dark','iridescent'); localStorage.setItem('liv_theme','light');")
mcp__chrome-devtools__take_screenshot(filename=<route>-375-light.png, full_page=true)
mcp__chrome-devtools__evaluate_script(script="document.body.classList.add('dark'); localStorage.setItem('liv_theme','dark');")
mcp__chrome-devtools__take_screenshot(filename=<route>-375-dark.png, full_page=true)
```

(Exact tool names per chrome-devtools-mcp may differ — check `mcp__chrome-devtools__*` namespace for the actual capabilities; adapt if `evaluate_script` is called `execute_script` etc. The agent should introspect the available tools first.)

**ZERO SOURCE EDITS** (D-115-READ-ONLY): Screenshots write only to `.planning/phases/115-ui-component-inventory/baseline-screenshots/`.
  </action>
  <verify>
    <automated>test -d .planning/phases/115-ui-component-inventory/baseline-screenshots && [ "$(ls .planning/phases/115-ui-component-inventory/baseline-screenshots/*.png 2>/dev/null | wc -l)" -ge 20 ]</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/phases/115-ui-component-inventory/baseline-screenshots/` exists
    - ≥20 PNG files inside (target: 48 if MCP fully reachable; fallback minimum 20)
    - Filename pattern enforced: `<route-slug>-<1920|375>-<light|dark>[-redirected].png`
    - At least one PNG for each of: landing root (`index-*`), dashboard (`dashboard-*`), login (`login-*`), register (`register-*`), store (`store-*`), download (`download-*`), forgot-password (`forgot-password-*`), at minimum 2 Mini PC routes (`minipc-*`)
    - If MCP unavailable → `.work/screenshot-blocked.md` documents the gap with the full planned target list AND the task is re-runnable
  </acceptance_criteria>
  <done>Baseline screenshot directory populated with ≥20 PNGs; Phase 117/118/120/121 have a visual diff target.</done>
</task>

<task type="auto">
  <name>Task 3: Write COMPONENT-MAP.md cross-surface element identity table</name>
  <read_first>
    - .planning/phases/115-ui-component-inventory/INVENTORY-MINI-PC.md (produced by Plan 115-01 — may not be present at task start time if 115-01 runs in parallel; if missing, fall back to scanning `livos/packages/ui/src/` directly for primitive components and TODO-stub the map for those rows)
    - .planning/phases/115-ui-component-inventory/INVENTORY-SERVER5.md (produced by Plan 115-02 — same fallback rule)
    - .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md (produced by Task 1 of THIS plan — already exists)
    - .planning/v35-DESIGN-SYSTEM-MILESTONE.md § "Phase 119 — Reusable Component Library" (initial ui-kit export list: Button, Card, Stepper, Pill, Input, PasswordInput, CommandBox, Modal, Toast, ThemeToggle, NavBar — these are the rows of COMPONENT-MAP.md)
  </read_first>
  <files>.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md</files>
  <action>
**IMPORTANT — parallel-execution handling:** Plans 115-01 and 115-02 run in the same wave as this plan. INVENTORY-MINI-PC.md and INVENTORY-SERVER5.md may not exist yet when this task starts. Strategy:

- If both sibling INVENTORY files exist → read them and cross-reference.
- If either is missing → use direct file inspection on `livos/packages/ui/src/` (read tool) and SSH-list `/opt/platform/web/src/components/` for the relevant primitive components. The map still ships; rows referencing missing inventory get a `(TODO: cross-link after sibling inventory ships)` marker, which is OK and grep-able.

**Write COMPONENT-MAP.md.**

```markdown
# Cross-Surface Component Identity Map

**Phase:** 115 (v35.0 Design System Unification milestone foundation)
**Purpose:** For each conceptual UI primitive, name the concrete instance on each of the 3 surfaces. Feeds Phase 119 (`@livinity/ui-kit` selection).
**Snapshot date:** 2026-05-14

## How to read this map

Each row = one conceptual primitive (e.g., "Button"). Columns = the three surfaces:

- **Mini PC** = where this primitive lives in `livos/packages/ui/src/` (TSX file path or "—" if absent)
- **Server5** = where it lives in `/opt/platform/web/src/` (TSX file path or "—")
- **Landing** = the CSS class name in `/opt/landing/livinity.io/dashboard.html` (and the HTML file that defines it canonically) or "—"

The **Canonical idiom** column captures dashboard.html's expression — Phase 119 ui-kit components MUST visually match this.

## Primary primitives (Phase 119 ui-kit initial export set)

| Primitive | Mini PC | Server5 | Landing (dashboard.html class) | Canonical idiom | Phase 119 ui-kit export | Notes |
|---|---|---|---|---|---|---|
| Button (primary) | {path or — / TODO} | {path or — / TODO} | `.h-btn.solid` | Solid bg `var(--accent-blue)`, white text, radius 999px, hover lift -1px, transition 0.18s ease | `<Button variant="solid" />` | |
| Button (ghost/secondary) | {path or — / TODO} | {path or — / TODO} | `.h-btn` (no .solid) | Transparent bg, `var(--dash-line-strong)` border, text color `var(--text)` | `<Button variant="ghost" />` | |
| Button (danger) | {path or — / TODO} | {path or — / TODO} | (none in dashboard.html — Phase 119 derives from `--accent-red`) | Solid bg `var(--accent-red)`, white text | `<Button variant="danger" />` | New in Phase 119 |
| Card | {path or — / TODO} | {path or — / TODO} | `.b-card` (+ `.span-N` grid modifier) | bg `var(--card-bg)`, radius 18px, padding 28px, shadow `var(--card-shadow)` | `<Card padding="default" radius="default" />` | |
| Card (tight) | {path or — / TODO} | {path or — / TODO} | (variant of `.b-card`) | Smaller padding (16px) | `<Card padding="tight" />` | |
| Input (text) | {path or — / TODO} | {path or — / TODO} | (inline in auth.html — `<input class="i-text">` or similar; verify on read) | bg `var(--card-bg-2)`, border `var(--dash-line)`, padding 12px 16px, radius 12px, focus ring `var(--accent-blue)` | `<Input label hint error />` | |
| Input (password) | {path or — / TODO} | {path or — / TODO} | (auth.html) | Same as Input + show/hide toggle | `<PasswordInput />` | |
| Stepper | {path or — / TODO} | {path or — / TODO} | `.stepper .step.{active,done}` | Horizontal pill stepper, active = bg `var(--accent-blue)`, done = `var(--accent-green)` checkmark | `<Stepper steps={[]} current={N} />` | dashboard-install.html is the live example |
| Pill | {path or — / TODO} | {path or — / TODO} | `.pill.{ok,err,warn}` | Small rounded badge, ok = green, err = red, warn = amber, mono font, uppercase | `<Pill tone="ok|warn|err|neutral" />` | |
| CommandBox | {path or — / TODO} | {path or — / TODO} | `.cmd-box` + `.cmd-key` + `.copy` | Mono font box with copy button, dark-on-light or light-on-dark | `<CommandBox text copyButton />` | Used in dashboard-install.html |
| Modal | {path or — / TODO} | {path or — / TODO} | (no canonical class — dashboard.html lacks modal; Phase 119 must derive) | Center-screen card, backdrop blur, radius 18px | `<Modal />` | New visual definition in Phase 119 |
| Toast | {path or — / TODO} | {path or — / TODO} | (no canonical class) | Bottom-right pill, slide-in, auto-dismiss | `<Toast />` | New visual definition in Phase 119 |
| NavBar (top brand bar) | {path or — / TODO} | {path or — / TODO} | (hero header in dashboard.html — `.hero-card` + brand + theme toggle) | Brand wordmark + theme toggle + sign-in/dashboard link | `<NavBar brand user signOut />` | |
| ThemeToggle | {path or — / TODO} | {path or — / TODO} | (button in dashboard.html `.hero-card` cycling body class) | Cycles light → dark → iridescent → light | `<ThemeToggle />` | localStorage `liv_theme` persistence |

## Secondary primitives (encountered in surface scan but NOT in initial ui-kit export — note for future iteration)

| Primitive | Mini PC | Server5 | Landing | Notes |
|---|---|---|---|---|
| Status dot | — | — | `.status-dot.{on,off}` | Used inline in `.hero-status-row`. Simple span; may inline rather than export. |
| Hero card | — | — | `.hero-card` | Composite layout-specific; may be macro-level (not extracted as primitive). |
| Bento grid layout | — | — | `.bento` parent of `.b-card` | A layout primitive, not a component — Phase 116 design-tokens handles via CSS, not React. |

## Cross-surface drift findings

Use this section to record any primitive where the THREE surface implementations are visually meaningfully different (not just trivial variations). Each finding feeds Phase 119 ui-kit design.

Example findings format:

### Finding F-115-MAP-01: Button radius drift

- Mini PC `<Button>` (livos/packages/ui/src/components/Button.tsx): radius 6px (`rounded-md`)
- Server5 `<Button>` (/opt/platform/web/src/components/...): radius 4px (`rounded`)
- Landing `.h-btn`: radius 999px (pill)

Canonical (dashboard.html): radius 999px. Phase 119 ui-kit ships pill (999px); Mini PC + Server5 migrate.

### Finding F-115-MAP-02: Input border drift
...

### Finding F-115-MAP-03: Stepper presence
- Mini PC: NO Stepper component exists in `livos/packages/ui/src/` (greppable verification)
- Server5: `app/dashboard/install/page.tsx` has a bespoke inline stepper
- Landing: `dashboard-install.html` ships `.stepper` (canonical)

Phase 119 ui-kit ships `<Stepper>`; Mini PC adopts new; Server5 migrates inline → ui-kit.

## Coverage summary (feeds Phase 119 plan)

| Primitive | Currently triple-implemented? | Action |
|---|---|---|
| Button | yes (all 3 surfaces) | Ship ui-kit; migrate all 3 |
| Card | yes | Ship ui-kit; migrate all 3 |
| Input | partial (Mini PC + Server5 only; Landing inlines) | Ship ui-kit; landing keeps inline + matches canonical |
| Stepper | partial (Server5 inline + Landing canonical; Mini PC absent) | Ship ui-kit; Mini PC adopts |
| Pill | {fill in} | {fill in} |
| CommandBox | partial (Landing canonical; others maybe absent) | Ship ui-kit |
| Modal | yes (all 3) | Ship ui-kit; new canonical visual |
| Toast | yes likely | Ship ui-kit; new canonical visual |
| NavBar | yes (varies wildly) | Ship ui-kit; new canonical |
| ThemeToggle | partial | Ship ui-kit |
```

**How to fill the Mini PC + Server5 columns:**
- If sibling INVENTORY files exist → grep them for the primitive name in the "Primary purpose" column or the file name.
- If sibling INVENTORY files do NOT exist (parallel race) → directly grep the source trees:
  - Mini PC: `grep -rn "export.*Button\|export.*Card\|export.*Input\|export.*Stepper\|export.*Pill\|export.*Modal\|export.*Toast\|export.*NavBar\|export.*ThemeToggle" livos/packages/ui/src/ | head -50`
  - Server5: SSH-grep `find /opt/platform/web/src -name "*.tsx" -exec grep -l "export.*Button\|export.*Card\|..." {} \;`
- For each match, record the file path. For multiple matches, pick the most likely "canonical" one (shortest path, in `components/` rather than buried in a feature).
- For "TODO: cross-link" markers — ONLY use these where the file walk genuinely cannot disambiguate. Aim for ≤2 TODO rows in the final document.

**Minimum row requirement:** All 13 primary primitives (Button×3, Card×2, Input×2, Stepper, Pill, CommandBox, Modal, Toast, NavBar, ThemeToggle) must appear in the primary table. At least 3 cross-surface drift findings (F-115-MAP-NN) must be documented.

**ZERO SOURCE EDITS** (D-115-READ-ONLY).
  </action>
  <verify>
    <automated>test -f .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && [ "$(wc -l < .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md)" -ge 80 ] && grep -q "Button" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "Card" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "Input" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "Stepper" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "Modal" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "NavBar" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "Pill" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && grep -q "F-115-MAP-" .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md && [ -z "$(git diff HEAD -- livos/ liv/ scripts/ packages/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md` exists with ≥80 lines
    - Primary primitives table contains all required rows: Button, Card, Input, Stepper, Pill, Modal, NavBar (minimum set per quality_gate)
    - At least 3 cross-surface drift findings labeled F-115-MAP-NN
    - "Coverage summary" table present (feeds Phase 119 plan)
    - ≤2 "TODO: cross-link" markers (rest of rows fully populated)
    - `git diff HEAD -- livos/ liv/ scripts/ packages/` returns empty
  </acceptance_criteria>
  <done>COMPONENT-MAP.md ready; Phase 119 ui-kit design has concrete row-per-primitive scope; cross-surface drift catalogued.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md` and ≥60 lines.
- `test -f .planning/phases/115-ui-component-inventory/COMPONENT-MAP.md` and ≥80 lines.
- `ls .planning/phases/115-ui-component-inventory/baseline-screenshots/*.png | wc -l` ≥20.
- All 8 HTML files referenced in INVENTORY-LANDING.md (one row each).
- COMPONENT-MAP.md contains all required primitive rows (Button, Card, Input, Stepper, Modal, NavBar, Pill).
- ≥3 F-115-MAP-NN drift findings recorded.
- `git diff HEAD -- livos/ liv/ scripts/ packages/` returns empty (D-115-READ-ONLY enforced).
</verification>

<success_criteria>
- INVENTORY-LANDING.md ships at `.planning/phases/115-ui-component-inventory/INVENTORY-LANDING.md` with all 8 HTML files inventoried + drift detail.
- COMPONENT-MAP.md ships at `.planning/phases/115-ui-component-inventory/COMPONENT-MAP.md` with cross-surface element identity map covering all Phase 119 ui-kit primitives.
- baseline-screenshots/ directory contains ≥20 PNG captures spanning landing pages, Server5 public routes, and at least 2 Mini PC public routes.
- D-115-READ-ONLY honored: zero source-tree edits.
- D-115-SCREENSHOT-EVERY-PUBLIC-ROUTE honored: every public route captured at 1920px + 375px, light + dark themes where applicable.
</success_criteria>

<output>
After completion, create `.planning/phases/115-ui-component-inventory/115-03-SUMMARY.md` covering:
- Landing inventory drift summary (canonical vs needs-migration vs unknown counts for the 8 HTML files)
- Cross-surface drift findings list (F-115-MAP-NN with one-line summaries)
- Baseline screenshot count + any failed captures (e.g., 401 redirects, unreachable Mini PC routes)
- Any "TODO: cross-link" markers in COMPONENT-MAP.md that need sibling inventory data to resolve
- Recommendations for Phase 116 (which design tokens have the most drift across surfaces — prioritize those)
</output>
