---
phase: 04-files-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/modules/window/app-contents/files-content.tsx
  - livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
  - livos/packages/ui/src/features/files/components/sidebar/mobile-sidebar-wrapper.tsx
  - livos/packages/ui/src/features/files/components/listing/index.tsx
  - livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
autonomous: true
requirements: [FILE-01, FILE-02]

must_haves:
  truths:
    - "Folder sidebar renders as a slide-in drawer on mobile with a toggle button, not permanently visible"
    - "Sidebar items have 44px minimum touch targets on mobile"
    - "Icons grid view shows 3 columns on narrow mobile and 2 on very narrow, not 1 oversized column"
    - "File list items have adequate tap spacing and no accidental adjacent selection"
    - "Listing card height fills available viewport without overflow or underflow on mobile"
  artifacts:
    - path: "livos/packages/ui/src/modules/window/app-contents/files-content.tsx"
      provides: "Mobile sidebar drawer integration and content area layout"
    - path: "livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx"
      provides: "44px touch targets on sidebar navigation items"
    - path: "livos/packages/ui/src/features/files/components/listing/index.tsx"
      provides: "Mobile-appropriate listing card height"
    - path: "livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx"
      provides: "Responsive icon card sizing for mobile grids"
  key_links:
    - from: "files-content.tsx"
      to: "MobileSidebarWrapper"
      via: "isMobile conditional rendering"
      pattern: "isMobile.*MobileSidebarWrapper"
    - from: "files-content.tsx"
      to: "listing/index.tsx"
      via: "content area height flows to Card"
      pattern: "h-\\[calc"
---

<objective>
Make the Files sidebar and file listing fully mobile-friendly with proper touch targets and adaptive layout.

Purpose: FILE-01 (sidebar as drawer) and FILE-02 (file list/grid adapts to mobile width) require touch-friendly sidebar items, properly sized grid cards, and correct content area height on mobile viewports.
Output: Sidebar with 44px touch targets, responsive icons grid, mobile-safe listing height.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-files-mobile/04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From files-content.tsx (main layout):
- isMobile conditional renders MobileSidebarWrapper vs inline Sidebar
- Content grid: `grid-cols-1 lg:grid-cols-[188px_1fr]` -- sidebar column hidden on mobile
- Header: hamburger button opens sidebar drawer on mobile

From sidebar/sidebar-item.tsx:
- Button inner padding: `px-2 py-[7px]` (~14px vertical padding)
- Icon container: `h-[26px] w-[26px]` (26px)
- Total item height: ~47px (borderline 44px target)

From listing/index.tsx:
- Card height: `h-[calc(100svh-214px)]` on mobile, `lg:h-[calc(100vh-300px)]` on desktop
- The 214px offset does NOT account for files-content.tsx header bar height correctly

From icons-view-file-item.tsx:
- Fixed `w-32` (128px) width per card
- Fixed `h-[68px] w-[68px]` icon container
- getGridDimensions in virtualized-list.tsx uses `itemWidth = 128`

From virtualized-list.tsx:
- Grid columns calculated: `Math.floor((width + minGap) / (containerWidth + minGap))`
- itemHeight = 140, itemWidth = 128
- List view itemSize: isMobile ? 50 : 40
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar 44px touch targets + mobile drawer polish + content height fix</name>
  <files>
    livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
    livos/packages/ui/src/features/files/components/sidebar/mobile-sidebar-wrapper.tsx
    livos/packages/ui/src/features/files/components/listing/index.tsx
    livos/packages/ui/src/modules/window/app-contents/files-content.tsx
  </files>
  <read_first>
    livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx
    livos/packages/ui/src/features/files/components/sidebar/mobile-sidebar-wrapper.tsx
    livos/packages/ui/src/features/files/components/listing/index.tsx
    livos/packages/ui/src/modules/window/app-contents/files-content.tsx
    livos/packages/ui/src/hooks/use-is-mobile.ts
  </read_first>
  <action>
    CRITICAL: Desktop UI must NOT change. All mobile changes gated on useIsMobile() or Tailwind responsive prefixes.

    **sidebar-item.tsx** -- Increase touch target for mobile:
    - Import useIsMobile from `@/hooks/use-is-mobile`
    - Change button className from `px-2 py-[7px]` to `px-2 py-[7px] lg:py-[7px]` and add mobile-specific padding:
      When isMobile, apply `py-2.5` (10px top+bottom on ~26px icon = 46px total, above 44px minimum)
    - Use isMobile conditional: `cn('flex w-full items-center gap-2.5 px-2', isMobile ? 'py-2.5' : 'py-[7px]', disabled && 'cursor-default')`
    - This ensures sidebar items have 44px+ touch targets on mobile matching the Apple HIG pattern from phases 01-03

    **mobile-sidebar-wrapper.tsx** -- Polish close button touch target:
    - The ChevronLeft close button is `h-4 w-4` with no touch padding
    - Wrap it in a 44px touch target div: `<div className="flex h-11 w-11 items-center justify-center">` around the ChevronLeft
    - Keep the icon itself at `h-4 w-4` for visual sizing

    **listing/index.tsx (ListingContent)** -- Fix mobile Card height:
    - Current: `h-[calc(100svh-214px)]` -- the 214px magic number doesn't account for mobile header
    - Change to: `h-[calc(100svh-180px)] lg:h-[calc(100vh-300px)]`
    - The mobile value (180px) accounts for: ~48px files header + ~48px actions bar + ~84px bottom safe area/padding
    - Test by verifying listing fills available space without overflow on mobile viewport

    **files-content.tsx** -- Increase hamburger button touch target:
    - The HiMenuAlt2 hamburger icon has no touch padding wrapper (just `h-5 w-5`)
    - Wrap it in a 44px touch div: replace the bare `<HiMenuAlt2 ... />` with:
      ```
      <button
        onClick={() => setIsMobileSidebarOpen(true)}
        className="flex h-11 w-11 items-center justify-center -ml-2"
      >
        <HiMenuAlt2 className="h-5 w-5 text-text-primary" />
      </button>
      ```
    - Remove the onClick and role='button' from the HiMenuAlt2 itself
    - Also increase the back button from `h-8 w-8` to `h-11 w-11` for 44px touch target on mobile (use className with conditional: isMobile uses h-11 w-11, desktop keeps h-8 w-8; or just make it h-11 w-11 globally like phase 01 did)
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "py-2.5" livos/packages/ui/src/features/files/components/sidebar/sidebar-item.tsx returns a match (mobile touch padding)
    - grep -n "h-11 w-11" livos/packages/ui/src/features/files/components/sidebar/mobile-sidebar-wrapper.tsx returns a match (close button touch target)
    - grep -n "h-11 w-11" livos/packages/ui/src/modules/window/app-contents/files-content.tsx returns a match (hamburger touch target)
    - grep -n "100svh-180px" livos/packages/ui/src/features/files/components/listing/index.tsx returns a match (mobile card height)
  </acceptance_criteria>
  <done>Sidebar items have 44px touch targets on mobile, drawer close button has 44px target, hamburger has 44px target, listing card height is correct for mobile viewport</done>
</task>

<task type="auto">
  <name>Task 2: Responsive icons grid sizing + list view mobile refinement</name>
  <files>
    livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
    livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx
  </files>
  <read_first>
    livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx
    livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx
    livos/packages/ui/src/hooks/use-is-mobile.ts
  </read_first>
  <action>
    CRITICAL: Desktop UI must NOT change. All mobile changes gated on useIsMobile().

    **icons-view-file-item.tsx** -- Make card width responsive for mobile:
    - Current card div has fixed `w-32` (128px). On a ~375px phone with padding, this means only 2 items per row with wasted space, or items get clipped
    - Change the card width from `w-32` to `w-full` so it fills whatever width the grid cell provides
    - The actual item sizing is controlled by `getGridDimensions` in virtualized-list.tsx, so the card just needs to fill its container
    - Keep `max-w-32` to prevent cards from growing too large on desktop: `w-full max-w-32`
    - This lets the grid control sizing while the card fills the available cell width

    **virtualized-list.tsx** -- Adapt grid dimensions for mobile:
    - Import useIsMobile at the top (it already imports this)
    - In `getGridDimensions`, make `itemWidth` responsive to mobile:
      ```
      const itemWidth = isMobile ? 100 : 128
      ```
    - But `getGridDimensions` is a useCallback that currently does NOT have access to `isMobile`. Fix this:
      - Add `isMobile` to the dependency array of the `getGridDimensions` useCallback
      - Reference `isMobile` inside the callback to set `itemWidth`
    - Also reduce `itemHeight` on mobile from 140 to 120 to fit more items:
      ```
      const itemHeight = isMobile ? 120 : 140
      ```
    - This gives 3 columns on a 375px phone: (375 + 8) / (104 + 8) = ~3.4 = 3 columns
    - For the grid cell rendering, the icons-view-file-item.tsx icon container sizes should reduce too:
      Back in icons-view-file-item.tsx, use isMobile to shrink icon container:
      ```
      const iconSize = isMobile ? 'h-[52px] w-[52px]' : 'h-[68px] w-[68px]'
      ```
      Apply this to all three icon container divs (known folder, generic folder, file icon)
    - Import useIsMobile in icons-view-file-item.tsx
    - For the file name text, keep `text-[12px]` on both -- it's already small enough

    **virtualized-list.tsx** -- Also fix the `gridToListIndices` callback:
    - It calls `getGridDimensions(window.innerWidth)` directly which won't use the mobile-aware values
    - It should use the same column count computed during render. Extract columnCount from the closure:
      Actually, the gridToListIndices is only called during grid render and it uses getGridDimensions which now uses isMobile, so it will be correct. Just ensure `isMobile` is in the dependency array of `gridToListIndices` as well since it transitively depends on `getGridDimensions`.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "isMobile ? 100 : 128" livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx returns a match (responsive item width)
    - grep -n "isMobile ? 120 : 140" livos/packages/ui/src/features/files/components/listing/virtualized-list.tsx returns a match (responsive item height)
    - grep -n "w-full max-w-32" livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx returns a match (responsive card width)
    - grep -n "h-\[52px\] w-\[52px\]" livos/packages/ui/src/features/files/components/listing/file-item/icons-view-file-item.tsx returns a match (mobile icon size)
  </acceptance_criteria>
  <done>Icons grid shows 3 columns on 375px mobile (100px items), icon containers shrink to 52px on mobile, card fills grid cell width, all TypeScript compiles clean</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd livos && npx tsc --noEmit --project packages/ui/tsconfig.json`
2. grep verifications for all acceptance criteria
3. Desktop layout unchanged: sidebar items use `py-[7px]` on lg+, icons keep 128px/68px on desktop
</verification>

<success_criteria>
- Sidebar drawer has 44px touch targets on all interactive elements (items, close button, hamburger toggle)
- Icons grid renders 3 columns on 375px mobile with smaller 100px/52px items
- Listing card fills mobile viewport height correctly without overflow
- Desktop UI is completely unchanged (all changes gated on isMobile or responsive prefixes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-files-mobile/04-01-SUMMARY.md`
</output>
