---
phase: 04-files-mobile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/features/files/components/listing/actions-bar/mobile-actions.tsx
  - livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
  - livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx
  - livos/packages/ui/src/features/files/components/file-viewer/image-viewer/index.tsx
  - livos/packages/ui/src/features/files/components/file-viewer/video-viewer/index.tsx
autonomous: true
requirements: [FILE-03, FILE-04]

must_haves:
  truths:
    - "Toolbar actions (upload, new folder, select, sort/view menu) are accessible on mobile with 44px touch targets"
    - "File preview images render within the viewport on mobile without horizontal overflow"
    - "Video viewer fits mobile width without horizontal scrollbar"
    - "Navigation back/forward buttons have adequate touch targets on mobile"
  artifacts:
    - path: "livos/packages/ui/src/features/files/components/listing/actions-bar/mobile-actions.tsx"
      provides: "44px touch targets on Select button and dots menu trigger"
    - path: "livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx"
      provides: "44px touch targets on nav back/forward buttons on mobile"
    - path: "livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx"
      provides: "Mobile-safe viewer overlay with proper padding"
    - path: "livos/packages/ui/src/features/files/components/file-viewer/image-viewer/index.tsx"
      provides: "Mobile-constrained image with max-height"
    - path: "livos/packages/ui/src/features/files/components/file-viewer/video-viewer/index.tsx"
      provides: "Mobile-constrained video player width"
  key_links:
    - from: "mobile-actions.tsx"
      to: "actions-bar/index.tsx"
      via: "MobileActions component rendered in md:hidden div"
      pattern: "md:hidden"
    - from: "viewer-wrapper.tsx"
      to: "image-viewer/index.tsx"
      via: "ViewerWrapper wraps all viewer content"
      pattern: "ViewerWrapper"
---

<objective>
Make the Files toolbar and file preview/viewer fully mobile-friendly with proper touch targets and viewport-safe rendering.

Purpose: FILE-03 (toolbar actions accessible on mobile) and FILE-04 (preview/details panel doesn't overflow on mobile) require 44px touch targets on all toolbar buttons and viewport-constrained file viewers.
Output: Touch-friendly toolbar controls, mobile-safe image/video preview overlays.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-files-mobile/04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From mobile-actions.tsx:
- Select button: `h-[1.9rem]` (~30px) -- below 44px minimum
- TbDots trigger: `h-5 w-5` (20px) with no touch padding -- far below 44px minimum
- DropdownMenuContent: `w-44` (176px)

From navigation-controls.tsx:
- Back/forward buttons: `h-8 w-8` (32px) -- below 44px minimum
- Wrapped in Magnetic hover effect

From viewer-wrapper.tsx:
- Overlay: `absolute left-1/2 top-0 z-10 h-full w-full`
- Inner div: `p-2 md:px-10`
- Click outside to close behavior

From image-viewer/index.tsx:
- img: `max-w-[calc(100vw-40px)]` but NO max-height on mobile (only `md:max-h-[80%]`)
- Missing: image can be taller than viewport on mobile causing scroll

From video-viewer/index.tsx:
- Video wrapped in plain `<div className='bg-black'>` with no width constraint
- Uses @triyanox/react-video which may overflow on narrow mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 44px touch targets on mobile toolbar actions and nav controls</name>
  <files>
    livos/packages/ui/src/features/files/components/listing/actions-bar/mobile-actions.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
  </files>
  <read_first>
    livos/packages/ui/src/features/files/components/listing/actions-bar/mobile-actions.tsx
    livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx
    livos/packages/ui/src/hooks/use-is-mobile.ts
  </read_first>
  <action>
    CRITICAL: Desktop UI must NOT change. All mobile changes gated on useIsMobile() or responsive prefixes.

    **mobile-actions.tsx** -- Increase Select button and dots menu to 44px touch targets:
    - Select Button: change `h-[1.9rem]` to `h-11` (44px). Keep `rounded-full px-3 text-body-sm` styling.
      Updated className: `'h-11 rounded-full px-3 text-body-sm'` (replacing `h-[1.9rem]`)
    - TbDots trigger: wrap in a touch-friendly container. Change:
      ```
      <DropdownMenuTrigger className='focus:outline-none ...'>
        <TbDots className='h-5 w-5' />
      </DropdownMenuTrigger>
      ```
      to:
      ```
      <DropdownMenuTrigger className='flex h-11 w-11 items-center justify-center rounded-full focus:outline-none focus:ring-0 focus:ring-offset-0 focus-visible:outline-none focus-visible:ring-0'>
        <TbDots className='h-5 w-5' />
      </DropdownMenuTrigger>
      ```
    - The icon stays `h-5 w-5` visually but the touch area is now 44px
    - Keep the DropdownMenuContent `w-44` and `align='start'` unchanged

    **navigation-controls.tsx** -- Increase back/forward buttons to 44px on mobile:
    - Import useIsMobile from `@/hooks/use-is-mobile`
    - Add `const isMobile = useIsMobile()` at the top of NavigationControls
    - Change both motion.button className from `h-8 w-8` to `h-11 w-11`:
      Since this component renders on mobile within the actions bar, and phase 01/03 established the pattern of making touch targets 44px globally (not just mobile), change both buttons to `h-11 w-11` unconditionally.
    - Keep `rounded-xl` and all other styling unchanged
    - This matches the Phase 01 decision: "Increased button sizes globally, not just mobile -- consistent sizing"
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "h-11" livos/packages/ui/src/features/files/components/listing/actions-bar/mobile-actions.tsx returns matches for both Select button and dots trigger (at least 2 matches)
    - grep -n "h-11 w-11" livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx returns matches (44px nav buttons)
    - grep -cP "h-8 w-8" livos/packages/ui/src/features/files/components/listing/actions-bar/navigation-controls.tsx returns 0 (old 32px size removed)
  </acceptance_criteria>
  <done>Select button is 44px tall, dots menu trigger is 44px x 44px, nav back/forward buttons are 44px x 44px, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-safe file viewer overlays (image, video, wrapper)</name>
  <files>
    livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx
    livos/packages/ui/src/features/files/components/file-viewer/image-viewer/index.tsx
    livos/packages/ui/src/features/files/components/file-viewer/video-viewer/index.tsx
  </files>
  <read_first>
    livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx
    livos/packages/ui/src/features/files/components/file-viewer/image-viewer/index.tsx
    livos/packages/ui/src/features/files/components/file-viewer/video-viewer/index.tsx
    livos/packages/ui/src/features/files/components/file-viewer/pdf-viewer/index.tsx
  </read_first>
  <action>
    CRITICAL: Desktop UI must NOT change. All mobile changes use responsive prefixes or isMobile.

    **viewer-wrapper.tsx** -- Improve mobile overlay padding and containment:
    - The outer overlay div is `absolute left-1/2 top-0 z-10 flex h-full w-full -translate-x-1/2 items-center justify-center bg-black/30`
    - Change to `fixed inset-0 z-50 flex items-center justify-center bg-black/30`
      Use `fixed inset-0` instead of `absolute` with transforms -- this is more reliable on mobile where the parent may have overflow constraints
      Use `z-50` to ensure it sits above the mobile sidebar drawer (z-40/z-50)
    - The inner content div `p-2 md:px-10`: change to `p-3 md:px-10` for slightly more mobile padding
    - Add `max-h-[100svh] max-w-[100svw] overflow-hidden` to the inner content div to prevent any child from overflowing viewport

    **image-viewer/index.tsx** -- Add mobile max-height constraint:
    - Current img: `max-w-[calc(100vw-40px)]` with `md:max-h-[80%] md:max-w-[90%] md:rounded-lg`
    - The image has NO max-height on mobile (< md), so tall images overflow the viewport
    - Change className to: `max-h-[80svh] max-w-[calc(100vw-24px)] object-contain md:max-h-[80%] md:max-w-[90%] md:rounded-lg rounded-lg`
    - Key changes: added `max-h-[80svh]` for mobile (uses safe viewport height), reduced mobile max-width padding from 40px to 24px (matches p-3 wrapper), added `rounded-lg` for mobile too
    - Remove the absolute positioning (`absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2`) since the parent ViewerWrapper already centers with flexbox (`flex items-center justify-center`)

    **video-viewer/index.tsx** -- Constrain video width on mobile:
    - The video is wrapped in `<div className='bg-black'>` with no width constraint
    - Change to: `<div className='bg-black w-[calc(100vw-24px)] max-w-4xl md:w-auto md:max-w-none'>`
    - On mobile: video width = viewport minus 24px padding, capped at max-w-4xl
    - On desktop (md+): reverts to auto width with no max (original behavior)
    - This prevents the @triyanox/react-video player from overflowing the mobile viewport

    **PDF viewer** -- Already handles mobile correctly (opens download in new tab). No changes needed.
    **Audio viewer** -- Delegates to floating island. No changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/hello/Desktop/Projects/contabo/livinity-io/livos && npx tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "fixed inset-0 z-50" livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx returns a match (mobile-safe fixed overlay)
    - grep -n "max-h-\[80svh\]" livos/packages/ui/src/features/files/components/file-viewer/image-viewer/index.tsx returns a match (mobile image height constraint)
    - grep -n "100vw-24px" livos/packages/ui/src/features/files/components/file-viewer/video-viewer/index.tsx returns a match (mobile video width constraint)
    - grep -n "max-h-\[100svh\]" livos/packages/ui/src/features/files/components/file-viewer/viewer-wrapper.tsx returns a match (viewport containment)
  </acceptance_criteria>
  <done>Image viewer constrained to 80svh height on mobile, video player fits mobile width, viewer overlay uses fixed positioning with z-50, all viewers render within viewport without overflow</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd livos && npx tsc --noEmit --project packages/ui/tsconfig.json`
2. grep verifications for all acceptance criteria
3. Desktop layout unchanged: viewer-wrapper uses md: prefixes, video uses md:w-auto to restore desktop behavior, image keeps md:max-h-[80%] and md:max-w-[90%]
</verification>

<success_criteria>
- All toolbar buttons (Select, dots menu, nav back/forward) have 44px touch targets
- Image viewer is height-constrained on mobile (80svh) preventing vertical overflow
- Video viewer is width-constrained on mobile (100vw-24px) preventing horizontal overflow
- Viewer overlay uses fixed positioning for reliable mobile rendering
- Desktop UI is completely unchanged (all changes gated on responsive prefixes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-files-mobile/04-02-SUMMARY.md`
</output>
