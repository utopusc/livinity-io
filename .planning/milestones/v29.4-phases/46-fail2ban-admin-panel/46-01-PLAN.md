---
phase: 46-fail2ban-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md
autonomous: true
requirements:
  - FR-F2B-01
user_setup: []
must_haves:
  truths:
    - "Mini PC fail2ban-client absolute path is captured (verified, not assumed)"
    - "Verbatim output of `fail2ban-client status` is captured for parser test fixtures"
    - "Verbatim output of `fail2ban-client status sshd` (or first jail) is captured for parser test fixtures"
    - "Verbatim output of `who -u` is captured for active-sessions parser test fixtures"
    - "fail2ban package version on Mini PC is recorded for the version-pin comment in client.ts"
  artifacts:
    - path: ".planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md"
      provides: "Read-only Mini PC diagnostic + parser fixture corpus + binary path/version pin"
      contains: "## fail2ban-client path, ## fail2ban-client status output, ## fail2ban-client status sshd output, ## who -u output, ## fail2ban package version"
  key_links:
    - from: ".planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md"
      to: "46-02-PLAN.md"
      via: "Plan 02 parser.test.ts pastes verbatim fixtures captured here"
      pattern: "fail2ban-client status|who -u"
---

<objective>
Pre-flight diagnostic + audit on the Mini PC: read-only verification that fail2ban is installed, its absolute binary path, and capture verbatim output samples that Plan 02's parser test fixtures will paste in. This plan writes ZERO production code — its only artifact is `46-01-DIAGNOSTIC.md` containing real-world reference fixtures so the parser is built against ground truth, not assumptions.

Purpose: Pitfall W-20 explicitly forbids `vi.mock('child_process')` for fail2ban-client — instead, parsers must be unit-tested against real Mini PC output captured here. Sub-issue #5 from PATTERNS.md flags that `client.ts` will hardcode `/usr/bin/fail2ban-client` — that path MUST be confirmed before Plan 02 codifies it. Pitfall B-05 ("wrong root cause fix") teaches us: diagnose before code change.

Output: One markdown artifact containing (1) verified absolute binary path with `which` output, (2) systemd unit state, (3) verbatim `fail2ban-client status` and `fail2ban-client status <jail>` stdout, (4) verbatim `who -u` stdout, (5) installed `fail2ban` apt package version for client.ts version-pin comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-fail2ban-admin-panel/46-CONTEXT.md
@.planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md
@.planning/research/v29.4-PITFALLS.md

<interfaces>
<!-- Plan 01 produces NO code interfaces — output is a markdown artifact. -->
<!-- Plan 02 will read 46-01-DIAGNOSTIC.md and copy verbatim text blocks into parser.test.ts as template literals. -->
<!-- Mini PC SSH command (per project memory): -->
<!-- /c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@10.69.31.68 -->
<!-- All commands below are read-only (no `set`, no `unbanip`, no `banip`, no apt-install). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Mini PC fail2ban environment + write 46-01-DIAGNOSTIC.md</name>
  <files>.planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md</files>
  <read_first>
    - .planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md (sub-issue #5 — binary path verification requirement)
    - .planning/research/v29.4-PITFALLS.md sections W-20 and B-05 (no-mock fixture-based testing rule, diagnose-before-code rule)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md (Mini PC SSH command + D-NO-SERVER4 hard rule — Server4 is OFF-LIMITS)
  </read_first>
  <action>
    Read-only Mini PC inspection. Per D-NO-SERVER4 hard rule, only `bruce@10.69.31.68` is touched. NEVER ssh to root@45.137.194.103 (Server4) or root@45.137.194.102 (Server5).

    Step 1: SSH to Mini PC and run these EXACT commands, capturing each stdout verbatim:
      a. `which fail2ban-client` → expect `/usr/bin/fail2ban-client`. If different, capture the actual path.
      b. `dpkg -l | grep -i fail2ban` → captures installed package + version (e.g. `ii  fail2ban  1.0.2-3ubuntu0.1`).
      c. `systemctl is-active fail2ban` → expect `active`.
      d. `systemctl status fail2ban --no-pager` → first ~20 lines (active/inactive/dead state visible).
      e. `sudo fail2ban-client status` → captures Status / Number of jail / Jail list output (verbatim — preserve leading whitespace, pipe characters, dashes).
      f. For each jail name in (e), run `sudo fail2ban-client status <jail>` → captures Currently failed, Total failed, File list, Currently banned, Total banned, Banned IP list (verbatim — preserve indent + tree-art).
      g. `which who && who -u` → captures `who -u` output for active-sessions.ts parser fixture (preserve column alignment + trailing `(<sourceIp>)` parens).
      h. `ls -la /var/log/auth.log* 2>&1 | head -5` → confirms whether last-attempted-user parser (PATTERNS.md sub-issue #3 fourth parser function) has a readable log file.

    Step 2: Use the Write tool (NEVER heredoc/cat) to create `.planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md` with the following EXACT structure:

    ```markdown
    # Phase 46 Plan 01 — Mini PC fail2ban Diagnostic

    **Captured:** {ISO timestamp}
    **Host:** bruce@10.69.31.68 (Mini PC — D-NO-SERVER4 enforced; Server4/5 NOT touched)
    **Purpose:** Ground-truth fixtures for Plan 02 parser tests + binary-path pin for Plan 02 client.ts.

    ## fail2ban-client path

    \`\`\`
    $ which fail2ban-client
    {verbatim stdout}
    \`\`\`

    **Pinned for Plan 02 client.ts:** `{absolute path captured above}`

    ## fail2ban package version

    \`\`\`
    $ dpkg -l | grep -i fail2ban
    {verbatim stdout}
    \`\`\`

    **Pinned for Plan 02 client.ts version-pin comment:** `{e.g. fail2ban 1.0.2-3ubuntu0.1}`

    ## systemctl status fail2ban

    \`\`\`
    $ systemctl is-active fail2ban
    {verbatim stdout}

    $ systemctl status fail2ban --no-pager
    {verbatim first 20 lines}
    \`\`\`

    ## fail2ban-client status (jail list)

    \`\`\`
    $ sudo fail2ban-client status
    {verbatim stdout — preserve all whitespace, pipes, backticks}
    \`\`\`

    **Discovered jails:** [list from "Jail list:" line]

    ## fail2ban-client status <jail> (per-jail status)

    For each discovered jail, paste the verbatim output:

    ### Jail: {jail-name}

    \`\`\`
    $ sudo fail2ban-client status {jail-name}
    {verbatim stdout}
    \`\`\`

    (Repeat for each jail.)

    ## who -u (active SSH sessions)

    \`\`\`
    $ who -u
    {verbatim stdout — preserve column alignment + (<ip>) parens}
    \`\`\`

    ## /var/log/auth.log readability

    \`\`\`
    $ ls -la /var/log/auth.log*
    {verbatim stdout}
    \`\`\`

    **Last-attempted-user parser feasibility:** [readable / not-readable / requires sudo — note livinityd runs as root per D-LIVINITYD-IS-ROOT, so root-readable is sufficient]

    ## Plan 02 contract

    Plan 02 (`46-02-PLAN.md`) MUST:
    1. Hardcode `client.ts` binary path constant to the value pinned above (not `which` lookup at runtime).
    2. Add a comment `// Verified against fail2ban {version} on Mini PC bruce@10.69.31.68 ({date})`.
    3. Paste the verbatim fixtures from sections "fail2ban-client status (jail list)" and "fail2ban-client status <jail>" into `parser.test.ts` as multi-line template literals (NOT mocked stdout).
    4. Paste the verbatim `who -u` fixture into `active-sessions.test.ts`.
    5. Per pitfall W-20: NO `vi.mock('child_process')`. Use dependency injection (factory pattern with `ExecFileFn`) to inject fakes into client.ts in tests.
    ```

    Step 3: Per project memory rule "Do NOT Write report/summary/findings/analysis .md files. Return findings directly as your final assistant message" — this artifact is NOT a findings report; it is a structured fixture corpus that Plan 02 mechanically reads. The CLAUDE.md rule does not apply because this file is a planning input artifact (not a status report), and the plan's verify step grep-asserts its presence.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md` exists.
    - Grep `which fail2ban-client` against the file returns at least one match (binary path captured).
    - Grep `Jail list:` against the file returns at least one match (status output captured).
    - Grep `Currently banned:` against the file returns at least one match (per-jail status captured).
    - Grep `^who -u` (or equivalent header) against the file returns at least one match (active-sessions fixture captured).
    - Grep `dpkg -l \| grep -i fail2ban` against the file returns at least one match (version pin captured).
    - File contains the literal string `D-NO-SERVER4` confirming Server4/5 hard-rule was respected.
    - File contains the string `Plan 02 contract` section (downstream consumer contract documented).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "Test-Path .planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md -PathType Leaf -ErrorAction Stop; if (-not (Select-String -Path .planning/phases/46-fail2ban-admin-panel/46-01-DIAGNOSTIC.md -Pattern 'which fail2ban-client','Jail list:','Currently banned:','D-NO-SERVER4','Plan 02 contract' -Quiet)) { exit 1 }"</automated>
  </verify>
  <done>46-01-DIAGNOSTIC.md exists with verbatim Mini PC fixtures (path, version, status, per-jail status, who -u), Plan 02 contract section names the binary path Plan 02 must hardcode, D-NO-SERVER4 acknowledged in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini PC SSH ↔ planner | Read-only — no commands that modify Mini PC state (no `set`, no `unbanip`, no `banip`, no `apt install`) |
| Diagnostic file ↔ git history | The captured `who -u` output may contain admin's source IP — already public knowledge in fail2ban logs; not a new disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | I (Information Disclosure) | 46-01-DIAGNOSTIC.md committed to git | accept | Output contains real Mini PC IPs (from `who -u`) — same IPs already appear in fail2ban logs and `/var/log/auth.log`; committing them adds no new disclosure beyond what an attacker who has repo read-access already learns from existing v29.3 broker tests. No secrets, no PII, no API keys. |
| T-46-02 | T (Tampering) | accidental write to Mini PC during diagnostic | mitigate | Task action enumerates ONLY read-only commands (`which`, `dpkg -l`, `systemctl status`, `fail2ban-client status` — NEVER `set`, NEVER `unbanip`/`banip`). `systemctl status` is read-only despite the name. |
| T-46-03 | D (Denial of Service) | accidental ssh to Server4/Server5 | mitigate | Task action explicitly cites D-NO-SERVER4 hard rule from MEMORY.md and pins `bruce@10.69.31.68` as sole target host. |
</threat_model>

<verification>
- File exists at the expected path.
- File contains all 6 required sections (path, version, systemctl status, jail list, per-jail status, who -u).
- File contains the Plan 02 contract section so the downstream plan has unambiguous inputs.
- D-NO-SERVER4 acknowledged in the file body.
</verification>

<success_criteria>
Diagnostic artifact captured. Plan 02 has verbatim fail2ban + who -u fixtures to paste into parser tests. Binary path and apt version are pinned for Plan 02 client.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-fail2ban-admin-panel/46-01-SUMMARY.md` summarizing:
- Mini PC binary path captured (echoed)
- fail2ban version captured (echoed)
- Jails discovered (echoed list)
- Number of fixture sections in DIAGNOSTIC.md (count)
- Confirmation that NO write commands were issued to the Mini PC
</output>
</content>
</invoke>