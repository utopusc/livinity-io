---
phase: 46-fail2ban-admin-panel
plan: 05
type: execute
wave: 5
depends_on: [46-04]
files_modified:
  - nexus/packages/core/package.json
  - .planning/phases/46-fail2ban-admin-panel/46-UAT.md
autonomous: true
requirements:
  - FR-F2B-01
  - FR-F2B-02
  - FR-F2B-03
  - FR-F2B-04
  - FR-F2B-05
  - FR-F2B-06
user_setup: []
must_haves:
  truths:
    - "nexus/packages/core/package.json has new test:phase46 npm script chaining test:phase45 + 4 new fail2ban-admin tests"
    - "Running `npm run test:phase46` from nexus/packages/core/ exits 0"
    - "test:phase46 transitively runs every Phase 39-46 test (no regression in earlier phases)"
    - "46-UAT.md exists with manual SSH-self-ban-recovery walkthrough on Mini PC"
    - "46-UAT.md covers all 8 ROADMAP success criteria + all 6 pitfall mitigations + 3 service-state banners"
    - "46-UAT.md explicitly references D-NO-SERVER4 hard rule (Mini PC bruce@10.69.31.68 only)"
  artifacts:
    - path: "nexus/packages/core/package.json"
      provides: "test:phase46 npm script extending Phase 45 chain"
      contains: "test:phase46"
    - path: ".planning/phases/46-fail2ban-admin-panel/46-UAT.md"
      provides: "Manual UAT walkthrough — 9 sections covering jail discovery, unban+whitelist, self-ban gate, cellular toggle, audit log, Settings backout, service-state banners, restart-livinityd-mid-session, end-to-end SSH lockout recovery"
  key_links:
    - from: "package.json test:phase46"
      to: "Phase 45's test:phase45"
      via: "npm run test:phase45 && tsx ... 4 new fail2ban tests"
      pattern: "test:phase46.*test:phase45"
    - from: "46-UAT.md"
      to: "all 6 FR-F2B requirements + pitfalls B-01/B-02/B-03/B-04/B-05/B-19"
      via: "Each UAT section maps to specific FR + pitfall + verifies the user-visible behavior"
      pattern: "FR-F2B-(0[1-6])|B-(01|02|03|04|05|19)"
---

<objective>
Wave 5 — gate + UAT. Wire the new fail2ban-admin tests into the `test:phase46` master gate (extending Phase 45's `test:phase45` chain), and produce the manual UAT walkthrough that exercises end-to-end SSH-lockout recovery on the Mini PC. The UAT is the integration coverage that pitfall W-20 said `vi.mock` could not provide.

Purpose: Closes ROADMAP §46 success criterion #6 (npm run test:phase46 passes including chained Phase 45) and the v29.4 milestone-level gate that every phase has a master test script. The UAT addresses the items deferred from automated testing per W-20:
- restart-livinityd-mid-session integration (deferred from Plan 03's integration.test.ts)
- real fail2ban-client text-format drift detection (Plan 02's parser fixtures are point-in-time; UAT catches drift)
- mobile cellular UX validation (FR-F2B-05 + B-19)
- visual confirmation of all 4 service-state banners (FR-F2B-01 + B-04)

Output: `package.json` updated with new script; `46-UAT.md` with 9 sections covering the milestone close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-fail2ban-admin-panel/46-CONTEXT.md
@.planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md
@.planning/phases/46-fail2ban-admin-panel/46-01-SUMMARY.md
@.planning/phases/46-fail2ban-admin-panel/46-02-SUMMARY.md
@.planning/phases/46-fail2ban-admin-panel/46-03-SUMMARY.md
@.planning/phases/46-fail2ban-admin-panel/46-04-SUMMARY.md
@.planning/research/v29.4-PITFALLS.md

@nexus/packages/core/package.json
@.planning/milestones/v29.3-phases/42-broker-openai-translation/42-UAT.md
@.planning/milestones/v29.3-phases/44-per-user-usage-dashboard/44-UAT.md

<interfaces>
<!-- Existing nexus/packages/core/package.json test:phase45 line shape (Phase 45 Plan 04 output): -->
"test:phase45": "npm run test:phase44 && tsx ../../../livos/packages/livinityd/source/modules/livinity-broker/integration.test.ts && tsx ../../../livos/packages/livinityd/source/modules/server/trpc/common.test.ts && tsx ../../../livos/packages/livinityd/source/modules/livinity-broker/openai-sse-adapter.test.ts"

<!-- New entry to add (after test:phase45 line): -->
"test:phase46": "npm run test:phase45 && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/parser.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/client.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/active-sessions.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/integration.test.ts"

<!-- Note: common.test.ts is ALREADY in the test:phase45 chain (Plan 03 of Phase 45 added it).  -->
<!-- Plan 03 of Phase 46 EXTENDED common.test.ts (added 4 assertions for fail2ban entries). -->
<!-- Re-running test:phase45 transitively re-runs the now-extended common.test.ts and validates new assertions. -->
<!-- DO NOT duplicate common.test.ts in test:phase46 line. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test:phase46 npm script chaining test:phase45 + 4 fail2ban tests</name>
  <files>nexus/packages/core/package.json</files>
  <read_first>
    - nexus/packages/core/package.json (find existing test:phase45 line — it's the anchor for insertion)
    - .planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md (lines 648-655 — exact test:phase46 script format)
  </read_first>
  <action>
    Step 1: Read `nexus/packages/core/package.json` to locate the exact existing `test:phase45` line. Capture surrounding context.

    Step 2: Use Edit tool to add a new line IMMEDIATELY AFTER the existing `test:phase45` entry. The new line:
    ```json
    "test:phase46": "npm run test:phase45 && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/parser.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/client.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/active-sessions.test.ts && tsx ../../../livos/packages/livinityd/source/modules/fail2ban-admin/integration.test.ts",
    ```
    - Trailing comma if there are entries below; no trailing comma if it's the last entry.
    - Match indentation of surrounding entries exactly.
    - Do NOT include `common.test.ts` in this chain — it is already in the transitive `test:phase45` chain (Phase 45 Plan 03 added it). Re-running test:phase45 transitively re-asserts our new entries via the extended common.test.ts.

    Step 3: Run `npm run test:phase46` from `nexus/packages/core/` directory. Confirm:
    - Exit code 0.
    - Output includes Phase 45's tracker count (38/38) + Phase 46's new ones (14/14 + 13/13 + 4/4 + 10/10 = 41 new + 38 inherited = 79 total).
    - No regressions in any earlier-phase test.

    Step 4: If the command fails:
    - Capture stderr.
    - Diagnose: typecheck error → flag for backout; test failure in parser → fixture drift (re-run Plan 01 diagnostic capture); test failure in integration → backend wiring issue (revisit Plan 03).
    - DO NOT modify production code in this task — failure means a prior plan needs revision; flag and stop.
  </action>
  <acceptance_criteria>
    - `nexus/packages/core/package.json` contains the literal string `test:phase46`.
    - The test:phase46 line begins with `npm run test:phase45 &&` (chains to prior gate).
    - The test:phase46 line references all 4 fail2ban-admin test files: parser.test.ts, client.test.ts, active-sessions.test.ts, integration.test.ts.
    - The test:phase46 line does NOT duplicate common.test.ts (transitively included via test:phase45).
    - Running `cd nexus/packages/core && npm run test:phase46` exits 0.
    - stdout contains the literal strings `parser.test.ts tests passed`, `client.test.ts tests passed`, `active-sessions.test.ts tests passed`, `integration.test.ts tests passed` (4 confirmations from new tests + transitive Phase 45 + earlier).
  </acceptance_criteria>
  <verify>
    <automated>cd nexus/packages/core && npm run test:phase46</automated>
  </verify>
  <done>Master gate green; all Phase 46 backend tests + transitive Phase 45/44/43/42/41/40/39 tests pass; ROADMAP §46 success criterion #6 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Write 46-UAT.md — 9 sections covering end-to-end SSH lockout recovery</name>
  <files>.planning/phases/46-fail2ban-admin-panel/46-UAT.md</files>
  <read_first>
    - .planning/milestones/v29.3-phases/42-broker-openai-translation/42-UAT.md (analog: 9-section UAT structure with explicit pre-conditions + step-by-step + expected output)
    - .planning/milestones/v29.3-phases/44-per-user-usage-dashboard/44-UAT.md (analog: more recent UAT format)
    - .planning/REQUIREMENTS.md (FR-F2B-01..06 — every requirement maps to a UAT section)
    - .planning/research/v29.4-PITFALLS.md sections B-01, B-02, B-03, B-04, B-05, B-19, W-04, W-19 (every BLOCKER pitfall must be covered by a UAT step)
    - .planning/ROADMAP.md (Phase 46 success criteria 1-8 — UAT is the manual verification path for every criterion)
    - $HOME/.claude/projects/C--Users-hello-Desktop-Projects-contabo-livinity-io/memory/MEMORY.md (Mini PC SSH command + D-NO-SERVER4 hard rule)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/46-fail2ban-admin-panel/46-UAT.md` with the following EXACT structure (UAT is a planning artifact, not a status report — the no-md-files rule does not apply here per project convention which already has 5 prior phase UATs):

    ```markdown
    # Phase 46 UAT — Fail2ban Admin Panel (Manual)

    **Status:** un-executed (deferred to next Mini PC deploy per v29.3 UAT pattern)
    **Host:** bruce@10.69.31.68 — Mini PC ONLY (D-NO-SERVER4 — Server4/Server5 OFF-LIMITS)
    **Time budget:** ~25-35 min walkthrough
    **Pre-requirement:** test:phase46 npm gate green; pnpm --filter ui build clean
    **SSH command (per project memory):**
    \`\`\`
    /c/Windows/System32/OpenSSH/ssh.exe -i C:/Users/hello/Desktop/Projects/contabo/pem/minipc bruce@10.69.31.68
    \`\`\`

    ## Pre-flight (5 min)

    1. Deploy Phase 46 to Mini PC: `bash /opt/livos/update.sh` (NOT pm2 reset).
    2. Verify all four services running: `systemctl status livos liv-core liv-worker liv-memory --no-pager` — note `liv-memory` may already be in restart loop (pre-existing v29.3 issue, NOT a Phase 46 regression).
    3. Verify fail2ban running: `systemctl status fail2ban --no-pager`.
    4. Verify Phase 45 carry-forwards still green: `cd /opt/livos/livos/livinityd && npm run test:phase45 || true` (or run on local clone — test:phase45 chain).
    5. Open https://bruce.livinity.io/ in browser logged in as admin user.

    ## Section 1 — FR-F2B-01: Sidebar registration + jail discovery (5 min)

    **Maps to:** FR-F2B-01, ROADMAP §46 success #1, sub-issue #1 (UI under routes/docker/security/), pitfalls W-03 (auto-discover jails) + W-04 (binary-detection states)

    Steps:
    1. Open Server Management → expect "Security" entry as the 13th sidebar item, between "Activity" and "Schedules".
    2. Click "Security" → section renders without errors.
    3. Verify discovered jails appear as tabs (NOT hardcoded `sshd`). For Mini PC default install, expected: `sshd`, `recidive` (or whatever was captured in `46-01-DIAGNOSTIC.md`).
    4. Watch the network tab — `fail2ban.listJails` query fires every ~5s.
    5. Click "Refresh" button → `listJails` + `getJailStatus` invalidated and re-fetched immediately.

    PASS: 13th sidebar item visible, jails discovered, 5s polling observed, manual Refresh works.

    ## Section 2 — FR-F2B-01 + B-04 + W-04: Three service-state banners (5 min)

    **Maps to:** ROADMAP §46 success #6, pitfall W-04 (anti-fragile fail2ban-client missing detection)

    Sub-test 2a — service-inactive:
    1. SSH to Mini PC. Run `sudo systemctl stop fail2ban`.
    2. Wait 6s. Refresh UI.
    3. Expect yellow banner: "Fail2ban service is stopped."
    4. SSH: `sudo systemctl start fail2ban`. Refresh. Expect banner clears.

    Sub-test 2b — binary-missing simulation:
    1. SSH: `sudo mv /usr/bin/fail2ban-client /usr/bin/fail2ban-client.bak` (ALSO run `sudo systemctl stop fail2ban` to ensure no zombie process).
    2. Refresh UI. Expect red banner: "Fail2ban not installed..."
    3. SSH: `sudo mv /usr/bin/fail2ban-client.bak /usr/bin/fail2ban-client && sudo systemctl start fail2ban`. Refresh. Expect banner clears.

    Sub-test 2c — no-jails-configured (skip if Mini PC has multiple jails configured — synthesize via temp config swap if curious):
    1. Optional: SSH and `mv /etc/fail2ban/jail.local /etc/fail2ban/jail.local.bak; systemctl restart fail2ban`. Wait 6s. Refresh.
    2. Expect yellow banner: "Fail2ban running but no jails configured."
    3. Restore: `mv /etc/fail2ban/jail.local.bak /etc/fail2ban/jail.local; systemctl restart fail2ban`.

    PASS: All 3 banner states render with correct copy + correct color.

    ## Section 3 — FR-F2B-02 + B-01: Unban + whitelist (5 min)

    **Maps to:** FR-F2B-02, ROADMAP §46 success #2, pitfall B-01 (action-targeted unban prevents re-cycle)

    Steps:
    1. SSH to Mini PC. Manually ban a fake IP: `sudo fail2ban-client set sshd banip 192.0.2.99`.
    2. UI: Security → sshd jail → expect 192.0.2.99 in banned list within 5s.
    3. Click "Unban" on the row → modal opens with title "Unban 192.0.2.99 from sshd".
    4. Verify modal contains the inline note: "After unban, fail2ban may re-ban this IP if connection attempts continue with bad credentials. Verify your SSH key is correct first."
    5. Verify "Add to ignoreip whitelist" checkbox is UNCHECKED by default.
    6. Click "Unban" without checking whitelist. Button greys out for ~5s (W-01 debounce).
    7. SSH: `sudo fail2ban-client status sshd | grep "Banned IP list"` → 192.0.2.99 should NOT appear.
    8. SSH: `sudo fail2ban-client get sshd ignoreip` → 192.0.2.99 should NOT be in ignore list (because whitelist box was unchecked).
    9. Re-ban: `sudo fail2ban-client set sshd banip 192.0.2.100`.
    10. UI: Click Unban on .100 → CHECK "Add to ignoreip whitelist" → confirm.
    11. SSH: `sudo fail2ban-client get sshd ignoreip` → 192.0.2.100 SHOULD appear.

    PASS: Unban removes IP from banned list; whitelist checkbox extends with addignoreip when checked.

    ## Section 4 — FR-F2B-03 + B-02 + B-03: Manual ban + self-ban gate (4 min)

    **Maps to:** FR-F2B-03, ROADMAP §46 success #3, pitfalls B-02 (LOCK ME OUT gate) + B-03 (CIDR rejection)

    Sub-test 4a — Normal ban:
    1. UI: Security → "Ban an IP" button → modal opens.
    2. Type IP `198.51.100.42` (TEST-NET-2 — guaranteed not your real IP). Select jail `sshd`. Confirm.
    3. SSH: `sudo fail2ban-client status sshd` → 198.51.100.42 in banned list.
    4. Cleanup: `sudo fail2ban-client set sshd unbanip 198.51.100.42`.

    Sub-test 4b — Self-ban gate (B-02):
    1. SSH and run `who -u` → note your active SSH source IP (e.g. 203.0.113.5).
    2. UI: "Ban an IP" → type that exact IP → Confirm.
    3. Expect modal Stage 2: "WARNING: 203.0.113.5 is YOUR CURRENT CONNECTION IP." + Input field requiring `LOCK ME OUT`.
    4. Type `lock me out` (lowercase) → Confirm button stays disabled (literal-string strict equality).
    5. Type `LOCK ME OUT` (exact) → Confirm enabled. Click Confirm. (DO NOT actually confirm — Cancel instead — to avoid actually banning yourself.)

    Sub-test 4c — CIDR rejection (B-03):
    1. UI: "Ban an IP" → type `0.0.0.0/0` → Confirm.
    2. Expect: client-side error "IPv4 dotted-quad only — no CIDR allowed" — mutation does NOT fire (network tab confirms).
    3. Type `1.2.3.4/8` → same rejection.

    PASS: Normal ban works; self-ban triggers Stage 2 modal; CIDR rejected before mutation fires.

    ## Section 5 — FR-F2B-04: Audit log immutability (3 min)

    **Maps to:** FR-F2B-04, ROADMAP §46 success #4, REUSE of device_audit_log (NO new table)

    Steps:
    1. UI: Security → Audit Log tab → expect rows from Sections 3+4 (unban, whitelist, ban events).
    2. Verify columns: When (relative time), Action (ban/unban/whitelist Badge), Jail, IP, Admin (your username), Result.
    3. SSH to Mini PC: `psql -U livos -d livos -c "SELECT device_id, tool_name, params_digest FROM device_audit_log WHERE device_id = 'fail2ban-host' ORDER BY timestamp DESC LIMIT 5"`.
    4. Verify rows have `device_id='fail2ban-host'` (sentinel), `tool_name IN ('unban_ip','ban_ip','whitelist_ip')`, `params_digest` is 64-char hex SHA-256.
    5. Try to UPDATE: `UPDATE device_audit_log SET tool_name='tampered' WHERE device_id='fail2ban-host'` → expect trigger error (immutability — v22.0 Phase 15 trigger).
    6. Verify JSON belt-and-suspenders: `ls -la /opt/livos/data/security-events/ | head -10` → recent JSON files match Section 3+4 events.

    PASS: Audit rows present in PG; immutability trigger blocks UPDATE; JSON files written; sentinel device_id correct; NO new table created (verify via `\dt` — only `device_audit_log` exists, no `fail2ban_audit_log`).

    ## Section 6 — FR-F2B-05 + B-19: Mobile cellular toggle (3 min)

    **Maps to:** FR-F2B-05, ROADMAP §46 success #5, pitfall B-19 (CGNAT mismatch)

    Steps:
    1. Open https://bruce.livinity.io on a phone connected via cellular (4G/5G — NOT WiFi).
    2. Login as admin. Open Security section.
    3. Verify "I'm on cellular" toggle is visible and OFF by default.
    4. Toggle ON.
    5. Click "Ban an IP" → type your phone's WiFi router IP (which is NOT your current cellular IP).
    6. Confirm — should succeed without Stage 2 self-ban gate (cellular bypass suppressed the check).
    7. Cleanup: unban that IP via UI.
    8. Toggle cellular OFF → repeat ban with same IP → still no self-ban gate (because the IP doesn't match your CURRENT IP — cellular CGNAT IP).
    9. Now type your CURRENT cellular IP (visible via `curl ifconfig.me` from a terminal app on the phone OR check upstream session logs).
    10. With cellular OFF, expect Stage 2 self-ban gate. With cellular ON, expect no gate (B-19 bypass suppresses the check).

    PASS: Cellular toggle suppresses self-ban detection; non-cellular admin still gets the gate.

    ## Section 7 — FR-F2B-06: Settings backout toggle (2 min)

    **Maps to:** FR-F2B-06, ROADMAP §46 success #7, sub-issue #4 (non-destructive backout)

    Steps:
    1. UI: Settings → find "Security panel" toggle row.
    2. Verify toggle is ON by default.
    3. Click toggle OFF.
    4. Navigate to Server Management → Sidebar → "Security" entry should DISAPPEAR (no auto-redirect — sub-issue #4).
    5. Open Network tab — confirm fail2ban itself is NOT uninstalled (`fail2ban` package still on Mini PC; `systemctl status fail2ban` still active).
    6. Refresh. Sidebar still hides Security.
    7. Toggle back ON. Sidebar entry returns.
    8. SSH to Mini PC: `psql -U livos -d livos -c "SELECT * FROM user_preferences WHERE key='security_panel_visible'"` → expect row with bool value matching current state.

    PASS: Toggle hides UI; fail2ban still running on Mini PC; preference persisted.

    ## Section 8 — ROADMAP §46.8 + B-12 + X-04: httpOnlyPaths restart-livinityd-mid-session (5 min)

    **Maps to:** ROADMAP §46 success #8, pitfalls B-12 + X-04 (WS hang under reconnect)

    Steps:
    1. UI: Open Security panel + open browser DevTools Network tab.
    2. SSH to Mini PC: `sudo systemctl restart livos`.
    3. UI immediately (within 1s): click "Ban an IP" → type a fake IP → Confirm.
    4. Watch network tab — POST to `/api/trpc/fail2ban.banIp` should fire (HTTP, NOT WS frame).
    5. Mutation completes within ~2s of livinityd restart finishing.
    6. Repeat with Unban: SSH banip a fake IP, refresh UI, restart livos again, immediately click Unban → mutation completes.

    PASS: Both mutations resolve under WS reconnect; HTTP transport visible in network tab.

    ## Section 9 — End-to-End SSH Lockout Recovery (5 min)

    **Maps to:** Phase 46's headline value proposition — operator goes from "I'm SSH-locked-out" to "I'm back in" via UI alone.

    Steps:
    1. (Optional dry-run — skip if you only have one admin account):
    2. SSH to Mini PC. Force a self-ban: `sudo fail2ban-client set sshd banip <YOUR-WORK-LAPTOP-IP>` (use a different machine's IP than the one you're connecting from).
    3. From the BANNED machine, attempt `ssh bruce@10.69.31.68` → expect connection refused.
    4. From your SSH-connected admin device (different IP, NOT banned), open https://bruce.livinity.io.
    5. Login as admin. Open Security. Click Unban on the banned IP. CHECK "Add to ignoreip whitelist".
    6. From the formerly-banned machine, retry `ssh bruce@10.69.31.68` → expect successful connection.
    7. Verify whitelist persists: `sudo fail2ban-client get sshd ignoreip` shows the IP.

    PASS: Operator recovered SSH access via UI without console/physical access to Mini PC.

    ## Closing checklist

    - [ ] All 9 sections passed.
    - [ ] No regressions in earlier-phase functionality (`test:phase45` chain still green).
    - [ ] sacred file `nexus/packages/core/src/sdk-agent-runner.ts` byte-identical (`git diff --shortstat` empty).
    - [ ] D-NO-SERVER4 honored throughout — Server4/Server5 NEVER touched.
    - [ ] Mark `46-UAT.md` as `Status: COMPLETE` in the header and stamp the run date.

    ## Deferred from this UAT (not blockers)

    - Containerized integration test for fail2ban (W-20 mitigation strategy 2 chosen — UAT is the integration coverage).
    - geo-IP / ASN enrichment for SSH viewer (FR-SSH-future-01 — Phase 48 + v30+).
    - Bulk unban (FR-F2B-future-01 — anti-feature per research).
    ```

    Use the Write tool to create the file with this exact content.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/46-fail2ban-admin-panel/46-UAT.md` exists.
    - File contains 9 numbered sections (`## Section 1` through `## Section 9` + Pre-flight + Closing).
    - Each section header references the FR-F2B requirement(s) and pitfall ID(s) it covers.
    - File contains the literal string `D-NO-SERVER4` (hard rule acknowledged).
    - File contains the literal string `bruce@10.69.31.68` (Mini PC SSH target).
    - File contains the literal string `LOCK ME OUT` (B-02 type-confirm gate verified in section 4b).
    - File contains the literal string `cellular` (B-19 + FR-F2B-05 in section 6).
    - File contains the literal string `device_audit_log` (FR-F2B-04 + REUSE invariant in section 5).
    - File contains the literal string `httpOnlyPaths` OR `HTTP transport` (ROADMAP §46.8 in section 8).
    - File contains the literal string `binary-missing` AND `service-inactive` AND `no-jails` (or equivalent banner copy) — 3 banner states in section 2.
    - All 6 FR-F2B requirements (`FR-F2B-01` through `FR-F2B-06`) appear in section headers (grep returns 6 matches).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "if (-not (Test-Path .planning/phases/46-fail2ban-admin-panel/46-UAT.md -PathType Leaf)) { exit 1 }; $matches = Select-String -Path .planning/phases/46-fail2ban-admin-panel/46-UAT.md -Pattern 'D-NO-SERVER4','bruce@10.69.31.68','LOCK ME OUT','device_audit_log','cellular','FR-F2B-01','FR-F2B-02','FR-F2B-03','FR-F2B-04','FR-F2B-05','FR-F2B-06' -SimpleMatch -CaseSensitive; if ($matches.Count -lt 11) { Write-Host \"FAIL: only found $($matches.Count) of 11 required strings\"; exit 1 }; Write-Host 'OK: all required strings present'"</automated>
  </verify>
  <done>46-UAT.md written; covers all 6 FR-F2B requirements + all 6 BLOCKER pitfalls (B-01/02/03/04/05/19) + ROADMAP §46.8 httpOnlyPaths + sub-issue #4 non-destructive backout + end-to-end SSH lockout recovery walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json npm script ↔ executor running tests | Same boundary as Phase 45 — chained tsx invocations all run in the same Node context. No new boundary added. |
| 46-UAT.md ↔ git history | UAT instructs operator to ban + unban TEST-NET ranges (RFC 5737) + their own machine IP. Captured IPs are public; same exposure as fail2ban logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-24 | T (Tampering) | accidental SSH to Server4/Server5 during UAT execution | mitigate | UAT explicitly cites D-NO-SERVER4 + pins `bruce@10.69.31.68` as sole target; SSH command provided verbatim from project memory. |
| T-46-25 | D (Denial of Service) | UAT operator self-bans during section 9 dry-run | mitigate | Section 9 explicitly notes "skip if you only have one admin account"; section 4b cancels at the strict-equality typing step (does NOT actually fire the ban); whitelist-after-unban flow (section 3 step 10-11) provides safe recovery. |
| T-46-26 | R (Repudiation) | UAT not run before milestone close | accept | UAT defer-to-deploy is the established v29.3 pattern (5 prior phase UATs un-executed pending deploy). Documented as accepted debt in STATE.md. |
</threat_model>

<verification>
- `cd nexus/packages/core && npm run test:phase46` exits 0.
- `46-UAT.md` exists, has 9 sections, references all 6 FR-F2B requirements + all 6 BLOCKER pitfalls.
- Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` byte-identical (`git diff --shortstat` empty).
- All 5 Phase 46 plans (01-05) in `.planning/phases/46-fail2ban-admin-panel/` complete.
</verification>

<success_criteria>
1. test:phase46 master gate green — chains test:phase45 + 4 new fail2ban tests = ~79+ assertions transitively.
2. UAT covers ALL 8 ROADMAP §46 success criteria + ALL 6 BLOCKER pitfall mitigations + sub-issue #4 backout.
3. UAT honors D-NO-SERVER4 hard rule throughout.
4. Phase 46 milestone-closeable: every code path tested (unit + integration) AND every user-visible behavior has a UAT step.
</success_criteria>

<output>
After completion, create `.planning/phases/46-fail2ban-admin-panel/46-05-SUMMARY.md` with:
- test:phase46 npm script line (echoed verbatim)
- npm run test:phase46 exit code + total assertions tracked
- 46-UAT.md section count + word count
- FR-F2B coverage matrix (6/6 requirements → UAT sections mapped)
- Pitfall coverage matrix (B-01/02/03/04/05/19 → UAT sections mapped)
- Sacred file diff (echoed empty)
- Phase 46 milestone-close readiness checklist (8/8 ROADMAP success criteria met by automated + UAT combination)
</output>
</content>
</invoke>