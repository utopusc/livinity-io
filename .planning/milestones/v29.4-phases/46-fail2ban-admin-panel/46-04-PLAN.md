---
phase: 46-fail2ban-admin-panel
plan: 04
type: execute
wave: 4
depends_on: [46-03]
files_modified:
  - livos/packages/ui/src/routes/docker/store.ts
  - livos/packages/ui/src/routes/docker/docker-app.tsx
  - livos/packages/ui/src/routes/docker/sidebar.tsx
  - livos/packages/ui/src/routes/docker/sections/security.tsx
  - livos/packages/ui/src/routes/docker/security/security-section.tsx
  - livos/packages/ui/src/routes/docker/security/jail-status-card.tsx
  - livos/packages/ui/src/routes/docker/security/unban-modal.tsx
  - livos/packages/ui/src/routes/docker/security/ban-ip-modal.tsx
  - livos/packages/ui/src/routes/docker/security/audit-log-tab.tsx
  - livos/packages/ui/src/routes/settings/_components/security-toggle-row.tsx
autonomous: true
requirements:
  - FR-F2B-01
  - FR-F2B-02
  - FR-F2B-03
  - FR-F2B-04
  - FR-F2B-05
  - FR-F2B-06
user_setup: []
must_haves:
  truths:
    - "Sidebar shows 'Security' as the 13th section inside LIVINITY_docker (between 'activity' and 'schedules')"
    - "Security section renders three distinct service-state banners: binary-missing, service-inactive, no-jails (FR-F2B-01 ROADMAP §46.6)"
    - "Polled jail-status table refreshes every 5s + manual Refresh button (pitfall W-02)"
    - "Unban modal includes 'Add to ignoreip whitelist' checkbox + onConfirm signature passes addToWhitelist boolean (FR-F2B-02)"
    - "Ban-IP modal implements two-stage type-LOCK-ME-OUT gate when backend returns CONFLICT 'self_ban' (FR-F2B-03 + pitfall B-02)"
    - "Ban-IP modal Zod-validates IPv4 dotted-quad client-side BEFORE mutation fires — rejects CIDR /0-/7 (B-03)"
    - "UI surfaces BOTH HTTP X-Forwarded-For source IP AND active SSH session IPs with 'I'm on cellular' toggle (FR-F2B-05 + pitfall B-19)"
    - "Audit log tab uses trpcReact.fail2ban.listEvents.useQuery + renders table with columns When/Action/Jail/IP/Admin/Result (FR-F2B-04)"
    - "Settings 'Show Security panel' toggle row defaults ON; persists to user_preferences key 'security_panel_visible' (FR-F2B-06)"
    - "Toggle hides sidebar entry only — does NOT auto-redirect open section (sub-issue #4 — non-destructive backout)"
    - "pnpm --filter ui build succeeds with zero TypeScript errors"
  artifacts:
    - path: "livos/packages/ui/src/routes/docker/store.ts"
      provides: "SectionId union + SECTION_IDS array now include 'security' (13th)"
      contains: "'security'"
    - path: "livos/packages/ui/src/routes/docker/sidebar.tsx"
      provides: "SECTION_META has 'security' entry; conditional filter on user_preferences"
      contains: "security:.*IconShieldLock"
    - path: "livos/packages/ui/src/routes/docker/docker-app.tsx"
      provides: "Section switch case 'security' returns <SecuritySection />"
      contains: "case 'security'"
    - path: "livos/packages/ui/src/routes/docker/sections/security.tsx"
      provides: "1-line re-export pattern matching activity.tsx + logs.tsx convention"
    - path: "livos/packages/ui/src/routes/docker/security/security-section.tsx"
      provides: "Section root: service-state banner + jail tabs + polling + Refresh button + cellular toggle"
    - path: "livos/packages/ui/src/routes/docker/security/jail-status-card.tsx"
      provides: "Per-jail card: counts table + banned IP rows with Unban actions"
    - path: "livos/packages/ui/src/routes/docker/security/unban-modal.tsx"
      provides: "Unban dialog with whitelist checkbox + 5s click debounce"
    - path: "livos/packages/ui/src/routes/docker/security/ban-ip-modal.tsx"
      provides: "Two-stage ban dialog with type-LOCK-ME-OUT gate"
    - path: "livos/packages/ui/src/routes/docker/security/audit-log-tab.tsx"
      provides: "Audit table querying fail2ban.listEvents"
    - path: "livos/packages/ui/src/routes/settings/_components/security-toggle-row.tsx"
      provides: "Settings toggle row writing user_preferences.security_panel_visible"
  key_links:
    - from: "security-section.tsx"
      to: "trpcReact.fail2ban.listJails"
      via: "useQuery with refetchInterval: 5000, staleTime: 2500"
      pattern: "fail2ban\\.listJails.*useQuery"
    - from: "unban-modal.tsx"
      to: "trpcReact.fail2ban.unbanIp"
      via: "useMutation; onConfirm passes {jail, ip, addToWhitelist}"
      pattern: "fail2ban\\.unbanIp.*useMutation"
    - from: "ban-ip-modal.tsx"
      to: "trpcReact.fail2ban.banIp"
      via: "useMutation; on CONFLICT 'self_ban' → re-render Stage 2 with type-LOCK-ME-OUT input"
      pattern: "fail2ban\\.banIp.*useMutation"
    - from: "audit-log-tab.tsx"
      to: "trpcReact.fail2ban.listEvents"
      via: "useQuery with limit: 50"
      pattern: "fail2ban\\.listEvents.*useQuery"
    - from: "security-toggle-row.tsx"
      to: "trpcReact.preferences.set"
      via: "useMutation key='security_panel_visible'"
      pattern: "security_panel_visible"
    - from: "sidebar.tsx"
      to: "trpcReact.preferences.get"
      via: "useQuery keys=['security_panel_visible']; conditionally hide 'security' from SECTION_IDS"
      pattern: "security_panel_visible"
---

<objective>
Wave 4 — UI surface. Build the Security section under `LIVINITY_docker`'s sidebar, all 4 sub-components (jail-status-card, unban-modal, ban-ip-modal, audit-log-tab), the 3-touchpoint sidebar registration (store / docker-app / sidebar), the section barrel re-export, and the Settings toggle row that persists `security_panel_visible` in `user_preferences`. Total: 7 new files + 3 modified files.

Purpose: Closes FR-F2B-01 (jail-list UI with 5s poll + 4-state service banner), FR-F2B-02 (unban modal with whitelist checkbox + last-attempted-user surface), FR-F2B-03 (manual ban with two-stage type-LOCK-ME-OUT gate + client-side Zod validation rejecting CIDR), FR-F2B-04 (audit log tab querying device_audit_log), FR-F2B-05 (mobile cellular toggle + dual-IP surface), FR-F2B-06 (Settings toggle hiding sidebar entry without uninstall — non-destructive backout per sub-issue #4).

UI lives at `livos/packages/ui/src/routes/docker/security/` per PATTERNS.md sub-issue #1 (NOT the `modules/docker-app/` path the original prompt suggested — corrected to match `routes/docker/{activity,logs}/` convention from Phase 28/33).

Output: pnpm --filter ui build clean; security section renders end-to-end against Plan 03 backend; sidebar visibility coupled to preference toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-fail2ban-admin-panel/46-CONTEXT.md
@.planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md
@.planning/phases/46-fail2ban-admin-panel/46-03-SUMMARY.md
@.planning/research/v29.4-PITFALLS.md

@livos/packages/ui/src/routes/docker/store.ts
@livos/packages/ui/src/routes/docker/docker-app.tsx
@livos/packages/ui/src/routes/docker/sidebar.tsx
@livos/packages/ui/src/routes/docker/activity/activity-section.tsx
@livos/packages/ui/src/routes/docker/activity/use-activity-feed.ts
@livos/packages/ui/src/routes/docker/sections/activity.tsx
@livos/packages/ui/src/routes/docker/stacks/stack-dialogs.tsx
@livos/packages/ui/src/routes/settings/_components/past-deploys-table.tsx
@livos/packages/livinityd/source/modules/user/preferences-routes.ts

<interfaces>
<!-- Existing tRPC routes consumed by this UI plan (Plan 03 output): -->
trpcReact.fail2ban.listJails — query, returns {state: 'binary-missing'|'service-inactive'|'no-jails'|'running', jails: string[], transient: boolean}
trpcReact.fail2ban.getJailStatus — query, input {jail}, returns {currentlyFailed, totalFailed, currentlyBanned, totalBanned, bannedIps: Array<{ip, lastAttemptedUser?, lastAttemptedAt?}>}
trpcReact.fail2ban.unbanIp — mutation, input {jail, ip, addToWhitelist}, returns {ok}
trpcReact.fail2ban.banIp — mutation, input {jail, ip, confirmation?, cellularBypass}, throws TRPCError CONFLICT 'self_ban' on self-ban detection
trpcReact.fail2ban.listEvents — query, input {limit}, returns Array<{ts, action, jail, ip, admin_username, success, error?}>
trpcReact.preferences.get — query, input {keys: string[]}, returns Record<string, unknown>
trpcReact.preferences.set — mutation, input {key, value}, returns {ok}

<!-- Existing UI primitives to reuse (no new libraries — D-NO-NEW-DEPS): -->
- shadcn Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription/DialogFooter
- shadcn Button (variants: default, outline, destructive)
- shadcn Checkbox
- shadcn Input
- shadcn Table/TableBody/TableCell/TableHead/TableHeader/TableRow
- shadcn Badge
- @tabler/icons-react: IconShieldLock (or IconLock if Shield variant unavailable — VERIFY by checking @tabler/icons-react package version's icon list)
- date-fns: formatDistanceToNow, parseISO

<!-- Existing store.ts shape (modify; do NOT replace): -->
```typescript
// store.ts lines 20-32 — SectionId union (currently 12 entries)
export type SectionId =
  | 'dashboard'
  | 'apps'
  | 'stacks'
  | 'images'
  | 'volumes'
  | 'networks'
  | 'logs'
  | 'health'
  | 'environment'
  | 'activity'
  // ADD here, after 'activity', before 'schedules':
  | 'security'
  | 'schedules'
  | 'updates'

// store.ts lines 40-53 — SECTION_IDS readonly array
export const SECTION_IDS: readonly SectionId[] = [
  'dashboard',
  // ... existing entries ...
  'activity',
  'security',  // ADD
  'schedules',
  'updates',
] as const
```

<!-- sidebar.tsx existing SECTION_META shape (modify): -->
```typescript
// sidebar.tsx lines 52-65
const SECTION_META: Record<SectionId, {icon: ..., label: string, comingPhase?: number}> = {
  dashboard: {icon: IconDashboard, label: 'Dashboard'},
  // ...
  activity: {icon: IconActivity, label: 'Activity'},
  // ADD:
  security: {icon: IconShieldLock, label: 'Security'},
  schedules: {...},
  updates: {...},
}
```

<!-- docker-app.tsx existing switch shape (modify): -->
```typescript
// docker-app.tsx lines 64-91 SectionView switch
function SectionView({section}: {section: SectionId}) {
  switch (section) {
    case 'dashboard': return <DashboardSection />
    // ... existing cases ...
    case 'activity': return <ActivitySection />
    // ADD:
    case 'security': return <SecuritySection />
    // ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar registration + section barrel + 4 sub-components (jail-status / unban / ban / audit-log)</name>
  <files>
    livos/packages/ui/src/routes/docker/store.ts,
    livos/packages/ui/src/routes/docker/docker-app.tsx,
    livos/packages/ui/src/routes/docker/sidebar.tsx,
    livos/packages/ui/src/routes/docker/sections/security.tsx,
    livos/packages/ui/src/routes/docker/security/jail-status-card.tsx,
    livos/packages/ui/src/routes/docker/security/unban-modal.tsx,
    livos/packages/ui/src/routes/docker/security/ban-ip-modal.tsx,
    livos/packages/ui/src/routes/docker/security/audit-log-tab.tsx
  </files>
  <read_first>
    - livos/packages/ui/src/routes/docker/store.ts (lines 20-53 — exact insertion anchor for SectionId + SECTION_IDS)
    - livos/packages/ui/src/routes/docker/sidebar.tsx (lines 52-65 — SECTION_META + existing icon import pattern)
    - livos/packages/ui/src/routes/docker/docker-app.tsx (lines 16-33 imports + 64-91 switch)
    - livos/packages/ui/src/routes/docker/sections/activity.tsx (1-line re-export analog)
    - livos/packages/ui/src/routes/docker/stacks/stack-dialogs.tsx (lines 25-76 — Dialog + Checkbox analog for unban-modal)
    - livos/packages/ui/src/routes/settings/_components/past-deploys-table.tsx (analog for audit-log-tab table render)
    - .planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md (lines 416-571 — UI pattern assignments + 552-560 — file location decision)
  </read_first>
  <action>
    Step 1: Modify `store.ts`:
    - Add `'security'` to SectionId union BETWEEN `'activity'` and `'schedules'` (operator-cluster ordering — security sits next to activity timeline).
    - Add `'security'` to SECTION_IDS readonly array in same position.
    - Use Edit tool with strict context anchoring (read first, then surgical change).

    Step 2: Modify `sidebar.tsx`:
    - Add icon import. Try `IconShieldLock` from `@tabler/icons-react`. If unavailable in installed version (verify via `node_modules/@tabler/icons-react/package.json` or grep the existing icon imports), fall back to `IconLock`. Pick whichever is present and add it as new import line near existing tabler imports.
    - Add SECTION_META entry: `security: {icon: IconShieldLock, label: 'Security'}` (label is 'Security' NOT 'Fail2ban' per architecture research).
    - Add visibility check: read `trpcReact.preferences.get.useQuery({keys: ['security_panel_visible']})`. Treat undefined OR true as visible (default-ON per FR-F2B-06). Filter SECTION_IDS via `useMemo` so 'security' is removed when preference === false.
    - Per sub-issue #4: do NOT auto-redirect when toggle flips off. Just hide sidebar entry. Open section continues displaying.

    Step 3: Modify `docker-app.tsx`:
    - Add import: `import {SecuritySection} from './security/security-section'` (Plan's Task 2 will create this file — placeholder export OK now).
    - Add switch case: `case 'security': return <SecuritySection />`.

    Step 4: Create `sections/security.tsx` 1-liner:
    ```tsx
    export {SecuritySection} from '../security/security-section'
    ```

    Step 5: Create `security/jail-status-card.tsx`:
    - Props: `{jail: string, status: JailStatus, onUnbanClick: (ip: string) => void}`.
    - Renders shadcn Table with columns: Banned IP | Last Attempted User | Last Attempt | Action.
    - Counts header: 4 chips showing currentlyFailed / totalFailed / currentlyBanned / totalBanned.
    - Each banned IP row has a "Unban" Button (variant='outline' size='sm') calling `onUnbanClick(ip)`.
    - Empty state: "No IPs currently banned in {jail}."
    - Last-attempted-user column gracefully renders `—` when field is undefined (sub-issue #3 — backend may not always supply it).

    Step 6: Create `security/unban-modal.tsx`:
    - Props: `{open, onOpenChange, ip, jail, lastAttemptedUser?, lastAttemptedAt?, onConfirm: (addToWhitelist: boolean) => void, isUnbanning: boolean}`.
    - Mirror RemoveStackDialog from stack-dialogs.tsx exactly:
      - DialogTitle: `Unban {ip} from {jail}`.
      - DialogDescription: includes last-attempt-at (formatDistanceToNow if present) + last-attempted-user (gracefully omits when undefined).
      - Inline note (per pitfall B-01): "After unban, fail2ban may re-ban this IP if connection attempts continue with bad credentials. Verify your SSH key is correct first."
      - Checkbox: "Add to ignoreip whitelist (prevents re-ban from this IP)" — defaults UNCHECKED. Local state `addToWhitelist`.
      - Footer: Cancel + Unban button (variant='destructive'). On click: `onConfirm(addToWhitelist)`. Disable button for 5s after click using `useState<boolean>(false)` + `setTimeout` (pitfall W-01 — prevent double-fire).
    - On submit, button label switches to "Unbanning…".

    Step 7: Create `security/ban-ip-modal.tsx` (two-stage with type-LOCK-ME-OUT gate per pitfall B-02):
    - Props: `{open, onOpenChange, jails: string[], onSubmit: (input: {jail, ip, confirmation?, cellularBypass}) => Promise<void>, currentHttpIp: string | null, activeSshIps: string[], isCellular: boolean}`.
    - Stage 1 (NORMAL ban):
      - Inputs: shadcn Input for IP, shadcn Select for jail (from `jails` prop).
      - Client-side Zod validation (defense in depth — backend also validates):
        ```ts
        const ipSchema = z.string().trim().refine(
          (s) => /^(?:25[0-5]|2[0-4]\d|[01]?\d\d?)(?:\.(?:25[0-5]|2[0-4]\d|[01]?\d\d?)){3}$/.test(s),
          'IPv4 dotted-quad only — no CIDR allowed (pitfall B-03)'
        )
        ```
      - Display: "Your HTTP request came from: {currentHttpIp ?? 'unknown'}" + "Active SSH sessions from: {activeSshIps.join(', ') || '(none)'}".
      - "I'm on cellular" checkbox surfaces on top of the modal (FR-F2B-05).
      - Submit calls `onSubmit({jail, ip, cellularBypass: isCellular})`.
    - Stage 2 (SELF-BAN — only when onSubmit's call rejects with TRPCError CONFLICT 'self_ban'):
      - Re-render with destructive copy: `WARNING: {ip} is YOUR CURRENT CONNECTION IP. Banning will lock you out.`
      - shadcn Input bound to local state `confirmText`. Compare `confirmText === 'LOCK ME OUT'` strict equality.
      - Confirm Ban button disabled until match.
      - On confirm: re-call `onSubmit({jail, ip, confirmation: 'LOCK ME OUT', cellularBypass: isCellular})`.
    - Use `try/catch` around onSubmit to detect TRPC error code+message. If `err?.data?.code === 'CONFLICT' && err?.message === 'self_ban'` → set `stage: 'self-ban'` state.

    Step 8: Create `security/audit-log-tab.tsx`:
    - `const eventsQuery = trpcReact.fail2ban.listEvents.useQuery({limit: 50})`.
    - Mirror past-deploys-table.tsx structure: shadcn Table with columns When (relative via `formatDistanceToNow(parseISO(ts))`), Action (Badge variant tied to ban/unban/whitelist), Jail, IP, Admin (admin_username), Result (success/failed Badge).
    - Empty state: "No ban/unban events recorded yet."
    - Loading state: "Loading…" placeholder.
    - Use Plan 03's `trpcReact.fail2ban.listEvents` route (admin-gated).
  </action>
  <acceptance_criteria>
    - `store.ts` contains `'security'` in BOTH the SectionId union AND the SECTION_IDS array (grep returns 2+ matches in this file).
    - `sidebar.tsx` contains literal `security:` in SECTION_META AND uses `IconShieldLock` (or `IconLock` if Shield variant unavailable — flag in summary).
    - `sidebar.tsx` references `security_panel_visible` (preference visibility check — FR-F2B-06).
    - `docker-app.tsx` contains `case 'security'` in section switch.
    - `sections/security.tsx` exists and is 1-line re-export.
    - All 4 sub-component files exist under `routes/docker/security/`.
    - `unban-modal.tsx` contains the literal string `addToWhitelist` (FR-F2B-02 whitelist checkbox).
    - `unban-modal.tsx` contains the inline note about "fail2ban may re-ban" (B-01 user education).
    - `ban-ip-modal.tsx` contains the literal string `'LOCK ME OUT'` (B-02 type-confirm gate).
    - `ban-ip-modal.tsx` contains a regex matching IPv4-only (no CIDR — B-03).
    - `ban-ip-modal.tsx` contains `cellularBypass` (B-19 mitigation).
    - `ban-ip-modal.tsx` contains `'self_ban'` (handles CONFLICT response).
    - `audit-log-tab.tsx` calls `trpcReact.fail2ban.listEvents.useQuery` (FR-F2B-04).
    - `pnpm --filter ui build` exits 0 (TypeScript compiles).
    - Sacred file untouched.
  </acceptance_criteria>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <done>13th section ('security') registered in 3 touchpoints; barrel re-export matches activity/logs convention; 4 sub-components built; B-01/B-02/B-03/B-19 + FR-F2B-02/03/04/05/06 mitigations encoded; ui builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: security-section.tsx top-level orchestration + Settings toggle row</name>
  <files>
    livos/packages/ui/src/routes/docker/security/security-section.tsx,
    livos/packages/ui/src/routes/settings/_components/security-toggle-row.tsx
  </files>
  <read_first>
    - livos/packages/ui/src/routes/docker/activity/activity-section.tsx (analog: sticky header + tabs + filter chips + polled body — lines 38-156)
    - livos/packages/ui/src/routes/docker/activity/use-activity-feed.ts (analog: 3-query polling pattern — lines 41-122)
    - livos/packages/ui/src/routes/docker/security/jail-status-card.tsx (Task 1 output — wired in here)
    - livos/packages/ui/src/routes/docker/security/unban-modal.tsx (Task 1 output)
    - livos/packages/ui/src/routes/docker/security/ban-ip-modal.tsx (Task 1 output)
    - livos/packages/ui/src/routes/docker/security/audit-log-tab.tsx (Task 1 output)
    - livos/packages/livinityd/source/modules/user/preferences-routes.ts (Settings preference API)
    - .planning/phases/46-fail2ban-admin-panel/46-PATTERNS.md (lines 421-456 — security-section.tsx layout + service-state banner copy + lines 562-571 — Settings toggle pattern)
    - .planning/research/v29.4-PITFALLS.md sections W-02, W-04, W-05, B-19 (polling cadence, banner states, transient retry, cellular)
  </read_first>
  <action>
    Step 1: Create `security-section.tsx` — the section root that ties everything together:

    Layout (mirror activity-section.tsx):
    - Sticky header with title 'Security' + Refresh button + "I'm on cellular" toggle.
    - Body region:
      a. **Service-state banner** (4 distinct states from `listJails.data.state`):
         - `'binary-missing'`: red banner "Fail2ban not installed. Run /opt/livos/install.sh on Mini PC. Phase 46 install button deferred to Settings — see audit log."
         - `'service-inactive'`: yellow banner "Fail2ban service is stopped. Run `systemctl start fail2ban` on Mini PC."
         - `'no-jails'`: yellow banner "Fail2ban running but no jails configured. See https://www.fail2ban.org/wiki/index.php/MANUAL_0_8.5#Jail_Options"
         - `'running'`: NO banner; show jail tabs.
         - `transient: true`: small inline "Fail2ban restarting…" badge (per pitfall B-05) — does NOT crash, render last-known data faded.
      b. **Tabs of discovered jails** (auto-discover from `listJails.data.jails`; pitfall W-03 — never hardcode `sshd`).
      c. **Selected jail's <JailStatusCard>** with the live status query.
      d. **Audit log tab** at bottom (or as 2nd top-level tab beside "Jails").
      e. **"Ban an IP" floating button** opens BanIpModal.

    Polling (mirror use-activity-feed.ts):
    ```tsx
    const POLL_INTERVAL_MS = 5_000
    const STALE_TIME_MS = 2_500
    const listJailsQuery = trpcReact.fail2ban.listJails.useQuery(undefined, {
      refetchInterval: POLL_INTERVAL_MS,
      staleTime: STALE_TIME_MS,
      retry: false,
    })
    const jailStatusQuery = trpcReact.fail2ban.getJailStatus.useQuery(
      {jail: selectedJail},
      {enabled: !!selectedJail, refetchInterval: POLL_INTERVAL_MS, staleTime: STALE_TIME_MS, retry: false}
    )
    ```
    Manual Refresh button: `queryClient.invalidateQueries(['fail2ban'])`.

    Cellular state:
    - Local `useState<boolean>` for cellular toggle (NOT persisted — session-scoped per FR-F2B-05).
    - Pass into BanIpModal as `isCellular` prop.

    Self-ban detection inputs (FR-F2B-05):
    - Read HTTP request source via existing `trpcReact.session.whoami` or equivalent — if no such route exists, surface NA. (PATTERNS.md notes the request IP is server-derived in routes.ts banIp; the UI's role is to display BOTH IPs, but it can show "Your IP: <will be detected at submit>" if no client-readable route exists.) Acceptable v29.4: display the detected `currentHttpIp` from a new lightweight tRPC route OR show "Detected at submit time" placeholder.
    - Read active SSH sessions via NEW tRPC route OR call backend's existing — for v29.4, the simplest path is: BanIpModal triggers backend self-ban check; backend's CONFLICT response includes `cause.adminIps` which the modal then displays in Stage 2. This avoids needing a separate session-IP route. **Choose this simpler path** to ship within budget; FR-F2B-05's "I'm on cellular" toggle still functions as it suppresses the self-ban check entirely on backend.
    - **Note in code comment:** "v29.4 — IPs surfaced post-detection only (in Stage 2 modal). v30+ may add a `fail2ban.detectAdminIps` query for upfront display."

    Mutation wiring:
    ```tsx
    const unbanMutation = trpcReact.fail2ban.unbanIp.useMutation({
      onSuccess: () => queryClient.invalidateQueries(['fail2ban']),
    })
    const banMutation = trpcReact.fail2ban.banIp.useMutation()  // NO onSuccess invalidate — modal handles flow
    ```

    Step 2: Create `routes/settings/_components/security-toggle-row.tsx`:
    - Props: none — self-contained.
    - Reads `trpcReact.preferences.get.useQuery({keys: ['security_panel_visible']})`. Default is ON (true).
    - Renders a card row mirroring `usage-section.tsx` neighbor pattern:
      - Title: "Security panel"
      - Description: "Show the Security sidebar entry inside Server Management. Toggling off hides the panel without uninstalling fail2ban."
      - shadcn Switch bound to current preference value.
      - On change: `trpcReact.preferences.set.useMutation` with `{key: 'security_panel_visible', value: bool}`.
    - On mutation success: invalidate the get query so sidebar.tsx (Task 1) re-reads.

    Step 3: Verify `pnpm --filter ui build` exits 0 with all wiring complete.
  </action>
  <acceptance_criteria>
    - `security-section.tsx` exists; exports `SecuritySection`.
    - `security-section.tsx` calls `trpcReact.fail2ban.listJails.useQuery` + `trpcReact.fail2ban.getJailStatus.useQuery` (grep for both).
    - `security-section.tsx` contains `refetchInterval: 5_000` (or `5000` — pitfall W-02 5s polling).
    - `security-section.tsx` renders all 4 service-state branches: `'binary-missing'`, `'service-inactive'`, `'no-jails'`, and the running/with-jails happy path (grep for all 3 literal state strings).
    - `security-section.tsx` contains `transient` handling (B-05 — does NOT crash on transient errors).
    - `security-section.tsx` contains "I'm on cellular" toggle (FR-F2B-05).
    - `security-section.tsx` integrates BanIpModal + UnbanModal + AuditLogTab + JailStatusCard.
    - `security-toggle-row.tsx` exists; reads `security_panel_visible` from preferences; defaults to ON.
    - `security-toggle-row.tsx` calls `trpcReact.preferences.set` mutation with key `'security_panel_visible'`.
    - `pnpm --filter ui build` exits 0.
    - Sacred file untouched: `git diff --shortstat nexus/packages/core/src/sdk-agent-runner.ts` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>cd livos && pnpm --filter ui build</automated>
  </verify>
  <done>Section root wires backend queries + mutations + 4 sub-components; FR-F2B-01 service-state banners + FR-F2B-05 cellular toggle + FR-F2B-06 Settings persistence all live; UI builds clean; sacred file untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ tRPC over HTTP | All ban/unban traffic flows through `httpOnlyPaths` (FR-F2B success #8) — survives WS reconnect. |
| Browser-side input ↔ Zod | Defense in depth: client-side IPv4 regex matches server-side schema. CIDR rejected at both layers (B-03). |
| Sidebar visibility ↔ user_preferences | Toggle hides UI only — backend tRPC routes remain available, fail2ban itself untouched. Non-destructive backout (FR-F2B-06 + sub-issue #4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-18 | T (Tampering) | client-side type-LOCK-ME-OUT bypass | mitigate | Backend (Plan 03) ALSO enforces literal-string `confirmation: z.literal('LOCK ME OUT')` — client-side gate is UX-only; tampered client cannot bypass. |
| T-46-19 | T (Tampering) | client-side CIDR bypass to mass-unban | mitigate | Server-side ipSchema (Plan 03) is canonical. Client-side regex is UX feedback only. |
| T-46-20 | I (Information Disclosure) | exposing active SSH source IPs to non-admin | accept | All routes are adminProcedure. UI is only rendered for authenticated admins. Sidebar visibility is preference-toggle only — non-admins never load the components. |
| T-46-21 | D (Denial of Service) | aggressive polling DoSing fail2ban-client | mitigate | refetchInterval: 5000 (pitfall W-02); manual Refresh debounced via React Query's invalidateQueries (idempotent). |
| T-46-22 | I (Information Disclosure) | banned IP list leaked through browser DevTools history | accept | Banned IPs are not user-secrets; they're attacker public IPs already visible in fail2ban logs. Same exposure as `/var/log/auth.log`. |
| T-46-23 | T (Tampering) | toggle-off mid-session leaving open security section in zombie state | accept | Sub-issue #4 conscious decision: hide sidebar entry only, don't auto-redirect. Operator-friendly; section continues working until operator navigates away. Preference re-applies on next mount. |
</threat_model>

<verification>
- `pnpm --filter ui build` exits 0 with zero TypeScript errors.
- All 4 service-state strings present in security-section.tsx (`grep -E "binary-missing|service-inactive|no-jails|running" security-section.tsx`).
- Sidebar visibility coupled to preference (`grep security_panel_visible sidebar.tsx`).
- Settings toggle row writes the preference (`grep security_panel_visible security-toggle-row.tsx`).
- Sacred file untouched.
</verification>

<success_criteria>
1. UI lives at `routes/docker/security/` (corrected from prompt's `modules/docker-app/` — sub-issue #1).
2. Security sidebar entry is the 13th LIVINITY_docker section.
3. All 6 FR-F2B requirements have UI surface: jail list (FR-F2B-01), unban+whitelist (FR-F2B-02), manual ban with type-LOCK-ME-OUT (FR-F2B-03), audit log table (FR-F2B-04), cellular toggle + dual-IP (FR-F2B-05), Settings toggle (FR-F2B-06).
4. Pitfall mitigations B-01 (user education in unban modal), B-02 (two-stage ban modal), B-03 (client-side CIDR rejection), B-19 (cellular bypass toggle) all encoded.
5. `pnpm --filter ui build` clean.
6. Sacred file `nexus/packages/core/src/sdk-agent-runner.ts` byte-identical.
</success_criteria>

<output>
After completion, create `.planning/phases/46-fail2ban-admin-panel/46-04-SUMMARY.md` with:
- 7 new files + 3 modified files (echo paths)
- Sidebar registration confirmed in 3 touchpoints
- Service-state banners count (4 distinct states)
- Pitfall coverage table (B-01/B-02/B-03/B-19 → file:line refs)
- pnpm --filter ui build status (echoed)
- Sacred file diff (echoed empty)
- Note on @tabler icon choice (IconShieldLock vs IconLock based on availability)
</output>
</content>
</invoke>