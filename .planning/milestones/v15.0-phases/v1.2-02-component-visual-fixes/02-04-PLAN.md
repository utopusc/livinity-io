---
phase: v1.2-02-component-visual-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/shadcn-components/ui/button.tsx
autonomous: true

must_haves:
  truths:
    - "Default button variant has 1px inset highlight (shadow-button-highlight-soft), not 0.5px"
    - "Small button desktop height is 30px (md:h-[30px]), not 28px"
    - "Default button desktop height is 36px (md:h-[36px]), not 34px"
    - "Medium button desktop height is 36px (md:h-[36px]), not 34px"
  artifacts:
    - path: "livos/packages/ui/src/shadcn-components/ui/button.tsx"
      provides: "Button with full 1px highlight and taller desktop heights"
      contains: "shadow-button-highlight-soft"
  key_links:
    - from: "button.tsx buttonVariants"
      to: "All button consumers across the app"
      via: "cva variant export"
      pattern: "buttonVariants"
---

<objective>
Upgrade button highlight from 0.5px to 1px and increase desktop heights for better visual substance and click targets.

Purpose: A full 1px highlight makes the top-edge glass effect visible. Taller desktop buttons (30px sm, 36px default/md) provide more comfortable click targets and better visual weight in the UI.
Output: Updated button.tsx with all CV-08 changes applied.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.2-02-component-visual-fixes/02-RESEARCH.md
@livos/packages/ui/src/shadcn-components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade button highlight shadow (CV-08)</name>
  <files>livos/packages/ui/src/shadcn-components/ui/button.tsx</files>
  <action>
In `button.tsx`, find the `default` variant definition (line ~16) in `buttonVariants` cva. Change the shadow class:

Current: `shadow-button-highlight-soft-hpx`
Target:  `shadow-button-highlight-soft`

This upgrades the inset top-edge highlight from 0.5px to 1px, making the glass effect visible.

The full current variant string contains:
```
'bg-surface-1 hover:bg-surface-2 active:bg-surface-base border border-border-default ring-white/20 data-[state=open]:bg-surface-2 hover:border-border-emphasis focus-visible:border-border-emphasis data-[state=open]:border-border-emphasis shadow-button-highlight-soft-hpx'
```
  </action>
  <verify>
Read button.tsx and confirm the default variant has `shadow-button-highlight-soft` (not `shadow-button-highlight-soft-hpx`).
  </verify>
  <done>Default button variant has a full 1px inset highlight.</done>
</task>

<task type="auto">
  <name>Task 2: Increase button desktop heights (CV-08)</name>
  <files>livos/packages/ui/src/shadcn-components/ui/button.tsx</files>
  <action>
In `button.tsx`, find the `size` variants in `buttonVariants` cva and make these 3 height changes:

1. **Size `sm`** (line ~26): Change `md:h-[28px]` to `md:h-[30px]`.

   Current: `sm: 'rounded-radius-sm h-[44px] md:h-[28px] px-3 text-caption gap-1.5'`
   Target:  `sm: 'rounded-radius-sm h-[44px] md:h-[30px] px-3 text-caption gap-1.5'`

2. **Size `md`** (line ~27): Change `md:h-[34px]` to `md:h-[36px]`.

   Current: `md: 'rounded-radius-md h-[44px] md:h-[34px] min-w-[80px] px-4 text-body-sm'`
   Target:  `md: 'rounded-radius-md h-[44px] md:h-[36px] min-w-[80px] px-4 text-body-sm'`

3. **Size `default`** (line ~29): Change `md:h-[34px]` to `md:h-[36px]`.

   Current: `default: 'rounded-radius-md h-[44px] md:h-[34px] px-3.5 text-body-sm'`
   Target:  `default: 'rounded-radius-md h-[44px] md:h-[36px] px-3.5 text-body-sm'`

NOTE: Mobile heights (h-[44px]) remain unchanged -- those are the touch targets from Phase v1.1-08-02.
NOTE: Do NOT change `icon-only` size -- it has `md:h-[34px] md:w-[34px]` and needs to stay square at its current size since the requirement only specifies sm, md, and default.
  </action>
  <verify>
Read button.tsx and confirm:
- Size `sm` has `md:h-[30px]` (not 28px)
- Size `md` has `md:h-[36px]` (not 34px)
- Size `default` has `md:h-[36px]` (not 34px)
- Mobile h-[44px] values unchanged
- `icon-only` size unchanged
  </verify>
  <done>Button desktop heights increased: sm 30px, default 36px, md 36px.</done>
</task>

</tasks>

<verification>
After both tasks, run TypeScript check:
```bash
cd livos && npx tsc --noEmit --pretty 2>&1 | head -30
```
Visually confirm: buttons have a visible top-edge highlight and appear slightly taller on desktop, providing better visual weight.
</verification>

<success_criteria>
- button.tsx default variant has `shadow-button-highlight-soft` (not hpx)
- button.tsx sm size has `md:h-[30px]`
- button.tsx default size has `md:h-[36px]`
- button.tsx md size has `md:h-[36px]`
- Mobile heights (h-[44px]) unchanged
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-02-component-visual-fixes/02-04-SUMMARY.md`
</output>
