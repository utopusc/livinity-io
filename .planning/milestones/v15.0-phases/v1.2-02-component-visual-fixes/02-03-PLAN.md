---
phase: v1.2-02-component-visual-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx
  - livos/packages/ui/src/shadcn-components/ui/shared/menu.ts
autonomous: true

must_haves:
  truths:
    - "File list desktop icons are 24px (h-6 w-6), not 20px (h-5 w-5)"
    - "Context menu and dropdown focus/highlight background is surface-1 (0.10), not surface-base (0.06)"
    - "Context menu content and items use radius-md (12px), not radius-sm (8px)"
    - "Dropdown items use radius-md (12px) for consistency with context menu"
  artifacts:
    - path: "livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx"
      provides: "File list with larger desktop icons"
      contains: "h-6 w-6"
    - path: "livos/packages/ui/src/shadcn-components/ui/shared/menu.ts"
      provides: "Menu system with upgraded focus states and radius"
      contains: "focus:bg-surface-1"
  key_links:
    - from: "menu.ts menuItemClass"
      to: "context menu + dropdown consumers"
      via: "Shared base class imported by both menu types"
      pattern: "menuItemClass"
    - from: "menu.ts contextMenuItemClass"
      to: "context-menu.tsx consumer"
      via: "cn(menuItemClass, 'rounded-radius-md')"
      pattern: "contextMenuItemClass"
---

<objective>
Upgrade file list icon sizing and menu system visual states for better usability.

Purpose: Larger file icons improve scanability in list view. Brighter menu hover/focus states and larger radius make menus feel more modern and easier to navigate.
Output: Updated list-view-file-item.tsx and shared/menu.ts with all CV-05 and CV-06 changes applied.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.2-02-component-visual-fixes/02-RESEARCH.md
@livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx
@livos/packages/ui/src/shadcn-components/ui/shared/menu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase file list desktop icon size (CV-05)</name>
  <files>livos/packages/ui/src/features/files/components/listing/file-item/list-view-file-item.tsx</files>
  <action>
In `list-view-file-item.tsx`, find the `FileItemIcon` component usage (line ~87) with className `'h-5 w-5'` and change to `'h-6 w-6'`.

Current: `<FileItemIcon item={item} className='h-5 w-5' />`
Target:  `<FileItemIcon item={item} className='h-6 w-6' />`

This increases the desktop list view file icon from 20px to 24px for better visibility.

NOTE: The hover state requirement (hover:bg-surface-base on rows) is already satisfied by the parent `FileItem` wrapper in `index.tsx` line ~198 which has `md:hover:!bg-surface-base`. No change needed for hover.
  </action>
  <verify>
Read list-view-file-item.tsx and confirm FileItemIcon has `className='h-6 w-6'` (not h-5 w-5).
  </verify>
  <done>File list desktop icons are 24px (h-6 w-6) for improved scanability.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade menu focus states and radius (CV-06)</name>
  <files>livos/packages/ui/src/shadcn-components/ui/shared/menu.ts</files>
  <action>
In `shared/menu.ts`, make these 4 changes:

1. **menuItemClass focus background** (line ~10): Change `focus:bg-surface-base` to `focus:bg-surface-1`.

   Current: `focus:bg-surface-base`
   Target:  `focus:bg-surface-1`

2. **menuItemClass highlighted background** (line ~10): Change `data-[highlighted]:bg-surface-base` to `data-[highlighted]:bg-surface-1`.

   Current: `data-[highlighted]:bg-surface-base`
   Target:  `data-[highlighted]:bg-surface-1`

   Both changes are in the same `menuItemClass` string. The full current string is:
   ```
   const menuItemClass = tw`relative flex cursor-default select-none items-center px-3 py-2 text-body-sm font-medium -tracking-3 leading-tight outline-none transition-colors data-[disabled]:pointer-events-none data-[disabled]:opacity-50 focus:bg-surface-base focus:text-white data-[highlighted]:bg-surface-base data-[highlighted]:text-white`
   ```

3. **Context menu content radius** (line ~19): Change `rounded-radius-sm` to `rounded-radius-md` in the `contextMenuClasses.content` definition.

   Current: `content: cn(menuContentClass, 'shadow-context-menu rounded-radius-sm'),`
   Target:  `content: cn(menuContentClass, 'shadow-context-menu rounded-radius-md'),`

4. **Context menu item radius** (line ~16): Change `rounded-radius-sm` to `rounded-radius-md` in the `contextMenuItemClass` definition.

   Current: `const contextMenuItemClass = cn(menuItemClass, 'rounded-radius-sm')`
   Target:  `const contextMenuItemClass = cn(menuItemClass, 'rounded-radius-md')`

5. **Dropdown item radius** (line ~34): Change `rounded-radius-sm` to `rounded-radius-md` in the `dropdownItemClass` definition for consistency.

   Current: `const dropdownItemClass = cn(menuItemClass, 'rounded-radius-sm')`
   Target:  `const dropdownItemClass = cn(menuItemClass, 'rounded-radius-md')`

NOTE: Dropdown content already uses `rounded-radius-md` so it does NOT need changing.
  </action>
  <verify>
Read shared/menu.ts and confirm:
- `menuItemClass` has `focus:bg-surface-1` (not surface-base)
- `menuItemClass` has `data-[highlighted]:bg-surface-1` (not surface-base)
- `contextMenuClasses.content` has `rounded-radius-md` (not radius-sm)
- `contextMenuItemClass` has `rounded-radius-md` (not radius-sm)
- `dropdownItemClass` has `rounded-radius-md` (not radius-sm)
- Dropdown content radius is unchanged (already radius-md)
  </verify>
  <done>Menu focus/highlight uses surface-1 for visible hover states. Context menu and dropdown items use 12px radius for modern feel.</done>
</task>

</tasks>

<verification>
After both tasks, run TypeScript check:
```bash
cd livos && npx tsc --noEmit --pretty 2>&1 | head -30
```
Visually confirm: file list icons are noticeably larger, context menus have visible hover states and rounder items.
</verification>

<success_criteria>
- list-view-file-item.tsx has `h-6 w-6` icon size
- menu.ts menuItemClass has `focus:bg-surface-1` and `data-[highlighted]:bg-surface-1`
- menu.ts context menu content and items have `rounded-radius-md`
- menu.ts dropdown items have `rounded-radius-md`
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-02-component-visual-fixes/02-03-SUMMARY.md`
</output>
