---
phase: 18-cost-control-settings-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/hooks/use-agent-socket.ts
  - livos/packages/ui/src/routes/ai-chat/index.tsx
  - livos/packages/ui/src/routes/settings/_components/settings-content.tsx
  - livos/packages/ui/src/routes/settings/index.tsx
  - livos/packages/ui/src/routes/settings/nexus-config.tsx
autonomous: true
requirements:
  - SDK-08
  - SDK-09

must_haves:
  truths:
    - "Each conversation displays estimated cost (USD) after the agent finishes"
    - "System respects maxBudgetUsd cap and the result message indicates when budget is exceeded"
    - "Settings no longer shows the Nexus AI Settings panel with token/tool limit sliders"
    - "AI configuration page still works (API key entry, provider selection, computer use toggle)"
  artifacts:
    - path: "livos/packages/ui/src/hooks/use-agent-socket.ts"
      provides: "Cost/usage state extracted from SDK result messages"
      exports: ["useAgentSocket"]
    - path: "livos/packages/ui/src/routes/ai-chat/index.tsx"
      provides: "Cost badge displayed in the chat connection status bar"
    - path: "livos/packages/ui/src/routes/settings/_components/settings-content.tsx"
      provides: "NexusConfigSection removed from inline settings, menu item removed"
  key_links:
    - from: "use-agent-socket.ts"
      to: "SDK result message"
      via: "handleSdkMessage result case extracting total_cost_usd and usage"
      pattern: "total_cost_usd|totalCost"
    - from: "ai-chat/index.tsx"
      to: "use-agent-socket.ts"
      via: "agent.totalCost displayed in connection bar"
      pattern: "agent\\.totalCost"
---

<objective>
Add real-time cost tracking per conversation and remove the obsolete Nexus AI Settings panel.

Purpose: Users see how much each conversation costs (SDK-08). The old Nexus AI Settings page with token limits, tool limits, retry, heartbeat, session, and response style settings is removed since the Claude Agent SDK handles all of this natively (SDK-09).

Output: Updated use-agent-socket hook with cost state, cost badge in chat UI, and settings cleaned up.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v15.0-phases/18-cost-control-settings-cleanup/18-CONTEXT.md

@livos/packages/ui/src/hooks/use-agent-socket.ts
@livos/packages/ui/src/routes/ai-chat/index.tsx
@livos/packages/ui/src/routes/ai-chat/chat-messages.tsx
@livos/packages/ui/src/routes/settings/_components/settings-content.tsx
@livos/packages/ui/src/routes/settings/index.tsx
@livos/packages/ui/src/routes/settings/nexus-config.tsx

<interfaces>
<!-- SDK result message structure (from @anthropic-ai/claude-agent-sdk sdk.d.ts) -->
<!-- Both SDKResultSuccess and SDKResultError include these fields: -->

SDKResultSuccess = {
  type: 'result';
  subtype: 'success';
  duration_ms: number;
  duration_api_ms: number;
  is_error: boolean;
  num_turns: number;
  result: string;
  total_cost_usd: number;
  usage: { input_tokens: number; output_tokens: number };
}

SDKResultError = {
  type: 'result';
  subtype: 'error_during_execution' | 'error_max_turns' | 'error_max_budget_usd' | ...;
  total_cost_usd: number;
  usage: { input_tokens: number; output_tokens: number };
  errors: string[];
}

<!-- These arrive at the client inside: { type: 'sdk_message', data: <above> } -->
<!-- The handleSdkMessage function in use-agent-socket.ts receives data directly -->

<!-- Current useAgentSocket return shape: -->
{
  messages, isConnected, isStreaming, connectionStatus, currentSessionId, conversationId,
  sendMessage, sendFollowUp, interrupt, cancel, clearMessages, loadConversation
}

<!-- Settings navigation in settings-content.tsx: -->
MENU_ITEMS array contains: { id: 'nexus-config', icon: TbBrain, label: 'Nexus AI Settings', ... }
SettingsSection type includes: 'nexus-config'
SectionContent switch includes: case 'nexus-config': return <NexusConfigSection />
NexusConfigSection function is ~120 lines starting at line 1056

<!-- settings/index.tsx: -->
Line 39: const NexusConfigPage = React.lazy(() => import('@/routes/settings/nexus-config'))
Line 145: <Route path='/nexus-config' Component={NexusConfigPage} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cost/usage state to useAgentSocket and display cost badge in chat</name>
  <files>livos/packages/ui/src/hooks/use-agent-socket.ts, livos/packages/ui/src/routes/ai-chat/index.tsx</files>
  <read_first>
    livos/packages/ui/src/hooks/use-agent-socket.ts
    livos/packages/ui/src/routes/ai-chat/index.tsx
  </read_first>
  <action>
**In use-agent-socket.ts** (per SDK-08):

1. Add new state fields after the existing `useState` declarations (around line 117-118):
   ```ts
   const [totalCost, setTotalCost] = useState<number>(0)
   const [usageStats, setUsageStats] = useState<{inputTokens: number; outputTokens: number; durationMs: number; numTurns: number} | null>(null)
   ```

2. In the `handleSdkMessage` callback, inside the `case 'result':` block (around line 354), AFTER the existing `flushBuffer()` and `dispatch({type: 'FINALIZE_MESSAGE'})` and BEFORE `setIsStreaming(false)`, extract cost data from the SDK result message:
   ```ts
   // Extract cost and usage from SDK result
   if (data.total_cost_usd != null) {
     setTotalCost(data.total_cost_usd)
   }
   if (data.usage) {
     setUsageStats({
       inputTokens: data.usage.input_tokens ?? 0,
       outputTokens: data.usage.output_tokens ?? 0,
       durationMs: data.duration_ms ?? 0,
       numTurns: data.num_turns ?? 0,
     })
   }
   ```

3. In the `clearMessages` callback, also reset cost state:
   ```ts
   setTotalCost(0)
   setUsageStats(null)
   ```

4. Add `totalCost` and `usageStats` to the return object.

**In ai-chat/index.tsx** (per SDK-08):

5. In the WebSocket connection status bar (around line 405-421), add a cost badge that appears when `agent.totalCost > 0`. Place it after the connection status text, before the closing `</div>`:
   ```tsx
   {agent.totalCost > 0 && (
     <span className='ml-auto text-caption-sm font-mono text-text-tertiary'>
       ${agent.totalCost.toFixed(4)}
     </span>
   )}
   ```

   This shows the cost as e.g. "$0.0234" in a subtle monospace font on the right side of the connection bar. It appears only after a result is received and resets on new conversation.

6. Optionally, when `agent.usageStats` is available and streaming has ended, show a brief usage summary as a tooltip or inline text. Keep it minimal — just the dollar amount is the primary display. Add a title attribute on the cost span for hover detail:
   ```tsx
   title={agent.usageStats ? `Input: ${agent.usageStats.inputTokens.toLocaleString()} tokens | Output: ${agent.usageStats.outputTokens.toLocaleString()} tokens | ${(agent.usageStats.durationMs / 1000).toFixed(1)}s` : undefined}
   ```
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - useAgentSocket returns totalCost (number) and usageStats (object with inputTokens, outputTokens, durationMs, numTurns)
    - Cost badge appears in the chat status bar when totalCost > 0
    - Cost resets to 0 when starting a new conversation (clearMessages)
    - Hovering cost badge shows token breakdown
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Cost is extracted from SDK result messages and displayed as a subtle "$X.XXXX" badge in the chat connection bar, with token details on hover</done>
</task>

<task type="auto">
  <name>Task 2: Remove Nexus AI Settings panel from settings</name>
  <files>
    livos/packages/ui/src/routes/settings/_components/settings-content.tsx,
    livos/packages/ui/src/routes/settings/index.tsx,
    livos/packages/ui/src/routes/settings/nexus-config.tsx
  </files>
  <read_first>
    livos/packages/ui/src/routes/settings/_components/settings-content.tsx
    livos/packages/ui/src/routes/settings/index.tsx
  </read_first>
  <action>
**In settings-content.tsx** (per SDK-09):

1. Remove `'nexus-config'` from the `SettingsSection` type union (line ~121).

2. Remove the nexus-config menu item from `MENU_ITEMS` array (line ~150):
   Delete: `{id: 'nexus-config', icon: TbBrain, label: 'Nexus AI Settings', description: 'Agent behavior & response style'},`

3. Remove the `case 'nexus-config':` from the `SectionContent` switch (lines ~353-354):
   Delete: `case 'nexus-config': return <NexusConfigSection />`

4. Delete the entire `NexusConfigSection` function and its `NexusConfig` interface. These start at line ~1045 and extend to approximately line 1300+ (the function with all its tabs for Response, Agent, Retry, Heartbeat, Session, Advanced). Remove the full block.

5. Remove any now-unused imports that were only used by NexusConfigSection. Check for: `TbBrain` in the icon imports (may still be used elsewhere — verify before removing), `TbMessageCircle`, `TbRefresh`, `TbHeartbeat`, `TbClock` if only used by NexusConfig tabs. Verify each import has other consumers before removing.

**In settings/index.tsx** (per SDK-09):

6. Remove the NexusConfigPage lazy import (line 39):
   Delete: `const NexusConfigPage = React.lazy(() => import('@/routes/settings/nexus-config'))`

7. Remove the nexus-config route (line 145):
   Delete: `<Route path='/nexus-config' Component={NexusConfigPage} />`

**Delete file** (per SDK-09):

8. Delete the entire file `livos/packages/ui/src/routes/settings/nexus-config.tsx` — it is the standalone page version and is no longer needed.

**Verify ai-config.tsx is untouched** — the AI Configuration page (API key entry, provider selection, computer use consent) must remain as-is. It already contains exactly what SDK-09 requires: API key and model preference only. No simplification needed since it doesn't have token/tool limits.
  </action>
  <verify>
    <automated>cd livos && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - nexus-config.tsx file is deleted
    - Settings navigation no longer shows "Nexus AI Settings" menu item
    - No route exists for /settings/nexus-config
    - NexusConfigSection function and NexusConfig interface are removed from settings-content.tsx
    - AI Configuration page still works (API key, provider selection, computer use)
    - TypeScript compiles without errors — no dangling references
  </acceptance_criteria>
  <done>Nexus AI Settings panel completely removed from settings. No token limits, tool limits, retry, heartbeat, session, or response style settings remain. AI Configuration page (API key + provider) is preserved.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd livos && npx tsc --noEmit` passes without errors
2. Build check: `cd livos && pnpm --filter @livos/config build && pnpm --filter ui build` completes successfully
3. No references to nexus-config remain: `grep -r "nexus-config\|NexusConfig\|NexusConfigPage\|NexusConfigSection" livos/packages/ui/src/` returns no results
4. useAgentSocket exports totalCost and usageStats: `grep "totalCost\|usageStats" livos/packages/ui/src/hooks/use-agent-socket.ts`
</verification>

<success_criteria>
- Cost badge appears in chat UI connection bar showing "$X.XXXX" when a conversation has received a result
- Hovering the cost badge shows input/output token counts and duration
- Cost resets when starting a new conversation
- Settings no longer has "Nexus AI Settings" entry
- AI Configuration page still functions (API key, provider selection, computer use toggle)
- Full UI build succeeds
</success_criteria>

<output>
After completion, create `.planning/milestones/v15.0-phases/18-cost-control-settings-cleanup/18-01-SUMMARY.md`
</output>
