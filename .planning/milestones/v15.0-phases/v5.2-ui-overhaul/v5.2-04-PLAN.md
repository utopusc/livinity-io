---
phase: v5.2-ui-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/src/routes/app-store/discover.tsx
  - livos/packages/ui/src/modules/app-store/shared.tsx
  - livos/packages/ui/src/modules/app-store/discover/apps-grid-section.tsx
  - livos/packages/ui/src/modules/app-store/discover/apps-row-section.tsx
  - livos/packages/ui/src/modules/app-store/discover/apps-three-column-section.tsx
  - livos/packages/ui/src/modules/app-store/gallery-section.tsx
  - livos/packages/ui/src/modules/app-store/app-store-nav.tsx
autonomous: true

must_haves:
  truths:
    - "App Store discover page has a creative, editorial-magazine layout"
    - "Featured hero cards use refined light-theme gradients with depth"
    - "Grid section cards have a clean, Apple-App-Store-inspired design"
    - "Horizontal scroll cards have elevated panoramic design with color-extracted gradients"
    - "Three-column showcase section has an engaging fan layout"
    - "Banner gallery has polished hover states and transitions"
    - "Category nav has clear active states on light background"
    - "All styles use white/light theme palette"
  artifacts:
    - path: "livos/packages/ui/src/routes/app-store/discover.tsx"
      provides: "Redesigned discover page with premium hero cards"
      contains: "FeaturedHeroCard"
    - path: "livos/packages/ui/src/modules/app-store/shared.tsx"
      provides: "Updated shared styles for light theme"
      contains: "cardClass"
    - path: "livos/packages/ui/src/modules/app-store/discover/apps-grid-section.tsx"
      provides: "Redesigned grid cards"
      contains: "AppWithDescription"
    - path: "livos/packages/ui/src/modules/app-store/discover/apps-row-section.tsx"
      provides: "Redesigned horizontal scroll cards"
      contains: "AppCard"
    - path: "livos/packages/ui/src/modules/app-store/discover/apps-three-column-section.tsx"
      provides: "Redesigned fan showcase"
      contains: "FeaturedAppCard"
  key_links:
    - from: "discover.tsx"
      to: "apps-grid-section.tsx"
      via: "component import"
      pattern: "AppsGridSection"
    - from: "discover.tsx"
      to: "apps-row-section.tsx"
      via: "component import"
      pattern: "AppsRowSection"
    - from: "discover.tsx"
      to: "gallery-section.tsx"
      via: "component import"
      pattern: "AppsGallerySection"
---

<objective>
Complete visual redesign of the App Store sheet/page version with a creative, editorial-magazine aesthetic. Redesign all section types: hero cards, grid cards, horizontal scroll cards, three-column showcase, banner gallery, and category navigation.

Purpose: Transform the App Store from a functional app listing into a premium, visually engaging discovery experience inspired by Apple's App Store editorial design — with rich gradients, refined cards, and engaging animations.
Output: Redesigned sheet-version App Store components with premium visual styling.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@livos/packages/ui/src/routes/app-store/discover.tsx
@livos/packages/ui/src/modules/app-store/shared.tsx
@livos/packages/ui/src/modules/app-store/discover/apps-grid-section.tsx
@livos/packages/ui/src/modules/app-store/discover/apps-row-section.tsx
@livos/packages/ui/src/modules/app-store/discover/apps-three-column-section.tsx
@livos/packages/ui/src/modules/app-store/gallery-section.tsx
@livos/packages/ui/src/modules/app-store/app-store-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign shared styles, hero cards, and gallery</name>
  <files>
    livos/packages/ui/src/modules/app-store/shared.tsx
    livos/packages/ui/src/routes/app-store/discover.tsx
    livos/packages/ui/src/modules/app-store/gallery-section.tsx
    livos/packages/ui/src/modules/app-store/app-store-nav.tsx
  </files>
  <action>
  **A) Shared styles (shared.tsx)**

  Update the shared style constants for a premium light-theme look:

  1. Update `cardClass` for a cleaner card with more depth:
     ```tsx
     export const cardClass = cn(
       cardBaseClass,
       tw`relative overflow-hidden`,
       tw`bg-white`,
       tw`border border-neutral-200/80`,
       tw`shadow-[0_1px_3px_rgba(0,0,0,0.04),0_4px_12px_rgba(0,0,0,0.03)]`,
       tw`rounded-2xl`,
     )
     ```

  2. Update `cardFaintClass` for subtler secondary cards:
     ```tsx
     export const cardFaintClass = cn(
       cardBaseClass,
       tw`bg-neutral-50/50 hover:bg-white transition-all duration-300`,
       tw`border border-neutral-200/60`,
       tw`rounded-2xl`,
     )
     ```

  3. Update `cardFeaturedClass`:
     ```tsx
     export const cardFeaturedClass = cn(
       cardBaseClass,
       tw`relative overflow-hidden`,
       tw`bg-gradient-to-br from-white to-neutral-50`,
       tw`border border-neutral-200/80`,
       tw`shadow-[0_2px_8px_rgba(0,0,0,0.06),0_8px_24px_rgba(0,0,0,0.04)]`,
       tw`rounded-2xl`,
     )
     ```

  4. Update section typography:
     ```tsx
     export const sectionOverlineClass = tw`text-[11px] font-bold uppercase tracking-[0.08em] text-brand mb-1.5`
     export const sectionTitleClass = tw`text-2xl md:text-3xl font-bold text-text-primary tracking-tight`
     export const sectionDescriptionClass = tw`text-body-sm md:text-body text-text-secondary mt-2 max-w-2xl leading-relaxed`
     ```

  5. Update `PremiumAppCard` styling for a refined look — add a subtle left accent bar on hover and make the card more tactile:
     - Add `relative` to the button
     - Add a pseudo-element-like left accent bar: wrap the inner content and add a 2px rounded accent on the left side that appears on hover (use a small div with `className='absolute left-0 top-2 bottom-2 w-0.5 rounded-full bg-brand opacity-0 transition-opacity group-hover:opacity-100'`)

  6. Keep ALL functional components (SectionTitle, AppStoreSheetInner, AppWithName, CategoryPill, StatsBadge, FeaturedAppSpotlight) structurally the same — only update Tailwind classes.

  **B) Featured hero cards (discover.tsx)**

  Redesign the GRADIENTS and FeaturedHeroCard for a light theme:

  1. Update GRADIENTS for light-theme-friendly colors (softer, more pastel, no dark overlays):
     ```tsx
     const GRADIENTS = [
       {
         from: 'from-blue-50', via: 'via-indigo-50/80', to: 'to-violet-50/50',
         orb1: 'bg-blue-200/40', orb2: 'bg-indigo-200/30',
         accent: 'text-blue-600',
         badge: 'bg-blue-100 text-blue-700 ring-blue-200',
         dot: 'bg-blue-500', glow: 'bg-blue-200/50',
       },
       {
         from: 'from-amber-50', via: 'via-orange-50/80', to: 'to-rose-50/50',
         orb1: 'bg-amber-200/40', orb2: 'bg-orange-200/30',
         accent: 'text-amber-600',
         badge: 'bg-amber-100 text-amber-700 ring-amber-200',
         dot: 'bg-amber-500', glow: 'bg-amber-200/50',
       },
     ]
     ```

  2. Update `FeaturedHeroCard`:
     - Remove the dark overlay: remove `<div className='absolute inset-0 bg-gradient-to-r from-black/5 via-transparent to-transparent' />` (or change from-black/5 to from-white/30)
     - Make the card border lighter: `border border-neutral-200/80`
     - Badge should use `ring-1` instead of `ring-1` (keep) but ensure readable on light bg
     - App name `h3`: `text-xl font-bold tracking-tight text-neutral-900 md:text-2xl`
     - Tagline: `text-body-sm text-neutral-600`
     - The shine sweep: change `from-transparent via-white/8 to-transparent` to `from-transparent via-white/40 to-transparent` for more visible sweep on light theme
     - Keep Tilt, Spotlight, InView, Link/WindowAwareLink, AppIcon, all app data rendering

  **C) Gallery section (gallery-section.tsx)**

  Refine for light theme:

  1. `AppsGallerySection` banners: change the gradient overlay from `from-black/20 via-black/5` to `from-black/10 via-transparent` for lighter overlay on banner images.
  2. Border on hover: change `border-2 border-transparent` to `border border-neutral-200/60` with hover: `border-neutral-300`.
  3. `HeroBanner`: update gradient overlay to lighter: `from-black/5 via-transparent to-transparent`. Update "Explore now" text to use `text-neutral-600 hover:text-neutral-900`.

  4. Keep all PhotoSwipe lightbox logic, FadeInImg, all functional behavior exactly as-is.

  **D) App Store nav (app-store-nav.tsx)**

  Read the file first. Then update for light theme clarity:
  - Active tab/category: use `bg-brand text-white` (keep if already this)
  - Inactive tabs: `text-neutral-500 hover:text-neutral-900 hover:bg-neutral-100`
  - The nav container: ensure it has a clean background, no dark remnants
  - AnimatedBackground indicator: ensure it works on light bg (it likely already does)
  - Keep ALL functional logic (category filtering, AnimatedBackground, routing)
  </action>
  <verify>
  1. Read shared.tsx and confirm card classes use white bg, neutral borders, subtle shadows.
  2. Read discover.tsx and confirm GRADIENTS use light-theme pastel colors.
  3. Read gallery-section.tsx and confirm lighter overlays.
  4. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20`.
  </verify>
  <done>
  - Shared card styles use premium light-theme aesthetics (white bg, subtle shadows, neutral borders)
  - Hero cards use pastel gradient backgrounds instead of dark overlays
  - Gallery banners have lighter overlays
  - Category nav has clear light-theme states
  - All functional behavior preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign grid, horizontal scroll, and three-column sections</name>
  <files>
    livos/packages/ui/src/modules/app-store/discover/apps-grid-section.tsx
    livos/packages/ui/src/modules/app-store/discover/apps-row-section.tsx
    livos/packages/ui/src/modules/app-store/discover/apps-three-column-section.tsx
  </files>
  <action>
  **A) Grid section (apps-grid-section.tsx)**

  Redesign `AppWithDescription` for a more polished app listing card:

  1. Update the Link wrapper:
     ```tsx
     className={cn(
       'group relative flex w-full items-start gap-4 rounded-2xl p-4',
       'border border-transparent',
       'bg-transparent',
       'transition-all duration-200 ease-out',
       'hover:bg-white hover:border-neutral-200/80 hover:shadow-[0_2px_8px_rgba(0,0,0,0.04)]',
       'focus:outline-none focus-visible:ring-2 focus-visible:ring-brand/30',
       'animate-in fade-in slide-in-from-bottom-4',
     )}
     ```

  2. Update AppIcon styling:
     ```tsx
     className={cn(
       'rounded-[14px]',
       'shadow-[0_1px_4px_rgba(0,0,0,0.08)]',
       'ring-1 ring-neutral-200/60',
       'transition-all duration-200',
       'group-hover:scale-105 group-hover:shadow-[0_2px_8px_rgba(0,0,0,0.1)]',
     )}
     ```

  3. App name: `text-body font-semibold tracking-tight text-neutral-900`
  4. Tagline: `text-body-sm leading-snug text-neutral-500 group-hover:text-neutral-600`
  5. Arrow indicator: change `text-text-tertiary` to `text-neutral-400`

  6. Update `FeaturedAppCard` with a refined spotlight-card style:
     - Background: `bg-gradient-to-br from-neutral-50 to-white`
     - Border: `border border-neutral-200/80`
     - On hover: `hover:shadow-[0_4px_16px_rgba(0,0,0,0.06)]`
     - Background glow: change from `bg-surface-1` / `bg-surface-2` to `bg-neutral-100/60` / `bg-neutral-200/40`

  Keep ALL functional logic: showAll prop, isMobile slicing, unstable_viewTransition, preloadFirstFewGalleryImages.

  **B) Horizontal scroll section (apps-row-section.tsx)**

  Redesign `AppCard` for a premium panoramic card:

  1. Update the card container dimensions: change `h-[180px] w-[280px] md:h-[220px] md:w-[360px]` to `h-[200px] w-[300px] md:h-[240px] md:w-[380px]` for more visual impact.

  2. Update the card border and hover:
     ```tsx
     'border border-neutral-200/80',
     'transition-all duration-300 ease-out',
     'group-hover:border-neutral-300',
     'group-hover:shadow-[0_8px_30px_rgba(0,0,0,0.08)]',
     ```

  3. The background gradient uses ColorThief — keep that. But make the gradient softer on light theme:
     ```tsx
     style={{
       background: `linear-gradient(145deg, ${gradientStart}15 0%, ${gradientEnd}08 60%, rgba(255,255,255,0.95) 100%)`,
     }}
     ```
     The gradient should be very subtle — just a tinted wash, not saturated.

  4. Update the Spotlight: `className='from-white/40 via-white/20 to-transparent'` (brighter on light theme).

  5. Icon positioning: move from `absolute left-6 top-6` to `absolute left-7 top-7` with slightly larger shadow:
     ```tsx
     className={cn(
       'rounded-[16px]',
       'shadow-[0_4px_12px_rgba(0,0,0,0.12)]',
       'ring-1 ring-neutral-200/80',
       'transition-all duration-300',
       'group-hover:scale-110 group-hover:shadow-[0_6px_20px_rgba(0,0,0,0.15)]',
     )}
     ```

  6. Content text at bottom:
     - Name: `text-xl font-bold tracking-tight text-neutral-900 md:text-2xl`
     - Tagline: `text-body-sm leading-snug text-neutral-500 md:text-body`

  7. Replace the shine effect with a subtler version:
     ```tsx
     <div className='absolute inset-0 bg-gradient-to-br from-white/60 via-transparent to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100' />
     ```

  8. `AppsCompactRow` — update for light theme:
     - Active item: `bg-white border-neutral-200/80 shadow-sm`
     - Hover: `hover:bg-neutral-50 hover:border-neutral-200/60`
     - App name: `text-neutral-900`, tagline: `text-neutral-500`

  Keep ALL functional logic: Link, useColorThief, preloadFirstFewGalleryImages, animationDelay, Tilt, Spotlight.

  **C) Three-column section (apps-three-column-section.tsx)**

  Redesign the fan showcase for a more dramatic visual:

  1. Update the section container:
     ```tsx
     className={cn(
       slideInFromBottomClass,
       'relative overflow-hidden rounded-3xl',
       'border border-neutral-200/80',
       'bg-gradient-to-br from-white to-neutral-50/80',
       'p-8 md:p-12',
     )}
     ```

  2. Text content typography:
     - Overline: `text-[11px] font-bold uppercase tracking-[0.08em] text-brand`
     - Title: `text-2xl font-bold tracking-tight text-neutral-900 md:text-3xl`
     - Description: `text-body-sm leading-relaxed text-neutral-500 md:text-body`

  3. `FeaturedAppCard` in the fan:
     - Card dimensions: keep `h-[280px] w-[180px]` as-is (good proportions)
     - Border: `border border-neutral-200/80`
     - Background gradient: make it lighter for light theme:
       ```tsx
       style={{
         background: `linear-gradient(180deg, ${gradientStart}25 0%, ${gradientEnd}15 50%, rgba(255,255,255,0.98) 100%)`,
       }}
       ```
     - Icon: keep sizes, update shadow to `shadow-[0_4px_16px_rgba(0,0,0,0.12)]` and ring to `ring-1 ring-neutral-200/80`
     - Bottom content area: change `bg-surface-base/80 backdrop-blur-sm` to `bg-white/90 backdrop-blur-sm border-t border-neutral-100`
     - "View" button: `bg-neutral-100 text-neutral-700 group-hover:bg-neutral-200 group-hover:text-neutral-900`
     - Shine effect: `from-white/50 via-transparent to-transparent`

  4. `FeaturedCategorySection` — update similarly for light theme:
     - Container: `bg-gradient-to-br from-white to-neutral-50` with `border-neutral-200/80`
     - App items: `bg-transparent hover:bg-neutral-50`
     - "Browse all" link: `text-neutral-500 hover:text-neutral-900`
     - "View" badge: `bg-neutral-100 text-neutral-600 group-hover:bg-neutral-200`

  Keep ALL functional logic: ColorThief, Tilt, Link, preloadFirstFewGalleryImages, FadeScroller, textLocation, children (CTA button).
  </action>
  <verify>
  1. Read all three files and confirm light-theme colors applied throughout.
  2. Confirm no dark-theme remnants (no from-black references higher than /10, no dark bg).
  3. Run `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json 2>&1 | head -20`.
  </verify>
  <done>
  - Grid cards have clean hover with subtle shadow and border reveal
  - Horizontal cards are larger with softer color-extracted gradients
  - Three-column fan cards use light-theme gradients and white-base design
  - All functional behavior preserved (ColorThief, Tilt, Spotlight, navigation, preloading)
  - Consistent light-theme palette across all sections
  </done>
</task>

</tasks>

<verification>
1. All App Store sections use light-theme palette (neutrals, pastels, whites)
2. No dark-theme remnants (check for from-black references above /10)
3. Cards have refined shadows and borders
4. Gradients are subtle and pastel, not saturated
5. All section types render correctly (hero, grid, horizontal, three-column, gallery)
6. TypeScript compiles cleanly
</verification>

<success_criteria>
- App Store discover page has an editorial-magazine feel on light theme
- Hero cards use pastel gradients with depth
- Grid cards have clean hover states
- Horizontal cards have elevated panoramic design
- Three-column showcase has engaging fan layout
- Banner gallery has polished hover states
- All functional behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/v5.2-ui-overhaul/v5.2-04-SUMMARY.md`
</output>
