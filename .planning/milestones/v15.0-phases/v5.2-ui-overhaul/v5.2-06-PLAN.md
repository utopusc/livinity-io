---
phase: v5.2-ui-overhaul
plan: 06
type: execute
wave: 3
depends_on: ["v5.2-01", "v5.2-02", "v5.2-03", "v5.2-04", "v5.2-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All UI changes compile and render without errors"
    - "Files manager is visually premium with redesigned layout, sidebar, toolbar, and items"
    - "App Store is visually premium in both sheet and window versions"
    - "Settings routing works correctly in window context"
    - "Window chrome has no grab cursor"
    - "Build succeeds with no TypeScript errors"
  artifacts: []
  key_links: []
---

<objective>
Build verification and visual checkpoint. Confirm all v5.2 changes compile, the development server runs, and present UI for user approval.

Purpose: Final quality gate before shipping v5.2 changes.
Output: Verified build and user-approved visual state.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v5.2-ui-overhaul/v5.2-01-SUMMARY.md
@.planning/phases/v5.2-ui-overhaul/v5.2-02-SUMMARY.md
@.planning/phases/v5.2-ui-overhaul/v5.2-03-SUMMARY.md
@.planning/phases/v5.2-ui-overhaul/v5.2-04-SUMMARY.md
@.planning/phases/v5.2-ui-overhaul/v5.2-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files></files>
  <action>
  Run a full TypeScript check and Vite build to confirm all changes compile:

  1. `cd livos && pnpm tsc --noEmit --project packages/ui/tsconfig.json`
     - Must exit 0 with no errors
     - If errors, fix them (they would be from the prior plans' changes)

  2. `cd livos && pnpm --filter ui build`
     - Must complete successfully
     - If it fails, identify and fix the issue

  3. Check for any console warnings by reviewing the build output.

  This is a compile-time verification only. No runtime testing needed here — the visual checkpoint handles that.
  </action>
  <verify>
  Both commands exit 0. No TypeScript errors. Build output shows successful completion.
  </verify>
  <done>TypeScript compiles cleanly and Vite build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete v5.2 UI overhaul:
  1. Window chrome: grab cursor removed from title pill
  2. Settings: routing fixed for change-name/change-password in window, button sizes normalized
  3. Files: redesigned layout (wider sidebar, vertical divider), refined sidebar (uppercase labels, clean dividers, clear active states), frosted-glass toolbar, larger grid items (128px/72px icons), refined list view, improved empty states
  4. App Store (sheet): premium light-theme hero cards, refined grid cards, elevated panoramic scroll cards, dramatic fan showcase, polished gallery
  5. App Store (window): matching light-theme design across all window components
  </what-built>
  <how-to-verify>
  Start the dev server if not running: `cd livos && pnpm --filter ui dev`

  Then verify each area:

  1. **Window Chrome**: Open any app in a window. Hover over the title pill — cursor should be default pointer, NOT grab hand.

  2. **Settings Window**: Open Settings in a window. Click "Change name" — should open dialog in-place, NOT navigate to /settings page. Same for "Change password". Check that all buttons (Logout, Restart, Shutdown, Change name, Change password) are the same size.

  3. **Files Manager**: Open Files. Check:
     - Sidebar is wider with uppercase section labels
     - Vertical divider between sidebar and content
     - Sidebar items have clear active state (subtle bg + border + shadow)
     - Toolbar is a unified frosted-glass bar
     - View toggle looks like a segmented control
     - Grid view: items are larger with 72px icons and hover backgrounds
     - List view: clean rows with refined spacing
     - Empty folder: larger icon, readable text

  4. **App Store (Sheet)**: Open App Store as a sheet/page. Check:
     - Hero cards use pastel gradients (blue/indigo, amber/orange) — no dark overlays
     - Grid section cards have clean hover with subtle shadow
     - Horizontal scroll cards are larger with soft gradients
     - Three-column fan showcase has light-theme cards
     - Banner gallery has polished hover borders

  5. **App Store (Window)**: Open App Store in a window. Check:
     - Visual design matches the sheet version
     - Navigation works within the window
     - All section types render correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all areas look good, or describe specific issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles with 0 errors
2. Vite build succeeds
3. User approves visual state of all 5 areas
</verification>

<success_criteria>
- Build passes
- User confirms all UI areas meet quality expectations
- No visual regressions in unrelated areas
</success_criteria>

<output>
After completion, create `.planning/phases/v5.2-ui-overhaul/v5.2-06-SUMMARY.md`
</output>
