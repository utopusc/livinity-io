---
phase: v1.2-01-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/tailwind.config.ts
autonomous: true

must_haves:
  truths:
    - "Surface layers are visually distinguishable from each other (0.06, 0.10, 0.16, 0.22)"
    - "Borders are visible enough to define component edges (0.10, 0.16, 0.30)"
    - "Text-secondary and text-tertiary are comfortably readable (0.65, 0.45)"
    - "Elevated elements have subtle white inset glow at the top edge"
    - "Sheet-shadow uses clean top-edge highlight (0px 1px inset), not 2px diagonal offset"
    - "Dialog shadow uses clean 0px 1px 0px inset, not 1px 1px"
  artifacts:
    - path: "livos/packages/ui/tailwind.config.ts"
      provides: "Updated semantic token values for surfaces, borders, text, shadows"
      contains: "surface-base.*0\\.06"
  key_links:
    - from: "livos/packages/ui/tailwind.config.ts"
      to: "all UI components"
      via: "Tailwind utility classes (bg-surface-1, border-border-default, text-text-secondary, shadow-elevation-md, etc.)"
      pattern: "surface-base.*0\\.06|border-subtle.*0\\.10|text-secondary.*0\\.65"
---

<objective>
Update all semantic token values in tailwind.config.ts to make surfaces more visible, borders more defined, text more readable, and shadows richer with white inset glows.

Purpose: This is the foundation of the v1.2 Visual Impact Redesign. By changing token VALUES (not names), every component that already uses semantic tokens gets an immediate visual upgrade with zero component-level changes.
Output: Updated tailwind.config.ts with new opacity/shadow values for TF-01 through TF-06.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@livos/packages/ui/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update surface, border, and text color token opacities</name>
  <files>livos/packages/ui/tailwind.config.ts</files>
  <action>
In the `colors` section of `theme.extend`, update these rgba values:

SURFACES (TF-01):
- `surface-base`: change `0.04` to `0.06`
- `surface-1`: change `0.06` to `0.10`
- `surface-2`: change `0.10` to `0.16`
- `surface-3`: change `0.14` to `0.22`

BORDERS (TF-02):
- `border-subtle`: change `0.06` to `0.10`
- `border-default`: change `0.10` to `0.16`
- `border-emphasis`: change `0.20` to `0.30`

TEXT (TF-04):
- `text-primary`: change `0.90` to `0.92`
- `text-secondary`: change `0.60` to `0.65`
- `text-tertiary`: change `0.40` to `0.45`

All values remain rgba(255, 255, 255, X) format. Only the alpha value changes.

IMPORTANT: Do NOT touch any other color values. The brand, destructive, success, and dialog-content colors must remain unchanged.
  </action>
  <verify>
Read livos/packages/ui/tailwind.config.ts and confirm:
- `surface-base` contains `0.06`
- `surface-1` contains `0.10`
- `surface-2` contains `0.16`
- `surface-3` contains `0.22`
- `border-subtle` contains `0.10`
- `border-default` contains `0.16`
- `border-emphasis` contains `0.30`
- `text-primary` contains `0.92`
- `text-secondary` contains `0.65`
- `text-tertiary` contains `0.45`
  </verify>
  <done>All 10 color token opacities updated to their new values. No other colors modified.</done>
</task>

<task type="auto">
  <name>Task 2: Update elevation shadows with inset glows and fix sheet/dialog shadows</name>
  <files>livos/packages/ui/tailwind.config.ts</files>
  <action>
In the `boxShadow` section of `theme.extend`, update these shadow values:

ELEVATION SHADOWS (TF-03) — add white inset glow + stronger outer opacity:
- `elevation-sm`: replace entire value with `'0px 2px 8px rgba(0, 0, 0, 0.25), 0px 0.5px 0px 0px rgba(255, 255, 255, 0.06) inset'`
- `elevation-md`: replace entire value with `'0px 4px 16px rgba(0, 0, 0, 0.35), 0px 1px 0px 0px rgba(255, 255, 255, 0.08) inset'`
- `elevation-lg`: replace entire value with `'0px 8px 24px rgba(0, 0, 0, 0.45), 0px 1px 0px 0px rgba(255, 255, 255, 0.10) inset'`
- `elevation-xl`: replace entire value with `'0px 16px 48px rgba(0, 0, 0, 0.55), 0px 1px 0px 0px rgba(255, 255, 255, 0.12) inset'`

SHEET SHADOW (TF-05) — fix from diagonal offset to clean top-edge highlight:
- `sheet-shadow`: replace `'2px 2px 2px 0px rgba(255, 255, 255, 0.05) inset'` with `'0px 1px 0px 0px rgba(255, 255, 255, 0.10) inset'`

DIALOG SHADOW (TF-06) — fix inset from 1px 1px to 0px 1px 0px for clean top highlight:
- `dialog`: replace `'0px 24px 48px 0px rgba(0, 0, 0, 0.35), 0px 1px 1px 0px rgba(255, 255, 255, 0.1) inset'` with `'0px 24px 48px 0px rgba(0, 0, 0, 0.35), 0px 1px 0px 0px rgba(255, 255, 255, 0.12) inset'`

IMPORTANT: Do NOT touch dock, floating-island, glass-button, widget, context-menu, dropdown, button-highlight*, immersive-dialog-close, radio-outline, card-elevated, card-hover, or input-focus shadows.
  </action>
  <verify>
Read livos/packages/ui/tailwind.config.ts and confirm:
- `elevation-sm` contains `0.25` and `inset`
- `elevation-md` contains `0.35` and `inset`
- `elevation-lg` contains `0.45` and `inset`
- `elevation-xl` contains `0.55` and `inset`
- `sheet-shadow` is `'0px 1px 0px 0px rgba(255, 255, 255, 0.10) inset'`
- `dialog` ends with `0px 1px 0px 0px rgba(255, 255, 255, 0.12) inset'`
  </verify>
  <done>All 6 shadow tokens updated. Elevation shadows have white inset glow highlights and stronger outer opacity. Sheet-shadow uses proper top-edge highlight. Dialog shadow uses clean 0px 1px 0px inset.</done>
</task>

</tasks>

<verification>
After both tasks, run a full verification:
1. Read the entire colors section and confirm all 10 opacity values match requirements
2. Read the entire boxShadow section and confirm all 6 shadow values match requirements
3. Confirm no other values in the file were modified (dock, floating-island, glass-button, widget, etc. unchanged)
4. Run `cd livos && pnpm build` (or typecheck) to verify no syntax errors introduced
</verification>

<success_criteria>
- TF-01: Surface opacities are 0.06, 0.10, 0.16, 0.22
- TF-02: Border opacities are 0.10, 0.16, 0.30
- TF-03: All 4 elevation shadows have dual value (outer shadow + white inset glow)
- TF-04: Text opacities are 0.92, 0.65, 0.45
- TF-05: sheet-shadow is `0px 1px 0px 0px rgba(255, 255, 255, 0.10) inset`
- TF-06: dialog inset is `0px 1px 0px 0px rgba(255, 255, 255, 0.12) inset`
- No syntax errors — file parses correctly
- No unrelated values modified
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-01-token-foundation/01-01-SUMMARY.md`
</output>
