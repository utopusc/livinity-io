---
phase: v1.2-03-design-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - livos/packages/ui/tailwind.config.ts
  - livos/packages/ui/src/routes/ai-chat/index.tsx
autonomous: true

must_haves:
  truths:
    - "radius-2xl (24px) and radius-3xl (28px) semantic tokens exist in tailwind.config.ts"
    - "info (#3B82F6) and warning (#F59E0B) colors exist in tailwind.config.ts"
    - "AI Chat assistant messages have a left border accent in brand color"
  artifacts:
    - path: "livos/packages/ui/tailwind.config.ts"
      provides: "New semantic radius and status color tokens"
      contains: "radius-2xl.*24px"
    - path: "livos/packages/ui/src/routes/ai-chat/index.tsx"
      provides: "Assistant message left border accent"
      contains: "border-l-2.*border-brand"
  key_links:
    - from: "livos/packages/ui/tailwind.config.ts"
      to: "dialog/sheet consumers"
      via: "rounded-radius-2xl, rounded-radius-3xl Tailwind classes"
      pattern: "radius-2xl|radius-3xl"
---

<objective>
Add semantic radius tokens, status colors, and AI chat assistant message accent border.

Purpose: Complete the v1.2 design enhancement requirements with new tokens and visual distinction features.
Output: Updated tailwind.config.ts with new tokens, updated AI chat with assistant message accent.
</objective>

<execution_context>
@C:\Users\hello\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hello\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@livos/packages/ui/tailwind.config.ts
@livos/packages/ui/src/routes/ai-chat/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic radius and status color tokens to tailwind.config.ts</name>
  <files>livos/packages/ui/tailwind.config.ts</files>
  <action>
In the `borderRadius` section, after the existing semantic radii, add two new tokens:

After the line `'radius-xl': '20px',` add:
```
'radius-2xl': '24px',
'radius-3xl': '28px',
```

In the `colors` section, after the existing text colors (after `'text-tertiary': ...`), add:

```
// Semantic status colors
info: '#3B82F6',
'info-surface': 'rgba(59, 130, 246, 0.08)',
warning: '#F59E0B',
'warning-surface': 'rgba(245, 158, 11, 0.08)',
```

IMPORTANT: Do NOT modify any existing values. Only ADD new lines.
  </action>
  <verify>
Read livos/packages/ui/tailwind.config.ts and confirm:
- `radius-2xl` with value `24px` exists
- `radius-3xl` with value `28px` exists
- `info` with value `#3B82F6` exists
- `warning` with value `#F59E0B` exists
- `info-surface` and `warning-surface` exist
- All existing values remain unchanged
  </verify>
  <done>Added 2 semantic radius tokens (radius-2xl, radius-3xl) and 4 status color tokens (info, warning, info-surface, warning-surface).</done>
</task>

<task type="auto">
  <name>Task 2: Add left border accent to AI chat assistant messages</name>
  <files>livos/packages/ui/src/routes/ai-chat/index.tsx</files>
  <action>
In the ChatMessage component (around line 87-89), find the assistant message styling:

Current:
```typescript
className={cn('rounded-radius-xl px-4 py-3',
    isUser ? 'bg-brand text-white' : 'bg-surface-2 text-text-primary'
)}
```

Change the assistant (non-user) branch to add a left border accent:

New:
```typescript
className={cn('rounded-radius-xl px-4 py-3',
    isUser ? 'bg-brand text-white' : 'bg-surface-2 text-text-primary border-l-2 border-brand/30'
)}
```

This adds a subtle 2px left border in the brand color at 30% opacity, giving assistant messages a distinct visual accent without being too prominent.
  </action>
  <verify>
Read the ChatMessage function in livos/packages/ui/src/routes/ai-chat/index.tsx and confirm:
- Assistant messages have `border-l-2 border-brand/30` classes
- User messages (isUser branch) remain unchanged with `bg-brand text-white`
  </verify>
  <done>Assistant messages now have a subtle brand-colored left border accent for visual distinction.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Confirm new tokens in tailwind.config.ts (radius-2xl, radius-3xl, info, warning, info-surface, warning-surface)
2. Confirm assistant message left border in ai-chat/index.tsx
3. Run `cd livos && pnpm build` to verify no syntax errors
</verification>

<success_criteria>
- DE-01: radius-2xl (24px) and radius-3xl (28px) semantic tokens exist
- DE-02: info (#3B82F6) and warning (#F59E0B) colors with surface variants defined
- DE-03: AI Chat assistant messages have left border accent (border-l-2 border-brand/30)
- No syntax errors — both files parse correctly
- No existing values modified
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-03-design-enhancements/03-01-SUMMARY.md`
</output>
