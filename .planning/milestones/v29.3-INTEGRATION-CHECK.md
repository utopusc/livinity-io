---
milestone: v29.3
checked: 2026-05-01T00:00:00Z
status: gaps_found
flows_verified: 2/3
seams_verified: 7/8
---

## Integration Check v29.3 Marketplace AI Broker (Subscription-Only)

### Seam Q1 - Phase 39: OAuth Fallback Closure (FR-RISK-01)

PASS

nexus/packages/core/src/providers/claude.ts getClient() lines 99-128 tries getApiKey() and on failure throws ClaudeAuthMethodMismatchError. Zero authToken: key-value constructions exist. Comments at lines 105-107 describe deleted code only. isAvailable() credentials-file branch removed (lines 309-314 document it). No bearer-token Anthropic client construction anywhere in file.

REQ-IDs satisfied: FR-RISK-01

---
### Seam Q2 - Phase 40: homeOverride Field and SdkAgentRunner Wiring (FR-AUTH-03)

PASS

nexus/packages/core/src/agent.ts line 34: homeOverride?: string declared in AgentConfig.
nexus/packages/core/src/sdk-agent-runner.ts line 297: HOME: this.config.homeOverride || process.env.HOME || /root
Phase 40 AI Chat HOME wiring intentionally deferred to Phase 41 per plan.

REQ-IDs satisfied: FR-AUTH-03

---

### Seam Q3 - Phase 41 Wiring of X-LivOS-User-Id (FR-AUTH-03, FR-BROKER-A-04)

PASS

livos/.../livinity-broker/agent-runner-factory.ts line 58: sends X-LivOS-User-Id: userId when multiUser and not BROKER_FORCE_ROOT_HOME.
nexus/packages/core/src/api.ts lines 2418-2436: reads header, validates, sets homeOverride = join(dataDir, users, headerUserId, .claude).
api.ts line 2513: homeOverride spread into agentConfig.
BROKER_FORCE_ROOT_HOME=true escape hatch documented at agent-runner-factory.ts lines 17-25.

REQ-IDs satisfied: FR-AUTH-03, FR-BROKER-A-04

---

### Seam Q4 - Phase 41 Broker Mount (FR-BROKER-A-01, FR-BROKER-A-02)

PASS

server/index.ts line 45 imports mountBrokerRoutes. Line 1217: mountUsageCaptureMiddleware called BEFORE line 1223 mountBrokerRoutes. livinity-broker/index.ts line 27: app.use(/u, router). livinity-broker/router.ts line 30: containerSourceIpGuard is first middleware before body parsing. Full RFC 1918 allowlist at auth.ts lines 37-59.

REQ-IDs satisfied: FR-BROKER-A-01, FR-BROKER-A-02

---
### Seam Q5 - Phase 42 Reuse of Phase 41 Infrastructure (FR-BROKER-O-01 to FR-BROKER-O-04)

PASS with OpenAI SSE usage-token gap

registerOpenAIRoutes(router, deps) at router.ts line 168 inside createBrokerRouter() inherits router-level containerSourceIpGuard and express.json. Routes /:userId/v1/chat/completions and /:userId/v1/models under same /u prefix. Both sync and SSE paths call createSdkAgentRunnerForUser (same factory as Phase 41). resolveModelAlias called at openai-router.ts line 143; unknown-model warning logged at line 145.

GAP (FR-BROKER-O-02, FR-DASH-01, FR-DASH-02): parse-usage.ts line 108 documents current broker Phase 42 does NOT emit usage in SSE chunks. OpenAI SSE streaming requests produce no broker_usage row.

---

### Seam Q6 - Phase 43 Broker URL Injection (FR-MARKET-01, FR-MARKET-02)

PASS structurally; MiroFish manifest is draft-only

inject-ai-provider.ts lines 8-9: base = http://livinity-broker:8080/u/userId, v1 = base/v1. Matches actual broker mount. Injects 8 env vars: ANTHROPIC_BASE_URL, ANTHROPIC_REVERSE_PROXY, LLM_BASE_URL, OPENAI_API_BASE_URL, OPENAI_LIKE_API_BASE_URL, ANTHROPIC_API_KEY sentinel, OPENAI_API_KEY sentinel. extra_hosts: livinity-broker:host-gateway appended at line 87. apps.ts line 1061 inside installForUser: unconditional call; no-op when requiresAiProvider not true (line 52). Single-user path at apps.ts lines 478-496 also covered. UI badge at app-content.tsx lines 37-41 and 60-64 conditional on app.requiresAiProvider.

GAP (FR-MARKET-02): MiroFish manifest in .planning/phases/43-.../draft-mirofish-manifest/ is planning material only, not registered as builtin or platform-store app.

---

### Seam Q7 - Phase 44 Capture Middleware Placement (FR-DASH-01, FR-DASH-02, FR-DASH-03)

PASS with 429 propagation gap

server/index.ts line 1217 before line 1223 (ordering correct). capture-middleware.ts line 152: app.use(/u/:userId/v1, createCaptureMiddleware(livinityd)). parse-usage.ts imports zero symbols from livinity-broker/*. Anthropic sync: usage.input_tokens/usage.output_tokens (lines 75-76). OpenAI sync: usage.prompt_tokens/usage.completion_tokens (lines 88-89). Anthropic SSE: message_start.message.usage.input_tokens + message_delta.usage.output_tokens. OpenAI SSE: returns null (Q5 GAP). 429 guard at capture-middleware.ts line 134 is unreachable via broker error path (see Flow C).

---

### Seam Q8 - Phase 44 tRPC Routes Registration (FR-DASH-01, FR-DASH-02)

PASS with transport gap

routes.ts line 30: getMine uses privateProcedure. Line 67: getAll uses adminProcedure. server/trpc/index.ts lines 18+45: usage imported and registered in appRouter.
GAP: Neither usage.getMine nor usage.getAll in httpOnlyPaths (common.ts lines 8-194 exhaustively checked). Both route over WebSocket by default. Inconsistent with project practice (system.checkUpdate moved to HTTP for same degraded-WS reason).

---
### E2E Flow A - Container to Credentials (All broker REQ-IDs)

PASS with BROKER_FORCE_ROOT_HOME caveat

All links verified: extra_hosts maps broker hostname (inject-ai-provider.ts:86) -> IP guard admits Docker bridge IPs (auth.ts:47-58) -> request translated -> agent-runner-factory fetches nexus:3200/api/agent/stream with X-LivOS-User-Id (line 58) -> api.ts reads header and sets homeOverride (lines 2418-2436) -> homeOverride spread into agentConfig (line 2513) -> SdkAgentRunner.safeEnv.HOME = per-user dir (line 297) -> Claude CLI reads credentials from that dir. Capture middleware taps res.write/res.end for SSE and res.json for sync. Caveat: BROKER_FORCE_ROOT_HOME=true bypasses per-user isolation globally with no per-request log trace.

---

### E2E Flow B - MiroFish Install (FR-MARKET-01, FR-MARKET-02)

PASS structurally; FR-MARKET-02 blocked on manifest registration

Install path: installForUser -> injectAiProviderConfig (apps.ts:1061) -> 8 env vars + extra_hosts -> patched YAML written -> docker compose up -d -> container uses ANTHROPIC_BASE_URL etc. -> Flow A executes. Blocking gap: MiroFish manifest must be promoted from draft to registered app before marketplace UI can trigger this flow.

---

### E2E Flow C - 429 Rate Limit Propagation (FR-DASH-03)

BROKEN

Anthropic 429 surfaces as thrown error inside SdkAgentRunner. agent-runner-factory.ts lines 75-76: only reads response.status from nexus HTTP response; stream errors arrive as AgentEvent type:error data:message_string, not HTTP 429 status. Broker catch block at router.ts line 159 returns res.status(500). capture-middleware.ts line 134 checks res.statusCode === 429 which is unreachable. Retry-After header not extracted from Anthropic SDK error and not forwarded. Result: Anthropic 429 -> broker HTTP 500 to container; throttled row not written; banner stays none; container receives 500 not 429 with no Retry-After.

---

### Seam Q12 - Sacred File Integrity (FR-AUTH-03 baseline)

GAP - integrity test will FAIL

git hash-object nexus/packages/core/src/sdk-agent-runner.ts = 4f868d318abff71f8c8bfbcf443b2393a553018b
sdk-agent-runner-integrity.test.ts line 33 asserts BASELINE_SHA = 623a65b9a50a89887d36f770dcd015b691793a7f
Hashes differ. homeOverride wiring is correct and present (Q2 PASS). This is a test maintenance gap, not a functional gap. The sacred-file guard mechanism is broken and will fail when run.

---

## Milestone-Level Note on VERIFICATION.md Absence

No VERIFICATION.md exists for phases 39-44. UAT deferred to post-deploy per milestone scope. Flagged once.

---
## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| FR-RISK-01 | Phase 39 claude.ts getClient() throws ClaudeAuthMethodMismatchError on no API key | WIRED | none |
| FR-AUTH-01 | Phase 40 per-user-claude.ts getUserClaudeDir()+ensureUserClaudeDir() create per-user .claude/ | WIRED | none |
| FR-AUTH-02 | Phase 40 ai/routes.ts three procedures + ai-config.tsx consumes all three | WIRED | claudePerUserStartLogin is tRPC subscription (WS) not on httpOnlyPaths |
| FR-AUTH-03 | Phase 40 AgentConfig.homeOverride -> Phase 41 header -> api.ts -> SdkAgentRunner line 297 | WIRED | Integrity test SHA stale (Q12) |
| FR-BROKER-A-01 | Phase 41 POST /:userId/v1/messages -> IP guard -> validation -> runner -> sync JSON | WIRED | none |
| FR-BROKER-A-02 | Phase 41 SSE path -> createSseAdapter emits Anthropic event sequence | WIRED | none |
| FR-BROKER-A-03 | Phase 41 translate-request.ts maps system+messages to sdkArgs | WIRED | Client tool defs ignored per D-41-14 |
| FR-BROKER-A-04 | Phase 41 agent-runner-factory.ts -> X-LivOS-User-Id -> api.ts homeOverride | WIRED | BROKER_FORCE_ROOT_HOME bypass untraced per-request |
| FR-BROKER-O-01 | Phase 42 POST /:userId/v1/chat/completions -> sync OpenAI JSON | WIRED | none |
| FR-BROKER-O-02 | Phase 42 OpenAI SSE adapter emits OpenAI-format chunks | WIRED | SSE usage row not captured (parse-usage returns null) |
| FR-BROKER-O-03 | Phase 42 resolveModelAlias() maps gpt-4/gpt-4o/claude-* to default | WIRED | none |
| FR-BROKER-O-04 | Phase 42 buildSyncOpenAIResponse() produces OpenAI shape | WIRED | No live round-trip test executed |
| FR-MARKET-01 | Phase 43 injectAiProviderConfig() at apps.ts:1061 + apps.ts:489 | WIRED | none |
| FR-MARKET-02 | Phase 43 MiroFish manifest draft in planning dir; not in builtins/platform store | PARTIAL | Manifest must be registered before install flow can execute |
| FR-DASH-01 | Phase 44 usage.getMine privateProcedure -> usage-section.tsx useQuery | WIRED | Not on httpOnlyPaths; OpenAI SSE rows missing |
| FR-DASH-02 | Phase 44 usage.getAll adminProcedure -> admin-cross-user-view.tsx useQuery | WIRED | Not on httpOnlyPaths; OpenAI SSE rows missing |
| FR-DASH-03 | Phase 44 capture middleware 429-throttled row + banner computation | PARTIAL | Broker returns HTTP 500 for upstream 429; throttled row unreachable; Retry-After not forwarded |

Requirements with no cross-phase wiring (self-contained):
FR-BROKER-A-03: translator is internal to Phase 41; output consumed within same router module.

---

## Cross-Phase Wiring Gaps Punch List

| # | Gap | Affected REQ-IDs | File:Line |
|---|-----|-----------------|-----------|
| 1 | Broker error path returns HTTP 500 for all upstream errors; capture middleware res.statusCode===429 guard unreachable via broker | FR-DASH-03 | livos/.../livinity-broker/router.ts:159, usage-tracking/capture-middleware.ts:134 |
| 2 | Retry-After header not extracted from Anthropic SDK error; not forwarded to container | FR-DASH-03 | livos/.../livinity-broker/agent-runner-factory.ts:75-76 |
| 3 | Sacred-file integrity test SHA stale: expected 623a65b9..., actual 4f868d31...; test will FAIL when run | FR-AUTH-03 | nexus/packages/core/src/providers/sdk-agent-runner-integrity.test.ts:33 |
| 4 | OpenAI SSE streaming produces no broker_usage row (parse-usage.ts returns null; documented limitation) | FR-BROKER-O-02, FR-DASH-01, FR-DASH-02 | livos/.../usage-tracking/parse-usage.ts:108-110 |
| 5 | claudePerUserStartLogin is tRPC subscription (WS); not on httpOnlyPaths; login can hang silently on bad WS | FR-AUTH-02 | livos/.../modules/ai/routes.ts:403, server/trpc/common.ts (absent) |
| 6 | usage.getMine and usage.getAll not on httpOnlyPaths; WS-only; inconsistent with project practice | FR-DASH-01, FR-DASH-02 | livos/.../usage-tracking/routes.ts:89-93, server/trpc/common.ts (absent) |
| 7 | MiroFish manifest is planning draft only; not registered as builtin or platform app | FR-MARKET-02 | .planning/phases/43-.../draft-mirofish-manifest/ |