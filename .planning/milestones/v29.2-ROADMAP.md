# v29.2 Factory Reset (mini-milestone) — Archived Roadmap

**Status:** ✅ **SHIPPED 2026-04-29**
**Duration:** 2026-04-28 (planning) → 2026-04-29 (shipped) — 2 days
**Phases:** 36 (audit) → 37 (backend) → 38 (UI)
**Audit:** [v29.2-MILESTONE-AUDIT.md](../v29.2-MILESTONE-AUDIT.md) — passed (19/19 reqs, 3/3 phases, 10/10 integration)

## Goal

Tek tıkla "fabrika ayarlarına dön" — kullanıcı UI'dan Settings > Advanced > Factory Reset'e bastığında kirli/bozuk bir Mini PC durumundan SSH'e gerek kalmadan temiz bir kuruluma geri dönebilsin.

## Phases

### Phase 36: install.sh Audit & Hardening (entry — audit-only)
**Goal:** De-risk the entire factory-reset chain by proving (or hardening) that `livinity.io/install.sh` exists, accepts an API key without leaking it, runs idempotently on already-installed hosts, and has a recovery story for half-deleted state.
**Requirements:** FR-AUDIT-01..05 (5/5)
**Status:** ✅ Complete 2026-04-29
**Plans (3):**
- 36-01-snapshot-provenance — Fetch install.sh, capture headers, scaffold AUDIT-FINDINGS.md
- 36-02-static-analysis — Argument surface + idempotency verdict + API key transport
- 36-03-recovery-server5-hardening — Recovery model + Server5 fallback + hardening proposals + Phase 37 readiness

**Key findings:**
- install.sh: **NOT-IDEMPOTENT** (74 commands classified, 4 critical NOT_IDEMPOTENT at line:861/1086/1125/1136-1137)
- API key transport: **argv-only** (FR-AUDIT-04 FAIL → wrapper proposed)
- Recovery: pre-wipe `tar -czf` snapshot (install.sh has no `--resume`)
- Server5 fallback: cached install.sh on Mini PC at `/opt/livos/data/cache/install.sh.cached`
- All 4 D-10 Phase 37 readiness questions answered with literal bash

### Phase 37: Backend Factory Reset
**Goal:** A `system.factoryReset({preserveApiKey})` tRPC route triggers an idempotent root-level wipe of LivOS (services + scoped Docker + DB + filesystem) and re-executes install.sh as a detached cgroup-escaped process so it survives killing its own livinityd parent.
**Requirements:** FR-BACKEND-01..07 (7/7)
**Status:** ✅ Complete 2026-04-29
**Plans (4):**
- 37-01-bash-scripts — factory-reset.sh + livos-install-wrap.sh (shellcheck-clean)
- 37-02-trpc-route — adminProcedure + httpOnlyPaths + Zod + pre-flight + key stash
- 37-03-spawn-deploy — `systemd-run --scope --collect` cgroup-escape + lazy first-call artifact deploy
- 37-04-failure-handling-integration — PIPESTATUS exit capture + 4-way classification + opt-in destructive integration scaffold

**Tests:** 41/41 unit (vitest, hermetic). 3 bash artifacts shellcheck-clean. Opt-in destructive integration test ships as scaffold (4 fail-closed gates).

### Phase 38: UI Factory Reset
**Goal:** A user can find the Factory Reset button in Settings > Advanced, read an explicit list of what will be deleted, type-to-confirm, choose preserve-account-vs-fresh, and watch the reinstall progress in a BarePage cover.
**Requirements:** FR-UI-01..07 (7/7)
**Status:** ✅ Complete 2026-04-29
**Plans (4):**
- 38-01-foundation-and-unbreak — Pure-logic lib (types/error-tags/state-machine/typed-confirm/network-preflight/deletion-list) + un-break Phase 37 schema in useReset + GlobalSystemStateContext
- 38-02-danger-zone-button — Settings > Advanced Danger Zone red button + admin gate + non-admin note
- 38-03-confirmation-modal — Explicit-list modal + radio + type-to-confirm + pre-flight gating
- 38-04-progress-overlay-and-post-reset-routing — BarePage progress overlay + listUpdateHistory poll @ 2s + 90s reconnect threshold + redirect/error fan-out + recovery static page

**Tests:** 143/143 unit (vitest + jsdom + smoke + pure-logic + source-text invariant). RTL not installed — patterns mirror existing project conventions.

## Stats

- **Phases:** 3
- **Plans:** 11
- **Tests:** 184/184 passing
- **Source files modified/added:** 48 across `livos/packages/{livinityd,ui}/`
- **LOC delta:** +3956 / -342 in source code; +12813 / -? in `.planning/`
- **Git range:** `510183a1..c9dbf44b` (50 commits)
- **Timeline:** 2026-04-28 (Phase 36 plan) → 2026-04-29 (Phase 38 ship) — ~2 days continuous

## Key Accomplishments

1. **Static-analysis audit produced concrete Phase 37 contract.** AUDIT-FINDINGS.md's "Phase 37 Readiness" section answered all 4 hard-gate questions (reinstall command, recovery action, idempotency boolean, API key transport mechanism) with literal bash — Phase 37's planner read it alone and designed the wipe+reinstall without re-running the audit.
2. **install.sh argv-leak closed (degraded mode).** v29.2 ships a `livos-install-wrap.sh` wrapper that closes the route-spawn argv leak window. Full env-var transport (closes install.sh's own argv window) deferred to v29.2.1 as a 5-line install.sh patch.
3. **Cgroup-escape spawn pattern reused from v29.1.** `systemd-run --scope --collect` argv copied verbatim from `reference_cgroup_escape.md` — wipe survives `systemctl stop livos` mid-flight.
4. **Idempotent wipe with literal-path safety.** factory-reset.sh has no `eval`, no unscoped `docker volume prune`, no `docker stop $(docker ps -aq)`, no variable-derived `rm -rf`. Pre-wipe `tar -czf` snapshot before any destructive op; trap-on-EXIT API key cleanup.
5. **UI consent surface IS the deletion list.** Modal renders 7 verbatim deletion items as `<ul>` + `<li>` (not paragraph). Strict-equality `FACTORY RESET` matcher (no trim/lowercase) gated by ≥7 negative variant tests.
6. **Forward-compat for v30.0 Backup.** `// TODO: backup mutex (v30.0)` placeholder in modal pre-flight; wires once `BAK-SCHED-04` ships.

## Carry-Forwards (v29.2.1)

- install.sh env-var fallback patch (closes install.sh's own argv leak window)
- install.sh ALTER USER patch (improves install.sh's native idempotency)
- update.sh patch to populate `/opt/livos/data/cache/install.sh.cached`

## Manual Verification (opt-in, not blockers)

- Phase 37: `factory-reset.integration.test.sh` (4 fail-closed gates; destructive; scratchpad-only; refuses bruce@10.69.31.68)
- Phase 38: 11 browser-based UI flow checks (modal visuals, tooltips, redirect observation, error/recovery page flows)

## Hard Rules Honored

- Server4 OFF-LIMITS — only off-limits disclaimers in source/docs (project memory hard rule)
- Mini PC (`bruce@10.69.31.68`) is the sole LivOS deployment target
- No live install.sh execution during audit/build
- No source-tree edits to `livos/packages/livinityd/` from Phase 38 (UI consumer-only)
- All shellcheck-gated bash; all tsc --noEmit-clean TypeScript

---

*Archived: 2026-04-29*
