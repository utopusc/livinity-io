# v34 Handoff — 2026-05-13 Session End State + Action Plan

**Generated:** 2026-05-13 by Claude Opus 4.7
**Session duration:** ~7 hours
**Commits shipped this session:** 32 (`ca623cc0..e6e57e7f`)
**Sacred SHA:** `f3538e1d811992b782a9bb057d1b7f0a0189f95f` — preserved 32/32 ✓

---

## Mainserver Snapshot (`154.53.56.75` / `test.livinity.live`)

| Component | State | Notes |
|---|---|---|
| `https://test.livinity.live` (root) | ✅ Working | Login screen, register, dashboard all OK |
| `*.test.livinity.live` Cloudflare DNS | ✅ Wildcard A-record exists (id `1c76adcc51dc3d00d1a74cf4c6bec3cf`) |
| Caddy TLS (wildcard cert) | ✅ Obtained via LE DNS-01 | `wildcard_.test.livinity.live` cert dir present |
| Caddyfile | ✅ `test.livinity.live, *.test.livinity.live { tls { dns cloudflare } reverse_proxy 127.0.0.1:8080 }` | Single SAN block, all subdomains proxy to livinityd |
| 5/5 systemd services | ✅ active | livos, liv-core, liv-worker, liv-memory, caddy |
| Sacred SHA | ✅ `f3538e1d8...` |
| livinityd `livos:domain:*` Redis | ✅ Clean | config DELETED, subdomains has n8n entry |
| livinityd `livos:platform:*` Redis | ❌ Empty | App Store iframe needs api_key seed |
| Tunnel | ✅ Idle | "No API key configured, staying idle" |

## Confirmed Issues Pending Resolution

### Issue 1: App Store iframe — "Connect to Livinity Platform" prompt
**Root cause:** `livos:platform:api_key` is empty (DELed during 503 troubleshooting earlier in session). App Store UI gate at `app-store-content.tsx:52` requires this key.

**Resolution paths (pick one):**
- **A — Manual paste (5 sec):** User pastes their plain `liv_k_PQwvNjjp...` key in chat → orchestrator SSH-injects to mainserver Redis → restart livinityd → done. User must re-enter on Mini PC Settings too.
- **B — Server5 rotate (blocked):** Server5 down → can't rotate. Need Contabo panel restart first.
- **C — Phase 111 ship (proper):** Server5 dashboard wizard generates fresh key, install command auto-seeds it. Blocked by Server5 down.

**Recommendation:** A for immediate UAT; C for production UX.

---

### Issue 2: n8n WebApp not opening
**Root cause:** TWO independent bugs:

**Bug A: `appToUrl` heuristic was mis-stripping 3-part custom domains** → fixed in commit `e6e57e7f`. n8n now correctly composes `https://n8n.test.livinity.live`. **VERIFIED WORKING in commit.**

**Bug B (NEW — discovered 2026-05-13T22:03Z):** Even with correct subdomain URL + DNS + Caddy cert, livinityd's gateway middleware (`server/index.ts:150-200`) does NOT proxy `n8n.test.livinity.live` requests to the n8n container (127.0.0.1:5678). Instead serves livinityd's own UI (HTML index) at the subdomain. Evidence:
```
curl -sIL -m 5 -H "Host: n8n.test.livinity.live" http://127.0.0.1:8080
HTTP/1.1 200 OK
Content-Security-Policy: ... (livinityd CSP)
```
Expected: 302 redirect or 200 with n8n's HTML (not livinityd's).

**Hypothesis:** Gateway reads `domainInfo.appMapping[subPrefix]` (from `livos:custom_domain:*` namespace) but not `livos:domain:subdomains` (where n8n entry actually lives). The two systems may not be wired together. Need to inspect server/index.ts:150-200 gateway middleware end-to-end.

**Plan:** New phase **112** — WebApp subdomain gateway proxy fix. Estimate 1 plan, 3 tasks (investigate + fix routing + UAT). Quick: ~1 hour.

---

### Issue 3: Server5 (`45.137.194.102`) DOWN
**Root cause:** Per executor agent diagnosis 2026-05-13T21:30Z — TCP SYN/ACK OK but SSH KEX disconnects ("Connection reset by peer"). Caddy `livinity.io` timeout during HTTPS handshake. Likely high load, OOM, disk full, or service deadlock. fail2ban DROP excluded (handshake completes).

**Required action:** Contabo control panel → Server5 → Restart (or console access for `systemctl restart sshd caddy && pm2 restart all && uptime && free -m && df -h /`).

**Blocks:** Phase 111 execute (entire wizard ships to Server5 platform/web).

---

### Issue 4: Mini PC API key invalidated
**Root cause:** This session rotated `burakcanoztruk@gmail.com` key (old prefix `liv_k_jxYiqPKt` → new prefix `liv_k_PQwvNjjp`). Mini PC has the OLD key cached in `livos:platform:api_key`. App Store on Mini PC will silently break next reload.

**Resolution:** User pastes new plain key, orchestrator SSH-injects to Mini PC Redis (same pattern as mainserver fix).

---

### Issue 5: Caddy logs CLOUDFLARE_API_TOKEN in plaintext
**Root cause:** `journalctl -u caddy` contains the FULL `CLOUDFLARE_API_TOKEN=cfut_...` env-var on every reload. This was actually how I recovered the token earlier in this session.

**Severity:** Medium — anyone with mainserver root or journalctl read access can recover the CF token.

**Resolution:** Set Caddy systemd unit to redirect stderr to /dev/null OR use a Caddy plugin that masks env-vars in logs. Defer to a v34 polish phase.

---

### Issue 6: Spotlight + cmdk dangling references to `/app-store/*`
**Root cause:** Phase 108 revert + native route removal (commit `40fdd1af`) left these UI components calling `navigate('/app-store/{appId}')` which now 404s silently.

**Files:**
- `livos/packages/ui/src/components/apple-spotlight.tsx:545,563`
- `livos/packages/ui/src/components/cmdk.tsx:239,254`

**Resolution:** Remove app-search results from spotlight + cmdk OR redirect those handlers to "open in App Store iframe" flow. Defer to Phase 107 (default apps cleanup) — naturally fits in scope.

---

## Updated v34 Phase Sequence

```
v34.0 Milestone — Bootstrap Polish + First-Run UX

✅ Phase 106  — bootstrap-layer hotfix back-port (SHIPPED + UAT PASSED)
✅ Phase 109  — MCP auto-seed (SHIPPED + UAT PASSED via 109-02 + 109-03 hotfixes)
❌ Phase 108  — App Store local mode (REVERTED post-UAT; design rejected)
✅ v34 misc   — _dld_seed_platform_api_key + appToUrl fix + /app-store routes removed + DNS wildcard

⏳ Phase 107  — Default apps + dock cleanup + spotlight/cmdk dangling-ref cleanup
              (~2-3 plans; UI-only changes)

⏳ Phase 110  — WebApp Launcher VNC swap (carry-over from v33)
              (~3-4 plans; streaming layer)

⏳ Phase 111  — Server5 dashboard install wizard
              (5 plans PLANNED, execute BLOCKED by Server5 down)

⏳ Phase 112  — WebApp subdomain gateway proxy fix (NEW)
              (~1 plan, 3 tasks; investigate livinityd server/index.ts:150-200
              gateway, fix routing to use livos:domain:subdomains
              alongside livos:custom_domain:* mappings)

⏳ Phase 113  — Caddy token-leak remediation (NEW)
              (~1 plan, 1 task; mask CLOUDFLARE_API_TOKEN in journalctl)
```

## Recommended Next-Session Order

1. **Quick wins (~30 min total):**
   - User pastes liv_k_PQwvNjjp... key → orchestrator inject mainserver Redis + Mini PC Redis → App Store iframe works on both
   - User restarts Server5 via Contabo panel → confirm 45.137.194.102 alive

2. **Phase 112 (highest user impact):** Fix n8n subdomain routing. Without this, App Store install of any app is half-broken (installs container, can't access app).

3. **Phase 111 execute:** Wave 1 paralel 3 plan (install.sh route fix + api_keys API + cf-resolve API). Wave 2 wizard UI. Wave 3 docs. ~2-3 hours.

4. **Phase 110 plan:** VNC swap from fMP4 (separate from this session's work).

5. **Phase 107 plan:** Default apps cleanup + spotlight/cmdk dangling refs (lower priority).

## Memory Files Worth Reading Next Session

- `~/.claude/projects/.../memory/project_v34_session_2026_05_13.md` — this session's detailed log
- `~/.claude/projects/.../memory/project_p106_complete.md`
- `~/.claude/projects/.../memory/project_v34_account_tunnel_marketplace_vision.md` — original v34 vision

## Critical Invariants Maintained This Session

- **Sacred SHA `f3538e1d811992b782a9bb057d1b7f0a0189f95f`** — preserved through 32/32 commits
- **D-NO-PROD-IMPACT** on Mini PC's `livos/install.sh` + `update.sh` (untouched)
- **D-104-RELAY-ZERO-DATA-PLANE** — restored (tunnel disabled, only test.livinity.live direct)

## What NOT To Do Next Session

- ❌ Do NOT re-attempt Phase 108 local-mode App Store (user explicitly rejected post-UAT)
- ❌ Do NOT auto-enable `livos:platform:enabled=1` (causes livinityd to register `utopusc.livinity.io` subdomain → Caddy tries cert for it → Server5 ACME fails → all 443 TLS errors). Only api_key without enabled is safe.
- ❌ Do NOT touch sacred `liv/packages/core/src/sdk-agent-runner.ts` (SHA gate enforced)
- ❌ Do NOT use `--no-verify` on git commits

## Quick Session Resume Steps

```bash
# Verify mainserver still healthy
ssh -i C:/Users/hello/Desktop/Projects/contabo/pem/contabo_master \
    -o StrictHostKeyChecking=no root@154.53.56.75 \
    'systemctl is-active livos liv-core liv-worker liv-memory caddy; \
     curl -sIL -m 5 https://test.livinity.live 2>&1 | head -3; \
     git hash-object /opt/liv/packages/core/src/sdk-agent-runner.ts'

# Verify Server5 (expect "alive" or "still down")
ssh -i .../contabo_master -o ConnectTimeout=10 root@45.137.194.102 \
    'uptime; systemctl is-active caddy postgresql' 2>&1 | head -5

# Verify local repo state
cd C:/Users/hello/Desktop/Projects/contabo/livinity-io
git status
git log --oneline -5
git hash-object liv/packages/core/src/sdk-agent-runner.ts
# Expect: f3538e1d811992b782a9bb057d1b7f0a0189f95f
```

If all 3 above return expected → resume with Phase 112 (n8n routing fix).
