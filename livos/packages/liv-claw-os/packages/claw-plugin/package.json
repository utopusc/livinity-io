{
  "name": "@openuidev/openclaw-os-plugin",
  "version": "0.1.5",
  "type": "module",
  "description": "OpenClaw plugin that turns agent responses into Generative UI and serves the claw-client at /plugins/openclawos.",
  "keywords": [
    "openclaw",
    "openclaw web ui",
    "openclaw-plugin",
    "openclaw-os",
    "openui",
    "openui-lang",
    "generative-ui",
    "agent-ui"
  ],
  "license": "MIT",
  "homepage": "https://openui.com/openclaw-os",
  "repository": {
    "type": "git",
    "url": "https://github.com/thesysdev/openclaw-os.git",
    "directory": "packages/claw-plugin"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "static",
    "skills",
    "prompts",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.js --external:openclaw --external:openclaw/* --external:node:* --loader:.json=json --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "bundle-ui": "cd ../claw-client && pnpm install --frozen-lockfile=false && pnpm build && shx rm -rf ../claw-plugin/static && shx cp -r out ../claw-plugin/static",
    "prepack": "pnpm bundle-ui && pnpm build",
    "open": "node ../../scripts/open-ui.mjs",
    "lint:check": "eslint ./src",
    "lint:fix": "eslint ./src --fix",
    "format:check": "prettier --check ./src",
    "format:fix": "prettier --write ./src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "ci": "pnpm lint:check && pnpm format:check && pnpm typecheck"
  },
  "dependencies": {
    "@openuidev/lang-core": "^0.2.2"
  },
  "peerDependencies": {
    "openclaw": ">=2026.4.12"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "esbuild": "^0.25.0",
    "shx": "^0.3.4",
    "openclaw": "*",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.4.12",
      "minGatewayVersion": "2026.4.12"
    },
    "build": {
      "openclawVersion": "2026.4.12",
      "pluginSdkVersion": "2026.4.12"
    }
  }
}
