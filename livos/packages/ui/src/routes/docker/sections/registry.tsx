// Phase 29 Plan 29-02 — Registry section (DOC-16). Replaces the Phase 24
// placeholder; the live implementation lives in routes/docker/registry/.
export {RegistrySection as Registry} from '../registry/registry-section'
