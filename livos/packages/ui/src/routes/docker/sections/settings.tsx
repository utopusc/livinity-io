// Phase 29 Plan 29-02 — Docker Settings section (DOC-17). Replaces the
// Phase 24 placeholder; the live implementation lives in routes/docker/settings/.
export {SettingsSection as Settings} from '../settings/settings-section'
