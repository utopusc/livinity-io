// Phase 174-01 — ItemTreeRow scaffold (skeleton only).
//
// Stubbed by Plan 174-01. Plan 174-03 fills in per-type icon + label +
// optional badge using the D-V38-O palette (Project=warm-amber/folder-kanban,
// Agent=cyan/bot, Chat=text-secondary/message-square) via Livinity DS tokens.
//
// Do NOT add behaviour here — Plan 174-03 owns the body.

export interface ItemTreeRowProps {
	/** The Item shape from vault.items.list — Plan 174-03 narrows on `type`. */
	item: unknown
}

export function ItemTreeRow(_props: ItemTreeRowProps) {
	// Stub body — Plan 174-03 replaces with icon + label + badge.
	return null
}
