# scripts/install/EXPECTED_RELEASE — LivOS release pin (Phase 311-04, UPDSAFE-02)
#
# WHAT THIS IS
#   A single "pin line" naming the release this repo tree belongs to. update.sh's
#   livos_verify_fetched_ref() reads it out of the freshly-cloned release tree and
#   compares the resolved commit against the fetched HEAD.
#
# FORMAT (consumed by update.sh:  grep -vE '^\s*(#|$)' | head -1 | tr -d '[:space:]')
#   * Lines beginning with '#' and blank lines are ignored.
#   * The FIRST non-comment line is the pin.
#   * Use the FULL  refs/tags/<tag>  form. A refs/tags/<tag> line is resolved to a
#     commit via  git rev-parse "<tag>^{commit}"  INSIDE the cloned tree, then
#     compared to the fetched HEAD SHA. Because boxes clone a release BY that same
#     tag, HEAD == the tag's commit == what refs/tags/<tag> resolves to → a
#     self-consistent match.
#
# WARN-ONLY (Phase 311) — NOT enforcement
#   In Phase 311 a pin mismatch is WARN-ONLY: livos_verify_fetched_ref() logs a
#   SIGNATURE-WARN, records a signature_verification verdict into the update-history
#   JSON, and PROCEEDS — it never aborts an update. The fail-closed flip (refuse to
#   deploy on mismatch) is Phase 312, a strictly LATER release, gated on one full
#   release cycle of this warn-only telemetry. Do NOT add enforcement here or in the
#   verify function as part of Phase 311.
#
# CURRENT VALUE = refs/tags/v1.1.12 (STABLE, first-run unblock — Phase 368.7 ONBOARD2FA.
#   Cut from v1.1.1 plus the four 368.7 commits ONLY, NOT master, for the same reason
#   v1.1.1 was: master still carries Phase 368.5 Safety Snapshots, whose stable gate
#   (exclude vm-data + Chrome profile caches, mid-run/ENOSPC handling, live-box growth
#   measurement) is still open. See the v1.1.1 note below.
#   WHY THIS IS URGENT: every fresh DB-backed install since 4d03b2e2 (2026-07-16) has
#   been dead-ended at the setup 2FA screen. enable2fa verifies the CORRECT code, sets
#   totp_enabled and returns 10 recovery codes; use-2fa fires its completion callback
#   ONLY when recoveryCodes is empty (Phase 328, for the Settings panel), which on a DB
#   session it never is; onboarding's account-step passed onContinue as that callback and
#   never read recoveryCodes/confirmEnrolled — so the wizard never advanced, and the
#   sub-state rendered no FooterBar, so there was no Back, Skip or Continue. Every retry
#   then hit the alreadyEnabled guard (thrown BEFORE the token is verified) and rendered
#   as the same shake as a wrong code. Two external testers hit this and never saw the
#   product. NOT a clock or TOTP bug — the crypto was cross-checked against a
#   from-scratch RFC 6238 reference and is correct.
#   ALSO IN THIS CUT: 2FA is now OPTIONAL at setup (operator decision 2026-07-25);
#   recovery codes are finally enterable at login (they are 16 hex chars and every entry
#   surface was a 6-char digits-only PinInput, so the documented anti-lockout escape was
#   unreachable — a drifted clock meant reinstall); a rejected code now names the box's
#   clock error instead of blaming the authenticator (new user.clockCheck; the ±300s
#   verification window is UNCHANGED); and notp is replaced by an in-house RFC 6238
#   implementation (its negative-substr truncation rejected ~1 in 21,475 valid codes).
#   DOES NOT FIX ALREADY-BRICKED BOXES: an account that enrolled through the broken
#   wizard has totp_enabled = TRUE with recovery codes that were never displayed. Those
#   need disableUserTotp / adminResetUserTotp, or a reinstall.
#   Cut as STABLE, make_latest=true.)
# PREVIOUS = refs/tags/v1.1.1 (STABLE, selector-fix ONLY — deliberately v1.1.0 plus
#   a single cherry-picked commit (a364db8b), NOT master. Fixes the beta-channel
#   selector for the SemVer migration: both selectors (update.ts pickMaxReleaseTag
#   and update.sh's sort -V pipeline) now accept STRICT 3-part vX.Y.Z[-pre] tags
#   only and DROP the legacy 2-part tags. Before this, coercion ranked the legacy
#   v45.31-beta.11 above the whole v1.x line, so a box switched to the Beta channel
#   was offered — and would have deployed — v45.31-beta.11: a silent multi-release
#   DOWNGRADE. The fix could not reach boxes through the beta channel itself (the
#   broken selector can never select the release that fixes it), which is why it
#   ships as a STABLE cut: stable resolution is /releases/latest + string compare
#   and is provably unaffected by the bug, so every box (and every fresh install,
#   whose install.sh hands off to update.sh) self-heals on the ordinary Update.
#   Cross-proven by update.beta.unit.test.ts (21 cases) + update.beta-selector.test.sh.
#   DELIBERATELY EXCLUDED: Phase 368.5 Safety Snapshots (present in the
#   v1.1.1-beta.1 prerelease and on master, NOT here) — default-ON hourly local
#   snapshots have not had their live-box UAT, the disk-pressure guard is
#   pre-flight-only (no mid-run/ENOSPC handling) and vm-data (VM disk images) is
#   not excluded from snapshot scope, so shipping it to every box today could fill
#   a small disk. It ships next as v1.2.0-beta.1 on the beta channel, which this
#   release makes safe to use. Cut as STABLE, make_latest=true.
#   NOTE 1.1.1 > 1.1.1-beta.1 in semver, so a box that later switches to Beta is
#   never downgraded by this ordering.)
# PREVIOUS = refs/tags/v1.1.0 (First SemVer MINOR release — Backups v2 Phase 368
#   BKPREANIMATE. New capability: interrupted-backup boot recovery (BKP-03) — boot
#   preflight unpauses app containers stranded paused by a crashed backup (wired
#   strictly BEFORE cleanDockerState, which would hang on a frozen container),
#   sweeps stale staging, and marks the interrupted run FAILED in the new
#   backups.lastRunStatus store record (compare-and-set terminal writes so
#   concurrent runs never clobber each other). Also carries two field fixes from
#   external beta testing: (a) update.sh no longer dies silently under
#   set -euo pipefail when livinity.yaml lacks a releaseChannel key (every fresh
#   stable box hit this right after "Pulling latest code"); (b) the update-failure
#   dialog strips ANSI escapes. Shipping this release un-gates v47 phases
#   314/315/321 (they waited on Backups-v2 P0). Cut as STABLE, make_latest=true.)
#   The pin line below names the exact tag being cut in this commit (pre-tag-prep
#   flow, step 1). release.yml verifies pin == tag and publishes. On a box the
#   warn-only path resolves it to the tag's commit and records a self-consistent
#   match. The NEXT release cut MUST bump this line again to its own tag before
#   tagging (see the mandatory procedure below); leaving a stale pin makes
#   release.yml refuse the next publish until it is bumped.
#
# MANDATORY PRE-TAG RELEASE-CUT PROCEDURE (self-consistency; never force-tag)
#   A git tag's tree is immutable once pushed, so the pin MUST already be correct
#   INSIDE the tagged commit — a post-tag "generate" step could never reach a box
#   cloning that tag. Therefore, before cutting any release tag:
#     (1) bump the pin line below to  refs/tags/<next-tag>  (the exact tag to cut);
#     (2) commit that change on master;
#     (3) THEN create and push the tag  (git tag <next-tag> ; git push origin <next-tag>).
#   This ordering guarantees the tagged commit's own tree carries a pin equal to its
#   own tag. Do NOT force-move a tag (git tag -f) — the pre-tag-prep flow makes it
#   unnecessary and it is explicitly disallowed. release.yml verifies this and
#   refuses to publish on any tag/pin mismatch.
refs/tags/v1.1.12
