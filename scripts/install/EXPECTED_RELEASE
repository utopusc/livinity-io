# scripts/install/EXPECTED_RELEASE — LivOS release pin (Phase 311-04, UPDSAFE-02)
#
# WHAT THIS IS
#   A single "pin line" naming the release this repo tree belongs to. update.sh's
#   livos_verify_fetched_ref() reads it out of the freshly-cloned release tree and
#   compares the resolved commit against the fetched HEAD.
#
# FORMAT (consumed by update.sh:  grep -vE '^\s*(#|$)' | head -1 | tr -d '[:space:]')
#   * Lines beginning with '#' and blank lines are ignored.
#   * The FIRST non-comment line is the pin.
#   * Use the FULL  refs/tags/<tag>  form. A refs/tags/<tag> line is resolved to a
#     commit via  git rev-parse "<tag>^{commit}"  INSIDE the cloned tree, then
#     compared to the fetched HEAD SHA. Because boxes clone a release BY that same
#     tag, HEAD == the tag's commit == what refs/tags/<tag> resolves to → a
#     self-consistent match.
#
# WARN-ONLY (Phase 311) — NOT enforcement
#   In Phase 311 a pin mismatch is WARN-ONLY: livos_verify_fetched_ref() logs a
#   SIGNATURE-WARN, records a signature_verification verdict into the update-history
#   JSON, and PROCEEDS — it never aborts an update. The fail-closed flip (refuse to
#   deploy on mismatch) is Phase 312, a strictly LATER release, gated on one full
#   release cycle of this warn-only telemetry. Do NOT add enforcement here or in the
#   verify function as part of Phase 311.
#
# CURRENT VALUE = refs/tags/v1.1.15-beta.1 (PRERELEASE — group 368.8b, the three things the
#   operator actually asked for at the start of this phase. 368.8a shipped as v1.1.14 and is
#   untouched by this; the fleet's stable line stays there until the UAT below passes.
#
#   368.8-06 — THE BUTTON LABELLED "CONFIGURE" OPENED THE SETUP WIZARD. settings-content.tsx
#   never imported BackupsConfigureWizard, so per-destination Last backup / Used / Available /
#   Total backups / Status / Back up now / Remove — AND the backup scope and exclusions
#   surfaces — could not be reached from desktop Settings at all.
#   ⚠ Deviation from the plan, and it was not optional: Configure's own "Add backup location"
#   entries navigated to /settings/backups/setup, the SAME dead route that made "Finish setup"
#   a 404 in 368.8-17. Making Configure reachable without fixing that would have re-introduced
#   the 404 one click deeper. The wizard now takes an optional onAddDestination callback.
#   ⚠ Found, NOT fixed: the mobile drawer's three /settings/backups/* links are dead for the
#   same reason. Desktop-only plan; tracked separately.
#
#   368.8-07 — THE SAFETY INTERVAL IS CONFIGURABLE. "saatlik mi yapıyor bunu ayarlayamıyor
#   muyuz" — it was hardcoded to an hour. Now 30 min / 1 hour (default) / 6 hours / daily,
#   persisted; absent field = 1 hour, so existing boxes need no migration.
#   The part that actually mattered: the old loop had ONE cursor and ONE interval, and the
#   tick is a FLOOR on resolution — offering "30 minutes" while waking hourly would have
#   silently meant 60. So the gate wakes on min(safetyInterval, backupInterval) and each class
#   carries its own cursor, with a per-repository gate so a safety-only wake does NOT speed up
#   USB/NAS. backupInterval itself is untouched.
#   ⚠ The six cadence tests exercise a MODEL of the scheduler; deleting the real gate would
#   leave them all green while USB/NAS quietly sped up — the exact failure recorded in
#   feedback_pure_module_tests_never_exercise_call_path. So there is ALSO a call-path test
#   pinning the real loop, proven to fail with the gate removed.
#
#   368.8-08 — THE CARD SAYS WHERE, WHEN, HOW LONG AND HOW BIG. "bu snapshotlar nereye". It
#   said only "Hourly local snapshots on the internal disk", which after 368.8-07 was also
#   FALSE — that string is gone from en and tr. Size is fetched lazily behind a Check button
#   because getRepositorySize shells out to kopia twice behind a concurrency:1 queue and blocks
#   during a running backup. Retention is described from the actual flags (--keep-hourly=24
#   --keep-daily=7). OP-02 held: the safety repo keeps its own card, is in no destination
#   count, and gains no delete affordance. OP-03 held: Migration is byte-identical.
#
#   ⚠ WHY A PRERELEASE. This changes a SCHEDULER LOOP. A unit test cannot prove that a box
#   actually takes a snapshot every 30 minutes, nor that a USB destination did not speed up —
#   only the journal on real hardware can (feedback_milestone_uat_gate). `bash update.sh`
#   clones the latest RELEASE TAG, so unreleased code cannot reach a box any other way. The
#   UAT greps two new lines:
#     Backups interval classes due: safety=<bool> user=<bool> (wake=<n>ms, …)
#     Safety snapshot interval set to <option> (<n>ms) by admin
#   Gates: livinityd tsc 385 (baseline 385); 5 pure backups suites 159/159 (the 2 integration
#   suites cannot collect on this host — native bindings — and gate nothing); vite build exit
#   0; UI vitest 68/1381, inside the 68<->69 baseline.)
# PREVIOUS = refs/tags/v1.1.14 (STABLE, make_latest=true — cut from master. Phase 368.8
#   BKPFIXUP in full. The fleet has been on v1.1.13 throughout; everything below is new to it.
#
#   ⚠ THIS CARRIES A LOGIN SECURITY FIX. 368.8-13: on every single-user DB-backed box — the
#   normal install shape — the login form sent no username, so the server took the legacy
#   single-owner YAML branch: it checked the password against the YAML credential instead of
#   the account's bcrypt hash, and evaluated 2FA through a store that is a hard false on a DB
#   box. That is a 2FA BYPASS AT LOGIN, not a UX defect. Anyone holding the legacy YAML
#   password reached an admin session without the second factor.
#
#   WHAT ELSE WAS BROKEN FOR EVERYONE, and how long:
#     · 368.8-01 "This device" as a backup destination could not succeed on ANY box (shipped
#       broken in v1.1.13 — the P0 this phase existed for).
#     · 368.8-10 /kopia was root-owned with no chown anywhere in the repo, so on a box whose
#       run-user is not root NO kopia repository had ever been created — USB, NAS, pool and
#       the 368.5 safety snapshots had all been failing silently since they shipped.
#     · 368.8-11 Settings has misreported 2FA as "Disabled" on every DB-backed box since
#       Phase 328, which also made enabling it impossible ("2FA is already enabled").
#     · 368.8-16 RESTORING A BACKUP HAD NEVER WORKED ANYWHERE — it bind-mounted, and
#       bind-mounting is root-only while livinityd is unprivileged (exit 32). Browsing a
#       backup was dead for the same reason.
#     · 368.8-19 the heartbeat could not read its own root-only key file, so boxes whose
#       run-user is not root had never sent a heartbeat at all.
#     · 368.8-18 one unused `CREATE EXTENSION vector` took four CREATE TABLEs down with it on
#       every box without pgvector, while the log called it non-fatal.
#     · 368.8-15 the dock island sat at 0% for entire backups (kopia renders progress only
#       onto a terminal; livinityd spawned it through a pipe, so nothing was ever parsed).
#     · 368.8-17 finishing the setup wizard ended on a 404 and never confirmed anything.
#     · 368.8-20 a refused reboot reported a SUCCESSFUL restore as failed.
#
#   PROVEN ON HARDWARE, not just green tests — the operator ran these on their own box:
#     · a full restore end to end (`Detected first start after backup restore`, Postgres and
#       Liv assistant data restored, 13 containers back up)
#     · three backup destinations plus the safety repo completing runs
#     · the island percentage moving
#     · mastra_* tables existing for the first time (4/4)
#     · the heartbeat actually POSTing
#     · Settings reading the truth about 2FA — the last gate, cleared 2026-07-27
#
#   ⚠ KNOWN AND DELIBERATELY NOT FIXED HERE (each needs its own phase):
#     · A restore still needs a MANUAL RESTART at the end. `reboot` is refused for a
#       session-less service; the polkit/sudoers grant belongs in the installer and would
#       land one release late. 368.8-20 makes the UI say so instead of lying.
#     · createContextExpress still never populates currentUser, so any publicProcedure that
#       reads it can carry 368.8-11's bug class. Fixing it centrally changes isLoggedIn
#       (which SIGNS A TOKEN), webauthnLoginOptions and loginWithPasskey — own phase plus a
#       security audit.
#     · files.ts:224 hardcodes uid 1000; on a box whose run-user differs the Files subsystem
#       may silently never start.
#
#   Gates: livinityd tsc 385 (baseline 385); engine 24/24, api-key 17/17, migrate 4/4, with
#   every new guard proven to FAIL against the unfixed code first; vite build exit 0; UI
#   vitest 68/1381, inside the 68<->69 baseline.)
# PREVIOUS = refs/tags/v1.1.14-beta.7 (PRERELEASE — beta.6's restore fix is PROVEN:
#   the operator ran a full restore on their box and it completed end to end. Boot log:
#     [migration] Found Livinity data to import, activating...
#     [livinityd] Detected first start after backup restore
#     [livinityd] restored livos Postgres DB from snapshot (Liv memory + app instances…)
#     [livinityd] restored /opt/liv-assistant/data from snapshot
#   13 containers back up. An hour earlier the same flow died on `mount --bind`. The island
#   percentage also moves, so 368.8-15 and 368.8-16 are both confirmed in the field.
#
#   That restore then surfaced three more defects, and ALL THREE were things the box had
#   been doing wrong quietly for a long time — none were caused by the restore.
#
#   368.8-20 — A REFUSED REBOOT REPORTED A SUCCESSFUL RESTORE AS A FAILURE. The restore had
#   already finished (data copied, moved into data/import, first-start flag written) when:
#     Error: Command failed with exit code 1: reboot
#     Call to Reboot failed: Interactive authentication required.
#   system.ts runs `reboot` bare; livinityd is unprivileged with no session, so polkit
#   refuses. The throw escaped restoreBackup and the UI said "Restore failed" over a restore
#   that was one restart from done. That is the worst form of the quiet lie this phase keeps
#   removing — it invites the operator to run the whole thing AGAIN over correct data. The
#   reboot is now guarded and reported honestly: "Restore complete — restart this device to
#   finish". ⚠ The privilege grant itself (polkit rule / sudoers entry) belongs in the
#   installer and would land one release late, so it is NOT in this build: a restore still
#   needs a manual restart at the end. The message now says so.
#
#   368.8-19 — THE HEARTBEAT NEVER SENT ANYTHING ON THIS BOX. The journal claimed
#   "API key unavailable (file missing/empty/malformed)"; the file was none of those.
#   /etc/livos/secrets/api-key is root:root 0600 and the run-user is `everything` (uid 1001)
#   → `test -r` = NO. It was UNREADABLE, which readApiKey's single null return could not
#   distinguish. FIFTH instance of the archetype in this phase. The key was never missing:
#   install.sh already writes the identical value into Redis for the App Store — sha256 of
#   both matched exactly. readApiKey now falls back to Redis when the file cannot be read,
#   tries the file FIRST so readable boxes are unchanged, reports which source answered, and
#   refuses a value that is not a liv_k_ key. No new secret surface: that value is already in
#   Redis in plaintext.
#
#   368.8-18 — ONE UNUSED EXTENSION KILLED A WHOLE MIGRATION. `extension "vector" is not
#   available` on a host Postgres with no pgvector. The .sql file runs as ONE query, so the
#   failing FIRST statement took all four CREATE TABLEs with it, while the caller logged it
#   as "non-fatal". Checked before deleting: no column in that file uses the vector type, and
#   the only `vector` in the wider schema is `content_tsv TSVECTOR` — Postgres's built-in
#   full-text type, unrelated to pgvector. The extension gated tables that never needed it.
#
#   Gates: livinityd tsc 385 (baseline 385); engine.test.ts 24/24, api-key.test.ts 17/17,
#   migrate.test.ts 4/4, with every new guard proven to FAIL against the unfixed code first.
#   ⚠ Still unverified in a browser: 368.8-11's 2FA fix — the one remaining gate on stable.)
# PREVIOUS = refs/tags/v1.1.14-beta.6 (PRERELEASE — beta.5 was called the last beta.
#   It was not: putting it in front of the operator immediately surfaced that RESTORING A
#   BACKUP HAS NEVER WORKED ON ANY BOX. beta.5's own change is confirmed good — the island
#   percentage moves in the field, so 368.8-15 is proven.
#
#   368.8-16 — RESTORE WAS IMPOSSIBLE, FLEET-WIDE. Verbatim from the box:
#     Command failed with exit code 32: mount --bind
#       /opt/livos/data/backup-mounts/<t>/home /opt/livos/data/backups/<t>/Home
#     mount: …/Home: must be superuser to use mount.
#   livinityd is unprivileged and bind-mounting is root-only, so this code could never
#   have run anywhere. It is the SAME ARCHETYPE as /kopia and /opt/livos-backups earlier
#   in this phase — a privileged operation on an unprivileged service's critical path —
#   which now makes three in one phase.
#
#   Measured on the box AS THE RUN-USER instead of reasoned about: `mount --bind` → exit
#   32; `kopia mount <snapshot>/home` → mounts fine; `umount` on that FUSE mount → rc 0,
#   because it belongs to the user that made it. So the two bind mounts become two
#   ordinary FUSE mounts — kopia addresses a snapshot subdirectory the same way
#   listBackupFiles already does for `kopia ls`. No sudoers entry, no privileged helper.
#
#   ⚠ A SECOND, INDEPENDENT DEFECT, and the reason a restore DIED rather than merely
#   losing its file browser: restoreBackup reads ONLY the internal mountpoint, yet it
#   awaited mountBackup, which also stood up the browse-only mounts. A failure in a layer
#   the restore never reads took the whole restore down. mountBackup now accepts
#   {virtualFilesystem: false} and restoreBackup passes it, so the two are independent.
#
#   368.8-17 — FINISHING SETUP ENDED ON A 404. The wizard closed itself by navigating to
#   /settings/backups/configure, which stopped existing when Settings became window-only
#   (router.tsx states it outright). Three call sites did it. Nothing underneath was
#   broken — the destination was created and the first backup was already running — but
#   the last thing the operator saw was an error page instead of a confirmation. There is
#   now a Done step, reachable only from a successful submit, claiming only what is true
#   at that moment. Plus two things from the same screenshot: Review printed
#   "This device · System disk · System disk" (the folder defaults to the disk's own
#   name), and the "This device" panel now names the chosen disk with its free space,
#   previews the destination path live as it is typed, and raises the same-disk caveat
#   from tertiary grey to an amber panel — it is that destination's one real limitation.
#
#   ⚠ STILL UNVERIFIED IN A BROWSER — and now there are two such claims, which is why
#   this is again not the stable: 368.8-11's 2FA fix, and the restore path above. The
#   restore fix in particular has NO automated end-to-end coverage; the box is the only
#   evidence. Gates prove only non-regression: livinityd tsc 385 (baseline 385),
#   engine.test.ts 23/23 with every new guard proven to FAIL against the unfixed code
#   first, vite build exit 0, UI vitest 68/1381 inside the 68<->69 baseline.)
# PREVIOUS = refs/tags/v1.1.14-beta.5 (PRERELEASE — the last beta before the v1.1.14
#   stable cut. Two changes, both about the dock island, both cosmetic in the sense that
#   no backup succeeds or fails differently because of them — and both about the island
#   telling the truth, which is what the rest of this phase has been.
#
#   368.8-15 — THE ISLAND SAT AT 0% FOR THE WHOLE BACKUP. The percentage never moved,
#   on any run, on any destination. The parser was never wrong: measured on the
#   operator's box across whole multi-minute backups, journal lines containing
#   `estimated` = 0 and containing `hashing` = 0. Nothing was arriving to parse. kopia
#   renders its progress line ONLY when its output is a terminal, and livinityd spawns
#   it through a pipe (execa, stdout+stderr piped).
#
#   Reproduced in isolation on that box rather than reasoned about: a 1.5 GB snapshot
#   piped emits zero progress lines; the identical command under script(1) emits frames
#   within a second. No flag forces it — `--progress` is already the default and does
#   nothing here, and 0.23.1 has no `--progress-interval` at the snapshot level (checked
#   against the box's own --help, not assumed). So the terminal has to be real, and
#   util-linux script(1) supplies one without adding a native pty dependency.
#
#   SCOPE IS ONE CALL, deliberately. Only `snapshot create` asks for a pty. Everything
#   else stays on a plain pipe, because a pty interleaves progress frames into stdout
#   and several calls parse `--json` off it — getRepositorySize reads
#   `repository status --json` and would begin throwing on JSON.parse. A test asserts
#   there is exactly ONE pty request in backups.ts, so neither half can drift.
#
#   ⚠ THE TWO THINGS THIS HAD TO GET RIGHT. (a) Quoting: `script -qec` takes ONE string
#   that a shell then parses, and the repository path carries an operator-typed folder
#   name straight from the setup wizard — so an unquoted argument would have been shell
#   injection reachable from the UI. Every argument is single-quoted with the POSIX
#   escape, and that is asserted directly. (b) Absence: a box without script(1) loses
#   the percentage and keeps its backup. Losing a backup to gain a number would be a bad
#   trade. The percentage also stays ABSENT rather than 0 while kopia is still
#   estimating, which on a first snapshot lasts a while — a 0 that means "no news" is
#   the same quiet lie this phase has spent itself removing, and 368.8-14's pulse
#   already carries "working" through that window.
#
#   368.8-14 — the island now carries LivOS's own identity: the Livinity donut glyph
#   drawn from currentColor (so contrast is right in both themes by construction, not by
#   coincidence), type on the brand's numeric scale, and the mark breathing while a
#   backup runs. Committed before beta.4 was cut but not carried by it, so this is its
#   first build on a box.
#
#   ⚠ STILL UNVERIFIED IN A BROWSER, and it is why this beta exists rather than the
#   stable: 368.8-11's 2FA fix (Settings must read the truth and a fresh enrol must be
#   accepted) has never been seen running. Gates prove only that nothing regressed:
#   livinityd tsc 385 (baseline 385), engine.test.ts 21/21 with the pty and quoting
#   tests each proven to FAIL against the unfixed code before being kept.)
# PREVIOUS = refs/tags/v1.1.14-beta.4 (PRERELEASE — 368.8-13, a LOGIN fix, and the
#   most serious defect this phase has surfaced.
#
#   Reported from the field: password rejected, and after the password no 2FA prompt —
#   on a box whose Settings correctly read "Enabled" and whose Postgres row had
#   totp_enabled=t.
#
#   CAUSE. `setSelectedUser` is called from one place only: UserSelectStep's onSelect.
#   A single-user box never renders UserSelectStep (login/index.tsx:199 goes straight to
#   PasswordStep), so `selectedUser` stayed null and the submit posted {password} with NO
#   username. `user.login` gates DB auth on that field (user/routes.ts:189). Without it
#   the server took the legacy single-owner YAML branch (:231-246), which checked the
#   password against the YAML credential instead of the account's bcrypt hash, and
#   evaluated 2FA through ctx.user.is2faEnabled() — the YAML store, a hard false on a
#   DB-backed box.
#
#   ⚠ So this was a 2FA BYPASS AT LOGIN, not only a UX defect: whoever held the legacy
#   YAML password reached an admin session without the second factor, on every
#   single-user DB box — which is the normal install shape. It also explains the
#   rejected password: the account's real one was never the one being checked.
#
#   FIX: derive the target account rather than read state —
#   `effectiveUser = selectedUser ?? (users.length === 1 ? users[0] : null)` — used by
#   both the password and the TOTP submit. Multi-user is untouched. The bare {password}
#   call survives only for the zero-user case, where the YAML branch is correct.
#
#   NOT caused by 368.8-11: that commit touched only the is2faEnabled procedure
#   (`git show --stat`), and login/index.tsx had been untouched since d717aba9. 368.8-11
#   simply made Settings tell the truth, which is how the operator noticed.
#
#   Gates: vite build exit 0. UI vitest measured on BOTH sides by stashing the one file:
#   69 failing before, 69 after, and the failing-test SETS are identical once per-test
#   timings are stripped. The baseline genuinely oscillates 68<->69; this run's was 69.
#   ⚠ Still unproven in a browser — same caveat as beta.3.)
# PREVIOUS = refs/tags/v1.1.14-beta.3 (PRERELEASE — the 368.8a P0 is CONFIRMED FIXED
#   in the field on beta.2; this build adds two findings the operator reported while
#   testing it. Cut so an AUTH change is seen running once before it reaches the fleet.
#
#   ✅ P0 PROVEN ON HARDWARE (beta.2, operator's box, journal verbatim):
#     Accepted backup destination /ThisDevice/System disk/… -> /opt/livos/backups-internal/…
#     Creating repository 190aa42e → Connected → Creating snapshot
#     Clearing notification: backups-failing:190aa42e
#     Backed up … size after backup: Used 3.22 GB of 966 GB
#   Route C held end to end: livinityd created /opt/livos/backups-internal AND
#   /opt/livos/kopia unaided, no installer, one update. As a bonus the 368.5 safety
#   repo came alive for the first time on that box (3.1 GB, local-safety.config now
#   exists) — before 368.8-10 no kopia repository had EVER been created there, so
#   USB / NAS / pool / safety were all silently broken too.
#
#   368.8-11 — THE 2FA SETTINGS LIE, and it is fleet-wide, not box-specific.
#   Operator: "2fa çalışmıyor, enable diyorum, QR okutup kodu giriyorum, kabul etmiyor."
#   The code was never wrong. Settings showed 2FA as Disabled while the DB had
#   totp_enabled=t, so enable2fa threw "2FA is already enabled" (routes.ts:426-431)
#   before verifying anything. Cause: `is2faEnabled` was a publicProcedure whose answer
#   depends on ctx.currentUser, but currentUser is only ever populated by the
#   isAuthenticated middleware — which never runs for a publicProcedure — and the
#   procedure is in httpOnlyPaths (common.ts:92), so EVERY UI call rides HTTP where
#   createContextExpress does not set it. It therefore always answered from the legacy
#   single-owner YAML store, which returns a hard false on a DB box. **Settings has
#   misreported 2FA as "Disabled" on every DB-backed box since Phase 328/IDENT-05.**
#   Fix: privateProcedure, plus the is2faEnabled read moved OUT of the use2fa hook into
#   settings/2fa.tsx — onboarding's account-step.tsx mounts that hook while still
#   unauthenticated, so leaving the query in it would have logged four spurious
#   UNAUTHORIZED per fresh-box install. The legacy no-DB YAML branch is untouched.
#   ⚠ DELIBERATELY NOT FIXED HERE: createContextExpress still never populates
#   currentUser, so the same class of bug can bite any other publicProcedure that reads
#   it. Fixing it centrally changes behaviour for isLoggedIn (routes.ts:321, a
#   publicProcedure that SIGNS A TOKEN at :343-344), webauthnLoginOptions (:624) and
#   loginWithPasskey (:657). That blast radius needs its own phase and a security audit.
#
#   368.8-12 — the floating island now carries LivOS's own visual identity. Its radii
#   moved onto the semantic scale in tailwind.config.ts:100-106 (minimized 22 → 20
#   radius-xl, which a 40px-tall box already clamped to anyway; expanded 32 → 28
#   radius-3xl). It gained a wallpaper-derived brand tint, applied as backgroundImage
#   rather than backgroundColor because --surface-3 is rgba(0,0,0,0.12) and overwriting
#   background-color would have dropped that panel and left a 6% wash over the bare
#   wallpaper. The colour comes from --color-brand, which wallpaper.tsx:220-222 sets at
#   runtime, so the island follows whatever wallpaper the operator has chosen — the same
#   token the backups island's own progress gradient already used. Backdrop blur was
#   added because at 12% opacity the island showed the wallpaper through it unblurred,
#   which cost legibility on busy images. No dimension changed, so the four Files
#   islands mounted on the same component keep one silhouette.
#
#   ⚠ WHY A THIRD BETA. 368.8-11 is an AUTH change with ZERO real-browser verification:
#   tsc 385, user 33/33, stepup 19/19, backups 142/142 and an unchanged UI vitest
#   baseline prove only that nothing regressed. Two claims need the box before stable:
#   Settings must read "Enabled" for a user whose DB row says so, and a fresh-box
#   onboarding must log NO "Failed to verify token" from this route. Shipping an auth
#   patch to every box without seeing it run once is precisely the failure mode this
#   whole phase exists to remove.
#   ⚠ STILL LIVE, NOT FIXED: files.ts:224 hardcodes uid 1000. The operator's run-user is
#   `everything`, uid 1001, so the Files subsystem may silently never start there.
#   Deferred 368.6 companion defect, tracked separately.)
# PREVIOUS = refs/tags/v1.1.14-beta.2 (PRERELEASE — 368.8-10, the SECOND wall the
#   beta.1 UAT uncovered. beta.1 worked: it got the wizard all the way to
#   `kopia repository create --path=/opt/livos/backups-internal/<folder>/…`, which proves
#   livinityd created the root unaided, the pre-flight found it writable, and the probe
#   accepted a not-yet-existing leaf. Route C is confirmed in the field.
#
#   Then it died on the SAME DEFECT CLASS one layer down:
#     Unable to create logs directory: mkdir /kopia/cache/kopia: permission denied
#     unable to write config file: … /kopia/config/….config…: permission denied
#
#   /kopia/{config,cache} were created ONLY by root (update.sh:1887,
#   deploy-livinityd.sh:225) and NOTHING in this repo ever chowned them. livinityd runs
#   unprivileged and spawns kopia with a plain execa pointing XDG_CACHE_HOME /
#   XDG_CONFIG_HOME at those root-owned directories; engine.ts's mkdir could never
#   succeed because /kopia sits at the filesystem root, and its .catch(() => {})
#   swallowed the EACCES. Measured on the operator's box (run-user `everything`, uid
#   1001 — not bruce, not 1000): all three root:root, and **/kopia/config EMPTY, proving
#   no kopia repository had ever been created there.** USB, NAS, pool and the 368.5
#   safety snapshots had all been failing on that box since they shipped. The existing
#   sudo grant cannot repair it — sudoers.d/livinityd:30 hardcodes uid 1000.
#
#   FIX = Route C a second time. kopia's state moved to /opt/livos/kopia/{config,cache},
#   inside the tree update.sh:4568 already chowns to the DERIVED run-user; livinityd
#   creates it itself via Backups#ensureKopiaStateDirs, awaited inside kopia() — the one
#   choke point every spawn passes through, so it also covers boxes whose kopia came from
#   update.sh and never ran livinityd's self-install. Memoised on success, forgotten on
#   failure, so a box that gains the permission mid-life repairs itself. Any pre-move
#   *.config is COPIED across once (never moved, never fatal) so a box where it did work
#   cannot be silently orphaned. Both installer mkdirs are gone, and $LIVOS_DIR/kopia
#   joins 368.8-02's pruned chown walks — the content cache is an unbounded blob tree.
#   Verified, not assumed: every rsync --delete targets a package subdirectory, never
#   $LIVOS_DIR, so /opt/livos/kopia survives updates like backups-local and data do; and
#   it is outside dataDirectory, so outside the snapshot source and the restore-wipe bound.
#
#   ⚠ STILL NO automated test covers the fix end to end — 142/142 pure unit tests and
#   tsc 385 prove only that nothing regressed. The box UAT is the ONLY evidence. Do not
#   promote to stable without it.
#   ⚠ NOT fixed here and LIVE on that box: files.ts:224 hardcodes uid 1000, so the Files
#   subsystem silently never starts on a uid≠1000 box. Deferred 368.6 companion defect.)
# PREVIOUS = refs/tags/v1.1.14-beta.1 (PRERELEASE — Phase 368.8a BKPFIXUP, the UAT
#   build for the P0 that v1.1.13 shipped broken: the "This device" backup destination
#   could not succeed on ANY box. Selecting it, naming a folder and pressing Continue
#   always failed with "LivOS couldn't resolve that location."
#
#   ROOT CAUSE (proven, not hypothesised). probeDestination realpath'd
#   <root>/<operator-typed-folder> — a path that by construction cannot pre-exist,
#   because the folder name is per-repository user input. The mkdir that would have
#   created it lived 18 lines later, inside `if (createNew)`. Worse, the root itself
#   (/opt/livos-backups, a SIBLING of /opt/livos) was created by nothing at all: the
#   368.6 installer one-liner was filed as a follow-up and never landed, and livinityd
#   runs unprivileged so it could not have created a root under root-owned /opt anyway.
#   60+ policy tests were green throughout, because the suite injected a fake realpath
#   that could never return null. That is the defect this build exists to correct.
#
#   ROUTE C (operator decision 2026-07-25) — WHY THE ROOT MOVED. The obvious fix, an
#   installer mkdir+chown, CANNOT heal an existing box on the update that ships it:
#   update.sh self-replaces via atomic mv (:1884-1904) and never re-execs the fresh
#   clone — the hazard already named in-tree at :4429-4435, which cost v45.05→v45.06 a
#   no-op re-ship. So INTERNAL_BACKUP_ROOT moved to /opt/livos/backups-internal, INSIDE
#   a tree update.sh already chowns to the run-user. livinityd now creates the root
#   itself via ensureInternalBackupRoot(), exactly as it already creates the 368.5
#   SAFETY_REPO_PATH, and its source is rsynced + the service restarted in the SAME
#   update run. No installer is on the critical path, and a box heals on the FIRST
#   update. INSTALLER-01 was withdrawn; no *.sh creates this root.
#   ⚠ Do NOT "restore" the /opt/livos-backups sibling — it is undeliverable by
#   construction. The rationale is recorded at destination-policy.ts alongside the
#   constant so a future reader cannot walk back into it.
#
#   ALSO IN THIS BUILD. The wizard no longer offers what the server will refuse:
#   getDestinationRoots pre-flights the root's existence, its writability (reusing the
#   SAME canWrite proof the probe uses, so wizard and addRepository can never disagree)
#   and the 15% free-space floor, and the "This device" row renders DISABLED with a
#   reason — before a folder name or a password is typed. COR-05's floor is now named
#   before the password step rather than after it. And update.sh's two recursive chown
#   walks over /opt/livos prune both backup roots, so an update never re-owns a kopia
#   blob tree; that one is a PERFORMANCE fix and, being inline in update.sh, it lands
#   one update late — harmless, since the single unpruned walk runs over an empty tree.
#
#   WHY A PRERELEASE. `bash /opt/livos/update.sh` clones the latest RELEASE TAG
#   (update.sh:1461-1479), so unreleased code cannot reach a box any other way. This
#   tag exists so 368.8-04's box UAT tests the FIX rather than re-deploying v1.1.13.
#   Tag carries "-" ⇒ release.yml publishes prerelease + NOT latest, so stable boxes
#   never receive it. The stable cut is v1.1.14 (368.8-05), gated on that UAT.
#   ⚠ A box must be on v1.1.1+ before being put on the Beta channel — a pre-v1.1.1
#   selector would still pick the legacy v45.x tag. The UAT box is on v1.1.13, so it
#   is safe; switch it back to stable when the UAT ends.
#   ⚠ NO automated test covers the fix end to end: 140/140 pure unit tests and a
#   zero-delta UI vitest prove only that nothing regressed. The box UAT is the ONLY
#   evidence that "This device" works. Do not promote to stable without it.)
# PREVIOUS = refs/tags/v1.1.13 (STABLE — cut from master, the first release since
#   v1.1.0 that is NOT a narrow cherry-pick. It carries everything: Phase 368
#   BKPREANIMATE, 368.5 Safety Snapshots WITH ITS STABLE GATE NOW CLOSED, 368.6
#   BKPANYDEST, and the 368.7 first-run 2FA unblock already shipped as v1.1.12.
#
#   WHY 368.5 MAY NOW SHIP TO EVERY BOX. The gate recorded below on v1.1.11-beta.1
#   had two structural causes, both closed in `2d8302be`:
#     (a) Snapshot scope. VM disk images (${dataDirectory}/vm-data) are tens of GB
#         and every VM boot rewrites blocks throughout them — the single worst
#         thing to snapshot hourly onto the same disk. Now excluded BY DEFAULT via
#         a visible, reversible toggle beside the existing scope switches (never a
#         hidden rule: silently dropping someone's VM from their backups would be
#         the exact dishonesty this milestone keeps removing). Browser-profile
#         caches under chrome-master are excluded unconditionally — pure
#         regenerable bulk, while Cookies/Login Data/Preferences stay in. Those
#         patterns are ANCHORED to the profile root; a depth-agnostic `Cache/`
#         would have matched a folder a user named "Cache" among their own files.
#     (b) Pre-flight-only disk protection. A snapshot beginning at 16% free could
#         still write the disk to zero on the way through. A watchdog now polls
#         during any snapshot written to the system disk and kills kopia below 5%
#         free. Safe: kopia commits atomically, so the repo keeps its previous
#         snapshots and simply gains no new one; the run is marked failed and the
#         >24h alert fires. The 5% floor is deliberately BELOW the 15% pre-flight
#         floor (crossing 15% mid-run is normal for a large first snapshot), and
#         its failure mode is deliberately the OPPOSITE: an unreadable probe
#         mid-run does NOT abort, because the pre-flight already proved there was
#         room and aborting on a transient df hiccup would leave a flaky box never
#         completing a backup.
#   Operator decision 2026-07-25: ship stable now rather than measure growth on
#   beta first, since both structural causes are gone and the residual risk is
#   bounded by the 5% floor.
#
#   ALSO NEW — Phase 368.6 BKPANYDEST answers "a box with no USB drive and no NAS
#   has nowhere to back up to": the storage pool and a NAMED folder on the system
#   disk are now destinations, behind a new "This device" wizard tab. Honesty is
#   enforced server-side — a same-disk destination can never show green and never
#   silences the "add a drive or a NAS" reminder, though it does back up. Two live
#   bugs died on the way: a repository created against an EMPTY /External (no
#   drive plugged in) used to land on the OS disk inside the snapshot source,
#   count as a real destination, and be deleted by the restore it served; and
#   isRepoConnected reported any unrecognised root disconnected, which four call
#   sites gate on — an internal destination would have been non-restorable.
#   Cut as STABLE, make_latest=true.)
# PREVIOUS = refs/tags/v1.1.12 (STABLE — Phase 368.7, the first-run 2FA dead end.
#   Cut from v1.1.1 + four commits, deliberately without 368.5 because this gate
#   was still open at the time.)
# PREVIOUS = refs/tags/v1.1.11-beta.1 (PRERELEASE — Backups v2 Phase 368.5 Safety
#   Snapshots, default ON: system-managed local kopia repo on the internal disk
#   (/opt/livos/backups-local, OUTSIDE dataDirectory), hourly via the existing
#   interval, per-repo aggressive thinning that cannot leak onto USB/SMB repos,
#   15%-free disk-pressure guard, honest AMBER card; the safety repo is excluded
#   from every "has destinations" count (7 surfaces) so the add-a-real-destination
#   nag never goes silent, plus an admin opt-out.
#   VERSIONING (operator decision 2026-07-25): the release line stays on 1.1.x and
#   increments the PATCH digit (…1.1.11, 1.1.12, …) rather than taking the SemVer
#   MINOR bump a new capability would normally imply. A previously-published
#   v1.2.0-beta.1 carrying this exact content was DELETED (tag + release) because
#   1.2.0-beta.1 > 1.1.11 would have permanently shadowed the 1.1.x line in the
#   beta selector — the same class of wrong-version-selected bug v1.1.1 just fixed.
#   Tag carries "-" ⇒ release.yml publishes prerelease + NOT latest, so stable
#   boxes never receive it automatically.
#   BETA CHANNEL IS NOW SAFE: the strict-semver selector shipped in v1.1.1
#   (stable), so a box on v1.1.1+ resolves this correctly instead of the legacy
#   v45.31-beta.11. A box still running a PRE-v1.1.1 update.sh must NOT be put on
#   the Beta channel — its old selector would still pick the legacy tag.
#   ⚠ BEFORE ANY STABLE PROMOTION OF THIS CONTENT (v1.2.0): snapshot scope is the
#   WHOLE dataDirectory and does NOT exclude vm-data (VM disk images) or Chrome
#   profile caches, and the disk guard is pre-flight-only (no mid-run/ENOSPC
#   handling). Measure safety-repo growth on a real box (T+0 / +6h / +48h) and
#   add the exclusions before this reaches every box on stable.
#   Previous line: v1.1.1 notes below.)
# PREVIOUS = refs/tags/v1.1.1 (STABLE, selector-fix ONLY — cut from release/v1.1.1
#   = v1.1.0 + cherry-pick a364db8b, deliberately WITHOUT 368.5. Fixed the beta
#   channel offering legacy v45.x tags — a silent multi-release downgrade — which
#   could not be fixed via beta itself because the broken selector can never
#   select its own fix.)
# PREVIOUS = refs/tags/v1.1.0 (First SemVer MINOR release — Backups v2 Phase 368
#   BKPREANIMATE. New capability: interrupted-backup boot recovery (BKP-03) — boot
#   preflight unpauses app containers stranded paused by a crashed backup (wired
#   strictly BEFORE cleanDockerState, which would hang on a frozen container),
#   sweeps stale staging, and marks the interrupted run FAILED in the new
#   backups.lastRunStatus store record (compare-and-set terminal writes so
#   concurrent runs never clobber each other). Also carries two field fixes from
#   external beta testing: (a) update.sh no longer dies silently under
#   set -euo pipefail when livinity.yaml lacks a releaseChannel key (every fresh
#   stable box hit this right after "Pulling latest code"); (b) the update-failure
#   dialog strips ANSI escapes. Shipping this release un-gates v47 phases
#   314/315/321 (they waited on Backups-v2 P0). Cut as STABLE, make_latest=true.)
#   The pin line below names the exact tag being cut in this commit (pre-tag-prep
#   flow, step 1). release.yml verifies pin == tag and publishes. On a box the
#   warn-only path resolves it to the tag's commit and records a self-consistent
#   match. The NEXT release cut MUST bump this line again to its own tag before
#   tagging (see the mandatory procedure below); leaving a stale pin makes
#   release.yml refuse the next publish until it is bumped.
#
# MANDATORY PRE-TAG RELEASE-CUT PROCEDURE (self-consistency; never force-tag)
#   A git tag's tree is immutable once pushed, so the pin MUST already be correct
#   INSIDE the tagged commit — a post-tag "generate" step could never reach a box
#   cloning that tag. Therefore, before cutting any release tag:
#     (1) bump the pin line below to  refs/tags/<next-tag>  (the exact tag to cut);
#     (2) commit that change on master;
#     (3) THEN create and push the tag  (git tag <next-tag> ; git push origin <next-tag>).
#   This ordering guarantees the tagged commit's own tree carries a pin equal to its
#   own tag. Do NOT force-move a tag (git tag -f) — the pre-tag-prep flow makes it
#   unnecessary and it is explicitly disallowed. release.yml verifies this and
#   refuses to publish on any tag/pin mismatch.
refs/tags/v1.1.15-beta.1
