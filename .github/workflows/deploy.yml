# Server4 deploy disabled per project HARD RULE 2026-04-27 — see MEMORY.md
#
# This workflow previously deployed to a legacy server via the
# `git pull + pm2 restart` flow targeting /opt/livos/nexus and /opt/nexus/.
# Per HARD RULE 2026-04-27 (user-confirmed: "We have nothing to do with
# Server 4"), Server4 is OFF-LIMITS for LivOS work. The Mini PC
# (bruce@10.69.31.68) is the user's only LivOS deployment that matters,
# and it is updated via /opt/livos/update.sh — NOT via this workflow.
#
# Deploy logic intentionally deleted, not renamed to /opt/liv/. Re-enabling
# any deploy from this workflow requires explicit user authorization that
# overrides the HARD RULE.

name: Deploy to Server (DISABLED)

on:
  workflow_dispatch:

jobs:
  deploy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Server4 deploy disabled per HARD RULE 2026-04-27
        run: |
          echo "This workflow is disabled per project HARD RULE 2026-04-27."
          echo "Mini PC deployment uses /opt/livos/update.sh (not this workflow)."
          echo "See MEMORY.md for context."
          exit 0
